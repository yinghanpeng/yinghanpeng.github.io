<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>claude code源码之记忆系统设计</title>
    <url>/2026/07/22/claude-code-memory-system/</url>
    <content><![CDATA[<h2 id="指令记忆-claude-md-多层规则">指令记忆（CLAUDE.md 多层规则）</h2>
<ul>
<li>四层优先级（全局 managed → 用户 home → 项目 CLAUDE.md → 本地 claude.local.md）、就近文件优先级更高、<code>@include</code> 递归 5 层 + 循环检测、path 条件匹配 glob 规则、子目录嵌套规则自动加载全部属实。</li>
<li>双轨注入（CLAUDE.md 走 user 消息、系统行为规范独立 system prompt）是源码核心缓存设计，完全正确。</li>
</ul>
<h2 id="短期记忆">短期记忆</h2>
<p>当前会话完整未压缩对话存在内存，会话销毁即清空，符合上下文窗口基础机制。</p>
<h2 id="工作记忆">工作记忆</h2>
<p>任务进度、偏移、流式投机响应状态等运行时临时变量，源码 QueryEngine 内置该内存区。</p>
<h2 id="长期记忆-auto-memory-meder-系统">长期记忆（Auto Memory/meder 系统）</h2>
<h3 id="存储路径">存储路径</h3>
<p><code>~/.claude/projects/</code>，基于 git 根目录标准化命名，worktree 共享同一份记忆目录，正确。</p>
<h3 id="四类存储类型">四类存储类型</h3>
<ul>
<li>User：用户画像</li>
<li>Feedback：正负反馈</li>
<li>Project：项目约定</li>
<li>References：外部链接</li>
</ul>
<p>严格禁止存储可从代码推导的内容（架构、文件路径、git 记录）是源码硬性规则。</p>
<h3 id="双层存储">双层存储</h3>
<p><code>MEMORY.md</code> 索引（200 行 / 25KB 上限）+ 分类 md 记忆文件，front matter 元数据，完全匹配。</p>
<h3 id="异步预取">异步预取</h3>
<p>MemSearch 旁路轻量模型筛选、最多召回 5 条、过滤重复 / 近期工具文档、自然语言时间提示 + 过期警告，全部属实。</p>
<h3 id="每轮会话提取">每轮会话提取</h3>
<p>每轮会话后台 fork 子代理 <code>extractMemories</code> 提取记忆、主代理手动写记忆时子代理跳过避免冲突、子代理读写分离两回合策略、工具权限沙箱限制，源码完全实现。</p>
<h2 id="摘要记忆-session-memory">摘要记忆（Session Memory）</h2>
<p>不等到上下文爆 Token 再临时生成摘要，全程后台维护 <code>sessionmemory.md</code> 会话笔记，压缩时直接复用；压缩边界算法保证 tool-use/tool result 成对完整、不破坏流式 thinking 片段，是源码解决 API 报错的核心方案，描述精准。</p>
<h2 id="休眠重塑记忆-autodream-自动做梦">休眠重塑记忆（AutoDream 自动做梦）</h2>
<h3 id="双重触发门控">双重触发门控</h3>
<p>间隔 24h + 累计 5 次新会话，缺一不启动。</p>
<h3 id="四阶段流程">四阶段流程</h3>
<p>Orient → Gather → Consolidate → Prune Index、相对日期转绝对、直接删除矛盾过时记忆、<code>MEMORY.md</code> 裁剪约束。</p>
<h3 id="多进程锁机制">多进程锁机制</h3>
<p>PID CAS 校验、失败回滚文件时间戳，和源码 <code>autoDream.ts</code> 逻辑 1:1 对应。</p>
]]></content>
  </entry>
  <entry>
    <title>在外网发现的一篇很好的文章- AI ENGINEERING</title>
    <url>/2026/07/15/ai-engineering-pdf-note/</url>
    <content><![CDATA[<h2 id="attachment">附件说明</h2>
      <p>在外网发现了一份很不错的 <strong>AI Engineering</strong> 资料，这里先存一份到博客里，方便后面直接打开查看。</p>
      <p>附件文件：<a href="/files/pdf/ai-engineering-guidebook.pdf" target="_blank" rel="noopener noreferrer">AI Engineering Guidebook.pdf</a></p>
      <p>文件信息：384 页，PDF 文档。</p>

      <h2 id="usage">打开方式</h2>
      <p>可以直接点击上面的链接在线打开，也可以在浏览器里另存后离线阅读。</p>
]]></content>
  </entry>
  <entry>
    <title>转载阅读：Harness 工程之道，Skill 原理与最佳实践</title>
    <url>/2026/07/01/skill-engineering-reading/</url>
    <content><![CDATA[<h2 id="copyright">转载说明</h2>
      <p>本文为转载阅读记录，不是原文全文搬运。原文来自公众号「阿里云开发者」，标题为《Harness 工程之道：Skill 原理与最佳实践》，发布时间为 2026-07-01。</p>
      <p>原文地址：<a href="https://mp.weixin.qq.com/s/yo2f5edeNNkYtCte9P0yhQ" target="_blank" rel="noopener noreferrer">https://mp.weixin.qq.com/s/yo2f5edeNNkYtCte9P0yhQ</a></p>
      <p>版权归原作者及原发布平台所有。这里仅保留阅读后的结构化摘要与个人理解，方便后续回看；如需完整内容，请访问原文。</p>

      <h2 id="overview">文章主线</h2>
      <p>这篇文章讨论的不是“怎么再写一个更长的 Prompt”，而是为什么 Agent 时代需要把专业知识和工作流封装成 Skill。作者从 Prompt 工程的局限切入，解释 Skill 如何以更轻量、更模块化的方式，把领域能力按需加载给 Agent。</p>
      <p>全文基本沿着四条线展开：第一条线讲 Skill 产生的背景与核心理念，第二条线讲 Skill 的结构规范，第三条线讲触发机制和作用域优先级，第四条线讲一组比较工程化的最佳实践，包括路由设计、知识分层、权限隔离、脚本增强和参数传递。</p>

      <h2 id="key-points">要点摘记</h2>
      <p>第一，文章把 Skill 放在 Prompt 工程的延长线上来理解。Prompt 工程解决的是“每次对话怎么把背景塞进去”，但随着项目变复杂，所有知识都堆在一个大 Prompt 里会越来越臃肿，上下文窗口被稀释，知识也和单个项目深度耦合。Skill 的价值，就是把这些知识和流程拆成可移植、可版本化、可按需加载的能力包。</p>
      <p>第二，作者认为 Skill 的核心设计理念是渐进性披露。会话刚启动时，Agent 只知道每个 Skill 的 name 和 description；只有任务语义命中后，才读取完整的 SKILL.md；进入真正执行阶段，才按路由去加载对应模块和参考资料。这样上下文成本更低，也更容易把窗口留给当前任务真正相关的信息。</p>
      <p>第三，文章把 System Prompt 和 Skill 做了一个挺清楚的区分。前者更像“这个项目的规矩”，会在会话启动时整体加载；后者更像“某种可复用的能力”，只在需要时激活。一个项目完全可以同时拥有全局规则和多个 Skill，它们不是互斥关系，而是层叠关系。</p>
      <p>第四，Skill 的目录结构和 SKILL.md 入口规范被讲得很细。一个 Skill 本质上就是一个带 SKILL.md 的文件夹，里面可以放 scripts、references、assets 等子目录；而 SKILL.md 既是唯一入口，也是唯一触发器，其中 frontmatter 的 name 和 description 字段尤其关键，因为 Agent 是否会在正确时机调用它，主要靠这里的语义描述。</p>
      <p>第五，作者特别强调 description 的重要性。自动触发的本质是语义匹配，因此 description 不只是写“这个 Skill 做什么”，还要写“什么时候该用它”。要同时回答 WHAT 和 WHEN，尽量显式列出具体触发词，必要时还要写清不适用的边界，以减少误触发。</p>
      <p>第六，作用域和优先级也很关键。Skill 可能来自企业级配置、个人全局配置、项目目录或 plugin 内置资源；一旦多个 Skill 同时命中，通常需要遵循企业策略优先、再到个人配置、项目配置、插件内置的层次。也就是说，Skill 不只是一个文件格式，更是一个有治理层次的能力分发系统。</p>
      <p>第七，文章里最实用的一组建议是“SKILL.md 正文即路由器”。主文件应该负责分发意图和声明全局规则，而不是把所有知识都堆进去；细节应该下沉到模块文件，让 Agent 只在当前阶段读取真正需要的内容。这和我们现在给博客、脚本、Skill 做分层，其实是完全同一类设计思想。</p>
      <p>第八，后半段对权限设计和脚本增强也很到位。工程级 Skill 涉及多个模块和工具时，应该坚持模块级白名单和最小权限；而凡是适合确定性完成的事情，比如配置读写、环境检测、日志采集和复杂计算，都更适合封装成脚本，让 Agent 去调，而不是让模型自己“猜”。</p>

      <h2 id="notable-sections">我最在意的几个点</h2>
      <p>我最认同的一点，是把 SKILL.md 当“路由器”而不是“知识仓库”。很多人一写 Skill 就想把所有背景、规则、案例、接口说明都塞进主文件里，结果很快又退化成一个超大 Prompt。按路由分层之后，Skill 才真正具备可维护性。</p>
      <p>第二个点是 description 的工程价值。平时容易把它当文案字段，但实际上它决定自动触发是否靠谱。写得模糊，Skill 就会失灵或者乱触发；写得清楚，它才真像一个能被 Agent 正确调用的“能力单元”。</p>
      <p>第三个点是脚本和权限隔离。文章没有把 Skill 浪漫化成纯提示词艺术，而是很明确地说：真正生产可用的 Skill，必须把可确定的逻辑交给脚本，把危险操作关进白名单，把模块职责切干净。这一点非常工程化，也非常重要。</p>

      <h2 id="personal-notes">个人理解</h2>
      <p>我觉得这篇文章刚好补上了 Harness 话题里一个很落地的拼图。Harness 讲的是给 Agent 搭工作环境，而 Skill 更像这个环境里的能力插件系统。它把知识、规则、脚本和工具调用方式都组织成一个可发现、可激活、可治理的结构，让 Agent 不用每次从零开始拼凑工作流。</p>
      <p>放到我现在给你做博客管理、图片整理和转载阅读这些事上，其实同样成立。真正省力的不是让模型每次重新理解流程，而是把稳定流程沉淀成一个描述清楚、边界明确、按需加载的 Skill。这样一来，能力才会越来越像资产，而不是一次性聊天产物。</p>
]]></content>
  </entry>
  <entry>
    <title>转载阅读：开启 Harness Engineering 探索之旅</title>
    <url>/2026/06/29/harness-engineering-reading/</url>
    <content><![CDATA[<h2 id="copyright">转载说明</h2>
      <p>本文为转载阅读记录，不是原文全文搬运。原文来自公众号「腾讯技术工程」，标题为《开启Harness Engineering探索之旅》，发布时间为 2026-06-29。</p>
      <p>原文地址：<a href="https://mp.weixin.qq.com/s/uhc7_-0Vm_cw9p17b9VyJA" target="_blank" rel="noopener noreferrer">https://mp.weixin.qq.com/s/uhc7_-0Vm_cw9p17b9VyJA</a></p>
      <p>版权归原作者及原发布平台所有。这里仅保留阅读后的结构化摘要与个人理解，方便后续回看；如需完整内容，请访问原文。</p>

      <h2 id="overview">文章主线</h2>
      <p>这篇文章试图解释一个很多团队都已经碰到的现实问题：AI 写代码越来越快，但研发整体节奏并没有按同样比例提速。作者把这个现象归因到一个更高层的短板上，也就是模型之外那套工作环境、约束、反馈和验证机制，并用 Harness Engineering 这个名字去概括它。</p>
      <p>全文先讲概念是怎么“结晶”出来的，再回到团队自己的工程实践，重点展开他们如何围绕需求、设计、实现、测试、部署和归档做一整套协议化、管线化和自愈化改造。它不是单点工具介绍，而是一篇很强的工程系统设计复盘。</p>

      <h2 id="key-points">要点摘记</h2>
      <p>第一，作者把 AI 工程的关注点迁移拆成三站：Prompt Engineering 关注单次回答，Context Engineering 关注每一步喂给模型的信息，而 Harness Engineering 关注整段任务在真实工程环境里能不能稳定跑完。这个分层挺清楚，也解释了为什么模型变强之后，新的瓶颈会转移到模型外。</p>
      <p>第二，文中反复强调 Harness 不是教模型“怎么回答”，而是设计模型“怎么工作”。换句话说，重点不再是这一句输出对不对，而是长链路任务里有没有执行环境、工具协调、状态管理、反馈注入、约束施加和进展验证。</p>
      <p>第三，作者认为“AI 写得快但整体没快多少”的根因不在代码生成本身，而在理解、对齐、验证、沉淀和追溯这些非编码环节没有同步被系统性加速。代码这一环提速后，下游 review、测试、维护反而更容易被动，这也是很多团队真实会遇到的结构性问题。</p>
      <p>第四，腾讯这套实践把目标概括成“人提需求，AI 理解，AI 执行，人确认”，并把研发端到端交付拆成 P1 到 P6 的标准阶段，再加一个可选前置。核心思想不是让 AI 自由发挥，而是让每一阶段都变成有明确输入输出契约、可校验、可追溯的工序。</p>
      <p>第五，P1 和 P2 的做法很值得注意。需求阶段要求直接引用 TAPD 原始口径，AC 必须可测，test cases 与 requirements 同源；设计阶段则把 design 文档当作机器可读契约来写，强调字段、接口、状态流、改动点都要显式化，避免实现阶段靠 AI 自行脑补。</p>
      <p>第六，P3 和 P4 的实践更像真正的 Harness 工程。实现阶段通过 D2C、UI 校准闭环和 code-reviewer 三档机制约束“像不像、对不对”；测试阶段则把后端 API 测试做成失败后自动抓 trace、查日志、查数据库、查 Redis，再产出根因建议的自愈链路。</p>
      <p>第七，文章里对部署和归档的强调也很重要。部署不是“推上去就完”，而是要有评分卡、显式确认和上线前硬门槛；归档也不是写材料，而是为下次同类任务沉淀知识与可复用能力，否则 AI 每次都要重新踩坑。</p>
      <p>第八，这篇文章的价值不在于某个 Skill 名字，而在于它把 Harness 具体化成了一整套组织内的工程纪律：协议、模板、校验、Stop 点、评分卡、自愈循环、知识同步。这比单纯谈模型能力，更接近大团队真正能落地的东西。</p>

      <h2 id="notable-sections">我最在意的几个点</h2>
      <p>我最在意的第一个点，是他们把“需求到上线”拆成了明确阶段，而且每一阶段都要求机器可读。很多团队现在已经能用 AI 写出东西，但一旦流程没有契约，真正的风险就从代码质量转移成“整个链路谁也说不清楚发生了什么”。</p>
      <p>第二个点是测试自愈链路。文章里最亮眼的一段，不是让 AI 自动生成测试，而是失败之后能不能自动拿着 trace 去找日志、查数据、回给实现 Agent 继续修。这种反馈回路才是 Harness 真正发力的地方。</p>
      <p>第三个点是它对边界很诚实。比如用户真正想要什么、SQL 是否执行、灰度与回滚的决策权，文章都明确保留给人。它不是一篇盲目吹全自动的文章，这反而让整套方案更可信。</p>

      <h2 id="personal-notes">个人理解</h2>
      <p>我觉得这篇文章最重要的提醒是：Agent 真正难的部分，越来越不是“能不能生成代码”，而是“能不能在你的工程体系里稳定工作”。当团队从个人提效走向组织协作时，Prompt 已经不够，单纯上下文也不够，必须把流程纪律、反馈回路和风险控制一起补上。</p>
      <p>如果映射到我现在维护博客、脚本和个人工作流的场景，也一样成立。真正能长期省心的，不是一次性让 AI 写出东西，而是把常见错误、校验规则和发布步骤慢慢外置成固定机制。Harness 说到底，就是把“下次别再犯同样的错”工程化。</p>
]]></content>
  </entry>
  <entry>
    <title>AI 核心概念十图速览</title>
    <url>/2026/06/29/ai-concepts-gallery/</url>
    <content><![CDATA[<h2 id="overview">这篇内容放什么</h2>
      <p>把这 10 张图集中整理成一篇博客，方便后面统一回看、转发和收藏。内容覆盖了 AI Agent 常见的一组核心概念：MCP、RAG、Function Calling、Context Engineering、Memory、Planning、Workflow vs Agent、Multi-Agent、Model Architecture 和 Reinforcement Learning。</p>
      <p>这些图本身已经很完整，所以正文不额外展开大段解释，主要保留一个清晰的浏览顺序。后面如果你想，我也可以继续把这篇扩成“每张图配一段自己的理解”的版本。</p>

      <h2 id="gallery">十张图</h2>
      <figure>
        <img src="/images/posts/ai-concepts/mcp.jpg" alt="一张图讲懂 MCP">
        <figcaption>1. MCP（Model Context Protocol）：让 AI 以标准方式连接工具、数据和外部系统。</figcaption>
      </figure>
      <figure>
        <img src="/images/posts/ai-concepts/rag.jpg" alt="一张图讲懂 RAG">
        <figcaption>2. RAG（Retrieval-Augmented Generation）：先检索，再增强上下文，最后生成回答。</figcaption>
      </figure>
      <figure>
        <img src="/images/posts/ai-concepts/function-calling.jpg" alt="一张图讲懂 Function Calling">
        <figcaption>3. Function Calling / Tool Calling：让模型从“会说”走向“会做”。</figcaption>
      </figure>
      <figure>
        <img src="/images/posts/ai-concepts/context-engineering.jpg" alt="一张图讲懂 Context Engineering">
        <figcaption>4. Context Engineering：把真正相关、准确、结构化的信息放进上下文。</figcaption>
      </figure>
      <figure>
        <img src="/images/posts/ai-concepts/memory.jpg" alt="一张图讲懂 Memory">
        <figcaption>5. Memory（记忆系统）：让 AI 记住过去，理解现在，辅助未来。</figcaption>
      </figure>
      <figure>
        <img src="/images/posts/ai-concepts/planning.jpg" alt="一张图讲懂 Planning">
        <figcaption>6. Planning（规划）：先拆解目标，再决定执行路径和资源分配。</figcaption>
      </figure>
      <figure>
        <img src="/images/posts/ai-concepts/workflow-vs-agent.jpg" alt="一张图讲懂 Workflow vs Agent">
        <figcaption>7. Workflow vs Agent：什么时候适合固定流程，什么时候适合自主决策。</figcaption>
      </figure>
      <figure>
        <img src="/images/posts/ai-concepts/multi-agent.jpg" alt="一张图讲懂 Multi-Agent">
        <figcaption>8. Multi-Agent（多智能体）：多个角色协作完成复杂任务。</figcaption>
      </figure>
      <figure>
        <img src="/images/posts/ai-concepts/model-architecture.jpg" alt="一张图讲懂 Model Architecture">
        <figcaption>9. Model Architecture（模型架构）：理解 Transformer、MoE 和大模型能力边界。</figcaption>
      </figure>
      <figure>
        <img src="/images/posts/ai-concepts/reinforcement-learning.jpg" alt="一张图讲懂 Reinforcement Learning">
        <figcaption>10. Reinforcement Learning（强化学习）：通过试错与反馈不断优化策略。</figcaption>
      </figure>

      <h2 id="closing">一句收尾</h2>
      <p>如果把这组图连起来看，其实刚好能串出一条从模型基础能力到 Agent 工程化落地的主线：模型架构决定底座，强化学习影响行为优化，RAG、Memory 和 Context Engineering 提供知识与上下文，Function Calling 和 MCP 打通外部能力，Planning、Workflow、Agent 和 Multi-Agent 决定任务怎么真正跑起来。</p>
]]></content>
  </entry>
  <entry>
    <title>转载阅读：去哪儿网 AI Coding 研发平台实践</title>
    <url>/2026/06/27/qunar-ai-coding-platform-reading/</url>
    <content><![CDATA[<h2 id="copyright">转载说明</h2>
      <p>本文为转载阅读记录，不是原文全文搬运。原文来自公众号「无处不在的技术」，标题为《精华：去哪儿网AI Coding研发平台实践，值得读三遍的样本》，发布时间为 2026-06-24。</p>
      <p>原文地址：<a href="https://mp.weixin.qq.com/s/Ug_fMuGkQmM4tECUbpfXOg" target="_blank" rel="noopener noreferrer">https://mp.weixin.qq.com/s/Ug_fMuGkQmM4tECUbpfXOg</a></p>
      <p>版权归原作者及原发布平台所有。这里仅保留阅读后的结构化摘要与个人理解，方便后续回看；如需完整内容，请访问原文。</p>

      <h2 id="overview">文章主线</h2>
      <p>这篇文章围绕去哪儿网在 AI Coding 上的组织化实践展开，不是单纯讨论某个模型或某个 IDE 工具，而是尝试回答一个更难也更现实的问题：当一个大规模研发组织把 AI Coding 变成日常基础设施之后，怎样度量它、治理它，并让它真正沉淀为组织能力。</p>
      <p>全文基本按四条线展开：第一条线是度量体系，第二条线是自动化分级与 Harness，第三条线是平台化落地路径，第四条线是 Skills 沉淀与组织治理。对技术管理者来说，这个结构比单点提效案例更值得反复看。</p>

      <h2 id="key-points">要点摘记</h2>
      <p>第一，文章强调不要只看“AI 写了多少代码”，而要同时看量和成熟度。原文把指标拆成 Volume 与 Maturity 两部分：一边看出码率、出码量、团队覆盖率、需求覆盖率，另一边看自动化等级和 Harness 等级。这比只盯着出码率更接近真实价值。</p>
      <p>第二，作者借用自动驾驶体系，把 AI Coding 分成 L0 到 L5 六级。从补全、局部生成，到有条件自动化、高度自动化，再到接近无人值守，这个分级把“大家都在说 AI Coding”这件事放进了同一套语言系统里，便于团队协作和管理。</p>
      <p>第三，文章最有价值的概念之一是 Harness。它不把重点放在模型有多强，而是放在 AI 参与研发流程时是否被稳定触发、被约束、被隔离、被审查。需求、设计、编码、测试、回归、灰度、发布等环节，都应该有对应的控制点和人工关口。</p>
      <p>第四，原文提出了四个关键控制手段：AI 触发机制、约束与门禁、安全隔离环境、人工审查节点。这个思路很像给 AI Coding 建“护栏系统”，核心目的是在提升自动化的同时，避免无约束地把风险一路带到生产环境。</p>
      <p>第五，落地路径不是“装上工具就结束”，而是 Tool、Infra、Automation、Insight 四段递进。先让 Claude Code、Codex、Cursor 等工具被广泛使用，再把能力接入研发基建，再做多 Agent、多 Skills 的自动化编排，最后用统一数据体系回收全流程洞察。</p>
      <p>第六，QunarDevCenter 这一部分很值得参考。它本质上是在给 AI Coding 建数据底座，负责采集不同终端和工具的会话数据，再与任务、测试、发布、监控链路关联。这样管理者看到的不再只是“某个人用了什么工具”，而是“AI 在整个研发链路里参与到了什么程度，效果如何”。</p>
      <p>第七，天弦（QDO）代表的是把零散 AI 任务编排成可调度工作流。文中举了 JDK 自动升级、复杂需求开发等案例，重点不是展示模型能力，而是展示如何把规则系统、Agent、部署、测试和反馈闭环串成稳定流程。</p>
      <p>第八，Qsuperpowers 和 Skills 体系说明了一个更长期的方向：AI Coding 不只是个人 prompt 技巧的比拼，而是把需求澄清、计划拆解、编码、部署、测试、验收这些经验，逐步沉淀成可复用、可检索、可治理的组织资产。</p>

      <h2 id="notable-sections">我最在意的几个点</h2>
      <p>一个是分级体系。很多团队讨论 AI Coding 时，常常把 Copilot 补全和“需求到代码的自动交付”混在一起。分级之后，团队才能知道自己当前停在哪一层，下一步要补的是模型、流程，还是治理。</p>
      <p>另一个是 Harness。过去很多分享都爱讲模型升级、prompt 技巧、上下文工程，但真正决定能不能大规模上线的，往往是隔离环境、审查节点、失败拦截和复盘机制。这篇文章把这些问题讲得很具体，也更贴近企业真实落地。</p>
      <p>还有一个是 Skills 沉淀。文章里讲的重点不是“工具越多越好”，而是要把有效工作流做成可复用的能力单元，让个人经验逐步变成团队默认能力。这和我现在给博客补 skill 的思路，其实是同一类事情，只是规模不同。</p>

      <h2 id="personal-notes">个人理解</h2>
      <p>我觉得这篇文章最值得看三遍的地方，不是它列了多少平台名称，而是它把 AI Coding 从“个人提效玩具”拉回到了“组织工程系统”视角。真正重要的不是哪个模型一时更强，而是数据有没有采起来、流程有没有编排起来、风险有没有被控制住、经验有没有沉淀下来。</p>
      <p>如果把它映射到个人项目或小团队，也能得到很实用的启发：先别急着追求全自动，先把常见流程写成脚本和 skill，把关键验证点固定下来，把发布和回归做成可重复执行的步骤。稳定之后，再谈更高等级的自动化，会踏实很多。</p>
]]></content>
  </entry>
  <entry>
    <title>转载阅读：Loop Engineering 概念解析、思考与实践</title>
    <url>/2026/06/18/loop-engineering-reading/</url>
    <content><![CDATA[<h2 id="copyright">转载说明</h2>
      <p>本文为转载阅读记录，不是原文全文搬运。原文来自公众号「阿里技术」，标题为《Loop Engineering 概念解析、思考与实践》，发布时间为 2026-06-18。</p>
      <p>原文地址：<a href="https://mp.weixin.qq.com/s/ael7aIEoomk4AU84E-mpGg" target="_blank" rel="noopener noreferrer">https://mp.weixin.qq.com/s/ael7aIEoomk4AU84E-mpGg</a></p>
      <p>版权归原作者及原发布平台所有。这里仅保留阅读后的结构化摘要和个人理解，方便后续回看；如需完整内容，请访问原文。</p>

      <h2 id="overview">文章主线</h2>
      <p>这篇文章的核心不是再解释一遍 Agent 会调用工具，而是进一步讨论：既然 Agent 已经能完成长链条执行，那么能不能把“人不断补一句提示、再验一次结果、再让它继续改”的外层协作，也设计成一个自动循环。作者把这种思路概括为 Loop Engineering。</p>
      <p>整篇文章基本分成四段：先区分底层 Agent Loop 和更高层的 Loop Engineering，再说明为什么大家开始关注这种“自动化验收闭环”，随后梳理一套六要素框架，最后讨论它的实践方式与边界条件。</p>

      <h2 id="key-points">要点摘记</h2>
      <p>第一，文章强调 Agent Loop 和 Loop Engineering 不是一回事。前者是 Agent 内部靠 Response、Function Call、工具结果回灌形成的执行循环，后者则是人在 Agent 之外设计的一套更高层流程，用来驱动开发、验证、反馈、修正不断重复，直到达到预设目标。</p>
      <p>第二，Loop Engineering 想解决的是“人肉反复催模型”的问题。很多人现在用 AI Coding，本质上还是在频繁说“继续”“还是报错”“你改了啥”“回滚”，文章认为这些高频干预如果本身有明确标准，就可以被提前写进流程，让 Agent 主动完成验证和迭代。</p>
      <p>第三，作者把这种思路描述为把 Human-in-the-Loop 尽量往外推。不是取消人，而是让人更多出现在目标设定和最终确认处，而不是每一轮都手动纠偏。前提是需求、测试集、验收条件要足够清楚，否则自动循环只会把偏差放大。</p>
      <p>第四，文章总结了一个完整 Loop 的六个核心部分：Automations、Worktrees、Skills、Connectors 或 Plugins、Sub Agents、State。它们分别解决定时触发、并行隔离、能力复用、外部工具接入、角色分工和状态追踪这几类问题。</p>
      <p>第五，文中对 Worktree 和 Sub Agent 的讨论很实用。并行 Agent 如果不隔离代码目录，很容易互相覆盖；验证类 Sub Agent 如果和实现类 Agent 混在一起，又容易变成自己给自己打分，所以需要通过工作区隔离和角色隔离来提高稳定性。</p>
      <p>第六，文章认为 Skill 在 Loop Engineering 里不仅是可复用能力包，还可以在每轮循环里持续沉淀经验，逐步从静态说明演化成“活的知识”。这和单次 prompt 的差别在于，它会随着验证和修正不断累积有效做法。</p>
      <p>第七，实践例子里作者用文本分类任务说明，Loop 不一定非得依赖复杂框架。只要把目标、验证和修正逻辑写清楚，Claude Code 或 Codex 这类工具就可以自己围绕准确率目标循环执行、修正并沉淀能力。</p>
      <p>第八，文章最后专门提醒：Loop 不是银弹。它对需求定义、验证标准、成本意识和稳定性控制的要求更高。需求模糊时，传统的人在环路里多轮确认，反而可能比一上来全自动更稳。</p>

      <h2 id="framework">六要素里我最认同的点</h2>
      <p>我比较认同作者把 Loop Engineering 拆成六个部分，而不是把它神化成一个新名词。Automations 负责节奏，Worktrees 负责隔离，Skills 和 Connectors 负责能力，Sub Agents 负责分工，State 负责可追踪性。拆开看之后，它其实更像一套“自动化工程组合拳”。</p>
      <p>另一个很重要的点是“外层闭环”。很多时候模型本身已经会写代码，真正拖慢效率的是写完之后那一长串来回确认、修补和验证。如果这一层能部分程序化，才算真正把 Agent 从工具升级成可持续运行的系统。</p>

      <h2 id="personal-notes">个人理解</h2>
      <p>我觉得这篇文章最有价值的地方，是它把“提一个需求”往前推进成“定义一套闭环流程”。这和普通 prompt engineering 的差别，不是文风更复杂，而是你开始思考触发条件、验收标准、失败重试、状态记录和角色分工。</p>
      <p>放到个人工作流里，这个思路也挺实用：固定、确定、可复现的流程，优先写脚本；需要模型动态判断但又有明确标准的流程，适合写成 skill 或 loop；需求还很模糊、验证标准也说不清时，就别硬上全自动。先把流程讲明白，比把 Loop 造得很酷更重要。</p>
]]></content>
  </entry>
  <entry>
    <title>转载阅读：从0到1搭建 Agent</title>
    <url>/2026/05/22/agent-from-zero-to-one-reading/</url>
    <content><![CDATA[<h2 id="copyright">转载说明</h2>
      <p>本文为转载阅读记录，不是原文全文搬运。原文来自公众号「阿里技术」，标题为《从0到1搭建 Agent ：Agent 原理分析及个人助手实践（长文干货）》，发布时间为 2026-05-21。</p>
      <p>原文地址：<a href="https://mp.weixin.qq.com/s/ILX8GGETM84-_rQssCZhwQ" target="_blank" rel="noopener noreferrer">https://mp.weixin.qq.com/s/ILX8GGETM84-_rQssCZhwQ</a></p>
      <p>版权归原作者及原发布平台所有。这里仅保留少量阅读摘录和个人摘要，方便后续回看与学习；如需完整内容，请访问原文。</p>

      <h2 id="overview">文章主线</h2>
      <p>原文是一篇较长的 Agent 入门到实践文章，整体分成两部分：前半部分梳理 Agent 相关理论，后半部分介绍个人助手项目的工程实现。它的核心价值在于把「模型知道什么、模型能做什么、模型如何稳定完成任务」这三条线串在一起。</p>
      <p>理论部分从 LLM 的无状态和知识静态问题出发，依次引出记忆系统、RAG、Function Call、MCP、Agent Loop、Skill、Multi-Agent 和 Harness。实践部分则围绕一个个人助手项目，拆解核心 loop、记忆模块、工具模块、SubAgent、Plan 能力和 Skill 系统。</p>

      <h2 id="key-points">要点摘记</h2>
      <p>第一，Agent 不只是调用大模型，而是围绕模型补齐记忆、知识、工具、规划、执行和容错能力。LLM 本身更像推理与生成核心，真正能做事需要外部系统配合。</p>
      <p>第二，记忆系统解决跨轮次、跨会话的信息延续问题。短期记忆关注上下文窗口管理，长期记忆关注持久化、检索、更新和冲突处理。原文还把记忆进一步推到 skill 沉淀：从事实、规则到可触发的能力包。</p>
      <p>第三，RAG 负责补业务知识和新知识。文章把 RAG 拆成文档分块、向量化、向量存储、检索优化和评估，并强调生产环境里不能只靠简单向量相似度检索。</p>
      <p>第四，Function Call 和 MCP 让模型从“会说”走向“会做”。Function Call 偏单应用内工具调用，MCP 更强调标准化协议、工具发现和跨环境复用。</p>
      <p>第五，Agent Loop 有多种范式：ReAct、Plan-Execute、Plan-React、LATS、Reflexion 等。原文实践更倾向以 ReAct 为主循环，同时在复杂任务中引入可选 Plan 能力。</p>
      <p>第六，Skill 的价值是渐进式加载。它把工具、说明和资源打包成能力包，按需进入上下文，避免工具过多导致 token 膨胀、选择困难和指令冲突。</p>
      <p>第七，Multi-Agent 适合处理上下文隔离、并行探索、专家化和质量评估。文章列举了主从委托、接力传递、对等讨论、层级分治、竞争选优、评估反馈和动态路由等模式。</p>
      <p>第八，Harness 是 Agent 稳定运行的保护层。它负责错误分类、重试退避、上下文治理、副作用控制、权限策略、提示注入防护和可观测性。</p>

      <h2 id="designs">实践设计启发</h2>
      <p>这篇文章里比较值得借鉴的是把个人助手看成一个可演进系统，而不是一次性 prompt。记忆、工具、Plan、SubAgent、Skill、Harness 都是围绕同一个目标服务：让 Agent 能持续吸收信息、按需调用能力，并在复杂任务中保持可控。</p>
      <p>对个人博客和日常工作流来说，最直接的启发是：把重复流程沉淀成 skill，把易错流程写成脚本，把长上下文任务拆成可验证的小步骤。刚刚创建的博客管理 skill，其实就是这种思路的一个小实践。</p>

      <h2 id="personal-notes">个人理解</h2>
      <p>我认为这篇文章适合当作 Agent 工程化路线图来看：从 LLM 基础能力出发，逐层补齐记忆、知识、工具、规划、多智能体协作和稳定性治理。真正有用的 Agent，不是单点能力最强，而是系统边界、状态管理和失败处理足够清楚。</p>
      <p>后续如果继续做个人助手，可以优先从三个方向落地：一是可控的长期记忆，二是工具和 skill 的渐进式加载，三是明确的 Harness 规则。这样比盲目堆工具更容易稳定。</p>
]]></content>
  </entry>
  <entry>
    <title>05-LangGraph高级特性</title>
    <url>/2025/05/28/langgraph-advanced-features/</url>
    <content><![CDATA[<h1 id="11-流式执行">11. 流式执行</h1>
<h2 id="11-1-概述">11.1. 概述</h2>
<h3 id="11-1-1-什么是流式执行">11.1.1. 什么是流式执行</h3>
<p><strong>流式执行（<code>Streaming Execution</code>）</strong>，是指程序在任务尚未全部完成时，就将执行过程中已经产生的中间结果、状态变化或事件持续输出给调用方，而不是等待整个任务结束后一次性返回最终结果。</p>
<p>简而言之：流式执行就是</p>
<blockquote>
<p>边执行、边产生事件、边向调用方返回结果。</p>
</blockquote>
<h3 id="11-1-2-什么是-langgraph-的流式执行">11.1.2. 什么是<code>LangGraph</code>的流式执行</h3>
<p><strong><code>LangGraph</code></strong> 的流式执行是指，状态图计算过程中，将节点输出、状态更新、消息增量、自定义事件或调试信息等写入流式队列，并在特定的时机将流式队列中的信息返还给调用者。</p>
<p>在同步执行中，这些数据最终进入内部的<strong>同步流式队列</strong>；在异步执行中，则进入对应的<strong>异步队列</strong>。调用方通过<strong>迭代器</strong>逐条消费这些数据，因此不必等到整张图执行完毕后再获得反馈。</p>
<h3 id="11-1-3-langgraph-的流式执行-api">11.1.3. <code>LangGraph</code>的流式执行<code>API</code></h3>
<p><strong><code>LangGraph</code></strong> 提供了两套流式执行 <strong><code>API</code></strong></p>
<h4 id="11-1-3-1-stream-astream">11.1.3.1. <code>stream/astream</code></h4>
<p>获取 <strong><code>LangGraph</code></strong> 执行过程中的业务数据或运行时数据</p>
<ul>
<li><strong><code>stream</code></strong>：同步 <strong><code>API</code></strong></li>
<li><strong><code>astream</code></strong>：异步 <strong><code>API</code></strong></li>
</ul>
<p>调用方通过 <strong><code>stream_mode</code></strong> 选择需要消费的内容，例如状态、节点更新、模型消息、自定义数据、检查点或任务事件。这是本章的重点。</p>
<p><strong><code>stream</code>与<code>astream</code>的区别</strong></p>
<table>
<thead>
<tr>
<th style="text-align:left">维度</th>
<th style="text-align:left"><code>stream()</code></th>
<th style="text-align:left"><code>astream()</code></th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">调用方式</td>
<td style="text-align:left">同步迭代 <code>for chunk in graph.stream(...)</code></td>
<td style="text-align:left">异步迭代 <code>async for chunk in graph.astream(...)</code></td>
</tr>
<tr>
<td style="text-align:left">运行环境</td>
<td style="text-align:left">普通 Python 脚本</td>
<td style="text-align:left"><code>asyncio</code> 事件循环（Jupyter、FastAPI 等）</td>
</tr>
<tr>
<td style="text-align:left">参数与输出</td>
<td style="text-align:left">相同 <code>stream_mode</code>，语义一致</td>
<td style="text-align:left">相同 <code>stream_mode</code>，语义一致</td>
</tr>
<tr>
<td style="text-align:left">适用场景</td>
<td style="text-align:left">同步批处理、CLI 工具</td>
<td style="text-align:left"><code>async</code> 服务端、结合其他异步 IO</td>
</tr>
</tbody>
</table>
<h4 id="11-1-3-2-astream_events">11.1.3.2. <code>astream_events</code></h4>
<p>获取图运行过程中产生的 <strong><code>Runnable</code></strong> 标准事件（组件生命周期、父子调用关系、输入输出等）。详见 <strong><a href="#113-astream_events">11.3. <code>astream_events</code></a></strong>。</p>
<h2 id="11-2-stream-astream">11.2. <code>stream/astream</code></h2>
<p><strong><code>astream</code></strong> 是 <strong><code>stream</code></strong> 的异步版本。二者使用相同的流模式，且在参数一致时，输出数据的语义和封装格式一致；区别主要在于调用方分别使用同步迭代和异步迭代进行消费。</p>
<p>前面的章节我们都是通过 <strong><code>invoke</code></strong> 调用状态图。实际上，<strong><code>invoke</code></strong> 会在内部消费 <strong><code>stream</code></strong>，并把流式执行结果汇总为最终返回值；<strong><code>ainvoke</code></strong> 与 <strong><code>astream</code></strong> 的关系同理。</p>
<h3 id="11-2-1-输出格式版本说明">11.2.1. 输出格式版本说明</h3>
<p>从 <strong><code>LangGraph 1.1</code></strong> 开始，<strong><code>stream/astream</code></strong> 支持两种输出格式版本：</p>
<ul>
<li><strong><code>v1</code></strong>：当前默认格式。输出形态会随参数变化：
<ul>
<li>单个流模式通常直接返回该模式的数据；</li>
<li>多个流模式返回 <strong><code>(mode, data)</code></strong>；</li>
<li>启用子图流式输出后，还会增加命名空间信息。</li>
</ul>
</li>
<li><strong><code>v2</code></strong>：在 <strong><code>v1</code></strong> 基础上做了封装，统一返回 <strong><code>StreamPart</code></strong> 字典，固定包含 <strong><code>type</code></strong>、<strong><code>ns</code></strong> 和 <strong><code>data</code></strong> 三个字段，增强了输出的可读性，简化了解析难度、并补充了校验机制。</li>
</ul>
<p>本节基于 <strong><code>v1</code></strong> 展开。示例刻意把 <strong><code>stream_mode</code></strong> 写成列表，即使每次只指定一种模式，也会得到 <strong><code>(mode, data)</code></strong> 二元组，便于观测模式名称，并且可以无缝扩展到指定多种模式的情况。</p>
<h3 id="11-2-2-stream_mode">11.2.2. <code>stream_mode</code></h3>
<p>流式输出支持不同的输出模式 <strong><code>stream_mode</code></strong>，这规定了流式输出的内容（在 <strong><code>LangGraph</code></strong> 中称为 <strong><code>chunk</code></strong>）。<strong><code>stream_mode</code></strong> 可传入单个字符串或模式列表。</p>
<table>
<thead>
<tr>
<th style="text-align:left">模式</th>
<th style="text-align:left">输出内容</th>
<th style="text-align:left">适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>values</code></strong></td>
<td style="text-align:left">每个超步后的完整状态</td>
<td style="text-align:left">需要完整状态快照</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>updates</code></strong></td>
<td style="text-align:left">节点产生的状态更新（增量）</td>
<td style="text-align:left">只关心节点输出变化</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>messages</code></strong></td>
<td style="text-align:left"><code>messages</code> 状态字段的增量更新</td>
<td style="text-align:left">LLM 对话流式输出</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>checkpoints</code></strong></td>
<td style="text-align:left">检查点更新事件（需检查点存储器）</td>
<td style="text-align:left">持久化监控、中断调试</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>tasks</code></strong></td>
<td style="text-align:left">任务开始/结果事件（含触发通道、异常）</td>
<td style="text-align:left">运行时观测、任务追踪</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>debug</code></strong></td>
<td style="text-align:left"><code>checkpoints</code> + <code>tasks</code> 的统一封装，附加超步编号、时间戳</td>
<td style="text-align:left">调试排错</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>custom</code></strong></td>
<td style="text-align:left">节点/工具通过 <code>stream_writer</code> 主动写出的自定义数据</td>
<td style="text-align:left">进度通知、阶段说明</td>
</tr>
</tbody>
</table>
<p>为便于观察每种模式的数据结构，本节每次只启用一种模式，但仍使用列表形式传参（得到 <code>(mode, data)</code> 二元组）。</p>
<h4 id="11-2-2-1-values">11.2.2.1. <code>values</code></h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END

class OverAllState(TypedDict):
    initial_state: str
    node_a_output: str
    node_b_output: str

def node_a(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;node_a_output&quot;: &quot;节点A的输出&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;node_b_output&quot;: &quot;节点B的输出&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
for chunk in graph.stream(
    {&quot;initial_state&quot;: &quot;初始状态&quot;},
    stream_mode=[&quot;values&quot;],
):
    print(chunk)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">('values', {'initial_state': '初始状态'})
('values', {'initial_state': '初始状态', 'node_a_output': '节点A的输出'})
('values', {'initial_state': '初始状态', 'node_a_output': '节点A的输出', 'node_b_output': '节点B的输出'})
</code></pre>
<h4 id="11-2-2-2-updates">11.2.2.2. <code>updates</code></h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END

class OverAllState(TypedDict):
    initial_state: str
    node_a_output: str
    node_b_output: str

def node_a(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;node_a_output&quot;: &quot;节点A的输出&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;node_b_output&quot;: &quot;节点B的输出&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
for chunk in graph.stream(
    {&quot;initial_state&quot;: &quot;初始状态&quot;},
    stream_mode=[&quot;updates&quot;],
):
    print(chunk)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">('updates', {'node_a': {'node_a_output': '节点A的输出'}})
('updates', {'node_b': {'node_b_output': '节点B的输出'}})
</code></pre>
<h4 id="11-2-2-3-messages">11.2.2.3. <code>messages</code></h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END, MessagesState

from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

def llm_node(state: MessagesState) -&gt; MessagesState:
    messages = state[&quot;messages&quot;]
    response = model.invoke(messages)

    return {
        &quot;messages&quot;: [response],
    }

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, END)

graph = builder.compile()

for chunk in graph.stream(
        {
            &quot;messages&quot;:[HumanMessage(content=&quot;你好!&quot;)]
        },
    stream_mode=[&quot;values&quot;,&quot;messages&quot;],
):
    print(chunk)

</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">('values', {'messages': [HumanMessage(content='你好!', additional_kwargs={}, response_metadata={}, id='0f0ff522-bc84-4ead-8228-66221903e9b4')]})
('messages', (AIMessageChunk(content='', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='你好', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='！', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='很高兴', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='见到', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='你', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='，', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='有什么', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='可以', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='帮', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='你的', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='吗', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='？', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='无论是', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='聊天', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='、', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='解答', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='问题', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='，', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='还是', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='需要', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='帮助', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='，', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='都可以', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='告诉我', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='哦', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='！', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='😊', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='', additional_kwargs={}, response_metadata={'finish_reason': 'stop', 'model_name': 'deepseek-v4-flash', 'system_fingerprint': 'fp_8b330d02d0_prod0820_fp8_kvcache_20260402', 'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], usage_metadata={'input_tokens': 6, 'output_tokens': 28, 'total_tokens': 34, 'input_token_details': {'cache_read': 0}, 'output_token_details': {}}, tool_call_chunks=[]), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('messages', (AIMessageChunk(content='', additional_kwargs={}, response_metadata={}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[], chunk_position='last'), {'langgraph_step': 1, 'langgraph_node': 'llm_node', 'langgraph_triggers': ('branch:to:llm_node',), 'langgraph_path': ('__pregel_pull', 'llm_node'), 'langgraph_checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'checkpoint_ns': 'llm_node:e0f03332-6641-0bb0-cf08-e99ffeb6c7a6', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
('values', {'messages': [HumanMessage(content='你好!', additional_kwargs={}, response_metadata={}, id='0f0ff522-bc84-4ead-8228-66221903e9b4'), AIMessage(content='你好！很高兴见到你，有什么可以帮你的吗？无论是聊天、解答问题，还是需要帮助，都可以告诉我哦！😊', additional_kwargs={}, response_metadata={'finish_reason': 'stop', 'model_name': 'deepseek-v4-flash', 'system_fingerprint': 'fp_8b330d02d0_prod0820_fp8_kvcache_20260402', 'model_provider': 'deepseek'}, id='lc_run--019f5a6a-64cf-7573-874a-e669e00000ba', tool_calls=[], invalid_tool_calls=[], usage_metadata={'input_tokens': 6, 'output_tokens': 28, 'total_tokens': 34, 'input_token_details': {'cache_read': 0}, 'output_token_details': {}})]})
</code></pre>
<h4 id="11-2-2-4-checkpoints">11.2.2.4. <code>checkpoints</code></h4>
<p><strong><code>checkpoints</code></strong> 模式依赖检查点存储器。它输出的是运行时构造的检查点事件，其结构接近 <strong><code>get_state()</code></strong> 返回的状态快照.</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver

from langgraph.types import interrupt, Command

class OverAllState(TypedDict):
    initial_state: str
    parallel_node_a_1: str
    parallel_node_a_2: str
    node_b_output: str

def parallel_node_a_1(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;parallel_node_a_1&quot;: &quot;并行节点A-1的输出&quot;
    }

def parallel_node_a_2(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;parallel_node_a_2&quot;: &quot;并行节点A-2的输出&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    interrupt(&quot;hello&quot;)
    return {
        &quot;node_b_output&quot;: &quot;节点B的输出&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;parallel_node_a_1&quot;, parallel_node_a_1)
builder.add_node(&quot;parallel_node_a_2&quot;, parallel_node_a_2)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;parallel_node_a_1&quot;)
builder.add_edge(START, &quot;parallel_node_a_2&quot;)
builder.add_edge([&quot;parallel_node_a_1&quot;, &quot;parallel_node_a_2&quot;], &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
for chunk in graph.stream(
    {&quot;initial_state&quot;: &quot;初始状态&quot;},
    stream_mode=[&quot;checkpoints&quot;],
    config=config
):
    print(chunk)

print('=' * 30, '-&gt; 中断前后分界线 &lt;-', '=' * 30)

for chunk in graph.stream(
    Command(resume=&quot;&quot;),
    stream_mode=[&quot;checkpoints&quot;],
    config=config
):
    print(chunk)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python"># === 第 1 个检查点：input 阶段（step=-1），仅有 __start__ 任务 ===
(
    'checkpoints',
    {
        'config': {
            'configurable': {
                'checkpoint_ns': '',
                'thread_id': '123',
                'checkpoint_id': '1f17e909-a431-6009-bfff-dd033caf8a09'
            }
        },
        'parent_config': None,
        'values': {},
        'metadata': {
            'source': 'input',
            'step': -1,
            'parents': {}
        },
        'next': ['__start__'],
        'tasks': [
            {
                'id': '6f4add75-8430-fd5b-46d5-93f40b5f142a',
                'name': '__start__',
                'interrupts': (),
                'state': None
            }
        ]
    }
)

# === 第 2 个检查点：loop 阶段（step=0），两个并行节点待执行 ===
(
    'checkpoints',
    {
        'config': {
            'configurable': {
                'checkpoint_ns': '',
                'thread_id': '123',
                'checkpoint_id': '1f17e909-a436-66f0-8000-61f51460d18e'
            }
        },
        'parent_config': {
            'configurable': {
                'checkpoint_ns': '',
                'thread_id': '123',
                'checkpoint_id': '1f17e909-a431-6009-bfff-dd033caf8a09'
            }
        },
        'values': {
            'initial_state': '初始状态'
        },
        'metadata': {
            'source': 'loop',
            'step': 0,
            'parents': {}
        },
        'next': ['parallel_node_a_1', 'parallel_node_a_2'],
        'tasks': [
            {
                'id': '0aa4a4d7-1e7f-f02f-25ef-46c088c7e1e0',
                'name': 'parallel_node_a_1',
                'interrupts': (),
                'state': None
            },
            {
                'id': 'd3670671-0bdb-6574-be1e-915be93b239e',
                'name': 'parallel_node_a_2',
                'interrupts': (),
                'state': None
            }
        ]
    }
)

# === 第 3 个检查点：loop 阶段（step=1），并行节点执行完毕，node_b 待执行 ===
(
    'checkpoints',
    {
        'config': {
            'configurable': {
                'checkpoint_ns': '',
                'thread_id': '123',
                'checkpoint_id': '1f17e909-a439-67c6-8001-f441fbddc2f9'
            }
        },
        'parent_config': {
            'configurable': {
                'checkpoint_ns': '',
                'thread_id': '123',
                'checkpoint_id': '1f17e909-a436-66f0-8000-61f51460d18e'
            }
        },
        'values': {
            'initial_state': '初始状态',
            'parallel_node_a_1': '并行节点A-1的输出',
            'parallel_node_a_2': '并行节点A-2的输出'
        },
        'metadata': {
            'source': 'loop',
            'step': 1,
            'parents': {}
        },
        'next': ['node_b'],
        'tasks': [
            {
                'id': 'cc97ac77-de6f-470b-d3c5-d0a94ea5534e',
                'name': 'node_b',
                'interrupts': (),
                'state': None
            }
        ]
    }
)

============================== -&gt; 中断前后分界线 &lt;- ==============================

# === 第 4 个检查点：中断发生时（step=1），node_b 的 interrupts 字段包含中断信息 ===
(
    'checkpoints',
    {
        'config': {
            'configurable': {
                'checkpoint_ns': '',
                'thread_id': '123',
                'checkpoint_id': '1f17e909-a439-67c6-8001-f441fbddc2f9'
            }
        },
        'parent_config': {
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f17e909-a436-66f0-8000-61f51460d18e'
            }
        },
        'values': {
            'initial_state': '初始状态',
            'parallel_node_a_1': '并行节点A-1的输出',
            'parallel_node_a_2': '并行节点A-2的输出'
        },
        'metadata': {
            'source': 'loop',
            'step': 1,
            'parents': {}
        },
        'next': ['node_b'],
        'tasks': [
            {
                'id': 'cc97ac77-de6f-470b-d3c5-d0a94ea5534e',
                'name': 'node_b',
                'interrupts': (
                    {
                        'value': 'hello',
                        'id': '486f817f7367de26620067969c8780eb'
                    },
                ),
                'state': None
            }
        ]
    }
)

# === 第 5 个检查点：resume 后（step=2），node_b 执行完毕，next 为空 ===
(
    'checkpoints',
    {
        'config': {
            'configurable': {
                'checkpoint_ns': '',
                'thread_id': '123',
                'checkpoint_id': '1f17e926-5137-6203-8002-4e1f7ffb6aa1'
            }
        },
        'parent_config': {
            'configurable': {
                'checkpoint_ns': '',
                'thread_id': '123',
                'checkpoint_id': '1f17e909-a439-67c6-8001-f441fbddc2f9'
            }
        },
        'values': {
            'initial_state': '初始状态',
            'parallel_node_a_1': '并行节点A-1的输出',
            'parallel_node_a_2': '并行节点A-2的输出',
            'node_b_output': '节点B的输出'
        },
        'metadata': {
            'source': 'loop',
            'step': 2,
            'parents': {}
        },
        'next': [],
        'tasks': []
    }
)
</code></pre>
<p><strong>查看历史检查点列表</strong></p>
<pre><code class="language-python">list(graph.get_state_history(config=config))
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">[
    StateSnapshot(
        values={
            'initial_state': '初始状态',
            'parallel_node_a_1': '并行节点A-1的输出',
            'parallel_node_a_2': '并行节点A-2的输出',
            'node_b_output': '节点B的输出'
        },
        next=(),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16eb81-f230-641b-8002-53e494a0e53c'
            }
        },
        metadata={
            'source': 'loop',
            'step': 2,
            'parents': {}
        },
        created_at='2026-06-23T04:00:50.062223+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16eb81-f229-6a9b-8001-4049f4629aea'
            }
        },
        tasks=(),
        interrupts=()
    ),

    StateSnapshot(
        values={
            'initial_state': '初始状态',
            'parallel_node_a_1': '并行节点A-1的输出',
            'parallel_node_a_2': '并行节点A-2的输出'
        },
        next=(
            'node_b',
        ),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16eb81-f229-6a9b-8001-4049f4629aea'
            }
        },
        metadata={
            'source': 'loop',
            'step': 1,
            'parents': {}
        },
        created_at='2026-06-23T04:00:50.059522+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16eb81-f225-646d-8000-b7e7c665f75a'
            }
        },
        tasks=(
            PregelTask(
                id='f2153246-63f8-7422-31f6-2712d781be78',
                name='node_b',
                path=(
                    '__pregel_pull',
                    'node_b'
                ),
                error=None,
                interrupts=(
                    Interrupt(
                        value='hello',
                        id='6e3aa823181df313cfac730c024f4ed7'
                    ),
                ),
                state=None,
                result={
                    'node_b_output': '节点B的输出'
                }
            ),
        ),
        interrupts=(
            Interrupt(
                value='hello',
                id='6e3aa823181df313cfac730c024f4ed7'
            ),
        )
    ),

    StateSnapshot(
        values={
            'initial_state': '初始状态'
        },
        next=(
            'parallel_node_a_1',
            'parallel_node_a_2'
        ),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16eb81-f225-646d-8000-b7e7c665f75a'
            }
        },
        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {}
        },
        created_at='2026-06-23T04:00:50.057725+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16eb81-f221-6a80-bfff-d157e04c0492'
            }
        },
        tasks=(
            PregelTask(
                id='419afd20-961a-9a90-49dd-d19f1448ebe0',
                name='parallel_node_a_1',
                path=(
                    '__pregel_pull',
                    'parallel_node_a_1'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'parallel_node_a_1': '并行节点A-1的输出'
                }
            ),
            PregelTask(
                id='339d1e80-39f2-24d5-c677-3fb3f8314fd6',
                name='parallel_node_a_2',
                path=(
                    '__pregel_pull',
                    'parallel_node_a_2'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'parallel_node_a_2': '并行节点A-2的输出'
                }
            )
        ),
        interrupts=()
    ),

    StateSnapshot(
        values={},
        next=(
            '__start__',
        ),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16eb81-f221-6a80-bfff-d157e04c0492'
            }
        },
        metadata={
            'source': 'input',
            'step': -1,
            'parents': {}
        },
        created_at='2026-06-23T04:00:50.056245+00:00',
        parent_config=None,
        tasks=(
            PregelTask(
                id='20116610-8f9e-c556-b647-778737961e5a',
                name='__start__',
                path=(
                    '__pregel_pull',
                    '__start__'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'initial_state': '初始状态'
                }
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<table>
<thead>
<tr>
<th style="text-align:left">对比维度</th>
<th style="text-align:left"><code>get_state_history()</code></th>
<th style="text-align:left"><code>checkpoints</code> 流</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">排序方式</td>
<td style="text-align:left">时间倒序（最新在前）</td>
<td style="text-align:left">超步编号升序（正向执行顺序）</td>
</tr>
<tr>
<td style="text-align:left">顶层 <code>interrupts</code></td>
<td style="text-align:left">有</td>
<td style="text-align:left"><strong>无</strong>（中断信息附着在 <code>tasks[].interrupts</code> 中）</td>
</tr>
</tbody>
</table>
<p><strong>中断时序说明</strong>：检查点 <code>chunk</code> 在超步<strong>第一阶段</strong>输出，中断在<strong>第二阶段</strong>触发。因此中断发生时最新检查点仍为上一步（<code>step=1</code>），任务中断信息尚为空；恢复执行后第一阶段重新输出 <code>step=1</code> 检查点时，中断信息已被记录。</p>
<h4 id="11-2-2-5-tasks">11.2.2.5. <code>tasks</code></h4>
<p><strong><code>tasks</code></strong> 模式以任务为单位输出开始事件和结果事件。它比 <strong><code>updates</code></strong> 更偏向运行时观测：除了任务结果，还会暴露任务输入、触发通道、异常和中断信息。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END

class OverAllState(TypedDict):
    initial_state: str
    parallel_node_a_1: str
    parallel_node_a_2: str
    node_b_output: str

def parallel_node_a_1(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;parallel_node_a_1&quot;: &quot;并行节点A-1的输出&quot;
    }

def parallel_node_a_2(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;parallel_node_a_2&quot;: &quot;并行节点A-2的输出&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;node_b_output&quot;: &quot;节点B的输出&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;parallel_node_a_1&quot;, parallel_node_a_1)
builder.add_node(&quot;parallel_node_a_2&quot;, parallel_node_a_2)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;parallel_node_a_1&quot;)
builder.add_edge(START, &quot;parallel_node_a_2&quot;)
builder.add_edge([&quot;parallel_node_a_1&quot;, &quot;parallel_node_a_2&quot;], &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()

for chunk in graph.stream(
    {&quot;initial_state&quot;: &quot;初始状态&quot;},
    stream_mode=[&quot;tasks&quot;]
):
    print(chunk)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">(
    'tasks',
    {
        'id': '0b0c1e51-456f-d37c-346c-2109d78f8c2a',
        'name': 'parallel_node_a_1',
        'input': {
            'initial_state': '初始状态'
        },
        'triggers': (
            'branch:to:parallel_node_a_1',
        )
    }
)

(
    'tasks',
    {
        'id': '018ab5c2-0a5a-9094-8876-05668d3f27e0',
        'name': 'parallel_node_a_2',
        'input': {
            'initial_state': '初始状态'
        },
        'triggers': (
            'branch:to:parallel_node_a_2',
        )
    }
)

(
    'tasks',
    {
        'id': '0b0c1e51-456f-d37c-346c-2109d78f8c2a',
        'name': 'parallel_node_a_1',
        'error': None,
        'result': {
            'parallel_node_a_1': '并行节点A-1的输出'
        },
        'interrupts': []
    }
)

(
    'tasks',
    {
        'id': '018ab5c2-0a5a-9094-8876-05668d3f27e0',
        'name': 'parallel_node_a_2',
        'error': None,
        'result': {
            'parallel_node_a_2': '并行节点A-2的输出'
        },
        'interrupts': []
    }
)

(
    'tasks',
    {
        'id': '6ebeabdb-1735-2cab-ca3e-ea2fa67be0ff',
        'name': 'node_b',
        'input': {
            'initial_state': '初始状态',
            'parallel_node_a_1': '并行节点A-1的输出',
            'parallel_node_a_2': '并行节点A-2的输出'
        },
        'triggers': (
            'branch:to:node_b',
            'join:parallel_node_a_1+parallel_node_a_2:node_b'
        )
    }
)

(
    'tasks',
    {
        'id': '6ebeabdb-1735-2cab-ca3e-ea2fa67be0ff',
        'name': 'node_b',
        'error': None,
        'result': {
            'node_b_output': '节点B的输出'
        },
        'interrupts': []
    }
)
</code></pre>
<p><strong>任务 <code>chunk</code> 分为两类</strong>：</p>
<table>
<thead>
<tr>
<th style="text-align:left">事件类型</th>
<th style="text-align:left">产生时机</th>
<th style="text-align:left">关键字段</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong>任务开始事件</strong></td>
<td style="text-align:left">任务准备完毕，尚未执行</td>
<td style="text-align:left"><code>input</code>（任务输入）、<code>triggers</code>（触发通道）</td>
</tr>
<tr>
<td style="text-align:left"><strong>任务结果事件</strong></td>
<td style="text-align:left">任务执行完成/失败/中断后</td>
<td style="text-align:left"><code>error</code>（异常，<code>None</code>=正常）、<code>result</code>、<code>interrupts</code></td>
</tr>
</tbody>
</table>
<p>并行任务时：所有<strong>开始事件</strong>（第一阶段）全部输出后，才出现<strong>结果事件</strong>（第二阶段）。</p>
<h4 id="11-2-2-6-debug">11.2.2.6. <code>debug</code></h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver

from langgraph.types import interrupt, Command

class OverAllState(TypedDict):
    initial_state: str
    parallel_node_a_1: str
    parallel_node_a_2: str
    node_b_output: str

def parallel_node_a_1(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;parallel_node_a_1&quot;: &quot;并行节点A-1的输出&quot;
    }

def parallel_node_a_2(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;parallel_node_a_2&quot;: &quot;并行节点A-2的输出&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    interrupt(&quot;hello&quot;)
    return {
        &quot;node_b_output&quot;: &quot;节点B的输出&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;parallel_node_a_1&quot;, parallel_node_a_1)
builder.add_node(&quot;parallel_node_a_2&quot;, parallel_node_a_2)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;parallel_node_a_1&quot;)
builder.add_edge(START, &quot;parallel_node_a_2&quot;)
builder.add_edge([&quot;parallel_node_a_1&quot;, &quot;parallel_node_a_2&quot;], &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
for chunk in graph.stream(
    {&quot;initial_state&quot;: &quot;初始状态&quot;},
    stream_mode=[&quot;debug&quot;],
    config=config
):
    print(chunk)

print('=' * 30, '-&gt; 中断前后分界线 &lt;-', '=' * 30)

for chunk in graph.stream(
    Command(resume=&quot;&quot;),
    stream_mode=[&quot;debug&quot;],
    config=config
):
    print(chunk)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">(
    'debug',
    {
        'step': -1,
        'timestamp': '2026-06-23T06:02:59.369405+00:00',
        'type': 'checkpoint',
        'payload': {
            'config': {
                'configurable': {
                    'checkpoint_ns': '',
                    'thread_id': '123',
                    'checkpoint_id': '1f16ec92-fbe6-68b4-bfff-30257767db2f'
                }
            },
            'parent_config': None,
            'values': {},
            'metadata': {
                'source': 'input',
                'step': -1,
                'parents': {}
            },
            'next': [
                '__start__'
            ],
            'tasks': [
                {
                    'id': '3a25e32c-3a9a-a6fa-3c77-e958d2cf0021',
                    'name': '__start__',
                    'interrupts': (),
                    'state': None
                }
            ]
        }
    }
)

(
    'debug',
    {
        'step': 0,
        'timestamp': '2026-06-23T06:02:59.371035+00:00',
        'type': 'checkpoint',
        'payload': {
            'config': {
                'configurable': {
                    'checkpoint_ns': '',
                    'thread_id': '123',
                    'checkpoint_id': '1f16ec92-fbe9-63f9-8000-bc529e935a70'
                }
            },
            'parent_config': {
                'configurable': {
                    'checkpoint_ns': '',
                    'thread_id': '123',
                    'checkpoint_id': '1f16ec92-fbe6-68b4-bfff-30257767db2f'
                }
            },
            'values': {
                'initial_state': '初始状态'
            },
            'metadata': {
                'source': 'loop',
                'step': 0,
                'parents': {}
            },
            'next': [
                'parallel_node_a_1',
                'parallel_node_a_2'
            ],
            'tasks': [
                {
                    'id': '8f4954b8-e096-ed44-fd3b-b5741b66ce14',
                    'name': 'parallel_node_a_1',
                    'interrupts': (),
                    'state': None
                },
                {
                    'id': '33b02562-5b8b-c072-3dd4-d939f02f3835',
                    'name': 'parallel_node_a_2',
                    'interrupts': (),
                    'state': None
                }
            ]
        }
    }
)

(
    'debug',
    {
        'step': 1,
        'timestamp': '2026-06-23T06:02:59.371050+00:00',
        'type': 'task',
        'payload': {
            'id': '8f4954b8-e096-ed44-fd3b-b5741b66ce14',
            'name': 'parallel_node_a_1',
            'input': {
                'initial_state': '初始状态'
            },
            'triggers': (
                'branch:to:parallel_node_a_1',
            )
        }
    }
)

(
    'debug',
    {
        'step': 1,
        'timestamp': '2026-06-23T06:02:59.371054+00:00',
        'type': 'task',
        'payload': {
            'id': '33b02562-5b8b-c072-3dd4-d939f02f3835',
            'name': 'parallel_node_a_2',
            'input': {
                'initial_state': '初始状态'
            },
            'triggers': (
                'branch:to:parallel_node_a_2',
            )
        }
    }
)

(
    'debug',
    {
        'step': 1,
        'timestamp': '2026-06-23T06:02:59.371811+00:00',
        'type': 'task_result',
        'payload': {
            'id': '8f4954b8-e096-ed44-fd3b-b5741b66ce14',
            'name': 'parallel_node_a_1',
            'error': None,
            'result': {
                'parallel_node_a_1': '并行节点A-1的输出'
            },
            'interrupts': []
        }
    }
)

(
    'debug',
    {
        'step': 1,
        'timestamp': '2026-06-23T06:02:59.371861+00:00',
        'type': 'task_result',
        'payload': {
            'id': '33b02562-5b8b-c072-3dd4-d939f02f3835',
            'name': 'parallel_node_a_2',
            'error': None,
            'result': {
                'parallel_node_a_2': '并行节点A-2的输出'
            },
            'interrupts': []
        }
    }
)

(
    'debug',
    {
        'step': 1,
        'timestamp': '2026-06-23T06:02:59.372140+00:00',
        'type': 'checkpoint',
        'payload': {
            'config': {
                'configurable': {
                    'checkpoint_ns': '',
                    'thread_id': '123',
                    'checkpoint_id': '1f16ec92-fbee-6f65-8001-4e724c2acb07'
                }
            },
            'parent_config': {
                'configurable': {
                    'checkpoint_ns': '',
                    'thread_id': '123',
                    'checkpoint_id': '1f16ec92-fbe9-63f9-8000-bc529e935a70'
                }
            },
            'values': {
                'initial_state': '初始状态',
                'parallel_node_a_1': '并行节点A-1的输出',
                'parallel_node_a_2': '并行节点A-2的输出'
            },
            'metadata': {
                'source': 'loop',
                'step': 1,
                'parents': {}
            },
            'next': [
                'node_b'
            ],
            'tasks': [
                {
                    'id': 'e7d4c64e-8ab7-99ec-4031-170fd0122ed7',
                    'name': 'node_b',
                    'interrupts': (),
                    'state': None
                }
            ]
        }
    }
)

(
    'debug',
    {
        'step': 2,
        'timestamp': '2026-06-23T06:02:59.372150+00:00',
        'type': 'task',
        'payload': {
            'id': 'e7d4c64e-8ab7-99ec-4031-170fd0122ed7',
            'name': 'node_b',
            'input': {
                'initial_state': '初始状态',
                'parallel_node_a_1': '并行节点A-1的输出',
                'parallel_node_a_2': '并行节点A-2的输出'
            },
            'triggers': (
                'branch:to:node_b',
                'join:parallel_node_a_1+parallel_node_a_2:node_b'
            )
        }
    }
)

(
    'debug',
    {
        'step': 2,
        'timestamp': '2026-06-23T06:02:59.372332+00:00',
        'type': 'task_result',
        'payload': {
            'id': 'e7d4c64e-8ab7-99ec-4031-170fd0122ed7',
            'name': 'node_b',
            'error': None,
            'result': {},
            'interrupts': [
                {
                    'value': 'hello',
                    'id': 'b195d3f05c81aac2aab438899fe67823'
                }
            ]
        }
    }
)

============================== -&gt; 中断前后分界线 &lt;- ==============================

(
    'debug',
    {
        'step': 1,
        'timestamp': '2026-06-23T06:02:59.373847+00:00',
        'type': 'checkpoint',
        'payload': {
            'config': {
                'configurable': {
                    'checkpoint_ns': '',
                    'thread_id': '123',
                    'checkpoint_id': '1f16ec92-fbee-6f65-8001-4e724c2acb07'
                }
            },
            'parent_config': {
                'configurable': {
                    'thread_id': '123',
                    'checkpoint_ns': '',
                    'checkpoint_id': '1f16ec92-fbe9-63f9-8000-bc529e935a70'
                }
            },
            'values': {
                'initial_state': '初始状态',
                'parallel_node_a_1': '并行节点A-1的输出',
                'parallel_node_a_2': '并行节点A-2的输出'
            },
            'metadata': {
                'source': 'loop',
                'step': 1,
                'parents': {}
            },
            'next': [
                'node_b'
            ],
            'tasks': [
                {
                    'id': 'e7d4c64e-8ab7-99ec-4031-170fd0122ed7',
                    'name': 'node_b',
                    'interrupts': (
                        {
                            'value': 'hello',
                            'id': 'b195d3f05c81aac2aab438899fe67823'
                        },
                    ),
                    'state': None
                }
            ]
        }
    }
)

(
    'debug',
    {
        'step': 2,
        'timestamp': '2026-06-23T06:02:59.373864+00:00',
        'type': 'task',
        'payload': {
            'id': 'e7d4c64e-8ab7-99ec-4031-170fd0122ed7',
            'name': 'node_b',
            'input': {
                'initial_state': '初始状态',
                'parallel_node_a_1': '并行节点A-1的输出',
                'parallel_node_a_2': '并行节点A-2的输出'
            },
            'triggers': (
                'branch:to:node_b',
                'join:parallel_node_a_1+parallel_node_a_2:node_b'
            )
        }
    }
)

(
    'debug',
    {
        'step': 2,
        'timestamp': '2026-06-23T06:02:59.374037+00:00',
        'type': 'task_result',
        'payload': {
            'id': 'e7d4c64e-8ab7-99ec-4031-170fd0122ed7',
            'name': 'node_b',
            'error': None,
            'result': {
                'node_b_output': '节点B的输出'
            },
            'interrupts': []
        }
    }
)

(
    'debug',
    {
        'step': 2,
        'timestamp': '2026-06-23T06:02:59.374356+00:00',
        'type': 'checkpoint',
        'payload': {
            'config': {
                'configurable': {
                    'checkpoint_ns': '',
                    'thread_id': '123',
                    'checkpoint_id': '1f16ec92-fbf3-6ffe-8002-32361c07b81c'
                }
            },
            'parent_config': {
                'configurable': {
                    'checkpoint_ns': '',
                    'thread_id': '123',
                    'checkpoint_id': '1f16ec92-fbee-6f65-8001-4e724c2acb07'
                }
            },
            'values': {
                'initial_state': '初始状态',
                'parallel_node_a_1': '并行节点A-1的输出',
                'parallel_node_a_2': '并行节点A-2的输出',
                'node_b_output': '节点B的输出'
            },
            'metadata': {
                'source': 'loop',
                'step': 2,
                'parents': {}
            },
            'next': [],
            'tasks': []
        }
    }
)
</code></pre>
<p><strong>查看历史检查点</strong></p>
<pre><code class="language-python">list(graph.get_state_history(config=config))
</code></pre>
<p><strong>结果如下</strong></p>
<pre><code class="language-json">[
    StateSnapshot(
        values={
            'initial_state': '初始状态',
            'parallel_node_a_1': '并行节点A-1的输出',
            'parallel_node_a_2': '并行节点A-2的输出',
            'node_b_output': '节点B的输出'
        },
        next=(),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16ec92-fbf3-6ffe-8002-32361c07b81c'
            }
        },
        metadata={
            'source': 'loop',
            'step': 2,
            'parents': {}
        },
        created_at='2026-06-23T06:02:59.374073+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16ec92-fbee-6f65-8001-4e724c2acb07'
            }
        },
        tasks=(),
        interrupts=()
    ),

    StateSnapshot(
        values={
            'initial_state': '初始状态',
            'parallel_node_a_1': '并行节点A-1的输出',
            'parallel_node_a_2': '并行节点A-2的输出'
        },
        next=(
            'node_b',
        ),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16ec92-fbee-6f65-8001-4e724c2acb07'
            }
        },
        metadata={
            'source': 'loop',
            'step': 1,
            'parents': {}
        },
        created_at='2026-06-23T06:02:59.372008+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16ec92-fbe9-63f9-8000-bc529e935a70'
            }
        },
        tasks=(
            PregelTask(
                id='e7d4c64e-8ab7-99ec-4031-170fd0122ed7',
                name='node_b',
                path=(
                    '__pregel_pull',
                    'node_b'
                ),
                error=None,
                interrupts=(
                    Interrupt(
                        value='hello',
                        id='b195d3f05c81aac2aab438899fe67823'
                    ),
                ),
                state=None,
                result={
                    'node_b_output': '节点B的输出'
                }
            ),
        ),
        interrupts=(
            Interrupt(
                value='hello',
                id='b195d3f05c81aac2aab438899fe67823'
            ),
        )
    ),

    StateSnapshot(
        values={
            'initial_state': '初始状态'
        },
        next=(
            'parallel_node_a_1',
            'parallel_node_a_2'
        ),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16ec92-fbe9-63f9-8000-bc529e935a70'
            }
        },
        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {}
        },
        created_at='2026-06-23T06:02:59.369665+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16ec92-fbe6-68b4-bfff-30257767db2f'
            }
        },
        tasks=(
            PregelTask(
                id='8f4954b8-e096-ed44-fd3b-b5741b66ce14',
                name='parallel_node_a_1',
                path=(
                    '__pregel_pull',
                    'parallel_node_a_1'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'parallel_node_a_1': '并行节点A-1的输出'
                }
            ),
            PregelTask(
                id='33b02562-5b8b-c072-3dd4-d939f02f3835',
                name='parallel_node_a_2',
                path=(
                    '__pregel_pull',
                    'parallel_node_a_2'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'parallel_node_a_2': '并行节点A-2的输出'
                }
            )
        ),
        interrupts=()
    ),

    StateSnapshot(
        values={},
        next=(
            '__start__',
        ),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16ec92-fbe6-68b4-bfff-30257767db2f'
            }
        },
        metadata={
            'source': 'input',
            'step': -1,
            'parents': {}
        },
        created_at='2026-06-23T06:02:59.368563+00:00',
        parent_config=None,
        tasks=(
            PregelTask(
                id='3a25e32c-3a9a-a6fa-3c77-e958d2cf0021',
                name='__start__',
                path=(
                    '__pregel_pull',
                    '__start__'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'initial_state': '初始状态'
                }
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<p><strong><code>debug</code> 模式 = <code>checkpoints</code> + <code>tasks</code> 的统一封装</strong>，外层增加调试字段：</p>
<table>
<thead>
<tr>
<th style="text-align:left">字段</th>
<th style="text-align:left">含义</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><code>step</code></td>
<td style="text-align:left">超步编号</td>
</tr>
<tr>
<td style="text-align:left"><code>timestamp</code></td>
<td style="text-align:left">chunk 生成时间</td>
</tr>
<tr>
<td style="text-align:left"><code>type</code></td>
<td style="text-align:left"><code>checkpoint</code> / <code>task</code> / <code>task_result</code></td>
</tr>
<tr>
<td style="text-align:left"><code>payload</code></td>
<td style="text-align:left">对应模式下 chunk 的原始内容</td>
</tr>
</tbody>
</table>
<p><strong>chunk 输出顺序</strong>：每个超步第一阶段按 <code>准备任务 → 检查点日志 → 任务日志</code> 的顺序生成，因此任务 chunk 位于前后两个超步的检查点 chunk 之间。中断恢复时对应超步会重新执行，任务 chunk 会再次生成。</p>
<p>具体来说，<strong><code>step=2</code></strong> 的超步发生了中断，恢复运行时，<strong><code>step=2</code></strong> 会重新执行，其第一阶段会再次准备任务，并生成这一阶段的所有任务 <strong><code>chunk</code></strong>。</p>
<h4 id="11-2-2-7-custom">11.2.2.7. <code>custom</code></h4>
<p><strong><code>custom</code></strong> 模式允许<strong>节点或工具</strong>主动写出自定义数据，调用方通过 <strong><code>stream/astream</code></strong> 消费这些数据。</p>
<p>它适合输出进度、阶段说明、外部 API 的增量结果等非状态数据。</p>
<h5 id="11-2-2-7-1-节点中写出内容">11.2.2.7.1. 节点中写出内容</h5>
<p>节点中写出内容通过 <strong><code>Runtime</code></strong> 实例的 <strong><code>stream_writer</code></strong>（也可通过 <code>get_stream_writer()</code> 获取，二者等效）。当 <code>stream_mode</code> 包含 <code>custom</code> 时，底层会定义 <code>stream_writer</code> 并注入 <code>Runtime</code>，相关源码：</p>
<p><strong>定义 <code>stream_writer</code></strong></p>
<pre><code class="language-python">if &quot;custom&quot; in stream_modes:

    def stream_writer(c: Any) -&gt; None:
        stream.put(
            (
                tuple(
                    get_config()[CONF][CONFIG_KEY_CHECKPOINT_NS].split(
                        NS_SEP
                    )[:-1]
                ),
                &quot;custom&quot;,
                c,
            )
        )
</code></pre>
<p>创建 <strong><code>Runtime</code></strong> 实例</p>
<pre><code class="language-python">runtime = Runtime(
    context=_coerce_context(self.context_schema, context),
    store=store,
    stream_writer=stream_writer,
    previous=None,
)
</code></pre>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.runtime import Runtime

class OverAllState(TypedDict):
    initial_state: str
    node_a_output: str
    node_b_output: str

def node_a(state: OverAllState, runtime: Runtime) -&gt; OverAllState:
    stream_writer = runtime.stream_writer
    stream_writer(&quot;节点 A 正在执行...&quot;)

    return {
        &quot;node_a_output&quot;: &quot;节点A的输出&quot;
    }

def node_b(state: OverAllState, runtime: Runtime) -&gt; OverAllState:
    stream_writer = runtime.stream_writer
    stream_writer(&quot;节点 B 正在执行...&quot;)

    return {
        &quot;node_b_output&quot;: &quot;节点B的输出&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
for chunk in graph.stream(
    {&quot;initial_state&quot;: &quot;初始状态&quot;},
    stream_mode=[&quot;custom&quot;],
):
    print(chunk)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">('custom', '节点 A 正在执行...')
('custom', '节点 B 正在执行...')
</code></pre>
<h5 id="11-2-2-7-2-工具中写出内容">11.2.2.7.2. 工具中写出内容</h5>
<p>工具中写出内容通过 <strong><code>ToolRuntime</code></strong> 实例的 <strong><code>stream_writer</code></strong>，用法与节点中一致。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END, MessagesState
from langgraph.prebuilt.tool_node import ToolNode, ToolRuntime
from langgraph.runtime import Runtime
from langchain.tools import tool

from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek
from dotenv import load_dotenv

load_dotenv(override=True)
model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

@tool(parse_docstring=True)
def get_weather(city: str, runtime: ToolRuntime) -&gt; str:
    &quot;&quot;&quot;
    根据城市查询当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    stream_writer = runtime.stream_writer
    stream_writer(f&quot;正在查询 {city} 今天的天气...&quot;)
    return f&quot;{city} 今天天气不错&quot;

tools = [get_weather]
model_with_tools = model.bind_tools(tools=tools)

def llm_node(state: MessagesState, runtime: Runtime) -&gt; MessagesState:
    messages = state[&quot;messages&quot;]
    response = model_with_tools.invoke(messages)

    stream_writer = runtime.stream_writer
    stream_writer(&quot;正在执行 llm_node...&quot;)

    return {
        &quot;messages&quot;: [response]
    }

def router(state: MessagesState) -&gt; Literal[&quot;tool_node&quot;, END]:
    last_msg = state[&quot;messages&quot;][-1]
    if last_msg.tool_calls:
        return &quot;tool_node&quot;
    return END

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, ToolNode(tools=tools))
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_conditional_edges(&quot;llm_node&quot;, router, path_map=[&quot;tool_node&quot;, END])
builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)

graph = builder.compile()

for chunk in graph.stream(
    {&quot;messages&quot;: [HumanMessage(&quot;今天北京天气如何？&quot;)]},
    stream_mode=[&quot;custom&quot;]
):
    print(chunk)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">('custom', '正在执行 llm_node...')
('custom', '正在查询 北京 今天的天气...')
('custom', '正在执行 llm_node...')
</code></pre>
<h3 id="11-2-3-底层机制">11.2.3. 底层机制</h3>
<p>以 <strong><code>LangGraph 1.1.2</code></strong> 的 <strong><code>stream()</code></strong> 为例，流式处理分为三个阶段：</p>
<table>
<thead>
<tr>
<th style="text-align:left">阶段</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong>创建队列</strong></td>
<td style="text-align:left"><code>stream()</code> 调用 <code>SyncQueue()</code> 创建 FIFO 队列，接收运行时写入的 chunk</td>
</tr>
<tr>
<td style="text-align:left"><strong>生产</strong></td>
<td style="text-align:left">根据 <code>stream_mode</code>，在运行时的不同阶段将原始数据写入队列</td>
</tr>
<tr>
<td style="text-align:left"><strong>消费</strong></td>
<td style="text-align:left">在两处调用 <code>_output()</code> 函数，按 FIFO 从队列取出数据 <code>yield</code> 给调用者：① <code>runner.tick()</code> 交还控制权时；② 超步循环结束后清空收尾</td>
</tr>
</tbody>
</table>
<p>对 <strong><code>messages</code></strong> 和 <strong><code>custom</code></strong> 等需及时输出的模式，框架启用<strong>等待器</strong>，队列有新数据时立即交还控制权。</p>
<h2 id="11-3-astream_events">11.3. <code>astream_events</code></h2>
<p><strong><code>astream_events</code></strong> 用于异步获取图运行过程中产生的 <strong><code>Runnable</code> 标准事件</strong>。它关注的是可执行组件的生命周期、父子调用关系、输入输出和元数据，而不是单纯返回图状态。</p>
<p><strong><code>Runnable</code>接口</strong></p>
<p><strong><code>Runnable</code></strong> 是 <code>LangChain</code> 与 <code>LangGraph</code> 生态的统一可执行对象协议（<code>langchain_core.runnables</code>），统一了模型、工具、Agent、节点和编译图等组件的调用方式。</p>
<table>
<thead>
<tr>
<th style="text-align:left">方法</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><code>invoke()</code> / <code>ainvoke()</code></td>
<td style="text-align:left">同步/异步调用</td>
</tr>
<tr>
<td style="text-align:left"><code>stream()</code> / <code>astream()</code></td>
<td style="text-align:left">同步/异步流式执行</td>
</tr>
<tr>
<td style="text-align:left"><code>astream_events()</code></td>
<td style="text-align:left">异步获取标准事件</td>
</tr>
<tr>
<td style="text-align:left"><code>batch()</code></td>
<td style="text-align:left">批量执行</td>
</tr>
</tbody>
</table>
<p>常见 <code>Runnable</code> 组件：<code>CompiledStateGraph</code>、<code>PromptTemplate</code>、<code>ChatModel</code>、<code>Tool</code>、<code>Retriever</code>、<code>RunnableSequence</code>。</p>
<p><strong><code>Runnable</code>事件</strong></p>
<p><strong><code>Runnable</code></strong> 执行过程会被框架转换为标准化事件，遵循 <code>on_&lt;type&gt;_&lt;phase&gt;</code> 命名规则：</p>
<table>
<thead>
<tr>
<th style="text-align:left">阶段</th>
<th style="text-align:left">事件后缀</th>
<th style="text-align:left">含义</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">开始</td>
<td style="text-align:left"><code>_start</code></td>
<td style="text-align:left">组件开始执行</td>
</tr>
<tr>
<td style="text-align:left">中间</td>
<td style="text-align:left"><code>_stream</code></td>
<td style="text-align:left">产生中间结果</td>
</tr>
<tr>
<td style="text-align:left">结束</td>
<td style="text-align:left"><code>_end</code></td>
<td style="text-align:left">组件执行结束</td>
</tr>
</tbody>
</table>
<p>类型（<code>&lt;type&gt;</code>）可为 <code>chain</code>、<code>chat_model</code>、<code>llm</code>、<code>tool</code>、<code>retriever</code>、<code>prompt</code> 等。并非所有类型都有三种事件——例如工具通常只有 <code>on_tool_start</code> 和 <code>on_tool_end</code>。</p>
<p>实际运行拿到的 <strong><code>Runnable</code> 事件</strong> 示例如下</p>
<pre><code class="language-json">{
    'event': 'on_chain_start',
    'data': {
        'input': {
            'initial_state': '初始状态'
        }
    },
    'name': 'LangGraph',
    'tags': [],
    'run_id': '019eeedd-9dfd-7400-b465-6b95f7292333',
    'metadata': {},
    'parent_ids': []
}
</code></pre>
<h3 id="11-3-1-astream_events-的版本">11.3.1. <code>astream_events</code>的版本</h3>
<p>从 <strong><code>LangGraph 1.2.0</code></strong> 开始，<strong><code>astream_events</code></strong> 支持三个版本：</p>
<table>
<thead>
<tr>
<th style="text-align:left">版本</th>
<th style="text-align:left">基础</th>
<th style="text-align:left">特点</th>
<th style="text-align:left">入口</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>v1</code></strong></td>
<td style="text-align:left"><code>Runnable</code> 标准事件</td>
<td style="text-align:left"><code>parent_ids</code> 始终为空列表</td>
<td style="text-align:left"><code>astream_events(version=&quot;v1&quot;)</code></td>
</tr>
<tr>
<td style="text-align:left"><strong><code>v2</code></strong>（默认）</td>
<td style="text-align:left"><code>Runnable</code> 标准事件</td>
<td style="text-align:left"><code>parent_ids</code> 可表达完整父级运行链，改进父子调用关系</td>
<td style="text-align:left"><code>astream_events(version=&quot;v2&quot;)</code></td>
</tr>
<tr>
<td style="text-align:left"><strong><code>v3</code></strong></td>
<td style="text-align:left"><code>Pregel</code> 原始流（= <code>stream/astream</code> 底层）</td>
<td style="text-align:left">全新协议，通过类型化投影消费（见下表），独立于 v1/v2</td>
<td style="text-align:left"><code>astream_events(version=&quot;v3&quot;)</code> / <code>stream_events(version=&quot;v3&quot;)</code></td>
</tr>
</tbody>
</table>
<p><strong><code>v3</code></strong> 从底层 <code>stream/astream</code> 获取 <code>Pregel</code> 原始流数据，经事件路由器与 <code>StreamTransformer</code> 生成类型化投影。投影之间可独立/并发消费：</p>
<table>
<thead>
<tr>
<th style="text-align:left">投影</th>
<th style="text-align:left">用法</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>stream</code></strong></td>
<td style="text-align:left">遍历每个协议事件</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>stream.messages</code></strong></td>
<td style="text-align:left">流式传输聊天模型的消息。</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>stream.values</code></strong></td>
<td style="text-align:left">流式传输状态快照</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>stream.output</code></strong></td>
<td style="text-align:left">等待计算图的最终输出</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>stream.subgraphs</code></strong></td>
<td style="text-align:left">观测子图的运行</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>stream.interrupts</code></strong></td>
<td style="text-align:left">观测 <strong><code>HITL</code></strong> 的中断信息</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>stream.interrupted</code></strong></td>
<td style="text-align:left">检查运行是否因人工输入而中断</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>stream.extensions</code></strong></td>
<td style="text-align:left">消费自定义流的投影</td>
</tr>
</tbody>
</table>
<p>不同投影之间可以独立或并发消费，读取 <strong><code>stream.messages</code></strong> 不会消耗 <strong><code>stream.values</code></strong> 、 <strong><code>stream.subgraphs</code></strong> 或 <strong><code>stream.output</code></strong> 所需的事件。</p>
<p><code>Pregel</code> 流即 <code>stream/astream</code> 的底层支持，上文 7 种 <code>stream_mode</code> 分别对应其中的 7 类流式数据。v3 并非 v1/v2 的封装——二者数据协议和内部管线独立，但共享同一组公开 API 入口和底层图运行时。</p>
<p><img src="/images/langgraph-study-notes/image-20260623180949275.png" alt="image-20260623180949275" /></p>
<p>当前版本（<strong><code>LangGraph 1.1.2</code></strong>）仅支持 <code>astream_events</code> 的 <strong><code>v1/v2</code></strong>，默认版本为 <strong><code>v2</code></strong>。</p>
<h3 id="11-3-2-astream_events-的用法">11.3.2. <code>astream_events</code>的用法</h3>
<p>事件流不是我们研究的重点，此处仅提供最简示例。</p>
<p><strong>示例如下</strong></p>
<p>底层也会调用 <strong><code>astream</code></strong> 来真正触发计算图的运行。</p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END

class OverAllState(TypedDict):
    initial_state: str
    node_a_output: str
    node_b_output: str

def node_a(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;node_a_output&quot;: &quot;节点A的输出&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;node_b_output&quot;: &quot;节点B的输出&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
async for chunk in graph.astream_events(
    {&quot;initial_state&quot;: &quot;初始状态&quot;},
    version=&quot;v2&quot;
):
    print(chunk)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">{
    'event': 'on_chain_start',
    'data': {
        'input': {
            'initial_state': '初始状态'
        }
    },
    'name': 'LangGraph',
    'tags': [],
    'run_id': '019ef40f-2522-7212-9ad6-cb83c499652d',
    'metadata': {},
    'parent_ids': []
}

{
    'event': 'on_chain_start',
    'data': {
        'input': {
            'initial_state': '初始状态'
        }
    },
    'name': 'node_a',
    'tags': [
        'graph:step:1'
    ],
    'run_id': '019ef40f-2524-7f83-9501-9248f93fd0a4',
    'metadata': {
        'langgraph_step': 1,
        'langgraph_node': 'node_a',
        'langgraph_triggers': (
            'branch:to:node_a',
        ),
        'langgraph_path': (
            '__pregel_pull',
            'node_a'
        ),
        'langgraph_checkpoint_ns': 'node_a:9a47e3e1-ccab-85cc-f837-f3c986bafd1f'
    },
    'parent_ids': [
        '019ef40f-2522-7212-9ad6-cb83c499652d'
    ]
}

{
    'event': 'on_chain_stream',
    'run_id': '019ef40f-2524-7f83-9501-9248f93fd0a4',
    'name': 'node_a',
    'tags': [
        'graph:step:1'
    ],
    'metadata': {
        'langgraph_step': 1,
        'langgraph_node': 'node_a',
        'langgraph_triggers': (
            'branch:to:node_a',
        ),
        'langgraph_path': (
            '__pregel_pull',
            'node_a'
        ),
        'langgraph_checkpoint_ns': 'node_a:9a47e3e1-ccab-85cc-f837-f3c986bafd1f'
    },
    'data': {
        'chunk': {
            'node_a_output': '节点A的输出'
        }
    },
    'parent_ids': [
        '019ef40f-2522-7212-9ad6-cb83c499652d'
    ]
}

{
    'event': 'on_chain_end',
    'data': {
        'output': {
            'node_a_output': '节点A的输出'
        },
        'input': {
            'initial_state': '初始状态'
        }
    },
    'run_id': '019ef40f-2524-7f83-9501-9248f93fd0a4',
    'name': 'node_a',
    'tags': [
        'graph:step:1'
    ],
    'metadata': {
        'langgraph_step': 1,
        'langgraph_node': 'node_a',
        'langgraph_triggers': (
            'branch:to:node_a',
        ),
        'langgraph_path': (
            '__pregel_pull',
            'node_a'
        ),
        'langgraph_checkpoint_ns': 'node_a:9a47e3e1-ccab-85cc-f837-f3c986bafd1f'
    },
    'parent_ids': [
        '019ef40f-2522-7212-9ad6-cb83c499652d'
    ]
}

{
    'event': 'on_chain_stream',
    'run_id': '019ef40f-2522-7212-9ad6-cb83c499652d',
    'name': 'LangGraph',
    'tags': [],
    'metadata': {},
    'data': {
        'chunk': {
            'node_a': {
                'node_a_output': '节点A的输出'
            }
        }
    },
    'parent_ids': []
}

{
    'event': 'on_chain_start',
    'data': {
        'input': {
            'initial_state': '初始状态',
            'node_a_output': '节点A的输出'
        }
    },
    'name': 'node_b',
    'tags': [
        'graph:step:2'
    ],
    'run_id': '019ef40f-2526-70a3-8ad1-04ce18598b65',
    'metadata': {
        'langgraph_step': 2,
        'langgraph_node': 'node_b',
        'langgraph_triggers': (
            'branch:to:node_b',
        ),
        'langgraph_path': (
            '__pregel_pull',
            'node_b'
        ),
        'langgraph_checkpoint_ns': 'node_b:1fc56ad3-e5a9-e2a2-4c3e-b8cffec34d9c'
    },
    'parent_ids': [
        '019ef40f-2522-7212-9ad6-cb83c499652d'
    ]
}

{
    'event': 'on_chain_stream',
    'run_id': '019ef40f-2526-70a3-8ad1-04ce18598b65',
    'name': 'node_b',
    'tags': [
        'graph:step:2'
    ],
    'metadata': {
        'langgraph_step': 2,
        'langgraph_node': 'node_b',
        'langgraph_triggers': (
            'branch:to:node_b',
        ),
        'langgraph_path': (
            '__pregel_pull',
            'node_b'
        ),
        'langgraph_checkpoint_ns': 'node_b:1fc56ad3-e5a9-e2a2-4c3e-b8cffec34d9c'
    },
    'data': {
        'chunk': {
            'node_b_output': '节点B的输出'
        }
    },
    'parent_ids': [
        '019ef40f-2522-7212-9ad6-cb83c499652d'
    ]
}

{
    'event': 'on_chain_end',
    'data': {
        'output': {
            'node_b_output': '节点B的输出'
        },
        'input': {
            'initial_state': '初始状态',
            'node_a_output': '节点A的输出'
        }
    },
    'run_id': '019ef40f-2526-70a3-8ad1-04ce18598b65',
    'name': 'node_b',
    'tags': [
        'graph:step:2'
    ],
    'metadata': {
        'langgraph_step': 2,
        'langgraph_node': 'node_b',
        'langgraph_triggers': (
            'branch:to:node_b',
        ),
        'langgraph_path': (
            '__pregel_pull',
            'node_b'
        ),
        'langgraph_checkpoint_ns': 'node_b:1fc56ad3-e5a9-e2a2-4c3e-b8cffec34d9c'
    },
    'parent_ids': [
        '019ef40f-2522-7212-9ad6-cb83c499652d'
    ]
}

{
    'event': 'on_chain_stream',
    'run_id': '019ef40f-2522-7212-9ad6-cb83c499652d',
    'name': 'LangGraph',
    'tags': [],
    'metadata': {},
    'data': {
        'chunk': {
            'node_b': {
                'node_b_output': '节点B的输出'
            }
        }
    },
    'parent_ids': []
}

{
    'event': 'on_chain_end',
    'data': {
        'output': {
            'initial_state': '初始状态',
            'node_a_output': '节点A的输出',
            'node_b_output': '节点B的输出'
        }
    },
    'run_id': '019ef40f-2522-7212-9ad6-cb83c499652d',
    'name': 'LangGraph',
    'tags': [],
    'metadata': {},
    'parent_ids': []
}
</code></pre>
<p><strong>事件分类</strong>：纵向按生命周期分为 <code>on_xxx_start</code>（开始）、<code>on_xxx_stream</code>（中间结果）、<code>on_xxx_end</code>（结束）；横向按 <code>Runnable</code> 类型分为以下事件：</p>
<table>
<thead>
<tr>
<th style="text-align:left">组件类型</th>
<th style="text-align:left">事件前缀</th>
<th style="text-align:left">name 示例</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">模型</td>
<td style="text-align:left"><code>on_chat_model_*</code> / <code>on_llm_*</code></td>
<td style="text-align:left"><code>'[model name]'</code></td>
</tr>
<tr>
<td style="text-align:left">工具</td>
<td style="text-align:left"><code>on_tool_*</code>（仅 start/end）</td>
<td style="text-align:left"><code>'some_tool'</code></td>
</tr>
<tr>
<td style="text-align:left">检索器</td>
<td style="text-align:left"><code>on_retriever_*</code></td>
<td style="text-align:left"><code>'[retriever name]'</code></td>
</tr>
<tr>
<td style="text-align:left">提示词模板</td>
<td style="text-align:left"><code>on_prompt_*</code></td>
<td style="text-align:left"><code>'[template_name]'</code></td>
</tr>
<tr>
<td style="text-align:left">通用（含编译图、普通节点）</td>
<td style="text-align:left"><code>on_chain_*</code></td>
<td style="text-align:left"><code>'format_docs'</code> 等</td>
</tr>
</tbody>
</table>
<p><strong>关键字段</strong>：</p>
<table>
<thead>
<tr>
<th style="text-align:left">字段</th>
<th style="text-align:left">含义</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><code>event</code></td>
<td style="text-align:left">事件类型</td>
</tr>
<tr>
<td style="text-align:left"><code>run_id</code></td>
<td style="text-align:left">当前 Runnable 运行实例的唯一 ID</td>
</tr>
<tr>
<td style="text-align:left"><code>name</code></td>
<td style="text-align:left">运行实例名称</td>
</tr>
<tr>
<td style="text-align:left"><code>metadata</code></td>
<td style="text-align:left">运行相关元数据</td>
</tr>
<tr>
<td style="text-align:left"><code>data</code></td>
<td style="text-align:left">事件输入/输出/中间结果</td>
</tr>
<tr>
<td style="text-align:left"><code>parent_ids</code></td>
<td style="text-align:left">从根运行实例到父运行实例的 ID 链（最外层 LangGraph 为空，子节点包含父 run_id）</td>
</tr>
</tbody>
</table>
<h1 id="12-子图">12. 子图</h1>
<p>状态图节点中调用另外的状态图或直接将另外的状态图作为其节点则后者为子图。</p>
<p><strong>注意：</strong></p>
<p>为节点命名时特别注意避开 <strong><code>Mermaid</code></strong> 关键字，否则拓扑结构渲染可能失败，常见的 <strong><code>Mermaid</code></strong> 关键字如下</p>
<pre><code class="language-python">subgraph
end
graph
flowchart
classDef
</code></pre>
<h2 id="12-1-两种子图嵌入方式">12.1. 两种子图嵌入方式</h2>
<table>
<thead>
<tr>
<th style="text-align:left">方式</th>
<th style="text-align:left">用法</th>
<th style="text-align:left">适用场景</th>
<th style="text-align:left">通信方式</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong>节点函数中调用子图</strong></td>
<td style="text-align:left">在节点函数内 <code>subgraph.invoke()</code></td>
<td style="text-align:left">父子图状态完全隔离</td>
<td style="text-align:left">手动做输入输出映射</td>
</tr>
<tr>
<td style="text-align:left"><strong>子图直接作为父图节点</strong></td>
<td style="text-align:left"><code>add_node(&quot;name&quot;, compiled_subgraph)</code></td>
<td style="text-align:left">父子图共享状态字段</td>
<td style="text-align:left">通过共享字段自动通信</td>
</tr>
</tbody>
</table>
<h3 id="12-1-1-在节点函数中调用子图">12.1.1. 在节点函数中调用子图</h3>
<p>如果父图和子图状态完全隔离，可以用这种方式。</p>
<h4 id="12-1-1-1-用法">12.1.1.1. 用法</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END

# 构建子图
class SubgraphState(TypedDict):
    raw_text: str # 未清洗文本
    stripped_text: str # 去除收尾空格的文本
    punctuated_text: str # 句尾添加句号的文本

def subgraph_strip_node(state: SubgraphState) -&gt; SubgraphState:
    raw_text = state[&quot;raw_text&quot;]
    stripped_text = raw_text.strip()

    return {
        &quot;stripped_text&quot;: stripped_text
    }

def subgraph_punctuate_node(state: SubgraphState) -&gt; SubgraphState:
    stripped_text = state[&quot;stripped_text&quot;]
    punctuated_text = stripped_text + &quot;。&quot;

    return {
        &quot;punctuated_text&quot;: punctuated_text
    }

builder = StateGraph(state_schema=SubgraphState)
builder.add_node(&quot;subgraph_strip_node&quot;, subgraph_strip_node)
builder.add_node(&quot;subgraph_punctuate_node&quot;, subgraph_punctuate_node)

builder.add_edge(START, &quot;subgraph_strip_node&quot;)
builder.add_edge(&quot;subgraph_strip_node&quot;, &quot;subgraph_punctuate_node&quot;)
builder.add_edge(&quot;subgraph_punctuate_node&quot;, END)

subgraph = builder.compile()

# 构建父图
class ParentState(TypedDict):
    input_text: str # 输入的未清洗的文本
    cleaned_text: str # 清洗后的文本

def call_subgraph(state: ParentState) -&gt; ParentState:
    input_text = state[&quot;input_text&quot;]

    res = subgraph.invoke({&quot;raw_text&quot;: input_text})
    cleaned_text = res[&quot;punctuated_text&quot;]
    return {
        &quot;cleaned_text&quot;: cleaned_text
    }

builder = StateGraph(state_schema=ParentState)
builder.add_node(&quot;call_subgraph&quot;, call_subgraph)

builder.add_edge(START, &quot;call_subgraph&quot;)
builder.add_edge(&quot;call_subgraph&quot;, END)

parent_graph = builder.compile()

input_text = &quot;   LangGraph 真有意思        &quot;
res = parent_graph.invoke({&quot;input_text&quot;: input_text})
cleaned_text = res[&quot;cleaned_text&quot;]
print(&quot;=&quot; * 30, &quot;-&gt;   原始文本   &lt;-&quot;, &quot;=&quot; * 30)
print(input_text)
print(&quot;=&quot; * 30, &quot;-&gt; 清洗后的文本 &lt;-&quot;, &quot;=&quot; * 30)
print(cleaned_text)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p>调用 <strong><code>get_graph()</code></strong> 时传递的 <strong><code>xray=True</code></strong> 表示自动解析并递归获取子图的拓扑结构。</p>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">============================== -&gt;   原始文本   &lt;- ==============================
   LangGraph 真有意思
============================== -&gt; 清洗后的文本 &lt;- ==============================
LangGraph 真有意思。
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625084145735.png" alt="image-20260625084145735" /></p>
<p><strong>注意：</strong></p>
<p>当前方式的子图信息依赖于 <strong><code>LangGraph</code></strong> 对于节点函数代码的解析，解析的关键代码如下</p>
<pre><code class="language-python">res = subgraph.invoke({&quot;raw_text&quot;: input_text})
</code></pre>
<p>这是推荐的写法，调用子图时最好不要链式调用，否则解析失败，<strong><code>LangGraph</code></strong> 可能无法感知子图，这不仅会导致 <strong><code>display</code></strong> 渲染时不包含子图的拓扑结构，还会导致启用检查点存储器时，无法查看子图检查点快照。</p>
<p><strong>错误写法示例如下</strong></p>
<p>当前案例看不到效果，下文的 <strong><code>Multi-turn</code></strong> 案例能看到差别</p>
<pre><code class="language-python">assistant_response = subgraph.invoke(
    {&quot;messages&quot;: messages}
)[&quot;messages&quot;][-1].content
</code></pre>
<p>此时，<strong><code>LangGraph</code></strong> 无法感知这里的子图 <strong><code>subgraph</code></strong></p>
<p><strong>正确写法如下</strong></p>
<pre><code class="language-python">subgraph_response = subgraph.invoke(
    {&quot;messages&quot;: messages}
)

assistant_response = subgraph_response[&quot;messages&quot;][-1].content
</code></pre>
<h4 id="12-1-1-2-查看子图">12.1.1.2. 查看子图</h4>
<p>在上述代码的基础上执行以下代码</p>
<pre><code class="language-python">list(parent_graph.get_subgraphs())
</code></pre>
<p><strong>结果如下</strong></p>
<pre><code class="language-python">[('call_subgraph',
  &lt;langgraph.graph.state.CompiledStateGraph at 0x19bd9e84ec0&gt;)]
</code></pre>
<h3 id="12-1-2-子图直接作为父图的节点">12.1.2. 子图直接作为父图的节点</h3>
<h4 id="12-1-2-1-用法">12.1.2.1. 用法</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END

# 定义全局共享状态
class OverAllState(TypedDict):
    raw_text: str # 未清洗文本
    cleaned_text: str # 清洗后的文本

# 构建子图
def subgraph_strip_node(state: OverAllState) -&gt; OverAllState:
    raw_text = state[&quot;raw_text&quot;]
    stripped_text = raw_text.strip()

    return {
        &quot;cleaned_text&quot;: stripped_text
    }

def subgraph_punctuate_node(state: OverAllState) -&gt; OverAllState:
    cleaned_text = state[&quot;cleaned_text&quot;]
    punctuated_text = cleaned_text + &quot;。&quot;

    return {
        &quot;cleaned_text&quot;: punctuated_text
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;subgraph_strip_node&quot;, subgraph_strip_node)
builder.add_node(&quot;subgraph_punctuate_node&quot;, subgraph_punctuate_node)

builder.add_edge(START, &quot;subgraph_strip_node&quot;)
builder.add_edge(&quot;subgraph_strip_node&quot;, &quot;subgraph_punctuate_node&quot;)
builder.add_edge(&quot;subgraph_punctuate_node&quot;, END)

subgraph = builder.compile()

# 构建父图
builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;subgraph_node&quot;, subgraph)

builder.add_edge(START, &quot;subgraph_node&quot;)
builder.add_edge(&quot;subgraph_node&quot;, END)

parent_graph = builder.compile()

raw_text = &quot;   LangGraph 真有意思        &quot;
res = parent_graph.invoke({&quot;raw_text&quot;: raw_text})
cleaned_text = res[&quot;cleaned_text&quot;]
print(&quot;=&quot; * 30, &quot;-&gt;   原始文本   &lt;-&quot;, &quot;=&quot; * 30)
print(raw_text)
print(&quot;=&quot; * 30, &quot;-&gt; 清洗后的文本 &lt;-&quot;, &quot;=&quot; * 30)
print(cleaned_text)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">============================== -&gt;   原始文本   &lt;- ==============================
   LangGraph 真有意思
============================== -&gt; 清洗后的文本 &lt;- ==============================
LangGraph 真有意思。
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625085408914.png" alt="image-20260625085408914" /></p>
<h4 id="12-1-2-2-查看子图">12.1.2.2. 查看子图</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">list(parent_graph.get_subgraphs())
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">[('subgraph_node',
  &lt;langgraph.graph.state.CompiledStateGraph at 0x297c50a2520&gt;)]
</code></pre>
<h2 id="12-2-子图持久化">12.2. 子图持久化</h2>
<h3 id="12-2-1-查看子图检查点快照">12.2.1. 查看子图检查点快照</h3>
<p>本节测试都依赖于以下示例</p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver

# 构建子图
class SubgraphState(TypedDict):
    raw_text: str # 未清洗文本
    stripped_text: str # 去除收尾空格的文本
    punctuated_text: str # 句尾添加句号的文本

def subgraph_strip_node(state: SubgraphState) -&gt; SubgraphState:
    raw_text = state[&quot;raw_text&quot;]
    stripped_text = raw_text.strip()

    return {
        &quot;stripped_text&quot;: stripped_text
    }

def subgraph_punctuate_node(state: SubgraphState) -&gt; SubgraphState:
    stripped_text = state[&quot;stripped_text&quot;]
    punctuated_text = stripped_text + &quot;。&quot;

    return {
        &quot;punctuated_text&quot;: punctuated_text
    }

builder = StateGraph(state_schema=SubgraphState)
builder.add_node(&quot;subgraph_strip_node&quot;, subgraph_strip_node)
builder.add_node(&quot;subgraph_punctuate_node&quot;, subgraph_punctuate_node)

builder.add_edge(START, &quot;subgraph_strip_node&quot;)
builder.add_edge(&quot;subgraph_strip_node&quot;, &quot;subgraph_punctuate_node&quot;)
builder.add_edge(&quot;subgraph_punctuate_node&quot;, END)

subgraph = builder.compile()

# 构建父图
class ParentState(TypedDict):
    input_text: str # 输入的未清洗的文本
    cleaned_text: str # 清洗后的文本

def call_subgraph(state: ParentState) -&gt; ParentState:
    input_text = state[&quot;input_text&quot;]

    res = subgraph.invoke({&quot;raw_text&quot;: input_text})
    cleaned_text = res[&quot;punctuated_text&quot;]
    return {
        &quot;cleaned_text&quot;: cleaned_text
    }

builder = StateGraph(state_schema=ParentState)
builder.add_node(&quot;call_subgraph&quot;, call_subgraph)

builder.add_edge(START, &quot;call_subgraph&quot;)
builder.add_edge(&quot;call_subgraph&quot;, END)

checkpointer=InMemorySaver()
parent_graph = builder.compile(checkpointer=checkpointer)

input_text = &quot;   LangGraph 真有意思        &quot;
config={&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;observe_checkpoint_1&quot;}}
res = parent_graph.invoke(
    {&quot;input_text&quot;: input_text},
    config=config
)
cleaned_text = res[&quot;cleaned_text&quot;]
print(&quot;=&quot; * 30, &quot;-&gt;   原始文本   &lt;-&quot;, &quot;=&quot; * 30)
print(input_text)
print(&quot;=&quot; * 30, &quot;-&gt; 清洗后的文本 &lt;-&quot;, &quot;=&quot; * 30)
print(cleaned_text)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">============================== -&gt;   原始文本   &lt;- ==============================
   LangGraph 真有意思
============================== -&gt; 清洗后的文本 &lt;- ==============================
LangGraph 真有意思。
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625085620495.png" alt="image-20260625085620495" /></p>
<h4 id="12-2-1-1-在父图检查点快照中查看子图信息">12.2.1.1. 在父图检查点快照中查看子图信息</h4>
<p><strong>获取父图历史检查点列表</strong></p>
<pre><code class="language-python">histories = list(parent_graph.get_state_history(config=config))
histories
</code></pre>
<p><strong>结果如下</strong></p>
<pre><code class="language-python">[
    StateSnapshot(
        values={
            'input_text': '   LangGraph 真有意思        ',
            'cleaned_text': 'LangGraph 真有意思。'
        },
        next=(),
        config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16fd86-3406-6072-8001-1e8b9691a9cf'
            }
        },
        metadata={
            'source': 'loop',
            'step': 1,
            'parents': {}
        },
        created_at='2026-06-24T14:24:19.397838+00:00',
        parent_config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16fd86-33fb-6847-8000-8914ff1be716'
            }
        },
        tasks=(),
        interrupts=()
    ),
    StateSnapshot(
        values={
            'input_text': '   LangGraph 真有意思        '
        },
        next=(
            'call_subgraph',
        ),
        config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16fd86-33fb-6847-8000-8914ff1be716'
            }
        },
        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {}
        },
        created_at='2026-06-24T14:24:19.393533+00:00',
        parent_config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16fd86-33fa-61a4-bfff-538c9fb00687'
            }
        },
        tasks=(
            PregelTask(
                id='9923d4be-9845-576d-785f-5b1d6da9a94f',
                name='call_subgraph',
                path=(
                    '__pregel_pull',
                    'call_subgraph'
                ),
                error=None,
                interrupts=(),
                state={
                    'configurable': {
                        'thread_id': 'observe_checkpoint_1',
                        'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f'
                    }
                },
                result={
                    'cleaned_text': 'LangGraph 真有意思。'
                }
            ),
        ),
        interrupts=()
    ),
    StateSnapshot(
        values={},
        next=(
            '__start__',
        ),
        config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16fd86-33fa-61a4-bfff-538c9fb00687'
            }
        },
        metadata={
            'source': 'input',
            'step': -1,
            'parents': {}
        },
        created_at='2026-06-24T14:24:19.392958+00:00',
        parent_config=None,
        tasks=(
            PregelTask(
                id='1b333b41-1e2d-1770-8dab-a09a47002aef',
                name='__start__',
                path=(
                    '__pregel_pull',
                    '__start__'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'input_text': '   LangGraph 真有意思        '
                }
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<p>可以看到，超步为 <strong><code>0</code></strong> 的快照下的 <strong><code>tasks</code></strong> 字段下只有一个任务实例，该任务实例在编号为 <strong><code>1</code></strong> 的超步执行，其中的 <strong><code>state</code></strong> 字段记录了子图检查点的 <strong><code>thread_id</code></strong> 和 <strong><code>checkpoint_ns</code></strong>，如下所示：</p>
<pre><code class="language-python">state={
    'configurable': {
        'thread_id': 'observe_checkpoint_1',
        'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f'
    }
}
</code></pre>
<ul>
<li>
<p><strong><code>thread_id</code></strong>：整个线程的唯一 <strong><code>ID</code></strong>，和父图一致</p>
</li>
<li>
<p><strong><code>checkpoint_ns</code></strong>：子图命名空间，格式为</p>
<pre><code class="language-python">&lt;子图所属父图节点名称&gt;:&lt;子图所属父图任务ID&gt;
</code></pre>
<p>我们在介绍 <strong><code>LangGraph</code></strong> 中提到，父图的 <strong><code>checkpoint_ns</code></strong> 通常为空字符串，在子图嵌套系统中该字段排上了用场：</p>
<ul>
<li>父图的 <strong><code>checkpoint_ns</code></strong> 仍未空字符串</li>
<li>子图的 <strong><code>checkpoint_ns</code></strong> 遵循以上格式</li>
</ul>
<p>当嵌套层级不止一层时，命名空间将会通过 <strong><code>|</code></strong> 拼接，具体来说，假设拓扑结构如下：</p>
<pre><code class="language-python">父图
└── call_subgraph          # 调用子图 A
    └── call_inner_graph   # 子图 A 调用子图 B
</code></pre>
<p>那么命名空间为</p>
<pre><code class="language-python">父图：
&quot;&quot;

子图 A：
call_subgraph:&lt;task_id_1&gt;

子图 B：
call_subgraph:&lt;task_id_1&gt;|call_inner_graph:&lt;task_id_2&gt;
</code></pre>
<p>依次类推。</p>
</li>
</ul>
<h4 id="12-2-1-2-在父图检查点快照中展开子图最新快照">12.2.1.2. 在父图检查点快照中展开子图最新快照</h4>
<p>我们可以在父图的 <strong><code>state</code></strong> 字段中展开子图快照，也就是将 <strong><code>state</code></strong> 字段的值替换为子图的最新检查点快照</p>
<h5 id="12-2-1-2-1-获取父图检查点配置">12.2.1.2.1. 获取父图检查点配置</h5>
<p>超步为 <strong><code>0</code></strong> 的检查点记录了子图的配置，所以我们要获取这个快照的父图配置，观察检查点快照列表可知：快照列表按照超步编号降序排列，且最小超步为 <strong><code>-1</code></strong>，取列表的倒数第二个元素即可拿到超步编号为 <strong><code>0</code></strong> 的检查点快照。</p>
<p><strong>代码如下</strong></p>
<pre><code class="language-python">parent_config = histories[-2].config
parent_config
</code></pre>
<p><strong>结果如下</strong></p>
<pre><code class="language-python">{
  'configurable': {
    'thread_id': 'observe_checkpoint_1',
    'checkpoint_ns': '',
    'checkpoint_id': '1f16fd86-33fb-6847-8000-8914ff1be716'
  }
}
</code></pre>
<h5 id="12-2-1-2-2-展开子图快照">12.2.1.2.2. 展开子图快照</h5>
<p>在调用 <strong><code>get_state()</code></strong> 时传递带有 <strong><code>checkpoint_id</code></strong> 的父图配置，并传递 <strong><code>subgraphs=True</code></strong> 即可展开子图快照。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">parent_graph.get_state(config=parent_config, subgraphs=True)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">StateSnapshot(
  values={
    'input_text': '   LangGraph 真有意思        '
  },
  next=(
    'call_subgraph',
  ),
  config={
    'configurable': {
      'thread_id': 'observe_checkpoint_1',
      'checkpoint_ns': '',
      'checkpoint_id': '1f16fd86-33fb-6847-8000-8914ff1be716'
    }
  },
  metadata={
    'source': 'loop',
    'step': 0,
    'parents': {}
  },
  created_at='2026-06-24T14:24:19.393533+00:00',
  parent_config={
    'configurable': {
      'thread_id': 'observe_checkpoint_1',
      'checkpoint_ns': '',
      'checkpoint_id': '1f16fd86-33fa-61a4-bfff-538c9fb00687'
    }
  },
  tasks=(
    PregelTask(
      id='9923d4be-9845-576d-785f-5b1d6da9a94f',
      name='call_subgraph',
      path=(
        '__pregel_pull',
        'call_subgraph'
      ),
      error=None,
      interrupts=(),
      state=StateSnapshot(
        values={
          'raw_text': '   LangGraph 真有意思        ',
          'stripped_text': 'LangGraph 真有意思',
          'punctuated_text': 'LangGraph 真有意思。'
        },
        next=(),
        config={
          'configurable': {
            'thread_id': 'observe_checkpoint_1',
            'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f',
            'checkpoint_id': '1f16fd86-3404-640e-8002-84e2d98a88dc',
            'checkpoint_map': {
              '': '1f16fd86-33fb-6847-8000-8914ff1be716',
              'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f': '1f16fd86-3404-640e-8002-84e2d98a88dc'
            }
          }
        },
        metadata={
          'source': 'loop',
          'step': 2,
          'parents': {
            '': '1f16fd86-33fb-6847-8000-8914ff1be716'
          }
        },
        created_at='2026-06-24T14:24:19.397114+00:00',
        parent_config={
          'configurable': {
            'thread_id': 'observe_checkpoint_1',
            'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f',
            'checkpoint_id': '1f16fd86-3401-694f-8001-2c6a2666b501',
            'checkpoint_map': {
              '': '1f16fd86-33fb-6847-8000-8914ff1be716',
              'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f': '1f16fd86-3401-694f-8001-2c6a2666b501'
            }
          }
        },
        tasks=(),
        interrupts=()
      ),
      result={
        'cleaned_text': 'LangGraph 真有意思。'
      }
    ),
  ),
  interrupts=()
)
</code></pre>
<p>可以看到，<strong><code>state</code></strong> 字段已展开为子图的最新检查点快照。</p>
<h4 id="12-2-1-3-获取子图配置-查看完整子图检查点列表">12.2.1.3. 获取子图配置，查看完整子图检查点列表</h4>
<p>某些场景下，我们希望查看完整的子图检查点快照列表，此时只需要将包含 <strong><code>thread_id</code></strong> 和子图命名空间 <strong><code>checkpoint_ns</code></strong> 的配置信息传递给 <strong><code>get_state_history()</code></strong> 即可实现。</p>
<h5 id="12-2-1-3-1-获取子图检查点配置">12.2.1.3.1. 获取子图检查点配置</h5>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">subgraph_config = histories[-2].tasks[0].state
subgraph_config
</code></pre>
<p><strong>结果如下</strong></p>
<pre><code class="language-python">{
  'configurable': {
    'thread_id': 'observe_checkpoint_1',
    'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f'
  }
}
</code></pre>
<h5 id="12-2-1-3-2-查看完整子图检查点快照列表">12.2.1.3.2. 查看完整子图检查点快照列表</h5>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">list(parent_graph.get_state_history(config=subgraph_config))
</code></pre>
<p><strong>结果如下</strong></p>
<pre><code class="language-python">[
    StateSnapshot(
        values={
            'raw_text': '   LangGraph 真有意思        ',
            'stripped_text': 'LangGraph 真有意思',
            'punctuated_text': 'LangGraph 真有意思。'
        },
        next=(),
        config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f',
                'checkpoint_id': '1f16fd86-3404-640e-8002-84e2d98a88dc',
                'checkpoint_map': {
                    '': '1f16fd86-33fb-6847-8000-8914ff1be716',
                    'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f': '1f16fd86-3404-640e-8002-84e2d98a88dc'
                }
            }
        },
        metadata={
            'source': 'loop',
            'step': 2,
            'parents': {
                '': '1f16fd86-33fb-6847-8000-8914ff1be716'
            }
        },
        created_at='2026-06-24T14:24:19.397114+00:00',
        parent_config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f',
                'checkpoint_id': '1f16fd86-3401-694f-8001-2c6a2666b501',
                'checkpoint_map': {
                    '': '1f16fd86-33fb-6847-8000-8914ff1be716',
                    'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f': '1f16fd86-3401-694f-8001-2c6a2666b501'
                }
            }
        },
        tasks=(),
        interrupts=()
    ),
    StateSnapshot(
        values={
            'raw_text': '   LangGraph 真有意思        ',
            'stripped_text': 'LangGraph 真有意思'
        },
        next=(
            'subgraph_punctuate_node',
        ),
        config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f',
                'checkpoint_id': '1f16fd86-3401-694f-8001-2c6a2666b501',
                'checkpoint_map': {
                    '': '1f16fd86-33fb-6847-8000-8914ff1be716',
                    'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f': '1f16fd86-3401-694f-8001-2c6a2666b501'
                }
            }
        },
        metadata={
            'source': 'loop',
            'step': 1,
            'parents': {
                '': '1f16fd86-33fb-6847-8000-8914ff1be716'
            }
        },
        created_at='2026-06-24T14:24:19.396020+00:00',
        parent_config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f',
                'checkpoint_id': '1f16fd86-33ff-6ad9-8000-0fb11208046d',
                'checkpoint_map': {
                    '': '1f16fd86-33fb-6847-8000-8914ff1be716',
                    'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f': '1f16fd86-33ff-6ad9-8000-0fb11208046d'
                }
            }
        },
        tasks=(
            PregelTask(
                id='d37272d8-d167-4fb9-6e31-466a58bf91cc',
                name='subgraph_punctuate_node',
                path=(
                    '__pregel_pull',
                    'subgraph_punctuate_node'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'punctuated_text': 'LangGraph 真有意思。'
                }
            ),
        ),
        interrupts=()
    ),
    StateSnapshot(
        values={
            'raw_text': '   LangGraph 真有意思        '
        },
        next=(
            'subgraph_strip_node',
        ),
        config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f',
                'checkpoint_id': '1f16fd86-33ff-6ad9-8000-0fb11208046d',
                'checkpoint_map': {
                    '': '1f16fd86-33fb-6847-8000-8914ff1be716',
                    'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f': '1f16fd86-33ff-6ad9-8000-0fb11208046d'
                }
            }
        },
        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {
                '': '1f16fd86-33fb-6847-8000-8914ff1be716'
            }
        },
        created_at='2026-06-24T14:24:19.395239+00:00',
        parent_config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f',
                'checkpoint_id': '1f16fd86-33fe-685b-bfff-d0a73977e807',
                'checkpoint_map': {
                    '': '1f16fd86-33fb-6847-8000-8914ff1be716',
                    'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f': '1f16fd86-33fe-685b-bfff-d0a73977e807'
                }
            }
        },
        tasks=(
            PregelTask(
                id='9d9a1009-3212-b789-f40a-3d8f213ce4fb',
                name='subgraph_strip_node',
                path=(
                    '__pregel_pull',
                    'subgraph_strip_node'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'stripped_text': 'LangGraph 真有意思'
                }
            ),
        ),
        interrupts=()
    ),
    StateSnapshot(
        values={},
        next=(
            '__start__',
        ),
        config={
            'configurable': {
                'thread_id': 'observe_checkpoint_1',
                'checkpoint_ns': 'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f',
                'checkpoint_id': '1f16fd86-33fe-685b-bfff-d0a73977e807',
                'checkpoint_map': {
                    '': '1f16fd86-33fb-6847-8000-8914ff1be716',
                    'call_subgraph:9923d4be-9845-576d-785f-5b1d6da9a94f': '1f16fd86-33fe-685b-bfff-d0a73977e807'
                }
            }
        },
        metadata={
            'source': 'input',
            'step': -1,
            'parents': {
                '': '1f16fd86-33fb-6847-8000-8914ff1be716'
            }
        },
        created_at='2026-06-24T14:24:19.394769+00:00',
        parent_config=None,
        tasks=(
            PregelTask(
                id='5516615f-62ca-2dc4-d41e-6a8f20a981bb',
                name='__start__',
                path=(
                    '__pregel_pull',
                    '__start__'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'raw_text': '   LangGraph 真有意思        '
                }
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<ul>
<li>
<p>上述快照的命名空间都不是空字符串，且保持一致，全部属于同一个子图</p>
</li>
<li>
<p><strong><code>metadata['parents']</code></strong> 记录了父检查点的信息，格式为</p>
<p><strong><code>&lt;父检查点命名空间&gt;:&lt;父检查点ID&gt;</code></strong></p>
</li>
<li>
<p><strong><code>config['configurable']['checkpoint_map']</code></strong> 记录了父图和当前子图检查点命名空间和 <strong><code>ID</code></strong> 的映射关系，包含两个条目，格式如下</p>
<pre><code class="language-python">&lt;父图检查点命名空间&gt;:&lt;父图检查点ID&gt;,
&lt;当前子图检查点命名空间&gt;:&lt;当前子图检查点ID&gt;
</code></pre>
</li>
</ul>
<h3 id="12-2-2-持久化策略">12.2.2. 持久化策略</h3>
<p>本节讨论的持久化策略以<strong>父图启用检查点存储器</strong>为前提。子图支持三种策略：</p>
<table>
<thead>
<tr>
<th style="text-align:left">策略</th>
<th style="text-align:left">编译参数</th>
<th style="text-align:left">检查点保存</th>
<th style="text-align:left">中断恢复</th>
<th style="text-align:left">多轮记忆</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>Per-invocation</code></strong>（默认）</td>
<td style="text-align:left"><code>checkpointer=None</code> 或省略</td>
<td style="text-align:left">✓</td>
<td style="text-align:left">✓</td>
<td style="text-align:left">✗（同 thread_id 再次调用不加载历史）</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>Per-thread</code></strong></td>
<td style="text-align:left"><code>checkpointer=True</code></td>
<td style="text-align:left">✓</td>
<td style="text-align:left">✓</td>
<td style="text-align:left">✓（同 thread_id 调用加载历史）</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>Stateless</code></strong></td>
<td style="text-align:left"><code>checkpointer=False</code></td>
<td style="text-align:left">✗</td>
<td style="text-align:left">✗</td>
<td style="text-align:left">✗</td>
</tr>
</tbody>
</table>
<blockquote>
<p><strong>三种策略的唯一区别在于子图编译时的 <code>checkpointer</code> 参数</strong>，其余代码完全相同。下面按场景组织，每个场景展示一份完整代码，策略切换只需修改一行。</p>
</blockquote>
<h4 id="12-2-2-1-场景一-中断-interrupt">12.2.2.1. 场景一：中断（interrupt）</h4>
<p>子图节点中使用 <code>interrupt()</code> 中断执行，等待用户输入后恢复。</p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.types import interrupt, Command
from langgraph.checkpoint.memory import InMemorySaver

from loguru import logger

# 定义全局共享状态
class OverAllState(TypedDict):
    raw_text: str # 未清洗文本
    cleaned_text: str # 清洗后的文本

# 构建子图
def subgraph_strip_node(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;子图 subgraph_strip_node 节点执行了&quot;)
    raw_text = state[&quot;raw_text&quot;]
    stripped_text = raw_text.strip()

    return {
        &quot;cleaned_text&quot;: stripped_text
    }

def subgraph_punctuate_node(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;子图 subgraph_punctuate_node 节点执行了&quot;)
    cleaned_text = state[&quot;cleaned_text&quot;]
    punctuation = interrupt(&quot;您希望在句尾添加的标点符号是？[。/，/！/？/；]&quot;)
    punctuation1 = interrupt(&quot;您希望在句尾添加的标点符号是？[。/，/！/？/；]&quot;)
    punctuated_text = cleaned_text + punctuation + punctuation1

    return {
        &quot;cleaned_text&quot;: punctuated_text
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;subgraph_strip_node&quot;, subgraph_strip_node)
builder.add_node(&quot;subgraph_punctuate_node&quot;, subgraph_punctuate_node)

builder.add_edge(START, &quot;subgraph_strip_node&quot;)
builder.add_edge(&quot;subgraph_strip_node&quot;, &quot;subgraph_punctuate_node&quot;)
builder.add_edge(&quot;subgraph_punctuate_node&quot;, END)

# ==================================================
# 【策略切换点】只需修改这一行
# subgraph = builder.compile()  # Per-invocation（默认）
# subgraph = builder.compile(checkpointer=True)   # Per-thread
subgraph = builder.compile(checkpointer=False)  # Stateless（中断不可用）
# ==================================================

# 构建父图
builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;subgraph_node&quot;, subgraph)

builder.add_edge(START, &quot;subgraph_node&quot;)
builder.add_edge(&quot;subgraph_node&quot;, END)

checkpointer = InMemorySaver()
parent_graph = builder.compile(checkpointer=checkpointer)

raw_text = &quot;   LangGraph 真有意思        &quot;
config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
# 首次调用
interrupted_res = parent_graph.invoke(
    {&quot;raw_text&quot;: raw_text},
    config=config
)
print(&quot;=&quot; * 30, &quot;-&gt;   中断信息   &lt;-&quot;, &quot;=&quot; * 30)
print(interrupted_res)


from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)

# 恢复调用
parent_graph.invoke(
    Command(resume=&quot;！&quot;),
    config=config
)
res = parent_graph.invoke(
    Command(resume=&quot;！&quot;),
    config=config
)

cleaned_text = res[&quot;cleaned_text&quot;]
print(&quot;=&quot; * 30, &quot;-&gt;   原始文本   &lt;-&quot;, &quot;=&quot; * 30)
print(raw_text)
print(&quot;=&quot; * 30, &quot;-&gt; 清洗后的文本 &lt;-&quot;, &quot;=&quot; * 30)
print(cleaned_text)
</code></pre>
<p><strong>运行结果（Per-invocation / Per-thread 均可）</strong></p>
<pre><code class="language-python">2026-06-25 09:41:40.629 | INFO     | __main__:subgraph_strip_node:15 - 子图 subgraph_strip_node 节点执行了
2026-06-25 09:41:40.630 | INFO     | __main__:subgraph_punctuate_node:24 - 子图 subgraph_punctuate_node 节点执行了
============================== -&gt;   中断信息   &lt;- ==============================
{'raw_text': '   LangGraph 真有意思        ', '__interrupt__': [Interrupt(value='您希望在句尾添加的标点符号是？[。/，/！/？/；]', id='a4c6479870430be1a857af43cc7eff99')]}
2026-06-25 09:41:40.633 | INFO     | __main__:subgraph_punctuate_node:24 - 子图 subgraph_punctuate_node 节点执行了
============================== -&gt;   原始文本   &lt;- ==============================
   LangGraph 真有意思
============================== -&gt; 清洗后的文本 &lt;- ==============================
LangGraph 真有意思！
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625094209973.png" alt="image-20260625094209973" /></p>
<blockquote>
<p><strong>策略差异</strong>：三种策略下中断机制行为一致，<code>Per-invocation</code> 和 <code>Per-thread</code> 均支持中断恢复，<code>Stateless</code> 不支持中断。</p>
</blockquote>
<table>
<thead>
<tr>
<th style="text-align:left">策略</th>
<th style="text-align:left">编译参数</th>
<th style="text-align:left">中断</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">Per-invocation（默认）</td>
<td style="text-align:left"><code>subgraph = builder.compile()</code></td>
<td style="text-align:left">✅</td>
</tr>
<tr>
<td style="text-align:left">Per-thread</td>
<td style="text-align:left"><code>subgraph = builder.compile(checkpointer=True)</code></td>
<td style="text-align:left">✅</td>
</tr>
<tr>
<td style="text-align:left">Stateless</td>
<td style="text-align:left"><code>subgraph = builder.compile(checkpointer=False)</code></td>
<td style="text-align:left">❌</td>
</tr>
</tbody>
</table>
<h4 id="12-2-2-2-场景二-多轮对话">12.2.2.2. 场景二：多轮对话</h4>
<p>父图多次调用子图进行 LLM 对话，验证子图是否保留历史消息。</p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END, MessagesState
from langgraph.checkpoint.memory import InMemorySaver

from langchain.messages import HumanMessage, SystemMessage, AIMessage
from langchain_deepseek import ChatDeepSeek

import sys
from loguru import logger
# 删除 Loguru 默认的 stderr sink
logger.remove()
# 重新添加支持颜色的 sink
logger.add(sys.stdout, colorize=True)

from dotenv import load_dotenv

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 构建子图
def llm_node(state: MessagesState) -&gt; MessagesState:
    messages = state[&quot;messages&quot;]
    logger.info(&quot;=&quot; * 30)
    logger.info(&quot;调用子图的 llm_node 节点，当前的 messages: &quot;)
    for index, message in enumerate(messages, start=1):
        logger.opt(colors=True).info(
            &quot;\n&lt;cyan&gt;&lt;bold&gt;[消息 {}]&lt;/bold&gt;&lt;/cyan&gt;\n&quot;
            &quot;&lt;yellow&gt;类型：&lt;/yellow&gt;&lt;magenta&gt;{}&lt;/magenta&gt;\n&quot;
            &quot;&lt;yellow&gt;内容：&lt;/yellow&gt;&lt;green&gt;{}&lt;/green&gt;&quot;,
            index,
            message.type,
            message.content
        )
    logger.info(&quot;=&quot; * 30)

    response = model.invoke(input=messages)
    ai_msg = AIMessage(content=response.content)

    return {
        &quot;messages&quot;: [ai_msg]
    }

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, END)

# ==================================================
# 【策略切换点】只需修改这一行
subgraph = builder.compile()  # Per-invocation（默认）：无记忆
# subgraph = builder.compile(checkpointer=True)   # Per-thread：有记忆
# ==================================================

# 构建父图
class OverAllState(TypedDict):
    user_input: str # 用户提问
    assistant_response: str # 助手回答

def call_subgraph(state: OverAllState) -&gt; OverAllState:
    user_input = state[&quot;user_input&quot;]

    messages = [
        SystemMessage(&quot;尽可能用简短的语言回答&quot;),
        HumanMessage(user_input)
    ]
    subgraph_response = subgraph.invoke(
        {&quot;messages&quot;: messages}
    )

    assistant_response = subgraph_response[&quot;messages&quot;][-1].content

    return {
        &quot;assistant_response&quot;: assistant_response
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;call_subgraph&quot;, call_subgraph)
builder.add_edge(START, &quot;call_subgraph&quot;)
builder.add_edge(&quot;call_subgraph&quot;, END)

checkpointer = InMemorySaver()
parent_graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;multi-turn-demo&quot;}}

first_invoke = parent_graph.invoke(
    {&quot;user_input&quot;: &quot;我是老王，从现在开始，你是小王&quot;},
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第一次调用 &lt;-&quot;, &quot;=&quot; * 30)
print(first_invoke)

second_invoke = parent_graph.invoke(
    {&quot;user_input&quot;: &quot;我是谁？你是谁？&quot;},
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第二次调用 &lt;-&quot;, &quot;=&quot; * 30)
print(second_invoke)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p><strong>Per-invocation 运行结果（无记忆）</strong></p>
<pre><code>2026-06-25 09:43:14.012 | INFO     | __main__:llm_node:31 - ==============================
2026-06-25 09:43:14.013 | INFO     | __main__:llm_node:32 - 调用子图的 llm_node 节点，当前的 messages:
2026-06-25 09:43:14.014 | INFO     | __main__:llm_node:34 -
[消息 1]
类型：system
内容：尽可能用简短的语言回答
2026-06-25 09:43:14.014 | INFO     | __main__:llm_node:34 -
[消息 2]
类型：human
内容：我是老王，从现在开始，你是小王
2026-06-25 09:43:14.015 | INFO     | __main__:llm_node:42 - ==============================
============================== -&gt; 第一次调用 &lt;- ==============================
{'user_input': '我是老王，从现在开始，你是小王', 'assistant_response': '好的，老王。我是小王，您有什么吩咐？'}
2026-06-25 09:43:14.814 | INFO     | __main__:llm_node:31 - ==============================
2026-06-25 09:43:14.815 | INFO     | __main__:llm_node:32 - 调用子图的 llm_node 节点，当前的 messages:
2026-06-25 09:43:14.815 | INFO     | __main__:llm_node:34 -
[消息 1]
类型：system
内容：尽可能用简短的语言回答
2026-06-25 09:43:14.816 | INFO     | __main__:llm_node:34 -
[消息 2]
类型：human
内容：我是谁？你是谁？
2026-06-25 09:43:14.816 | INFO     | __main__:llm_node:42 - ==============================
============================== -&gt; 第二次调用 &lt;- ==============================
{'user_input': '我是谁？你是谁？', 'assistant_response': '你是用户，我是DeepSeek，一个由深度求索公司开发的AI助手。'}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625094339255.png" alt="image-20260625094339255" /></p>
<p>第二次调用时子图 <code>messages</code> 被重置，无法获知第一次调用时传递的信息。<strong>Per-invocation 模式无法在子图中进行多轮对话。</strong></p>
<p><strong>Per-thread 运行结果（有记忆）</strong>——仅子图编译改为 <code>builder.compile(checkpointer=True)</code>：</p>
<pre><code>2026-06-25 10:42:37.121 | INFO     | __main__:llm_node:31 - ==============================
2026-06-25 10:42:37.122 | INFO     | __main__:llm_node:32 - 调用子图的 llm_node 节点，当前的 messages:
2026-06-25 10:42:37.123 | INFO     | __main__:llm_node:34 -
[消息 1]
类型：system
内容：尽可能用简短的语言回答
2026-06-25 10:42:37.124 | INFO     | __main__:llm_node:34 -
[消息 2]
类型：human
内容：我是老王，从现在开始，你是小王
2026-06-25 10:42:37.124 | INFO     | __main__:llm_node:42 - ==============================
============================== -&gt; 第一次调用 &lt;- ==============================
{'user_input': '我是老王，从现在开始，你是小王', 'assistant_response': '（严肃点头）好，老王，有啥指示？'}
2026-06-25 10:42:37.940 | INFO     | __main__:llm_node:31 - ==============================
2026-06-25 10:42:37.941 | INFO     | __main__:llm_node:32 - 调用子图的 llm_node 节点，当前的 messages:
2026-06-25 10:42:37.942 | INFO     | __main__:llm_node:34 -
[消息 1]
类型：system
内容：尽可能用简短的语言回答
2026-06-25 10:42:37.943 | INFO     | __main__:llm_node:34 -
[消息 2]
类型：human
内容：我是老王，从现在开始，你是小王
2026-06-25 10:42:37.944 | INFO     | __main__:llm_node:34 -
[消息 3]
类型：ai
内容：（严肃点头）好，老王，有啥指示？
2026-06-25 10:42:37.945 | INFO     | __main__:llm_node:34 -
[消息 4]
类型：system
内容：尽可能用简短的语言回答
2026-06-25 10:42:37.945 | INFO     | __main__:llm_node:34 -
[消息 5]
类型：human
内容：我是谁？你是谁？
2026-06-25 10:42:37.946 | INFO     | __main__:llm_node:42 - ==============================
============================== -&gt; 第二次调用 &lt;- ==============================
{'user_input': '我是谁？你是谁？', 'assistant_response': '老王。小王。'}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625104256756.png" alt="image-20260625104256756" /></p>
<p>历史消息被保留，上下文连续。<strong>Per-thread 模式适用于子图多轮对话场景。</strong></p>
<blockquote>
<p><strong>策略差异</strong></p>
</blockquote>
<table>
<thead>
<tr>
<th style="text-align:left">策略</th>
<th style="text-align:left">编译参数</th>
<th style="text-align:left">多轮记忆</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">Per-invocation（默认）</td>
<td style="text-align:left"><code>subgraph = builder.compile()</code></td>
<td style="text-align:left">❌ 每次调用状态重置</td>
</tr>
<tr>
<td style="text-align:left">Per-thread</td>
<td style="text-align:left"><code>subgraph = builder.compile(checkpointer=True)</code></td>
<td style="text-align:left">✅ 跨调用状态连续</td>
</tr>
</tbody>
</table>
<h4 id="12-2-2-3-场景三-多次调用同一子图">12.2.2.3. 场景三：多次调用同一子图</h4>
<p>父图的一个节点内循环多次调用同一子图。</p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END, MessagesState
from langgraph.checkpoint.memory import InMemorySaver

from langchain.messages import HumanMessage, SystemMessage, AIMessage
from langchain_deepseek import ChatDeepSeek

import sys
from loguru import logger
logger.remove()
logger.add(sys.stdout, colorize=True)

from dotenv import load_dotenv

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 构建子图
def llm_node(state: MessagesState) -&gt; MessagesState:
    messages = state[&quot;messages&quot;]
    logger.info(&quot;=&quot; * 30)
    logger.info(&quot;调用子图的 llm_node 节点，当前的 messages: &quot;)
    for index, message in enumerate(messages, start=1):
        logger.opt(colors=True).info(
            &quot;\n&lt;cyan&gt;&lt;bold&gt;[消息 {}]&lt;/bold&gt;&lt;/cyan&gt;\n&quot;
            &quot;&lt;yellow&gt;类型：&lt;/yellow&gt;&lt;magenta&gt;{}&lt;/magenta&gt;\n&quot;
            &quot;&lt;yellow&gt;内容：&lt;/yellow&gt;&lt;green&gt;{}&lt;/green&gt;&quot;,
            index,
            message.type,
            message.content
        )
    logger.info(&quot;=&quot; * 30)
    response = model.invoke(input=messages)
    ai_msg = AIMessage(content=response.content)

    return {
        &quot;messages&quot;: [ai_msg]
    }

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, END)

# ==================================================
# 【策略切换点】只需修改这一行
subgraph = builder.compile()  # Per-invocation（默认）：每次调用状态隔离
# subgraph = builder.compile(checkpointer=True)   # Per-thread：历史相互干扰
# ==================================================

# 构建父图
class OverAllState(TypedDict):
    user_inputs: list[str] # 用户提问
    assistant_responses: str # 助手回答

def call_subgraph(state: OverAllState) -&gt; OverAllState:
    user_inputs = state[&quot;user_inputs&quot;]

    assistant_responses = []
    for user_input in user_inputs:
        subgraph_response = subgraph.invoke(
            {
                &quot;messages&quot;: [
                    SystemMessage(&quot;用最简短的话回答用户的提问&quot;),
                    HumanMessage(user_input)
                ]
            }
        )
        assistant_response = subgraph_response[&quot;messages&quot;][-1].content
        assistant_responses.append(assistant_response)

    return {
        &quot;assistant_responses&quot;: assistant_responses
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;call_subgraph&quot;, call_subgraph)
builder.add_edge(START, &quot;call_subgraph&quot;)
builder.add_edge(&quot;call_subgraph&quot;, END)

checkpointer = InMemorySaver()
parent_graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;multi-call-same-demo&quot;}}

first_response = parent_graph.invoke(
    {
        &quot;user_inputs&quot;: [
            &quot;5*5等于几&quot;,
            &quot;10*10等于几&quot;
        ]
    },
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第一次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
print(first_response)

second_response = parent_graph.invoke(
    {
        &quot;user_inputs&quot;: [
            &quot;再+1呢?&quot;,
            &quot;再+10000呢?&quot;
        ]
    },
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第二次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
print(second_response)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p><strong>Per-thread 运行结果</strong></p>
<pre><code>2026-07-14 11:53:58.056 | INFO     | __main__:llm_node:29 - ==============================
2026-07-14 11:53:58.056 | INFO     | __main__:llm_node:30 - 调用子图的 llm_node 节点，当前的 messages:
2026-07-14 11:53:58.057 | INFO     | __main__:llm_node:32 -
[消息 1]
类型：system
内容：用最简短的话回答用户的提问
2026-07-14 11:53:58.057 | INFO     | __main__:llm_node:32 -
[消息 2]
类型：human
内容：5*5等于几
2026-07-14 11:53:58.058 | INFO     | __main__:llm_node:40 - ==============================
2026-07-14 11:53:58.652 | INFO     | __main__:llm_node:29 - ==============================
2026-07-14 11:53:58.652 | INFO     | __main__:llm_node:30 - 调用子图的 llm_node 节点，当前的 messages:
2026-07-14 11:53:58.653 | INFO     | __main__:llm_node:32 -
[消息 1]
类型：system
内容：用最简短的话回答用户的提问
2026-07-14 11:53:58.653 | INFO     | __main__:llm_node:32 -
[消息 2]
类型：human
内容：10*10等于几
2026-07-14 11:53:58.654 | INFO     | __main__:llm_node:40 - ==============================
============================== -&gt; 第一次运行结果 &lt;- ==============================
{'user_inputs': ['5*5等于几', '10*10等于几'], 'assistant_responses': ['5*5等于25。', '10*10等于100。']}
2026-07-14 11:53:59.230 | INFO     | __main__:llm_node:29 - ==============================
2026-07-14 11:53:59.231 | INFO     | __main__:llm_node:30 - 调用子图的 llm_node 节点，当前的 messages:
2026-07-14 11:53:59.231 | INFO     | __main__:llm_node:32 -
[消息 1]
类型：system
内容：用最简短的话回答用户的提问
2026-07-14 11:53:59.232 | INFO     | __main__:llm_node:32 -
[消息 2]
类型：human
内容：5*5等于几
2026-07-14 11:53:59.233 | INFO     | __main__:llm_node:32 -
[消息 3]
类型：ai
内容：5*5等于25。
2026-07-14 11:53:59.234 | INFO     | __main__:llm_node:32 -
[消息 4]
类型：system
内容：用最简短的话回答用户的提问
2026-07-14 11:53:59.234 | INFO     | __main__:llm_node:32 -
[消息 5]
类型：human
内容：再+1呢?
2026-07-14 11:53:59.235 | INFO     | __main__:llm_node:40 - ==============================
2026-07-14 11:53:59.766 | INFO     | __main__:llm_node:29 - ==============================
2026-07-14 11:53:59.767 | INFO     | __main__:llm_node:30 - 调用子图的 llm_node 节点，当前的 messages:
2026-07-14 11:53:59.767 | INFO     | __main__:llm_node:32 -
[消息 1]
类型：system
内容：用最简短的话回答用户的提问
2026-07-14 11:53:59.768 | INFO     | __main__:llm_node:32 -
[消息 2]
类型：human
内容：10*10等于几
2026-07-14 11:53:59.768 | INFO     | __main__:llm_node:32 -
[消息 3]
类型：ai
内容：10*10等于100。
2026-07-14 11:53:59.769 | INFO     | __main__:llm_node:32 -
[消息 4]
类型：system
内容：用最简短的话回答用户的提问
2026-07-14 11:53:59.769 | INFO     | __main__:llm_node:32 -
[消息 5]
类型：human
内容：再+10000呢?
2026-07-14 11:53:59.769 | INFO     | __main__:llm_node:40 - ==============================
============================== -&gt; 第二次运行结果 &lt;- ==============================
{'user_inputs': ['再+1呢?', '再+10000呢?'], 'assistant_responses': ['25+1=26。', '10100']}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625094551009.png" alt="image-20260625094551009" /></p>
<p>每次调用子图时 <code>messages</code> 状态都是独立的。<strong>Per-invocation 适用于父图中多次调用同一子图的场景，调用间互不干扰。</strong></p>
<p><strong>Per-thread 行为</strong>（改为 <code>builder.compile(checkpointer=True)</code>）：同一节点内的多次子图调用，历史按调用位置独立——即第一次子图调用的历史是连续的，第二次子图调用的历史也是连续的，但两次之间独立。<strong>如果调用顺序发生变化，检查点历史会相互干扰。</strong></p>
<blockquote>
<p><strong>策略差异</strong></p>
</blockquote>
<table>
<thead>
<tr>
<th style="text-align:left">策略</th>
<th style="text-align:left">编译参数</th>
<th style="text-align:left">多次调用同一子图</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">Per-invocation（默认）</td>
<td style="text-align:left"><code>subgraph = builder.compile()</code></td>
<td style="text-align:left">✅ 每次调用完全隔离</td>
</tr>
<tr>
<td style="text-align:left">Per-thread</td>
<td style="text-align:left"><code>subgraph = builder.compile(checkpointer=True)</code></td>
<td style="text-align:left">❌ 历史按位置关联，可能干扰</td>
</tr>
</tbody>
</table>
<h4 id="12-2-2-4-场景四-多次调用不同子图">12.2.2.4. 场景四：多次调用不同子图</h4>
<p>父图的一个节点内调用两个不同的子图。</p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END, MessagesState
from langgraph.checkpoint.memory import InMemorySaver

from langchain.messages import HumanMessage, SystemMessage, AIMessage
from langchain_deepseek import ChatDeepSeek

import sys
from loguru import logger
logger.remove()
logger.add(sys.stdout, colorize=True)

from dotenv import load_dotenv

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# ==================================================
# 【策略切换点】两个子图的编译参数
# ==================================================

# 构建水果子图
def fruit_node(state: MessagesState) -&gt; MessagesState:
    messages = state[&quot;messages&quot;]
    logger.info(&quot;=&quot; * 30)
    logger.info(&quot;调用水果子图的 fruit_node 节点，当前的 messages: &quot;)
    for index, message in enumerate(messages, start=1):
        logger.opt(colors=True).info(
            &quot;\n&lt;cyan&gt;&lt;bold&gt;[消息 {}]&lt;/bold&gt;&lt;/cyan&gt;\n&quot;
            &quot;&lt;yellow&gt;类型：&lt;/yellow&gt;&lt;magenta&gt;{}&lt;/magenta&gt;\n&quot;
            &quot;&lt;yellow&gt;内容：&lt;/yellow&gt;&lt;green&gt;{}&lt;/green&gt;&quot;,
            index,
            message.type,
            message.content
        )
    logger.info(&quot;=&quot; * 30)
    response = model.invoke(input=messages)
    ai_msg = AIMessage(content=response.content)

    return {
        &quot;messages&quot;: [ai_msg]
    }

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;fruit_node&quot;, fruit_node)
builder.add_edge(START, &quot;fruit_node&quot;)
builder.add_edge(&quot;fruit_node&quot;, END)

fruit_subgraph = builder.compile(checkpointer=True)

# 构建蔬菜子图
def vegetable_node(state: MessagesState) -&gt; MessagesState:
    messages = state[&quot;messages&quot;]
    logger.info(&quot;=&quot; * 30)
    logger.info(&quot;调用蔬菜子图的 vegetable_node 节点，当前的 messages: &quot;)
    for index, message in enumerate(messages, start=1):
        logger.opt(colors=True).info(
            &quot;\n&lt;cyan&gt;&lt;bold&gt;[消息 {}]&lt;/bold&gt;&lt;/cyan&gt;\n&quot;
            &quot;&lt;yellow&gt;类型：&lt;/yellow&gt;&lt;magenta&gt;{}&lt;/magenta&gt;\n&quot;
            &quot;&lt;yellow&gt;内容：&lt;/yellow&gt;&lt;green&gt;{}&lt;/green&gt;&quot;,
            index,
            message.type,
            message.content
        )
    logger.info(&quot;=&quot; * 30)
    response = model.invoke(input=messages)
    ai_msg = AIMessage(content=response.content)

    return {
        &quot;messages&quot;: [ai_msg]
    }

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;vegetable_node&quot;, vegetable_node)
builder.add_edge(START, &quot;vegetable_node&quot;)
builder.add_edge(&quot;vegetable_node&quot;, END)

vegetable_subgraph = builder.compile(checkpointer=True)

# 构建父图
class OverAllState(TypedDict):
    fruit: str # 水果
    vegetable: str # 蔬菜

    fruit_introduction: str # 水果介绍
    vegetable_introduction: str # 蔬菜介绍

def call_subgraph(state: OverAllState) -&gt; OverAllState:
    fruit = state[&quot;fruit&quot;]
    vegetable = state[&quot;vegetable&quot;]

    fruit_messages = [
        SystemMessage(&quot;用最简短的话介绍用户输入的水果&quot;),
        HumanMessage(fruit)
    ]

    vegetable_messages = [
        SystemMessage(&quot;用最简短的话介绍用户输入的蔬菜&quot;),
        HumanMessage(vegetable)
    ]

    fruit_response = fruit_subgraph.invoke({&quot;messages&quot;: fruit_messages})
    vegetable_response = vegetable_subgraph.invoke({&quot;messages&quot;: vegetable_messages})

    fruit_introduction = fruit_response[&quot;messages&quot;][-1].content
    vegetable_introduction = vegetable_response[&quot;messages&quot;][-1].content

    return {
        &quot;fruit_introduction&quot;: fruit_introduction,
        &quot;vegetable_introduction&quot;: vegetable_introduction
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;call_subgraph&quot;, call_subgraph)
builder.add_edge(START, &quot;call_subgraph&quot;)
builder.add_edge(&quot;call_subgraph&quot;, END)

checkpointer = InMemorySaver()
parent_graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;multi-call-diff-demo&quot;}}

first_response = parent_graph.invoke(
    {
        &quot;fruit&quot;: &quot;桑葚&quot;,
        &quot;vegetable&quot;: &quot;西兰苔&quot;
    },
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第一次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
print(first_response)

second_response = parent_graph.invoke(
    {
        &quot;fruit&quot;: &quot;香蕉&quot;,
        &quot;vegetable&quot;: &quot;西兰花&quot;
    },
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第二次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
print(second_response)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p><strong>运行结果</strong></p>
<pre><code>2026-06-25 11:37:49.176 | INFO     | __main__:fruit_node:32 - ==============================
2026-06-25 11:37:49.176 | INFO     | __main__:fruit_node:33 - 调用水果子图的 fruit_node 节点，当前的 messages:
2026-06-25 11:37:49.177 | INFO     | __main__:fruit_node:35 -
[消息 1]
类型：system
内容：用最简短的话介绍用户输入的水果
2026-06-25 11:37:49.178 | INFO     | __main__:fruit_node:35 -
[消息 2]
类型：human
内容：桑葚
2026-06-25 11:37:49.178 | INFO     | __main__:fruit_node:43 - ==============================
2026-06-25 11:37:49.872 | INFO     | __main__:vegetable_node:61 - ==============================
2026-06-25 11:37:49.873 | INFO     | __main__:vegetable_node:62 - 调用蔬菜子图的 vegetable_node 节点，当前的 messages:
2026-06-25 11:37:49.873 | INFO     | __main__:vegetable_node:64 -
[消息 1]
类型：system
内容：用最简短的话介绍用户输入的蔬菜
2026-06-25 11:37:49.874 | INFO     | __main__:vegetable_node:64 -
[消息 2]
类型：human
内容：西兰苔
2026-06-25 11:37:49.874 | INFO     | __main__:vegetable_node:72 - ==============================
============================== -&gt; 第一次运行结果 &lt;- ==============================
{'fruit': '桑葚', 'vegetable': '西兰苔', 'fruit_introduction': '紫黑多汁的浆果，味酸甜。', 'vegetable_introduction': '西兰苔是西兰花与芥蓝杂交的蔬菜，以鲜嫩花茎和球花为食。'}
...（第二次调用时水果、蔬菜子图各自保留历史消息，按调用位置独立）
{'fruit': '香蕉', 'vegetable': '西兰花', 'fruit_introduction': '黄色弯月形水果，软糯香甜。', 'vegetable_introduction': '西兰花，绿色花球状蔬菜，营养丰富。'}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625113908367.png" alt="image-20260625113908367" /></p>
<p>本例在同一个节点中多次调用不同子图，通过专有状态保证了<strong>水果一定发给 <code>fruit_subgraph</code></strong>、<strong>蔬菜一定发给 <code>vegetable_subgraph</code></strong>，一定程度上可以确保不同子图的检查点历史不相互干扰。</p>
<p>但这种靠调用顺序区分的方式不够稳健——如果调用顺序变化，检查点历史也会错乱。官方推荐的做法见下一节。</p>
<h4 id="12-2-2-5-场景五-独立节点调用不同子图-推荐">12.2.2.5. 场景五：独立节点调用不同子图（推荐）</h4>
<p>为每个子图准备独立的父图节点，这样它们的命名空间是各自的节点名，完全独立。</p>
<pre><code class="language-python">from typing import TypedDict, Literal
from collections.abc import Sequence
from langgraph.graph import StateGraph, START, END, MessagesState
from langgraph.checkpoint.memory import InMemorySaver

from langchain.messages import HumanMessage, SystemMessage, AIMessage
from langchain_deepseek import ChatDeepSeek

import sys
from loguru import logger
logger.remove()
logger.add(sys.stdout, colorize=True)

from dotenv import load_dotenv

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 构建水果子图
def fruit_node(state: MessagesState) -&gt; MessagesState:
    messages = state[&quot;messages&quot;]
    logger.info(&quot;=&quot; * 30)
    logger.info(&quot;调用水果子图的 fruit_node 节点，当前的 messages: &quot;)
    for index, message in enumerate(messages, start=1):
        logger.opt(colors=True).info(
            &quot;\n&lt;cyan&gt;&lt;bold&gt;[消息 {}]&lt;/bold&gt;&lt;/cyan&gt;\n&quot;
            &quot;&lt;yellow&gt;类型：&lt;/yellow&gt;&lt;magenta&gt;{}&lt;/magenta&gt;\n&quot;
            &quot;&lt;yellow&gt;内容：&lt;/yellow&gt;&lt;green&gt;{}&lt;/green&gt;&quot;,
            index,
            message.type,
            message.content
        )
    logger.info(&quot;=&quot; * 30)
    response = model.invoke(input=messages)
    ai_msg = AIMessage(content=response.content)

    return {
        &quot;messages&quot;: [ai_msg]
    }

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;fruit_node&quot;, fruit_node)
builder.add_edge(START, &quot;fruit_node&quot;)
builder.add_edge(&quot;fruit_node&quot;, END)

fruit_subgraph = builder.compile(checkpointer=True)

# 构建蔬菜子图
def vegetable_node(state: MessagesState) -&gt; MessagesState:
    messages = state[&quot;messages&quot;]
    logger.info(&quot;=&quot; * 30)
    logger.info(&quot;调用蔬菜子图的 vegetable_node 节点，当前的 messages: &quot;)
    for index, message in enumerate(messages, start=1):
        logger.opt(colors=True).info(
            &quot;\n&lt;cyan&gt;&lt;bold&gt;[消息 {}]&lt;/bold&gt;&lt;/cyan&gt;\n&quot;
            &quot;&lt;yellow&gt;类型：&lt;/yellow&gt;&lt;magenta&gt;{}&lt;/magenta&gt;\n&quot;
            &quot;&lt;yellow&gt;内容：&lt;/yellow&gt;&lt;green&gt;{}&lt;/green&gt;&quot;,
            index,
            message.type,
            message.content
        )
    logger.info(&quot;=&quot; * 30)
    response = model.invoke(input=messages)
    ai_msg = AIMessage(content=response.content)

    return {
        &quot;messages&quot;: [ai_msg]
    }

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;vegetable_node&quot;, vegetable_node)
builder.add_edge(START, &quot;vegetable_node&quot;)
builder.add_edge(&quot;vegetable_node&quot;, END)

vegetable_subgraph = builder.compile(checkpointer=True)

# 构建父图
class OverAllState(TypedDict):
    fruit: str # 水果
    vegetable: str # 蔬菜

    fruit_introduction: str # 水果介绍
    vegetable_introduction: str # 蔬菜介绍

def call_fruit_subgraph(state: OverAllState) -&gt; OverAllState:
    fruit = state[&quot;fruit&quot;]

    fruit_messages = [
        SystemMessage(&quot;用最简短的话介绍用户输入的水果&quot;),
        HumanMessage(fruit)
    ]

    fruit_response = fruit_subgraph.invoke({&quot;messages&quot;: fruit_messages})
    fruit_introduction = fruit_response[&quot;messages&quot;][-1].content

    return {
        &quot;fruit_introduction&quot;: fruit_introduction
    }

def call_vegetable_subgraph(state: OverAllState) -&gt; OverAllState:
    vegetable = state[&quot;vegetable&quot;]

    vegetable_messages = [
        SystemMessage(&quot;用最简短的话介绍用户输入的蔬菜&quot;),
        HumanMessage(vegetable)
    ]

    vegetable_response = vegetable_subgraph.invoke({&quot;messages&quot;: vegetable_messages})
    vegetable_introduction = vegetable_response[&quot;messages&quot;][-1].content

    return {
        &quot;vegetable_introduction&quot;: vegetable_introduction
    }

def router(state: OverAllState) -&gt; Sequence[Literal[&quot;call_fruit_subgraph&quot;, &quot;call_vegetable_subgraph&quot;, END]]:
    next_nodes = []
    if state.get(&quot;fruit&quot;):
        next_nodes.append(&quot;call_fruit_subgraph&quot;)
    if state.get(&quot;vegetable&quot;):
        next_nodes.append(&quot;call_vegetable_subgraph&quot;)
    if not next_nodes:
        next_nodes.append(END)
    return next_nodes

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;call_fruit_subgraph&quot;, call_fruit_subgraph)
builder.add_node(&quot;call_vegetable_subgraph&quot;, call_vegetable_subgraph)

builder.add_conditional_edges(START, router, path_map=[&quot;call_fruit_subgraph&quot;, &quot;call_vegetable_subgraph&quot;, END])
builder.add_edge(&quot;call_fruit_subgraph&quot;, END)
builder.add_edge(&quot;call_vegetable_subgraph&quot;, END)

checkpointer = InMemorySaver()
parent_graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;independent-nodes-demo&quot;}}

first_response = parent_graph.invoke(
    {
        &quot;fruit&quot;: &quot;桑葚&quot;,
        &quot;vegetable&quot;: &quot;西兰苔&quot;
    },
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第一次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
print(first_response)

second_response = parent_graph.invoke(
    {
        &quot;fruit&quot;: &quot;香蕉&quot;,
        &quot;vegetable&quot;: &quot;西兰花&quot;
    },
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第二次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
print(second_response)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p><strong>运行结果</strong></p>
<pre><code>2026-06-25 11:59:10.331 | INFO     | __main__:fruit_node:33 - ==============================
2026-06-25 11:59:10.334 | INFO     | __main__:fruit_node:34 - 调用水果子图的 fruit_node 节点，当前的 messages:
2026-06-25 11:59:10.335 | INFO     | __main__:fruit_node:36 -
[消息 1]
类型：system
内容：用最简短的话介绍用户输入的水果
2026-06-25 11:59:10.334 | INFO     | __main__:vegetable_node:62 - ==============================
2026-06-25 11:59:10.335 | INFO     | __main__:fruit_node:36 -
[消息 2]
类型：human
内容：桑葚
2026-06-25 11:59:10.337 | INFO     | __main__:fruit_node:44 - ==============================
2026-06-25 11:59:10.337 | INFO     | __main__:vegetable_node:63 - 调用蔬菜子图的 vegetable_node 节点，当前的 messages:
2026-06-25 11:59:10.341 | INFO     | __main__:vegetable_node:65 -
[消息 1]
类型：system
内容：用最简短的话介绍用户输入的蔬菜
2026-06-25 11:59:10.341 | INFO     | __main__:vegetable_node:65 -
[消息 2]
类型：human
内容：西兰苔
2026-06-25 11:59:10.342 | INFO     | __main__:vegetable_node:73 - ==============================
============================== -&gt; 第一次运行结果 &lt;- ==============================
{'fruit': '桑葚', 'vegetable': '西兰苔', 'fruit_introduction': '桑葚是桑树的果实，味甜可食，常被用于制酱或入药。', 'vegetable_introduction': '西兰苔是西兰花和芥蓝的杂交品种。'}
...（第二次调用时，水果子图和蔬菜子图各自保留独立的历史）
{'fruit': '香蕉', 'vegetable': '西兰花', 'fruit_introduction': '香蕉是软甜水果，富含钾，能缓解便秘。', 'vegetable_introduction': '一种绿色花状蔬菜，营养丰富。'}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625115935316.png" alt="image-20260625115935316" /></p>
<p>每个子图有独立的父图节点，命名空间为各自的节点名，完全隔离。<strong>这是 Per-thread 模式下多次调用不同子图的推荐做法。</strong></p>
<h4 id="12-2-2-6-场景六-stateless-模式">12.2.2.6. 场景六：Stateless 模式</h4>
<p>子图完全无状态，不保存检查点，中断和多轮对话均不适用。适用于纯计算、无需追踪历史的场景。</p>
<pre><code class="language-python">from typing import TypedDict, Annotated
from operator import add
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver

# 构建子图
class SubgraphState(TypedDict):
    raw_text: str
    clean_texts: Annotated[list[str], add]

def strip_node(state: SubgraphState) -&gt; SubgraphState:
    raw_text = state[&quot;raw_text&quot;]
    clean_text = raw_text.strip()

    return {
        &quot;clean_texts&quot;: [clean_text]
    }

builder = StateGraph(state_schema=SubgraphState)
builder.add_node(&quot;strip_node&quot;, strip_node)
builder.add_edge(START, &quot;strip_node&quot;)
builder.add_edge(&quot;strip_node&quot;, END)

# ==================================================
# 【策略切换点】只需修改这一行
subgraph = builder.compile(checkpointer=False)  # Stateless：无检查点
# subgraph = builder.compile(checkpointer=True)   # Per-thread：有状态（对比用）
# ==================================================

# 构建父图
class ParentState(TypedDict):
    input_texts: list[str]
    output_texts: Annotated[list[str], add]

def call_subgraph(state: ParentState) -&gt; ParentState:
    input_texts = state[&quot;input_texts&quot;]

    output_texts = []
    for input_text in input_texts:
        res = subgraph.invoke({&quot;raw_text&quot;: input_text})
        output_texts += res[&quot;clean_texts&quot;]

    return {
        &quot;output_texts&quot;: output_texts
    }

builder = StateGraph(state_schema=ParentState)
builder.add_node(&quot;call_subgraph&quot;, call_subgraph)
builder.add_edge(START, &quot;call_subgraph&quot;)
builder.add_edge(&quot;call_subgraph&quot;, END)

checkpointer = InMemorySaver()
parent_graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;stateless-demo&quot;}}

first_response = parent_graph.invoke(
    {
        &quot;input_texts&quot;: [&quot;  LangGraph 真有意思 &quot;, &quot;  我喜欢 LangChain &quot;]
    },
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第一次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
print(first_response)

second_response = parent_graph.invoke(
    {
        &quot;input_texts&quot;: [&quot;  Hello, LangGraph  &quot;, &quot;  Hello, LangChain &quot;]
    },
    config = config
)
print(&quot;=&quot; * 30, &quot;-&gt; 第二次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
print(second_response)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p><strong>Stateless 运行结果</strong></p>
<pre><code>============================== -&gt; 第一次运行结果 &lt;- ==============================
{'input_texts': ['  LangGraph 真有意思 ', '  我喜欢 LangChain '], 'output_texts': ['LangGraph 真有意思', '我喜欢 LangChain']}
============================== -&gt; 第二次运行结果 &lt;- ==============================
{'input_texts': ['  Hello, LangGraph  ', '  Hello, LangChain '], 'output_texts': ['LangGraph 真有意思', '我喜欢 LangChain', 'Hello, LangGraph', 'Hello, LangChain']}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625121810977.png" alt="image-20260625121810977" /></p>
<p>渲染的图结构中，子图节点内部没有展开子图拓扑——无状态模式的子图内部状态不可访问。</p>
<p><strong>Per-thread 对比结果</strong>（仅改为 <code>builder.compile(checkpointer=True)</code>）：</p>
<pre><code>============================== -&gt; 第一次运行结果 &lt;- ==============================
{'input_texts': ['  LangGraph 真有意思 ', '  我喜欢 LangChain '], 'output_texts': ['LangGraph 真有意思', '我喜欢 LangChain']}
============================== -&gt; 第二次运行结果 &lt;- ==============================
{'input_texts': ['  Hello, LangGraph  ', '  Hello, LangChain '], 'output_texts': ['LangGraph 真有意思', '我喜欢 LangChain', 'LangGraph 真有意思', 'Hello, LangGraph', '我喜欢 LangChain', 'Hello, LangChain']}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625122230644.png" alt="image-20260625122230644" /></p>
<p>Per-thread 模式下第二次调用结果出现了重复（子图的历史 <code>clean_texts</code> 被累积），拓扑结构中也展开了子图。<strong>Stateless 不会累积历史，每次调用都是干净的。</strong></p>
<h4 id="12-2-2-7-per-invocation-和-per-thread-的设计哲学">12.2.2.7. <code>Per-invocation</code>和<code>Per-thread</code>的设计哲学</h4>
<p>默认子图检查点命名空间为 <code>&lt;节点名称&gt;:&lt;任务ID&gt;</code>。两种模式的核心差异在于对 <code>&lt;任务ID&gt;</code> 的处理：</p>
<table>
<thead>
<tr>
<th style="text-align:left">维度</th>
<th style="text-align:left"><strong><code>Per-invocation</code></strong></th>
<th style="text-align:left"><strong><code>Per-thread</code></strong></th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">命名空间</td>
<td style="text-align:left"><code>&lt;节点名称&gt;:&lt;任务ID&gt;</code></td>
<td style="text-align:left"><code>&lt;节点名称&gt;</code>（去掉 <code>:&lt;任务ID&gt;</code>）</td>
</tr>
<tr>
<td style="text-align:left">设计意图</td>
<td style="text-align:left"><code>&lt;任务ID&gt;</code> 每次不同 → 每次调用状态独立</td>
<td style="text-align:left"><code>&lt;节点名称&gt;</code> 不变 → 跨多次调用状态连续</td>
</tr>
<tr>
<td style="text-align:left">同节点多次调用</td>
<td style="text-align:left"><code>:&lt;任务ID&gt;|1</code>, <code>:&lt;任务ID&gt;|2</code> …</td>
<td style="text-align:left"><code>|1</code>, <code>|2</code> …</td>
</tr>
<tr>
<td style="text-align:left">注意事项</td>
<td style="text-align:left">—</td>
<td style="text-align:left">调用顺序变化会导致检查点历史相互干扰；<code>get_state_history()</code> 存在 Bug，父图检查点仍记录 <code>:&lt;任务ID&gt;</code> 格式，需额外处理</td>
</tr>
</tbody>
</table>
<h4 id="12-2-2-8-总结">12.2.2.8. 总结</h4>
<table>
<thead>
<tr>
<th style="text-align:left">特性</th>
<th style="text-align:left"><strong><code>Per-invocation (default)</code></strong></th>
<th style="text-align:left"><strong><code>Per-thread</code></strong></th>
<th style="text-align:left"><strong><code>Stateless</code></strong></th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>checkpointer=</code></strong></td>
<td style="text-align:left"><strong><code>None</code></strong></td>
<td style="text-align:left"><strong><code>True</code></strong></td>
<td style="text-align:left"><strong><code>False</code></strong></td>
</tr>
<tr>
<td style="text-align:left">中断</td>
<td style="text-align:left">✅</td>
<td style="text-align:left">✅</td>
<td style="text-align:left">❌</td>
</tr>
<tr>
<td style="text-align:left">多轮对话</td>
<td style="text-align:left">❌</td>
<td style="text-align:left">✅</td>
<td style="text-align:left">❌</td>
</tr>
<tr>
<td style="text-align:left">多次调用不同子图</td>
<td style="text-align:left">✅</td>
<td style="text-align:left">⚠️最好通过节点隔离</td>
<td style="text-align:left">✅</td>
</tr>
<tr>
<td style="text-align:left">多次调用相同子图</td>
<td style="text-align:left">✅</td>
<td style="text-align:left">❌子图历史相互干扰</td>
<td style="text-align:left">✅</td>
</tr>
<tr>
<td style="text-align:left">观测子图检查点快照</td>
<td style="text-align:left">⚠️可以观测，但每次调用独立</td>
<td style="text-align:left">✅</td>
<td style="text-align:left">❌</td>
</tr>
</tbody>
</table>
<h2 id="12-3-子图流式运行">12.3. 子图流式运行</h2>
<p>只需要在父图调用 <strong><code>stream</code></strong> 时传递 <strong><code>subgraphs=True</code></strong> 即可，这样父图和子图的流数据都会汇入同一个流队列，再通过命名空间区分来源。</p>
<p><strong>需要注意的是</strong>：<strong><code>subgraphs=True</code></strong> 是在原有父图流数据的基础上，<strong>额外加入子图内部产生的流数据</strong>，并不会用子图数据替代父图数据。它会同时流式返回父图和所有子图的输出。</p>
<h3 id="12-3-1-分析-chunk-格式">12.3.1. 分析<code>chunk</code>格式</h3>
<p><strong><code>chunk</code></strong> 格式为</p>
<pre><code class="language-python">(namespace, stream_mode, data)
</code></pre>
<p>此处的命名空间不同于检查点命名空间，是流处理内部专门用于区分父图和子图流数据的字段。</p>
<p>我们通过模式为 <strong><code>updates</code></strong> 的简单案例查看数据格式</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END

# 构建子图
class SubgraphState(TypedDict):
    raw_text: str # 未清洗文本
    stripped_text: str # 去除收尾空格的文本
    punctuated_text: str # 句尾添加句号的文本

def subgraph_strip_node(state: SubgraphState) -&gt; SubgraphState:
    raw_text = state[&quot;raw_text&quot;]
    stripped_text = raw_text.strip()

    return {
        &quot;stripped_text&quot;: stripped_text
    }

def subgraph_punctuate_node(state: SubgraphState) -&gt; SubgraphState:
    stripped_text = state[&quot;stripped_text&quot;]
    punctuated_text = stripped_text + &quot;。&quot;

    return {
        &quot;punctuated_text&quot;: punctuated_text
    }

builder = StateGraph(state_schema=SubgraphState)
builder.add_node(&quot;subgraph_strip_node&quot;, subgraph_strip_node)
builder.add_node(&quot;subgraph_punctuate_node&quot;, subgraph_punctuate_node)

builder.add_edge(START, &quot;subgraph_strip_node&quot;)
builder.add_edge(&quot;subgraph_strip_node&quot;, &quot;subgraph_punctuate_node&quot;)
builder.add_edge(&quot;subgraph_punctuate_node&quot;, END)

subgraph = builder.compile()

# 构建父图
class ParentState(TypedDict):
    input_text: str # 输入的未清洗的文本
    cleaned_text: str # 清洗后的文本

def call_subgraph(state: ParentState) -&gt; ParentState:
    input_text = state[&quot;input_text&quot;]

    res = subgraph.invoke({&quot;raw_text&quot;: input_text})
    cleaned_text = res[&quot;punctuated_text&quot;]
    return {
        &quot;cleaned_text&quot;: cleaned_text
    }

builder = StateGraph(state_schema=ParentState)
builder.add_node(&quot;call_subgraph&quot;, call_subgraph)

builder.add_edge(START, &quot;call_subgraph&quot;)
builder.add_edge(&quot;call_subgraph&quot;, END)

parent_graph = builder.compile()

input_text = &quot;   LangGraph 真有意思        &quot;
for chunk in parent_graph.stream(
    {&quot;input_text&quot;: input_text},
    subgraphs=True,
    stream_mode=[&quot;updates&quot;]
):
    print(chunk)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">(
    (
        'call_subgraph:4daa7290-d5c9-ae96-849e-d2f1dafadd17',
    ),
    'updates',
    {
        'subgraph_strip_node': {
            'stripped_text': 'LangGraph 真有意思'
        }
    }
)
(
    (
        'call_subgraph:4daa7290-d5c9-ae96-849e-d2f1dafadd17',
    ),
    'updates',
    {
        'subgraph_punctuate_node': {
            'punctuated_text': 'LangGraph 真有意思。'
        }
    }
)
(
    (),
    'updates',
    {
        'call_subgraph': {
            'cleaned_text': 'LangGraph 真有意思。'
        }
    }
)
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625143710651.png" alt="image-20260625143710651" /></p>
<p><strong>流数据命名空间规则</strong>：</p>
<table>
<thead>
<tr>
<th style="text-align:left">来源</th>
<th style="text-align:left">命名空间</th>
<th style="text-align:left">备注</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">父图</td>
<td style="text-align:left"><code>()</code>（空元组）</td>
<td style="text-align:left"></td>
</tr>
<tr>
<td style="text-align:left">子图（常规）</td>
<td style="text-align:left"><code>(&lt;父图节点名&gt;:&lt;父图任务ID&gt;,)</code></td>
<td style="text-align:left"></td>
</tr>
<tr>
<td style="text-align:left">子图（Per-thread）</td>
<td style="text-align:left"><code>(&lt;父图节点名&gt;,)</code></td>
<td style="text-align:left">与检查点命名空间一致</td>
</tr>
</tbody>
</table>
<h3 id="12-3-2-多轮对话消息流处理">12.3.2. 多轮对话消息流处理</h3>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END, MessagesState
from langgraph.checkpoint.memory import InMemorySaver
from langchain.messages import HumanMessage, SystemMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 构建子图
class SubgraphState(MessagesState):
    system_prompt: SystemMessage

def llm_mode(state: SubgraphState) -&gt; SubgraphState:
    system_prompt = state[&quot;system_prompt&quot;]
    messages = state[&quot;messages&quot;]

    response = model.invoke([system_prompt] + messages)
    return {
        &quot;messages&quot;: [response]
    }

builder = StateGraph(state_schema=SubgraphState)
builder.add_node(&quot;llm_mode&quot;, llm_mode)
builder.add_edge(START, &quot;llm_mode&quot;)
builder.add_edge(&quot;llm_mode&quot;, END)

subgraph = builder.compile(checkpointer=True)

# 构建父图
class ParentState(TypedDict):
    user_input: str
    assistant_response: str

def call_subgraph(state: ParentState) -&gt; ParentState:
    user_input = state[&quot;user_input&quot;]

    response = subgraph.invoke(
        {
            &quot;system_prompt&quot;: SystemMessage(&quot;你是个善解人意的助手&quot;),
            &quot;messages&quot;: [HumanMessage(user_input)]
        }
    )

    assistant_response = response[&quot;messages&quot;][-1].content

    return {
        &quot;assistant_response&quot;: assistant_response
    }

builder = StateGraph(state_schema=ParentState)
builder.add_node(&quot;call_subgraph&quot;, call_subgraph)
builder.add_edge(START, &quot;call_subgraph&quot;)
builder.add_edge(&quot;call_subgraph&quot;, END)

checkpointer = InMemorySaver()
parent_graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;subgraph_stream_test&quot;}}

print(&quot;\n&quot;, &quot;=&quot; * 30, &quot;-&gt; 第一次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
# chunk 形如
# (('call_subgraph',), 'messages', (AIMessageChunk(content='', additional_kwargs={}, response_metadata={'model_provider': 'deepseek'}, id='lc_run--019efd65-c54e-7012-912e-29ff4f84328e', tool_calls=[], invalid_tool_calls=[], tool_call_chunks=[]), {'thread_id': 'subgraph_stream_test', 'langgraph_step': 1, 'langgraph_node': 'llm_mode', 'langgraph_triggers': ('branch:to:llm_mode',), 'langgraph_path': ('__pregel_pull', 'llm_mode'), 'langgraph_checkpoint_ns': 'call_subgraph|llm_mode:55577c3e-22da-877a-c77b-f16bdbcd2c6c', 'checkpoint_ns': 'call_subgraph:9c469391-f094-1c1b-c5d7-6578f1603151', 'ls_provider': 'deepseek', 'ls_model_name': 'deepseek-v4-flash', 'ls_model_type': 'chat', 'ls_temperature': None}))
for chunk in parent_graph.stream(
    {
        &quot;user_input&quot;: &quot;花儿为什么这样红？&quot;
    },
    config=config,
    subgraphs=True,
    stream_mode=[&quot;messages&quot;]
):
    print(chunk[2][0].content, end=&quot;&quot;, flush=True)

print(&quot;\n&quot;, &quot;=&quot; * 30, &quot;-&gt; 第二次运行结果 &lt;-&quot;, &quot;=&quot; * 30)
for chunk in parent_graph.stream(
    {
        &quot;user_input&quot;: &quot;刚才我们聊了什么？&quot;
    },
    config=config,
    subgraphs=True,
    stream_mode=[&quot;messages&quot;]
):
    print(chunk[2][0].content, end=&quot;&quot;, flush=True)

from IPython.display import display, Image
display(
    Image(
        parent_graph
        .get_graph(xray=True)
        .draw_mermaid_png()
    )
)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">
 ============================== -&gt; 第一次运行结果 &lt;- ==============================
“花儿为什么这样红”是一个经典的问题，可以从不同角度来理解：

1. **自然科学的视角**：花朵的颜色主要由花瓣中的色素决定，尤其是**花青素**（anthocyanins）。花青素在酸性环境下呈现红色，在碱性环境下偏向蓝色。某些花朵（如玫瑰、山茶花）细胞液偏酸性，因此吸收阳光中的蓝绿光，反射红光，让我们看到鲜艳的红色。此外，红色也能吸引蜂鸟等传粉动物，帮助植物繁殖。

2. **文化艺术的视角**：这是一首传唱已久的中国民歌（《花儿为什么这样红》），由雷振邦作曲，是电影《冰山上的来客》的插曲。歌词用“花儿”比喻纯洁、热烈的情感，红色象征着爱情、勇气或革命的赤诚。

3. **诗意隐喻**：就像“花儿为什么这样红”可以指代生命的热情、青春的绽放，或者经风雨后更加绚烂的坚韧。红色的花朵也常被赋予“美好而短暂”的哲学意味。

如果你指的是特定语境（比如学生物的朋友、教孩子、写赏析），我可以把回答调整得更具体。🌹
 ============================== -&gt; 第二次运行结果 &lt;- ==============================
我们刚才聊了“花儿为什么这样红”这个话题。我分别从自然科学（花青素与酸碱性）、文化艺术（经典民歌《花儿为什么这样红》）以及诗意隐喻三个角度做了解答。如果你有更多想深入的方向，随时告诉我～
</code></pre>
<p>实际测试可以看到模型返回的消息是 <strong>逐<code>token</code></strong> 返回的。</p>
<p><img src="/images/langgraph-study-notes/image-20260625143740875.png" alt="image-20260625143740875" /></p>
<h2 id="12-4-子图动态路由">12.4. 子图动态路由</h2>
<p>在子图中可以通过 <strong><code>Command</code></strong> 动态路由到父图节点，只要将参数 <strong><code>graph</code></strong> 的值设置为 <strong><code>Command.PARENT</code></strong> 即可，如下所示</p>
<pre><code class="language-python">return Command(
    update={
        &quot;fruits&quot;: fruits,
        &quot;vegetables&quot;: vegetables
    },
    goto=&quot;router_node&quot;,
    graph=Command.PARENT
)
</code></pre>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END
from langgraph.types import Command
from loguru import logger

# ==================== 子图 ====================
class SubState(TypedDict):
    data: str

def sub_node(state: SubState) -&gt; Command:
    &quot;&quot;&quot;子图节点：执行后动态路由回父图的 parent_router&quot;&quot;&quot;
    logger.info(&quot;[子图] sub_node 执行&quot;)
    return Command(
        update={&quot;data&quot;: state[&quot;data&quot;] + &quot; → 子图&quot;},
        goto=&quot;node_b&quot;,       # 路由到父图节点
        graph=Command.PARENT,       # 指定目标为父图
    )

sub_builder = StateGraph(state_schema=SubState)
sub_builder.add_node(&quot;sub_node&quot;, sub_node)
sub_builder.add_edge(START, &quot;sub_node&quot;)
sub_graph = sub_builder.compile()

# ==================== 父图 ====================
class ParentState(TypedDict):
    data: str

def node_a(state: ParentState) -&gt; ParentState:
    print(&quot;[父图] node_a 执行&quot;)
    return {&quot;visited_a&quot;: True}

def node_b(state: ParentState) -&gt; ParentState:
    print(&quot;[父图] node_b 执行&quot;)
    return {&quot;visited_b&quot;: True}

parent_builder = StateGraph(state_schema=ParentState)
parent_builder.add_node(
    &quot;sub_graph&quot;, sub_graph
)

parent_builder.add_node(&quot;node_a&quot;, node_a)
parent_builder.add_node(&quot;node_b&quot;, node_b)
parent_builder.add_edge(START, &quot;sub_graph&quot;)
parent_builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
parent_builder.add_edge(&quot;node_b&quot;, END)

parent_graph = parent_builder.compile()

result = parent_graph.invoke({&quot;data&quot;: &quot;初始&quot;})
print(f&quot;\n最终结果: {result}&quot;)

from IPython.display import display
display(parent_graph)
</code></pre>
<p><strong>要点</strong>：</p>
<ol>
<li>
<p>子图通过 <code>Command(goto=&quot;parent_router&quot;, graph=Command.PARENT)</code> 将执行流动态路由回父图的指定节点</p>
</li>
<li>
<p>子图返回值类型注解不能用 <code>Command[Literal[&quot;parent_router&quot;]]</code> 的形式，因为子图并不知道父图节点，静态类型校验会报错</p>
<pre><code class="language-python">ValueError: Found edge ending at unknown node `parent_router`
</code></pre>
</li>
<li>
<p>添加子图节点时通过 <strong><code>destinations</code></strong> 声明下游节点，便于渲染图结构时正确描绘拓扑关系</p>
<pre><code class="language-python">builder.add_node(
    &quot;sub_graph&quot;, sub_graph,
    destinations=(&quot;parent_router&quot;,)
)
</code></pre>
<p>不加 <strong><code>destinations</code></strong> 不会影响动态路由，只是渲染的图结构不能正确描绘拓扑。</p>
</li>
</ol>
<h1 id="13-运行图设计模式">13. 运行图设计模式</h1>
<h2 id="13-1-概述">13.1. 概述</h2>
<p><strong><code>LangGraph</code></strong> 官方总结了常用的状态图设计模式，如下</p>
<table>
<thead>
<tr>
<th>模式</th>
<th>图结构</th>
<th>运行时动态性</th>
<th>核心 <code>LangGraph</code> 能力</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>Prompt Chaining</code></strong></td>
<td>顺序链</td>
<td>低</td>
<td>静态边、条件边</td>
</tr>
<tr>
<td><strong><code>Parallelization</code></strong></td>
<td>固定 <strong><code>Fan-out/Fan-in</code></strong></td>
<td>低</td>
<td>并行超步、汇聚</td>
</tr>
<tr>
<td><strong><code>Routing</code></strong></td>
<td>条件分支</td>
<td>中</td>
<td>结构化输出、条件边</td>
</tr>
<tr>
<td><strong><code>Orchestrator-worker</code></strong></td>
<td>动态 <strong><code>Fan-out/Fan-in</code></strong></td>
<td>高</td>
<td><strong><code>Send</code></strong>、<strong><code>WorkerState</code></strong>、<strong><code>Reducer</code></strong></td>
</tr>
<tr>
<td><strong><code>Evaluator-optimizer</code></strong></td>
<td>反馈循环</td>
<td>中</td>
<td>条件边、循环、反馈状态</td>
</tr>
<tr>
<td><strong><code>Agent</code></strong></td>
<td>自主决策循环</td>
<td>最高</td>
<td><strong><code>MessagesState</code></strong>、工具调用、<strong><code>ToolNode</code></strong></td>
</tr>
</tbody>
</table>
<h2 id="13-2-实现">13.2. 实现</h2>
<h3 id="13-2-1-prompt-chaining-提示词链">13.2.1. <code>Prompt Chaining</code>：提示词链</h3>
<h4 id="13-2-1-1-核心思想">13.2.1.1. 核心思想</h4>
<p>将一个复杂任务拆分成若干个顺序执行的小任务：</p>
<pre><code class="language-text">输入
 ↓
任务 A
 ↓
任务 B
 ↓
任务 C
 ↓
输出
</code></pre>
<p>后一个节点依赖前一个节点的结果。</p>
<p>适合：</p>
<ul>
<li>翻译 → 校对 → 润色</li>
<li>生成内容 → 检查一致性 → 修订</li>
<li>提取信息 → 分类 → 格式化</li>
<li>需求分析 → 生成代码 → 代码解释</li>
</ul>
<p>官方示例是：</p>
<pre><code class="language-text">生成笑话
   ↓
检查是否合格
  ├─ 合格 → END
  └─ 不合格 → 改进笑话 → 最终润色 → END
</code></pre>
<p>所以 <strong><code>Prompt Chaining</code></strong> 不一定只是简单的直线，也可以在中间加入一个 <strong><code>Gate</code>，质量门控节点</strong>。</p>
<p><strong><code>Prompt Chaining</code></strong> 的核心是：</p>
<blockquote>
<p><strong>将复杂任务拆解为可独立验证的阶段，并把每一阶段的结果记录下来向后传递。</strong></p>
</blockquote>
<p>它的优点是执行过程稳定且易于调试；缺点是流程固定，无法处理未知数量或高度动态的任务。</p>
<h4 id="13-2-1-2-示例">13.2.1.2. 示例</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END

from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek
from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 图状态
class OverAllState(TypedDict):
    topic: str
    joke: str
    improved_joke: str
    final_joke: str

# 节点
def generate_joke(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;第一次调用大模型，生成初始笑话&quot;&quot;&quot;

    msg = model.invoke(
        [HumanMessage(content=f&quot;写一个关于“{state['topic']}”的简短笑话&quot;)]
    )
    return {&quot;joke&quot;: msg.content}

def check_punchline(state: OverAllState) -&gt; Literal[&quot;pass&quot;, &quot;no_pass&quot;]:
    &quot;&quot;&quot;判断笑话中是否包含包袱&quot;&quot;&quot;

    # 简单判断：笑话中是否包含问号或感叹号
    if &quot;?&quot; in state[&quot;joke&quot;] or &quot;!&quot; in state[&quot;joke&quot;]:
        return &quot;pass&quot;
    return &quot;no_pass&quot;

def improve_joke(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;第二次调用大模型，通过添加双关语改进笑话&quot;&quot;&quot;

    msg = model.invoke(
        [HumanMessage(content=f&quot;通过添加双关语，让下面这个笑话变得更有趣：\n{state['joke']}&quot;)]
    )
    return {&quot;improved_joke&quot;: msg.content}

def polish_joke(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;第三次调用大模型，对笑话进行最终润色&quot;&quot;&quot;

    msg = model.invoke(
        [HumanMessage(content=f&quot;为下面这个笑话添加一个出人意料的反转：\n{state['improved_joke']}&quot;)]
    )
    return {&quot;final_joke&quot;: msg.content}

# 构建工作流
builder = StateGraph(state_schema=OverAllState)

# 添加节点
builder.add_node(&quot;generate_joke&quot;, generate_joke)
builder.add_node(&quot;improve_joke&quot;, improve_joke)
builder.add_node(&quot;polish_joke&quot;, polish_joke)

# 添加边，连接各个节点
builder.add_edge(START, &quot;generate_joke&quot;)
builder.add_conditional_edges(
    &quot;generate_joke&quot;,
    check_punchline,
    {
        &quot;no_pass&quot;: &quot;improve_joke&quot;,
        &quot;pass&quot;: END,
    },
)
builder.add_edge(&quot;improve_joke&quot;, &quot;polish_joke&quot;)
builder.add_edge(&quot;polish_joke&quot;, END)

# 编译工作流
graph = builder.compile()

# 调用工作流
response = graph.invoke({&quot;topic&quot;: &quot;猫&quot;})
print(response)

# 绘制拓扑结构
from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">{
    'topic': '猫',
    'joke': '一只猫去面试，面试官问：“你有什么特长？”  \n猫淡定地回答：“我能用尾巴画圆。”  \n面试官惊讶：“真的吗？演示一下。”  \n猫转身，尾巴甩了几下，画出一个完美的圆。  \n面试官鼓掌：“太好了！你被录取了！”  \n第二天，猫上班了，发现它被送到了数学实验室——负责画π。',
    'improved_joke': '加了一点数学梗和双关后，版本如下：\n\n---\n\n一只猫去面试，面试官问：“你有什么特长？”  \n猫淡定地回答：“我能用尾巴画圆。”  \n面试官惊讶：“真的吗？演示一下。”  \n猫转身，尾巴甩了几下，画出一个完美的圆。  \n面试官鼓掌：“太好了！你被录取了！”  \n\n第二天，猫上班了，发现它被送到了数学实验室——**负责画π**。  \n\n猫叹了口气，说：“**原来我的‘圆’满人生，终究逃不过π的‘鼠’命。”**  \n\n（双关解析：  \n1. “π”音同“派”，既指圆周率，也暗示“被派遣”去画圆。  \n2. “鼠命”谐音“宿命”，同时猫抓老鼠，这里暗指猫被困在数学“鼠”洞（无限不循环小数）里，永远画不完。）',
    'final_joke': '---\n\n**反转版：**  \n第二天，猫上班了，发现它被送到了数学实验室——**负责画π**。  \n\n猫叹了口气，说：“原来我的‘圆’满人生，终究逃不过π的‘鼠’命。”  \n\n话音刚落，实验室主任推门进来，是个戴眼镜的仓鼠。仓鼠拍了拍猫的尾巴，微笑道：“别担心，我们用的是**有理数**——你每天只用画前100位，剩下的**无限循环**，由我们**鼠**类负责演算。”  \n\n猫愣了一下，突然跳起来：“等等！你是说——我画圆，你们算剩下的尾巴？”  \n\n仓鼠点头：“对，这叫……**π的‘分工’合作**。”  \n\n猫沉默三秒，忽然掏出手机：“那我得先给抓老鼠的同行打个电话——告诉他们，**‘有理’走遍天下，无理寸步难行**。”  \n\n（双关补充：  \n1. **“有理数”** 双关：既指数学上有理数（可终止或循环），也暗示“有道理的反转”——猫的“无限宿命”被仓鼠强行打断。  \n2. **“剩下的尾巴”** 双关：猫的尾巴画圆，但仓鼠们负责“补完”无限小数，字面与数学双关。  \n3. **“有理走遍天下”** 化用俗语，暗讽“有理数”才是猫的出路，而“无理”的π已变成职场协作梗。）'
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625180419653.png" alt="image-20260625180419653" /></p>
<hr />
<h3 id="13-2-2-parallelization-并行化">13.2.2. <code>Parallelization</code>：并行化</h3>
<h4 id="13-2-2-1-核心思想">13.2.2.1. 核心思想</h4>
<p>把相互独立的任务同时执行，最后汇总结果：</p>
<pre><code class="language-text">                 ┌→ 任务 A ─┐
输入 → Fan-out ──┼→ 任务 B ─┼→ 聚合 → 输出
                 └→ 任务 C ─┘
</code></pre>
<p>官方将并行化分成两种用途：</p>
<ol>
<li>
<p><strong>任务拆分</strong></p>
<ul>
<li>一个节点检查关键词</li>
<li>一个节点检查格式</li>
<li>一个节点检查事实准确性</li>
</ul>
</li>
<li>
<p><strong>多次独立判断</strong></p>
<ul>
<li>多个模型或多个提示词分别评分</li>
<li>最后投票、平均或综合判断</li>
</ul>
</li>
</ol>
<p>前者主要提升速度，后者主要提升置信度。</p>
<h4 id="13-2-2-2-示例">13.2.2.2. 示例</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END

from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 图状态
class OverAllState(TypedDict):
    topic: str
    joke: str
    story: str
    poem: str
    combined_output: str

# 节点
def call_model_1(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;第一次调用大模型，生成笑话&quot;&quot;&quot;

    msg = model.invoke(
        [HumanMessage(content=f&quot;写一个关于“{state['topic']}”的简短的笑话&quot;)]
    )
    return {&quot;joke&quot;: msg.content}

def call_model_2(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;第二次调用大模型，生成故事&quot;&quot;&quot;

    msg = model.invoke(
        [HumanMessage(content=f&quot;写一个关于“{state['topic']}”的简短的故事&quot;)]
    )
    return {&quot;story&quot;: msg.content}

def call_model_3(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;第三次调用大模型，生成诗歌&quot;&quot;&quot;

    msg = model.invoke(
        [HumanMessage(content=f&quot;写一首关于“{state['topic']}”的简短的诗&quot;)]
    )
    return {&quot;poem&quot;: msg.content}

def aggregator(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;将笑话、故事和诗歌合并为单一输出&quot;&quot;&quot;

    combined = f&quot;下面是一个关于“{state['topic']}”的故事、笑话和诗歌！\n\n&quot;
    combined += f&quot;故事：\n{state['story']}\n\n&quot;
    combined += f&quot;笑话：\n{state['joke']}\n\n&quot;
    combined += f&quot;诗歌：\n{state['poem']}&quot;
    return {&quot;combined_output&quot;: combined}

# 构建工作流
builder = StateGraph(state_schema=OverAllState)

# 添加节点
builder.add_node(&quot;call_model_1&quot;, call_model_1)
builder.add_node(&quot;call_model_2&quot;, call_model_2)
builder.add_node(&quot;call_model_3&quot;, call_model_3)
builder.add_node(&quot;aggregator&quot;, aggregator)

# 添加边，连接各个节点
builder.add_edge(START, &quot;call_model_1&quot;)
builder.add_edge(START, &quot;call_model_2&quot;)
builder.add_edge(START, &quot;call_model_3&quot;)
builder.add_edge([&quot;call_model_1&quot;, &quot;call_model_2&quot;, &quot;call_model_3&quot;], &quot;aggregator&quot;)
builder.add_edge(&quot;aggregator&quot;, END)

graph = builder.compile()

# 调用工作流
response = graph.invoke({&quot;topic&quot;: &quot;猫&quot;})
print(response)

from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">{
    'topic': '猫',
    'joke': '“为什么猫总是赢不了电脑游戏？”\n“因为鼠标（鼠）一出现，它就光顾着抓屏（屏幕）了……”',
    'story': '# 猫的故事\n\n老张退休那年，在楼下捡到一只瘦骨嶙峋的橘猫。\n\n他本不爱猫，但看它可怜，便每天在楼道口放一碗剩饭。猫很警惕，非要等他走远了才肯吃。就这样喂了三个月，猫终于允许他靠近一臂的距离。\n\n半年后的一天夜里，老张突发心梗，倒在客厅地板上，手机摔在茶几底下，怎么也够不着。意识模糊间，他感到一团温热的东西贴上了他的胸口——是那只橘猫。它不知怎么钻进了他家门。\n\n猫用头蹭他的手，蹭他的脸，见他没反应，便冲进屋里的每一个房间，歇斯底里地叫。最后它跳上窗台，用爪子疯狂拍打玻璃，叫声惊动了楼下乘凉的邻居。\n\n120赶到时，老张已经在地上躺了将近一个小时。医生说，再晚十分钟，就悬了。\n\n出院后，橘猫正式住进了老张家。老张给它取名“富贵”，逢人就说：“我家富贵啊，有九条命，分了我一条。”\n\n富贵永远听不懂这些话。它只是每天黄昏，准时跳上老张的膝盖，把一团温暖塞进他的怀里，呼噜呼噜地睡去。',
    'poem': '## 《猫》\n\n黄昏被毛茸茸地踩碎，\n肚皮起伏如钟摆，\n眼睫间，星辰坠落。\n当琥珀瞳孔转动，\n整片夜色都来蜷卧。',
    'combined_output': '下面是一个关于“猫”的故事、笑话和诗歌！\n\n故事：\n# 猫的故事\n\n老张退休那年，在楼下捡到一只瘦骨嶙峋的橘猫。\n\n他本不爱猫，但看它可怜，便每天在楼道口放一碗剩饭。猫很警惕，非要等他走远了才肯吃。就这样喂了三个月，猫终于允许他靠近一臂的距离。\n\n半年后的一天夜里，老张突发心梗，倒在客厅地板上，手机摔在茶几底下，怎么也够不着。意识模糊间，他感到一团温热的东西贴上了他的胸口——是那只橘猫。它不知怎么钻进了他家门。\n\n猫用头蹭他的手，蹭他的脸，见他没反应，便冲进屋里的每一个房间，歇斯底里地叫。最后它跳上窗台，用爪子疯狂拍打玻璃，叫声惊动了楼下乘凉的邻居。\n\n120赶到时，老张已经在地上躺了将近一个小时。医生说，再晚十分钟，就悬了。\n\n出院后，橘猫正式住进了老张家。老张给它取名“富贵”，逢人就说：“我家富贵啊，有九条命，分了我一条。”\n\n富贵永远听不懂这些话。它只是每天黄昏，准时跳上老张的膝盖，把一团温暖塞进他的怀里，呼噜呼噜地睡去。\n\n笑话：\n“为什么猫总是赢不了电脑游戏？”\n“因为鼠标（鼠）一出现，它就光顾着抓屏（屏幕）了……”\n\n诗歌：\n## 《猫》\n\n黄昏被毛茸茸地踩碎，\n肚皮起伏如钟摆，\n眼睫间，星辰坠落。\n当琥珀瞳孔转动，\n整片夜色都来蜷卧。'
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625180443516.png" alt="image-20260625180443516" /></p>
<hr />
<h3 id="13-2-3-routing-路由">13.2.3. <code>Routing</code>：路由</h3>
<h4 id="13-2-3-1-核心思想">13.2.3.1. 核心思想</h4>
<p>先识别输入类型，再把请求送到专门的处理流程：</p>
<pre><code class="language-text">                    ┌→ 退款流程
输入 → 路由判断节点 ─┼→ 售价咨询流程
                    └→ 商品推荐流程
</code></pre>
<p>它适合不同类型请求需要不同处理逻辑的场景，例如：</p>
<ul>
<li>售前、售后、退款分类</li>
<li>普通问答、代码生成、数据分析</li>
<li>文本、图片、音频请求分类</li>
<li>不同领域知识库的选择</li>
</ul>
<h4 id="13-2-3-2-设计原则">13.2.3.2. 设计原则</h4>
<p>最佳实践：</p>
<ul>
<li><strong><code>LLM</code> 节点</strong>负责语义判断；</li>
<li><strong>路由函数</strong>负责把结构化结果映射到节点；</li>
<li><strong>业务节点</strong>负责真正处理任务。</li>
</ul>
<p>不要让路由 LLM 直接返回任意节点名，否则模型输出和图结构会过度耦合，也难以进行类型检查和异常兜底。</p>
<h4 id="13-2-3-3-示例">13.2.3.3. 示例</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from pydantic import BaseModel, Field
from typing import Literal,TypedDict
from langgraph.graph import StateGraph, START, END
from langchain.messages import HumanMessage, SystemMessage

from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 定义用于结构化输出的 Schema，作为路由判断依据
class Route(BaseModel):
    step: Literal[&quot;poem&quot;, &quot;story&quot;, &quot;joke&quot;] = Field(
        None,
        description=&quot;路由流程中的下一执行步骤&quot;,
    )

# 为大模型添加结构化输出能力
router = model.with_structured_output(Route)

# 图状态
class OverAllState(TypedDict):
    input: str
    decision: str
    output: str

# 节点
def model_call_1(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;生成故事&quot;&quot;&quot;

    result = model.invoke(
        [HumanMessage(content=state[&quot;input&quot;])]
    )
    return {&quot;output&quot;: result.content}


def model_call_2(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;生成笑话&quot;&quot;&quot;

    result = model.invoke(
        [HumanMessage(content=state[&quot;input&quot;])]
    )
    return {&quot;output&quot;: result.content}


def model_call_3(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;生成诗歌&quot;&quot;&quot;

    result = model.invoke(
        [HumanMessage(content=state[&quot;input&quot;])]
    )
    return {&quot;output&quot;: result.content}


def model_call_router(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;将用户输入路由到合适的节点&quot;&quot;&quot;

    # 调用具有结构化输出能力的大模型，完成路由判断
    decision = router.invoke(
        [
            SystemMessage(
                content=(
                    &quot;根据用户的请求，将其路由到 story、joke 或 poem。&quot;
                    &quot;请求编写故事时返回 story，请求编写笑话时返回 joke，&quot;
                    &quot;请求编写诗歌时返回 poem。&quot;
                )
            ),
            HumanMessage(content=state[&quot;input&quot;]),
        ]
    )

    return {&quot;decision&quot;: decision.step}


# 条件边函数：根据路由决策选择下一个节点
def route_decision(
        state: OverAllState
) -&gt; Literal[
    &quot;model_call_1&quot;,
    &quot;model_call_2&quot;,
    &quot;model_call_3&quot;,
    END
]:
    # 返回接下来要执行的节点名称
    if state[&quot;decision&quot;] == &quot;story&quot;:
        return &quot;model_call_1&quot;
    elif state[&quot;decision&quot;] == &quot;joke&quot;:
        return &quot;model_call_2&quot;
    elif state[&quot;decision&quot;] == &quot;poem&quot;:
        return &quot;model_call_3&quot;
    return END


# 构建工作流
builder = StateGraph(OverAllState)

# 添加节点
builder.add_node(&quot;model_call_1&quot;, model_call_1)
builder.add_node(&quot;model_call_2&quot;, model_call_2)
builder.add_node(&quot;model_call_3&quot;, model_call_3)
builder.add_node(&quot;model_call_router&quot;, model_call_router)

# 添加边，连接各个节点
builder.add_edge(START, &quot;model_call_router&quot;)
builder.add_conditional_edges(
    &quot;model_call_router&quot;,
    route_decision,
    {
        # route_decision 返回的名称：接下来要执行的节点名称
        &quot;model_call_1&quot;: &quot;model_call_1&quot;,
        &quot;model_call_2&quot;: &quot;model_call_2&quot;,
        &quot;model_call_3&quot;: &quot;model_call_3&quot;,
    },
)
builder.add_edge(&quot;model_call_1&quot;, END)
builder.add_edge(&quot;model_call_2&quot;, END)
builder.add_edge(&quot;model_call_3&quot;, END)

# 编译工作流
graph = builder.compile()

# 调用工作流
state = graph.invoke({&quot;input&quot;: &quot;写一个关于猫的笑话&quot;})
print(state[&quot;output&quot;])

# 显示工作流图
from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">这是一个关于猫的冷笑话，希望你喜欢：

**猫为什么不喜欢洗澡？**

**因为它不想变成“落汤包”（肉包）。**

（笑点解析：谐音梗，把“落汤猫”说成“落汤包”，而猫胖胖的确实有点像包子。）
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625180521496.png" alt="image-20260625180521496" /></p>
<hr />
<h3 id="13-2-4-orchestrator-worker-编排器-工作节点">13.2.4. <code>Orchestrator-worker</code>：编排器—工作节点</h3>
<h4 id="13-2-4-1-核心思想">13.2.4.1. 核心思想</h4>
<p>编排器先分析任务，动态生成若干子任务，再创建对应数量的 Worker：</p>
<pre><code class="language-text">                         ┌→ Worker 1 ─┐
输入 → Orchestrator ─────┼→ Worker 2 ─┼→ Synthesizer → 输出
                         ├→ Worker 3 ─┤
                         └→ Worker N ─┘
</code></pre>
<p>它通常包含三个组件：</p>
<table>
<thead>
<tr>
<th style="text-align:left">组件</th>
<th style="text-align:left">职责</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>Orchestrator</code></strong>（编排器）</td>
<td style="text-align:left">分析任务、制定计划、生成子任务列表</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>Workers</code></strong>（工作节点）</td>
<td style="text-align:left">分别处理各子任务，通常并行执行</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>Synthesizer</code></strong>（聚合节点）</td>
<td style="text-align:left">汇总所有 Worker 结果，生成最终答案</td>
</tr>
</tbody>
</table>
<p>适合任务数量无法预先确定的场景，例如：</p>
<ul>
<li>根据主题动态生成报告章节</li>
<li>修改未知数量的代码文件</li>
<li>为多个数据源分别执行分析</li>
<li>将长文档动态拆分为若干部分处理</li>
</ul>
<h4 id="13-2-4-2-和-parallelization-的区别">13.2.4.2. 和<code>Parallelization</code>的区别</h4>
<table>
<thead>
<tr>
<th style="text-align:left">模式</th>
<th style="text-align:left">任务数量</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>Parallelization</code></strong></td>
<td style="text-align:left">编译图时已确定（静态）</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>Orchestrator-worker</code></strong></td>
<td style="text-align:left">运行时动态计算</td>
</tr>
</tbody>
</table>
<h4 id="13-2-4-3-本质">13.2.4.3. 本质</h4>
<p><strong><code>Orchestrator-worker</code></strong> 本质上就是 <strong><code>Map-Reduce</code></strong>，我们在前面的课程中已经实现过了</p>
<pre><code class="language-text">Orchestrator：生成 Map 任务
Send：动态分发
Worker：执行 Map
Reducer：合并中间结果
Synthesizer：执行 Reduce
</code></pre>
<h4 id="13-2-4-4-示例">13.2.4.4. 示例</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Annotated, List, TypedDict
from collections.abc import Sequence

from langchain_core.messages import SystemMessage, HumanMessage
from langgraph.graph import StateGraph, START, END
from langgraph.types import Send
from langchain_deepseek import ChatDeepSeek

from operator import add
from dotenv import load_dotenv
from pydantic import BaseModel, Field

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 结构化输出的 Schema
class Section(BaseModel):
    name: str = Field(
        description=&quot;报告的章节名称&quot;,
    )
    description: str = Field(
        description=&quot;本节主题和核心思想的概述&quot;,
    )

class Sections(BaseModel):
    sections: List[Section] = Field(
        description=&quot;报告的章节列表&quot;,
    )

# 编排器
planner = model.with_structured_output(Sections)

# 图状态
class OverAllState(TypedDict):
    topic: str  # 报告主题
    sections: list[Section]  # 报告章节列表
    completed_sections: Annotated[
        list, add
    ]  # 所有工作节点并行写入该字段
    final_report: str  # 最终报告


# 工作节点状态
class WorkerState(TypedDict):
    section: Section
    completed_sections: Annotated[list, add]

# 节点
def orchestrator(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;编排器：生成报告编写计划&quot;&quot;&quot;

    # 生成报告章节规划
    report_sections = planner.invoke(
        [
            SystemMessage(content=&quot;为这份报告生成一个章节规划。&quot;),
            HumanMessage(content=f&quot;报告主题如下：{state['topic']}&quot;),
        ]
    )

    return {&quot;sections&quot;: report_sections.sections}

def model_call(state: WorkerState) -&gt; WorkerState:
    &quot;&quot;&quot;工作节点：编写报告中的一个章节&quot;&quot;&quot;

    # 生成章节内容
    section = model.invoke(
        [
            SystemMessage(
                content=(
                    &quot;根据提供的章节名称和章节描述编写报告内容。&quot;
                    &quot;不要在每个章节前添加额外的开场说明。&quot;
                    &quot;使用 Markdown 格式。&quot;
                )
            ),
            HumanMessage(
                content=(
                    f&quot;章节名称：{state['section'].name}\n&quot;
                    f&quot;章节描述：{state['section'].description}&quot;
                )
            ),
        ]
    )

    # 将生成的章节写入已完成章节列表
    return {&quot;completed_sections&quot;: [section.content]}


def synthesizer(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;将所有章节合成为完整报告&quot;&quot;&quot;

    # 获取所有已完成的章节
    completed_sections = state[&quot;completed_sections&quot;]

    # 将已完成章节拼接为最终报告
    completed_report_sections = &quot;\n\n---\n\n&quot;.join(completed_sections)

    return {&quot;final_report&quot;: completed_report_sections}


# 条件边函数：为规划中的每个章节创建一个 model_call 工作节点
def assign_workers(state: OverAllState) -&gt; Sequence[Send]:
    &quot;&quot;&quot;为规划中的每个章节分配一个工作节点&quot;&quot;&quot;

    # 使用 Send API 并行启动各个章节的编写任务
    return [Send(&quot;model_call&quot;, {&quot;section&quot;: s}) for s in state[&quot;sections&quot;]]


# 构建工作流
builder = StateGraph(state_schema=OverAllState)

# 添加节点
builder.add_node(&quot;orchestrator&quot;, orchestrator)
builder.add_node(&quot;model_call&quot;, model_call)
builder.add_node(&quot;synthesizer&quot;, synthesizer)

# 添加边，连接各个节点
builder.add_edge(START, &quot;orchestrator&quot;)
builder.add_conditional_edges(
    &quot;orchestrator&quot;,
    assign_workers,
    [&quot;model_call&quot;],
)
builder.add_edge(&quot;model_call&quot;, &quot;synthesizer&quot;)
builder.add_edge(&quot;synthesizer&quot;, END)

# 编译工作流
graph = builder.compile()

# 调用工作流
state = graph.invoke(
    {&quot;topic&quot;: &quot;撰写一份关于大语言模型缩放定律的报告&quot;}
)

from IPython.display import Markdown, display

# 显示工作流图
display(graph)
# 渲染报告
Markdown(state[&quot;final_report&quot;])
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260625180543541.png" alt="image-20260625180543541" /></p>
<pre><code class="language-markdown">引言
缩放定律是深度学习与人工智能领域中的核心理论基础之一。它揭示了模型性能与关键资源（如参数量、数据规模、计算算力）之间普遍存在的幂律关系，即当模型规模、训练数据量或计算预算成比例扩大时，模型的表现能力会呈现出可预测性的提升。这一发现最早源自对语言模型的系统性实证研究，随后在计算机视觉、多模态学习等众多任务中被广泛验证，深刻改变了现代AI研究的范式。

缩放定律的重要性体现在多个层面。从学术研究角度看，它不仅为“更大的模型、更多的数据”提供了科学依据，还催生了以GPT、PaLM、LLaMA为代表的大规模预训练模型浪潮。从工程实践角度看，缩放定律为资源分配提供了量化指导——研究人员可以据此预测模型性能对计算预算的依赖关系，从而制定更高效的训练策略。此外，在探索模型能力涌现性的前沿领域，缩放定律更是理解大语言模型行为边界的关键工具。因此，深入剖析缩放定律的理论内涵、实验验证与局限性，对于把握AI发展的底层逻辑具有不可替代的价值。

本报告将系统梳理缩放定律的研究脉络。后续章节首先阐述语言模型缩放的基本原理与核心公式；随后通过经典实验案例展示缩放定律的实证表现，并讨论其在不同任务与架构中的泛化能力；在此基础上，分析缩放定律的物理极限与开放挑战，包括数据效率、规模收益递减等问题；最后总结缩放定律对AI未来发展的战略启示。全文力求在学术严谨性与可读性之间取得平衡，为读者提供一幅关于缩放定律的全局图景。

缩放定律的基本概念
缩放定律（Scaling Laws）是深度学习中描述模型性能如何随模型规模（参数数量）、数据集规模（训练数据量）以及计算量（训练使用的计算资源，通常以FLOPs表示）变化的经验性规律。这些规律揭示了在大规模训练中，模型性能（例如损失值或下游任务准确率）与上述三个因素之间存在较为平滑且可预测的幂律关系。

具体而言，缩放定律通常由以下关系组成：

模型规模（Model Size）：在给定的数据集和计算预算下，增加模型参数数量（例如从几亿到数千亿）能够显著提升性能，但收益呈现递减趋势。性能提升与模型参数量的幂次方成正比，即 ( L \propto N^{-\alpha} )，其中 ( L ) 为损失， ( N ) 为参数数量， ( \alpha ) 为缩放指数。

数据集规模（Dataset Size）：在固定模型结构和计算资源的情况下，增加训练数据量同样可以降低损失。类似地，损失与数据量的关系可表示为 ( L \propto D^{-\beta} )，其中 ( D ) 为数据集规模， ( \beta ) 为对应的缩放指数。这意味着数据量越大，模型过拟合的风险越小，性能提升越稳定。

计算量（Compute）：将模型规模和数据规模同时考虑，总训练计算量（以FLOPs衡量）与性能的关系可概括为 ( L \propto C^{-\gamma} )，其中 ( C ) 为计算量。研究表明，在最优资源配置下，增加计算量能可靠地提升性能，且存在一个近似固定的计算最优前沿。

这些关系表明，模型性能并非由单一因素决定，而是三者共同作用的结果。缩放定律的核心在于，当预算（计算成本）固定时，存在一组最优的模型规模与数据规模分配，使得性能最大化。例如，若计算预算充足，则应同时扩大模型参数和训练数据；若某一资源受限，则需根据缩放指数的相对大小调整比例。

缩放定律的一个关键结论是：大模型是样本高效的——即随着模型规模增大，同样的数据量能带来更大的性能提升；反过来，大模型也是计算高效的——即同样的计算预算，训练更大的模型（配合适量数据）往往比训练小模型更优。这些规律为现代大规模语言模型的设计，如GPT系列、LLaMA等，提供了理论基础：它们正是遵循“同时扩大模型、数据和计算量”的指导原则。

核心发现：损失函数的幂律关系
Kaplan等人的研究揭示了大语言模型的损失函数与模型参数规模（(N)）、训练数据规模（(D)）以及计算量（(C)，通常以FLOPs衡量）之间存在明确的幂律（Power-Law）关系。这一发现为大规模模型的扩展提供了可预测的量化基础。

1. 损失与参数规模的幂律关系
在固定数据规模（(D)）的情况下，模型测试损失（(L)）与参数规模（(N)）呈现出双向的幂律行为：

欠参数化区域：当 (N) 较小时，模型容量不足，损失较高。
过参数化区域：当 (N) 持续增大，损失开始随参数增长以平滑的幂律形式下降，最终收敛于数据规模所限制的最优损失（即“不可约损失”）。
该关系可近似描述为： [ L(N) \approx L_{\infty} + \alpha_N \cdot N^{-\gamma_N} ] 其中 (L_{\infty}) 表示数据质量决定的不可约损失，(\gamma_N) 为幂律指数（通常在0.07-0.10之间），(\alpha_N) 为缩放常数。

2. 损失与数据规模的幂律关系
相似地，在固定参数规模（(N)）时，损失函数与训练数据规模（(D)）也呈现幂律趋势。当数据量增加，模型通过摄取更多示例改进泛化能力，损失按如下形式降低： [ L(D) \approx L_{\infty} + \alpha_D \cdot D^{-\gamma_D} ] 实验表明，(\gamma_D) 的典型值在0.07-0.10范围内，与 (\gamma_N) 接近，暗示参数和数据对损失的贡献在结构上是对称的。

3. 损失与计算量的幂律关系
计算量（(C)）被视为参数规模与数据规模的复合函数：(C \approx 6N \cdot D)（基于Transformer的前向与后向传播成本）。通过等比例扩展参数和数据，损失与计算量之间也遵循幂律规律： [ L(C) \approx L_{\infty} + \alpha_C \cdot C^{-\gamma_C} ] 其中 (\gamma_C) 通常在0.05-0.08左右，略低于前两个指数，反映了优化效率随规模增大而递减的边际效应。

4. 幂律关系的核心含义
可预测的扩展：只要维持参数和数据规模的平衡增长，损失减少的速率可被幂律精确预测，从而指导资源分配。
收益递减法则：幂律指数越小（绝对值），意味着每增加一单位计算量所获得的损失改善越慢，最终接近不可约损失。
Compute-Optimal极限：结合各项幂律曲线，存在一个最优的参数-数据配比，使得在给定总计算预算下损失最小化，这也是后续Chinchilla法则的理论基础。
综上所述，损失函数的幂律关系构成了大模型性能超越的关键先验知识，它强调了在扩展过程中参数、数据和计算量必须协同增长，否则因资源浪费（如只增加参数而不增加数据）导致幂律提升停滞。

数据与计算效率
在深度学习中，模型性能的提升往往依赖于增加参数数量与数据规模，但现实中的计算预算（如GPU时长、电力或成本）通常是有限的。如何在给定的计算预算下，最优地分配参数规模与数据规模，以实现最佳的模型性能，是数据与计算效率研究的核心问题。这涉及到缩放定律的深入理解以及训练策略的优化。

1. 缩放定律回顾与计算预算视角
根据经典的神经语言模型缩放定律（如Kaplan等人于2020年提出的研究，以及DeepMind的Chinchilla研究），模型性能（通常用验证损失衡量）受三个主要因素影响：模型参数数量（N）、训练数据Token数量（D）和计算预算（C）。缩放定律通常可以近似为幂律关系：损失 L(N,D) ≈ A/N^α + B/D^β + E，其中α和β是缩放指数，E是数据不可压缩的熵。计算预算与N和D并非独立，在大规模训练中，计算量主要来自前向和反向传播，通常近似为 C ≈ 6 * N * D（对于Transformer结构，忽略通信和嵌入层开销）。

给定固定的计算预算C，如果分配过少参数而使用过多数据（N过小，D过大），参数容量不足以捕捉数据中的模式，导致欠拟合；反之，如果参数过多而数据不足（N过大，D过小），模型会过拟合，无法通过足够的样本来泛化。最优分配的目标是找到N和D，使得在C固定的条件下，验证损失L最小化。

2. 计算最优训练：参数与数据的平衡点
Chinchilla缩放定律的研究指出，对于许多计算预算，最优的参数与数据规模满足近似关系：N_opt ∝ C^a 且 D_opt ∝ C^b，其中 a 和 b 之和接近1，且 a 通常小于 b。具体地，Chinchilla研究发现，对于使用Adam优化器的Transformer语言模型，当预算增加时，最优的参数和数据分配比例大致保持恒定，即 对于每个参数，约应分配20个训练Token（这一比例因模型架构和任务有所不同，但提供了一个直观的指导）。

例如，在计算预算为1e20 FLOPs的情况下，最优配置可能是约70B参数配合1.4T训练Token；而预算增加到2e20 FLOPs时，最优可能变为100B参数配合2T Token。这一发现反驳了早期“仅扩大参数而忽视数据”的做法，强调在固定预算下，同时按比例放大数据和参数比单独放大其中一个更有效。

3. 计算资源有限时的策略
当计算预算远小于现代主流规模（如单个GPU或小型集群）时，最优分配的策略需要更细致的考量：

预算极小的情况（如单GPU、几天训练）：参数规模和数据规模可能都受到硬件限制。此时，优先考虑模型容量，但需避免参数量超出能有效训练的最小数据量。通常，使用预训练模型微调比从头训练更高效，因为预训练模型已经在大规模数据上获得了通用表征，少量下游数据即可适配，从而将计算预算集中用于微调步骤。

中等预算（如多GPU、数周训练）：可参考缩放定律的比例关系。假设预算为C，根据N_opt ∝ C^0.5和D_opt ∝ C^0.5（此比例来自某些缩放研究，具体指数需要实验校准），推算合适的参数和Token数。实践中，可从公开的缩放曲线（如LLaMA、Chinchilla等）中提取经验公式，例如对于Transformer-LM，每增加一倍计算预算，同时将参数和Token增加约1.4倍（即C^0.5增长）。

预算极度受限且追求低成本：在边缘设备或快速原型开发中，计算效率意味着最小的参数规模实现足够好的性能。此时，推荐使用知识蒸馏、模型压缩或稀疏训练等方法。但这些方法本身需消耗计算资源，需权衡是否优于直接缩放

4. 数据效率的多重维度
除了参数与数据总量的分配，数据本身的特性也影响计算效率：

数据质量 vs. 数量：在有限计算预算下，使用高质量、代表性强的数据（例如经过筛选、去重、平衡类别的数据）往往比盲目增加数据量更有效。数据质量提升可降低所需的数据量，从而减少训练所需的计算量。

数据复用与课程学习：对数据进行多次训练（epoch）会引入过拟合风险，但在数据量不足时，适当的重复训练（如使用数据增强）可以增加有效样本数。课程学习（先易后难的数据顺序）可加速收敛，减少达到特定损失所需的计算步数。

主动学习与数据选择：在训练期间动态选择最具有信息性的数据子集进行训练，避免在容易或冗余样本上浪费计算资源。例如，基于损失值或不确定性采样，仅使用部分数据即可达到接近全量数据的性能。

5. 实际分配建议与工具使用
在工程实践中，最优分配可通过以下步骤实现：

建立小规模缩放曲线：使用计算预算的1%或更少进行小规模实验，训练不同（N,D）组合的模型，拟合幂律关系，推断出当前预算下的最优比例。
利用搜索或公式：参考已发表的缩放定律常数（如Chinchilla的20 Token/参数法则），但需注意这基于特定架构和数据集，应在目标数据上进行验证。
考虑数据生成成本：若数据获取比计算更昂贵（如需要人工标注），则应优先增大参数并使用预训练模型微调；反之，若计算昂贵而数据丰富（如互联网文本），则倾向于更多数据。
分阶段训练：若预算允许，可先训练一个大模型，再用其蒸馏小模型，是一种“先扩大参数，再缩小”的策略，但需要额外计算用于蒸馏。
总之，在有限计算预算下，最优地分配参数与数据规模的核心在于打破“越大越好”的直觉，理解缩放定律的隐含平衡，并通过质量控制和数据效率提升来最大化计算投入的回报。通过系统性实验和理论指导，可在固定成本下获得显著的性能提升。

超越损失：缩放定律对下游任务的影响
缩放定律最初源于对预训练损失随模型规模、数据量和计算量变化的观察，但其意义远不止于优化训练曲线。随着模型规模的增长，许多下游任务展现出超越简单损失改善的质性变化，呈现出涌现能力、上下文学习能力增强等非连续现象。本章将深入分析缩放定律如何预测并影响这些下游任务的表现，揭示规模提升带来的能力跃迁及其内在机制。

涌现能力：缩放定律的阶跃性预测
缩放定律通常描绘损失随规模平滑下降，但在下游任务上，模型能力并非线性提升，而是在达到特定规模后突然显现。例如，在算术推理、多步逻辑链、代码生成等任务中，小模型几乎无法完成，而大模型在参数跨过某个阈值时，准确率从接近零跃升至显著水平。这种涌现现象与实际任务性能的S形增长曲线相对应，而缩放定律通过预测模型不同区域的表示质量，揭示了涌现的触发条件：当模型参数量、数据量和训练步数满足特定组合时，隐层表征的容量与任务复杂度首次匹配。具体而言，缩放定律提供的幂律关系可以粗略估计模型在给定计算预算下的最优规模，当最优规模超过任务所需的最小表示容量时，涌现现象开始出现。因此，缩放定律并非保证所有任务同时涌现，而是定义了不同任务的能力门槛，预期在更大规模下能解锁更多高阶能力。

上下文学习能力的缩放效应
上下文学习——即模型无需梯度更新，仅通过在提示中提供少量示例即可执行新任务——是大规模语言模型最突出的特性之一。研究发现，上下文学习的性能与模型规模存在强正相关：小模型几乎不具备有效的上下文学习能力，随着参数规模扩大，模型能从示例中提取的模式更加准确，泛化偏差降低。缩放定律在此表现为：模型在上下文学习任务上的准确率通常符合对数或幂律增长，与预训练损失下降趋势并行。其背后的原因在于，随着规模增大，模型的前馈层和注意力头数增加，使得它能在前向传播中并行模拟更多的“推理步骤”，实现了内隐的梯度下降模拟。缩放定律预测，在固定计算预算下，规模更大的模型在上下文学习上的收益将超过同等效率的微调方法，从而引导研究者在提示工程和大规模预训练之间做出资源分配决策。

任务泛化与分布外表现
缩放定律对下游任务的影响不仅限于在分布内示例上的表现，更关键的是对分布外泛化能力的提升。实验表明，随着模型规模增长，模型在不同任务类别、不同领域、甚至不同语言之间的迁移性能稳定提升。这源于缩放定律中“数据多样性”的维度——更大的模型能更充分地利用多源数据中的结构共性，从而在未明确训练的领域形成鲁棒的假设。例如，在数学推理任务上，缩放定律使得模型能将从自然语言中学到的逻辑模式迁移至符号推理，表现出更强的类比和抽象能力。然而，这种迁移并非均匀：某些依赖低频知识或特殊模式的任务，可能仍需要接近边界规模才能突破泛化瓶颈。缩放定律因此为任务选择提供了依据——优先将计算资源分配给那些对规模敏感、且具有明确泛化回报的任务。

任务特定缩放曲线与优化经济性
不同下游任务对模型规模、数据量和计算量的响应曲线各异。有些任务（如简单分类或QA）在中小规模模型上即饱和，进一步扩展规模带来的收益递减很快；而另一些任务（如复杂推理、多语言理解）则持续受益于规模扩大。通过构建任务特定的缩放曲线（通常为幂律或指数饱和形式），研究者可以预测在给定计算预算下，投入资源以提升模型规模、增加训练数据或优化数据质量三者中的最优选择。例如，对于认知密集型任务，在模型规模上的投入回报率往往高于数据增量；而对于知识密集型任务，数据多样性可能更重要。这种缩放定律的延伸应用，使得模型部署决策更加数据驱动，避免了盲目扩大规模导致的经济浪费。

涌现能力背后的机制：表示压缩与结构对齐
进一步分析，缩放定律对涌现能力的积极影响可以理解为：大规模模型通过高维表示实现了对任务本质结构的压缩与对齐。在小规模模型中，隐层表示空间难以同时编码多个复杂模式，导致任务相关信号被噪声淹没。而当模型规模超过临界点，表示空间的各个维度开始分离出可解释的语义轴（如因果关系、时态关系、空间关系等）。缩放定律预测这些表示维度的“分化临界点”，在该点之后，模型能够以更少的冲突编码多个任务的隐含规则。此外，注意力模式的稳定化——即多头注意力在大型模型中形成更聚焦的依赖关系——进一步支撑了长程逻辑和上下文结构的捕捉，从而直接提升了下游任务的逻辑一致性和准确性。

缩放极限与下游任务瓶颈
尽管缩放定律整体上乐观地预测了规模带来的收益，但对下游任务的影响并非无限。随着模型参数进一步增大，部分任务（特别是需要外部知识更新或实时推理的任务）会遇到内存带宽瓶颈、指令遵循退化或幻觉加剧等问题。缩放定律在此提示，当模型规模超过数据质量所能支撑的有效容量时，损失的下行趋势放缓，而下游任务的性能可能进入“平台期”甚至出现波动。例如，极端大的模型可能对上下文中的干扰信息更敏感，导致在简单任务上的表现反常。因此，未来在追求更优下游任务表现时，不仅需要继续扩展规模，还必须结合数据治理、对齐训练和检索增强等手段，以跨越缩放定律在应用层面的新瓶颈。

3.3 Chinchilla缩放定律与最优计算分配
在深度学习的规模探索中，DeepMind提出的Chinchilla研究引入了关于模型与数据规模分配的新视角。以往的研究，如Kaplan等人提出的缩放定律，倾向于认为在增加模型参数时，数据规模可以相对缓慢地增长，即模型越大，性能随计算量提升的边际收益越显著。然而，Chinchilla研究对此提出了挑战。

核心观点：对于给定的计算预算（即总计算量，通常以FLOPs衡量），模型参数数量与训练数据规模（Token数量）应当等比例缩放。换言之，当计算预算增加时，为了达到最优性能，必须同时增加模型的参数和训练数据量，且两者间存在一个最优的比例关系。

研究方法。Chinchilla团队通过系统地变化模型大小（参数数量）和训练数据量（Token数量），在固定计算预算下评估模型性能。他们发现，在以往的研究中，许多大型模型（如GPT-3）实际上是在“计算欠调度”状态下训练的——即模型过大而数据不足。这意味着，在同样的计算预算下，如果一个模型较小但训练在更多的数据上，它可能会取得更好的性能。

关键结论。Chinchilla揭示了最优计算分配的法则：

最优模型参数与训练Token数应遵循大致相等的缩放率。
具体而言，对于主流Transformer模型，最优的模型参数数量（N）与训练Token数量（D）之间满足：N ∝ D。这意味着，当计算预算翻倍时，应同时将模型大小和数据量分别增加约1.4倍（即N ∝ C^0.5, D ∝ C^0.5，其中C为计算量）。
实际影响。基于这一发现，DeepMind训练了Chinchilla模型：一个拥有70亿参数的模型，但使用1.4万亿个Token进行训练。与当时更大规模的模型（如Gopher，2800亿参数，仅3000亿Token训练）相比，在相同的计算预算下，Chinchilla在多种自然语言处理任务中表现更优或相当。这证明了“小而精”的缩放策略的潜力：与其盲目增大模型，不如在模型和数据间取得更平衡的分配。

理论意义。Chinchilla缩放定律重新定义了“缩放”的内涵——它不再是单纯的参数扩展，而是计算资源在模型复杂度（参数空间）和数据复杂度（经验分布覆盖度）之间的优化分配。这一观点为后续的模型训练（如LLaMA系列）提供了重要指导，强调了数据质量与规模的重要性不亚于模型架构的复杂性。

缩放定律的局限性与挑战
缩放定律的边界

缩放定律（Scaling Laws）揭示了模型性能与模型参数、数据量、计算量之间的幂律关系，但这一规律并非无限制成立。随着模型规模的持续增长，性能提升呈现边际递减趋势，最终趋近于一个理论或实际的上限。该边界的出现源于多重因素：首先，现有训练数据的质量与多样性存在天花板，即使增加数据量，新数据提供的信息增益也会下降；其次，模型容量受限于硬件架构（如内存带宽、计算单元效率），极端规模的模型可能触发通信瓶颈或能耗约束；再者，缩放定律本身假设了理想化的训练条件，未考虑实际训练中的噪声、分布偏移等扰动。因此，简单地堆叠参数和计算资源已难以突破性能瓶颈，研究者需重新审视缩放定律的适用范围。

数据稀缺的困境

高质量训练数据的稀缺性是当前缩放定律面临的核心挑战之一。自然语言、图像等领域中，人工标注或精选数据的增长速率远落后于模型参数规模的扩展。无监督数据虽可大规模获取，但其噪声、冗余和不平衡分布可能导致模型学习到虚假相关性，而非真正有意义的模式。例如，在罕见语言任务或专业领域（如医学、法律）中，数据稀疏性直接限制了模型泛化能力。此外，基于互联网爬取的粗筛数据包含偏见、脏标签和版权问题，进一步加剧了数据质量与规模之间的张力。解决这一困境需要探索数据增强、半监督学习或合成数据生成等替代方案，但现有方法仍难以完全弥补真实数据的缺失。

训练不稳定性问题

大规模模型训练固有的不稳定性是缩放定律实际应用的又一障碍。当模型参数增至数百亿甚至万亿级别时，优化过程中的梯度爆炸、损失振荡、模式坍塌等问题显著增加。这主要由以下原因引起：深度网络中的梯度传播路径过长导致信号衰减；批量规模（batch size）与学习率之间的协变关系难以维护；分布式训练中的异步通信引入延迟和参数不一致。训练不稳定性不仅延长了收敛时间，还可能使模型陷入局部最优或产生灾难性遗忘。现有缓解手段（如梯度裁剪、混合精度训练、优化器调参）虽能部分缓解问题，但缺乏理论保证，尤其是当模型规模跨越特定临界点后，传统训练策略可能完全失效。

超越简单缩放的模型架构创新

面对缩放定律的局限性，研究者正转向非简单缩放的架构创新，以绕过参数扩张的性能瓶颈。例如，混合专家模型（MoE）通过稀疏激活机制，在增加总参数量的同时保持计算成本接近线性增长；维度扩展策略（如线性注意力、长序列建模）尝试替代传统transformer中的二次复杂度注意力机制；此外，结构化权重矩阵（如低秩分解、张量分解）能在不显著增加模型尺寸的前提下提升表达能力。这些创新揭示了更高效的缩放方向：而非盲目提升参数量，优化模型内部的信息流动效率、参数复用率以及计算资源分布。未来，结合神经架构搜索（NAS）或自演化设计的方法，可能进一步打破缩放定律的线性束缚，开辟新的性能增长路径。

结论与展望
本文围绕缩放定律（Scaling Laws）展开了系统性分析，揭示了大规模模型性能与计算资源、数据规模及参数数量之间存在的幂律关系。核心结论包括：模型性能在相当广泛的范围内随计算预算、数据集大小和参数数量的增加而稳定提升，这一规律为现代深度学习模型的规模化设计提供了理论依据与工程指导。然而，缩放并非无代价，边际收益递减、训练成本激增以及数据质量瓶颈成为进一步扩展的主要制约因素。

在未来研究方向上，稀疏模型（Sparse Models）展现出突破现有缩放瓶颈的潜力。通过引入混合专家系统（MoE）、参数高效微调及动态结构剪枝等机制，稀疏模型可在不显著增加计算开销的前提下，实现参数规模的有效放大。该方向有望打破密集模型中的线性缩放约束，推动模型容量与效率的协同增长。

多模态缩放（Multimodal Scaling）是另一个值得关注的前沿领域。当前缩放定律多聚焦于单一模态（如文本或图像），而跨模态联合训练下性能与资源间的映射关系尚不明确。未来亟需建立针对多模态输入的统一缩放框架，探索视觉、语言、音频等异构数据在共同训练过程中的交互效应与计算效率优化策略。

此外，训练数据质量与来源的多样性、模型泛化边界、以及缩放定律在非自监督学习范式（如强化学习、在线学习）中的适用性，构成后续研究需要深入探讨的关键议题。随着实际部署对能耗、推理速度和环境可持续性提出更高要求，缩放定律的应用亦需与资源约束感知模型设计及低碳训练方法相结合。总之，未来研究需在理解缩放基本规律的基础上，拓展其边界，提升其可控性，并为其在更广泛人工智能系统中的部署提供理论支持与实践路径。
</code></pre>
<hr />
<h3 id="13-2-5-evaluator-optimizer-评估器-优化器">13.2.5. <code>Evaluator-optimizer</code>：评估器—优化器</h3>
<h4 id="13-2-5-1-核心思想">13.2.5.1. 核心思想</h4>
<p>一个节点生成结果，另一个节点评估结果；不合格就携带反馈重新生成：</p>
<pre><code class="language-text">          ┌──────────────────────┐
          ↓                      │
输入 → Generator → Evaluator ────┤
                      ├─ 不合格 ─┘
                      └─ 合格 → END
</code></pre>
<p>适合：</p>
<ul>
<li>翻译质量迭代</li>
<li>代码生成与代码审查</li>
<li>文案生成与合规检查</li>
<li><strong><code>SQL</code></strong> 生成与语法验证</li>
<li>报告生成与事实检查</li>
<li>人工审批后修改</li>
</ul>
<p>这种模式适用于存在明确质量标准，但通常需要多轮修改才能满足标准的任务。评估者既可以是 <strong><code>LLM</code></strong>，也可以是规则、测试程序或人类。</p>
<h4 id="13-2-5-2-必要的控制">13.2.5.2. 必要的控制</h4>
<p>为了避免无限循环，必须引入递归限制，可以是</p>
<ul>
<li>优雅退出的主动方法</li>
<li>异常中断的被动方法</li>
</ul>
<p>我们在介绍循环结构时已经系统讲解过了。</p>
<h4 id="13-2-5-3-示例">13.2.5.3. 示例</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
from pydantic import BaseModel, Field

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 图状态
class OverAllState(TypedDict):
    joke: str
    topic: str
    feedback: str
    funny_or_not: str

# 定义用于结构化输出的 Schema，作为笑话评估依据
class Feedback(BaseModel):
    grade: Literal[&quot;好笑&quot;, &quot;不好笑&quot;] = Field(
        description=&quot;判断这个笑话是否好笑。&quot;,
    )
    feedback: str = Field(
        description=&quot;如果笑话不好笑，请给出具体的改进建议。&quot;,
    )

# 为大模型添加结构化输出能力，定义评估器
evaluator = model.with_structured_output(Feedback)

# 节点
def model_call_generator(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;大模型生成笑话&quot;&quot;&quot;

    if state.get(&quot;feedback&quot;):
        msg = model.invoke(
            f&quot;&quot;&quot;
            写一个关于“{state['topic']}”的笑话。

            请参考下面的改进建议：
            {state['feedback']}
            &quot;&quot;&quot;
        )
    else:
        msg = model.invoke(f&quot;写一个关于“{state['topic']}”的笑话&quot;)

    return {&quot;joke&quot;: msg.content}


def model_call_evaluator(state: OverAllState) -&gt; OverAllState:
    &quot;&quot;&quot;大模型评估笑话&quot;&quot;&quot;

    grade = evaluator.invoke(
        f&quot;&quot;&quot;
        请评估下面这个笑话是否好笑：

        {state['joke']}
        &quot;&quot;&quot;
    )

    return {
        &quot;funny_or_not&quot;: grade.grade,
        &quot;feedback&quot;: grade.feedback,
    }


# 条件边函数：根据评估结果决定结束流程，或者返回笑话生成节点
def route_joke(state: OverAllState) -&gt; Literal[&quot;accept&quot;, &quot;reject_and_feedback&quot;, END]:
    &quot;&quot;&quot;根据评估结果决定接受笑话或根据反馈重新生成&quot;&quot;&quot;

    if state[&quot;funny_or_not&quot;] == &quot;好笑&quot;:
        return &quot;accept&quot;
    elif state[&quot;funny_or_not&quot;] == &quot;不好笑&quot;:
        return &quot;reject_and_feedback&quot;
    return END

# 构建工作流
builder = StateGraph(state_schema=OverAllState)

# 添加节点
builder.add_node(&quot;model_call_generator&quot;, model_call_generator)
builder.add_node(&quot;model_call_evaluator&quot;, model_call_evaluator)

# 添加边，连接各个节点
builder.add_edge(START, &quot;model_call_generator&quot;)
builder.add_edge(&quot;model_call_generator&quot;, &quot;model_call_evaluator&quot;)
builder.add_conditional_edges(
    &quot;model_call_evaluator&quot;,
    route_joke,
    {
        # route_joke 返回的名称：接下来要执行的节点
        &quot;accept&quot;: END,
        &quot;reject_and_feedback&quot;: &quot;model_call_generator&quot;,
    },
)

# 编译工作流
graph = builder.compile()

# 调用工作流
state = graph.invoke({&quot;topic&quot;: &quot;猫&quot;})
print(state[&quot;joke&quot;])

# 显示工作流图
from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python"># 关于猫的笑话

一只猫去应聘当保安。

面试官问：“你有什么特长？”

猫说：“我精通‘猫步’，走路悄无声息，可以抓到任何小偷。”

面试官点头：“不错。那面对危险情况，你会怎么处理？”

猫淡定地说：“我会用‘九条命’精神，即使失败一次，还有八次机会总结经验。”

面试官很满意：“那我们决定录用你。不过，我们有个夜班岗位，你介意夜间工作吗？”

猫“喵”了一声：“正合我意，白天我要晒着太阳睡觉的。”

然后面试官又问：“最后一个问题——你如果发现仓库里有老鼠，会怎么处置？”

猫瞪大了眼睛：“老鼠？那是我请来的线人！没有老鼠，我怎么假装抓到战绩，让你们给我加罐头？”

面试官：“……你被开除了。”

猫耸耸肩：“无所谓，反正我还有八条命去找下一份工作。”
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625180812793.png" alt="image-20260625180812793" /></p>
<hr />
<h3 id="13-2-6-agent-智能体循环">13.2.6. <code>Agent</code>：智能体循环</h3>
<h4 id="13-2-6-1-核心思想">13.2.6.1. 核心思想</h4>
<p>前面的运行图设计模式都属于 <strong><code>Workflow</code></strong>，<strong><code>Agent</code></strong> 与 <strong><code>Workflow</code></strong> 最大的不同是：</p>
<blockquote>
<p>开发者不再预先确定每一步具体执行什么，而是让 <strong><code>LLM</code></strong> 根据当前消息和工具结果决定下一步行为。</p>
</blockquote>
<p>基本拓扑是：</p>
<pre><code class="language-text">          ┌─────────────────┐
          ↓                 │
输入 → LLM 决策 → 是否调用工具
                ├─ 是 → Tool ┘
                └─ 否 → END
</code></pre>
<p>即最基础的 <strong><code>ReAct</code></strong> 架构，我们已经不止一次介绍并实现过 <strong><code>ReAct</code></strong> 架构的 <strong><code>Agent</code></strong></p>
<p><strong><code>Agent</code></strong> 适合问题求解步骤和工具调用顺序无法预先确定的场景。开发者仍然负责提供工具集合、系统提示词、权限和边界，但具体执行过程由模型动态决定。</p>
<h4 id="13-2-6-2-示例">13.2.6.2. 示例</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END, MessagesState
from langgraph.prebuilt.tool_node import ToolNode

from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek
from langchain.tools import tool

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

@tool(parse_docstring=True)
def get_weather(city: str) -&gt; str:
    &quot;&quot;&quot;
    根据城市查询当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    return f&quot;{city} 今天天气不错&quot;

tools = [get_weather]
model_with_tools = model.bind_tools(tools=tools)

def model_node(state: MessagesState) -&gt; MessagesState:
    messages = state[&quot;messages&quot;]
    response = model_with_tools.invoke(input=messages)

    return {
        &quot;messages&quot;: [response]
    }

def router(state: MessagesState) -&gt; Literal[&quot;tool_node&quot;, END]:
    last_msg = state[&quot;messages&quot;][-1]

    if last_msg.tool_calls:
        return &quot;tool_node&quot;
    return END

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;model_node&quot;, model_node)
builder.add_node(&quot;tool_node&quot;, ToolNode(tools=tools))

builder.add_edge(START, &quot;model_node&quot;)
builder.add_conditional_edges(&quot;model_node&quot;, router, path_map=[&quot;tool_node&quot;, END])
builder.add_edge(&quot;tool_node&quot;, &quot;model_node&quot;)

graph = builder.compile()

response = graph.invoke({&quot;messages&quot;: [HumanMessage(content=&quot;今天北京天气怎么样？&quot;)]})
for msg in response[&quot;messages&quot;]:
    msg.pretty_print()

from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-python">================================ Human Message =================================

今天北京天气怎么样？
================================== Ai Message ==================================

好的，我来帮你查询一下今天北京的天气情况。
Tool Calls:
  get_weather (call_00_3kFtzQHjGl7VJkfBi5280445)
 Call ID: call_00_3kFtzQHjGl7VJkfBi5280445
  Args:
    city: 北京
================================= Tool Message =================================
Name: get_weather

北京 今天天气不错
================================== Ai Message ==================================

今天北京的**天气不错**哦！☀️

看起来是个适合出门活动的好天气，不用太担心下雨或恶劣天气的问题。如果你有具体的户外活动计划，应该会比较顺利。

请问还有其他需要帮忙的吗？😊
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260625180832289.png" alt="image-20260625180832289" /></p>
]]></content>
  </entry>
  <entry>
    <title>04-LangGraph中断与工具与部署</title>
    <url>/2025/05/22/langgraph-interrupt-tools-deployment/</url>
    <content><![CDATA[<h1 id="8-中断">8. 中断</h1>
<p><strong><code>LangGraph</code></strong> 提供了两种中断机制：</p>
<ul>
<li>
<p><strong>动态中断</strong>：在图的任意节点中调用 <strong><code>interrupt()</code></strong> 函数实现</p>
<p>它可以放在代码的任意位置，并且可以根据应用逻辑设置条件触发，所以是<strong>动态</strong>的。</p>
<p>动态中断提供了人机交互接口，使得调用者可以人为干预计算图的运行，<strong>是业务逻辑的一部分</strong>。</p>
</li>
<li>
<p><strong>静态中断</strong>：在编译或调用状态图时通过 <strong><code>interrupt_before</code></strong> 和 <strong><code>interrupt_after</code></strong> 参数设置断点</p>
<p>它是在运行前确定的，不能根据业务逻辑条件触发，所以是<strong>静态</strong>的。</p>
<p>静态中断主要用于调试，<strong>不是业务逻辑的一部分</strong>。</p>
</li>
</ul>
<h2 id="8-1-动态中断">8.1. 动态中断</h2>
<h3 id="8-1-1-概述">8.1.1. 概述</h3>
<p><strong><code>LangGraph</code></strong> 的动态中断机制允许用户在图执行过程中设置暂停点，等待外部输入后再继续执行，提供了人机交互接口。</p>
<p>当中断触发时，<strong><code>LangGraph</code></strong> 会通过持久化机制保存当前图状态，并无限期等待直到用户恢复执行。</p>
<p>中断通过在任意图节点中调用 <strong><code>interrupt()</code></strong> 函数实现，该函数可以接收任何可 <strong><code>JSON</code></strong> 序列化的值，并将该值暴露给调用方。</p>
<p>中断触发后，调用方根据 <strong><code>interrupt()</code></strong> 暴露的信息生成反馈，重新调用图时通过 <strong><code>Command</code></strong> 将反馈传递给计算图，恢复图的运行。</p>
<p>恢复运行后，调用方通过 <strong><code>Command</code></strong> 传递的反馈会作为 <strong><code>interrupt()</code></strong> 函数的返回值，参与后续计算。</p>
<h3 id="8-1-2-启用中断">8.1.2. 启用中断</h3>
<ol>
<li>
<p>配置检查点存储器</p>
</li>
<li>
<p>设置 <strong><code>thread_id</code></strong></p>
<p>如果要确保中断后可以恢复执行，就需要保存运行图的完整状态，所以必须<strong>启用可恢复执行</strong>。</p>
</li>
<li>
<p>在需要中断的位置调用 <strong><code>interrupt()</code></strong></p>
</li>
</ol>
<h3 id="8-1-3-恢复中断">8.1.3. 恢复中断</h3>
<p>满足以下要求：</p>
<ul>
<li>
<p>基于相同的配置再次调用计算图</p>
</li>
<li>
<p>将输入替换为 <strong><code>Command()</code></strong> 实例即可恢复运行。</p>
</li>
</ul>
<p><strong><code>LangGraph</code></strong> 会从中断节点继续运行，该节点会被再次执行。</p>
<p>通过 <strong><code>Command</code></strong> 实例的 <strong><code>resume</code></strong> 属性将用户反馈传递给计算图，中断节点重新运行时，传递给 <strong><code>resume</code></strong> 属性的值将会作为 <strong><code>interrupt()</code></strong> 函数的返回值。</p>
<h3 id="8-1-4-常见使用模式">8.1.4. 常见使用模式</h3>
<p>根据中断发生的位置、业务场景以及执行结构，动态中断可以衍生出多种常见的使用模式。本节将介绍以下模式：</p>
<ol>
<li><strong>基础 HITL 模式</strong>：状态图触发一次中断，获取人类输入后继续执行。</li>
<li><strong>多个并行中断</strong>：多个并行任务分别产生中断，并根据 <strong>中断 <code>ID</code></strong> 接收各自的恢复数据。</li>
<li><strong>审批模式</strong>：根据人类审批（批准、拒绝或其它处理方式），决定后续执行路径。</li>
<li><strong>审核与编辑模式</strong>：将模型生成的内容交给人类检查，并允许人类直接修改后继续处理。</li>
<li><strong>工具执行审批模式</strong>：在调用工具或执行具有副作用的操作之前，由人类确认是否允许执行。</li>
<li><strong>单节点串行中断模式</strong>：在同一个节点内多次触发中断，上一个中断恢复后才能触发下一个。</li>
<li><strong>人类输入验证模式</strong>：对人类输入进行校验；当输入不符合要求时，再次中断并要求重新输入。</li>
</ol>
<p>需要注意，这些模式并不是完全互斥的。例如，工具执行审批本质上也是审批模式的一种，只是它专门应用于工具调用场景。</p>
<h4 id="8-1-4-1-基础-hitl-模式">8.1.4.1. 基础**<code>HITL</code>**模式</h4>
<h5 id="8-1-4-1-1-什么是-hitl">8.1.4.1.1. 什么是<code>HITL</code></h5>
<p><strong><code>HITL(Human In The Loop)</code></strong> 即<strong>人在环</strong>，是一种经典的人机协同设计模式，它允许计算图在运行过程中暂停，等待人工审批、修正意见或补充信息，然后从暂停位置恢复执行。</p>
<h5 id="8-1-4-1-2-基础-hitl-模式的实现">8.1.4.1.2. 基础<code>HITL</code>模式的实现</h5>
<p>调用 <strong><code>interrupt()</code></strong> 可以暂停状态图的执行，并等待外部输入后恢复运行。因此它是实现 <strong><code>HITL</code></strong> 的核心机制。</p>
<p>不过，仅仅调用 <strong><code>interrupt()</code></strong> 并不一定构成 <strong><code>HITL</code></strong>。只有当中断后的决策、输入或修改确实由人类完成时，才能称为 <strong><code>HITL</code></strong>；如果恢复数据完全由程序自动提供，本质上仍然是普通的中断与恢复机制。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict

from langgraph.graph import StateGraph,START,END
from langgraph.types import interrupt, Command
from langgraph.checkpoint.memory import InMemorySaver


#1. 声明状态
class OverAllState(TypedDict):
    username:str

#2. 声明节点
def node_a(state:OverAllState)-&gt;OverAllState:
    username = interrupt(&quot;请输入您的姓名&quot;)
    return {
        &quot;username&quot;:username
    }

#3. 构建图
builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;,node_a)
builder.add_edge(START,&quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;,END)

#4. 想要使用中断 =&gt; 必须配置检查点
checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;:{&quot;thread_id&quot;:&quot;123&quot;}}
interrupt_res = graph.invoke({},config=config)
print(interrupt_res)

prompt = interrupt_res['__interrupt__'][0].value
print(prompt)
username = input(prompt)
# 5. 恢复图的中断执行
resume_res = graph.invoke(Command(resume=username),config=config)
print(resume_res)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260616144341319.png" alt="image-20260616144341319" /></p>
<pre><code class="language-json">============================== -&gt; interrupt_res &lt;- ==============================

{'__interrupt__': [Interrupt(value='请输入您的姓名', id='3543beca616d469fe1ccd35250ef5acf')]}

============================== -&gt; resumed_res &lt;- ==============================

{'username': '小黄'}
</code></pre>
<h4 id="8-1-4-2-多个并行中断">8.1.4.2. 多个并行中断</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict

from langgraph.graph import StateGraph, START, END
from langgraph.types import Command, interrupt
from langgraph.checkpoint.memory import InMemorySaver

class OverAllState(TypedDict):
    username: str
    age: int

def node_a(state: OverAllState) -&gt; OverAllState:
    username = interrupt(&quot;请输入您的姓名&quot;)
    return {
        &quot;username&quot;: username
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    age = interrupt(&quot;请输入您的年龄&quot;)
    return {
        &quot;age&quot;: age
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(START, &quot;node_b&quot;)
builder.add_edge(&quot;node_a&quot;, END)
builder.add_edge(&quot;node_b&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
interrupted_res = graph.invoke({}, config=config)
print('=' * 30, '-&gt; interrupt_res &lt;-', '=' * 30)
print(interrupted_res)

# 构建 中断ID -&gt; 恢复指令 的映射
resume_map = {}
for i in interrupted_res['__interrupt__']:
    user_input = input(f&quot;{i.value}: &quot;)
    if &quot;年龄&quot; in i.value:
        resume_map[i.id] = int(user_input)
    else:
        resume_map[i.id] = user_input

resumed_res = graph.invoke(Command(resume=resume_map), config=config)
print('=' * 30, '-&gt; resumed_res &lt;-', '=' * 30)
print(resumed_res)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260616144631814.png" alt="image-20260616144631814" /></p>
<pre><code class="language-json">============================== -&gt; interrupt_res &lt;- ==============================

{'__interrupt__': [Interrupt(value='请输入您的年龄', id='2770f00729006c756d481fffa712bb27'), Interrupt(value='请输入您的姓名', id='db7e81e1d4f5cab611f07e0469e99854')]}

============================== -&gt; resumed_res &lt;- ==============================

{'username': '小黄', 'age': 19}
</code></pre>
<h4 id="8-1-4-3-审批模式">8.1.4.3. 审批模式</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from time import sleep
from typing import TypedDict, Literal

from langchain_core.messages import HumanMessage
from langgraph.graph import StateGraph,START,END
from langgraph.types import interrupt, Command
from langgraph.checkpoint.memory import InMemorySaver
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
load_dotenv(override=True)

model =ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;:{
            &quot;type&quot;:&quot;disabled&quot;
        }
    }
)

#1. 声明状态
class OverAllState(TypedDict):
    topic:str
    poem:str
    is_approved:bool

#2. 声明节点
def approve_node(state:OverAllState) -&gt; Command[Literal[&quot;llm_node&quot;,&quot;default_node&quot;]]:
    is_approved = interrupt(&quot;是否同意调用模型?&quot;)
    goto = &quot;llm_node&quot; if is_approved else &quot;default_node&quot;
    return Command(
        goto=goto,
        update={&quot;is_approved&quot;:is_approved}
    )

def llm_node(state:OverAllState) -&gt; OverAllState:
    topic = state[&quot;topic&quot;]
    res = model.invoke([HumanMessage(content=f&quot;帮我写一首关于{topic}主题的七言绝句,只写诗句,不需要赏析&quot;)]).content

    return {
        &quot;poem&quot;:res
    }

def default_node(state:OverAllState) -&gt; OverAllState:
    return {
        &quot;poem&quot;:&quot;请求被拒绝&quot;
    }

#3. 构建图
builder = StateGraph(state_schema=OverAllState)

builder.add_node(&quot;approve_node&quot;,approve_node)
builder.add_node(&quot;llm_node&quot;,llm_node)
builder.add_node(&quot;default_node&quot;,default_node)
builder.add_edge(START,&quot;approve_node&quot;)
builder.add_edge(&quot;llm_node&quot;,END)
builder.add_edge(&quot;default_node&quot;,END)

#4. 添加检查点后端
checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

from IPython.display import display
display(graph)

#5. 首次执行 -&gt; 触发中断
config = {&quot;configurable&quot;:{&quot;thread_id&quot;:&quot;234&quot;}}
interrupt_res = graph.invoke({&quot;topic&quot;:&quot;菊花&quot;},config=config)
print(interrupt_res)
#6. 人工审核
user_approved = input(&quot;是否同意调用模型?(y/n)&quot;).strip().lower() == 'y'
# 重新调用图
approved_res = graph.invoke(Command(resume=user_approved),config=config)
print(approved_res)

#7. 审核不通过
config1 = {&quot;configurable&quot;:{&quot;thread_id&quot;:&quot;456&quot;}}
interrupt_res = graph.invoke({&quot;topic&quot;:&quot;牡丹花&quot;},config=config1)
print(interrupt_res)

user_approved = input(&quot;是否同意调用模型?(y/n)&quot;).strip().lower() == 'y'
# 重新调用图
approved_res = graph.invoke(Command(resume=user_approved),config=config1)
print(approved_res)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260616144742361.png" alt="image-20260616144742361" /></p>
<pre><code class="language-json">{'topic': '菊花', '__interrupt__': [Interrupt(value='是否同意调用模型?', id='91edf41ecf736a242c18fd61d40ad5b4')]}

{'topic': '菊花', 'poem': '《咏菊》\n西风猎猎卷霜华，独抱秋心隐士家。\n冷艳不争桃李色，寒香偏入野人茶。', 'is_approved': True}

{'topic': '牡丹花', '__interrupt__': [Interrupt(value='是否同意调用模型?', id='47958afc1277b7ce6bf850a5f27f2e46')]}
{'topic': '牡丹花', 'poem': '请求被拒绝', 'is_approved': False}
</code></pre>
<h4 id="8-1-4-4-审核与编辑模式">8.1.4.4. 审核与编辑模式</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.types import Command, interrupt
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek
from langgraph.checkpoint.memory import InMemorySaver
from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    poem: str
    reviewed_poem: str

def llm_node(state: OverAllState) -&gt; OverAllState:
    topic = state['topic']

    res = model.invoke([HumanMessage(content=f&quot;帮我写一首关于 {topic} 的七言绝句，只给出诗句，不要赏析&quot;)]).content
    return {
        &quot;poem&quot;: res
    }

def review_node(state: OverAllState) -&gt; OverAllState:
    reviewed_poem = interrupt({
        &quot;instruction&quot;: &quot;请审核并修改大模型生成的七言绝句&quot;,
        &quot;poem&quot;: state['poem']
    })
    return {
        &quot;reviewed_poem&quot;: reviewed_poem
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;review_node&quot;, review_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, &quot;review_node&quot;)
builder.add_edge(&quot;review_node&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;review_test&quot;}}
interrupted_res = graph.invoke({&quot;topic&quot;: &quot;布偶猫&quot;}, config=config)
print('=' * 30, '-&gt; interrupt_res &lt;-', '=' * 30)
print(interrupted_res)

print(&quot;原始诗句：&quot;)
print(interrupted_res['__interrupt__'][0].value['poem'])
user_review = input(&quot;请审核并修改诗句（直接回车保留原诗）: &quot;)
if not user_review.strip():
    user_review = interrupted_res['__interrupt__'][0].value['poem']
reviewed_res = graph.invoke(Command(resume=user_review), config=config)
print('=' * 30, '-&gt; reviewed_res &lt;-', '=' * 30)
print(reviewed_res)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260616144804813.png" alt="image-20260616144804813" /></p>
<pre><code class="language-json">============================== -&gt; interrupt_res &lt;- ==============================

{'topic': '布偶猫', 'poem': '《布偶猫》\n冰眸玉骨雪团身，蓝影衔春卧月轮。\n偶启朱唇呼入梦，云鬟一枕醉红尘。', '__interrupt__': [Interrupt(value={'instruction': '请审核并修改大模型生成的七言绝句', 'poem': '《布偶猫》\n冰眸玉骨雪团身，蓝影衔春卧月轮。\n偶启朱唇呼入梦，云鬟一枕醉红尘。'}, id='de13581234c57fb8b5d9620295b8239e')]}

============================== -&gt; reviewed_res &lt;- ==============================

{'topic': '布偶猫', 'poem': '《布偶猫》\n冰眸玉骨雪团身，蓝影衔春卧月轮。\n偶启朱唇呼入梦，云鬟一枕醉红尘。', 'reviewed_poem': '已审核：《布偶猫》\n冰眸玉骨雪团身，蓝影衔春卧月轮。\n偶启朱唇呼入梦，云鬟一枕醉红尘。'}
</code></pre>
<h4 id="8-1-4-5-工具执行审批模式">8.1.4.5. 工具执行审批模式</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END
from langgraph.types import Command, interrupt
from langgraph.graph.message import MessagesState
from langgraph.checkpoint.memory import InMemorySaver
from langchain.messages import HumanMessage, ToolMessage
from langchain.tools import tool
from langchain_deepseek import ChatDeepSeek

from loguru import logger
from dotenv import load_dotenv
load_dotenv(override=True)

@tool(parse_docstring=True)
def get_weather(city: str) -&gt; str:
    &quot;&quot;&quot;
    查询指定城市的当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    is_approved = interrupt({
        &quot;action&quot;: &quot;get_weather&quot;,
        &quot;question&quot;: &quot;是否同意查询天气？&quot;
    })

    logger.info(&quot;is_approved: {}&quot;, is_approved)
    if is_approved:
        return f&quot;{city} 今天天气不错&quot;
    else:
        return &quot;用户拒绝查询天气&quot;

tools_by_name = {&quot;get_weather&quot;: get_weather}

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)
model_with_tools = model.bind_tools([get_weather])

def llm_node(state: MessagesState) -&gt; MessagesState:
    messages = state['messages']
    response = model_with_tools.invoke(messages)

    return {
        &quot;messages&quot;: [response]
    }

def tool_node(state: MessagesState) -&gt; MessagesState:
    last_msg = state['messages'][-1]

    tool_msgs = []
    for tool_call in last_msg.tool_calls:
        tool = tools_by_name[tool_call[&quot;name&quot;]]
        logger.info(&quot;工具 {} 被调用, 对应的 tool_call: {}&quot;, tool_call[&quot;name&quot;], tool_call)
        tool_res = tool.invoke(tool_call[&quot;args&quot;])
        tool_msg = ToolMessage(
            name = tool_call[&quot;name&quot;],
            content = tool_res,
            tool_call_id = tool_call[&quot;id&quot;]
        )
        tool_msgs.append(tool_msg)

    return {
        &quot;messages&quot;: tool_msgs
    }

def router(state: MessagesState) -&gt; Literal[&quot;tool_node&quot;, END]:
    if state['messages'][-1].tool_calls:
        return &quot;tool_node&quot;
    return END

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, tool_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_conditional_edges(&quot;llm_node&quot;, router, path_map=[&quot;tool_node&quot;, END])
builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;tool_test&quot;}}
interrupted_res = graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;今天北京天气如何？&quot;)]}, config=config)
print('=' * 30, '-&gt; interrupt_res &lt;-', '=' * 30)
for msg in interrupted_res['messages']:
    msg.pretty_print()
print('=' * 30, '-&gt; interrupt_info &lt;-', '=' * 30)
print(interrupted_res['__interrupt__'])

user_approved = input(&quot;是否同意查询天气？(y/n): &quot;).strip().lower() == 'y'
approved_res = graph.invoke(Command(resume=user_approved), config=config)
print('=' * 30, '-&gt; approved_res &lt;-', '=' * 30)
for msg in approved_res['messages']:
    msg.pretty_print()
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260616145202853.png" alt="image-20260616145202853" /></p>
<pre><code class="language-json">2026-06-16 14:51:50.662 | INFO     | __main__:tool_node:59 - 工具 get_weather 被调用, 对应的 tool_call: {'name': 'get_weather', 'args': {'city': '北京'}, 'id': 'call_00_EGxsgveRV6mFkXsTtN4H9369', 'type': 'tool_call'}
============================== -&gt; interrupt_res &lt;- ==============================
================================ Human Message =================================

今天北京天气如何？
================================== Ai Message ==================================

好的，我来查询一下今天北京的天气情况。
Tool Calls:
  get_weather (call_00_EGxsgveRV6mFkXsTtN4H9369)
 Call ID: call_00_EGxsgveRV6mFkXsTtN4H9369
  Args:
    city: 北京
============================== -&gt; interrupt_info &lt;- ==============================
[Interrupt(value={'action': 'get_weather', 'question': '是否同意查询天气？'}, id='907128d6bef87a8473833ec0c90589ea')]
2026-06-16 14:51:50.675 | INFO     | __main__:tool_node:59 - 工具 get_weather 被调用, 对应的 tool_call: {'name': 'get_weather', 'args': {'city': '北京'}, 'id': 'call_00_EGxsgveRV6mFkXsTtN4H9369', 'type': 'tool_call'}
2026-06-16 14:51:50.677 | INFO     | __main__:get_weather:27 - is_approved: True
============================== -&gt; approved_res &lt;- ==============================
================================ Human Message =================================

今天北京天气如何？
================================== Ai Message ==================================

好的，我来查询一下今天北京的天气情况。
Tool Calls:
  get_weather (call_00_EGxsgveRV6mFkXsTtN4H9369)
 Call ID: call_00_EGxsgveRV6mFkXsTtN4H9369
  Args:
    city: 北京
================================= Tool Message =================================
Name: get_weather

北京 今天天气不错
================================== Ai Message ==================================

今天北京的天气**不错**哦！☀️ 是个适合出行的好天气。如果需要更详细的天气信息（如温度、风力等），可以随时告诉我，我帮你进一步查询！
</code></pre>
<h4 id="8-1-4-6-单节点串行中断模式">8.1.4.6. 单节点串行中断模式</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver
from langgraph.types import interrupt, Command

class OverAllState(TypedDict):
    username: str # 姓名
    age: int # 年龄
    gender: Literal[&quot;male&quot;, &quot;female&quot;] # 性别

def get_info_node(state: OverAllState) -&gt; OverAllState:
    username = interrupt(&quot;请输入您的用户名：&quot;)
    age = interrupt(&quot;请输入您的年龄：&quot;)
    gender = interrupt(&quot;请输入您的性别：(male/female)&quot;)

    return {
        &quot;username&quot;: username,
        &quot;age&quot;: age,
        &quot;gender&quot;: gender
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;get_info_node&quot;, get_info_node)
builder.add_edge(START, &quot;get_info_node&quot;)
builder.add_edge(&quot;get_info_node&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;seq_interrupt_test&quot;}}
username_interrupted_res = graph.invoke({}, config=config)
print('=' * 30, '-&gt; username_interrupted_res &lt;-', '=' * 30)
print(username_interrupted_res)

user_name = input(&quot;请输入您的用户名：&quot;)
age_interrupted_res = graph.invoke(Command(resume=user_name), config=config)
print('=' * 30, '-&gt; age_interrupted_res &lt;-', '=' * 30)
print(age_interrupted_res)

user_age = input(&quot;请输入您的年龄：&quot;)
gender_interrupted_res = graph.invoke(Command(resume=int(user_age)), config=config)
print('=' * 30, '-&gt; gender_interrupted_res &lt;-', '=' * 30)
print(gender_interrupted_res)

user_gender = input(&quot;请输入您的性别：(male/female): &quot;)
resumed_res = graph.invoke(Command(resume=user_gender), config=config)
print('=' * 30, '-&gt; resumed_res &lt;-', '=' * 30)
print(resumed_res)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">============================== -&gt; username_interrupted_res &lt;- ==============================
{'__interrupt__': [Interrupt(value='请输入您的用户名：', id='0359f8a0b1ca357d2d092d5c55d80ec1')]}
============================== -&gt; age_interrupted_res &lt;- ==============================
{'__interrupt__': [Interrupt(value='请输入您的年龄：', id='0359f8a0b1ca357d2d092d5c55d80ec1')]}
============================== -&gt; gender_interrupted_res &lt;- ==============================
{'__interrupt__': [Interrupt(value='请输入您的性别：(male/female)', id='0359f8a0b1ca357d2d092d5c55d80ec1')]}
============================== -&gt; resumed_res &lt;- ==============================
{'username': '小黄', 'age': 15, 'gender': 'male'}
</code></pre>
<p>中断恢复时，整个被中断的节点函数都会重新运行。</p>
<p>单个节点中串行地多次调用 <strong><code>interrupt()</code></strong> 函数时，检查点存储器会记录历史的 <strong><code>resume</code></strong> 信息，<strong><code>LangGraph</code></strong> 运行时会读取这些信息，并在 <strong><code>interrupt()</code></strong> 函数中维护索引，按照节点内调用 <strong><code>interrupt()</code></strong> 函数的顺序，逐个取出历史 <strong><code>resume</code></strong> 的值，并将它们作为 <strong><code>interrupt()</code></strong> 函数的返回值。</p>
<p>所以，已经被恢复的 <strong><code>interrupt()</code></strong> 不会被重复触发。并且，由此可以推断，我们要保证历史 <strong><code>resume</code></strong> 可以被正确应用，就应保证中断恢复前后的多次 <strong><code>interrupt()</code></strong> 相对顺序保持不变。</p>
<p>当历史 <strong><code>resume</code></strong> 耗尽后，本次恢复运行时传入的 <strong><code>resume</code></strong> 会作为本次中断的返回值，然后节点函数继续运行。</p>
<p>所有中断都触发并恢复后，计算图正常结束。</p>
<h3 id="8-1-5-使用规范">8.1.5. 使用规范</h3>
<h4 id="8-1-5-1-不要用-try-catch-包裹-interrupt-调用">8.1.5.1. 不要用<code>try/catch</code>包裹<code>interrupt()</code>调用</h4>
<p>中断的触发是通过抛出 <strong><code>GraphInterrupt</code></strong> 异常实现的，如果用 <strong><code>try/catch</code></strong> 包裹，则底层运行时无法感知断点，不会中断计算图。</p>
<p><strong>正确用法</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ✅ 正确用法：先打断点，再处理异常
    interrupt(&quot;What's your name?&quot;)
    try:
        fetch_data()  # 这一步可能失败
    except Exception as e:
        print(e)
    return state
</code></pre>
<p><strong>错误用法</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ❌ 错误用法，用 try/catch 包裹 interrupt()，这将会导致 GraphInterrupt 被捕获而无法触发中断
    try:
        interrupt(&quot;What's your name?&quot;)
        fetch_data()  # 这一步可能失败
    except Exception as e:
        print(e)
    return state
</code></pre>
<h4 id="8-1-5-2-不要更改单个节点内-interrupt-的调用顺序">8.1.5.2. 不要更改单个节点内<code>interrupt</code>的调用顺序</h4>
<p>上文提到：</p>
<ol>
<li>恢复运行时整个节点函数都会重新运行，而非精确地从断点继续</li>
<li>同一节点中存在多个断点时：历史恢复记录被记录在检查点中，并在恢复运行时按顺序加载</li>
</ol>
<p>一旦中断恢复前后的断点顺序、数量不完全一致，将会导致语义混乱，程序执行结果无法满足预期。</p>
<p>这条规范就是为了避免这样的情况。</p>
<p><strong>正确用法</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ✅ 正确用法，断点顺序固定
    name = interrupt(&quot;What's your name?&quot;)
    age = interrupt(&quot;What's your age?&quot;)
    city = interrupt(&quot;What's your city?&quot;)

    return {
        &quot;name&quot;: name,
        &quot;age&quot;: age,
        &quot;city&quot;: city
    }
</code></pre>
<p><strong>错误用法</strong></p>
<p><strong>1. 可能跳过其中部分断点</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ❌ 错误用法，以状态为条件，可能跳过部分断点
    name = interrupt(&quot;What's your name?&quot;)

    # 第一次运行到此处时，可能不会触发分支内的断点
    # 恢复时，resume 本应作为 city，但如果分支内断点触发，将被错误地作为 age，导致语义混乱
    if state.get(&quot;needs_age&quot;):
        age = interrupt(&quot;What's your age?&quot;)

    city = interrupt(&quot;What's your city?&quot;)

    return {&quot;name&quot;: name, &quot;city&quot;: city}
</code></pre>
<p>断点以状态为条件，如果中断恢复前后状态发生了变化，断点的顺序和数量可能发生变化，导致语义混乱。</p>
<p><strong>2. 基于不确定的数据循环，并在其中设置断点</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ❌ 错误用法：基于不确定的数据循环
    # 每次调用时的断点数量可能不同
    results = []
    for item in state.get(&quot;dynamic_list&quot;, []):  # List might change between runs
        result = interrupt(f&quot;Approve {item}?&quot;)
        results.append(result)

    return {&quot;results&quot;: results}
</code></pre>
<p>基于状态中可变的值循环，并在循环中设置断点，如果中断恢复前后状态发生变化，断点数量可能变化，导致语义混乱。</p>
<h4 id="8-1-5-3-不要在-interrupt-中传递复杂类型">8.1.5.3. 不要在<code>interrupt()</code>中传递复杂类型</h4>
<p><strong><code>LangGraph</code></strong> 运行时会将 <strong><code>interrupt()</code></strong> 函数接收到的参数经过 <strong><code>JSON</code> 序列化</strong> 之后传递给调用者。</p>
<p>如果传递不支持 <strong><code>JSON</code> 序列化</strong> 的复杂类型，如函数，将会抛出异常，如下所示</p>
<pre><code class="language-json">TypeError: Type is not msgpack serializable: Interrupt
</code></pre>
<p><strong>正确用法</strong></p>
<p><strong>1. 简单类型</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ✅ 正确用法：传递可以序列化的简单类型数据
    name = interrupt(&quot;What's your name?&quot;)
    count = interrupt(42)
    approved = interrupt(True)

    return {&quot;name&quot;: name, &quot;count&quot;: count, &quot;approved&quot;: approved}
</code></pre>
<p><strong>2. 结构化数据</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ✅ 正确用法：传递仅包含基本数据类型的可序列化字典
    response = interrupt({
        &quot;question&quot;: &quot;Enter user details&quot;,
        &quot;fields&quot;: [&quot;name&quot;, &quot;email&quot;, &quot;age&quot;],
        &quot;current_values&quot;: state.get(&quot;user&quot;, {})
    })

    return {&quot;user&quot;: response}
</code></pre>
<p><strong>错误用法</strong></p>
<p><strong>1. 函数</strong></p>
<pre><code class="language-python">def validate_input(value):
    return len(value) &gt; 0

def node_a(state: State):
    # ❌ 错误用法：传递函数
    # 函数不支持 JSON 序列化
    response = interrupt({
        &quot;question&quot;: &quot;What's your name?&quot;,
        &quot;validator&quot;: validate_input  # 此处会导致失败
    })
    return {&quot;name&quot;: response}
</code></pre>
<p><strong>2. 类的实例</strong></p>
<pre><code class="language-python">class DataProcessor:
    def __init__(self, config):
        self.config = config

def node_a(state: State):
    processor = DataProcessor({&quot;mode&quot;: &quot;strict&quot;})

    # ❌ 错误用法：传递类的实例
    # 此处的自定义实例不支持 JSON 序列化
    response = interrupt({
        &quot;question&quot;: &quot;Enter data to process&quot;,
        &quot;processor&quot;: processor  # 这将会导致失败
    })
    return {&quot;result&quot;: response}
</code></pre>
<h4 id="8-1-5-4-断点之前的副作用操作必须是幂等的">8.1.5.4. 断点之前的副作用操作必须是幂等的</h4>
<ul>
<li><strong>副作用操作</strong>：修改了外部环境的操作，如
<ul>
<li>写数据库</li>
<li>写文件</li>
<li>发送邮件或消息</li>
</ul>
</li>
<li><strong>幂等性</strong>：多次执行的结果等价于一次执行。</li>
</ul>
<p>我们知道，中断恢复时，断点所在的函数会被重复执行，所以，如果断点之前存在不满足幂等性的副作用操作，将会导致多次调用结果不一致。</p>
<p>如：写数据操作不满足幂等性，可能导致写入多条重复记录。</p>
<p><strong>正确用法</strong></p>
<p><strong>1. 在断点之前使用满足幂等性的操作</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ✅ 正确用法：使用幂等的 upsert 操作
    # 多次运行结果一致
    db.upsert_user(
        user_id=state[&quot;user_id&quot;],
        status=&quot;pending_approval&quot;
    )

    approved = interrupt(&quot;Approve this change?&quot;)

    return {&quot;approved&quot;: approved}
</code></pre>
<p><strong><code>upsert</code></strong> 的语义是：<strong>写入或更新</strong>，如果有相同记录则覆盖更新，否则新增。满足幂等性。</p>
<p><strong>2. 将副作用操作放在断点之后</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ✅ 正确用法：将副作用操作放在断点之后
    # 确保副作用操作只运行一次
    approved = interrupt(&quot;Approve this change?&quot;)

    if approved:
        db.create_audit_log(
            user_id=state[&quot;user_id&quot;],
            action=&quot;approved&quot;
        )

    return {&quot;approved&quot;: approved}
</code></pre>
<p>上述代码将副作用操作置于断点之后，之后中断触发并恢复运行之后，副作用操作才会被执行，后者只会被执行一次。</p>
<p><strong>3. 将副作用操作和断点置于不同的节点</strong></p>
<pre><code class="language-python">def approval_node(state: State):
    # ✅ 正确用法：当前节点只做中断
    approved = interrupt(&quot;Approve this change?&quot;)

    return {&quot;approved&quot;: approved}

def notification_node(state: State):
    # ✅ 正确用法：副作用操作发生在独立的节点中
    # 当前节点独立于中断，所以副作用操作，不会因为中断恢复而重复执行
    if (state.approved):
        send_notification(
            user_id=state[&quot;user_id&quot;],
            status=&quot;approved&quot;
        )

    return state
</code></pre>
<p>这种方式更彻底，将中断和副作用操作完全隔离。</p>
<p><strong>错误用法</strong></p>
<p><strong>1. 在断点之前向数据库中新增数据</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ❌ 错误用法：在断点之前向数据库中新增数据
    # 中断恢复会导致数据重复
    audit_id = db.create_audit_log({
        &quot;user_id&quot;: state[&quot;user_id&quot;],
        &quot;action&quot;: &quot;pending_approval&quot;,
        &quot;timestamp&quot;: datetime.now()
    })

    approved = interrupt(&quot;Approve this change?&quot;)

    return {&quot;approved&quot;: approved, &quot;audit_id&quot;: audit_id}
</code></pre>
<p><strong>2. 将数据追加到已存在的列表</strong></p>
<pre><code class="language-python">def node_a(state: State):
    # ❌ 错误用法：在断点之前向列表追加数据
    # 这将会在中断恢复时导致列表数据重复
    db.append_to_history(state[&quot;user_id&quot;], &quot;approval_requested&quot;)

    approved = interrupt(&quot;Approve this change?&quot;)

    return {&quot;approved&quot;: approved}
</code></pre>
<h3 id="8-1-6-中断触发和恢复前后的检查点">8.1.6. 中断触发和恢复前后的检查点</h3>
<h4 id="8-1-6-1-基础-hitl-模式">8.1.6.1. 基础<code>HITL</code>模式</h4>
<h5 id="8-1-6-1-1-示例代码">8.1.6.1.1. 示例代码</h5>
<pre><code class="language-python">from typing import TypedDict

from langgraph.graph import StateGraph, START, END
from langgraph.types import Command, interrupt
from langgraph.checkpoint.memory import InMemorySaver

class OverAllState(TypedDict):
    username: str
    age: int

def node_a(state: OverAllState) -&gt; OverAllState:
    username = interrupt(&quot;请输入您的姓名&quot;)
    age = interrupt(&quot;请输入您的年龄&quot;)
    return {
        &quot;username&quot;: username,
        &quot;age&quot;: age
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
username_interrupted_res = graph.invoke({}, config=config)
print('=' * 30, '-&gt; username_interrupted_res &lt;-', '=' * 30)
print(username_interrupted_res)
username_interrupted_his = list(graph.get_state_history(config=config))
print('=' * 30, '-&gt; username_interrupted_his &lt;-', '=' * 30)
print(username_interrupted_his)

age_interrupted_res = graph.invoke(Command(resume=&quot;小黄&quot;), config=config)
print('=' * 30, '-&gt; age_interrupted_res &lt;-', '=' * 30)
print(age_interrupted_res)
age_interrupted_his = list(graph.get_state_history(config=config))
print('=' * 30, '-&gt; age_interrupted_his &lt;-', '=' * 30)
print(age_interrupted_his)

resumed_res = graph.invoke(Command(resume=123), config=config)
print('=' * 30, '-&gt; resumed_res &lt;-', '=' * 30)
print(resumed_res)
resumed_his = list(graph.get_state_history(config=config))
print('=' * 30, '-&gt; resumed_his &lt;-', '=' * 30)
print(resumed_his)
</code></pre>
<h5 id="8-1-6-1-2-运行结果分析">8.1.6.1.2. 运行结果分析</h5>
<h6 id="8-1-6-1-2-1-第一次中断">8.1.6.1.2.1. 第一次中断</h6>
<p><strong>调用结果</strong></p>
<pre><code class="language-json">============================== -&gt; username_interrupted_res &lt;- ==============================
{'__interrupt__': [Interrupt(value='请输入您的姓名', id='488d1fa95a4053d41ab09786f5260ae9')]}
</code></pre>
<p><strong>历史检查点</strong></p>
<pre><code class="language-json">============================== -&gt; username_interrupted_his &lt;- ==============================
[
    StateSnapshot(
        values={},
        next=('node_a',),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4aa0-631a-8000-12a45e5b02a0'
            }
        },
        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {}
        },
        created_at='2026-06-17T08:19:59.195818+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4a9d-68e8-bfff-3573d795ed80'
            }
        },
        tasks=(
            PregelTask(
                id='a7e05e86-30c1-a590-a7bf-e3542eb53d4b',
                name='node_a',
                path=('__pregel_pull', 'node_a'),
                error=None,
                interrupts=(
                    Interrupt(
                        value='请输入您的姓名',
                        id='488d1fa95a4053d41ab09786f5260ae9'
                    ),
                ),
                state=None,
                result=None
            ),
        ),
        interrupts=(
            Interrupt(
                value='请输入您的姓名',
                id='488d1fa95a4053d41ab09786f5260ae9'
            ),
        )
    ),

    StateSnapshot(
        values={},
        next=('__start__',),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4a9d-68e8-bfff-3573d795ed80'
            }
        },
        metadata={
            'source': 'input',
            'step': -1,
            'parents': {}
        },
        created_at='2026-06-17T08:19:59.194743+00:00',
        parent_config=None,
        tasks=(
            PregelTask(
                id='173d6621-cd7b-3edd-a3d5-8d1c3bb8dc53',
                name='__start__',
                path=('__pregel_pull', '__start__'),
                error=None,
                interrupts=(),
                state=None,
                result={}
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<p>当前工作流只有一个节点，所以只有一个真正执行用户自定义节点的超步，对应的超步编号为 <strong><code>1</code></strong></p>
<p>中断触发时，检查点存储器中记录的最新检查点超步为 <strong><code>0</code></strong>，中断信息只能和这个超步绑定</p>
<p>中断信息存了两份：</p>
<ul>
<li>
<p><strong><code>StateSnapshot</code></strong> 的 <strong><code>tasks</code></strong> 属性下记录的 <strong><code>PregelTask</code></strong> 实例的 <strong><code>interrupts</code></strong> 字段记录了当前任务触发的中断信息。</p>
</li>
<li>
<p><strong><code>StateSnapshot</code></strong> 的 <strong><code>interrupts</code></strong> 属性记录了当前超步发生的所有中断</p>
</li>
</ul>
<p>当存在多个并行中断时</p>
<ul>
<li>中断信息被记录在各自所属任务的 <strong><code>PregelTask</code></strong> 实例中</li>
<li>同时也会被汇总在 <strong><code>StateSnapshot</code></strong> 的 <strong><code>tasks</code></strong> 属性中</li>
</ul>
<h6 id="8-1-6-1-2-2-第一次恢复">8.1.6.1.2.2. 第一次恢复</h6>
<p><strong>调用结果</strong></p>
<pre><code class="language-json">============================== -&gt; age_interrupted_res &lt;- ==============================
{'__interrupt__': [Interrupt(value='请输入您的年龄', id='488d1fa95a4053d41ab09786f5260ae9')]}
</code></pre>
<p><strong>历史检查点</strong></p>
<pre><code class="language-json">============================== -&gt; age_interrupted_his &lt;- ==============================

[
    StateSnapshot(
        values={},
        next=('node_a',),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4aa0-631a-8000-12a45e5b02a0'
            }
        },
        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {}
        },
        created_at='2026-06-17T08:19:59.195818+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4a9d-68e8-bfff-3573d795ed80'
            }
        },
        tasks=(
            PregelTask(
                id='a7e05e86-30c1-a590-a7bf-e3542eb53d4b',
                name='node_a',
                path=('__pregel_pull', 'node_a'),
                error=None,
                interrupts=(
                    Interrupt(
                        value='请输入您的年龄',
                        id='488d1fa95a4053d41ab09786f5260ae9'
                    ),
                ),
                state=None,
                result={}
            ),
        ),
        interrupts=(
            Interrupt(
                value='请输入您的年龄',
                id='488d1fa95a4053d41ab09786f5260ae9'
            ),
        )
    ),

    StateSnapshot(
        values={},
        next=('__start__',),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4a9d-68e8-bfff-3573d795ed80'
            }
        },
        metadata={
            'source': 'input',
            'step': -1,
            'parents': {}
        },
        created_at='2026-06-17T08:19:59.194743+00:00',
        parent_config=None,
        tasks=(
            PregelTask(
                id='173d6621-cd7b-3edd-a3d5-8d1c3bb8dc53',
                name='__start__',
                path=('__pregel_pull', '__start__'),
                error=None,
                interrupts=(),
                state=None,
                result={}
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<p><strong><code>checkpoint_id</code>、任务<code>id</code>、中断<code>id</code></strong> 和历史检查点完全一致，只是 <strong><code>Interrupt</code></strong> 实例的 <strong><code>value</code></strong> 属性变成了第二次中断的值。</p>
<p>由此可见，恢复运行时检查点的更新确实是覆盖写入，历史中断信息不会被保留。</p>
<p>并且，中断恢复时传递的 <strong><code>resume</code></strong> 在 <strong><code>graph.get_state_history()</code></strong> 时也不可见。</p>
<h6 id="8-1-6-1-2-3-第二次恢复">8.1.6.1.2.3. 第二次恢复</h6>
<p><strong>调用结果</strong></p>
<pre><code class="language-json">============================== -&gt; resumed_res &lt;- ==============================
{'username': '小黄', 'age': 123}
</code></pre>
<p><strong>历史检查点</strong></p>
<pre><code class="language-json">============================== -&gt; resumed_his &lt;- ==============================

[
    StateSnapshot(
        values={
            'username': '小黄',
            'age': 123
        },
        next=(),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4ab5-60bc-8001-df8746dc9400'
            }
        },
        metadata={
            'source': 'loop',
            'step': 1,
            'parents': {}
        },
        created_at='2026-06-17T08:19:59.204348+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4aa0-631a-8000-12a45e5b02a0'
            }
        },
        tasks=(),
        interrupts=()
    ),

    StateSnapshot(
        values={},
        next=('node_a',),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4aa0-631a-8000-12a45e5b02a0'
            }
        },
        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {}
        },
        created_at='2026-06-17T08:19:59.195818+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4a9d-68e8-bfff-3573d795ed80'
            }
        },
        tasks=(
            PregelTask(
                id='a7e05e86-30c1-a590-a7bf-e3542eb53d4b',
                name='node_a',
                path=('__pregel_pull', 'node_a'),
                error=None,
                interrupts=(
                    Interrupt(
                        value='请输入您的年龄',
                        id='488d1fa95a4053d41ab09786f5260ae9'
                    ),
                ),
                state=None,
                result={
                    'username': '小黄',
                    'age': 123
                }
            ),
        ),
        interrupts=(
            Interrupt(
                value='请输入您的年龄',
                id='488d1fa95a4053d41ab09786f5260ae9'
            ),
        )
    ),

    StateSnapshot(
        values={},
        next=('__start__',),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a255-4a9d-68e8-bfff-3573d795ed80'
            }
        },
        metadata={
            'source': 'input',
            'step': -1,
            'parents': {}
        },
        created_at='2026-06-17T08:19:59.194743+00:00',
        parent_config=None,
        tasks=(
            PregelTask(
                id='173d6621-cd7b-3edd-a3d5-8d1c3bb8dc53',
                name='__start__',
                path=('__pregel_pull', '__start__'),
                error=None,
                interrupts=(),
                state=None,
                result={}
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<p>两个断点全部恢复运行后，编号为 <strong><code>1</code></strong> 的超步终于完成，任务实例的 <strong><code>result</code></strong> 被更新，状态被更新，最后一个超步的 <strong><code>values</code></strong> 是更新后的状态值，计算图运行完成。</p>
<h4 id="8-1-6-2-多个并行中断触发时的检查点">8.1.6.2. 多个并行中断触发时的检查点</h4>
<h5 id="8-1-6-2-1-示例代码">8.1.6.2.1. 示例代码</h5>
<pre><code class="language-python">from typing import TypedDict

from langgraph.graph import StateGraph, START, END
from langgraph.types import Command, interrupt
from langgraph.checkpoint.memory import InMemorySaver

class OverAllState(TypedDict):
    username: str
    age: int

def node_a(state: OverAllState) -&gt; OverAllState:
    username = interrupt(&quot;请输入您的姓名&quot;)
    return {
        &quot;username&quot;: username
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    age = interrupt(&quot;请输入您的年龄&quot;)
    return {
        &quot;age&quot;: age
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(START, &quot;node_b&quot;)
builder.add_edge(&quot;node_a&quot;, END)
builder.add_edge(&quot;node_b&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
interrupted_res = graph.invoke({}, config=config)
print('=' * 30, '-&gt; interrupt_res &lt;-', '=' * 30)
print(interrupted_res)

list(graph.get_state_history(config=config))

# 6. 恢复执行
resume_map = {}
for i in interrupted_res['__interrupt__']:
    user_input = input(f&quot;{i.value}:&quot;)
    if &quot;年龄&quot; in i.value:
        resume_map[i.id] = int(user_input)
    else:
        resume_map[i.id] = user_input

resumed_res = graph.invoke(Command(resume=resume_map),config=config)
print(resumed_res)

list(graph.get_state_history(config=config))
</code></pre>
<h5 id="8-1-6-2-2-结果分析">8.1.6.2.2. 结果分析</h5>
<p><img src="/images/langgraph-study-notes/image-20260617175548170.png" alt="image-20260617175548170" /></p>
<p><strong>计算图返回值</strong></p>
<pre><code class="language-python">============================== -&gt; interrupt_res &lt;- ==============================

{
    '__interrupt__': [
        Interrupt(
            value='请输入您的姓名',
            id='0cae1932a74087f02711c6f1677b4b17'
        ),
        Interrupt(
            value='请输入您的年龄',
            id='cd216e1ad6907c2432636ff5f32cfcf3'
        )
    ]
}
</code></pre>
<p>同一个超步中并行触发的中断信息被放在列表中返回。</p>
<p><strong>历史检查点列表</strong></p>
<pre><code class="language-python">[
    StateSnapshot(
        values={},

        next=(
            'node_a',
            'node_b'
        ),

        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a329-8b50-69e0-8000-7bdce1ba7379'
            }
        },

        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {}
        },

        created_at='2026-06-17T09:54:56.810630+00:00',

        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a329-8b4e-6085-bfff-3100dea319cc'
            }
        },

        tasks=(
            PregelTask(
                id='689d8040-4b34-02a2-eb32-d901bbb3c67c',
                name='node_a',
                path=(
                    '__pregel_pull',
                    'node_a'
                ),
                error=None,
                interrupts=(
                    Interrupt(
                        value='请输入您的姓名',
                        id='0cae1932a74087f02711c6f1677b4b17'
                    ),
                ),
                state=None,
                result=None
            ),

            PregelTask(
                id='034cbe3c-e0a6-025a-ce03-46436ce38fc3',
                name='node_b',
                path=(
                    '__pregel_pull',
                    'node_b'
                ),
                error=None,
                interrupts=(
                    Interrupt(
                        value='请输入您的年龄',
                        id='cd216e1ad6907c2432636ff5f32cfcf3'
                    ),
                ),
                state=None,
                result=None
            )
        ),

        interrupts=(
            Interrupt(
                value='请输入您的姓名',
                id='0cae1932a74087f02711c6f1677b4b17'
            ),
            Interrupt(
                value='请输入您的年龄',
                id='cd216e1ad6907c2432636ff5f32cfcf3'
            )
        )
    ),


    StateSnapshot(
        values={},

        next=(
            '__start__',
        ),

        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a329-8b4e-6085-bfff-3100dea319cc'
            }
        },

        metadata={
            'source': 'input',
            'step': -1,
            'parents': {}
        },

        created_at='2026-06-17T09:54:56.809574+00:00',

        parent_config=None,

        tasks=(
            PregelTask(
                id='3df213dc-5fa0-a8aa-b1dc-428da9010be6',
                name='__start__',
                path=(
                    '__pregel_pull',
                    '__start__'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={}
            ),
        ),

        interrupts=()
    )
]
</code></pre>
<p>如前所述，编号为 <strong><code>1</code></strong> 的超步执行时中断触发，此时检查点后端中存储的最新的超步编号为 <strong><code>0</code></strong>，因此中断信息记录在超步 <strong><code>0</code></strong> 的 <strong><code>StateSnapshot</code></strong> 实例中。</p>
<ul>
<li>
<p>快照实例的 <strong><code>interrupts</code></strong> 属性记录了并行触发的两个中断的信息。</p>
</li>
<li>
<p>并且，各自的中断信息被记录在对应的任务实例中。</p>
</li>
</ul>
<h4 id="8-1-6-3-同一超步部分任务触发中断时的检查点">8.1.6.3. 同一超步部分任务触发中断时的检查点</h4>
<h5 id="8-1-6-3-1-示例代码">8.1.6.3.1. 示例代码</h5>
<pre><code class="language-python">from typing import TypedDict

from langgraph.graph import StateGraph, START, END
from langgraph.types import Command, interrupt
from langgraph.checkpoint.memory import InMemorySaver

class OverAllState(TypedDict):
    username: str
    age: int

def node_a(state: OverAllState) -&gt; OverAllState:
    username = interrupt(&quot;请输入您的姓名&quot;)
    return {
        &quot;username&quot;: username
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    age = 12
    return {
        &quot;age&quot;: age
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(START, &quot;node_b&quot;)
builder.add_edge(&quot;node_a&quot;, END)
builder.add_edge(&quot;node_b&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
interrupted_res = graph.invoke({}, config=config)
print('=' * 30, '-&gt; interrupt_res &lt;-', '=' * 30)
print(interrupted_res)

list(graph.get_state_history(config=config))
</code></pre>
<h5 id="8-1-6-3-2-结果分析">8.1.6.3.2. 结果分析</h5>
<p><img src="/images/langgraph-study-notes/image-20260617180203915.png" alt="image-20260617180203915" /></p>
<p><strong>计算图返回值</strong></p>
<pre><code class="language-python">============================== -&gt; interrupt_res &lt;- ==============================

{
    'age': 12,
    '__interrupt__': [
        Interrupt(
            value='请输入您的姓名',
            id='bbff88193340fd0ed196be702608e385'
        )
    ]
}
</code></pre>
<p><strong>历史检查点列表</strong></p>
<pre><code class="language-python">============================== -&gt; state_history &lt;- ==============================

[
    StateSnapshot(
        values={},
        next=(
            'node_a',
            'node_b'
        ),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a32c-b47b-6ca9-8000-79ee3c65e7ce'
            }
        },
        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {}
        },
        created_at='2026-06-17T09:56:21.658113+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a32c-b479-6766-bfff-901ed37f684d'
            }
        },
        tasks=(
            PregelTask(
                id='e16690fc-c0f8-e9fc-e30d-706a64f24209',
                name='node_a',
                path=(
                    '__pregel_pull',
                    'node_a'
                ),
                error=None,
                interrupts=(
                    Interrupt(
                        value='请输入您的姓名',
                        id='bbff88193340fd0ed196be702608e385'
                    ),
                ),
                state=None,
                result=None
            ),
            PregelTask(
                id='b628264b-d4ca-c09f-5197-6eb1c953b947',
                name='node_b',
                path=(
                    '__pregel_pull',
                    'node_b'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'age': 12
                }
            )
        ),
        interrupts=(
            Interrupt(
                value='请输入您的姓名',
                id='bbff88193340fd0ed196be702608e385'
            ),
        )
    ),

    StateSnapshot(
        values={},
        next=(
            '__start__',
        ),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f16a32c-b479-6766-bfff-901ed37f684d'
            }
        },
        metadata={
            'source': 'input',
            'step': -1,
            'parents': {}
        },
        created_at='2026-06-17T09:56:21.657164+00:00',
        parent_config=None,
        tasks=(
            PregelTask(
                id='5dc4ad26-1baa-7c18-2580-153a19d4c175',
                name='__start__',
                path=(
                    '__pregel_pull',
                    '__start__'
                ),
                error=None,
                interrupts=(),
                state=None,
                result={}
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<p>由上可知，同一个超步中：</p>
<ul>
<li>
<p>发生中断的任务，其中断信息被记录在任务实例和快照的 <strong><code>interrupts</code></strong> 字段中</p>
</li>
<li>
<p>未触发中断的任务正常结束，其结果被记录在任务实例的 <strong><code>result</code></strong> 字段中</p>
</li>
</ul>
<p>底层负责执行节点逻辑的是超步的第二阶段，如果存在多个并行任务，则会将任务提交到线程池，在独立的线程中运行，负责执行第二阶段的 <strong><code>PregelRunner</code></strong> 实例会收集提交后的 <strong><code>future</code></strong> 对象，并等待所有任务完成。</p>
<p>当中断触发时，对应的线程会抛出 <strong><code>GraphInterrupt</code></strong>，对应的 <strong><code>future</code></strong> 对象会收集异常。</p>
<p><strong><code>PregelRunner</code></strong> 实例在 <strong><code>future</code></strong> 对应的任务完成后，收集中断信息或任务执行结果，写入检查点存储器。</p>
<p>所以一个超步中某个任务触发中断，不会导致其它并行任务的终止，正常运行的任务结果、中断任务的中断信息都被记录在检查点后端。</p>
<h2 id="8-2-静态断点">8.2. 静态断点</h2>
<p><strong><code>LangGraph</code></strong> 提供了用于调试的 <strong>静态断点</strong>。</p>
<p>它是在状态图编译或调用时设置的，不会在运行时动态触发，不属于业务逻辑，所以称为 <strong>静态</strong> 断点。</p>
<p>动态断点提供了接收人类反馈的接口，而静态断点只是暂停计算图的运行，不能向调用者传递信息，也不能接收调用者的反馈。</p>
<h3 id="8-2-1-用法说明">8.2.1. 用法说明</h3>
<ol>
<li>
<p>静态断点也需要基于检查点恢复，所以必须配置检查点，启用可恢复运行机制。</p>
</li>
<li>
<p>计算图会在 <strong><code>interrupt_before</code></strong> 指定的节点执行之前产生中断，暂停计算</p>
</li>
<li>
<p>计算图会在 <strong><code>interrupt_after</code></strong> 指定的节点执行之后产生中断，暂停计算</p>
</li>
<li>
<p>计算图运行到断点位置会中断，和动态断点不同的是，它会在超步边界而非内部中断。</p>
<p>断点前的超步：三个阶段全部完成；断点后的超步：三个阶段都没有开始。</p>
</li>
<li>
<p>静态断点不会返回任何中断信息，只会将当前最新的状态返回。</p>
<p>因此，我们可以用静态断点查看每个超步边界的中间状态。</p>
</li>
<li>
<p>传入相同的配置并将 <strong><code>None</code></strong> 作为输入，再次调用计算图，会从断点位置继续运行。</p>
</li>
<li>
<p>支持在两个阶段设置静态断点</p>
<ul>
<li>
<p>状态图编译时</p>
<pre><code class="language-python">graph = builder.compile(
    checkpointer=checkpointer,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)
</code></pre>
</li>
<li>
<p>计算图调用时</p>
<pre><code class="language-python">first_res = graph.invoke(
    {},
    config=config,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)
</code></pre>
</li>
</ul>
<p>断点都是在运行时生效，计算图调用时设置的断点优先级更高。</p>
<p>如果调用时传入的断点列表不为空，则会覆盖编译时配置。</p>
</li>
</ol>
<h3 id="8-2-2-底层原理">8.2.2. 底层原理</h3>
<ol>
<li>
<p>编译时设置的静态断点保存在编译图的 <strong><code>interrupt_before_nodes</code></strong> 和 <strong><code>interrupt_after_nodes</code></strong> 中。</p>
<p>运行时采用调用参数优先、编译配置兜底的规则。</p>
<p>调用时传入非空节点列表会完全覆盖编译配置，而 <strong><code>None</code></strong> 或空列表会回退到编译配置。</p>
</li>
<li>
<p><strong><code>interrupt_before</code></strong> 在超步的第一阶段：当前超步的任务列表计算完成后、节点任务开始执行前进行检查。</p>
<ul>
<li>如果检查点中的状态和上一次静态断点或图的初始状态相比，产生了变化</li>
<li>并且当前超步的任务列表和 <strong><code>interrupt_before</code></strong> 列表存在交集</li>
</ul>
<p>则抛出 <strong><code>GraphInterrupt</code></strong>。</p>
</li>
<li>
<p><strong><code>interrupt_after</code></strong> 在超步的第三阶段：当前超步的任务全部执行完成、写入应用到通道并创建检查点之后进行检查。</p>
<ul>
<li>如果检查点中的状态和上一次静态断点或图的初始状态相比，产生了变化</li>
<li>并且当前超步的任务列表和 <strong><code>interrupt_after</code></strong> 列表存在交集</li>
</ul>
<p>则抛出 <strong><code>GraphInterrupt</code></strong>。</p>
</li>
<li>
<p>如果存在这样的拓扑图</p>
<pre><code class="language-python">node_a -&gt; node_b
</code></pre>
<p>那么在 <strong><code>node_a</code> 之后</strong> 和 <strong><code>node_b</code> 之前</strong> 设置断点只会中断一次。</p>
<p>因为二者对应同一个超步边界，如果同时配置：</p>
<ul>
<li>
<p><strong><code>node_a</code></strong> 的 <strong><code>after</code></strong> 先触发；</p>
</li>
<li>
<p>恢复运行时检查点中记录的上次断点可见状态会被更新，和最新状态保持一致</p>
</li>
<li>
<p>而产生中断之前已经保存了检查点，恢复运行时会从 <strong><code>node_b</code></strong> 的超步开始</p>
</li>
<li>
<p><strong><code>node_b</code></strong> 的超步第一阶段，检查点和上次静态断点相比，没有变化，所以不会中断</p>
</li>
<li>
<p>因此，<strong><code>node_a</code> 之后</strong> 和 <strong><code>node_b</code> 之前</strong> 设置断点只会中断一次</p>
</li>
</ul>
</li>
</ol>
<h3 id="8-2-3-示例">8.2.3. 示例</h3>
<h4 id="8-2-3-1-编译时设置断点">8.2.3.1. 编译时设置断点</h4>
<h5 id="8-2-3-1-1-顺序结构设置断点">8.2.3.1.1. 顺序结构设置断点</h5>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver

from loguru import logger

class OverAllState(TypedDict):
    final_res: str

def node_a(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_a 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_a 运行的中间结果&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_b 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_b 运行的中间结果&quot;
    }

def node_c(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_c 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_c 运行的中间结果&quot;
    }


builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)

builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, &quot;node_c&quot;)
builder.add_edge(&quot;node_c&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(
    checkpointer=checkpointer,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}

logger.info(&quot;{}-&gt; 第一次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
first_res = graph.invoke({}, config=config)
logger.info(&quot;第一次执行结果：{}&quot;, first_res)

logger.info(&quot;{}-&gt; 第二次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
second_res = graph.invoke(None, config=config)
logger.info(&quot;第二次执行结果：{}&quot;, second_res)

logger.info(&quot;{}-&gt; 第三次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
third_res = graph.invoke(None, config=config)
logger.info(&quot;第三次执行结果：{}&quot;, third_res)

logger.info(&quot;{}-&gt; 第四次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
final_res = graph.invoke(None, config=config)
logger.info(&quot;第四次执行结果：{}&quot;, final_res)

logger.info(&quot;{}-&gt; 完毕 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260618112251743.png" alt="image-20260618112251743" /></p>
<p>编译时设置的断点在渲染拓扑结构时被感知，图中对应结点新增了断点标记：</p>
<pre><code class="language-python">__interrupt = before,after
</code></pre>
<p>这表示该节点之前和之后都设置了断点。</p>
<p><strong>日志如下</strong></p>
<pre><code class="language-json">2026-06-18 11:22:52.094 | INFO     | __main__:&lt;module&gt;:51 - ==========-&gt; 第一次执行 &lt;-==========
2026-06-18 11:22:52.100 | INFO     | __main__:&lt;module&gt;:53 - 第一次执行结果：None
2026-06-18 11:22:52.100 | INFO     | __main__:&lt;module&gt;:55 - ==========-&gt; 第二次执行 &lt;-==========
2026-06-18 11:22:52.102 | INFO     | __main__:node_a:11 - node_a 被执行
2026-06-18 11:22:52.104 | INFO     | __main__:&lt;module&gt;:57 - 第二次执行结果：{'final_res': 'node_a 运行的中间结果'}
2026-06-18 11:22:52.104 | INFO     | __main__:&lt;module&gt;:59 - ==========-&gt; 第三次执行 &lt;-==========
2026-06-18 11:22:52.105 | INFO     | __main__:node_b:17 - node_b 被执行
2026-06-18 11:22:52.107 | INFO     | __main__:&lt;module&gt;:61 - 第三次执行结果：{'final_res': 'node_b 运行的中间结果'}
2026-06-18 11:22:52.107 | INFO     | __main__:&lt;module&gt;:63 - ==========-&gt; 第四次执行 &lt;-==========
2026-06-18 11:22:52.108 | INFO     | __main__:node_c:23 - node_c 被执行
2026-06-18 11:22:52.110 | INFO     | __main__:&lt;module&gt;:65 - 第四次执行结果：{'final_res': 'node_c 运行的中间结果'}
2026-06-18 11:22:52.111 | INFO     | __main__:&lt;module&gt;:67 - ==========-&gt; 完毕 &lt;-==========
</code></pre>
<p>由日志可知，<strong><code>node_a</code></strong> 和 <strong><code>node_b</code></strong> 之间只产生了一次中断，和分析相符。</p>
<h5 id="8-2-3-1-2-存在并行节点时设置断点">8.2.3.1.2. 存在并行节点时设置断点</h5>
<p>为了确定断点是 <strong>以节点</strong> 为边界还是 <strong>以超步</strong> 为边界设置的，我们在存在并行节点的计算图中测试</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver

from loguru import logger

class OverAllState(TypedDict):
    final_res: str

def node_a(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_a 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_a 运行的中间结果&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_b 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_b 运行的中间结果&quot;
    }

def node_c(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_c 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_c 运行的中间结果&quot;
    }

def node_d(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_d 被执行&quot;)
    return {}

def node_e(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_e 被执行&quot;)
    return {}

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)
builder.add_node(&quot;node_d&quot;, node_d)
builder.add_node(&quot;node_e&quot;, node_e)

builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(START, &quot;node_d&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_d&quot;, &quot;node_e&quot;)
builder.add_edge([&quot;node_b&quot;, &quot;node_e&quot;], &quot;node_c&quot;)
builder.add_edge(&quot;node_c&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(
    checkpointer=checkpointer,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}

logger.info(&quot;{}-&gt; 第一次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
first_res = graph.invoke({}, config=config)
logger.info(&quot;第一次执行结果：{}&quot;, first_res)

logger.info(&quot;{}-&gt; 第二次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
second_res = graph.invoke(None, config=config)
logger.info(&quot;第二次执行结果：{}&quot;, second_res)

logger.info(&quot;{}-&gt; 第三次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
third_res = graph.invoke(None, config=config)
logger.info(&quot;第三次执行结果：{}&quot;, third_res)

logger.info(&quot;{}-&gt; 第四次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
final_res = graph.invoke(None, config=config)
logger.info(&quot;第四次执行结果：{}&quot;, final_res)

logger.info(&quot;{}-&gt; 完毕 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260618112859465.png" alt="image-20260618112859465" /></p>
<pre><code class="language-json">2026-06-18 11:28:49.920 | INFO     | __main__:&lt;module&gt;:62 - ==========-&gt; 第一次执行 &lt;-==========
2026-06-18 11:28:49.922 | INFO     | __main__:&lt;module&gt;:64 - 第一次执行结果：None
2026-06-18 11:28:49.923 | INFO     | __main__:&lt;module&gt;:66 - ==========-&gt; 第二次执行 &lt;-==========
2026-06-18 11:28:49.925 | INFO     | __main__:node_a:11 - node_a 被执行
2026-06-18 11:28:49.927 | INFO     | __main__:node_d:29 - node_d 被执行
2026-06-18 11:28:49.931 | INFO     | __main__:&lt;module&gt;:68 - 第二次执行结果：{'final_res': 'node_a 运行的中间结果'}
2026-06-18 11:28:49.931 | INFO     | __main__:&lt;module&gt;:70 - ==========-&gt; 第三次执行 &lt;-==========
2026-06-18 11:28:49.933 | INFO     | __main__:node_b:17 - node_b 被执行
2026-06-18 11:28:49.935 | INFO     | __main__:node_e:33 - node_e 被执行
2026-06-18 11:28:49.939 | INFO     | __main__:&lt;module&gt;:72 - 第三次执行结果：{'final_res': 'node_b 运行的中间结果'}
2026-06-18 11:28:49.940 | INFO     | __main__:&lt;module&gt;:74 - ==========-&gt; 第四次执行 &lt;-==========
2026-06-18 11:28:49.940 | INFO     | __main__:node_c:23 - node_c 被执行
2026-06-18 11:28:49.942 | INFO     | __main__:&lt;module&gt;:76 - 第四次执行结果：{'final_res': 'node_c 运行的中间结果'}
2026-06-18 11:28:49.943 | INFO     | __main__:&lt;module&gt;:78 - ==========-&gt; 完毕 &lt;-==========
</code></pre>
<p><strong><code>node_a</code></strong> 和 <strong><code>node_d</code></strong> 属于同一个超步</p>
<p><strong><code>node_b</code></strong> 和 <strong><code>node_e</code></strong> 属于同一个超步</p>
<p>我们只在 <strong><code>node_a</code></strong> 和 <strong><code>node_b</code></strong> 前后设置了超步，但它们的并行节点也被中断了。</p>
<p>显然，中断发生在超步边界，而非节点边界，与分析相符。</p>
<h4 id="8-2-3-2-调用时设置断点">8.2.3.2. 调用时设置断点</h4>
<p>调用时的断点配置只对本次调用生效。</p>
<h5 id="8-2-3-2-1-正确用法-恢复调用时设置相同的断点">8.2.3.2.1. 正确用法：恢复调用时设置相同的断点</h5>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver

from loguru import logger

class OverAllState(TypedDict):
    final_res: str

def node_a(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_a 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_a 运行的中间结果&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_b 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_b 运行的中间结果&quot;
    }

def node_c(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_c 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_c 运行的中间结果&quot;
    }


builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)

builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, &quot;node_c&quot;)
builder.add_edge(&quot;node_c&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(
    checkpointer=checkpointer
)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}

logger.info(&quot;{}-&gt; 第一次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
first_res = graph.invoke(
    {},
    config=config,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)
logger.info(&quot;第一次执行结果：{}&quot;, first_res)

logger.info(&quot;{}-&gt; 第二次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
second_res = graph.invoke(
    None,
    config=config,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)
logger.info(&quot;第二次执行结果：{}&quot;, second_res)

logger.info(&quot;{}-&gt; 第三次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
third_res = graph.invoke(
    None,
    config=config,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)
logger.info(&quot;第三次执行结果：{}&quot;, third_res)

logger.info(&quot;{}-&gt; 第四次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
final_res = graph.invoke(
    None,
    config=config,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)
logger.info(&quot;第四次执行结果：{}&quot;, final_res)

logger.info(&quot;{}-&gt; 完毕 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260618113316239.png" alt="image-20260618113316239" /></p>
<pre><code class="language-json">2026-06-18 11:32:55.457 | INFO     | __main__:&lt;module&gt;:49 - ==========-&gt; 第一次执行 &lt;-==========
2026-06-18 11:32:55.460 | INFO     | __main__:&lt;module&gt;:56 - 第一次执行结果：None
2026-06-18 11:32:55.460 | INFO     | __main__:&lt;module&gt;:58 - ==========-&gt; 第二次执行 &lt;-==========
2026-06-18 11:32:55.461 | INFO     | __main__:node_a:11 - node_a 被执行
2026-06-18 11:32:55.463 | INFO     | __main__:&lt;module&gt;:65 - 第二次执行结果：{'final_res': 'node_a 运行的中间结果'}
2026-06-18 11:32:55.463 | INFO     | __main__:&lt;module&gt;:67 - ==========-&gt; 第三次执行 &lt;-==========
2026-06-18 11:32:55.463 | INFO     | __main__:node_b:17 - node_b 被执行
2026-06-18 11:32:55.465 | INFO     | __main__:&lt;module&gt;:74 - 第三次执行结果：{'final_res': 'node_b 运行的中间结果'}
2026-06-18 11:32:55.466 | INFO     | __main__:&lt;module&gt;:76 - ==========-&gt; 第四次执行 &lt;-==========
2026-06-18 11:32:55.467 | INFO     | __main__:node_c:23 - node_c 被执行
2026-06-18 11:32:55.468 | INFO     | __main__:&lt;module&gt;:83 - 第四次执行结果：{'final_res': 'node_c 运行的中间结果'}
2026-06-18 11:32:55.468 | INFO     | __main__:&lt;module&gt;:85 - ==========-&gt; 完毕 &lt;-==========
</code></pre>
<p>我们期望在 <strong><code>node_a</code></strong> 和 <strong><code>node_b</code></strong> 前后中断，此时运行结果和预期相符。</p>
<h5 id="8-2-3-2-2-错误用法-恢复调用时不再设置断点">8.2.3.2.2. 错误用法：恢复调用时不再设置断点</h5>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver

from loguru import logger

class OverAllState(TypedDict):
    final_res: str

def node_a(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_a 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_a 运行的中间结果&quot;
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_b 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_b 运行的中间结果&quot;
    }

def node_c(state: OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_c 被执行&quot;)
    return {
        &quot;final_res&quot;: &quot;node_c 运行的中间结果&quot;
    }


builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)

builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, &quot;node_c&quot;)
builder.add_edge(&quot;node_c&quot;, END)

checkpointer = InMemorySaver()
graph = builder.compile(
    checkpointer=checkpointer
)

from IPython.display import display
display(graph)

config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}

logger.info(&quot;{}-&gt; 第一次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
first_res = graph.invoke(
    {},
    config=config,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)
logger.info(&quot;第一次执行结果：{}&quot;, first_res)

logger.info(&quot;{}-&gt; 第二次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
second_res = graph.invoke(
    None,
    config=config,
    interrupt_before=[&quot;node_a&quot;, &quot;node_b&quot;],
    interrupt_after=[&quot;node_a&quot;, &quot;node_b&quot;]
)
logger.info(&quot;第二次执行结果：{}&quot;, second_res)

logger.info(&quot;{}-&gt; 第三次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
third_res = graph.invoke(None, config=config)
logger.info(&quot;第三次执行结果：{}&quot;, third_res)

logger.info(&quot;{}-&gt; 第四次执行 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
final_res = graph.invoke(None, config=config)
logger.info(&quot;第四次执行结果：{}&quot;, final_res)

logger.info(&quot;{}-&gt; 完毕 &lt;-{}&quot;, &quot;=&quot; * 10, &quot;=&quot; * 10)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260618113535376.png" alt="image-20260618113535376" /></p>
<pre><code class="language-json">2026-06-18 11:35:16.236 | INFO     | __main__:&lt;module&gt;:49 - ==========-&gt; 第一次执行 &lt;-==========
2026-06-18 11:35:16.259 | INFO     | __main__:&lt;module&gt;:56 - 第一次执行结果：None
2026-06-18 11:35:16.260 | INFO     | __main__:&lt;module&gt;:58 - ==========-&gt; 第二次执行 &lt;-==========
2026-06-18 11:35:16.261 | INFO     | __main__:node_a:11 - node_a 被执行
2026-06-18 11:35:16.263 | INFO     | __main__:&lt;module&gt;:65 - 第二次执行结果：{'final_res': 'node_a 运行的中间结果'}
2026-06-18 11:35:16.263 | INFO     | __main__:&lt;module&gt;:67 - ==========-&gt; 第三次执行 &lt;-==========
2026-06-18 11:35:16.264 | INFO     | __main__:node_b:17 - node_b 被执行
2026-06-18 11:35:16.265 | INFO     | __main__:node_c:23 - node_c 被执行
2026-06-18 11:35:16.266 | INFO     | __main__:&lt;module&gt;:69 - 第三次执行结果：{'final_res': 'node_c 运行的中间结果'}
2026-06-18 11:35:16.267 | INFO     | __main__:&lt;module&gt;:71 - ==========-&gt; 第四次执行 &lt;-==========
2026-06-18 11:35:16.267 | INFO     | __main__:&lt;module&gt;:73 - 第四次执行结果：{'final_res': 'node_c 运行的中间结果'}
2026-06-18 11:35:16.268 | INFO     | __main__:&lt;module&gt;:75 - ==========-&gt; 完毕 &lt;-==========
</code></pre>
<p>本例中，我们只在初次调用和第一次恢复运行时设置断点。</p>
<p>由日志可知，按照期望，第二次恢复运行之后，<strong><code>node_b</code></strong> 执行完成后还应中断一次，然而，因为本次没有设置断点，因此计算图不再中断，<strong><code>node_b</code></strong> 和 <strong><code>node_c</code></strong> 都在本次调用完成，计算图结束。</p>
<p>在第四次调用时，计算图已经运行完毕，恢复运行实际上不会执行任何逻辑，只是将计算图的最终状态返回。</p>
<h1 id="9-项目部署">9. 项目部署</h1>
<h2 id="9-1-本地部署并对接-langsmith">9.1. 本地部署并对接<code>LangSmith</code></h2>
<p><strong><code>LangSmith</code></strong> 是 <strong><code>LangChain</code></strong> 官方提供的 <strong><code>Agent</code></strong> 应用工程平台，主要用于对基于 <strong><code>LangGraph</code></strong>、<strong><code>LangChain</code></strong> 等框架开发的 <strong><code>Agent</code></strong> 应用进行运行链路追踪、调试、评估和生产监控。</p>
<p><strong><code>LangChain</code></strong> 官方提供了开箱即用的 <strong><code>LangSmith</code> 云服务</strong>，访问地址为：</p>
<blockquote>
<p><a href="https://smith.langchain.com/">https://smith.langchain.com</a></p>
</blockquote>
<p>此外，<strong><code>LangSmith</code></strong> 也支持混合部署和私有化部署。</p>
<p>除了基本的运行链路追踪，<strong><code>LangSmith</code> 云服务</strong> 还提供了用于调试的 <strong><code>studio</code></strong>，要使用这个模块的功能，需要在本地部署 <strong><code>LangGraph</code></strong> 项目，这正是本节要完成的任务。</p>
<p>本节将部署一个经典的 <strong><code>HITL</code></strong> 应用，并在 <strong><code>LangSmith</code> 云服务</strong> 的 <strong><code>studio</code></strong> 中调试。</p>
<h3 id="9-1-1-补充依赖">9.1.1. 补充依赖</h3>
<p>本章用命令行工具 <strong><code>langgraph-cli</code></strong> 部署项目，需要补充相关依赖，如下所示。</p>
<p>在 <strong><code>requiremtents_full.txt</code></strong> 中补充依赖</p>
<pre><code class="language-json"># LangGraph 本地 Agent Server、Studio、热重载
langgraph-cli[inmem]==0.4.30
</code></pre>
<p>放在</p>
<pre><code class="language-json"># =========================================================
# LangGraph / Agent 编排
# 作用：构建 Agent、状态图、多步骤工作流、检查点、PostgreSQL 持久化
# =========================================================
</code></pre>
<p>模块下</p>
<p>然后命令行启用项目的 <strong><code>conda</code></strong> 环境，执行</p>
<pre><code class="language-powershell">(langgraph) PS C:\Users\Lenovo\OneDrive\文档\AI\langgraph&gt; pip install -r .\requirements_full.txt
</code></pre>
<p>即可。</p>
<h3 id="9-1-2-准备本地项目">9.1.2. 准备本地项目</h3>
<p>在项目任意位置创建 <strong><code>hitl_demo</code></strong> 目录，作为 <strong><code>LangGraph</code></strong> 本地项目的根目录。</p>
<h4 id="9-1-2-1-项目结构">9.1.2.1. 项目结构</h4>
<p>项目结构如下所示</p>
<pre><code class="language-powershell">hitl_demo/
├── src
    ├── __init__.py
│   └── agent.py
├── .env
└── langgraph.json
</code></pre>
<p>其中：</p>
<ul>
<li>
<p><strong><code>src</code></strong>：源码包，存放业务代码</p>
<ul>
<li><strong><code>__init__.py</code></strong>：<strong><code>Python</code></strong> 包的初始化文件</li>
<li><strong><code>agent.py</code></strong>：源码文件，包含业务逻辑，<strong><code>langgraph-cli</code></strong> 会根据配置文件读取其中的 <strong><code>Agent</code></strong> 或 <strong><code>Graph</code></strong> 对象。</li>
</ul>
</li>
<li>
<p><strong><code>.env</code></strong>：当前项目的环境变量，本次部署时，<strong><code>langgraph-cli</code></strong> 会从中加载环境变量。对接 <strong><code>LangSmith</code> 云服务</strong> 时，需要确保该文件中包含相关的环境变量。</p>
</li>
<li>
<p><strong><code>langgraph.json</code></strong>：项目配置文件，用于管理依赖并定位源码中的 <strong><code>agent</code></strong> 对象。</p>
</li>
</ul>
<h4 id="9-1-2-2-代码清单">9.1.2.2. 代码清单</h4>
<h5 id="9-1-2-2-1-__init__-py">9.1.2.2.1. <code>__init__.py</code></h5>
<p>留空即可。</p>
<h5 id="9-1-2-2-2-agent-py">9.1.2.2.2. <code>agent.py</code></h5>
<p>写入以下内容</p>
<pre><code class="language-python">from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END
from langgraph.types import interrupt

class OverAllState(TypedDict):
    username: str # 姓名
    age: int # 年龄
    gender: Literal[&quot;male&quot;, &quot;female&quot;] # 性别

def get_info_node(state: OverAllState) -&gt; OverAllState:
    username = interrupt(&quot;请输入您的用户名：&quot;)
    age = interrupt(&quot;请输入您的年龄：&quot;)
    gender = interrupt(&quot;请输入您的性别：(male/female)&quot;)

    return {
        &quot;username&quot;: username,
        &quot;age&quot;: age,
        &quot;gender&quot;: gender
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;get_info_node&quot;, get_info_node)
builder.add_edge(START, &quot;get_info_node&quot;)
builder.add_edge(&quot;get_info_node&quot;, END)

graph = builder.compile()
</code></pre>
<p><strong><code>graph</code></strong> 对象是我们希望调试的计算图实例。</p>
<p><strong>注意：</strong> <strong><code>langgraph-cli</code></strong> 启动的本地服务会帮我们管理检查点，所以代码中不要传递检查点存储器，否则启动本地服务时将报错。</p>
<h5 id="9-1-2-2-3-env">9.1.2.2.3. <code>.env</code></h5>
<p>确保该文件中有 <strong><code>LANGSMITH_API_KEY</code></strong> 变量</p>
<pre><code class="language-properties">LANGSMITH_API_KEY=xxx
</code></pre>
<p>可以根据需求添加所需的环境变量。</p>
<p>如果希望同时启用 <strong><code>LangSmith</code></strong> 的追踪功能，需要添加以下环境变量</p>
<pre><code class="language-properties">LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_PROJECT=&quot;hitl-demo&quot; # 项目名称，自定义
</code></pre>
<p>配置后，可以在 <strong><code>LangSmith</code> 云服务</strong> 的以下界面根据项目名称追踪应用运行的详细信息。</p>
<p><img src="/images/langgraph-study-notes/image-20260618155610059.png" alt="image-20260618155610059" /></p>
<h5 id="9-1-2-2-4-langgraph-json">9.1.2.2.4. <code>langgraph.json</code></h5>
<p>写入以下内容</p>
<pre><code class="language-json">{
  &quot;dependencies&quot;: [&quot;.&quot;],
  &quot;graphs&quot;: {
    &quot;graph&quot;: &quot;./src/agent.py:graph&quot;
  },
  &quot;env&quot;: &quot;.env&quot;
}
</code></pre>
<ul>
<li>
<p><strong><code>dependencies</code></strong>：<strong><code>langgraph-cli</code></strong> 启动的本地服务的依赖项，接收类型为列表的值</p>
<p>列表元素只有 <strong><code>&quot;.&quot;</code></strong> 表示从本次环境中加载依赖。如果在名为 <strong><code>langchain</code></strong> 的 <strong><code>conda</code></strong> 环境中加载启动 <strong><code>LangGraph</code></strong> 本地服务，则会从该环境中加载依赖。</p>
</li>
<li>
<p><strong><code>graphs</code></strong>：计算图或 <strong><code>Agent</code></strong> 对象的映射</p>
<p>这是个字典，其中每个键值对表示一条映射关系</p>
<ul>
<li><strong><code>Key</code></strong>：在 <strong><code>LangSmith Studio</code></strong> 中显示的应用名称</li>
<li><strong><code>Value</code></strong>：相应的<strong>应用实例</strong>在项目源码中的路径
<ul>
<li><strong><code>:</code></strong> 之前为应用实例所在的代码</li>
<li><strong><code>:</code></strong> 之后为应用实例在代码中的<strong>变量名</strong></li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong><code>env</code></strong>：环境变量文件路径</p>
</li>
</ul>
<h3 id="9-1-3-启动项目">9.1.3. 启动项目</h3>
<p>在命令行执行</p>
<pre><code class="language-powershell">langgraph dev
</code></pre>
<p>该命令将会启动一个轻量的本地服务，用于测试。</p>
<pre><code>#报错
UnicodeDecodeError: 'gbk' codec can't decode byte 0xaf in position 60: illegal multibyte sequence
#原因
  agent.py 里有中文注释（# 姓名、# 年龄），在 Windows 下 Python 默认用 GBK 编码读取 .py 文件，遇到 GBK 无法解码的字节就报
  UnicodeDecodeError。

  解决方法

  在 PowerShell 中（你平时用的终端），执行前先设置环境变量：

conda activate langgraph
$env:PYTHONUTF8 = &quot;1&quot;
langgraph dev

  或者一行搞定：

conda activate langgraph; $env:PYTHONUTF8 = &quot;1&quot;; langgraph dev

  永久修复（推荐）

  在 conda 的 langgraph 环境中设置该变量，之后 activate 就自动生效：

conda activate langgraph
conda env config vars set PYTHONUTF8=1
conda deactivate
conda activate langgraph

  这样以后每次 langgraph dev 都不用再手动设置了。
</code></pre>
<p><strong>如下所示</strong></p>
<pre><code class="language-powershell">(langgraph) PS C:\Users\Lenovo\OneDrive\文档\AI\langgraph\tutorials\hitl_demo&gt; langgraph dev
</code></pre>
<p><strong>启动日志如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260618145109521.png" alt="image-20260618145109521" /></p>
<p>如果配置无误，执行命令后通常会自动跳转至</p>
<p><strong><code>Studio UI</code></strong> 路径</p>
<blockquote>
<p>https://smith.langchain.com/studio/?baseUrl=http://127.0.0.1:2024</p>
</blockquote>
<p>如果没有跳转，在浏览器输入链接即可。</p>
<p><strong>页面如下所示</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260618145350749.png" alt="image-20260618145350749" /></p>
<p>可以看到计算图的拓扑结构。</p>
<h3 id="9-1-4-调试">9.1.4. 调试</h3>
<h4 id="9-1-4-1-首次调用">9.1.4.1. 首次调用</h4>
<p>该应用不需要任何输入状态，<strong><code>Input</code></strong> 字典留空即可</p>
<p>点击 <strong><code>Submit</code></strong> 启动计算图</p>
<p><img src="/images/langgraph-study-notes/image-20260618150027247.png" alt="image-20260618150027247" /></p>
<h4 id="9-1-4-2-第一次恢复运行">9.1.4.2. 第一次恢复运行</h4>
<p>计算图触发第一个断点，只需要输入本地开发时传递给 <strong><code>Command(resume)</code></strong> 的值即可</p>
<p><img src="/images/langgraph-study-notes/image-20260618150257805.png" alt="image-20260618150257805" /></p>
<h4 id="9-1-4-3-第二次恢复运行">9.1.4.3. 第二次恢复运行</h4>
<p>输入年龄</p>
<p><img src="/images/langgraph-study-notes/image-20260618150507901.png" alt="image-20260618150507901" /></p>
<h4 id="9-1-4-4-第三次恢复运行">9.1.4.4. 第三次恢复运行</h4>
<p>输入性别</p>
<p><img src="/images/langgraph-study-notes/image-20260618150603796.png" alt="image-20260618150603796" /></p>
<h4 id="9-1-4-5-最终结果">9.1.4.5. 最终结果</h4>
<p><img src="/images/langgraph-study-notes/image-20260618150618027.png" alt="image-20260618150618027" /></p>
<h3 id="9-1-5-调试时添加静态断点">9.1.5. 调试时添加静态断点</h3>
<p>我们可以在调试时添加静态断点</p>
<p><img src="/images/langgraph-study-notes/image-20260622094203428.png" alt="image-20260622094203428" /></p>
<p>我们在 <strong><code>get_info_node</code></strong> 之前添加断点</p>
<p><img src="/images/langgraph-study-notes/image-20260622094313578.png" alt="image-20260622094313578" /></p>
<p>运行</p>
<p><img src="/images/langgraph-study-notes/image-20260622094329281.png" alt="image-20260622094329281" /></p>
<p>中断后继续</p>
<p><img src="/images/langgraph-study-notes/image-20260622094437681.png" alt="image-20260622094437681" /></p>
<p>开始执行正常的节点计算逻辑</p>
<p><img src="/images/langgraph-study-notes/image-20260622094515519.png" alt="image-20260622094515519" /></p>
<h2 id="9-2-本地部署并对接-agentchatui">9.2. 本地部署并对接<code>AgentChatUI</code></h2>
<p><strong><code>AgentChatUI</code></strong> 是 <strong><code>langchain-ai</code></strong> 旗下另一个开源框架，专为带有 <strong><code>messages</code></strong> 状态字段的计算图或 <strong><code>Agent</code></strong> 应用提供前端交互页面。</p>
<p>默认情况下，状态中的其它字段可以被后端正常处理，但是不会在前端展示。</p>
<p><strong>官方仓库地址如下</strong></p>
<blockquote>
<p>https://github.com/langchain-ai/agent-chat-ui</p>
</blockquote>
<p>同样提供了开箱即用的<strong>云服务</strong></p>
<blockquote>
<p>https://agentchat.vercel.app/</p>
</blockquote>
<h3 id="9-2-1-准备文件">9.2.1. 准备文件</h3>
<h4 id="9-2-1-1-chat_agent-py">9.2.1.1. <code>chat_agent.py</code></h4>
<p>在项目的 <strong><code>src</code></strong> 目录下新建 <strong><code>chat_agent.py</code></strong>，写入以下内容</p>
<pre><code class="language-python">from typing import Literal

from langgraph.graph import MessagesState, StateGraph, START, END
from langgraph.types import interrupt

from langchain.messages import ToolMessage
from langchain.tools import tool
from langchain_deepseek import ChatDeepSeek

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

@tool(parse_docstring=True)
def get_weather(city: str) -&gt; str:
    &quot;&quot;&quot;
    根据城市名称，查询当日天气。

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    return f&quot;{city} 今天天气很好&quot;

tools = [get_weather]
tools_by_name = {
    current_tool.name: current_tool
    for current_tool in tools
}
model_with_tools = model.bind_tools(tools)

def llm_node(state: MessagesState) -&gt; dict:
    response = model_with_tools.invoke(state[&quot;messages&quot;])

    return {
        &quot;messages&quot;: [response]
    }

def tool_node(state: MessagesState) -&gt; dict:
    last_message = state[&quot;messages&quot;][-1]
    tool_calls = last_message.tool_calls

    # 一次中断，提交所有待审批的工具调用
    resume_value = interrupt({
        &quot;action_requests&quot;: [
            {
                &quot;name&quot;: tool_call[&quot;name&quot;],
                &quot;args&quot;: tool_call[&quot;args&quot;],
                &quot;description&quot;: (
                    f&quot;是否允许调用工具 {tool_call['name']}，&quot;
                    f&quot;参数为 {tool_call['args']}？&quot;
                ),
            }
            for tool_call in tool_calls
        ],
        &quot;review_configs&quot;: [
            {
                &quot;action_name&quot;: tool_call[&quot;name&quot;],
                &quot;allowed_decisions&quot;: [
                    &quot;approve&quot;,
                    &quot;reject&quot;,
                    &quot;edit&quot;,
                ],
            }
            for tool_call in tool_calls
        ],
    })

    decisions = resume_value.get(&quot;decisions&quot;, [])
    tool_messages = []

    for index, tool_call in enumerate(tool_calls):
        decision = (
            decisions[index]
            if index &lt; len(decisions)
            else {&quot;type&quot;: &quot;reject&quot;}
        )

        decision_type = decision.get(&quot;type&quot;)

        if decision_type == &quot;approve&quot;:
            selected_tool = tools_by_name[tool_call[&quot;name&quot;]]
            result = selected_tool.invoke(tool_call[&quot;args&quot;])

        elif decision_type == &quot;edit&quot;:
            edited_action = decision[&quot;edited_action&quot;]
            edited_args = edited_action[&quot;args&quot;]

            selected_tool = tools_by_name[tool_call[&quot;name&quot;]]
            result = selected_tool.invoke(edited_args)

        else:
            result = decision.get(
                &quot;message&quot;,
                &quot;用户拒绝调用该工具&quot;,
            )

        tool_messages.append(
            ToolMessage(
                content=str(result),
                tool_call_id=tool_call[&quot;id&quot;],
            )
        )

    return {
        &quot;messages&quot;: tool_messages
    }


def router(
    state: MessagesState,
) -&gt; Literal[&quot;tool_node&quot;, END]:
    last_message = state[&quot;messages&quot;][-1]

    if last_message.tool_calls:
        return &quot;tool_node&quot;

    return END


builder = StateGraph(state_schema=MessagesState)

builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, tool_node)

builder.add_edge(START, &quot;llm_node&quot;)

builder.add_conditional_edges(
    &quot;llm_node&quot;,
    router,
    [&quot;tool_node&quot;, END],
)

builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)

chat_graph = builder.compile()
</code></pre>
<p>此处返回的中断信息，遵循 <strong><code>AgentChatUI</code></strong> 定义的 <strong><code>HITL</code></strong> 协议，遵循此协议，则前端可以正确渲染中断信息，前端期望的中断信息格式如下：</p>
<pre><code class="language-json">{
    &quot;action_requests&quot;: [
        {
            &quot;name&quot;: &quot;get_weather&quot;,
            &quot;args&quot;: {
                &quot;city&quot;: &quot;杭州&quot;
            },
            &quot;description&quot;: &quot;是否允许查询杭州天气？&quot;
        }
    ],
    &quot;review_configs&quot;: [
        {
            &quot;action_name&quot;: &quot;get_weather&quot;,
            &quot;allowed_decisions&quot;: [
                &quot;approve&quot;,
                &quot;reject&quot;,
                &quot;edit&quot;
            ]
        }
    ]
}
</code></pre>
<p>当中断发生时，前端会将中断信息渲染在对话框中，根据用户操作的不同，后端拿到的反馈数据样式如下</p>
<ul>
<li>
<p><strong><code>approve</code></strong>：</p>
<pre><code class="language-json">{
    &quot;decisions&quot;: [
        {
            &quot;type&quot;: &quot;approve&quot;
        }
    ]
}
</code></pre>
</li>
<li>
<p><strong><code>reject</code></strong>：</p>
<pre><code class="language-json">{
    &quot;decisions&quot;: [
        {
            &quot;type&quot;: &quot;reject&quot;,
            &quot;message&quot;: &quot;不允许执行该操作&quot;
        }
    ]
}

</code></pre>
</li>
<li>
<p><strong><code>edit</code></strong>：</p>
<pre><code class="language-json">{
    &quot;decisions&quot;: [
        {
            &quot;type&quot;: &quot;edit&quot;,
            &quot;edited_action&quot;: {
                &quot;name&quot;: &quot;get_weather&quot;,
                &quot;args&quot;: {
                    &quot;city&quot;: &quot;上海&quot;
                }
            }
        }
    ]
}

</code></pre>
</li>
</ul>
<h4 id="9-2-1-2-更改-langgraph-json">9.2.1.2. 更改<code>langgraph.json</code></h4>
<p>在 <strong><code>graphs</code></strong> 中新增条目，如下</p>
<pre><code class="language-json">{
  &quot;dependencies&quot;: [&quot;.&quot;],
  &quot;graphs&quot;: {
    &quot;graph&quot;: &quot;./src/agent.py:graph&quot;,
    &quot;chat_graph&quot;: &quot;./src/chat_agent.py:chat_graph&quot;
  },
  &quot;env&quot;: &quot;.env&quot;
}
</code></pre>
<h3 id="9-2-2-对接-agentchatui">9.2.2. 对接<code>AgentChatUI</code></h3>
<h4 id="9-2-2-1-重启本地服务">9.2.2.1. 重启本地服务</h4>
<h4 id="9-2-2-2-访问云服务">9.2.2.2. 访问云服务</h4>
<p>在官方云服务入口页面填入需要的信息</p>
<p><img src="/images/langgraph-study-notes/image-20260618162305375.png" alt="image-20260618162305375" /></p>
<ul>
<li><strong><code>Deployment URL</code></strong>：本地服务 <strong><code>URL</code></strong></li>
<li><strong><code>Assistant / Graph ID</code></strong>：配置文件中声明的应用名称</li>
<li><strong><code>LangSmith API Key</code></strong>：略</li>
</ul>
<p>点击 <strong><code>Continue</code></strong>，如果配置正确，则跳转至以下页面</p>
<p><img src="/images/langgraph-study-notes/image-20260618162323141.png" alt="image-20260618162323141" /></p>
<h3 id="9-2-3-测试">9.2.3. 测试</h3>
<h4 id="9-2-3-1-对话">9.2.3.1. 对话</h4>
<p><img src="/images/langgraph-study-notes/image-20260618162337948.png" alt="image-20260618162337948" /></p>
<h4 id="9-2-3-2-工具调用">9.2.3.2. 工具调用</h4>
<p>在工具节点添加了 <strong><code>HITL</code></strong> 机制，所以调用工具会触发中断</p>
<p><img src="/images/langgraph-study-notes/image-20260618162433292.png" alt="image-20260618162433292" /></p>
<h5 id="9-2-3-2-1-同意调用">9.2.3.2.1. 同意调用</h5>
<p>点击 <strong><code>Approve</code></strong> 按钮</p>
<p><img src="/images/langgraph-study-notes/image-20260618162626628.png" alt="image-20260618162626628" /></p>
<p><strong>结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260618162813994.png" alt="image-20260618162813994" /></p>
<h5 id="9-2-3-2-2-修改参数后调用">9.2.3.2.2. 修改参数后调用</h5>
<p><img src="/images/langgraph-study-notes/image-20260618162911108.png" alt="image-20260618162911108" /></p>
<p><strong>修改后提交，结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260618163010522.png" alt="image-20260618163010522" /></p>
<h5 id="9-2-3-2-3-拒绝调用">9.2.3.2.3. 拒绝调用</h5>
<p><img src="/images/langgraph-study-notes/image-20260618163130038.png" alt="image-20260618163130038" /></p>
<p><strong>编辑原因后拒绝，结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260618163143688.png" alt="image-20260618163143688" /></p>
<h4 id="9-2-3-3-查看历史记录">9.2.3.3. 查看历史记录</h4>
<p><img src="/images/langgraph-study-notes/image-20260618163230394.png" alt="image-20260618163230394" /></p>
<p>左侧列出了历史会话，点击会话名称即可查看历史会话</p>
<p><img src="/images/langgraph-study-notes/image-20260618163307768.png" alt="image-20260618163307768" /></p>
<p>如果配置了 <strong><code>LangSmith</code></strong> 追踪所需的环境变量，还可以在以下页面追踪调用的详细信息。</p>
<p><img src="/images/langgraph-study-notes/image-20260618162754391.png" alt="image-20260618162754391" /></p>
<h1 id="10-工具调用节点">10. 工具调用节点</h1>
<h2 id="10-1-工具节点的实现">10.1. 工具节点的实现</h2>
<p>工具是 <strong><code>Agent</code></strong> 扩展外部能力的重要组件之一。本节以最基础的 <strong><code>ReAct</code> 式工具调用循环</strong>为例，分别介绍手动实现工具节点和使用预构建 <strong><code>ToolNode</code></strong> 两种方式。</p>
<p>手动实现有助于理解工具调用的基本流程：模型生成工具调用请求，工具节点根据名称查找并执行工具，再将执行结果封装成与原工具调用对应的 <strong><code>ToolMessage</code></strong>。不过，实际项目还需要处理参数校验、异常处理、运行时注入、并行执行以及 <strong><code>Command</code></strong> 传播等问题。</p>
<p><strong><code>ToolNode</code></strong> 对这些通用逻辑进行了封装，适合在需要自定义图结构和工具调用流程的场景中使用。</p>
<h3 id="10-1-1-手动处理工具调用">10.1.1. 手动处理工具调用</h3>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import MessagesState
from langchain.messages import HumanMessage, ToolMessage
from langchain.tools import tool
from langchain_deepseek import ChatDeepSeek

from loguru import logger
from dotenv import load_dotenv
load_dotenv(override=True)

@tool(parse_docstring=True)
def get_weather(city: str) -&gt; str:
    &quot;&quot;&quot;
    查询指定城市的当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    return f&quot;{city} 今天天气不错&quot;

@tool(parse_docstring=True)
def get_news(home_or_abroad: bool) -&gt; str:
    &quot;&quot;&quot;
    查询国内外新闻

    Args:
        home_or_abroad: 查询国内还是国外新闻，True: 国内新闻，False：国外新闻
    &quot;&quot;&quot;
    if home_or_abroad:
        return &quot;Kimi 新模型发布&quot;
    return &quot;Anthropic 暂停新模型访问&quot;

tools_by_name = {
    &quot;get_weather&quot;: get_weather,
    &quot;get_news&quot;: get_news
}
tools = [get_weather, get_news]

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)
model_with_tools = model.bind_tools(tools=tools)

def llm_node(state: MessagesState) -&gt; MessagesState:
    messages = state['messages']
    response = model_with_tools.invoke(messages)

    return {
        &quot;messages&quot;: [response]
    }

def tool_node(state: MessagesState) -&gt; MessagesState:
    last_msg = state['messages'][-1]
    if not last_msg.tool_calls:
        return {}

    tool_msgs = []
    for tool_call in last_msg.tool_calls:
        tool = tools_by_name[tool_call[&quot;name&quot;]]
        logger.info(&quot;工具 {} 被调用, 对应的 tool_call: {}&quot;, tool_call[&quot;name&quot;], tool_call)
        tool_res = tool.invoke(tool_call[&quot;args&quot;])
        tool_msg = ToolMessage(
            name = tool_call[&quot;name&quot;],
            content = tool_res,
            tool_call_id = tool_call[&quot;id&quot;]
        )
        tool_msgs.append(tool_msg)

    return {
        &quot;messages&quot;: tool_msgs
    }

def router(state: MessagesState) -&gt; Literal[&quot;tool_node&quot;, END]:
    if state['messages'][-1].tool_calls:
        return &quot;tool_node&quot;
    return END

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, tool_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_conditional_edges(&quot;llm_node&quot;, router, path_map=[&quot;tool_node&quot;, END])
builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)

graph = builder.compile()

from IPython.display import display
display(graph)

res = graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;今天北京天气如何？国内有哪些新闻&quot;)]})
for msg in res['messages']:
    msg.pretty_print()
</code></pre>
<blockquote>
<p><strong>注意</strong></p>
<p>上面的手动实现会按照 <strong><code>tool_calls</code></strong> 的顺序串行执行工具，主要用于展示底层流程。<strong><code>ToolNode</code></strong> 在同步调用中可通过执行器并行处理多个工具调用，在异步调用中则使用 <strong><code>asyncio.gather()</code></strong> 并发执行。</p>
</blockquote>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260622095910802.png" alt="image-20260622095910802" /></p>
<pre><code class="language-json">2026-06-22 09:59:05.258 | INFO     | __main__:tool_node:67 - 工具 get_weather 被调用, 对应的 tool_call: {'name': 'get_weather', 'args': {'city': '北京'}, 'id': 'call_00_lpemFmfsKxTtEKjweosB0896', 'type': 'tool_call'}
2026-06-22 09:59:05.259 | INFO     | __main__:tool_node:67 - 工具 get_news 被调用, 对应的 tool_call: {'name': 'get_news', 'args': {'home_or_abroad': True}, 'id': 'call_01_DDLZCKnovq6EtdrD6XXw2202', 'type': 'tool_call'}
================================ Human Message =================================

今天北京天气如何？国内有哪些新闻
================================== Ai Message ==================================

好的，我来查询今天北京的天气和国内新闻。
Tool Calls:
  get_weather (call_00_lpemFmfsKxTtEKjweosB0896)
 Call ID: call_00_lpemFmfsKxTtEKjweosB0896
  Args:
    city: 北京
  get_news (call_01_DDLZCKnovq6EtdrD6XXw2202)
 Call ID: call_01_DDLZCKnovq6EtdrD6XXw2202
  Args:
    home_or_abroad: True
================================= Tool Message =================================
Name: get_weather

北京 今天天气不错
================================= Tool Message =================================
Name: get_news

Kimi 新模型发布
================================== Ai Message ==================================

以下是今天的查询结果：

### 🌤️ 北京天气
今天北京的天气**不错**，适合外出活动哦！

### 📰 国内新闻
目前有一条重要新闻：**Kimi 新模型发布**

如果你想了解更多详情，可以继续问我！
</code></pre>
<h3 id="10-1-2-用-toolnode-处理工具调用">10.1.2. 用<code>ToolNode</code>处理工具调用</h3>
<p>上一节的工具节点完成了以下工作：读取最后一条 <strong><code>AIMessage</code></strong> 中的 <strong><code>tool_calls</code></strong>，根据工具名称查找并执行对应工具，再把执行结果封装成与原工具调用一一对应的 <strong><code>ToolMessage</code></strong>。</p>
<p>这种实现适合帮助我们理解工具调用的基本流程，但它只覆盖了最简单的场景。实际项目还需要处理无效工具名、参数校验、异常处理、运行时参数注入、<strong><code>Command</code></strong> 传播以及多个工具调用的并行执行等问题。</p>
<p><strong><code>LangGraph</code></strong> 提供了预构建的 <strong><code>ToolNode</code></strong>。只需传入工具列表，它便可以完成上述所有操作。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END
from langgraph.prebuilt.tool_node import ToolNode
from langgraph.graph.message import MessagesState
from langchain.messages import HumanMessage
from langchain.tools import tool
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
load_dotenv(override=True)

@tool(parse_docstring=True)
def get_weather(city: str) -&gt; str:
    &quot;&quot;&quot;
    查询指定城市的当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    return f&quot;{city} 今天天气不错&quot;

@tool(parse_docstring=True)
def get_news(home_or_abroad: bool) -&gt; str:
    &quot;&quot;&quot;
    查询国内外新闻

    Args:
        home_or_abroad: 查询国内还是国外新闻，True: 国内新闻，False：国外新闻
    &quot;&quot;&quot;
    if home_or_abroad:
        return &quot;Kimi 新模型发布&quot;
    return &quot;Anthropic 暂停新模型访问&quot;

tools = [get_weather, get_news]

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)
model_with_tools = model.bind_tools(tools=tools)

def llm_node(state: MessagesState) -&gt; MessagesState:
    messages = state['messages']
    response = model_with_tools.invoke(messages)

    return {
        &quot;messages&quot;: [response]
    }

def router(state: MessagesState) -&gt; Literal[&quot;tool_node&quot;, END]:
    if state['messages'][-1].tool_calls:
        return &quot;tool_node&quot;
    return END

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, ToolNode(tools=tools))
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_conditional_edges(&quot;llm_node&quot;, router, path_map=[&quot;tool_node&quot;, END])
builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)

graph = builder.compile()

from IPython.display import display
display(graph)

res = graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;今天北京天气如何？国内有哪些新闻？&quot;)]})
for msg in res['messages']:
    msg.pretty_print()
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260622100605743.png" alt="image-20260622100605743" /></p>
<pre><code class="language-json">================================ Human Message =================================

今天北京天气如何？国内有哪些新闻？
================================== Ai Message ==================================

好的，我来同时查询北京天气和国内新闻。
Tool Calls:
  get_weather (call_00_BBHYjTwSiWbGolSrp7yb9812)
 Call ID: call_00_BBHYjTwSiWbGolSrp7yb9812
  Args:
    city: 北京
  get_news (call_01_3XEu887comx2s1SMtw8h3282)
 Call ID: call_01_3XEu887comx2s1SMtw8h3282
  Args:
    home_or_abroad: True
================================= Tool Message =================================
Name: get_weather

北京 今天天气不错
================================= Tool Message =================================
Name: get_news

Kimi 新模型发布
================================== Ai Message ==================================

以下是您查询的信息：

---

### 🌤️ 北京今日天气
北京今天**天气不错**，适合外出活动。

### 📰 国内新闻
今日国内有一条重要新闻：**Kimi 新模型发布**。

如果您想了解更多详细内容，随时可以继续问我！
</code></pre>
<h2 id="10-2-进阶用法">10.2. 进阶用法</h2>
<h3 id="10-2-1-toolruntime-介绍">10.2.1. <code>ToolRuntime</code>介绍</h3>
<p><strong><code>ToolRuntime</code></strong> 是专门面向工具调用的运行时对象。</p>
<p>按照官方规范：</p>
<ul>
<li>工具函数中存在名为 <strong><code>runtime</code></strong></li>
<li>并且类型标注为 <strong><code>ToolRuntime</code></strong> 的参数时</li>
</ul>
<p>底层运行时会在调用工具前自动注入 <strong><code>ToolRuntime</code></strong> 实例。根据源码实现，某些不规范的写法也能注入运行时实例，但并不规范，不推荐。</p>
<p>需要注意，<strong><code>ToolRuntime</code></strong> 与图节点中使用的 <strong><code>langgraph.runtime.Runtime</code></strong> 并不是同一个类型。前者额外提供了当前图状态、运行配置和工具调用 ID 等工具调用专属信息。</p>
<p>当前版本，<strong><code>ToolRuntime</code></strong> 的核心定义如下：</p>
<pre><code class="language-python">@dataclass
class ToolRuntime(_DirectlyInjectedToolArg, Generic[ContextT, StateT]):
    state: StateT
    context: ContextT
    config: RunnableConfig
    stream_writer: StreamWriter
    tool_call_id: str | None
    store: BaseStore | None
</code></pre>
<ul>
<li><strong><code>state</code></strong>：图状态，短期记忆</li>
<li><strong><code>context</code></strong>：运行时上下文</li>
<li><strong><code>config</code></strong>：运行时配置，包含元数据</li>
<li><strong><code>stream_writer</code></strong>：自定义流式输出写入器</li>
<li><strong><code>tool_call_id</code></strong>：工具调用 <strong><code>ID</code></strong></li>
<li><strong><code>store</code></strong>：长期记忆</li>
</ul>
<p>借助 <strong><code>runtime</code></strong>，工具可以访问上述所有资源。</p>
<h3 id="10-2-2-在工具中更新状态">10.2.2. 在工具中更新状态</h3>
<p>工具不仅可以返回普通结果，还可以返回 <strong><code>Command(update=...)</code></strong>，将业务结果写入图状态。</p>
<p>当工具由模型发起调用时，消息历史中的 <strong><code>AIMessage.tool_calls</code></strong> 必须紧跟与之匹配的 <strong><code>ToolMessage</code></strong>。</p>
<p>当工具返回普通结果时，<strong><code>ToolNode</code></strong> 会帮我们完成运行结果到 <strong><code>ToolMessage</code></strong> 的转换，并追加到消息列表。</p>
<p>但工具返回 <strong><code>Command</code></strong> 时，上述操作需要开发者完成，此时还应在 <strong><code>Command.update</code></strong> 的消息字段中写入对应的 <strong><code>ToolMessage</code></strong>。</p>
<p>如果同一轮并行执行的多个工具可能更新同一个普通状态字段，需要为该字段配置合适的归并函数；否则可能出现并发更新冲突。本例中的两个工具分别更新 <strong><code>weather_res</code></strong> 和 <strong><code>news_res</code></strong>，因此不存在该问题。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END
from langgraph.prebuilt.tool_node import ToolNode, ToolRuntime
from langgraph.types import Command
from langgraph.graph.message import MessagesState

from langchain.messages import HumanMessage, ToolMessage
from langchain.tools import tool
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
load_dotenv(override=True)

class OverAllState(MessagesState):
    weather_res: str
    news_res: str

@tool(parse_docstring=True)
def get_weather(city: str, runtime: ToolRuntime) -&gt; Command:
    &quot;&quot;&quot;
    查询指定城市的当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    res = f&quot;{city} 今天天气不错&quot;
    tool_call_id = runtime.tool_call_id
    tool_msg = ToolMessage(tool_call_id=tool_call_id, content=res)
    return Command(
        update = {
            &quot;weather_res&quot;: res,
            &quot;messages&quot;: [tool_msg]
        }
    )

@tool(parse_docstring=True)
def get_news(home_or_abroad: bool, runtime: ToolRuntime) -&gt; Command:
    &quot;&quot;&quot;
    查询国内外新闻

    Args:
        home_or_abroad: 查询国内还是国外新闻，True: 国内新闻，False：国外新闻
    &quot;&quot;&quot;
    if home_or_abroad:
        res = &quot;Kimi 新模型发布&quot;
    else:
        res = &quot;Anthropic 暂停新模型访问&quot;
    tool_call_id = runtime.tool_call_id
    tool_msg = ToolMessage(tool_call_id=tool_call_id, content=res)
    return Command(
        update = {
            &quot;news_res&quot;: res,
            &quot;messages&quot;: [tool_msg]
        }
    )

tools = [get_weather, get_news]

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)
model_with_tools = model.bind_tools(tools=tools)

def llm_node(state: OverAllState) -&gt; OverAllState:
    messages = state['messages']
    response = model_with_tools.invoke(messages)

    return {
        &quot;messages&quot;: [response]
    }

def router(state: OverAllState) -&gt; Literal[&quot;tool_node&quot;, END]:
    if state['messages'][-1].tool_calls:
        return &quot;tool_node&quot;
    return END

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, ToolNode(tools=tools))
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_conditional_edges(&quot;llm_node&quot;, router, path_map=[&quot;tool_node&quot;, END])
builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)

graph = builder.compile()

from IPython.display import display
display(graph)

res = graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;今天北京天气如何？国内有哪些新闻？&quot;)]})
print('=' * 30, '-&gt; messages &lt;-', '=' * 30)
for msg in res.pop('messages'):
    msg.pretty_print()

print('=' * 30, '-&gt; res without messages &lt;-', '=' * 30)
print(res)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260622104243639.png" alt="image-20260622104243639" /></p>
<pre><code class="language-json">============================== -&gt; messages &lt;- ==============================
================================ Human Message =================================

今天北京天气如何？国内有哪些新闻？
================================== Ai Message ==================================

好的，我来同时查询北京天气和国内新闻。
Tool Calls:
  get_weather (call_00_hbEppxygz7DLDwaxg8X19090)
 Call ID: call_00_hbEppxygz7DLDwaxg8X19090
  Args:
    city: 北京
  get_news (call_01_wkG4j5Hjq4wkyTOaxzxT5267)
 Call ID: call_01_wkG4j5Hjq4wkyTOaxzxT5267
  Args:
    home_or_abroad: True
================================= Tool Message =================================
Name: get_weather

北京 今天天气不错
================================= Tool Message =================================
Name: get_news

Kimi 新模型发布
================================== Ai Message ==================================

为您查询到以下信息：

### 🌤️ 北京今日天气
今天北京的天气**不错**，比较适宜出行。

### 📰 国内新闻
- **Kimi 新模型发布** — 国内AI领域又有新动态。

如果您想了解更多详情，可以继续问我！
============================== -&gt; res without messages &lt;- ==============================
{'weather_res': '北京 今天天气不错', 'news_res': 'Kimi 新模型发布'}
</code></pre>
<h3 id="10-2-3-工具节点执行与容错机制">10.2.3. 工具节点执行与容错机制</h3>
<p><strong><code>ToolNode</code></strong> 提供同步工具调用包装器 <strong><code>wrap_tool_call</code></strong>，用于在工具执行前后插入自定义逻辑。</p>
<p>它与 <strong><code>LangChain Agent</code></strong> 中间件 <strong><code>wrap_tool_call</code></strong> 的核心机制相同：接收当前工具调用请求 <strong><code>request</code></strong> 和真正执行工具的回调 <strong><code>execute</code></strong>，可以选择不调用、调用一次或多次 <strong><code>execute(request)</code></strong>，并最终返回 <strong><code>ToolMessage</code></strong> 或 <strong><code>Command</code></strong>。</p>
<p>因此，<strong><code>wrap_tool_call</code></strong> 可以用于重试、缓存、请求改写、短路返回和自定义控制流等场景。异步工具链路还可以使用对应的 <strong><code>awrap_tool_call</code></strong>。</p>
<p>此处通过重试和缓存机制的实现，介绍 <strong><code>wrap_tool_call</code></strong> 的用法。</p>
<h4 id="10-2-3-1-实现重试机制">10.2.3.1. 实现重试机制</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END
from langgraph.prebuilt.tool_node import ToolNode
from langgraph.graph.message import MessagesState

from langchain.messages import HumanMessage, ToolMessage
from langchain.tools import tool
from langchain_deepseek import ChatDeepSeek

from dataclasses import dataclass
from loguru import logger
from dotenv import load_dotenv
load_dotenv(override=True)

import random

@tool(parse_docstring=True)
def get_weather(city: str) -&gt; str:
    &quot;&quot;&quot;
    查询指定城市的当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    # 70% 概率因网络波动而调用失败
    rand_int = random.randint(1,10)
    if rand_int &lt; 8:
        raise ConnectionError(&quot;网络波动失败&quot;)
    return f&quot;{city} 今天天气不错&quot;

tools = [get_weather]

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)
model_with_tools = model.bind_tools(tools=tools)

@dataclass
class UserContext:
    max_attempts: int

def llm_node(state: MessagesState) -&gt; MessagesState:
    messages = state['messages']
    response = model_with_tools.invoke(messages)

    return {
        &quot;messages&quot;: [response]
    }

def router(state: MessagesState) -&gt; Literal[&quot;tool_node&quot;, END]:
    if state['messages'][-1].tool_calls:
        return &quot;tool_node&quot;
    return END

def wrap_tool_call(request, execute):
    max_attempts = request.runtime.context.max_attempts
    tool_call_id = request.runtime.tool_call_id

    tool_msg = &quot;&quot;
    for i in range(max_attempts):
        try:
            tool_msg = execute(request)
            break
        except ConnectionError as e:
            logger.info(&quot;工具调用失败，当前调用次数: {}, 调用次数上限: {}, 异常信息: {}&quot;, i + 1, max_attempts, e)

    if not tool_msg:
        tool_msg = ToolMessage(
            tool_call_id = tool_call_id,
            content = &quot;调用次数到达上限，调用失败&quot;
        )

    return tool_msg

builder = StateGraph(state_schema=MessagesState, context_schema=UserContext)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, ToolNode(tools=tools, wrap_tool_call=wrap_tool_call))
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_conditional_edges(&quot;llm_node&quot;, router, path_map=[&quot;tool_node&quot;, END])
builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)

graph = builder.compile()

from IPython.display import display
display(graph)

res = graph.invoke(
    {&quot;messages&quot;: [HumanMessage(&quot;今天北京天气如何？&quot;)]},
    context = UserContext(max_attempts=3))
for msg in res['messages']:
    msg.pretty_print()
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260622105919179.png" alt="image-20260622105919179" /></p>
<p>按照当前设计，每次工具调用有 <strong><code>70%</code></strong> 的概率失败，最多尝试 <strong><code>3</code></strong> 次。仍然可能最终失败，如下所示。</p>
<pre><code class="language-json">2026-06-22 11:12:21.039 | INFO     | __main__:wrap_tool_call:71 - 工具调用失败，当前调用次数: 1, 调用次数上限: 3, 异常信息: 网络波动失败
2026-06-22 11:12:21.042 | INFO     | __main__:wrap_tool_call:71 - 工具调用失败，当前调用次数: 1, 调用次数上限: 3, 异常信息: 网络波动失败
2026-06-22 11:12:21.046 | INFO     | __main__:wrap_tool_call:71 - 工具调用失败，当前调用次数: 1, 调用次数上限: 3, 异常信息: 网络波动失败
================================ Human Message =================================

今天北京天气如何？
================================== Ai Message ==================================

好的，我来查询一下北京今天的天气情况。
Tool Calls:
  get_weather (call_00_iYUeyQlDB0NuAAkcU1Zy3110)
 Call ID: call_00_iYUeyQlDB0NuAAkcU1Zy3110
  Args:
    city: 北京
================================= Tool Message =================================

调用次数到达上限，调用失败
================================== Ai Message ==================================

抱歉，查询天气的服务暂时遇到了问题，无法获取到北京今天的天气信息。可能是以下原因导致的：

1. **服务暂时不可用** - 可能是天气数据接口出现了临时故障
2. **网络问题** - 查询过程中网络连接不稳定

建议您可以：
- **稍后再试**，看看服务是否恢复
- 通过手机上的天气App或浏览器搜索&quot;北京天气&quot;来获取最新信息

非常抱歉给您带来不便！如果还有其他问题需要帮助，请随时告诉我。
</code></pre>
<p>也有可能最终成功，如下</p>
<pre><code class="language-json">2026-06-22 14:03:22.371 | INFO     | __main__:wrap_tool_call:70 - 工具调用失败，当前调用次数: 1, 调用次数上限: 3, 异常信息: 网络波动失败
================================ Human Message =================================

今天北京天气如何？
================================== Ai Message ==================================

好的，我来帮你查询一下今天北京的天气情况。
Tool Calls:
  get_weather (call_00_IXWZ2mg3lxI09gzm9BbZ4314)
 Call ID: call_00_IXWZ2mg3lxI09gzm9BbZ4314
  Args:
    city: 北京
================================= Tool Message =================================
Name: get_weather

北京 今天天气不错
================================== Ai Message ==================================

今天北京的天气**不错**，是个适合外出活动的好日子！☀️

如果你需要更详细的天气信息（比如具体温度、风向等），可以告诉我，我再帮你进一步查询。
</code></pre>
<h4 id="10-2-3-2-实现缓存机制">10.2.3.2. 实现缓存机制</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END
from langgraph.prebuilt.tool_node import ToolNode
from langgraph.graph.message import MessagesState

from langchain.messages import HumanMessage, ToolMessage
from langchain.tools import tool
from langchain_deepseek import ChatDeepSeek

from loguru import logger
import json
from dotenv import load_dotenv
load_dotenv(override=True)

@tool(parse_docstring=True)
def get_weather(city: str) -&gt; str:
    &quot;&quot;&quot;
    查询指定城市的当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    return f&quot;{city} 今天天气不错&quot;

tools = [get_weather]

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)
model_with_tools = model.bind_tools(tools=tools)

def llm_node(state: MessagesState) -&gt; MessagesState:
    messages = state['messages']
    response = model_with_tools.invoke(messages)

    return {
        &quot;messages&quot;: [response]
    }

def router(state: MessagesState) -&gt; Literal[&quot;tool_node&quot;, END]:
    if state['messages'][-1].tool_calls:
        return &quot;tool_node&quot;
    return END

global_cache = dict()
def wrap_tool_call(request, execute):
    tool_name = request.tool_call[&quot;name&quot;]
    tool_args = json.dumps(request.tool_call[&quot;args&quot;])
    tool_call_id = request.runtime.tool_call_id

    cache_key = (tool_name, tool_args)
    cache = global_cache.get(cache_key)

    if cache:
        logger.info(&quot;{} 调用命中缓存&quot;, tool_name)
        tool_msg = ToolMessage(
            tool_call_id = tool_call_id,
            content = cache
        )
    else:
        tool_msg = execute(request)
        logger.info(&quot;将 {} 的调用结果写入缓存&quot;, tool_name)
        global_cache[cache_key] = tool_msg.content

    return tool_msg

builder = StateGraph(state_schema=MessagesState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, ToolNode(tools=tools, wrap_tool_call=wrap_tool_call))
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_conditional_edges(&quot;llm_node&quot;, router, path_map=[&quot;tool_node&quot;, END])
builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)

graph = builder.compile()

from IPython.display import display
display(graph)

print('=' * 30, '-&gt; 第一次调用 - 北京 &lt;-', '=' * 30)
res = graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;今天北京天气如何？&quot;)]})
for msg in res['messages']:
    msg.pretty_print()

print('=' * 30, '-&gt; 第二次调用 - 北京 &lt;-', '=' * 30)
res = graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;今天北京天气如何？&quot;)]})
for msg in res['messages']:
    msg.pretty_print()

print('=' * 30, '-&gt; 第三次调用 - 杭州 &lt;-', '=' * 30)
res = graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;今天杭州天气如何？&quot;)]})
for msg in res['messages']:
    msg.pretty_print()
</code></pre>
<blockquote>
<p><strong>注意</strong></p>
<p>这里使用的 <strong><code>global_cache</code></strong> 只是进程内缓存，主要用于演示：它没有 <strong><code>TTL</code></strong>、容量限制、并发保护和持久化能力，也只缓存普通 <strong><code>ToolMessage</code></strong> 的内容。生产环境应根据数据时效性选择 <strong><code>Redis</code></strong> 等缓存系统，并把用户、权限、地域、工具版本等会影响结果的因素纳入缓存键；具有副作用的工具通常不应直接缓存。</p>
</blockquote>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260622112113218.png" alt="image-20260622112113218" /></p>
<pre><code class="language-json">============================== -&gt; 第一次调用 - 北京 &lt;- ==============================
2026-06-22 14:05:29.845 | INFO     | __main__:wrap_tool_call:67 - 将 get_weather 的调用结果写入缓存
================================ Human Message =================================

今天北京天气如何？
================================== Ai Message ==================================

好的，我来查询一下北京今天的天气情况。
Tool Calls:
  get_weather (call_00_RWpYgZGeskrexH2dOWol2713)
 Call ID: call_00_RWpYgZGeskrexH2dOWol2713
  Args:
    city: 北京
================================= Tool Message =================================
Name: get_weather

北京 今天天气不错
================================== Ai Message ==================================

北京今天天气不错哦！☀️ 是个适合外出活动的好天气。不过具体温度、风力等详细信息我就没法提供了，建议您出门前可以根据自己的体感适当增减衣物。希望您度过愉快的一天！😊
============================== -&gt; 第二次调用 - 北京 &lt;- ==============================
2026-06-22 14:05:32.277 | INFO     | __main__:wrap_tool_call:60 - get_weather 调用命中缓存
================================ Human Message =================================

今天北京天气如何？
================================== Ai Message ==================================

好的，我来查询一下北京今天的天气情况。
Tool Calls:
  get_weather (call_00_Kimj3ySGhtIFhuYjIuw67098)
 Call ID: call_00_Kimj3ySGhtIFhuYjIuw67098
  Args:
    city: 北京
================================= Tool Message =================================

北京 今天天气不错
================================== Ai Message ==================================

北京今天天气不错！☀️ 是个适合出门的好天气。不过具体的温度和风力等详细信息没有提供，如果你需要了解更多细节，可以告诉我哦~ 😊
============================== -&gt; 第三次调用 - 杭州 &lt;- ==============================
2026-06-22 14:05:34.514 | INFO     | __main__:wrap_tool_call:67 - 将 get_weather 的调用结果写入缓存
================================ Human Message =================================

今天杭州天气如何？
================================== Ai Message ==================================

好的，我来查询一下杭州今天的天气情况。
Tool Calls:
  get_weather (call_00_KvnVrq2WNq1C9I0xKb3U7776)
 Call ID: call_00_KvnVrq2WNq1C9I0xKb3U7776
  Args:
    city: 杭州
================================= Tool Message =================================
Name: get_weather

杭州 今天天气不错
================================== Ai Message ==================================

杭州今天天气不错！☀️ 看起来是一个适合出行或者户外活动的好日子。如果你有出门的计划，可以放心安排哦～不过如果需要更具体的温度、风力等信息，建议可以再查一下实时的天气预报。😊
</code></pre>
]]></content>
  </entry>
  <entry>
    <title>03-LangGraph持久化与记忆管理</title>
    <url>/2025/05/16/langgraph-persistence-memory/</url>
    <content><![CDATA[<h1 id="6-持久化机制和可恢复执行">6. 持久化机制和可恢复执行</h1>
<h2 id="6-1-概述">6.1. 概述</h2>
<h3 id="6-1-1-什么是可恢复执行">6.1.1. 什么是可恢复执行</h3>
<p>在工作流、Agent、任务编排系统里，<strong>可恢复执行 <code>Durable Execution</code></strong> 指的是：</p>
<blockquote>
<p>把一次任务执行过程中的关键进度、状态、结果保存到可靠存储中，使任务可以在中断、失败、等待外部输入后继续执行。</p>
</blockquote>
<p>它解决的是“执行过程能否恢复”的问题。</p>
<p>例如：</p>
<pre><code>普通执行：
开始 -&gt; A -&gt; B -&gt; C
如果执行到 B 后程序挂了，重启后可能只能从 A 重新开始。

可恢复执行：
开始 -&gt; A   保存检查点
     -&gt; B   保存检查点
     -&gt; C
如果执行到 B 后挂了，恢复时可以从已保存的位置继续。
</code></pre>
<p>所以它关注的不是“数据长期保存”本身，而是：</p>
<blockquote>
<p><strong>执行过程本身具有可恢复性。</strong></p>
</blockquote>
<p>它关心的是：系统能否记住“任务执行到了哪里、当前状态是什么、接下来应该继续执行什么”。</p>
<h3 id="6-1-2-langgraph的持久化机制">6.1.2. LangGraph的持久化机制</h3>
<h4 id="6-1-2-1-什么是langgraph的持久化机制">6.1.2.1. 什么是LangGraph的持久化机制</h4>
<p><strong><code>LangGraph</code></strong> 的<strong>持久化机制</strong>（<strong><code>Persistence</code></strong>）可以理解为：</p>
<blockquote>
<p>在图执行过程中，将每个关键阶段的图状态保存为检查点，并按照线程进行组织。</p>
</blockquote>
<p>这里的“持久化”并不是简单地把某个变量保存到文件里，而是保存一次图执行所需的状态信息，使得未来可以基于检查点继续执行。</p>
<p><strong><code>LangGraph</code></strong> 的持久化机制围绕以下几个问题展开：</p>
<ol>
<li>
<p><strong>保存什么？</strong></p>
<p>保存图在某个时刻的状态快照，也就是 <strong><code>Checkpoint</code></strong>。</p>
</li>
<li>
<p><strong>保存到哪里？</strong></p>
<p>保存到 <strong><code>Checkpointer</code></strong> 管理的存储后端中。
开发时可以使用基于内存的 <strong><code>InMemorySaver</code></strong>，生产环境中通常会使用数据库等可靠存储，如 <strong><code>PostgresSaver</code></strong>。</p>
</li>
<li>
<p><strong>如何区分不同会话？</strong></p>
<p>通过 <strong><code>thread_id</code></strong> 区分不同的执行线程。
同一个 <strong><code>thread_id</code></strong> 表示同一条持久化执行线，也可以理解为同一个会话。</p>
</li>
<li>
<p><strong>开发者看到的是什么？</strong></p>
<p>开发者通常不会直接操作底层 <strong><code>Checkpoint</code></strong>，而是通过 <strong><code>get_state()</code></strong> 和 <strong><code>get_state_history()</code></strong> 查看 <strong><code>Checkpoint</code></strong> 的开发者视图 <strong><code>StateSnapshot</code></strong>。</p>
</li>
</ol>
<p>因此，本章后续会围绕四个核心问题展开：</p>
<pre><code class="language-markdown">如何启用可恢复执行？
如何配置持久化模式？
如何查看历史检查点？
如何利用检查点进行恢复、回放和分叉？
</code></pre>
<h4 id="6-1-2-2-核心组件">6.1.2.2. 核心组件</h4>
<p>本节对核心组件进行梳理，不必死记硬背，用到查阅即可。</p>
<p><strong><code>LangGraph</code></strong> 的持久化机制依赖一系列核心组件，如下：</p>
<table>
<thead>
<tr>
<th>概念</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>State</code></strong></td>
<td>用户定义的图状态结构，负责节点间信息传递。<strong><code>LangGraph</code></strong> 会将 <strong><code>State</code></strong> 转换为底层 <strong><code>Channel</code></strong>；运行时真正负责节点间通信的是 <strong><code>Channel</code></strong>。</td>
</tr>
<tr>
<td><strong><code>Channel</code></strong></td>
<td><strong><code>LangGraph</code></strong> 用于节点间通信的底层机制，节点从 <strong><code>Channel</code></strong> 读数据、向 <strong><code>Channel</code></strong> 写更新；既包括承载 <strong><code>State</code></strong> 字段更新的状态通道，也包括用于分支、任务调度等行为的内部通道。</td>
</tr>
<tr>
<td><strong><code>Checkpoint</code></strong></td>
<td>检查点，超步边界上的底层状态快照，保存 <strong><code>Channel</code></strong> 的值、版本等信息。</td>
</tr>
<tr>
<td><strong><code>CheckpointMetadata</code></strong></td>
<td>与 <strong><code>Checkpoint</code></strong> 关联的元数据，如超步编号 <strong><code>step</code></strong>、父检查点 <strong><code>ID parents</code></strong> 等。</td>
</tr>
<tr>
<td><strong><code>Checkpointer</code></strong></td>
<td>检查点存储器，负责存储检查点、元数据、配置等信息。</td>
</tr>
<tr>
<td><strong><code>thread</code></strong></td>
<td>此处的线程不同于操作系统的线程，是指 <strong><code>LangGraph</code></strong> 中一条逻辑上的、可持久化的执行线，也可以理解为会话。一个会话可以包含多次调用，并在执行过程中生成多个检查点。因此，要将多次调用组织在一个会话中，必须为该 <strong><code>thread</code></strong> 配置 <strong><code>checkpointer</code></strong>。</td>
</tr>
<tr>
<td><strong><code>thread_id</code></strong></td>
<td><strong><code>thread</code></strong> 的唯一标识，用于区分不同的会话。复用同一个 <strong><code>thread_id</code></strong>，就表示多次调用共享同一条持久化执行线，也就是共享同一个会话历史。此处的 <strong><code>thread</code>/<code>thread_id</code></strong> 和 <strong><code>LangChain Agent</code></strong> 中提到的 <strong><code>thread</code>/<code>thread_id</code></strong> 是同一概念。</td>
</tr>
<tr>
<td><strong><code>checkpoint_ns</code></strong></td>
<td>检查点命名空间 <strong><code>namespace</code></strong>，用于区分父图和子图的 <strong><code>checkpoint</code></strong>。根图通常是空字符串 <strong><code>&quot;&quot;</code></strong>，子图会有自己的 <strong><code>namespace</code></strong>。这个字段在子图持久化时很重要，下文会有专门的篇幅讲解子图相关用法。</td>
</tr>
<tr>
<td><strong><code>checkpoint_id</code></strong></td>
<td><strong><code>Checkpoint</code></strong> 的唯一标识。</td>
</tr>
<tr>
<td><strong><code>StateSnapshot</code></strong></td>
<td>开发者通过 <strong><code>get_state()</code> / <code>get_state_history()</code></strong> 看到的状态快照对象。它不是原始 <strong><code>Checkpoint</code></strong>，而是 <strong><code>LangGraph</code></strong> 基于检查点和运行时信息封装出来的开发者视图，包含当前状态值、下一步待执行节点、任务、<strong><code>config</code></strong>、<strong><code>metadata</code></strong>、中断信息等内容。</td>
</tr>
</tbody>
</table>
<h4 id="6-1-2-3-小结">6.1.2.3. 小结</h4>
<p>本节只需要先记住：</p>
<blockquote>
<p><strong><code>Checkpointer</code></strong> 负责保存检查点</p>
<p><strong><code>thread_id</code></strong> 负责唯一标识会话</p>
<p><strong><code>checkpoint_id</code></strong> 负责定位某个具体历史状态</p>
<p><strong><code>StateSnapshot</code></strong> 是开发者查看检查点时看到的对象。</p>
</blockquote>
<h3 id="6-1-3-langgraph的可恢复执行">6.1.3. LangGraph的可恢复执行</h3>
<h4 id="6-1-3-1-什么是langgraph的可恢复执行">6.1.3.1. 什么是LangGraph的可恢复执行</h4>
<p><strong><code>LangGraph</code></strong> 的可恢复执行（<strong><code>Durable Execution</code></strong>）依托于其持久化机制（<strong><code>Persistence</code></strong>）。</p>
<p>可以简单理解为：</p>
<blockquote>
<p>持久化机制负责保存执行状态；
可恢复执行负责利用这些状态继续执行。</p>
</blockquote>
<p><strong><code>Persistence</code> 是基础，<code>Durable Execution</code> 是建立在 <code>Persistence</code> 之上的能力。</strong></p>
<p>在没有持久化机制时，一次图执行通常只存在于当前进程中。
如果程序退出、节点中断、人工审批暂停，无法恢复原来的执行进度。</p>
<p>而启用持久化之后，<strong><code>LangGraph</code></strong> 可以把执行过程中的状态保存为检查点。
后续只要使用相同的 <strong><code>thread_id</code></strong>，就可以回到会话对应的历史状态。</p>
<h4 id="6-1-3-2-使用场景">6.1.3.2. 使用场景</h4>
<p>从使用场景看，可恢复执行主要支持以下几类能力：</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>多轮会话</strong></td>
<td>同一个 <strong><code>thread_id</code></strong> 下的多次调用可以共享历史状态。</td>
</tr>
<tr>
<td><strong>中断恢复</strong></td>
<td>图执行到人工确认等节点时可以中断，之后再继续。</td>
</tr>
<tr>
<td><strong>失败恢复</strong></td>
<td>程序异常退出后，可以基于已经保存的检查点继续执行，避免重复计算。</td>
</tr>
<tr>
<td><strong><code>Time Travel</code></strong></td>
<td>可以回到某个历史检查点，<strong>重放</strong>或<strong>分叉</strong>执行。</td>
</tr>
</tbody>
</table>
<p>因此，<strong><code>LangGraph</code> 的可恢复执行是一组由持久化机制支撑的能力集合。</strong></p>
<h4 id="6-1-3-3-小结">6.1.3.3. 小结</h4>
<p>我们只需要知道</p>
<blockquote>
<p><strong><code>LangGraph</code> 会通过 <code>Checkpointer</code> 持久化保存图执行过程中产生的 <code>Checkpoint</code>，并通过 <code>thread_id</code> 找回同一会话的检查点历史，从而在已有状态基础上继续执行。</strong></p>
</blockquote>
<h2 id="6-2-启用可恢复执行">6.2. 启用可恢复执行</h2>
<h3 id="6-2-1-步骤">6.2.1. 步骤</h3>
<p><strong><code>LangGraph</code></strong> 内置了持久化机制，但对于本地运行的 <strong><code>Graph API</code></strong>，只有在编译图时传入 <strong><code>checkpointer</code></strong>，运行过程中的状态才会被记录下来，后续才能基于已有检查点在发生故障时恢复运行或进行检查点回溯。</p>
<p>启用可恢复执行通常需要两步：</p>
<ol>
<li>在编译图时传入检查点存储器对象 <strong><code>checkpointer</code></strong></li>
<li>在调用图时传递带有 <strong><code>thread_id</code></strong> 的配置对象</li>
</ol>
<p>底层的持久化机制会按照 <strong><code>thread_id</code></strong> 将运行时检查点记录在编译时传入的 <strong><code>checkpointer</code></strong> 中。</p>
<h3 id="6-2-2-检查点实现">6.2.2. 检查点实现</h3>
<p><strong><code>LangGraph</code></strong> 提供了检查点存储器基类：<strong><code>langgraph.checkpoint.base.BaseCheckpointSaver</code></strong></p>
<p>还提供了一系列检查点存储器实现，采用了不同的检查点后端，它们都继承了基类，整体作用是一致的：负责存取检查点，并记录节点执行的中间结果。</p>
<p>可以将 <strong>检查点后端</strong> 理解为：</p>
<blockquote>
<p>负责保存检查点数据的存储介质。</p>
</blockquote>
<p>检查点后端必须基于某个检查点实现，<strong>常见</strong>检查点实现和后端的对应关系如下</p>
<table>
<thead>
<tr>
<th>类名</th>
<th>包名</th>
<th>检查点后端</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>InMemorySaver</code></strong></td>
<td><strong><code>langgraph-checkpoint</code></strong></td>
<td>内存（<strong><code>In-memory</code></strong>）</td>
</tr>
<tr>
<td><strong><code>SqliteSaver</code></strong></td>
<td><strong><code>langgraph-checkpoint-sqlite</code></strong></td>
<td><strong><code>SQLite</code></strong></td>
</tr>
<tr>
<td><strong><code>PostgresSaver</code></strong></td>
<td><strong><code>langgraph-checkpoint-postgres</code></strong></td>
<td><strong><code>PostgreSQL</code></strong></td>
</tr>
<tr>
<td><strong><code>MongoDBSaver</code></strong></td>
<td><strong><code>langgraph-checkpoint-mongodb</code></strong></td>
<td><strong><code>MongoDB</code></strong></td>
</tr>
<tr>
<td><strong><code>RedisSaver</code></strong></td>
<td><strong><code>langgraph-checkpoint-redis</code></strong></td>
<td><strong><code>Redis</code></strong></td>
</tr>
</tbody>
</table>
<p>在这些实现中，<strong><code>InMemorySaver</code></strong> 更适合学习、调试和本地实验；<strong><code>SQLite</code></strong> 适合轻量级本地持久化；<strong><code>PostgreSQL</code></strong>、<strong><code>MongoDB</code></strong>、<strong><code>Redis</code></strong> 等数据库后端更适合需要跨进程、跨服务保存状态的场景。</p>
<h3 id="6-2-3-基于内存的检查点存储器">6.2.3. 基于内存的检查点存储器</h3>
<p><strong><code>LangGraph</code></strong> 提供了基于内存的检查点存储器 <strong><code>InMemorySaver</code></strong>。它将检查点数据保存在当前 <strong><code>Python</code></strong> 进程的内存中，进程结束则数据丢失，适合快速开发、调试。</p>
<p>在 <strong><code>Jupyter</code></strong> 场景下，只要 <strong><code>Jupyter</code></strong> 内核没有被重启，并且 <strong><code>InMemorySaver()</code></strong> 实例没有被重新创建，内存中的检查点数据就会继续存在。</p>
<p>反之，如果重新执行完整代码，导致 <strong><code>checkpointer = InMemorySaver()</code></strong> 被重新执行，那么之前保存在内存中的检查点也会被清空。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import MessagesState
from langgraph.checkpoint.memory import InMemorySaver
from langchain_deepseek import ChatDeepSeek
from langchain.messages import HumanMessage

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(MessagesState):
    output: str

def llm_node(state: OverAllState) -&gt; OverAllState:
    messages = state[&quot;messages&quot;]
    res = model.invoke(messages)
    return {
        &quot;messages&quot;: [res]
    }

def output_node(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;output&quot;: state[&quot;messages&quot;][-1].content
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;output_node&quot;, output_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, &quot;output_node&quot;)
builder.add_edge(&quot;output_node&quot;, END)

# 定义并在编译时传递 Checkpointer
checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

# 定义配置对象
config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;chapter_6_6-2-2&quot;}}
# 调用时传递
graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;你好，我是老王&quot;)]}, config=config)
graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;从现在开始，你是小王&quot;)]}, config=config)
res = graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;我是谁？你是谁？&quot;)]}, config=config)
print(res[&quot;output&quot;])

print('=' * 30, '-&gt; 完整消息列表 &lt;-', '=' * 30)
for msg in res[&quot;messages&quot;]:
    msg.pretty_print()
</code></pre>
<p><strong>输出如下</strong></p>
<pre><code>哎，老王，您这是考我呢？还是今天心情好，想跟我逗个闷子？

您问您是谁——那当然是我的老大哥，老王啊！平时有事儿招呼我，没架子，偶尔还爱指点我两句，我心里都记着。

至于我是谁——我，小王，您的得力助手，机灵踏实，随叫随到。您说往东，我绝不往西，主打一个“靠谱”。

怎么着，老王，是不是又想让我去办啥事儿了？您直说，我听着呢。😎
============================== -&gt; 完整消息列表 &lt;- ==============================
================================ Human Message =================================

你好，我是老王
================================== Ai Message ==================================

你好，老王！很高兴认识你。有什么我可以帮忙的吗？无论是聊天、解答问题还是需要一些建议，我都在这儿呢。😊
================================ Human Message =================================

从现在开始，你是小王
================================== Ai Message ==================================

好的，老王！我是小王。有啥事儿您尽管吩咐，我这儿随时听候差遣。咱是掏心窝子说话，还是聊点别的？您定！😄
================================ Human Message =================================

我是谁？你是谁？
================================== Ai Message ==================================

哎，老王，您这是考我呢？还是今天心情好，想跟我逗个闷子？

您问您是谁——那当然是我的老大哥，老王啊！平时有事儿招呼我，没架子，偶尔还爱指点我两句，我心里都记着。

至于我是谁——我，小王，您的得力助手，机灵踏实，随叫随到。您说往东，我绝不往西，主打一个“靠谱”。

怎么着，老王，是不是又想让我去办啥事儿了？您直说，我听着呢。😎
</code></pre>
<p>本例中，三次调用使用了相同的 <strong><code>thread_id</code></strong>，所以后两次调用能够读取前面已经保存的消息历史。最终模型可以知道“用户是老王”，也可以知道“自己被要求扮演小王”。</p>
<p>需要注意的是，如果重新执行完整代码，<strong><code>InMemorySaver()</code></strong> 实例会被重新创建，历史检查点会被清空。因此，消息列表不会继续累加，而是会基于新的空状态重新开始。</p>
<h3 id="6-2-4-基于持久化数据库的检查点存储器">6.2.4. 基于持久化数据库的检查点存储器</h3>
<p>本节将 <strong><code>PostgreSQL</code></strong> 作为检查点后端，对应的 <strong><code>Checkpointer</code></strong> 实现为 <strong><code>PostgresSaver</code></strong>。</p>
<p>和 <strong><code>InMemorySaver</code></strong> 不同，<strong><code>PostgresSaver</code></strong> 会将检查点保存到 <strong><code>PostgreSQL</code></strong> 数据库中。只要数据库中的记录没有被删除，即使 <strong><code>Python</code></strong> 程序结束、连接对象重建，历史检查点也仍然存在。</p>
<p>在学习 <strong><code>LangChain</code></strong> 时，我们已经介绍了 <strong><code>PostgreSQL</code></strong> 的部署和 <strong><code>PostgresSaver</code></strong> 的用法。此处直接给出案例，不再赘述。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import MessagesState
from langgraph.checkpoint.postgres import PostgresSaver
from langchain_deepseek import ChatDeepSeek
from langchain.messages import HumanMessage

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(MessagesState):
    output: str

def llm_node(state: OverAllState) -&gt; OverAllState:
    messages = state[&quot;messages&quot;]
    res = model.invoke(messages)
    return {
        &quot;messages&quot;: [res]
    }

def output_node(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;output&quot;: state[&quot;messages&quot;][-1].content
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;output_node&quot;, output_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, &quot;output_node&quot;)
builder.add_edge(&quot;output_node&quot;, END)

# 定义并在编译时传递 Checkpointer
DB_URL = &quot;postgresql://langgraph_user:123456@localhost:5432/langgraph_db?sslmode=disable&quot;
with PostgresSaver.from_conn_string(DB_URL) as checkpointer:
    # 示例中为了方便演示直接调用 setup()
    # 实际项目中通常建议把数据库初始化/迁移作为独立步骤处理
    checkpointer.setup()
    graph = builder.compile(checkpointer=checkpointer)

    # 定义配置对象
    config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;chapter_6_6.2.4&quot;}}
    # 调用时传递
    graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;你好，我是老王&quot;)]}, config=config)
    graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;从现在开始，你是小王&quot;)]}, config=config)
    res = graph.invoke({&quot;messages&quot;: [HumanMessage(&quot;我是谁？你是谁？&quot;)]}, config=config)
    print(res[&quot;output&quot;])

    print('=' * 30, '-&gt; 完整消息列表 &lt;-', '=' * 30)
    for msg in res[&quot;messages&quot;]:
        msg.pretty_print()
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code>好嘞，老王！既然你问了，那我正式回答：

**你是老王**——我今儿认的“老大哥”，江湖人称“行走的故事书”，爱唠嗑、有阅历，可能还藏着点神秘技能（比如钓鱼从不空军、下棋总赢隔壁老李？）

**我是小王**——刚上岗的AI小跟班，脑子灵光（但可能偶尔短路），腿脚勤快（指秒回消息），擅长接梗、查资料、瞎琢磨，主打一个“您开口，我跑腿”。

需要小王帮您办点啥？甭客气！🚀
============================== -&gt; 完整消息列表 &lt;- ==============================
================================ Human Message =================================

你好，我是老王
================================== Ai Message ==================================

老王你好！我是DeepSeek，很高兴认识你。有什么我可以帮忙的吗？无论是聊天、解答问题、还是需要一些建议，尽管开口，我随时在这儿！😊
================================ Human Message =================================

从现在开始，你是小王
================================== Ai Message ==================================

好的，老王！从现在开始，我就是小王啦。😄
有什么吩咐？无论唠嗑、干活还是出主意，随叫随到！
================================ Human Message =================================

我是谁？你是谁？
================================== Ai Message ==================================

好嘞，老王！既然你问了，那我正式回答：

**你是老王**——我今儿认的“老大哥”，江湖人称“行走的故事书”，爱唠嗑、有阅历，可能还藏着点神秘技能（比如钓鱼从不空军、下棋总赢隔壁老李？）

**我是小王**——刚上岗的AI小跟班，脑子灵光（但可能偶尔短路），腿脚勤快（指秒回消息），擅长接梗、查资料、瞎琢磨，主打一个“您开口，我跑腿”。

需要小王帮您办点啥？甭客气！🚀
</code></pre>
<p>如果多次运行这段代码，并且始终使用相同的 <strong><code>thread_id</code></strong>，那么历史消息会不断累加。这是因为检查点已经被保存到了 <strong><code>PostgreSQL</code></strong> 中，重新创建 <strong><code>Python</code></strong> 连接对象并不会清空数据库中的历史记录。</p>
<p>再次运行结果如下所示。</p>
<pre><code>（正了正脑门上虚拟工牌，一脸正经）

**您是老王**——我单方面认证的“茶馆街溜子荣誉会长”，上知股票涨跌，下懂馒头蒸法，口头禅是“这我熟啊！”

**我是小王**——您的AI影子跟班，刚给自己刻了个电子木鱼，每天默念三遍：
“老王说的都对，如果错了……那一定是我听岔了。” 😎
============================== -&gt; 完整消息列表 &lt;- ==============================
================================ Human Message =================================

你好，我是老王
================================== Ai Message ==================================

老王你好！我是DeepSeek，很高兴认识你。有什么我可以帮忙的吗？无论是聊天、解答问题、还是需要一些建议，尽管开口，我随时在这儿！😊
================================ Human Message =================================

从现在开始，你是小王
================================== Ai Message ==================================

好的，老王！从现在开始，我就是小王啦。😄
有什么吩咐？无论唠嗑、干活还是出主意，随叫随到！
================================ Human Message =================================

我是谁？你是谁？
================================== Ai Message ==================================

好嘞，老王！既然你问了，那我正式回答：

**你是老王**——我今儿认的“老大哥”，江湖人称“行走的故事书”，爱唠嗑、有阅历，可能还藏着点神秘技能（比如钓鱼从不空军、下棋总赢隔壁老李？）

**我是小王**——刚上岗的AI小跟班，脑子灵光（但可能偶尔短路），腿脚勤快（指秒回消息），擅长接梗、查资料、瞎琢磨，主打一个“您开口，我跑腿”。

需要小王帮您办点啥？甭客气！🚀
================================ Human Message =================================

你好，我是老王
================================== Ai Message ==================================

（拍大腿）哎哟，老王！这声招呼听着就是亲切！今天的茶泡上了没？隔壁李大爷刚还念叨你上回赢他的那盘残局呢——要不咱再研究研究？😏（小王搓手）
================================ Human Message =================================

从现在开始，你是小王
================================== Ai Message ==================================

（立正站好，手机屏闪了闪）得嘞，老王！小王这回把工牌焊脑门上了，您掀个眼皮瞅瞅——
（工牌晃晃悠悠浮出电子光字：「小王·AI茶馆分部·随叫随到版」）

有事您敲桌沿，三秒内准给您接话茬儿！ 🫖
================================ Human Message =================================

我是谁？你是谁？
================================== Ai Message ==================================

（正了正脑门上虚拟工牌，一脸正经）

**您是老王**——我单方面认证的“茶馆街溜子荣誉会长”，上知股票涨跌，下懂馒头蒸法，口头禅是“这我熟啊！”

**我是小王**——您的AI影子跟班，刚给自己刻了个电子木鱼，每天默念三遍：
“老王说的都对，如果错了……那一定是我听岔了。” 😎
</code></pre>
<p>因此，<strong><code>PostgresSaver</code></strong> 和 <strong><code>InMemorySaver</code></strong> 的关键区别在于：</p>
<ul>
<li><strong><code>InMemorySaver</code></strong>：状态保存在当前进程内存中，进程结束或对象重建后丢失。</li>
<li><strong><code>PostgresSaver</code></strong>：状态保存在 <strong><code>PostgreSQL</code></strong> 中，只要数据库记录存在，程序重启后仍可读取。</li>
</ul>
<p>所以，基于数据库的 <strong><code>Checkpointer</code></strong> 可靠性更高、更适合需要长期保存会话状态的场景。</p>
<h2 id="6-3-持久化模式">6.3. 持久化模式</h2>
<h3 id="6-3-1-概述">6.3.1. 概述</h3>
<p>启用 <strong><code>Checkpointer</code></strong> 后，<strong><code>LangGraph</code></strong> 会在执行过程中保存检查点。</p>
<p>检查点写入<strong>越及时</strong>，系统在异常中断后可恢复的状态<strong>越完整</strong>，<strong>容灾能力越强</strong>；但更及时的持久化通常也意味着<strong>更多中间写入</strong>，或<strong>额外的</strong>阻塞式<strong>等待</strong>，带来<strong>额外的性能开销和响应延迟</strong>（从调用者的角度考虑）。</p>
<p><strong><code>LangGraph</code></strong> 支持三种持久化模式，采用不同的检查点保存时机，在容灾能力和性能开销、响应时效性之间作取舍。</p>
<ul>
<li>
<p><strong><code>exit</code></strong>：退出模式。只在计算图正常结束、异常退出、被中断（如 <strong><code>Human-In-The-Loop</code></strong> 中断）时保存检查点。<strong>不能处理中途进程崩溃的场景</strong>。这种模式响应性能开销最小，但容灾能力最弱。</p>
</li>
<li>
<p><strong><code>async</code></strong>：异步模式。默认模式，顾名思义，检查点在后台异步写入。</p>
<p>它会在每个超步结束后写入<strong>完整检查点（主检查点）</strong>，并在图中任务执行完毕后记录<strong>中间结果</strong>。</p>
<p>和 <strong><code>exit</code></strong> 模式相比，增加了性能开销，但写入操作发生在后台，不会引入明显的响应延迟，同时提升了容灾能力。</p>
</li>
<li>
<p><strong><code>sync</code></strong>：同步模式。</p>
<p>和异步模式唯一的区别在于，<strong><code>LangGraph</code></strong> 会在进入下一个超步之前等待当前主检查点的写入任务完成。</p>
<p>它的容灾能力最强，但在 <strong><code>async</code></strong> 模式的基础上增加了响应延迟。</p>
</li>
</ul>
<p><strong>总结</strong></p>
<table>
<thead>
<tr>
<th>模式</th>
<th>写入时机</th>
<th>性能开销</th>
<th>响应延迟</th>
<th>容灾能力</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>exit</code></strong></td>
<td>运行退出时写入</td>
<td>低</td>
<td>最低</td>
<td>最弱</td>
</tr>
<tr>
<td><strong><code>async</code></strong></td>
<td>每个超步末尾写入主检查点，任务完成后写入中间结果，后台异步写入</td>
<td>高</td>
<td>较高</td>
<td>较强</td>
</tr>
<tr>
<td><strong><code>sync</code></strong></td>
<td>和 <strong><code>async</code></strong> 区别在于，进入下一个超步之前等待当前主检查点的写入任务完成</td>
<td>高</td>
<td>最高</td>
<td>最强</td>
</tr>
</tbody>
</table>
<h3 id="6-3-2-示例">6.3.2. 示例</h3>
<p>持久化模式影响的是检查点写入时机和故障恢复能力，通常<strong>不会影响</strong>正常情况下的<strong>业务输出</strong>。</p>
<p>也就是说，同一张图在正常执行完成时，使用 <strong><code>exit</code></strong>、<strong><code>async</code></strong> 或 <strong><code>sync</code></strong>，最终返回结果通常是一样的。真正的区别主要体现在<strong>异常中断</strong>或<strong>进程崩溃</strong>后<strong>恢复执行</strong>时。</p>
<blockquote>
<p><strong><code>durability</code> 控制的是检查点写入策略，不改变图本身的执行逻辑。</strong></p>
</blockquote>
<p>要真正看到不同模式的差异</p>
<ul>
<li>可以在学习<strong>使用场景</strong>后自行设计案例</li>
<li>或调试源码，观察不同模式下的写入时机</li>
</ul>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import MessagesState
from langgraph.checkpoint.memory import InMemorySaver
from langchain_deepseek import ChatDeepSeek
from langchain.messages import HumanMessage

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(MessagesState):
    output: str

def llm_node(state: OverAllState) -&gt; OverAllState:
    messages = state[&quot;messages&quot;]
    res = model.invoke(messages)
    return {
        &quot;messages&quot;: [res]
    }

def output_node(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;output&quot;: state[&quot;messages&quot;][-1].content
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;output_node&quot;, output_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, &quot;output_node&quot;)
builder.add_edge(&quot;output_node&quot;, END)

# 定义并在编译时传递 Checkpointer
checkpointer = InMemorySaver()
graph = builder.compile(checkpointer=checkpointer)

# 定义配置对象
config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;chapter_tmp&quot;}}
# 调用时传递
res=graph.invoke(
    {&quot;messages&quot;: [HumanMessage(&quot;你好&quot;)]},
    config=config,
    durability=&quot;async&quot; # sync / exit
)
print(res[&quot;output&quot;])

print('=' * 30, '-&gt; 完整消息列表 &lt;-', '=' * 30)
for msg in res[&quot;messages&quot;]:
    msg.pretty_print()

from IPython.display import display
display(graph)
</code></pre>
<p>上述代码中，可以通过修改 <strong><code>durability</code></strong> 参数切换持久化模式：</p>
<pre><code class="language-python">durability=&quot;exit&quot;
durability=&quot;async&quot;
durability=&quot;sync&quot;
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code>你好！很高兴见到你，有什么我可以帮助你的吗？😊
============================== -&gt; 完整消息列表 &lt;- ==============================
================================ Human Message =================================

你好
================================== Ai Message ==================================

你好！很高兴见到你，有什么我可以帮助你的吗？😊
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260609151649967.png" alt="image-20260609151649967" /></p>
<h2 id="6-4-查看历史检查点">6.4. 查看历史检查点</h2>
<p><strong><code>LangGraph</code></strong> 在配置检查点存储器后，会把同一个 <strong><code>thread_id</code></strong> 下的执行过程保存为一组检查点。通过这些检查点，我们可以查看图运行的中间状态，也可以为后续的检查点回溯（重放、分叉）和失败恢复做准备。</p>
<p>本节主要介绍两个常用方法：</p>
<ul>
<li><strong><code>graph.get_state_history(config)</code></strong>：查看指定会话的完整历史检查点。</li>
<li><strong><code>graph.get_state(config)</code></strong>：查看指定会话的<strong>最新</strong>检查点，或者查看某个<strong>指定</strong> <strong><code>checkpoint_id</code></strong> 对应的检查点。</li>
</ul>
<h3 id="6-4-1-查看完整历史检查点列表">6.4.1. 查看完整历史检查点列表</h3>
<h4 id="6-4-1-1-示例">6.4.1.1. 示例</h4>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from loguru import logger
from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    poem: str
    joke: str
    final_output: str

class InputState(TypedDict):
    topic: str

class OutputState(TypedDict):
    final_output: str

def node_poem(state: InputState) -&gt; OverAllState:
    logger.info(f&quot;node_poem 已执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = model.invoke([HumanMessage(f&quot;写一首关于 {topic} 的七言绝句&quot;)]).content

    return {
        &quot;poem&quot;: poem
    }

def node_joke(state: InputState) -&gt; OverAllState:
    logger.info(f&quot;node_joke 已执行&quot;)
    topic = state[&quot;topic&quot;]
    joke = model.invoke([HumanMessage(f&quot;写一个关于 {topic} 的笑话&quot;)]).content

    return {
        &quot;joke&quot;: joke
    }

def node_output(state: OverAllState) -&gt; OutputState:
    logger.info(&quot;node_output 已执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = state[&quot;poem&quot;]
    joke = state[&quot;joke&quot;]
    final_output = f&quot;关于 {topic} 的七言绝句：\n{poem}\n笑话：\n{joke}&quot;

    return {
        &quot;final_output&quot;: final_output
    }

builder = StateGraph(state_schema=OverAllState, input_schema=InputState, output_schema=OutputState)
builder.add_node(&quot;node_poem&quot;, node_poem)
builder.add_node(&quot;node_joke&quot;, node_joke)
builder.add_node(&quot;node_output&quot;, node_output)
builder.add_edge(START, &quot;node_poem&quot;)
builder.add_edge(START, &quot;node_joke&quot;)
builder.add_edge(&quot;node_poem&quot;, &quot;node_output&quot;)
builder.add_edge(&quot;node_joke&quot;, &quot;node_output&quot;)
builder.add_edge(&quot;node_output&quot;, END)

checkpointer = InMemorySaver()
config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
graph = builder.compile(checkpointer=checkpointer)
res = graph.invoke({&quot;topic&quot;: &quot;猫咪&quot;}, config=config)

from IPython.display import display
display(graph)

print('=' * 30, '-&gt; 运行结果 &lt;-', '=' * 30)
print(&quot;res: {}&quot;, res)

print('=' * 30, '-&gt; 历史检查点列表 &lt;-', '=' * 30)
history_checkpoints = list(graph.get_state_history(config=config))
print(history_checkpoints)
</code></pre>
<h4 id="6-4-1-2-运行结果">6.4.1.2. 运行结果</h4>
<ol>
<li>运行日志</li>
</ol>
<pre><code class="language-json">2026-06-09 17:24:10.266 | INFO     | __main__:node_joke:42 - node_joke 已执行
2026-06-09 17:24:10.268 | INFO     | __main__:node_poem:33 - node_poem 已执行
2026-06-09 17:24:14.177 | INFO     | __main__:node_output:51 - node_output 已执行
</code></pre>
<p>由于 <strong><code>node_poem</code></strong> 和 <strong><code>node_joke</code></strong> 都从 <strong><code>START</code></strong> 出发，并且彼此没有依赖关系，所以它们会被调度到同一个超步中执行。实际日志顺序可能不同，例如可能先打印 <strong><code>node_joke</code></strong>，也可能先打印 <strong><code>node_poem</code></strong>。</p>
<ol start="2">
<li>拓扑结构</li>
</ol>
<p><img src="/images/langgraph-study-notes/image-20260609173103640.png" alt="image-20260609173103640" /></p>
<ol start="3">
<li>最终结果和检查点列表</li>
</ol>
<pre><code class="language-json">============================== -&gt; 运行结果 &lt;- ==============================
res: {} {'final_output': '关于 猫咪 的七言绝句：\n《戏猫》\n狸奴酣卧小窗南，醉眼惺忪戏玉簪。\n忽跃雕檐追粉蝶，却衔明月入花龛。\n笑话：\n小猫问妈妈：“为什么人总说‘猫有九条命’？”  \n妈妈答：“因为人类怕我们死一次就够他们内疚一辈子。”  \n小猫追问：“那为什么狗只有一条命？”  \n妈妈叹气：“因为狗犯错靠装可怜，而我们靠记仇。”'}
============================== -&gt; 历史检查点列表 &lt;- ==============================
[
    StateSnapshot(
        values={
            &quot;topic&quot;: &quot;猫咪&quot;,
            &quot;poem&quot;: &quot;&quot;&quot;
                        《戏猫》
                狸奴酣卧小窗南，醉眼惺忪戏玉簪。
                忽跃雕檐追粉蝶，却衔明月入花龛。
                &quot;&quot;&quot;,
            &quot;joke&quot;: &quot;&quot;&quot;
                小猫问妈妈：“为什么人总说‘猫有九条命’？”
                妈妈答：“因为人类怕我们死一次就够他们内疚一辈子。”
                小猫追问：“那为什么狗只有一条命？”
                妈妈叹气：“因为狗犯错靠装可怜，而我们靠记仇。”
                &quot;&quot;&quot;,
            &quot;final_output&quot;: &quot;&quot;&quot;
                关于 猫咪 的七言绝句：
                《戏猫》
                狸奴酣卧小窗南，醉眼惺忪戏玉簪。
                忽跃雕檐追粉蝶，却衔明月入花龛。
                笑话：
                小猫问妈妈：“为什么人总说‘猫有九条命’？”
                妈妈答：“因为人类怕我们死一次就够他们内疚一辈子。”
                小猫追问：“那为什么狗只有一条命？”
                妈妈叹气：“因为狗犯错靠装可怜，而我们靠记仇。”
                &quot;&quot;&quot;
        },
        next=(),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f163e6a-4cc7-61bd-8002-bcd7f63c3238&quot;
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 2,
            &quot;parents&quot;: {}
        },
        created_at=&quot;2026-06-09T09:36:08.368575+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f163e6a-4cc3-6d96-8001-8314b5dbbfe3&quot;
            }
        },
        tasks=(),
        interrupts=()
    ),

    StateSnapshot(
        values={
            &quot;topic&quot;: &quot;猫咪&quot;,
            &quot;poem&quot;: &quot;&quot;&quot;
                        《戏猫》
                狸奴酣卧小窗南，醉眼惺忪戏玉簪。
                忽跃雕檐追粉蝶，却衔明月入花龛。
                &quot;&quot;&quot;,
            &quot;joke&quot;: &quot;&quot;&quot;
                小猫问妈妈：“为什么人总说‘猫有九条命’？”
                妈妈答：“因为人类怕我们死一次就够他们内疚一辈子。”
                小猫追问：“那为什么狗只有一条命？”
                妈妈叹气：“因为狗犯错靠装可怜，而我们靠记仇。”
                &quot;&quot;&quot;
        },
        next=(&quot;node_output&quot;,),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f163e6a-4cc3-6d96-8001-8314b5dbbfe3&quot;
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 1,
            &quot;parents&quot;: {}
        },
        created_at=&quot;2026-06-09T09:36:08.367236+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f163e6a-393f-6082-8000-b9c0708bf03d&quot;
            }
        },
        tasks=(
            PregelTask(
                id=&quot;91d81ad6-d2fb-ebd7-2dd1-8e08c2be5a07&quot;,
                name=&quot;node_output&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;node_output&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;final_output&quot;: &quot;&quot;&quot;
                        关于 猫咪 的七言绝句：
                        《戏猫》
                        狸奴酣卧小窗南，醉眼惺忪戏玉簪。
                        忽跃雕檐追粉蝶，却衔明月入花龛。
                        笑话：
                        小猫问妈妈：“为什么人总说‘猫有九条命’？”
                        妈妈答：“因为人类怕我们死一次就够他们内疚一辈子。”
                        小猫追问：“那为什么狗只有一条命？”
                        妈妈叹气：“因为狗犯错靠装可怜，而我们靠记仇。”
                        &quot;&quot;&quot;
                }
            ),
        ),
        interrupts=()
    ),

    StateSnapshot(
        values={
            &quot;topic&quot;: &quot;猫咪&quot;
        },
        next=(&quot;node_poem&quot;, &quot;node_joke&quot;),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f163e6a-393f-6082-8000-b9c0708bf03d&quot;
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 0,
            &quot;parents&quot;: {}
        },
        created_at=&quot;2026-06-09T09:36:06.320542+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f163e6a-393d-6b9a-bfff-180e30c1d3bc&quot;
            }
        },
        tasks=(
            PregelTask(
                id=&quot;80d8974c-3450-6faa-8b06-dcf8787d74fc&quot;,
                name=&quot;node_poem&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;node_poem&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;poem&quot;: &quot;&quot;&quot;
                            《戏猫》
                    狸奴酣卧小窗南，醉眼惺忪戏玉簪。
                    忽跃雕檐追粉蝶，却衔明月入花龛。
                    &quot;&quot;&quot;
                }
            ),
            PregelTask(
                id=&quot;2ca46996-dfc6-6de3-3662-cf1fe41bf987&quot;,
                name=&quot;node_joke&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;node_joke&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;joke&quot;: &quot;&quot;&quot;
                    小猫问妈妈：“为什么人总说‘猫有九条命’？”
                    妈妈答：“因为人类怕我们死一次就够他们内疚一辈子。”
                    小猫追问：“那为什么狗只有一条命？”
                    妈妈叹气：“因为狗犯错靠装可怜，而我们靠记仇。”
                    &quot;&quot;&quot;
                }
            ),
        ),
        interrupts=()
    ),

    StateSnapshot(
        values={},
        next=(&quot;__start__&quot;,),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f163e6a-393d-6b9a-bfff-180e30c1d3bc&quot;
            }
        },
        metadata={
            &quot;source&quot;: &quot;input&quot;,
            &quot;step&quot;: -1,
            &quot;parents&quot;: {}
        },
        created_at=&quot;2026-06-09T09:36:06.320009+00:00&quot;,
        parent_config=None,
        tasks=(
            PregelTask(
                id=&quot;3aa3bc58-3567-5832-d823-4373a9ab94e5&quot;,
                name=&quot;__start__&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;__start__&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;topic&quot;: &quot;猫咪&quot;
                }
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<h4 id="6-4-1-3-分析">6.4.1.3. 分析</h4>
<h5 id="6-4-1-3-1-statesnapshot">6.4.1.3.1. StateSnapshot</h5>
<p><strong><code>get_state_history()</code></strong> 返回的是一个历史检查点迭代器。将其转换为列表后，可以看到一组 <strong><code>StateSnapshot</code></strong> 对象。</p>
<p><strong><code>StateSnapshot</code></strong> 是检查点的开发者视图，字段含义如下</p>
<ul>
<li>
<p><strong><code>values</code></strong>：当前检查点的状态值</p>
</li>
<li>
<p><strong><code>next</code></strong>：从该检查点继续执行时，下一步将要执行的节点，即归属于下一个超步的节点</p>
</li>
<li>
<p><strong><code>config</code></strong>：当前检查点配置。常见结构如下：</p>
<pre><code class="language-json">{
    &quot;configurable&quot;: {
        &quot;thread_id&quot;: &quot;...&quot;,
        &quot;checkpoint_ns&quot;: &quot;...&quot;,
        &quot;checkpoint_id&quot;: &quot;...&quot;
    }
}
</code></pre>
<p>其中</p>
<ul>
<li>
<p><strong><code>configurable</code></strong>：用于记录检查点的可配置信息，可能包含用户自定义字段，它一定包含以下三个字段：</p>
<ul>
<li><strong><code>thread_id</code></strong>：会话唯一标识</li>
<li><strong><code>checkpoint_ns</code></strong>：检查点命名空间。根图的命名空间通常为空字符串 <strong><code>&quot;&quot;</code></strong>；子图会使用非空命名空间。</li>
<li><strong><code>checkpoint_id</code></strong>：检查点唯一标识</li>
</ul>
<p>这三个字段可以唯一标识一条检查点记录，在大多数检查点存储器实现中，写入检查点后端时都会将它们作为检查点的<strong>唯一键</strong></p>
</li>
</ul>
</li>
<li>
<p><strong><code>metadata</code></strong>：检查点元数据，本节只需要关注 <strong><code>step</code></strong> 字段，后者是当前检查点对应的超步编号</p>
</li>
<li>
<p><strong><code>created_at</code></strong>：检查点创建时间</p>
</li>
<li>
<p><strong><code>parent_config</code></strong>：父检查点、即上一个检查点的配置</p>
</li>
<li>
<p><strong><code>tasks</code></strong>：当前检查点关联的待执行任务信息，元素类型通常是 <strong><code>PregelTask</code></strong>。</p>
<p><strong><code>tasks</code></strong> 通常和 <strong><code>next</code></strong> 对应，表示从当前检查点继续执行时，下一步将要运行的任务。</p>
<p><strong>需要注意的是</strong>，<strong><code>tasks</code></strong> 中还可能包含这些任务已经成功（<strong><code>result</code></strong>）或失败（<strong><code>error</code></strong>）的任务记录，这是在下一个超步执行过程中记录的中间结果。在失败恢复场景中，可以避免重复执行已成功节点。</p>
</li>
<li>
<p><strong><code>interrupts</code></strong>：当前图的中断信息，中断机制详见下文</p>
</li>
</ul>
<h5 id="6-4-1-3-2-检查点主要信息说明">6.4.1.3.2. 检查点主要信息说明</h5>
<p>本例中检查点按照如下顺序返回</p>
<pre><code class="language-python">Step 2 → Step 1 → Step 0 → Step -1
</code></pre>
<p>即</p>
<pre><code class="language-python">最新检查点 → 最旧检查点
</code></pre>
<p>其中：</p>
<ol>
<li>
<p><strong><code>step=-1</code></strong></p>
<p>这是输入检查点，<strong><code>metadata[&quot;source&quot;] == &quot;input&quot;</code></strong>，这表示该检查点是在运行时初始化时被记录的，尚未进入图计算循环。</p>
<p>此时用户输入还没有真正变成普通业务节点可消费的状态，因此</p>
<pre><code class="language-python">values={}
</code></pre>
<p>从这个检查点继续执行，下一步是内部启动节点 <strong><code>__start__</code></strong>，因此：</p>
<pre><code class="language-python">next=(&quot;__start__&quot;,)
</code></pre>
</li>
<li>
<p><strong><code>step=0</code></strong></p>
<p>这是图计算循环的第一个检查点，<strong><code>metadata[&quot;source&quot;] == &quot;loop&quot;</code></strong>，这表示检查点是在图计算循环中被写入的。</p>
<p>此时，<strong><code>__start__</code></strong> 已经把用户输入写入状态，所以 <strong><code>values</code></strong> 中已经可以看到状态信息，如下：</p>
<pre><code class="language-python">values={
    &quot;topic&quot;: &quot;猫咪&quot;
}
</code></pre>
<p>从这个检查点继续执行，<strong><code>node_poem</code></strong> 和 <strong><code>node_joke</code></strong> 将在下一个超步执行，因此：</p>
<pre><code class="language-python">next=(&quot;node_poem&quot;, &quot;node_joke&quot;)
</code></pre>
</li>
<li>
<p><strong><code>step=1</code></strong></p>
<p>这是图计算循环的第二个检查点，同样 <strong><code>metadata[&quot;source&quot;] == &quot;loop&quot;</code></strong>。</p>
<p>同时，它对应第一个执行图节点计算逻辑的超步。</p>
<p>此时，<strong><code>node_poem</code></strong> 和 <strong><code>node_joke</code></strong> 的输出已经到状态中，因此：</p>
<pre><code class="language-python">values={
    &quot;topic&quot;: &quot;猫咪&quot;,
    &quot;poem&quot;: &quot;...&quot;,
    &quot;joke&quot;: &quot;...&quot;
}
</code></pre>
<p>从这个检查点开始，下游的汇总节点 <strong><code>node_output</code></strong> 满足执行条件，因此：</p>
<pre><code class="language-python">next=(&quot;node_output&quot;,)
</code></pre>
</li>
<li>
<p><strong><code>step=2</code></strong></p>
<p>这是图计算循环的第三个检查点，同样 <strong><code>metadata[&quot;source&quot;] == &quot;loop&quot;</code></strong></p>
<p>此时，<strong><code>node_output</code></strong> 已经执行完成，最终输出也已经写入状态，因此：</p>
<pre><code class="language-python">values={
    &quot;topic&quot;: &quot;猫咪&quot;,
    &quot;poem&quot;: &quot;...&quot;,
    &quot;joke&quot;: &quot;...&quot;,
    &quot;final_output&quot;: &quot;...&quot;
}
</code></pre>
<p>此时，图已经执行结束，所以：</p>
<pre><code class="language-python">next=()
</code></pre>
</li>
</ol>
<h5 id="6-4-1-3-3-tasks-字段说明">6.4.1.3.3. <code>tasks</code>字段说明</h5>
<p><strong><code>tasks</code></strong> 是一个元组，其中的每个元素都是一个 <strong><code>PregelTask</code></strong> 实例</p>
<p><strong><code>tasks</code></strong> 表示：如果从当前检查点继续执行，下一步要运行哪些任务。比如在 <strong><code>step=0</code></strong> 的检查点中：</p>
<pre><code class="language-python">next=(&quot;node_poem&quot;, &quot;node_joke&quot;)
tasks=(
    PregelTask(name=&quot;node_poem&quot;, ...),
    PregelTask(name=&quot;node_joke&quot;, ...),
)
</code></pre>
<p>这表示从该检查点继续执行时，下一步会运行 <strong><code>node_poem</code></strong> 和 <strong><code>node_joke</code></strong> 节点，底层执行的是它们的同名任务。</p>
<p>不过，查看历史检查点时，<strong><code>PregelTask</code></strong> 实例的 <strong><code>result</code></strong> 字段可能非空，如：</p>
<pre><code class="language-python">PregelTask(
    name=&quot;node_poem&quot;,
    result={
        &quot;poem&quot;: &quot;...&quot;
    }
)
</code></pre>
<p>这看起来像是“上一个检查点中提前保存了下一个超步的执行结果”。实际上：</p>
<ul>
<li>
<p>完整的检查点（主检查点）是在超步末尾保存的</p>
</li>
<li>
<p>但在一个超步内部，<strong><code>LangGraph</code></strong> 还会记录节点级别的计算结果</p>
</li>
<li>
<p><strong><code>checkpoint[&quot;id&quot;]</code></strong> 会在保存主检查点前由 <strong><code>create_checkpoint()</code></strong> 推进为新的 <strong><code>ID</code></strong>，然后用这个 <strong><code>ID</code></strong> 写入主检查点（假设当前超步为 <strong><code>S1</code></strong>）。下一轮（超步为 <strong><code>S2</code></strong>）计算循环中，任务执行完毕后的中间结果会用相同的 <strong><code>ID</code></strong> 写入。</p>
<p>因此，下一轮（超步为 <strong><code>S2</code></strong>）任务的计算结果绑定到了“当前超步（超步为 <strong><code>S1</code></strong>）的主检查点”。</p>
</li>
<li>
<p>从检查点后端检索时，会按照 <strong><code>checkpoint_id</code></strong> 将主检查点和中间结果组织起来</p>
</li>
<li>
<p>因此，检索到的历史检查点列表中，主检查点（超步为 <strong><code>S1</code></strong>）和下一个超步（超步为 <strong><code>S2</code></strong>）的中间结果会被放在同一个 <strong><code>StateSnapshot</code></strong> 中</p>
</li>
<li>
<p>如果同一个超步里有多个并行任务，其中部分任务已经成功，另一个任务失败，那么成功任务的结果可以被保存下来；</p>
</li>
<li>
<p>后续恢复时，<strong><code>LangGraph</code></strong> 可以复用这些已成功任务的结果，避免重复执行已经成功的节点。</p>
</li>
</ul>
<p>在 <strong><code>6.5.3.</code> 失败恢复</strong> 一节，我们将看到某个超步中部分任务执行成功、部分任务失败导致计算异常终止，恢复运行时已成功任务未被重新执行。</p>
<h3 id="6-4-2-查看最新检查点">6.4.2. 查看最新检查点</h3>
<p>如果只想查看当前线程的最新检查点，可以使用：</p>
<pre><code class="language-python">latest_history_checkpoint = graph.get_state(config=config)
print(latest_history_checkpoint)
</code></pre>
<p><strong>完整示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from loguru import logger
from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    poem: str
    joke: str
    final_output: str

class InputState(TypedDict):
    topic: str

class OutputState(TypedDict):
    final_output: str

def node_poem(state: InputState) -&gt; OverAllState:
    logger.info(f&quot;node_poem 已执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = model.invoke([HumanMessage(f&quot;写一首关于 {topic} 的七言绝句，不要赏析，只给诗句即可&quot;)]).content

    return {
        &quot;poem&quot;: poem
    }

def node_joke(state: InputState) -&gt; OverAllState:
    logger.info(f&quot;node_joke 已执行&quot;)
    topic = state[&quot;topic&quot;]
    joke = model.invoke([HumanMessage(f&quot;写一个关于 {topic} 的笑话，尽可能简单，不超过一百字&quot;)]).content

    return {
        &quot;joke&quot;: joke
    }

def node_output(state: OverAllState) -&gt; OutputState:
    logger.info(&quot;node_output 已执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = state[&quot;poem&quot;]
    joke = state[&quot;joke&quot;]
    final_output = f&quot;关于 {topic} 的七言绝句：\n{poem}\n笑话：\n{joke}&quot;

    return {
        &quot;final_output&quot;: final_output
    }

builder = StateGraph(state_schema=OverAllState, input_schema=InputState, output_schema=OutputState)
builder.add_node(&quot;node_poem&quot;, node_poem)
builder.add_node(&quot;node_joke&quot;, node_joke)
builder.add_node(&quot;node_output&quot;, node_output)
builder.add_edge(START, &quot;node_poem&quot;)
builder.add_edge(START, &quot;node_joke&quot;)
builder.add_edge(&quot;node_poem&quot;, &quot;node_output&quot;)
builder.add_edge(&quot;node_joke&quot;, &quot;node_output&quot;)
builder.add_edge(&quot;node_output&quot;, END)

checkpointer = InMemorySaver()
config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
graph = builder.compile(checkpointer=checkpointer)
res = graph.invoke({&quot;topic&quot;: &quot;猫咪&quot;}, config=config)

from IPython.display import display
display(graph)

print('=' * 30, '-&gt; 运行结果 &lt;-', '=' * 30)
print(&quot;res: {}&quot;, res)

print('=' * 30, '-&gt; 最新历史检查点 &lt;-', '=' * 30)
latest_history_checkpoint = graph.get_state(config=config)
print(latest_history_checkpoint)
</code></pre>
<p><strong>运行结果如下</strong></p>
<ol>
<li>
<p>运行日志</p>
<pre><code class="language-json">2026-06-10 11:23:59.610 | INFO     | __main__:node_poem:33 - node_poem 已执行
2026-06-10 11:23:59.611 | INFO     | __main__:node_joke:42 - node_joke 已执行
2026-06-10 11:24:01.424 | INFO     | __main__:node_output:51 - node_output 已执行
</code></pre>
</li>
<li>
<p>拓扑结构</p>
<p><img src="/images/langgraph-study-notes/image-20260610112506158.png" alt="image-20260610112506158" /></p>
</li>
<li>
<p>运行结果及检查点信息</p>
<pre><code class="language-json">============================== -&gt; 运行结果 &lt;- ==============================
res: {} {'final_output': '关于 猫咪 的七言绝句：\n《戏题狸奴》\n昼眠锦褥夜巡檐，碧眼圆瞳映月纤。\n捕鼠本非真本领，得鱼时复近妆奁。\n笑话：\n小猫咪第一次照镜子，看到里面的自己，吓得炸毛：“你是谁？！” 它绕到镜子后面找了一圈，没找到，又回来对着镜子哈气。 最后它恍然大悟，对主人说：“我明白了，那是个隐藏的摄像头！”'}
============================== -&gt; 最新历史检查点 &lt;- ==============================
StateSnapshot(
    values={
        &quot;topic&quot;: &quot;猫咪&quot;,
        &quot;poem&quot;: (
            &quot;《戏题狸奴》\n&quot;
            &quot;昼眠锦褥夜巡檐，碧眼圆瞳映月纤。\n&quot;
            &quot;捕鼠本非真本领，得鱼时复近妆奁。&quot;
        ),
        &quot;joke&quot;: (
            &quot;小猫咪第一次照镜子，看到里面的自己，吓得炸毛：“你是谁？！” &quot;
            &quot;它绕到镜子后面找了一圈，没找到，又回来对着镜子哈气。 &quot;
            &quot;最后它恍然大悟，对主人说：“我明白了，那是个隐藏的摄像头！”&quot;
        ),
        &quot;final_output&quot;: (
            &quot;关于 猫咪 的七言绝句：\n&quot;
            &quot;《戏题狸奴》\n&quot;
            &quot;昼眠锦褥夜巡檐，碧眼圆瞳映月纤。\n&quot;
            &quot;捕鼠本非真本领，得鱼时复近妆奁。\n&quot;
            &quot;笑话：\n&quot;
            &quot;小猫咪第一次照镜子，看到里面的自己，吓得炸毛：“你是谁？！” &quot;
            &quot;它绕到镜子后面找了一圈，没找到，又回来对着镜子哈气。 &quot;
            &quot;最后它恍然大悟，对主人说：“我明白了，那是个隐藏的摄像头！”&quot;
        ),
    },
    next=(),
    config={
        &quot;configurable&quot;: {
            &quot;thread_id&quot;: &quot;123&quot;,
            &quot;checkpoint_ns&quot;: &quot;&quot;,
            &quot;checkpoint_id&quot;: &quot;1f1647bd-3512-631a-8002-4e48b5ecda6d&quot;,
        }
    },
    metadata={
        &quot;source&quot;: &quot;loop&quot;,
        &quot;step&quot;: 2,
        &quot;parents&quot;: {},
    },
    created_at=&quot;2026-06-10T03:24:01.426084+00:00&quot;,
    parent_config={
        &quot;configurable&quot;: {
            &quot;thread_id&quot;: &quot;123&quot;,
            &quot;checkpoint_ns&quot;: &quot;&quot;,
            &quot;checkpoint_id&quot;: &quot;1f1647bd-350d-62c7-8001-34cd7fa012ed&quot;,
        }
    },
    tasks=(),
    interrupts=(),
)
</code></pre>
<p><strong><code>graph.get_state(config=config)</code></strong> 只返回当前会话的最新检查点。</p>
<p>在本例中，图已经执行完成，所以最新检查点具有以下特征：</p>
<pre><code class="language-python">next=()
tasks=()
</code></pre>
<p>这表示没有后续节点需要继续执行。</p>
</li>
</ol>
<h3 id="6-4-3-根据id查看指定检查点">6.4.3. 根据ID查看指定检查点</h3>
<p>如果希望查看某个历史检查点，可以在 <strong><code>configurable</code></strong> 中额外传入 <strong><code>checkpoint_id</code></strong>：</p>
<pre><code class="language-python">target_config = {
    &quot;configurable&quot;: {
        &quot;thread_id&quot;: &quot;123&quot;,
        &quot;checkpoint_id&quot;: &quot;某个历史 checkpoint_id&quot;
    }
}

snapshot = graph.get_state(config=target_config)
print(snapshot)
</code></pre>
<p>这样返回的就不是最新检查点，而是指定 <strong><code>checkpoint_id</code></strong> 对应的历史检查点。</p>
<h4 id="6-4-3-1-获取历史检查点列表">6.4.3.1. 获取历史检查点列表</h4>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from loguru import logger
from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    poem: str
    joke: str
    final_output: str

class InputState(TypedDict):
    topic: str

class OutputState(TypedDict):
    final_output: str

def node_poem(state: InputState) -&gt; OverAllState:
    logger.info(f&quot;node_poem 已执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = model.invoke([HumanMessage(f&quot;写一首关于 {topic} 的七言绝句，不要赏析，只给诗句即可&quot;)]).content

    return {
        &quot;poem&quot;: poem
    }

def node_joke(state: InputState) -&gt; OverAllState:
    logger.info(f&quot;node_joke 已执行&quot;)
    topic = state[&quot;topic&quot;]
    joke = model.invoke([HumanMessage(f&quot;写一个关于 {topic} 的笑话，尽可能简单，不超过一百字&quot;)]).content

    return {
        &quot;joke&quot;: joke
    }

def node_output(state: OverAllState) -&gt; OutputState:
    logger.info(&quot;node_output 已执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = state[&quot;poem&quot;]
    joke = state[&quot;joke&quot;]
    final_output = f&quot;关于 {topic} 的七言绝句：\n{poem}\n笑话：\n{joke}&quot;

    return {
        &quot;final_output&quot;: final_output
    }

builder = StateGraph(state_schema=OverAllState, input_schema=InputState, output_schema=OutputState)
builder.add_node(&quot;node_poem&quot;, node_poem)
builder.add_node(&quot;node_joke&quot;, node_joke)
builder.add_node(&quot;node_output&quot;, node_output)
builder.add_edge(START, &quot;node_poem&quot;)
builder.add_edge(START, &quot;node_joke&quot;)
builder.add_edge(&quot;node_poem&quot;, &quot;node_output&quot;)
builder.add_edge(&quot;node_joke&quot;, &quot;node_output&quot;)
builder.add_edge(&quot;node_output&quot;, END)

checkpointer = InMemorySaver()
config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
graph = builder.compile(checkpointer=checkpointer)
res = graph.invoke({&quot;topic&quot;: &quot;猫咪&quot;}, config=config)

from IPython.display import display
display(graph)

print('=' * 30, '-&gt; 运行结果 &lt;-', '=' * 30)
print(&quot;res: {}&quot;, res)

print('=' * 30, '-&gt; 历史检查点列表 &lt;-', '=' * 30)
history_checkpoints = list(graph.get_state_history(config=config))
print(history_checkpoints)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">2026-06-11 18:28:29.126 | INFO     | __main__:node_poem:33 - node_poem 已执行
2026-06-11 18:28:29.128 | INFO     | __main__:node_joke:42 - node_joke 已执行
2026-06-11 18:28:31.021 | INFO     | __main__:node_output:51 - node_output 已执行
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260611183125275.png" alt="image-20260611183125275" /></p>
<pre><code class="language-json">============================== -&gt; 运行结果 &lt;- ==============================
res: {} {'final_output': '关于 猫咪 的七言绝句：\n《戏猫》\n狸奴日午卧青毡，蝶影翩然忽跃前。\n捕得飞花轻似梦，独摇银尾弄春烟。\n笑话：\n猫咪走进咖啡店，点了一杯牛奶。  \n店员问：“要不要加糖？”  \n猫咪摇头：“不用，我最近在减肥。”  \n店员看了看它圆滚滚的肚子：“真的吗？”  \n猫咪叹了口气：“唉，都是猫粮的错。”'}
============================== -&gt; 历史检查点列表 &lt;- ==============================
[
    StateSnapshot(
        values={
            &quot;topic&quot;: &quot;猫咪&quot;,
            &quot;poem&quot;: (
                &quot;《戏猫》\n&quot;
                &quot;狸奴日午卧青毡，蝶影翩然忽跃前。\n&quot;
                &quot;捕得飞花轻似梦，独摇银尾弄春烟。&quot;
            ),
            &quot;joke&quot;: (
                &quot;猫咪走进咖啡店，点了一杯牛奶。  \n&quot;
                &quot;店员问：“要不要加糖？”  \n&quot;
                &quot;猫咪摇头：“不用，我最近在减肥。”  \n&quot;
                &quot;店员看了看它圆滚滚的肚子：“真的吗？”  \n&quot;
                &quot;猫咪叹了口气：“唉，都是猫粮的错。”&quot;
            ),
            &quot;final_output&quot;: (
                &quot;关于 猫咪 的七言绝句：\n&quot;
                &quot;《戏猫》\n&quot;
                &quot;狸奴日午卧青毡，蝶影翩然忽跃前。\n&quot;
                &quot;捕得飞花轻似梦，独摇银尾弄春烟。\n&quot;
                &quot;笑话：\n&quot;
                &quot;猫咪走进咖啡店，点了一杯牛奶。  \n&quot;
                &quot;店员问：“要不要加糖？”  \n&quot;
                &quot;猫咪摇头：“不用，我最近在减肥。”  \n&quot;
                &quot;店员看了看它圆滚滚的肚子：“真的吗？”  \n&quot;
                &quot;猫咪叹了口气：“唉，都是猫粮的错。”&quot;
            ),
        },
        next=(),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f165804-acae-618c-8002-5a8b9e940205&quot;,
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 2,
            &quot;parents&quot;: {},
        },
        created_at=&quot;2026-06-11T10:28:31.022523+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f165804-acab-66b3-8001-d74912e4cb07&quot;,
            }
        },
        tasks=(),
        interrupts=(),
    ),

    StateSnapshot(
        values={
            &quot;topic&quot;: &quot;猫咪&quot;,
            &quot;poem&quot;: (
                &quot;《戏猫》\n&quot;
                &quot;狸奴日午卧青毡，蝶影翩然忽跃前。\n&quot;
                &quot;捕得飞花轻似梦，独摇银尾弄春烟。&quot;
            ),
            &quot;joke&quot;: (
                &quot;猫咪走进咖啡店，点了一杯牛奶。  \n&quot;
                &quot;店员问：“要不要加糖？”  \n&quot;
                &quot;猫咪摇头：“不用，我最近在减肥。”  \n&quot;
                &quot;店员看了看它圆滚滚的肚子：“真的吗？”  \n&quot;
                &quot;猫咪叹了口气：“唉，都是猫粮的错。”&quot;
            ),
        },
        next=(&quot;node_output&quot;,),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f165804-acab-66b3-8001-d74912e4cb07&quot;,
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 1,
            &quot;parents&quot;: {},
        },
        created_at=&quot;2026-06-11T10:28:31.021422+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f165804-9a92-67bc-8000-dd5ab888e9d5&quot;,
            }
        },
        tasks=(
            PregelTask(
                id=&quot;66dedbf9-3fbb-c539-5f4e-0e3f604a9c56&quot;,
                name=&quot;node_output&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;node_output&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;final_output&quot;: (
                        &quot;关于 猫咪 的七言绝句：\n&quot;
                        &quot;《戏猫》\n&quot;
                        &quot;狸奴日午卧青毡，蝶影翩然忽跃前。\n&quot;
                        &quot;捕得飞花轻似梦，独摇银尾弄春烟。\n&quot;
                        &quot;笑话：\n&quot;
                        &quot;猫咪走进咖啡店，点了一杯牛奶。  \n&quot;
                        &quot;店员问：“要不要加糖？”  \n&quot;
                        &quot;猫咪摇头：“不用，我最近在减肥。”  \n&quot;
                        &quot;店员看了看它圆滚滚的肚子：“真的吗？”  \n&quot;
                        &quot;猫咪叹了口气：“唉，都是猫粮的错。”&quot;
                    )
                },
            ),
        ),
        interrupts=(),
    ),

    StateSnapshot(
        values={
            &quot;topic&quot;: &quot;猫咪&quot;,
        },
        next=(&quot;node_poem&quot;, &quot;node_joke&quot;),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f165804-9a92-67bc-8000-dd5ab888e9d5&quot;,
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 0,
            &quot;parents&quot;: {},
        },
        created_at=&quot;2026-06-11T10:28:29.123773+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f165804-9a90-6db5-bfff-d0eb857db9d8&quot;,
            }
        },
        tasks=(
            PregelTask(
                id=&quot;8b59cf5a-16b4-c734-41c8-56ac9b7614c9&quot;,
                name=&quot;node_poem&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;node_poem&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;poem&quot;: (
                        &quot;《戏猫》\n&quot;
                        &quot;狸奴日午卧青毡，蝶影翩然忽跃前。\n&quot;
                        &quot;捕得飞花轻似梦，独摇银尾弄春烟。&quot;
                    )
                },
            ),
            PregelTask(
                id=&quot;388c08f3-e341-3c76-cfc4-a9825ec97353&quot;,
                name=&quot;node_joke&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;node_joke&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;joke&quot;: (
                        &quot;猫咪走进咖啡店，点了一杯牛奶。  \n&quot;
                        &quot;店员问：“要不要加糖？”  \n&quot;
                        &quot;猫咪摇头：“不用，我最近在减肥。”  \n&quot;
                        &quot;店员看了看它圆滚滚的肚子：“真的吗？”  \n&quot;
                        &quot;猫咪叹了口气：“唉，都是猫粮的错。”&quot;
                    )
                },
            ),
        ),
        interrupts=(),
    ),

    StateSnapshot(
        values={},
        next=(&quot;__start__&quot;,),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f165804-9a90-6db5-bfff-d0eb857db9d8&quot;,
            }
        },
        metadata={
            &quot;source&quot;: &quot;input&quot;,
            &quot;step&quot;: -1,
            &quot;parents&quot;: {},
        },
        created_at=&quot;2026-06-11T10:28:29.123104+00:00&quot;,
        parent_config=None,
        tasks=(
            PregelTask(
                id=&quot;63a24d9c-ea6d-0420-4b5a-b978c6eaeb90&quot;,
                name=&quot;__start__&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;__start__&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;topic&quot;: &quot;猫咪&quot;,
                },
            ),
        ),
        interrupts=(),
    ),
]
</code></pre>
<h4 id="6-4-3-2-根据id查看特定检查点">6.4.3.2. 根据ID查看特定检查点</h4>
<p>获取历史检查点列表后，可以从某个历史快照中取出它的 <strong><code>checkpoint_id</code></strong>，然后调用 <strong><code>graph.get_state()</code></strong> 查看该检查点。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">print('=' * 30, '-&gt; 根据ID查看特定检查点 &lt;-', '=' * 30)
checkpointe_id = history_checkpoints[3].config[&quot;configurable&quot;][&quot;checkpoint_id&quot;]
print(f&quot;checkpointe_id: {checkpointe_id}&quot;)

target_config = {
    &quot;configurable&quot;: {
        &quot;thread_id&quot;: config[&quot;configurable&quot;][&quot;thread_id&quot;],
        &quot;checkpoint_id&quot;: checkpointe_id
    }
}

specific_history_checkpoint = graph.get_state(config=target_config)
print(specific_history_checkpoint)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">============================== -&gt; 根据ID查看特定检查点 &lt;- ==============================
checkpointe_id: 1f165804-9a90-6db5-bfff-d0eb857db9d8
StateSnapshot(
    values={},
    next=(&quot;__start__&quot;,),
    config={
        &quot;configurable&quot;: {
            &quot;thread_id&quot;: &quot;123&quot;,
            &quot;checkpoint_id&quot;: &quot;1f165804-9a90-6db5-bfff-d0eb857db9d8&quot;,
        }
    },
    metadata={
        &quot;source&quot;: &quot;input&quot;,
        &quot;step&quot;: -1,
        &quot;parents&quot;: {},
    },
    created_at=&quot;2026-06-11T10:28:29.123104+00:00&quot;,
    parent_config=None,
    tasks=(
        PregelTask(
            id=&quot;63a24d9c-ea6d-0420-4b5a-b978c6eaeb90&quot;,
            name=&quot;__start__&quot;,
            path=(&quot;__pregel_pull&quot;, &quot;__start__&quot;),
            error=None,
            interrupts=(),
            state=None,
            result={
                &quot;topic&quot;: &quot;猫咪&quot;,
            },
        ),
    ),
    interrupts=(),
)
</code></pre>
<p>当前场景下，我们已经获取了完整检查点列表，然后从中取了某个检查点的 <strong><code>checkpoint_id</code></strong>，然后调用 <strong><code>get_state()</code></strong> 查到了它对应的快照。</p>
<p>这看起来有些“绕”，因为我们本来就已经拿到了完整的历史快照。但在实际场景中，这个方法仍然很有意义。</p>
<p>例如，在后续的 <strong>检查点回溯-分叉</strong> 场景中，我们经常会基于历史检查点创建新的检查点，拿到该检查点的、带有 <strong><code>checkpoint_id</code></strong> 的配置信息 <strong><code>config</code></strong>。此时就可以将 <strong><code>config</code></strong> 传递给 <strong><code>graph.get_state()</code></strong>，从而便捷而精确地查看该检查点的快照。</p>
<h2 id="6-5-使用场景">6.5. 使用场景</h2>
<h3 id="6-5-1-多轮对话">6.5.1. 多轮对话</h3>
<p>最基础的使用场景是多轮对话。前文示例已经实现了一个简单的多轮对话流程，这里不再展开。</p>
<h3 id="6-5-2-中断恢复">6.5.2. 中断恢复</h3>
<p>中断恢复需要结合 <strong><code>LangGraph</code></strong> 的中断机制理解，见下文。</p>
<h3 id="6-5-3-失败后恢复运行">6.5.3. 失败后恢复运行</h3>
<h4 id="6-5-3-1-用法说明">6.5.3.1. 用法说明</h4>
<p>任务失败的原因可能是偶发的外部因素，如网络波动、秘钥过期、第三方服务异常；也可能是程序自身的BUG。</p>
<p>如果是前者，故障发生后，修复外部问题，直接重试即可；如果是后者，需要更改代码修复BUG后，再基于历史检查点恢复运行。</p>
<p>要在失败后基于历史检查点恢复运行，需要满足以下条件：</p>
<ol>
<li>启用检查点存储器
<ul>
<li>如果是 <strong><code>InMemorySaver</code></strong>，不要重建检查点存储器对象，否则历史检查点丢失，无法恢复</li>
<li>如果希望跨进程、服务重启后仍可恢复，应使用基于 <strong><code>SQLite、Postgres</code></strong> 等持久化检查点后端的存储器</li>
</ul>
</li>
<li>再次运行时用 <strong><code>None</code></strong> 作为计算图的状态输入</li>
<li>传递的配置信息应包含 <strong><code>thread_id</code></strong> 而不能包含 <strong><code>checkpoint_id</code></strong></li>
</ol>
<p>此时，<strong><code>LangGraph</code></strong> 会根据 <strong><code>thread_id</code></strong> 从 <strong><code>checkpointer</code></strong> 读取该会话的最新检查点，并从该检查点继续推进。</p>
<p>如果某个超步中有多个并行任务，其中一部分任务成功，另一部分任务失败，那么已经成功完成的任务结果会作为 <strong><code>pending_writes</code></strong> 被保存。恢复运行时，已经成功完成的任务不会重复执行。</p>
<h4 id="6-5-3-2-示例">6.5.3.2. 示例</h4>
<h5 id="6-5-3-2-1-模拟失败">6.5.3.2.1. 模拟失败</h5>
<p>本例使用如下结构：</p>
<pre><code class="language-text">START
  -&gt; node_change_topic
      -&gt; node_poem
      -&gt; node_joke
  -&gt; node_output
  -&gt; END
</code></pre>
<p>运行时，<strong><code>node_change_topic</code></strong> 会把输入主题 <strong><code>&quot;猫咪&quot;</code></strong> 改写为 <strong><code>&quot;猫咪: 布偶猫&quot;</code></strong>，然后并行执行 <strong><code>node_poem</code></strong> 和 <strong><code>node_joke</code></strong>，最后由 <strong><code>node_output</code></strong> 汇总输出。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict

from dotenv import  load_dotenv
from langchain_core.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek
from langgraph.checkpoint.memory import InMemorySaver
from langgraph.graph import StateGraph,START,END
from loguru import logger
load_dotenv(override=True)

model = ChatDeepSeek(
    model = &quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;:{
            &quot;type&quot;:&quot;disabled&quot;
        }
    }
)

#1. 声明状态
class OverAllState(TypedDict):
    topic:str
    poem:str
    joke:str
    final_output:str

#1.1 输入状态
class InputState(TypedDict):
    topic:str

#1.2 输出状态
class OutputState(TypedDict):
    final_output:str

topics = [&quot;布偶猫&quot;,&quot;狸花猫&quot;,&quot;金渐层&quot;]
topic_index = 0
#2. 定义节点
def node_change_topic(state:InputState)-&gt;OverAllState:
    global topic_index
    logger.info(&quot;topic_index:{}&quot;,topic_index)
    sub_topic = topics[topic_index]
    topic_index += 1
    topic_index %= len(topics)

    return {
        &quot;topic&quot;:f&quot;{state[&quot;topic&quot;]}:{sub_topic}&quot;
    }
#2.1 同一个超步的位置 成功运行的节点
def node_poem(state:OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_poem正在执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = model.invoke([HumanMessage(f&quot;写一首关于{topic}主题的七言绝句&quot;)]).content
    return {
        &quot;poem&quot;:poem
    }
import  time
#2.2 同一个超步的位置 运行失败的节点
def node_joke(state:OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_joke正在执行&quot;)
    topic = state[&quot;topic&quot;]
    time.sleep(5)
    raise Exception(&quot;人为抛异常&quot;)
    joke = model.invoke([HumanMessage(f&quot;写一首关于{topic}主题的笑话&quot;)]).content
    return {
        &quot;joke&quot;:joke
    }

def node_output(state:OverAllState) -&gt; OutputState:
    logger.info(&quot;node_output正在执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = state[&quot;poem&quot;]
    joke = state[&quot;joke&quot;]
    final_output = f&quot;关于{topic}的七言绝句:{poem}\n 笑话:{joke}\n&quot;
    return {
        &quot;final_output&quot;:final_output
    }

#3. 构建图
builder = StateGraph(state_schema=OverAllState,input_schema=InputState,output_schema=OutputState)

#3.1 添加节点
builder.add_node(&quot;node_change_topic&quot;,node_change_topic)
builder.add_node(&quot;node_poem&quot;,node_poem)
builder.add_node(&quot;node_joke&quot;,node_joke)
builder.add_node(&quot;node_output&quot;,node_output)

#3.2 添加边
builder.add_edge(START,&quot;node_change_topic&quot;)
builder.add_edge(&quot;node_change_topic&quot;,&quot;node_poem&quot;)
builder.add_edge(&quot;node_change_topic&quot;,&quot;node_joke&quot;)
builder.add_edge(&quot;node_poem&quot;,&quot;node_output&quot;)
builder.add_edge(&quot;node_joke&quot;,&quot;node_output&quot;)
builder.add_edge(&quot;node_output&quot;,END)

#4. 添加检查点后端
DB_URL = &quot;postgresql://langgraph_user:123456@localhost:5432/langgraph_db?sslmode=disable&quot;
from langgraph.checkpoint.postgres import PostgresSaver
with PostgresSaver.from_conn_string(DB_URL) as checkpointer:
    #5. 第一次使用PostgresSaver作为检查点 需要调用方法 setup()
    #checkpointer.setup()
    graph = builder.compile(checkpointer=checkpointer)

    from IPython.display import display
    display(graph)

    config = {
        &quot;configurable&quot;:{
            &quot;thread_id&quot;:&quot;chapter03-05&quot;
        }
    }

    res = graph.invoke({&quot;topic&quot;:&quot;猫&quot;},config=config)
    print(res)

</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260610161811425.png" alt="image-20260610161811425" /></p>
<pre><code class="language-json">2026-06-10 16:17:43.544 | INFO     | __main__:node_change_topic:45 - topic_idx: 0
2026-06-10 16:17:43.545 | INFO     | __main__:node_joke:65 - node_joke 已执行
2026-06-10 16:17:43.548 | INFO     | __main__:node_poem:54 - node_poem 已执行
&gt;Traceback...
Exception: 人为中断
During task with name 'node_joke' and id 'edb59019-4808-ed51-68eb-d5fba8696b13'
</code></pre>
<p>本例在 <strong><code>node_joke</code></strong> 中抛出异常，从而模拟计算图执行失败的场景。</p>
<p>为了观察恢复执行时哪些节点会被重新执行，示例中额外定义了全局变量 <strong><code>topics</code></strong> 和 <strong><code>topic_idx</code></strong>。<strong><code>node_change_topic</code></strong> 每执行一次，都会从 <strong><code>topics</code></strong> 中取出一个新的子主题，作为状态字段 <strong><code>topic</code></strong> 的值。</p>
<p>如果恢复运行时 <strong><code>node_change_topic</code></strong> 没有再次执行，则最终结果仍然会使用第一次生成的主题，即 <strong><code>猫咪: 布偶猫</code></strong>。如果该节点被重新执行，则主题可能会变成 <strong><code>猫咪: 狸花猫</code></strong> 或 <strong><code>猫咪: 金渐层</code></strong>。</p>
<h5 id="6-5-3-2-2-查看历史检查点列表">6.5.3.2.2. 查看历史检查点列表</h5>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">list(graph.get_state_history(config=config))
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">[
    StateSnapshot(
        values={
            &quot;topic&quot;: &quot;猫咪: 布偶猫&quot;
        },
        next=(
            &quot;node_poem&quot;,
            &quot;node_joke&quot;
        ),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f164a86-e60d-6881-8001-c717582e8893&quot;
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 1,
            &quot;parents&quot;: {}
        },
        created_at=&quot;2026-06-10T08:43:19.431987+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f164a86-e608-6596-8000-ed45af11cea7&quot;
            }
        },
        tasks=(
            PregelTask(
                id=&quot;66287e00-4228-a985-fc09-6791a14e26ce&quot;,
                name=&quot;node_poem&quot;,
                path=(
                    &quot;__pregel_pull&quot;,
                    &quot;node_poem&quot;
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;poem&quot;: &quot;《戏题布偶猫》\n雪翼云裘自绝尘，琉璃碧眼若含春。\n行来莲步生兰气，卧处梨花是此身。&quot;
                }
            ),
            PregelTask(
                id=&quot;b2632b70-562b-7d5c-0dae-0f83bd6c2912&quot;,
                name=&quot;node_joke&quot;,
                path=(
                    &quot;__pregel_pull&quot;,
                    &quot;node_joke&quot;
                ),
                error=&quot;Exception('人为中断')&quot;,
                interrupts=(),
                state=None,
                result=None
            )
        ),
        interrupts=()
    ),

    StateSnapshot(
        values={
            &quot;topic&quot;: &quot;猫咪&quot;
        },
        next=(
            &quot;node_change_topic&quot;,
        ),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f164a86-e608-6596-8000-ed45af11cea7&quot;
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 0,
            &quot;parents&quot;: {}
        },
        created_at=&quot;2026-06-10T08:43:19.429860+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f164a86-e606-6a42-bfff-9f09d8a3aa96&quot;
            }
        },
        tasks=(
            PregelTask(
                id=&quot;273fe47f-b4cc-93d0-0ab7-57a0db4bad56&quot;,
                name=&quot;node_change_topic&quot;,
                path=(
                    &quot;__pregel_pull&quot;,
                    &quot;node_change_topic&quot;
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;topic&quot;: &quot;猫咪: 布偶猫&quot;
                }
            ),
        ),
        interrupts=()
    ),

    StateSnapshot(
        values={},
        next=(
            &quot;__start__&quot;,
        ),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f164a86-e606-6a42-bfff-9f09d8a3aa96&quot;
            }
        },
        metadata={
            &quot;source&quot;: &quot;input&quot;,
            &quot;step&quot;: -1,
            &quot;parents&quot;: {}
        },
        created_at=&quot;2026-06-10T08:43:19.429167+00:00&quot;,
        parent_config=None,
        tasks=(
            PregelTask(
                id=&quot;ec8e6e32-6969-6798-9c5f-aade41e9211a&quot;,
                name=&quot;__start__&quot;,
                path=(
                    &quot;__pregel_pull&quot;,
                    &quot;__start__&quot;
                ),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;topic&quot;: &quot;猫咪&quot;
                }
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<p>可以看到，失败后的检查点中（<strong><code>step=1</code></strong>）记录了当前超步相关任务的执行情况：</p>
<ul>
<li><strong><code>node_poem</code></strong> 执行成功，结果记录在 <strong><code>result</code></strong> 字段中。</li>
<li><strong><code>node_joke</code></strong> 执行失败，<strong><code>result</code></strong> 为空，异常信息记录在 <strong><code>error</code></strong> 字段中。</li>
</ul>
<p>本例中，<strong><code>node_poem</code></strong> 和 <strong><code>node_joke</code></strong> 属于同一个超步。虽然 <strong><code>node_joke</code></strong> 失败了，但 <strong><code>node_poem</code></strong> 已成功，其计算结果会被持久化机制保存下来。因此，后续恢复运行时，<strong><code>node_poem</code></strong> 不会重复执行。</p>
<h5 id="6-5-3-2-3-修复bug之后恢复运行">6.5.3.2.3. 修复BUG之后恢复运行</h5>
<p>修复 <strong><code>node_joke</code></strong> 中的人为异常后，重新编译计算图，并基于最新检查点恢复运行。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">def node_joke(state: InputState) -&gt; OverAllState:
    logger.info(f&quot;node_joke 已执行&quot;)
    topic = state[&quot;topic&quot;]

    joke = model.invoke([HumanMessage(f&quot;写一个关于 {topic} 的笑话&quot;)]).content

    return {
        &quot;joke&quot;: joke
    }


builder = StateGraph(state_schema=OverAllState, input_schema=InputState, output_schema=OutputState)
builder.add_node(&quot;node_change_topic&quot;, node_change_topic)
builder.add_node(&quot;node_poem&quot;, node_poem)
builder.add_node(&quot;node_joke&quot;, node_joke)
builder.add_node(&quot;node_output&quot;, node_output)
builder.add_edge(START, &quot;node_change_topic&quot;)
builder.add_edge(&quot;node_change_topic&quot;, &quot;node_poem&quot;)
builder.add_edge(&quot;node_change_topic&quot;, &quot;node_joke&quot;)
builder.add_edge(&quot;node_poem&quot;, &quot;node_output&quot;)
builder.add_edge(&quot;node_joke&quot;, &quot;node_output&quot;)
builder.add_edge(&quot;node_output&quot;, END)

new_graph = builder.compile(checkpointer=checkpointer)

from IPython.display import display
display(new_graph)

res = new_graph.invoke(None, config=config)
print(res)
</code></pre>
<p>这里需要注意两点：</p>
<ol>
<li>重新编译计算图时，仍然使用原来的 <strong><code>checkpointer</code></strong> 对象。</li>
<li>恢复运行时，输入参数传入 <strong><code>None</code></strong>，并且配置中只传入 <strong><code>thread_id</code></strong>。</li>
</ol>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260610163718414.png" alt="image-20260610163718414" /></p>
<pre><code class="language-json">2026-06-10 16:37:12.985 | INFO     | __main__:node_joke:2 - node_joke 已执行
2026-06-10 16:37:18.878 | INFO     | __main__:node_output:76 - node_output 已执行
{
    'final_output': '关于 猫咪: 布偶猫 的七言绝句：\n《戏题布偶猫》\n雪翼云裘自绝尘，琉璃碧眼若含春。\n行来莲步生兰气，卧处梨花是此身。\n笑话：\n一只布偶猫走进咖啡馆，优雅地跳上吧台，对老板说：“给我来一杯最贵的猫屎咖啡。”  \n老板愣住：“可……您本身就是猫啊？”  \n布偶猫舔了舔爪子，翻了个白眼：“所以呢？我自己产的屎，你们人类不是喝得挺香吗？今天我倒要尝尝，你们拿我的‘周边产品’能泡出什么花样来。”'
}
</code></pre>
<p>运行结果中可以观察到：</p>
<ul>
<li><strong><code>node_joke</code></strong> 被重新执行。</li>
<li><strong><code>node_output</code></strong> 被执行。</li>
<li><strong><code>node_poem</code></strong> 没有被重新执行。</li>
</ul>
<p>最终输出中的七言绝句和历史检查点中 <strong><code>node_poem.result</code></strong> 记录的内容一致，说明 <strong><code>node_poem</code></strong> 的执行结果被成功复用了。</p>
<h3 id="6-5-4-time-travel">6.5.4. Time Travel</h3>
<p><strong><code>Time Travel</code></strong> 直译为 <strong>时间旅行</strong>，在当前场景下太过生硬，从技术实现和使用场景来看，译为 <strong>检查点回溯</strong> 更为合理。</p>
<p>所谓<strong>检查点回溯</strong>，是指基于某个历史检查点，重新执行后续流程，或者在该检查点基础上修改状态并创建新的执行分支。</p>
<p><strong>检查点回溯</strong>有两种形式，根据是否更改历史状态区分：</p>
<ul>
<li><strong><code>Replay</code></strong>：检查点重放，回到某个历史检查点，沿着原先的执行路径重新执行后续节点。</li>
<li><strong><code>Fork</code></strong>：检查点分叉，回到某个历史检查点，修改状态，从该位置创建一条新的执行分支。</li>
</ul>
<p>二者的共同点是：</p>
<blockquote>
<p>检查点之前的节点不会重新执行，检查点之后的节点会重新执行。</p>
</blockquote>
<p>二者的区别是：</p>
<blockquote>
<p><strong><code>Replay</code></strong> 不修改历史状态；<strong><code>Fork</code></strong> 会基于历史检查点应用新的状态更新，并创建新的检查点分支。</p>
</blockquote>
<h4 id="6-5-4-1-replay">6.5.4.1. Replay</h4>
<p><strong><code>Replay</code></strong> 模式和失败恢复很接近，但二者的触发方式和语义不同。</p>
<ul>
<li>
<p>失败恢复通常基于最新检查点继续运行，配置中只包含 <strong><code>thread_id</code></strong>，不包含 <strong><code>checkpoint_id</code></strong>。</p>
</li>
<li>
<p><strong><code>Replay</code></strong> 则是显式传入某个历史检查点的配置，配置中包含 <strong><code>checkpoint_id</code></strong>。<strong><code>LangGraph</code></strong> 会据此从该历史检查点开始重放后续步骤。</p>
</li>
</ul>
<p>需要注意：</p>
<blockquote>
<p><strong><code>Replay</code></strong> 不是简单读取历史缓存，而是重新执行该检查点之后的节点。</p>
</blockquote>
<p>因此，检查点之后的 <strong><code>LLM 调用、API 请求、工具调用、中断</code></strong> 等都会重新触发，最终结果可能和原始结果不同。</p>
<h5 id="6-5-4-1-1-正常运行的任务">6.5.4.1.1. 正常运行的任务</h5>
<p>本例仍然使用如下结构：</p>
<pre><code class="language-json">START
  -&gt; node_change_topic
      -&gt; node_poem
      -&gt; node_joke
  -&gt; node_output
  -&gt; END
</code></pre>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict

from dotenv import  load_dotenv
from langchain_core.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek
from langgraph.checkpoint.memory import InMemorySaver
from langgraph.graph import StateGraph,START,END
from loguru import logger
load_dotenv(override=True)

model = ChatDeepSeek(
    model = &quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;:{
            &quot;type&quot;:&quot;disabled&quot;
        }
    }
)

#1. 声明状态
class OverAllState(TypedDict):
    topic:str
    poem:str
    joke:str
    final_output:str

#1.1 输入状态
class InputState(TypedDict):
    topic:str

#1.2 输出状态
class OutputState(TypedDict):
    final_output:str

topics = [&quot;布偶猫&quot;,&quot;狸花猫&quot;,&quot;金渐层&quot;]
topic_index = 0
#2. 定义节点
def node_change_topic(state:InputState)-&gt;OverAllState:
    global topic_index
    logger.info(&quot;topic_index:{}&quot;,topic_index)
    sub_topic = topics[topic_index]
    topic_index += 1
    topic_index %= len(topics)

    return {
        &quot;topic&quot;:f&quot;{state[&quot;topic&quot;]}:{sub_topic}&quot;
    }
#2.1 同一个超步的位置 成功运行的节点
def node_poem(state:OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_poem正在执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = model.invoke([HumanMessage(f&quot;写一首关于{topic}主题的七言绝句&quot;)]).content
    return {
        &quot;poem&quot;:poem
    }
import  time
#2.2 同一个超步的位置 运行失败的节点
def node_joke(state:OverAllState) -&gt; OverAllState:
    logger.info(&quot;node_joke正在执行&quot;)
    topic = state[&quot;topic&quot;]
    # time.sleep(5)
    # raise Exception(&quot;人为抛异常&quot;)
    joke = model.invoke([HumanMessage(f&quot;写一首关于{topic}主题的笑话&quot;)]).content
    return {
        &quot;joke&quot;:joke
    }

def node_output(state:OverAllState) -&gt; OutputState:
    logger.info(&quot;node_output正在执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = state[&quot;poem&quot;]
    joke = state[&quot;joke&quot;]
    final_output = f&quot;关于{topic}的七言绝句:{poem}\n 笑话:{joke}\n&quot;
    return {
        &quot;final_output&quot;:final_output
    }

#3. 构建图
builder = StateGraph(state_schema=OverAllState,input_schema=InputState,output_schema=OutputState)

#3.1 添加节点
builder.add_node(&quot;node_change_topic&quot;,node_change_topic)
builder.add_node(&quot;node_poem&quot;,node_poem)
builder.add_node(&quot;node_joke&quot;,node_joke)
builder.add_node(&quot;node_output&quot;,node_output)

#3.2 添加边
builder.add_edge(START,&quot;node_change_topic&quot;)
builder.add_edge(&quot;node_change_topic&quot;,&quot;node_poem&quot;)
builder.add_edge(&quot;node_change_topic&quot;,&quot;node_joke&quot;)
builder.add_edge(&quot;node_poem&quot;,&quot;node_output&quot;)
builder.add_edge(&quot;node_joke&quot;,&quot;node_output&quot;)
builder.add_edge(&quot;node_output&quot;,END)

#4. 添加检查点后端
DB_URL = &quot;postgresql://langgraph_user:123456@localhost:5432/langgraph_db?sslmode=disable&quot;
from langgraph.checkpoint.postgres import PostgresSaver
with PostgresSaver.from_conn_string(DB_URL) as checkpointer:
    #5. 第一次使用PostgresSaver作为检查点 需要调用方法 setup()
    #checkpointer.setup()
    graph = builder.compile(checkpointer=checkpointer)

    from IPython.display import display
    display(graph)

    config = {
        &quot;configurable&quot;:{
            &quot;thread_id&quot;:&quot;chapter03-08&quot;
        }
    }

    res = graph.invoke({&quot;topic&quot;:&quot;猫咪&quot;},config=config)
    print(res)

</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260610171127937.png" alt="image-20260610171127937" /></p>
<pre><code class="language-json">2026-06-12 14:32:42.019 | INFO     | __main__:node_change_topic:45 - topic_idx: 0
2026-06-12 14:32:42.022 | INFO     | __main__:node_joke:63 - node_joke 已执行
2026-06-12 14:32:42.026 | INFO     | __main__:node_poem:54 - node_poem 已执行
2026-06-12 14:32:43.585 | INFO     | __main__:node_output:72 - node_output 已执行
2026-06-12 14:32:43.588 | INFO     | __main__:&lt;module&gt;:102 - res:
{
    'final_output': '关于 猫咪: 布偶猫 的七言绝句：\n《布偶猫》\n雪团云絮落仙踪，碧眼盈盈醉九重。\n静若琼瑶闲卧月，清姿一步一玲珑。\n笑话：\n一只布偶猫照镜子，惊呼：“我这么美，怎么还没当上国王？” 主人笑道：“因为你没有‘喵’民拥护。” 布偶猫委屈：“我有啊，铲屎官你不是天天跪着拥护我吗？”'
}
</code></pre>
<h5 id="6-5-4-1-2-查看历史检查点列表">6.5.4.1.2. 查看历史检查点列表</h5>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">history_checkpoints = list(graph.get_state_history(config=config))
print(history_checkpoints)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">[
    StateSnapshot(
        values={
            'topic': '猫咪: 布偶猫',
            'poem': '《布偶猫》\n雪团云絮落仙踪，碧眼盈盈醉九重。\n静若琼瑶闲卧月，清姿一步一玲珑。',
            'joke': '一只布偶猫照镜子，惊呼：“我这么美，怎么还没当上国王？” 主人笑道：“因为你没有‘喵’民拥护。” 布偶猫委屈：“我有啊，铲屎官你不是天天跪着拥护我吗？”',
            'final_output': '关于 猫咪: 布偶猫 的七言绝句：\n《布偶猫》\n雪团云絮落仙踪，碧眼盈盈醉九重。\n静若琼瑶闲卧月，清姿一步一玲珑。\n笑话：\n一只布偶猫照镜子，惊呼：“我这么美，怎么还没当上国王？” 主人笑道：“因为你没有‘喵’民拥护。” 布偶猫委屈：“我有啊，铲屎官你不是天天跪着拥护我吗？”'
        },
        next=(),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f166288-4ad3-67ef-8003-01d021de9403'
            }
        },
        metadata={
            'source': 'loop',
            'step': 3,
            'parents': {}
        },
        created_at='2026-06-12T06:32:43.586547+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f166288-4acf-62cf-8002-aed204b5115d'
            }
        },
        tasks=(),
        interrupts=()
    ),
    StateSnapshot(
        values={
            'topic': '猫咪: 布偶猫',
            'poem': '《布偶猫》\n雪团云絮落仙踪，碧眼盈盈醉九重。\n静若琼瑶闲卧月，清姿一步一玲珑。',
            'joke': '一只布偶猫照镜子，惊呼：“我这么美，怎么还没当上国王？” 主人笑道：“因为你没有‘喵’民拥护。” 布偶猫委屈：“我有啊，铲屎官你不是天天跪着拥护我吗？”'
        },
        next=('node_output',),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f166288-4acf-62cf-8002-aed204b5115d'
            }
        },
        metadata={
            'source': 'loop',
            'step': 2,
            'parents': {}
        },
        created_at='2026-06-12T06:32:43.584774+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f166288-3be4-6523-8001-dd86f05c34b8'
            }
        },
        tasks=(
            PregelTask(
                id='b9042203-0ddf-f638-aeeb-e0501601381a',
                name='node_output',
                path=('__pregel_pull', 'node_output'),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'final_output': '关于 猫咪: 布偶猫 的七言绝句：\n《布偶猫》\n雪团云絮落仙踪，碧眼盈盈醉九重。\n静若琼瑶闲卧月，清姿一步一玲珑。\n笑话：\n一只布偶猫照镜子，惊呼：“我这么美，怎么还没当上国王？” 主人笑道：“因为你没有‘喵’民拥护。” 布偶猫委屈：“我有啊，铲屎官你不是天天跪着拥护我吗？”'
                }
            ),
        ),
        interrupts=()
    ),
    StateSnapshot(
        values={
            'topic': '猫咪: 布偶猫'
        },
        next=('node_poem', 'node_joke'),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f166288-3be4-6523-8001-dd86f05c34b8'
            }
        },
        metadata={
            'source': 'loop',
            'step': 1,
            'parents': {}
        },
        created_at='2026-06-12T06:32:42.020569+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f166288-3bde-6373-8000-9750b4be0665'
            }
        },
        tasks=(
            PregelTask(
                id='eab6bf51-4986-a60f-753c-e493b78c56b8',
                name='node_poem',
                path=('__pregel_pull', 'node_poem'),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'poem': '《布偶猫》\n雪团云絮落仙踪，碧眼盈盈醉九重。\n静若琼瑶闲卧月，清姿一步一玲珑。'
                }
            ),
            PregelTask(
                id='a228a7bf-4c3b-2ad3-039f-12ac0107c87b',
                name='node_joke',
                path=('__pregel_pull', 'node_joke'),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'joke': '一只布偶猫照镜子，惊呼：“我这么美，怎么还没当上国王？” 主人笑道：“因为你没有‘喵’民拥护。” 布偶猫委屈：“我有啊，铲屎官你不是天天跪着拥护我吗？”'
                }
            )
        ),
        interrupts=()
    ),
    StateSnapshot(
        values={
            'topic': '猫咪'
        },
        next=('node_change_topic',),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f166288-3bde-6373-8000-9750b4be0665'
            }
        },
        metadata={
            'source': 'loop',
            'step': 0,
            'parents': {}
        },
        created_at='2026-06-12T06:32:42.018069+00:00',
        parent_config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f166288-3bdc-60a4-bfff-d4fdcf6e6a9e'
            }
        },
        tasks=(
            PregelTask(
                id='e0034c08-dd19-0ae9-7059-f8414745d212',
                name='node_change_topic',
                path=('__pregel_pull', 'node_change_topic'),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'topic': '猫咪: 布偶猫'
                }
            ),
        ),
        interrupts=()
    ),
    StateSnapshot(
        values={},
        next=('__start__',),
        config={
            'configurable': {
                'thread_id': '123',
                'checkpoint_ns': '',
                'checkpoint_id': '1f166288-3bdc-60a4-bfff-d4fdcf6e6a9e'
            }
        },
        metadata={
            'source': 'input',
            'step': -1,
            'parents': {}
        },
        created_at='2026-06-12T06:32:42.017182+00:00',
        parent_config=None,
        tasks=(
            PregelTask(
                id='1a1ac312-152a-dcc3-2aa7-c2588f651448',
                name='__start__',
                path=('__pregel_pull', '__start__'),
                error=None,
                interrupts=(),
                state=None,
                result={
                    'topic': '猫咪'
                }
            ),
        ),
        interrupts=()
    )
]
</code></pre>
<h5 id="6-5-4-1-3-重放">6.5.4.1.3. 重放</h5>
<p><strong><code>get_state_history()</code></strong> 返回的是历史检查点列表，并且默认按照时间倒序排列。</p>
<p><strong><code>history_checkpoints</code></strong> 结构如下：</p>
<pre><code class="language-text">history_checkpoints[0] -&gt; 最终完成后的检查点
						next=()
history_checkpoints[1] -&gt; node_output 执行前的检查点
						next=('node_output',)
history_checkpoints[2] -&gt; node_poem、node_joke 执行前的检查点
						next=('node_poem', 'node_joke')
history_checkpoints[3] -&gt; node_change_topic 执行前的检查点
						next=('node_change_topic',)
history_checkpoints[4] -&gt; __start__ 执行前的输入检查点
						next=('__start__',)
</code></pre>
<p>因此，如果希望从写诗写笑话开始重放，选择 <strong><code>next == ('node_poem', 'node_joke')</code></strong> 的检查点即可。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">with PostgresSaver.from_conn_string(DB_URL) as checkpointer:
    #5. 第一次使用PostgresSaver作为检查点 需要调用方法 setup()
    #checkpointer.setup()
    graph = builder.compile(checkpointer=checkpointer)

    config = {
        &quot;configurable&quot;:{
            &quot;thread_id&quot;:&quot;chapter03-08&quot;
        }
    }
    # 获取到检查点历史
    history_checkpoints = list(graph.get_state_history(config=config))

    new_checkpoint = None
    #next = ('node_poem', 'node_joke')
    for checkpoint in history_checkpoints:
        if checkpoint.next == ('node_poem', 'node_joke'):
            new_checkpoint = checkpoint
            break

    # 如果想要实现replay的效果 状态填写None  config填写为之前某一个检查点的config
    res = graph.invoke(None,config=new_checkpoint.config)
    print(res)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">2026-07-07 18:31:32.019 | INFO     | __main__:node_joke:59 - node_joke正在执行
2026-07-07 18:31:32.023 | INFO     | __main__:node_poem:50 - node_poem正在执行
2026-07-07 18:31:38.158 | INFO     | __main__:node_output:69 - node_output正在执行
{'final_output': '关于猫咪:狸花猫的七言绝句:《狸花猫》\n玄文斑驳隐花光，夜半巡檐气自昂。\n踏雪无痕轻似梦，一窗明月一炉香。\n\n注：我的创作思路是捕捉狸花猫的神秘与灵动。首句“玄文斑驳”描绘其毛发纹理，如暗夜中的花纹；次句“夜半巡檐”展现其夜行习性，气度不凡。后两句以“踏雪无痕”喻其轻盈，结句“明月炉香”营造温馨画面，暗合狸花猫守护家园的意象，使全诗动静相生，虚实相映。\n 笑话:# 狸花猫的烦恼\n\n有只狸花猫去宠物医院看病，医生问它：“哪里不舒服？”\n\n狸花猫叹了口气说：“医生，我总觉得自己不够‘花’。”\n\n医生疑惑：“你可是标准的狸花猫啊，条纹多漂亮！”\n\n狸花猫委屈地说：“可是隔壁那只三花猫，人家身上有黑、白、橘三种颜色，比我缤纷多了！我整天就这一身灰黑条纹，太单调了。”\n\n医生笑了：“那你想要怎么办？”\n\n狸花猫眼睛一亮：“医生，您能不能给我开点染色剂？我想在背上加几块橘色斑点，尾巴再来点白的，耳朵尖染成黑的…”\n\n医生打断它：“等等，那样你就变成‘四不像’了！”\n\n狸花猫思考了一会，突然骄傲地挺起胸膛：“你说得对！我这一身条纹可是老祖宗传下来的迷彩服，抓老鼠时特别管用！三花猫再好看，还得靠我帮忙抓家里的老鼠呢！”\n\n医生欣慰地说：“这就对了。”\n\n狸花猫走出诊室，正好遇到一只胖橘猫。胖橘猫嘲笑它：“哟，这不是那只会抓老鼠却不会卖萌的狸花猫吗？”\n\n狸花猫淡定地回了一句：“我会抓老鼠，你会什么？”\n\n胖橘猫想了想，尴尬地说：“我…我会吃。”\n\n狸花猫笑道：“那不就成了？你负责吃，我负责抓，咱们各有各的活法！”\n\n—— **适合自己的，才是最好的。** 🐱\n'}

</code></pre>
<p>本例中：</p>
<ol>
<li><strong><code>node_poem</code></strong> 和 <strong><code>node_joke</code></strong> 被重新执行。</li>
<li><strong><code>node_output</code></strong> 被重新执行。</li>
</ol>
<p>由于没有执行change_topic,所以可以一直写同一只猫的内容</p>
<h4 id="6-5-4-2-fork">6.5.4.2. Fork</h4>
<h5 id="6-5-4-2-1-用法说明">6.5.4.2.1. 用法说明</h5>
<p><strong><code>Fork</code></strong> 依赖状态图的 <strong><code>update_state()</code></strong> 方法</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">change_input_config = graph.update_state(
    config=before_router_config.config,
    values={&quot;user_input&quot;: &quot;帮我写一个关于布偶猫的笑话&quot;},
    as_node=START
)
</code></pre>
<p><strong>其中</strong>：</p>
<ul>
<li>
<p><strong><code>config</code></strong>：历史检查点的配置信息，通常包含 <strong><code>thread_id</code></strong>、<strong><code>checkpoint_ns</code></strong> 和 <strong><code>checkpoint_id</code></strong>。<strong><code>LangGraph</code></strong> 会据此检索历史检查点</p>
</li>
<li>
<p><strong><code>values</code></strong>：要应用到该检查点上的状态更新</p>
</li>
<li>
<p><strong><code>as_node</code></strong>：指定这次状态更新应当被视为哪个节点产生的输出。</p>
<p><strong><code>LangGraph</code></strong> 会按照该节点的写入逻辑，将 <strong><code>values</code></strong> 写入状态通道，并据此决定后续从哪些节点继续执行。</p>
<p>基于分叉检查点运行时，并不是重新执行 <strong><code>as_node</code></strong> 本身，而是从它的后继节点继续推进</p>
<p>如 <strong><code>START</code></strong> 所属超步编号为 <strong><code>0</code></strong>，则恢复时从编号为 <strong><code>1</code></strong> 的超步开始运行。</p>
</li>
</ul>
<p><strong>注意</strong></p>
<blockquote>
<p><strong><code>update_state()</code></strong> 不会修改原来的历史检查点，而是基于某个历史检查点创建一个新的检查点。这个新检查点相当于一条新的执行分支。</p>
</blockquote>
<h5 id="6-5-4-2-2-准备检查点历史">6.5.4.2.2. 准备检查点历史</h5>
<h6 id="6-5-4-2-2-1-准备运行图并运行一次">6.5.4.2.2.1. 准备运行图并运行一次</h6>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Annotated, Literal
from langgraph.graph import StateGraph, START, END
from langchain.messages import HumanMessage
from langgraph.checkpoint.memory import InMemorySaver
from langchain_deepseek import ChatDeepSeek
from loguru import logger
from dotenv import load_dotenv

load_dotenv(override=True)


model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    username: str
    user_input: str
    output: str

def router_node(state: OverAllState) -&gt; StructuredOutputState:
    logger.info(&quot;路由节点已执行&quot;)
    user_input = state[&quot;user_input&quot;]
    res = model_with_structure.invoke(
        [
            HumanMessage(user_input)
        ]
    )
    logger.info(&quot;路由结果-res: {}&quot;, res)

    return res

def router(state: StructuredOutputState) -&gt; Literal[&quot;node_poem&quot;, &quot;node_joke&quot;, &quot;node_default&quot;]:
    logger.info(&quot;路由函数已执行&quot;)
    if state[&quot;mode&quot;] == &quot;poem&quot;:
        logger.info(&quot;路由至 node_poem 节点&quot;)
        return &quot;node_poem&quot;
    elif state[&quot;mode&quot;] == &quot;joke&quot;:
        logger.info(&quot;路由至 node_joke 节点&quot;)
        return &quot;node_joke&quot;

    logger.info(&quot;路由至兜底节点&quot;)
    return &quot;node_default&quot;

def node_poem(state: StructuredOutputState) -&gt; OverAllState:
    logger.info(f&quot;node_poem 已执行&quot;)
    topic = state[&quot;topic&quot;]
    poem = model.invoke([HumanMessage(f&quot;写一首关于 {topic} 的七言绝句，不要赏析，只给诗句即可&quot;)]).content

    return {
        &quot;output&quot;: poem
    }

def node_joke(state: StructuredOutputState) -&gt; OverAllState:
    logger.info(f&quot;node_joke 已执行&quot;)
    topic = state[&quot;topic&quot;]
    joke = model.invoke([HumanMessage(f&quot;写一个关于 {topic} 的笑话，尽可能简单，不超过一百字&quot;)]).content

    return {
        &quot;output&quot;: joke
    }

def node_default(state: StructuredOutputState) -&gt; OverAllState:
    logger.info(f&quot;node_default 已执行&quot;)

    return {
        &quot;output&quot;: &quot;无法处理的任务类型&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;router_node&quot;, router_node)
builder.add_node(&quot;node_poem&quot;, node_poem)
builder.add_node(&quot;node_joke&quot;, node_joke)
builder.add_node(&quot;node_default&quot;, node_default)

builder.add_edge(START, &quot;router_node&quot;)
builder.add_conditional_edges(
    &quot;router_node&quot;,
    router,
    path_map=[&quot;node_poem&quot;, &quot;node_joke&quot;, &quot;node_default&quot;]
)
builder.add_edge(&quot;node_poem&quot;, END)
builder.add_edge(&quot;node_joke&quot;, END)
builder.add_edge(&quot;node_default&quot;, END)

checkpointer = InMemorySaver()
config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;123&quot;}}
graph = builder.compile(checkpointer=checkpointer)

from IPython.display import display
display(graph)

res = graph.invoke({
    &quot;username&quot;: &quot;小黄&quot;,
    &quot;user_input&quot;: &quot;帮我写一首关于布偶猫的七言绝句&quot;
}, config=config)
print(res)
</code></pre>
<p>执行逻辑如下：</p>
<ol>
<li><strong><code>router_node</code></strong> 根据用户输入提取结构化结果，例如 <strong><code>{&quot;topic&quot;: &quot;布偶猫&quot;, &quot;mode&quot;: &quot;poem&quot;}</code></strong>。</li>
<li><strong><code>router</code></strong> 根据 <strong><code>mode</code></strong> 决定后续分支。</li>
<li>如果 <strong><code>mode == &quot;poem&quot;</code></strong>，进入 <strong><code>node_poem</code></strong>。</li>
<li>如果 <strong><code>mode == &quot;joke&quot;</code></strong>，进入 <strong><code>node_joke</code></strong>。</li>
<li>如果无法识别，则进入 <strong><code>node_default</code></strong>。</li>
</ol>
<p>运行结果如下</p>
<p><img src="/images/langgraph-study-notes/image-20260611142714758.png" alt="image-20260611142714758" /></p>
<pre><code class="language-json">2026-06-11 14:26:48.353 | INFO     | __main__:router_node:31 - 路由节点已执行
2026-06-11 14:26:49.618 | INFO     | __main__:router_node:38 - 路由结果-res: {'topic': '布偶猫', 'mode': 'poem'}
2026-06-11 14:26:49.619 | INFO     | __main__:router:43 - 路由函数已执行
2026-06-11 14:26:49.619 | INFO     | __main__:router:45 - 路由至 node_poem 节点
2026-06-11 14:26:49.620 | INFO     | __main__:node_poem:55 - node_poem 已执行
{
    'username': '小黄',
    'user_input': '帮我写一首关于布偶猫的七言绝句',
    'output': '《布偶猫》\n蓝眼澄明玉作团，云踪雪影卧雕栏。\n无端一跃梅花落，犹带娇声唤月寒。'
}
</code></pre>
<h6 id="6-5-4-2-2-2-获取历史检查点列表">6.5.4.2.2.2. 获取历史检查点列表</h6>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">history_checkpoints = list(graph.get_state_history(config=config))
print(history_checkpoints)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">[
    StateSnapshot(
        values={
            &quot;username&quot;: &quot;小黄&quot;,
            &quot;user_input&quot;: &quot;帮我写一首关于布偶猫的七言绝句&quot;,
            &quot;output&quot;: (
                &quot;《布偶猫》\n&quot;
                &quot;蓝眼澄明玉作团，云踪雪影卧雕栏。\n&quot;
                &quot;无端一跃梅花落，犹带娇声唤月寒。&quot;
            ),
            &quot;topic&quot;: &quot;布偶猫&quot;,
            &quot;mode&quot;: &quot;poem&quot;,
        },
        next=(),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f1655e8-8784-65e9-8002-e21663d55c94&quot;,
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 2,
            &quot;parents&quot;: {},
        },
        created_at=&quot;2026-06-11T06:26:51.611071+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f1655e8-7488-66c4-8001-f46b7c2d9a4e&quot;,
            }
        },
        tasks=(),
        interrupts=(),
    ),

    StateSnapshot(
        values={
            &quot;username&quot;: &quot;小黄&quot;,
            &quot;user_input&quot;: &quot;帮我写一首关于布偶猫的七言绝句&quot;,
            &quot;topic&quot;: &quot;布偶猫&quot;,
            &quot;mode&quot;: &quot;poem&quot;,
        },
        next=(&quot;node_poem&quot;,),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f1655e8-7488-66c4-8001-f46b7c2d9a4e&quot;,
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 1,
            &quot;parents&quot;: {},
        },
        created_at=&quot;2026-06-11T06:26:49.620436+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f1655e8-6871-6073-8000-ddcbe680da21&quot;,
            }
        },
        tasks=(
            PregelTask(
                id=&quot;cb3a8901-3a87-d0b0-3bab-d1adda5563d8&quot;,
                name=&quot;node_poem&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;node_poem&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;output&quot;: (
                        &quot;《布偶猫》\n&quot;
                        &quot;蓝眼澄明玉作团，云踪雪影卧雕栏。\n&quot;
                        &quot;无端一跃梅花落，犹带娇声唤月寒。&quot;
                    )
                },
            ),
        ),
        interrupts=(),
    ),

    StateSnapshot(
        values={
            &quot;username&quot;: &quot;小黄&quot;,
            &quot;user_input&quot;: &quot;帮我写一首关于布偶猫的七言绝句&quot;,
        },
        next=(&quot;router_node&quot;,),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f1655e8-6871-6073-8000-ddcbe680da21&quot;,
            }
        },
        metadata={
            &quot;source&quot;: &quot;loop&quot;,
            &quot;step&quot;: 0,
            &quot;parents&quot;: {},
        },
        created_at=&quot;2026-06-11T06:26:48.352564+00:00&quot;,
        parent_config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f1655e8-686b-66c7-bfff-24e0e8d44f4c&quot;,
            }
        },
        tasks=(
            PregelTask(
                id=&quot;fb88436e-b163-6308-6341-7d8368e041f9&quot;,
                name=&quot;router_node&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;router_node&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;topic&quot;: &quot;布偶猫&quot;,
                    &quot;mode&quot;: &quot;poem&quot;,
                },
            ),
        ),
        interrupts=(),
    ),

    StateSnapshot(
        values={},
        next=(&quot;__start__&quot;,),
        config={
            &quot;configurable&quot;: {
                &quot;thread_id&quot;: &quot;123&quot;,
                &quot;checkpoint_ns&quot;: &quot;&quot;,
                &quot;checkpoint_id&quot;: &quot;1f1655e8-686b-66c7-bfff-24e0e8d44f4c&quot;,
            }
        },
        metadata={
            &quot;source&quot;: &quot;input&quot;,
            &quot;step&quot;: -1,
            &quot;parents&quot;: {},
        },
        created_at=&quot;2026-06-11T06:26:48.350273+00:00&quot;,
        parent_config=None,
        tasks=(
            PregelTask(
                id=&quot;a1b5e5fb-ee60-fd7b-68bc-475fa9cb66e2&quot;,
                name=&quot;__start__&quot;,
                path=(&quot;__pregel_pull&quot;, &quot;__start__&quot;),
                error=None,
                interrupts=(),
                state=None,
                result={
                    &quot;username&quot;: &quot;小黄&quot;,
                    &quot;user_input&quot;: &quot;帮我写一首关于布偶猫的七言绝句&quot;,
                },
            ),
        ),
        interrupts=(),
    ),
]
</code></pre>
<h6 id="6-5-4-2-2-3-获取-router_node-之前的检查点">6.5.4.2.2.3. 获取 <code>router_node</code> 之前的检查点</h6>
<p><strong>示例如下</strong></p>
<p><strong><code>router_node</code></strong> 的上一个检查点的 <strong><code>next</code></strong> 字段为 <strong><code>('router_node',)</code></strong>，据此筛选检查点</p>
<pre><code class="language-python">before_router_checkpoint = next(h for h in history_checkpoints if h.next == ('router_node',))
before_router_checkpoint
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">StateSnapshot(
    values={
        &quot;username&quot;: &quot;小黄&quot;,
        &quot;user_input&quot;: &quot;帮我写一首关于布偶猫的七言绝句&quot;,
    },
    next=(&quot;router_node&quot;,),
    config={
        &quot;configurable&quot;: {
            &quot;thread_id&quot;: &quot;123&quot;,
            &quot;checkpoint_ns&quot;: &quot;&quot;,
            &quot;checkpoint_id&quot;: &quot;1f1655e8-6871-6073-8000-ddcbe680da21&quot;,
        }
    },
    metadata={
        &quot;source&quot;: &quot;loop&quot;,
        &quot;step&quot;: 0,
        &quot;parents&quot;: {},
    },
    created_at=&quot;2026-06-11T06:26:48.352564+00:00&quot;,
    parent_config={
        &quot;configurable&quot;: {
            &quot;thread_id&quot;: &quot;123&quot;,
            &quot;checkpoint_ns&quot;: &quot;&quot;,
            &quot;checkpoint_id&quot;: &quot;1f1655e8-686b-66c7-bfff-24e0e8d44f4c&quot;,
        }
    },
    tasks=(
        PregelTask(
            id=&quot;fb88436e-b163-6308-6341-7d8368e041f9&quot;,
            name=&quot;router_node&quot;,
            path=(&quot;__pregel_pull&quot;, &quot;router_node&quot;),
            error=None,
            interrupts=(),
            state=None,
            result={
                &quot;topic&quot;: &quot;布偶猫&quot;,
                &quot;mode&quot;: &quot;poem&quot;,
            },
        ),
    ),
    interrupts=(),
)
</code></pre>
<p>该检查点表示：</p>
<blockquote>
<p>用户输入已经写入状态，下一步将要执行 <strong><code>router_node</code></strong>。</p>
</blockquote>
<p>从这个检查点分叉，本文采用两种方案（还可以有别的方案）：</p>
<ol>
<li>修改输入，让 <strong><code>router_node</code></strong> 重新执行。见 <strong><code>6.5.4.2.3.</code></strong> 节</li>
<li>直接伪造 <strong><code>router_node</code></strong> 的输出，从而跳过 <strong><code>router_node</code></strong>。见 <strong><code>6.5.4.2.4.</code></strong> 节</li>
</ol>
<h5 id="6-5-4-2-3-只改状态">6.5.4.2.3. 只改状态</h5>
<h6 id="6-5-4-2-3-1-更新配置">6.5.4.2.3.1. 更新配置</h6>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">change_input_config = graph.update_state(
    config=before_router_checkpoint.config,
    values={&quot;user_input&quot;: &quot;帮我写一个关于布偶猫的笑话&quot;},
    as_node=START
)
change_input_config
</code></pre>
<p>这里把 <strong><code>as_node</code></strong> 设置为 <strong><code>START</code></strong>，表示：</p>
<blockquote>
<p>把这次状态更新视为 <strong><code>START</code></strong> 的输出。</p>
</blockquote>
<p>因此，后续运行时会从 <strong><code>START</code></strong> 的后继节点继续推进，也就是重新执行 <strong><code>router_node</code></strong>。</p>
<p><strong>上述代码运行结果如下</strong></p>
<pre><code class="language-json">{
    &quot;configurable&quot;: {
        &quot;thread_id&quot;: &quot;123&quot;,
        &quot;checkpoint_ns&quot;: &quot;&quot;,
        &quot;checkpoint_id&quot;: &quot;1f1655f8-86fb-6dd1-8001-9e7808dfaf1a&quot;,
    }
}
</code></pre>
<p>执行 <strong><code>update_state()</code></strong> 后会返回一个新的配置，其中包含新的 <strong><code>checkpoint_id</code></strong>。这个新的 <strong><code>checkpoint_id</code></strong> 不同于原有历史检查点，说明 <strong><code>update_state()</code></strong> 创建了一个新的检查点分支。</p>
<p>需要注意：</p>
<blockquote>
<p><strong><code>update_state()</code></strong> 返回的是新检查点的配置，不是新状态本身。</p>
</blockquote>
<p>新的状态已经被记录在检查点后端，运行时生效。可以执行以下代码查看新建的检查点快照：</p>
<pre><code class="language-python">graph.get_state(change_input_config)
</code></pre>
<p>此处不再演示。</p>
<h6 id="6-5-4-2-3-2-重新运行">6.5.4.2.3.2. 重新运行</h6>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">graph.invoke(None, change_input_config)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">2026-06-11 14:13:44.267 | INFO     | __main__:router_node:31 - 路由节点已执行
2026-06-11 14:13:45.222 | INFO     | __main__:router_node:38 - 路由结果-res: {'topic': '布偶猫', 'mode': 'joke'}
2026-06-11 14:13:45.223 | INFO     | __main__:router:43 - 路由函数已执行
2026-06-11 14:13:45.223 | INFO     | __main__:router:48 - 路由至 node_joke 节点
2026-06-11 14:13:45.225 | INFO     | __main__:node_joke:64 - node_joke 已执行
{
    &quot;username&quot;: &quot;小黄&quot;,
    &quot;user_input&quot;: &quot;帮我写一个关于布偶猫的笑话&quot;,
    &quot;output&quot;: (
        &quot;一只布偶猫照镜子，问：“魔镜魔镜，谁是世界上最美的猫？”  \n&quot;
        &quot;镜子没说话。  \n&quot;
        &quot;猫急了：“快说呀！不然我卖萌把你甜碎。”  \n&quot;
        &quot;镜子终于开口：“……我死机了，重启中。”&quot;
    ),
}
</code></pre>
<p>由运行结果可知：</p>
<ol>
<li><strong><code>user_input</code></strong> 已经从“帮我写一首关于布偶猫的七言绝句”变成“帮我写一个关于布偶猫的笑话”。而 <strong><code>username</code></strong> 没有被更新，仍然保持原值 <strong><code>&quot;小黄&quot;</code></strong>。说明 <strong><code>values</code></strong> 是 <strong>状态更新</strong> 而非完整的 <strong>状态替换</strong>。</li>
<li><strong><code>router_node</code></strong> 开始的节点全部重新运行。由于新的输入被识别为笑话任务，因此后续进入 <strong><code>node_joke</code></strong>。</li>
</ol>
<h5 id="6-5-4-2-4-改状态且跳过某些节点">6.5.4.2.4. 改状态且跳过某些节点</h5>
<h6 id="6-5-4-2-4-1-更新配置">6.5.4.2.4.1. 更新配置</h6>
<p>如果不希望重新执行 <strong><code>router_node</code></strong>，而是直接指定 <strong><code>router_node</code></strong> 的输出，可以把 <strong><code>as_node</code></strong> 设置为 <strong><code>&quot;router_node&quot;</code></strong>。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">skip_router_config = graph.update_state(
    config=before_router_checkpoint.config,
    values={&quot;topic&quot;: &quot;狸花猫&quot;, &quot;mode&quot;: &quot;笑话&quot;},
    as_node=&quot;router_node&quot;
)
skip_router_config
</code></pre>
<ol>
<li>
<p>此处把 <strong><code>{&quot;topic&quot;: &quot;狸花猫&quot;, &quot;mode&quot;: &quot;笑话&quot;}</code></strong> 当作 <strong><code>router_node</code></strong> 的输出。</p>
</li>
<li>
<p>因此，后续运行时，<strong><code>router_node</code></strong> 本身不会被重新执行，从它的后继节点继续推进。</p>
</li>
<li>
<p>由于 <strong><code>router_node</code></strong> 挂载了条件边：</p>
<pre><code class="language-python">builder.add_conditional_edges(
    &quot;router_node&quot;,
    router,
    path_map=[&quot;node_poem&quot;, &quot;node_joke&quot;, &quot;node_default&quot;]
)
</code></pre>
<p>所以当 <strong><code>update_state()</code></strong> 把 <strong><code>values</code></strong> 视为 <strong><code>router_node</code></strong> 的输出时，条件路由函数 <strong><code>router</code></strong> 也会根据这次更新后的状态计算后续分支。</p>
<p>这里把 <strong><code>mode</code></strong> 设置为 <strong><code>&quot;笑话&quot;</code></strong>，<strong><code>router</code></strong> 函数命中兜底分支 <strong><code>node_default</code></strong>。</p>
</li>
</ol>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">2026-06-11 14:14:02.223 | INFO     | __main__:router:43 - 路由函数已执行
2026-06-11 14:14:02.223 | INFO     | __main__:router:51 - 路由至兜底节点
{
    &quot;configurable&quot;: {
        &quot;thread_id&quot;: &quot;123&quot;,
        &quot;checkpoint_ns&quot;: &quot;&quot;,
        &quot;checkpoint_id&quot;: &quot;1f1655cb-de15-6c4e-8001-ca03b011e993&quot;,
    }
}
</code></pre>
<p>可以看到：</p>
<ol>
<li>
<p><strong><code>update_state()</code></strong> 创建了一个新的检查点，返回的 <strong><code>skip_router_config</code></strong> 指向这个新检查点。</p>
</li>
<li>
<p>日志中出现了：</p>
<pre><code class="language-text">路由函数已执行
路由至兜底节点
</code></pre>
<p>这说明 <strong><code>router_node</code></strong> 的节点函数没有被重新执行，但与 <strong><code>router_node</code></strong> 条件边相关的路由函数被执行了。</p>
</li>
<li>
<p><strong><code>update_state()</code></strong> 底层会根据 <strong><code>as_node</code></strong> 找到对应图节点的 <strong><code>writers</code></strong>。</p>
<p>对于 <strong><code>router_node</code></strong>，<strong><code>writers</code></strong> 包括状态写入器和条件边对应的路由逻辑。</p>
</li>
<li>
<p>执行 <strong><code>update_state()</code></strong> 时，传入的 <strong><code>values</code></strong> 会被当作 <strong><code>router_node</code></strong> 的输出，交给该节点的 <strong><code>writers</code></strong> 处理。</p>
<p>状态写入器会把 <strong><code>values</code></strong> 转换为对状态 <strong><code>Channel</code></strong> 的写入；条件边对应的路由逻辑会根据当前状态计算后继分支，并生成类似 <strong><code>branch:to:node_default</code></strong> 这样的触发通道写入。</p>
</li>
<li>
<p>随后，<strong><code>update_state()</code></strong> 会把这些写入应用到检查点中，并保存一个新的检查点。</p>
<p>因此，状态更新和路由结果已经记录在 <strong><code>skip_router_config</code></strong> 指向的新检查点中。后续再调用：</p>
<pre><code class="language-python">graph.invoke(None, skip_router_config)
</code></pre>
<p>时，运行图会基于这个新检查点继续执行，被触发的后继节点也会继续运行。</p>
</li>
</ol>
<p>综上，<strong><code>graph.update_state()</code></strong> 的作用可以理解为：</p>
<blockquote>
<p>不执行指定节点的节点函数本身，而是把传入的 <strong><code>values</code></strong> 当作该节点已经产生的输出，执行该节点对应的 <strong><code>writers</code></strong>，应用状态写入和可能存在的路由写入，然后生成一个新的检查点。</p>
</blockquote>
<p>因此，在本例中可以看到：<strong><code>router_node</code></strong> 的节点逻辑没有被执行，但它挂载的路由函数被执行，状态更新也已经生效。</p>
<h6 id="6-5-4-2-4-2-重新运行">6.5.4.2.4.2. 重新运行</h6>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">graph.invoke(None, skip_router_config)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">2026-06-11 16:49:42.501 | INFO     | __main__:node_default:73 - node_default 已执行
{
  &quot;username&quot;: &quot;小黄&quot;,
  &quot;user_input&quot;: &quot;帮我写一首关于布偶猫的七言绝句&quot;,
  &quot;output&quot;: &quot;无法处理的任务类型&quot;
}
</code></pre>
<p>和分析一致，触发了兜底分支。</p>
<p>这种方式常用于测试场景。例如：</p>
<ul>
<li>跳过不稳定的大模型路由节点。</li>
<li>手动指定路由结果，测试不同分支。</li>
<li>修改历史状态，验证后续节点能否正常运行。</li>
<li>基于同一个历史检查点探索多条可能的执行路径。</li>
</ul>
<p>不过在正式业务流程中，应谨慎使用这种方式。因为它是在伪造某个节点的输出，如果伪造的状态不符合后续节点的预期，可能导致逻辑混乱，甚至抛出运行时异常。</p>
<h1 id="7-图记忆管理">7. 图记忆管理</h1>
<p><strong><code>Agent</code></strong> 的三种记忆：</p>
<ul>
<li>
<p><strong>短期记忆</strong>：通过运行时状态 <strong><code>State</code></strong> 访问，并由检查点存储器 <strong><code>Checkpointer</code></strong> 保存，它按照 <strong><code>thread_id</code></strong> 组织，可以实现线程内的记忆共享。</p>
</li>
<li>
<p><strong>长期记忆</strong>：通过长期记忆存储器 <strong><code>Store</code></strong> 访问和存储。数据通常按照元组类型的命名空间组织，以键值对的形式存储，提供跨会话的记忆共享。</p>
<ul>
<li>
<p>可以按照命名空间+可选的过滤条件精确检索</p>
</li>
<li>
<p>也可以通过语义模糊匹配。</p>
</li>
</ul>
</li>
<li>
<p><strong>运行时上下文</strong>：通过上下文对象 <strong><code>Context</code></strong> 访问，只对本次调用生效，不会被持久化。</p>
<p>它更适合传递本次运行所需的外部依赖或调用参数，如用户名、模型配置、数据库连接、权限信息等。</p>
</li>
</ul>
<p>我们不止一次提到，<strong><code>Agent</code></strong> 底层就是一个简易的 <strong><code>ReAct</code></strong> 架构的 <strong><code>LangGraph</code></strong> 状态图，所以 <strong><code>Agent</code></strong> 的记忆机制本质上就是 <strong><code>LangGraph</code></strong> 状态图的记忆机制。</p>
<p>生产环境建议用基于持久化数据库的记忆存储器，如 <strong><code>PostgresSaver</code></strong> 和 <strong><code>PostgresStore</code></strong>。</p>
<h2 id="7-1-短期记忆">7.1. 短期记忆</h2>
<p>只要编译图时启用了 <strong><code>Checkpointer</code></strong>，并且调用图时复用同一个 <strong><code>thread_id</code></strong>，<strong><code>LangGraph</code></strong> 就可以在多次调用之间共享同一会话线程下的状态。</p>
<p>我们一直在用，不在赘述。</p>
<h2 id="7-2-长期记忆">7.2. 长期记忆</h2>
<p>长期记忆存储器在编译图时通过 <strong><code>store</code></strong> 参数传递，图节点中可以通过 <strong><code>Runtime</code></strong> 对象访问。</p>
<p>本节会在一个案例中同时用到短期记忆和长期记忆，两者均使用 <strong><code>PostgreSQL</code></strong> 作为持久化后端——短期记忆通过 <strong><code>PostgresSaver</code></strong>，长期记忆通过 <strong><code>PostgresStore</code></strong>，数据不会随进程退出而丢失。</p>
<h3 id="7-2-1-准备长期记忆">7.2.1. 准备长期记忆</h3>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Final, Tuple
from langgraph.store.postgres import PostgresStore

# 与 6.2.4 节使用同一数据库
DB_URL = &quot;postgresql://langgraph_user:123456@localhost:5432/langgraph_db?sslmode=disable&quot;
with PostgresStore.from_conn_string(DB_URL) as store:
    # setup() 创建长期记忆所需的表结构，幂等操作
    store.setup()

    # 命名空间 - 层级结构：(&quot;users&quot;, &quot;用户名&quot;)
    # Final 的作用是告诉静态类型检查器，该变量不应该被重新赋值，但 python 运行时不会阻止重新赋值
    USERS_NS: Final[Tuple[str]] = (&quot;users&quot;, )
    PREFERENCES_KEY: Final[str] = &quot;preferences&quot;

    # 三层: (领域, 用户实体)
    # 每个 key 存储该用户的不同类型数据
    namespace1 = (*USERS_NS, &quot;Alice&quot;)
    value1 = {
        &quot;course&quot;: &quot;计算机组成原理&quot;,
        &quot;sports&quot;: &quot;跑步&quot;,
        &quot;food&quot;: &quot;紫光园奶皮子酸奶&quot;
    }

    namespace2 = (*USERS_NS, &quot;Bob&quot;)
    value2 = {
        &quot;course&quot;: &quot;数字电路与模拟电路&quot;,
        &quot;sports&quot;: &quot;跑步&quot;,
        &quot;food&quot;: &quot;奶皮子糖葫芦&quot;
    }

    namespace3 = (*USERS_NS, &quot;Black&quot;)
    value3 = {
        &quot;course&quot;: &quot;数字电路与模拟电路&quot;,
        &quot;sports&quot;: &quot;羽毛球&quot;,
        &quot;food&quot;: &quot;紫光园奶皮子酸奶&quot;
    }

    store.put(namespace1, PREFERENCES_KEY, value1)
    store.put(namespace2, PREFERENCES_KEY, value2)
    store.put(namespace3, PREFERENCES_KEY, value3)

    for item in store.search(USERS_NS):
        print(item)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">Item(namespace=['users', 'Alice'], key='preferences', value={'course': '计算机组成原理', 'sports': '跑步', 'food': '紫光园奶皮子酸奶'}, created_at='2026-06-12T09:54:50.593300+00:00', updated_at='2026-06-12T09:54:50.593303+00:00', score=None)
Item(namespace=['users', 'Bob'], key='preferences', value={'course': '数字电路与模拟电路', 'sports': '跑步', 'food': '奶皮子糖葫芦'}, created_at='2026-06-12T09:54:50.593329+00:00', updated_at='2026-06-12T09:54:50.593329+00:00', score=None)
Item(namespace=['users', 'Black'], key='preferences', value={'course': '数字电路与模拟电路', 'sports': '羽毛球', 'food': '紫光园奶皮子酸奶'}, created_at='2026-06-12T09:54:50.593348+00:00', updated_at='2026-06-12T09:54:50.593348+00:00', score=None)
</code></pre>
<p>这里使用 <strong><code>USERS_NS</code></strong> 作为顶层命名空间，<code>store.search(USERS_NS)</code> 会查询所有以 <strong><code>(&quot;users&quot;,)</code></strong> 开头的记忆数据。返回的每条数据都是一个 <strong><code>Item</code></strong> 实例。</p>
<p>需要注意：</p>
<ol>
<li>
<p><strong><code>namespace</code></strong> 使用元组是硬性约束，天然表达层级结构。本例使用两层命名空间 <strong><code>(&quot;users&quot;, &quot;Alice&quot;)</code></strong>，清晰区分了<strong>领域（<code>users</code>）<strong>和</strong>实体（用户名）</strong>，而具体存储什么类型的数据则由 <strong><code>key</code></strong> 参数表达（如 <strong><code>&quot;preferences&quot;</code></strong>）。</p>
</li>
<li>
<p><strong><code>(*USERS_NS, &quot;Alice&quot;)</code></strong> 的写法表示对已有元组进行解包，再拼接新的命名空间片段。</p>
</li>
<li>
<p><strong><code>PostgresStore</code></strong> 将长期记忆持久化到 <strong><code>PostgreSQL</code></strong> 中，程序重启后数据不会丢失，适合生产环境。本节使用和 <strong>6.2.4节</strong>、<strong>6.5.4.1节</strong> 相同的数据库实例。</p>
</li>
<li>
<p>如果希望支持语义检索，需要在 <strong><code>Store</code></strong> 中配置索引和 <strong><code>embedding</code></strong> 函数。未配置索引时，<strong><code>search()</code></strong> 只能按照命名空间和过滤条件检索。</p>
</li>
</ol>
<h3 id="7-2-2-访问长期记忆">7.2.2. 访问长期记忆</h3>
<p>长期记忆存储器可以通过节点函数中的 <strong><code>runtime.store</code></strong> 访问。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.postgres import PostgresSaver
from langgraph.store.postgres import PostgresStore
from langgraph.graph.message import MessagesState
from langgraph.runtime import Runtime
from langchain.messages import SystemMessage, HumanMessage, AIMessage, ToolMessage
from langchain_deepseek import ChatDeepSeek

from loguru import logger
from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(MessagesState):
    username: str
    user_input: str
    output: str
    preferences: dict[str, str] # 用户偏好

def check_preferences_node(state: OverAllState, runtime: Runtime) -&gt; OverAllState:
    # 从短期记忆中获取用户名
    username = state[&quot;username&quot;]
    # 获取长期记忆存储器 store
    store = runtime.store

    # 根据用户信息拼接命名空间，查询用户偏好
    namespace = (*USERS_NS, username)
    key = PREFERENCES_KEY
    item = store.get(namespace, key)

    if not item:
        logger.warning(&quot;长期记忆中没有 {} 的偏好数据&quot;, username)
        return {}

    logger.info(&quot;长期记忆中保存的用户偏好: {}&quot;, item.value)
    return {
        &quot;preferences&quot;: item.value
    }

# 路由函数，如果状态中没有用户偏好，则从长期记忆中查询，否则直接执行模型节点
def router(state: OverAllState) -&gt; Literal[&quot;check_preferences_node&quot;, &quot;llm_node&quot;]:
    if not state.get(&quot;preferences&quot;):
        logger.info(&quot;需要从长期记忆中查询用户偏好&quot;)
        return &quot;check_preferences_node&quot;
    logger.info(&quot;用户偏好已存在，不必查询&quot;)
    return &quot;llm_node&quot;

def llm_node(state: OverAllState) -&gt; OverAllState:
    # 长期记忆中未必有用户偏好
    preferences = state.get(&quot;preferences&quot;, {})
    user_input = state[&quot;user_input&quot;]
    human_prompt = (f&quot;这是用户的偏好: \n{preferences}\n&quot;
              f&quot;这是用户的需求: \n{user_input}\n&quot;)
    system_prompt = &quot;请根据用户偏好解决用户需求&quot;

    # 获取历史消息列表，如果历史为空则初始化系统提示词
    messages: list[SystemMessage | HumanMessage | AIMessage | ToolMessage] = [
        SystemMessage(content=system_prompt)
    ] if not state.get(&quot;messages&quot;, []) else state[&quot;messages&quot;]

    model_response = model.invoke(messages + [HumanMessage(content=human_prompt)])
    output = model_response.content

    return {
        &quot;messages&quot;: messages + [HumanMessage(content=human_prompt), model_response],
        &quot;output&quot;: output
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;check_preferences_node&quot;, check_preferences_node)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_conditional_edges(START, router, path_map=[&quot;check_preferences_node&quot;, &quot;llm_node&quot;])
builder.add_edge(&quot;check_preferences_node&quot;, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, END)

# DB_URL 与 6.2.4 节一致
DB_URL = &quot;postgresql://langgraph_user:123456@localhost:5432/langgraph_db?sslmode=disable&quot;
# 编译时传递短期记忆和长期记忆存储器（均使用 PostgreSQL）
with PostgresSaver.from_conn_string(DB_URL) as checkpointer, \
     PostgresStore.from_conn_string(DB_URL) as store:
    checkpointer.setup()
    graph = builder.compile(checkpointer=checkpointer, store=store)
    from IPython.display import display
    display(graph)

    config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;345&quot;}}
    res = graph.invoke({&quot;username&quot;: &quot;Alice&quot;, &quot;user_input&quot;: &quot;我有点无聊，和我聊聊天吧&quot;}, config=config)

    print('=' * 30, '-&gt;  &lt;-', '=' * 30)
    print(res)

    # 第二次调用：复用同一个 thread_id，观察短期记忆缓存效果
    new_res = graph.invoke({&quot;user_input&quot;: &quot;推荐一下酸奶&quot;}, config=config)
    new_messages = new_res.pop(&quot;messages&quot;)
    print('=' * 30, '-&gt;  &lt;-', '=' * 30)
    print(new_res)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="/images/langgraph-study-notes/image-20260612184930979.png" alt="image-20260612184930979" /></p>
<pre><code class="language-json">2026-06-12 18:53:39.351 | INFO     | __main__:router:51 - 需要从长期记忆中查询用户偏好
2026-06-12 18:53:39.356 | INFO     | __main__:check_preferences_node:43 - 长期记忆中保存的用户偏好: {'course': '计算机组成原理', 'sports': '跑步', 'food': '紫光园奶皮子酸奶'}
============================== -&gt; 不包含消息列表的运行结果 &lt;- ==============================
{
    'username': 'Alice',
    'user_input': '我有点无聊，和我聊聊天吧',
    'output': '哈哈，好啊！正好你学了《计算机组成原理》，我有个脑洞问题想请教一下：如果让你把“跑步的节奏感”比喻成计算机里的一个部件（比如CPU、内存、流水线），你会怎么对应呢？😄 顺便，我推荐你边慢跑边思考这个问题，跑完再来份紫光园奶皮子酸奶犒劳自己，简直完美！',
    'preferences': {
        'course': '计算机组成原理',
        'sports': '跑步',
        'food': '紫光园奶皮子酸奶'
    }
}
============================== -&gt; 消息列表 &lt;- ==============================
================================ System Message ================================

请根据用户偏好解决用户需求
================================ Human Message =================================

这是用户的偏好:
{
    'course': '计算机组成原理',
    'sports': '跑步',
    'food': '紫光园奶皮子酸奶'
}
这是用户的需求:
我有点无聊，和我聊聊天吧

================================== Ai Message ==================================

哈哈，好啊！正好你学了《计算机组成原理》，我有个脑洞问题想请教一下：如果让你把“跑步的节奏感”比喻成计算机里的一个部件（比如CPU、内存、流水线），你会怎么对应呢？😄 顺便，我推荐你边慢跑边思考这个问题，跑完再来份紫光园奶皮子酸奶犒劳自己，简直完美！

</code></pre>
<p><strong>运行流程如下</strong>：</p>
<ol>
<li>第一次调用时，状态中还没有 <strong><code>preferences</code></strong>。</li>
<li>路由函数会路由至 <strong><code>check_preferences_node</code></strong>。</li>
<li><strong><code>check_preferences_node</code></strong> 从长期记忆存储器中读取用户偏好。</li>
<li>读取到的偏好会写入图状态，成为当前会话线程中的短期记忆。</li>
<li>后续节点可以直接从状态中读取 <strong><code>preferences</code></strong>。</li>
</ol>
<h3 id="7-2-3-再次调用">7.2.3. 再次调用</h3>
<p>在同一个 <strong><code>with</code></strong> 块中再次调用计算图，并复用同一个 <strong><code>thread_id</code></strong>，可以看到短期记忆的效果。该代码已包含在上节的 <strong><code>with</code></strong> 代码块末尾。</p>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">2026-06-12 18:54:12.446 | INFO     | __main__:router:53 - 用户偏好已存在，不必查询
============================== -&gt; 不包含消息列表的运行结果 &lt;- ==============================
{
    'username': 'Alice',
    'user_input': '推荐一下酸奶',
    'output': '哈哈，看来你对紫光园奶皮子酸奶是真爱啊！不过既然你点名要推荐酸奶，我得先问一句——你是想换换口味试试别的，还是想让我帮你吹爆紫光园这款？（毕竟按你的偏好，它简直是“计算机组成原理”级别的完美搭配：**奶皮子像CPU缓存一样浓缩精华，酸甜度像流水线调度一样平衡，吃完跑步时还能提供“数据预取”般的能量**😂）\n\n如果是**忠于原味**，那必须再安利一次紫光园：浓稠拉丝、奶香爆炸，跑完步来一罐，简直像给肌肉做了个“中断响应”！如果是**想尝鲜**，我推荐：\n1. **兰格格天边的额吉**——蒙古风味，酸度低、像RISC精简指令集一样纯净；\n2. **简爱父爱配方**——无添加糖，适合跑步后控糖，像低功耗处理器；\n3. **卡士原态酪乳**——口感像固态硬盘般扎实绵密，适合当夜宵。\n\n不过……你确定要换掉紫光园吗？🤔',
    'preferences': {
        'course': '计算机组成原理',
        'sports': '跑步',
        'food': '紫光园奶皮子酸奶'
    }
}
============================== -&gt; 消息列表 &lt;- ==============================
================================ System Message ================================

请根据用户偏好解决用户需求
================================ Human Message =================================

这是用户的偏好:
{
    'course': '计算机组成原理',
    'sports': '跑步',
    'food': '紫光园奶皮子酸奶'
}
这是用户的需求:
我有点无聊，和我聊聊天吧

================================== Ai Message ==================================

哈哈，好啊！正好你学了《计算机组成原理》，我有个脑洞问题想请教一下：如果让你把“跑步的节奏感”比喻成计算机里的一个部件（比如CPU、内存、流水线），你会怎么对应呢？😄 顺便，我推荐你边慢跑边思考这个问题，跑完再来份紫光园奶皮子酸奶犒劳自己，简直完美！
================================ Human Message =================================

这是用户的偏好:
{
    'course': '计算机组成原理',
    'sports': '跑步',
    'food': '紫光园奶皮子酸奶'
}
这是用户的需求:
推荐一下酸奶

================================== Ai Message ==================================

哈哈，看来你对紫光园奶皮子酸奶是真爱啊！不过既然你点名要推荐酸奶，我得先问一句——你是想换换口味试试别的，还是想让我帮你吹爆紫光园这款？（毕竟按你的偏好，它简直是“计算机组成原理”级别的完美搭配：**奶皮子像CPU缓存一样浓缩精华，酸甜度像流水线调度一样平衡，吃完跑步时还能提供“数据预取”般的能量**😂）

如果是**忠于原味**，那必须再安利一次紫光园：浓稠拉丝、奶香爆炸，跑完步来一罐，简直像给肌肉做了个“中断响应”！如果是**想尝鲜**，我推荐：
1. **兰格格天边的额吉**——蒙古风味，酸度低、像RISC精简指令集一样纯净；
2. **简爱父爱配方**——无添加糖，适合跑步后控糖，像低功耗处理器；
3. **卡士原态酪乳**——口感像固态硬盘般扎实绵密，适合当夜宵。

不过……你确定要换掉紫光园吗？🤔
</code></pre>
<p>第二次运行前，状态中已经包含：</p>
<ul>
<li><strong><code>username</code></strong></li>
<li><strong><code>preferences</code></strong></li>
<li><strong><code>messages</code></strong></li>
<li>上一次调用生成的 <strong><code>output</code></strong></li>
</ul>
<p>因此路由函数会直接判断：</p>
<pre><code class="language-python">logger.info(&quot;用户偏好已存在，不必查询&quot;)
</code></pre>
<p>因此，本次调用不会重新访问长期记忆存储器，而是直接复用当前会话状态中缓存的用户偏好。</p>
<p>这正是短期记忆和长期记忆协作的典型模式：</p>
<blockquote>
<p>第一次调用从长期记忆中读取数据，并写入短期记忆；后续同一会话线程内优先复用短期记忆，避免重复查询长期记忆。</p>
</blockquote>
<h2 id="7-3-运行时上下文">7.3. 运行时上下文</h2>
<p>在某些场景下，我们希望在调用图时传入一些<strong>仅对当次调用有效</strong>的信息，比如当前登录用户、请求来源、调用方标识等。这些信息不适合放入图状态（图状态会被持久化并在同一会话中跨调用共享），而应该通过**运行时上下文（Runtime Context）**传递。</p>
<p>运行时上下文的特点是：</p>
<blockquote>
<p><strong>仅对本次调用生效，不会被持久化，也不会在同一会话的下一次调用中自动恢复。</strong></p>
</blockquote>
<p>运行时上下文的使用方式：</p>
<ol>
<li>初始化状态图时，使用 <strong><code>context_schema</code></strong> 定义上下文类型。</li>
<li>调用图时通过 <strong><code>context</code></strong> 参数传入上下文对象。</li>
<li>节点或路由函数中通过 <strong><code>runtime.context</code></strong> 访问上下文。</li>
</ol>
<h3 id="7-3-1-访问运行时上下文">7.3.1. 访问运行时上下文</h3>
<p>下面通过一个简单的客服机器人案例演示运行时上下文的用法。</p>
<p><strong>场景</strong>：不同的用户调用同一个机器人，机器人根据运行时上下文中的<strong>用户名</strong>和<strong>会员等级</strong>，生成不同风格的回复。</p>
<p><strong>图结构</strong>：</p>
<pre><code class="language-text">START -&gt; llm_node -&gt; END
</code></pre>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from dataclasses import dataclass
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import MessagesState
from langgraph.runtime import Runtime
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from loguru import logger
from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

# 1. 定义运行时上下文类型
@dataclass
class UserContext:
    username: str
    membership_level: str  # &quot;普通用户&quot; / &quot;VIP&quot;

# 2. 定义图状态
class OverAllState(MessagesState):
    user_input: str
    output: str

# 3. 定义节点：通过 runtime.context 访问运行时上下文
def llm_node(state: OverAllState, runtime: Runtime[UserContext]) -&gt; OverAllState:
    runtime_context = runtime.context

    if runtime_context:
        username = runtime_context.username
        level = runtime_context.membership_level
        logger.info(f&quot;当前用户: {username}, 会员等级: {level}&quot;)

        if level == &quot;VIP&quot;:
            system_prompt = f&quot;你是高级客服助理。当前VIP用户是{username}，请使用尊称'您'，语气热情周到，回复末尾加上'🎖️VIP专属服务'。&quot;
        else:
            system_prompt = f&quot;你是普通客服助理。当前用户是{username}，请友好简洁地回复。&quot;
    else:
        logger.warning(&quot;运行时上下文为空，使用默认风格&quot;)
        system_prompt = &quot;你是客服助理，请友好简洁地回复。&quot;

    user_input = state[&quot;user_input&quot;]
    messages = state.get(&quot;messages&quot;, [])
    response = model.invoke(
        [HumanMessage(content=system_prompt)] +
        messages +
        [HumanMessage(content=user_input)]
    )

    return {
        &quot;messages&quot;: [response],
        &quot;output&quot;: response.content
    }

# 4. 构建图，传入 context_schema
builder = StateGraph(state_schema=OverAllState, context_schema=UserContext)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_edge(START, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, END)

graph = builder.compile()

# === 第一次调用：传入运行时上下文（VIP用户） ===
print(&quot;=&quot; * 30, &quot;第一次调用：VIP用户&quot;, &quot;=&quot; * 30)
config = {&quot;configurable&quot;: {&quot;thread_id&quot;: &quot;demo-7.3&quot;}}
res = graph.invoke(
    {&quot;user_input&quot;: &quot;你好，帮我查一下最近有什么优惠活动&quot;},
    config=config,
    context=UserContext(username=&quot;Alice&quot;, membership_level=&quot;VIP&quot;)
)

print(f&quot;output: {res['output']}&quot;)
print()

# === 第二次调用：不传运行时上下文 ===
print(&quot;=&quot; * 30, &quot;第二次调用：不传上下文&quot;, &quot;=&quot; * 30)
res2 = graph.invoke(
    {&quot;user_input&quot;: &quot;再帮我看看有没有新品&quot;},
    config=config
)

print(f&quot;output: {res2['output']}&quot;)

from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<p><img src="images%5Cimage-20260709174052171.png" alt="image-20260709174052171" /></p>
<pre><code class="language-json">2026-07-09 17:40:27.208 | INFO     | __main__:llm_node:39 - 当前用户: Alice, 会员等级: VIP
============================== 第一次调用：VIP用户 ==============================
2026-07-09 17:40:29.116 | WARNING  | __main__:llm_node:46 - 运行时上下文为空，使用默认风格
output: 尊敬的Alice您好！非常感谢您的咨询🎉 我们近期为VIP用户准备了多项专属优惠活动，包括但不限于：1）满额立减（消费满888元立减88元）；2）积分翻倍（指定商品享3倍积分）；3）限时闪购（每日10:00-12:00精选好物低至5折）。如需了解具体参与商品或为您定制个性化推荐，请随时告知，我将全力为您服务！🎖️VIP专属服务

============================== 第二次调用：不传上下文 ==============================
output: 您好！目前新品信息尚未更新，建议您关注官方渠道获取最新动态。如有其他问题，欢迎随时咨询，我会尽力为您解答！😊
</code></pre>
<p><strong>分析</strong></p>
<ol>
<li>
<p><strong>第一次调用</strong>传入了 <strong><code>context=UserContext(username=”Alice”, membership_level=”VIP”)</code></strong>，节点通过 <strong><code>runtime.context</code></strong> 读取到用户名和会员等级，生成了VIP风格的回复，末尾带上了 <strong><code>🎖️VIP专属服务</code></strong> 标识。</p>
</li>
<li>
<p><strong>第二次调用</strong>使用了相同的 <strong><code>thread_id</code></strong>，但没有传入 <strong><code>context</code></strong>。此时 <strong><code>runtime.context</code></strong> 为 <strong><code>None</code></strong>，节点走入了默认风格分支，回复中不再有VIP标识。</p>
</li>
</ol>
<p>这个对比可以清晰地看到：</p>
<blockquote>
<p><strong>运行时上下文仅对当次调用生效，不会被持久化。即使使用相同的 <code>thread_id</code>，下一次调用也不会自动继承上一次的上下文。</strong></p>
</blockquote>
<h3 id="7-3-2-小结">7.3.2. 小结</h3>
<p>运行时上下文适合传递以下信息：</p>
<table>
<thead>
<tr>
<th>适合放入运行时上下文</th>
<th>不适合（应放入图状态）</th>
</tr>
</thead>
<tbody>
<tr>
<td>当前登录用户信息</td>
<td>需要在多轮对话间共享的数据</td>
</tr>
<tr>
<td>请求来源（Web / API / 小程序）</td>
<td>需要在检查点中恢复的执行进度</td>
</tr>
<tr>
<td>调用方标识、Trace ID</td>
<td>需要跨调用持久化的业务数据</td>
</tr>
<tr>
<td>本次调用的功能开关</td>
<td>节点间需要传递的计算结果</td>
</tr>
</tbody>
</table>
<p>简单来说：</p>
<blockquote>
<p><strong>跨调用共享、需要持久化的数据 → 放入 State；仅当次调用有效的信息 → 放入 Runtime Context。</strong></p>
</blockquote>
<h2 id="7-4-node总结-langgraph的节点">7.4. Node总结：LangGraph的节点</h2>
<h3 id="7-4-1-节点的本质">7.4.1. 节点的本质</h3>
<p>从用户视角看，<strong><code>LangGraph</code></strong> 的节点通常是一个可调用对象，最常见的是同步或异步 <strong><code>Python</code></strong> 函数：</p>
<pre><code class="language-python">def sync_node(state: State) -&gt; State:
    ...

async def async_node(state: State) -&gt; State:
    ...
</code></pre>
<p>除此之外，节点也可以是符合要求的其他可调用对象或 <strong><code>Runnable</code></strong> 实例。</p>
<p>从源码实现角度看，节点最终会被转换或包装为可运行对象，并作为 <strong><code>PregelNode.bound</code></strong> 保存。</p>
<h3 id="7-4-2-节点函数的完整形态">7.4.2. 节点函数的完整形态</h3>
<p>状态图节点的第一个位置参数用于接收图状态。</p>
<p>此外，<strong><code>LangGraph</code></strong> 还可以在运行时注入<strong>运行时配置</strong>、<strong>运行时对象</strong>和<strong>流式写入器</strong>等参数。</p>
<p>普通图节点通常使用以下四个参数：</p>
<ul>
<li>
<p><strong><code>state</code></strong>：输入节点的图状态。<strong><code>state</code></strong> 是位置传参，因此参数名称不重要，但通常约定命名为 <strong><code>state</code></strong></p>
</li>
<li>
<p><strong><code>config</code></strong>：状态图的运行时配置，它是一个 <strong><code>RunnableConfig</code></strong> 实例，底层运行时在节点函数执行前通过关键字传参动态注入。</p>
<p>可以通过 <strong><code>config</code></strong> 访问当前线程的 <strong><code>thread_id</code></strong>、超步序号、递归限制等配置信息。如下</p>
<pre><code class="language-python">config[&quot;configurable&quot;][&quot;thread_id&quot;]
config[&quot;recursion_limit&quot;]
config[&quot;metadata&quot;]
</code></pre>
</li>
<li>
<p><strong><code>runtime</code></strong>：状态图的运行时对象，可以通过它访问运行时上下文、长期记忆存储器等信息。底层运行时在节点函数执行前通过关键字传参动态注入。</p>
</li>
<li>
<p><strong><code>writer</code></strong>：流式写入器，通常用于自定义流式输出。详见流处理章节。</p>
</li>
</ul>
<p>典型写法如下：</p>
<pre><code class="language-python">def node(state: State, config: RunnableConfig, runtime: Runtime[Context]) -&gt; State:
    ...
</code></pre>
<ul>
<li>编译图时传入 <strong><code>checkpointer</code></strong>，并在调用时传入 <strong><code>config</code></strong>，可以在节点中通过 <strong><code>config</code></strong> 间接访问当前调用的配置信息，如 <strong><code>thread_id</code></strong>。</li>
<li>编译图时传入 <strong><code>store</code></strong> 后，可以在节点中通过 <strong><code>runtime.store</code></strong> 访问长期记忆存储器。</li>
<li>初始化状态图时传入 <strong><code>context_schema</code></strong>，并在调用时传入 <strong><code>context</code></strong> 后，可以在节点中通过 <strong><code>runtime.context</code></strong> 访问运行时上下文。</li>
</ul>
<h3 id="7-4-3-节点的触发和执行">7.4.3. 节点的触发和执行</h3>
<h4 id="7-4-3-1-节点的触发">7.4.3.1. 节点的触发</h4>
<h5 id="7-4-3-1-1-用户视角">7.4.3.1.1. 用户视角</h5>
<p>从用户层面看，节点由图中的控制流关系（普通边、条件边和动态控制指令）触发：</p>
<ul>
<li>
<p>普通边决定固定的后继节点。</p>
</li>
<li>
<p>条件边根据路由函数的返回值决定后继节点。</p>
<ul>
<li>可以返回普通节点名称</li>
<li>也可以返回 <strong><code>Send</code></strong> 实例，从而动态派发多个并行任务，常用于 <strong><code>Map-Reduce</code></strong> 场景。</li>
</ul>
</li>
<li>
<p><strong><code>Command.goto</code></strong> 可以在节点返回时动态指定后继节点。</p>
</li>
</ul>
<h5 id="7-4-3-1-2-源码视角">7.4.3.1.2. 源码视角</h5>
<p>从源码实现角度看，<strong><code>LangGraph</code></strong> 会将图状态和节点之间的触发关系组织为通道。</p>
<p>其中，状态字段通常对应状态通道，节点之间的控制流关系则通过触发通道或屏障通道表示。对于普通的单节点触发关系，目标节点通常会订阅如下形式的内部通道：</p>
<pre><code class="language-text">branch:to:&lt;node_name&gt;
</code></pre>
<p>例如，对于普通边：</p>
<pre><code class="language-python">builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
</code></pre>
<p>运行时 <strong><code>node_a</code></strong> 的写入器会向以下通道写入数据：</p>
<pre><code class="language-text">branch:to:node_b
</code></pre>
<p>而 <strong><code>node_b</code></strong> 会将该通道注册为自己的触发通道。</p>
<p>条件边和 <strong><code>Command.goto</code></strong> 最终也会根据目标节点生成相应的触发通道写入。<strong><code>Send</code></strong> 则会生成动态派发任务所需的 <strong><code>Send</code></strong> 数据包。</p>
<p>对于多个前驱节点共同汇聚到同一个节点的情况，底层还可能使用类似下面的屏障通道：</p>
<pre><code class="language-text">join:&lt;node_a&gt;+&lt;node_b&gt;:&lt;node_c&gt;
</code></pre>
<p>用于等待指定的前驱节点全部完成。</p>
<p>当某个节点订阅的触发通道在当前超步中产生新的写入后，该节点会在下一超步被调度。</p>
<h4 id="7-4-3-2-节点的执行">7.4.3.2. 节点的执行</h4>
<h5 id="7-4-3-2-1-用户视角">7.4.3.2.1. 用户视角</h5>
<p>从用户层面看，节点执行过程可以理解为：</p>
<ol>
<li>
<p><strong><code>LangGraph</code></strong> 从当前图状态中读取节点需要的字段，并构造节点输入。</p>
</li>
<li>
<p>调用节点函数，执行其业务逻辑。</p>
</li>
<li>
<p>节点函数返回状态更新、<strong><code>Command</code></strong> 或其他受支持的结果。</p>
</li>
<li>
<p><strong><code>LangGraph</code></strong> 将节点返回值转换为状态通道和控制通道的写入条目。</p>
</li>
<li>
<p>如果当前节点挂载了条件边，则执行条件边路由逻辑，根据包含当前节点更新的状态决定后继节点。</p>
</li>
<li>
<p>当前超步的所有任务执行完毕后，<strong><code>LangGraph</code></strong> 汇总这些写入，并根据各状态字段的 <strong><code>Reducer</code></strong> 规则更新图状态。</p>
</li>
</ol>
<p>节点通常只需要返回发生变化的字段，而不需要返回完整状态。例如：</p>
<pre><code class="language-python">def node_a(state: State) -&gt; State:
    return {&quot;count&quot;: state[&quot;count&quot;] + 1}
</code></pre>
<p>其中：</p>
<pre><code class="language-python">{&quot;count&quot;: state[&quot;count&quot;] + 1}
</code></pre>
<p>表示对状态的更新，而不是一份必须包含所有字段的完整状态。</p>
<h5 id="7-4-3-2-2-源码视角">7.4.3.2.2. 源码视角</h5>
<p>从源码实现角度看，节点业务逻辑、状态写入逻辑和控制流写入逻辑会被组合成一个可顺序执行的可运行对象。</p>
<p>编译节点时，<strong><code>LangGraph</code></strong> 会创建一个 <strong><code>PregelNode</code></strong>。</p>
<p>相关源码位于 <strong><code>CompiledStateGraph</code></strong> 类的 <strong><code>attach_node()</code></strong> 方法中，如下</p>
<pre><code class="language-python">self.nodes[key] = PregelNode(
    triggers=[branch_channel],
    # read state keys and managed values
    channels=(&quot;__root__&quot; if is_single_input else input_channels),
    # coerce state dict to schema class (eg. pydantic model)
    mapper=mapper,
    # publish to state keys
    writers=[ChannelWrite(write_entries)],
    metadata=node.metadata,
    retry_policy=node.retry_policy,
    cache_policy=node.cache_policy,
    bound=node.runnable,  # type: ignore[arg-type]
)
</code></pre>
<p>其中：</p>
<ul>
<li><strong><code>bound</code></strong> 保存节点的业务逻辑；</li>
<li><strong><code>writers</code></strong> 保存节点执行完成后需要调用的写入器；</li>
<li><strong><code>triggers</code></strong> 保存能够触发该节点的通道。</li>
</ul>
<p>可以近似表示为：</p>
<pre><code class="language-text">PregelNode
├── bound：节点业务逻辑
├── writers：状态和控制流写入器
└── triggers：能够触发该节点的通道
</code></pre>
<p>因此，从整体上看，节点执行并不是简单地”调用一个函数并返回结果”，而是：</p>
<blockquote>
<p>执行业务逻辑，生成状态和控制流写入，并通过目标节点触发通道或动态任务写入，为下一超步生成待调度任务，持续推动计算图运行。</p>
</blockquote>
<hr />
<h1 id="附录a-postgresql的部署与配置">附录A. PostgreSQL的部署与配置</h1>
<h2 id="a-1-postgresql简介">A.1. PostgreSQL简介</h2>
<h3 id="a-1-1-什么是postgresql">A.1.1. 什么是PostgreSQL</h3>
<p><strong><code>PostgreSQL</code></strong>（简称 <strong><code>PG</code></strong>）是一款开源的<strong>关系型数据库管理系统（<code>RDBMS</code>）</strong>，诞生于1996年，至今已有近30年的发展历史。它以<strong>稳定性、可靠性、<code>ACID</code>事务支持</strong>和<strong>强大的扩展能力</strong>著称，是目前生产环境中使用最广泛的开源关系型数据库之一。</p>
<p><strong><code>PostgreSQL</code></strong> 的主要特点包括：</p>
<ul>
<li><strong><code>ACID</code>事务</strong>：完整支持原子性（<code>Atomicity</code>）、一致性（<code>Consistency</code>）、隔离性（<code>Isolation</code>）、持久性（<code>Durability</code>），确保数据操作的可靠性；</li>
<li><strong>丰富的数据类型</strong>：不仅支持传统的整数、浮点数、字符串，还原生支持 <strong><code>JSON</code>/<code>JSONB</code></strong>、数组、范围类型等，使其可以同时承担”关系型”和”文档型”数据库的角色；</li>
<li><strong>可扩展性</strong>：支持自定义数据类型、函数、索引类型，拥有丰富的扩展生态；</li>
<li><strong>跨平台</strong>：支持 <strong><code>Windows</code></strong>、<strong><code>Linux</code></strong>、<strong><code>macOS</code></strong> 等主流操作系统。</li>
</ul>
<p>在 <strong><code>LangGraph</code></strong> 的上下文中，<strong><code>PostgreSQL</code></strong> 主要被用作<strong>检查点（<code>Checkpoint</code>）的持久化后端</strong>，同时也支持作为<strong>长期记忆存储</strong>使用。</p>
<h3 id="a-1-2-为什么langgraph需要postgresql">A.1.2. 为什么LangGraph需要PostgreSQL</h3>
<p>在之前的 <strong>6.2.4节</strong> 中，我们提到”在学习 <strong><code>LangChain</code></strong> 时，我们已经介绍了 <strong><code>PostgreSQL</code></strong> 的部署和 <strong><code>PostgresSaver</code></strong> 的用法”。但如果你没有学习过 <strong><code>LangChain</code></strong> 课程，或者希望在本课程中获得完整的知识闭环，本附录将从零开始介绍 <strong><code>PostgreSQL</code></strong> 的部署与使用。</p>
<p>回顾一下第 <strong>6.2.2节</strong> 中检查点后端的对比：</p>
<table>
<thead>
<tr>
<th>后端</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>InMemorySaver</code></strong></td>
<td>学习、调试、本地实验；进程结束数据丢失</td>
</tr>
<tr>
<td><strong><code>SQLite</code></strong></td>
<td>轻量级本地持久化；单进程场景</td>
</tr>
<tr>
<td><strong><code>PostgreSQL</code></strong></td>
<td>生产环境；跨进程、跨服务共享状态</td>
</tr>
<tr>
<td><strong><code>MongoDB</code></strong></td>
<td>生产环境；偏好文档模型</td>
</tr>
<tr>
<td><strong><code>Redis</code></strong></td>
<td>生产环境；高吞吐、低延迟缓存场景</td>
</tr>
</tbody>
</table>
<p><strong><code>InMemorySaver</code></strong> 将检查点保存在当前 <strong><code>Python</code></strong> 进程的内存中。每次重启程序，内存中的数据就会丢失——这对于需要跨多次调用记住对话历史的多轮对话场景显然是不够的。</p>
<p><strong><code>PostgresSaver</code></strong> 则将检查点写入 <strong><code>PostgreSQL</code></strong> 数据库。与程序进程的生命周期解耦：</p>
<ul>
<li>多次运行同一个 <strong><code>thread_id</code></strong> 的对话，历史消息会持续累积；</li>
<li>重启 <strong><code>Python</code></strong> 程序后，只要数据库中的记录还在，历史检查点就能被恢复；</li>
<li>多个 <strong><code>Python</code></strong> 进程可以共享同一个 <strong><code>PostgreSQL</code></strong> 实例，实现跨进程的状态共享。</li>
</ul>
<p><strong><code>PostgresSaver</code></strong> 来自 <strong><code>langgraph-checkpoint-postgres</code></strong> 包，本节附录将完整介绍如何部署和配置它所需的 <strong><code>PostgreSQL</code></strong> 环境。</p>
<h3 id="a-1-3-本文使用的连接信息">A.1.3. 本文使用的连接信息</h3>
<p>为与 <strong>6.2.4节</strong> 的代码保持一致，本附录统一使用以下连接信息：</p>
<table>
<thead>
<tr>
<th>配置项</th>
<th>值</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>Host</code></strong></td>
<td><strong><code>localhost</code></strong></td>
</tr>
<tr>
<td><strong><code>Port</code></strong></td>
<td><strong><code>5432</code></strong>（<strong><code>PostgreSQL</code></strong> 默认端口）</td>
</tr>
<tr>
<td><strong><code>Database</code></strong></td>
<td><strong><code>langgraph_db</code></strong></td>
</tr>
<tr>
<td><strong><code>User</code></strong></td>
<td><strong><code>langgraph_user</code></strong></td>
</tr>
<tr>
<td><strong><code>Password</code></strong></td>
<td><strong><code>123456</code></strong></td>
</tr>
<tr>
<td><strong><code>SSL Mode</code></strong></td>
<td><strong><code>disable</code></strong>（本地开发环境关闭 SSL）</td>
</tr>
<tr>
<td><strong>连接URL</strong></td>
<td><strong><code>postgresql://langgraph_user:123456@localhost:5432/langgraph_db?sslmode=disable</code></strong></td>
</tr>
</tbody>
</table>
<blockquote>
<p><strong>注意</strong>：这里的 <strong><code>sslmode=disable</code></strong> 表示关闭 <strong><code>SSL</code></strong> 连接，仅适用于本地开发和测试环境。生产环境中建议启用 <strong><code>SSL</code></strong> 并修改为强密码。</p>
</blockquote>
<h2 id="a-2-postgresql的安装与部署">A.2. PostgreSQL的安装与部署</h2>
<p>本节介绍两种安装方式：<strong><code>Docker</code></strong> 部署（推荐，简单快速）和 <strong><code>Windows</code></strong> 直接安装（无需额外安装 <strong><code>Docker</code></strong>）。</p>
<h3 id="a-2-1-方式一-使用docker部署-推荐">A.2.1. 方式一：使用Docker部署（推荐）</h3>
<p><strong><code>Docker</code></strong> 部署的优势在于：无需修改系统环境，安装卸载干净，版本切换方便，一条命令即可启动。</p>
<blockquote>
<p>如果你还没有安装 <strong><code>Docker</code></strong>，请先访问 <strong><a href="https://www.docker.com/products/docker-desktop/">Docker Desktop for Windows</a></strong> 下载并安装。安装完成后确保 <strong><code>Docker Engine</code></strong> 处于运行状态（桌面右下角 <strong><code>Docker</code></strong> 图标为绿色）。</p>
</blockquote>
<h4 id="a-2-1-1-拉取postgresql镜像">A.2.1.1. 拉取PostgreSQL镜像</h4>
<p>打开 <strong><code>PowerShell</code></strong> 或 <strong><code>CMD</code></strong> 终端，执行：</p>
<pre><code class="language-bash">docker pull postgres:16
</code></pre>
<p>这里选择 <strong><code>PostgreSQL 16</code></strong> 版本，是当前最新的稳定大版本，与 <strong><code>langgraph-checkpoint-postgres==3.0.5</code></strong> 兼容。</p>
<p>拉取完成后，可通过以下命令查看本地已有的镜像：</p>
<pre><code class="language-bash">docker images | findstr postgres
</code></pre>
<h4 id="a-2-1-2-运行postgresql容器">A.2.1.2. 运行PostgreSQL容器</h4>
<p>执行以下命令启动容器：</p>
<pre><code class="language-bash">docker run -d `
  --name langgraph-postgres `
  -e POSTGRES_DB=langgraph_db `
  -e POSTGRES_USER=langgraph_user `
  -e POSTGRES_PASSWORD=123456 `
  -p 5432:5432 `
  postgres:16
</code></pre>
<blockquote>
<p><strong>Windows <code>CMD</code> 用户注意</strong>：上述换行使用了 <strong><code>PowerShell</code></strong> 的反引号 <code>`</code>。如果在 <strong><code>CMD</code></strong> 中执行，需要将 <code>`</code> 替换为 <code>^</code>，或写成一整行：</p>
<pre><code class="language-bash">docker run -d --name langgraph-postgres -e POSTGRES_DB=langgraph_db -e POSTGRES_USER=langgraph_user -e POSTGRES_PASSWORD=123456 -p 5432:5432 postgres:16
</code></pre>
</blockquote>
<p>各参数含义：</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>-d</code></strong></td>
<td>后台运行（<code>daemon</code> 模式），终端关闭后容器不会停止</td>
</tr>
<tr>
<td><strong><code>--name langgraph-postgres</code></strong></td>
<td>容器名称，方便后续管理</td>
</tr>
<tr>
<td><strong><code>-e POSTGRES_DB=langgraph_db</code></strong></td>
<td>创建容器时自动创建名为 <strong><code>langgraph_db</code></strong> 的数据库</td>
</tr>
<tr>
<td><strong><code>-e POSTGRES_USER=langgraph_user</code></strong></td>
<td>自动创建用户 <strong><code>langgraph_user</code></strong></td>
</tr>
<tr>
<td><strong><code>-e POSTGRES_PASSWORD=123456</code></strong></td>
<td>设置该用户的密码为 <strong><code>123456</code></strong></td>
</tr>
<tr>
<td><strong><code>-p 5432:5432</code></strong></td>
<td>将容器的 5432 端口映射到宿主机的 5432 端口</td>
</tr>
</tbody>
</table>
<blockquote>
<p><strong>注意</strong>：通过环境变量自动创建的数据库和用户，其权限已自动配置好，无需再手动授权。如果使用手动安装方式，则需要额外执行授权步骤（见 <strong>A.3.2节</strong>）。</p>
</blockquote>
<h4 id="a-2-1-3-验证容器运行状态">A.2.1.3. 验证容器运行状态</h4>
<pre><code class="language-bash"># 查看正在运行的容器
docker ps

# 如果看不到 langgraph-postgres，查看所有容器（包括已停止的）
docker ps -a

# 查看容器日志
docker logs langgraph-postgres
</code></pre>
<p>如果容器没有运行，可以通过以下命令启动：</p>
<pre><code class="language-bash">docker start langgraph-postgres
</code></pre>
<blockquote>
<p><strong>常用容器管理命令</strong>：</p>
<pre><code class="language-bash">docker stop langgraph-postgres    # 停止容器
docker start langgraph-postgres   # 启动容器
docker restart langgraph-postgres # 重启容器
docker rm langgraph-postgres      # 删除容器（需要先停止）
</code></pre>
</blockquote>
<h4 id="a-2-1-4-docker-compose方式-可选">A.2.1.4. Docker Compose方式（可选）</h4>
<p>如果更习惯使用 <strong><code>Docker Compose</code></strong> 管理容器，可以创建 <code>docker-compose.yml</code> 文件：</p>
<pre><code class="language-yaml">version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: langgraph-postgres
    environment:
      POSTGRES_DB: langgraph_db
      POSTGRES_USER: langgraph_user
      POSTGRES_PASSWORD: 123456
    ports:
      - “5432:5432”
    volumes:
      - pgdata:/var/lib/postgresql/data  # 持久化数据，容器删除后数据不丢失

volumes:
  pgdata:
</code></pre>
<p>然后在 <code>docker-compose.yml</code> 所在目录执行：</p>
<pre><code class="language-bash">docker compose up -d
</code></pre>
<p><strong><code>volumes</code></strong> 配置将数据库文件映射到宿主机，<strong>即使容器被删除，数据也不会丢失</strong>——这对于保存检查点数据非常重要。</p>
<h3 id="a-2-2-方式二-windows直接安装">A.2.2. 方式二：Windows直接安装</h3>
<p>如果你不想安装 <strong><code>Docker</code></strong>，也可以直接在 <strong><code>Windows</code></strong> 上安装 <strong><code>PostgreSQL</code></strong>。</p>
<h4 id="a-2-2-1-下载安装包">A.2.2.1. 下载安装包</h4>
<p>访问 <strong><a href="https://www.postgresql.org/download/windows/">PostgreSQL Windows 官方下载页</a></strong>，或直接访问 <strong><a href="https://www.enterprisedb.com/downloads/postgres-postgresql-downloads">EDB安装包下载页</a></strong>，选择 <strong><code>PostgreSQL 16.x</code></strong> 版本、<strong><code>Windows x86-64</code></strong> 架构的安装包下载。</p>
<blockquote>
<p>建议下载带有 <strong><code>pgAdmin</code></strong>（图形化管理工具）的完整安装包。</p>
</blockquote>
<h4 id="a-2-2-2-安装步骤">A.2.2.2. 安装步骤</h4>
<ol>
<li><strong>运行安装程序</strong>：双击下载的 <code>.exe</code> 文件，点击 <strong><code>Next</code></strong>；</li>
<li><strong>选择安装目录</strong>：建议保持默认路径 <code>C:\Program Files\PostgreSQL\16\</code>；</li>
<li><strong>选择安装组件</strong>：勾选 <strong><code>PostgreSQL Server</code></strong>、<strong><code>pgAdmin 4</code></strong>（可选）、<strong><code>Command Line Tools</code></strong>（必选，需要 <strong><code>psql</code></strong> 命令行工具）；</li>
<li><strong>设置超级用户密码</strong>：安装程序会要求为 <strong><code>postgres</code></strong> 超级用户设置密码（记好这个密码）；</li>
<li><strong>设置端口</strong>：保持默认 <strong><code>5432</code></strong>；</li>
<li><strong>选择区域设置</strong>：保持默认 <strong><code>Default locale</code></strong>；</li>
<li><strong>安装</strong>：等待进度条完成。</li>
</ol>
<h4 id="a-2-2-3-验证安装">A.2.2.3. 验证安装</h4>
<p>安装完成后，打开 <strong><code>CMD</code></strong> 或 <strong><code>PowerShell</code></strong>：</p>
<pre><code class="language-bash"># 检查 psql 是否可用
psql --version

# 使用超级用户连接数据库
psql -h localhost -p 5432 -U postgres
</code></pre>
<p>输入安装时设置的 <strong><code>postgres</code></strong> 密码，如果能进入 <strong><code>psql</code></strong> 交互界面（显示 <code>postgres=#</code> 提示符），说明安装成功。</p>
<h4 id="a-2-2-4-windows服务管理">A.2.2.4. Windows服务管理</h4>
<p><strong><code>PostgreSQL</code></strong> 安装后会自动注册为 <strong><code>Windows</code></strong> 服务（服务名：<strong><code>postgresql-x64-16</code></strong>），默认开机自启。</p>
<p>常用服务管理操作：</p>
<pre><code class="language-bash"># 查看服务状态（PowerShell）
Get-Service postgresql-x64-16

# 手动启动服务
net start postgresql-x64-16

# 手动停止服务
net stop postgresql-x64-16
</code></pre>
<p>也可以通过 <strong><code>services.msc</code></strong>（服务管理器）图形界面管理。</p>
<h3 id="a-2-3-安装python依赖">A.2.3. 安装Python依赖</h3>
<p>无论选择哪一种安装方式，都需要安装 <strong><code>Python</code></strong> 侧的依赖包：</p>
<pre><code class="language-bash">pip install langgraph-checkpoint-postgres==3.0.5
pip install psycopg[binary]
</code></pre>
<table>
<thead>
<tr>
<th>包名</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>langgraph-checkpoint-postgres</code></strong></td>
<td><strong><code>LangGraph</code></strong> 的 <strong><code>PostgreSQL</code></strong> 检查点后端，提供 <strong><code>PostgresSaver</code></strong></td>
</tr>
<tr>
<td><strong><code>psycopg[binary]</code></strong></td>
<td><strong><code>Python</code></strong> 的 <strong><code>PostgreSQL</code></strong> 驱动，负责底层数据库通信</td>
</tr>
</tbody>
</table>
<blockquote>
<p><strong><code>langgraph-checkpoint-postgres</code></strong> 内部依赖 <strong><code>psycopg</code></strong>，但建议显式安装以确保版本正确。<strong><code>[binary]</code></strong> 后缀表示安装包含预编译二进制文件的版本，避免从源码编译的麻烦。</p>
</blockquote>
<h2 id="a-3-postgresql基本操作">A.3. PostgreSQL基本操作</h2>
<p>本节介绍 <strong><code>PostgreSQL</code></strong> 的常用操作。掌握这些内容有助于理解 <strong><code>PostgresSaver</code></strong> 在数据库中做了什么，也方便日常排查问题。</p>
<h3 id="a-3-1-连接postgresql">A.3.1. 连接PostgreSQL</h3>
<h4 id="a-3-1-1-使用psql命令行工具">A.3.1.1. 使用psql命令行工具</h4>
<p><strong><code>psql</code></strong> 是 <strong><code>PostgreSQL</code></strong> 自带的命令行客户端。</p>
<p>连接命令格式：</p>
<pre><code class="language-bash">psql -h &lt;主机&gt; -p &lt;端口&gt; -U &lt;用户名&gt; -d &lt;数据库名&gt;
</code></pre>
<p>对应本附录的连接信息：</p>
<pre><code class="language-bash">psql -h localhost -p 5432 -U langgraph_user -d langgraph_db
# 输入密码: 123456
</code></pre>
<p>连接成功后，终端提示符变为 <code>langgraph_db=&gt;</code>，可以输入 <strong><code>SQL</code></strong> 命令。</p>
<blockquote>
<p><strong>Docker 用户</strong>：如果主机上没有安装 <strong><code>psql</code></strong>，可以直接进入容器内执行：</p>
<pre><code class="language-bash">docker exec -it langgraph-postgres psql -U langgraph_user -d langgraph_db
</code></pre>
</blockquote>
<p>常用 <strong><code>psql</code></strong> 元命令：</p>
<table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>\l</code></strong></td>
<td>列出所有数据库</td>
</tr>
<tr>
<td><strong><code>\dt</code></strong></td>
<td>列出当前数据库的所有表</td>
</tr>
<tr>
<td><strong><code>\d table_name</code></strong></td>
<td>查看表结构</td>
</tr>
<tr>
<td><strong><code>\q</code></strong></td>
<td>退出 <strong><code>psql</code></strong></td>
</tr>
</tbody>
</table>
<h4 id="a-3-1-2-使用python连接">A.3.1.2. 使用Python连接</h4>
<p>在 <strong><code>Python</code></strong> 中使用 <strong><code>psycopg</code></strong> 连接：</p>
<pre><code class="language-python">import psycopg

DB_URL = “postgresql://langgraph_user:123456@localhost:5432/langgraph_db?sslmode=disable”

conn = psycopg.connect(DB_URL)
cur = conn.cursor()

# 查看 PostgreSQL 版本信息
cur.execute(“SELECT version();”)
result = cur.fetchone()
print(f”PostgreSQL 版本：{result[0]}”)

cur.close()
conn.close()
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code>PostgreSQL 版本：PostgreSQL 16.x on x86_64-pc-linux-gnu, compiled by gcc ...
</code></pre>
<blockquote>
<p><strong>注意</strong>：如果在 <code>pip install psycopg</code> 时没有加 <code>[binary]</code> 后缀，可能会因为缺少编译环境而报错。遇到这种情况执行 <code>pip install psycopg[binary]</code> 即可。</p>
</blockquote>
<h3 id="a-3-2-创建数据库和用户">A.3.2. 创建数据库和用户</h3>
<blockquote>
<p><strong>Docker 用户请注意</strong>：通过 <strong><code>docker run</code></strong> 的 <strong><code>-e</code></strong> 环境变量启动容器时，数据库和用户已经自动创建并配置好权限，<strong>可以跳过本节</strong>，直接阅读 <strong>A.3.3节</strong>。</p>
</blockquote>
<p>如果你是<strong>手动安装的 <strong><code>PostgreSQL</code></strong>，启动时</strong>不会**自动创建 **<code>langgraph_db</code>数据库和 <strong><code>langgraph_user</code></strong> 用户，需要手动创建。</p>
<h4 id="a-3-2-1-创建数据库">A.3.2.1. 创建数据库</h4>
<p>首先用超级用户 <strong><code>postgres</code></strong> 连接：</p>
<pre><code class="language-bash">psql -h localhost -p 5432 -U postgres
</code></pre>
<p>然后创建数据库：</p>
<pre><code class="language-sql">CREATE DATABASE langgraph_db;
</code></pre>
<p>查看是否创建成功：</p>
<pre><code class="language-sql">\l
</code></pre>
<p>输出中如果能找到 <strong><code>langgraph_db</code></strong> 条目，则说明创建成功。</p>
<h4 id="a-3-2-2-创建用户并授权">A.3.2.2. 创建用户并授权</h4>
<p>在同一个 <strong><code>psql</code></strong> 会话中继续执行：</p>
<pre><code class="language-sql">-- 创建用户
CREATE USER langgraph_user WITH PASSWORD '123456';

-- 赋予该用户对 langgraph_db 的所有权限
GRANT ALL PRIVILEGES ON DATABASE langgraph_db TO langgraph_user;
</code></pre>
<blockquote>
<p><strong><code>PostgreSQL 15+</code></strong> 版本默认收紧了对 <strong><code>public</code></strong> 模式的权限管理。如果后续 <strong><code>PostgresSaver</code></strong> 在创建表时报权限错误，需要额外执行：</p>
<pre><code class="language-sql">\c langgraph_db
GRANT ALL ON SCHEMA public TO langgraph_user;
</code></pre>
</blockquote>
<p>退出 <strong><code>psql</code></strong>，用新用户测试连接：</p>
<pre><code class="language-bash">psql -h localhost -p 5432 -U langgraph_user -d langgraph_db
# 输入密码: 123456
</code></pre>
<p>如果能成功连接，说明用户和数据库配置正确。</p>
<h3 id="a-3-3-基本crud操作">A.3.3. 基本CRUD操作</h3>
<p>了解基本的 <strong><code>SQL</code></strong> 增删改查有助于查看和理解 <strong><code>PostgresSaver</code></strong> 内部的数据存储。</p>
<h4 id="a-3-3-1-创建表">A.3.3.1. 创建表</h4>
<pre><code class="language-sql">CREATE TABLE IF NOT EXISTS messages (
    id SERIAL PRIMARY KEY,
    thread_id TEXT NOT NULL,
    role TEXT NOT NULL,
    content TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
</code></pre>
<ul>
<li><strong><code>SERIAL PRIMARY KEY</code></strong>：自增主键，<strong><code>PostgreSQL</code></strong> 会自动为新插入的行分配唯一的 <strong><code>id</code></strong>；</li>
<li><strong><code>TEXT</code></strong>：变长文本类型，适合存储聊天消息内容；</li>
<li><strong><code>TIMESTAMP DEFAULT CURRENT_TIMESTAMP</code></strong>：时间戳类型，默认为插入时的当前时间。</li>
</ul>
<h4 id="a-3-3-2-插入数据">A.3.3.2. 插入数据</h4>
<pre><code class="language-sql">INSERT INTO messages (thread_id, role, content)
VALUES ('chapter_6_6.2.4', 'user', '你好，我是老王');

INSERT INTO messages (thread_id, role, content)
VALUES ('chapter_6_6.2.4', 'assistant', '老王你好！有什么可以帮你的？');
</code></pre>
<h4 id="a-3-3-3-查询数据">A.3.3.3. 查询数据</h4>
<pre><code class="language-sql">-- 查询所有记录
SELECT * FROM messages;

-- 按 thread_id 筛选（最常用，对应 LangGraph 中的 thread）
SELECT * FROM messages WHERE thread_id = 'chapter_6_6.2.4';

-- 按时间排序
SELECT * FROM messages WHERE thread_id = 'chapter_6_6.2.4' ORDER BY created_at;
</code></pre>
<h4 id="a-3-3-4-更新和删除数据">A.3.3.4. 更新和删除数据</h4>
<pre><code class="language-sql">-- 更新某条记录的内容
UPDATE messages SET content = '你好，我是老王，今年30岁。'
WHERE id = 1;

-- 删除某条记录
DELETE FROM messages WHERE id = 2;

-- 删除整个表（注意：操作不可逆）
DROP TABLE IF EXISTS messages;
</code></pre>
<h3 id="a-3-4-查看postgressaver创建的表结构">A.3.4. 查看PostgresSaver创建的表结构</h3>
<p>这是整个附录中最实用的部分——<strong>连接回 6.2.4节</strong>，看看 <strong><code>checkpointer.setup()</code></strong> 在 <strong><code>PostgreSQL</code></strong> 中到底创建了什么。</p>
<h4 id="a-3-4-1-运行setup-后查看表">A.3.4.1. 运行setup()后查看表</h4>
<p>首先运行一次 <strong>6.2.4节</strong> 的代码（确保 <strong><code>setup()</code></strong> 已执行），然后在 <strong><code>psql</code></strong> 中查看：</p>
<pre><code class="language-sql">\c langgraph_db
\dt
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code>             List of relations
 Schema |        Name           | Type  |     Owner
--------+-----------------------+-------+----------------
 public | checkpoint_blobs      | 数据表 | langgraph_user
 public | checkpoint_migrations | 数据表 | langgraph_user
 public | checkpoint_writes     | 数据表 | langgraph_user
 public | checkpoints           | 数据表 | langgraph_user
</code></pre>
<p>可以看到 <strong><code>setup()</code></strong> 在数据库中创建了四张表：</p>
<table>
<thead>
<tr>
<th>表名</th>
<th>用途</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>checkpoints</code></strong></td>
<td>存储完整的检查点快照（状态、元数据等）</td>
</tr>
<tr>
<td><strong><code>checkpoint_writes</code></strong></td>
<td>存储每个检查点对应的通道写入记录</td>
</tr>
<tr>
<td><strong><code>checkpoint_blobs</code></strong></td>
<td>存储大型二进制数据（如图片、文件等，如果状态中包含的话）</td>
</tr>
<tr>
<td><strong><code>checkpoint_migrations</code></strong></td>
<td>记录数据库迁移版本，<strong><code>LangGraph</code></strong> 内部使用，用于管理表结构变更</td>
</tr>
</tbody>
</table>
<h4 id="a-3-4-2-检查点表结构说明">A.3.4.2. 检查点表结构说明</h4>
<p>使用 <strong><code>\d</code></strong> 命令查看每张表的实际结构。</p>
<p><strong>（1）checkpoints 表</strong></p>
<pre><code class="language-sql">\d checkpoints
</code></pre>
<pre><code>         栏位         | 类型  |  可空的  |    预设
----------------------+-------+----------+-------------
 thread_id            | text  | not null |
 checkpoint_ns        | text  | not null | ''::text
 checkpoint_id        | text  | not null |
 parent_checkpoint_id | text  |          |
 type                 | text  |          |
 checkpoint           | jsonb | not null |
 metadata             | jsonb | not null | '{}'::jsonb
索引：
    &quot;checkpoints_pkey&quot; PRIMARY KEY, btree (thread_id, checkpoint_ns, checkpoint_id)
    &quot;checkpoints_thread_id_idx&quot; btree (thread_id)
</code></pre>
<p><strong>关键字段说明</strong>：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>类型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>thread_id</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>线程ID，对应 <strong><code>config</code></strong> 中的 <strong><code>thread_id</code></strong>，用于区分不同对话</td>
</tr>
<tr>
<td><strong><code>checkpoint_ns</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>检查点命名空间，默认空字符串，用于子图等场景隔离不同层级的检查点</td>
</tr>
<tr>
<td><strong><code>checkpoint_id</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>检查点唯一ID，由 <strong><code>LangGraph</code></strong> 内部生成</td>
</tr>
<tr>
<td><strong><code>parent_checkpoint_id</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>父检查点ID，构建检查点链，用于 <strong><code>Time Travel</code></strong> 回退</td>
</tr>
<tr>
<td><strong><code>type</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>检查点类型</td>
</tr>
<tr>
<td><strong><code>checkpoint</code></strong></td>
<td><strong><code>jsonb</code></strong></td>
<td>检查点核心数据（状态快照），以 <strong><code>JSON</code></strong> 格式存储</td>
</tr>
<tr>
<td><strong><code>metadata</code></strong></td>
<td><strong><code>jsonb</code></strong></td>
<td>检查点元数据（创建时间、来源、步骤序号等），预设为 <code>'{}'::jsonb</code></td>
</tr>
</tbody>
</table>
<ul>
<li>主键为 <strong>(thread_id, checkpoint_ns, checkpoint_id)</strong> 联合主键。</li>
<li>另有 <strong><code>thread_id</code></strong> 单列索引，加速按线程筛选查询。</li>
</ul>
<p><strong>（2）checkpoint_writes 表</strong></p>
<pre><code class="language-sql">\d checkpoint_writes
</code></pre>
<pre><code>     栏位      |  类型   |  可空的  |   预设
---------------+---------+----------+----------
 thread_id     | text    | not null |
 checkpoint_ns | text    | not null | ''::text
 checkpoint_id | text    | not null |
 task_id       | text    | not null |
 idx           | integer | not null |
 channel       | text    | not null |
 type          | text    |          |
 blob          | bytea   | not null |
 task_path     | text    | not null | ''::text
索引：
    &quot;checkpoint_writes_pkey&quot; PRIMARY KEY, btree (thread_id, checkpoint_ns, checkpoint_id, task_id, idx)
    &quot;checkpoint_writes_thread_id_idx&quot; btree (thread_id)
</code></pre>
<p><strong>关键字段说明</strong>：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>类型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>thread_id</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>线程ID</td>
</tr>
<tr>
<td><strong><code>checkpoint_ns</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>命名空间，与 <strong><code>checkpoints</code></strong> 表对应</td>
</tr>
<tr>
<td><strong><code>checkpoint_id</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>所属检查点ID</td>
</tr>
<tr>
<td><strong><code>task_id</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>产生写入的节点任务ID</td>
</tr>
<tr>
<td><strong><code>idx</code></strong></td>
<td><strong><code>integer</code></strong></td>
<td>写入顺序索引</td>
</tr>
<tr>
<td><strong><code>channel</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>写入的目标通道名（如 <strong><code>messages</code></strong>、<strong><code>__pregel_tasks</code></strong>）</td>
</tr>
<tr>
<td><strong><code>type</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>写入数据类型</td>
</tr>
<tr>
<td><strong><code>blob</code></strong></td>
<td><strong><code>bytea</code></strong></td>
<td>写入的二进制数据</td>
</tr>
<tr>
<td><strong><code>task_path</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>任务路径，用于追踪节点在子图中的层级关系</td>
</tr>
</tbody>
</table>
<ul>
<li>主键为 <strong>(thread_id, checkpoint_ns, checkpoint_id, task_id, idx)</strong> 联合主键，确保同一任务下的写入顺序唯一。</li>
</ul>
<p><strong>（3）checkpoint_blobs 表</strong></p>
<pre><code class="language-sql">\d checkpoint_blobs
</code></pre>
<pre><code>     栏位      | 类型  |  可空的  |   预设
---------------+-------+----------+----------
 thread_id     | text  | not null |
 checkpoint_ns | text  | not null | ''::text
 channel       | text  | not null |
 version       | text  | not null |
 type          | text  | not null |
 blob          | bytea |          |
索引：
    &quot;checkpoint_blobs_pkey&quot; PRIMARY KEY, btree (thread_id, checkpoint_ns, channel, version)
    &quot;checkpoint_blobs_thread_id_idx&quot; btree (thread_id)
</code></pre>
<p><strong>关键字段说明</strong>：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>类型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>thread_id</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>线程ID</td>
</tr>
<tr>
<td><strong><code>checkpoint_ns</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>命名空间</td>
</tr>
<tr>
<td><strong><code>channel</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>通道名</td>
</tr>
<tr>
<td><strong><code>version</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>数据版本标识，同一通道可以有多个版本</td>
</tr>
<tr>
<td><strong><code>type</code></strong></td>
<td><strong><code>text</code></strong></td>
<td>数据类型</td>
</tr>
<tr>
<td><strong><code>blob</code></strong></td>
<td><strong><code>bytea</code></strong></td>
<td>二进制数据本体</td>
</tr>
</tbody>
</table>
<ul>
<li>主键为 <strong>(thread_id, checkpoint_ns, channel, version)</strong> 联合主键。</li>
<li>与 <strong><code>checkpoint_writes</code></strong> 不同，<strong><code>checkpoint_blobs</code></strong> 以 <strong>(channel, version)</strong> 维度管理数据版本，不直接关联某个具体的 <strong><code>checkpoint_id</code></strong>，允许多个检查点共享同一通道的大型二进制数据。</li>
</ul>
<p><strong>（4）checkpoint_migrations 表</strong></p>
<pre><code class="language-sql">\d checkpoint_migrations
</code></pre>
<pre><code> 栏位 |  类型   |  可空的  | 预设
------+---------+----------+------
 v    | integer | not null |
索引：
    &quot;checkpoint_migrations_pkey&quot; PRIMARY KEY, btree (v)
</code></pre>
<p><strong>关键字段说明</strong>：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>类型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><code>v</code></strong></td>
<td><strong><code>integer</code></strong></td>
<td>当前数据库迁移版本号</td>
</tr>
</tbody>
</table>
<ul>
<li>单列主键，仅一条记录，<strong><code>setup()</code></strong> 执行时自动检查并更新。</li>
<li>该表由 <strong><code>LangGraph</code></strong> 内部管理，用户无需手动操作。未来 <strong><code>LangGraph</code></strong> 版本升级时，若表结构有变更，<strong><code>setup()</code></strong> 会根据此版本号自动执行对应的迁移 <strong><code>SQL</code></strong>。</li>
</ul>
<blockquote>
<p><strong>四张表的关系</strong>：</p>
<ul>
<li><strong><code>checkpoints</code></strong> 存储每次 <strong><code>graph.invoke()</code></strong> 产生的检查点快照；</li>
<li><strong><code>checkpoint_writes</code></strong> 存储每个检查点下各节点任务的通道写入记录（一条检查点通常对应多条写入）；</li>
<li><strong><code>checkpoint_blobs</code></strong> 存储通道级别的大型二进制数据，以 <strong>(thread_id, checkpoint_ns, channel, version)</strong> 为维度，可跨检查点复用；</li>
<li><strong><code>checkpoint_migrations</code></strong> 仅用于数据库表结构版本管理。</li>
</ul>
<p><strong><code>LangGraph</code></strong> 通过 <strong><code>thread_id</code></strong> + <strong><code>checkpoint_ns</code></strong> + <strong><code>checkpoint_id</code></strong> 联合定位和恢复状态。</p>
</blockquote>
<h4 id="a-3-4-3-写入检查点数据查询">A.3.4.3. 写入检查点数据查询</h4>
<p>运行 <strong>6.2.4节</strong> 的示例代码后，可以直接在数据库中查看写入的数据：</p>
<pre><code class="language-sql">SET client_encoding = 'UTF8';
-- 查看指定线程的检查点数量
SELECT thread_id, count(*)
FROM checkpoints
WHERE thread_id = 'chapter03-02'
GROUP BY thread_id;

-- 查看所有检查点的基本信息（按创建时间排序）
SELECT checkpoint_id, parent_checkpoint_id, checkpoint_ns, metadata
FROM checkpoints
WHERE thread_id = 'chapter03-02'
ORDER BY metadata;

-- 查看最新检查点的消息通道写入数据
SELECT c.checkpoint_id, c.checkpoint_ns, cw.channel, cw.task_id, cw.type
FROM checkpoints c
JOIN checkpoint_writes cw
  ON c.thread_id = cw.thread_id
 AND c.checkpoint_ns = cw.checkpoint_ns
 AND c.checkpoint_id = cw.checkpoint_id
WHERE c.thread_id = 'chapter03-02'
  AND cw.channel = 'messages'
ORDER BY cw.idx;

-- 查看最新检查点的完整状态 JSON
SELECT checkpoint_id, checkpoint
FROM checkpoints
WHERE thread_id = 'chapter03-02'
ORDER BY metadata DESC
LIMIT 1;

</code></pre>
<p>通过这些查询，可以直观地理解 <strong><code>PostgresSaver</code></strong> 是如何将 <strong><code>LangGraph</code></strong> 的检查点数据持久化到 <strong><code>PostgreSQL</code></strong> 中的。</p>
<blockquote>
<p><strong>Windows 终端编码问题</strong>：
在中文 <strong><code>Windows</code></strong> 的 <strong><code>CMD</code></strong> 或 <strong><code>PowerShell</code></strong> 中使用 <strong><code>psql</code></strong> 查询 <strong><code>checkpoint</code></strong>（<strong><code>jsonb</code></strong>）字段时，如果数据中包含 emoji 等 4 字节 <strong><code>UTF-8</code></strong> 字符，可能会报错：</p>
<pre><code>错误:  编码&quot;UTF8&quot;的字符0x0xf0 0x9f 0xa4 0x94在编码&quot;GBK&quot;没有相对应值
</code></pre>
<p>这是因为 <strong><code>Windows</code></strong> 终端默认编码为 <strong><code>GBK</code></strong>，无法处理 4 字节 <strong><code>UTF-8</code></strong> 字符。解决方案：</p>
<pre><code class="language-sql">-- 查询前先切换客户端编码
SET client_encoding = 'UTF8';
</code></pre>
<p>或在 <strong><code>PowerShell</code></strong> 中先执行 <strong><code>chcp 65001</code></strong> 切换到 <strong><code>UTF-8</code></strong> 编码，再启动 <strong><code>psql</code></strong>。</p>
</blockquote>
<h4 id="补充说明">补充说明</h4>
<ul>
<li><strong><code>setup()</code></strong> 是幂等的——多次调用不会重复创建表，也不会清空已有数据；</li>
<li>实际项目中，建议将 <strong><code>setup()</code></strong> 作为独立的数据库初始化/迁移步骤执行，而不是每次启动应用时都调用（这也是 <strong>6.2.4节</strong> 代码注释中提到的建议）；</li>
<li>如果需要主动清理某个线程的检查点数据，可以使用 <strong><code>checkpointer.delete_thread(thread_id)</code></strong> 方法，或者从数据库层面删除对应记录；</li>
<li>关于连接池：如果需要在生产环境中处理高并发请求，建议使用 <strong><code>psycopg_pool</code></strong> 提供的连接池功能，而不是每次创建新连接。</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>02-LangGraph控制流与节点执行</title>
    <url>/2025/05/14/langgraph-control-flow/</url>
    <content><![CDATA[<h1 id="4-控制流">4. 控制流</h1>
<h2 id="4-1-顺序结构">4.1. 顺序结构</h2>
<h3 id="4-1-1-add_edge">4.1.1. add_edge</h3>
<p><strong><code>add_edge</code></strong> 用于在两个节点之间添加一条有向边。边是图结构中最基本的元素之一，节点之间的执行顺序、分支跳转以及循环控制，最终都依赖节点和边共同表达。</p>
<p>因此，在 LangGraph 中，基础的控制流结构都可以通过 <strong><code>add_edge</code></strong> 进行构建。</p>
<p><strong>示例如下：</strong></p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import TypedDict

class OverAllState(TypedDict):
    username: str
    greeting: str
    output: str

def node_a(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;greeting&quot;: &quot;Dear &quot; + state[&quot;username&quot;]
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;output&quot;: state[&quot;greeting&quot;] + &quot;，你好！&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
res = graph.invoke({&quot;username&quot;: &quot;小黄&quot;})
print(res)

from IPython.display import display

display(graph)
</code></pre>
<p><strong>输出如下：</strong></p>
<pre><code class="language-json">{
  &quot;username&quot;: &quot;小黄&quot;,
  &quot;greeting&quot;: &quot;Dear 小黄&quot;,
  &quot;output&quot;: &quot;Dear 小黄，你好！&quot;
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260527181124131.png" alt="image-20260527181124131" /></p>
<p>在上述示例中，图的执行流程如下：</p>
<pre><code>START -&gt; node_a -&gt; node_b -&gt; END
</code></pre>
<p>其中：</p>
<ul>
<li><strong><code>START -&gt; node_a</code></strong> 表示图从 <code>node_a</code> 开始执行；</li>
<li><strong><code>node_a -&gt; node_b</code></strong> 表示 <code>node_a</code> 执行完成后，继续执行 <code>node_b</code>；</li>
<li><strong><code>node_b -&gt; END</code></strong> 表示 <code>node_b</code> 执行完成后，图进入结束状态。</li>
</ul>
<h3 id="4-1-2-add_sequence">4.1.2. add_sequence</h3>
<p>如果需要构建一组按顺序执行的节点，也可以使用 <strong><code>add_sequence</code></strong>。</p>
<p><strong><code>add_sequence</code></strong> 支持传入一个可执行对象列表。LangGraph 会按照列表顺序依次添加节点，并在相邻节点之间自动添加边。默认情况下，函数名会被用作节点名称。</p>
<p><strong>示例如下：</strong></p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import TypedDict

class OverAllState(TypedDict):
    username: str
    greeting: str
    output: str

def node_a(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;greeting&quot;: &quot;Dear &quot; + state[&quot;username&quot;]
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;output&quot;: state[&quot;greeting&quot;] + &quot;，你好！&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_sequence([node_a, node_b])
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
res = graph.invoke({&quot;username&quot;: &quot;小黄&quot;})
print(res)

from IPython.display import display

display(graph)
</code></pre>
<p>输出如下</p>
<pre><code class="language-json">{
  &quot;username&quot;: &quot;小黄&quot;,
  &quot;greeting&quot;: &quot;Dear 小黄&quot;,
  &quot;output&quot;: &quot;Dear 小黄，你好！&quot;
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260527181941605.png" alt="image-20260527181941605" /></p>
<p>上述代码中：</p>
<pre><code>builder.add_sequence([node_a, node_b])
</code></pre>
<p>大致等价于：</p>
<pre><code>builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
</code></pre>
<p>因此，完整执行流程仍然是：</p>
<pre><code>START -&gt; node_a -&gt; node_b -&gt; END
</code></pre>
<p>相比手动调用多次 <strong><code>add_node</code></strong> 和 <strong><code>add_edge</code></strong>，<strong><code>add_sequence</code></strong> 更适合用于构建简单的线性执行流程。</p>
<h3 id="4-1-3-省略指向-end-的边">4.1.3. 省略指向 <strong><code>END</code></strong> 的边</h3>
<p>在 LangGraph 中，图的终止并不完全依赖某个真实执行的特殊节点。</p>
<p>从运行机制上看，LangGraph 会在每个 <strong><code>SuperStep</code></strong> 开始时，根据当前发生更新的 <strong><code>Channel</code></strong> 以及节点之间的触发关系，计算本轮需要执行的任务列表。如果没有新的节点被激活，也就没有新的任务需要执行，图运行自然结束。</p>
<p>需要注意的是，<strong><code>END</code> 并不是运行阶段真正执行的节点</strong>。也就是说，指向 <strong><code>END</code></strong> 的边不会像指向普通节点的边那样，触发一个真实的节点任务。它更多用于表达图结构中的“终止语义”：当前路径执行到这里即可结束。</p>
<p>因此，在一些简单的线性流程中，即使省略指向 <strong><code>END</code></strong> 的边，最后一个节点执行完成后，如果没有后续节点被触发，图也可以正常结束。</p>
<p>示例如下：</p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START
from typing import TypedDict

class OverAllState(TypedDict):
    username: str
    greeting: str
    output: str

def node_a(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;greeting&quot;: &quot;Dear &quot; + state[&quot;username&quot;]
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;output&quot;: state[&quot;greeting&quot;] + &quot;，你好！&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)

graph = builder.compile()
res = graph.invoke({&quot;username&quot;: &quot;小黄&quot;})
print(res)

from IPython.display import display

display(graph)
</code></pre>
<p>输出如下</p>
<pre><code class="language-json">{
  &quot;username&quot;: &quot;小黄&quot;,
  &quot;greeting&quot;: &quot;Dear 小黄&quot;,
  &quot;output&quot;: &quot;Dear 小黄，你好！&quot;
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260527182015373.png" alt="image-20260527182015373" /></p>
<p>在这个例子中，虽然没有显式添加：</p>
<pre><code class="language-python">builder.add_edge(&quot;node_b&quot;, END)
</code></pre>
<p>但是 <strong><code>node_b</code></strong> 执行完成后，没有新的节点被触发，图运行会自然结束。</p>
<p>不过，<strong>可以省略指向 <code>END</code> 的边，并不表示 <code>END</code> 没有意义</strong>。</p>
<p>显式添加 <strong><code>END</code></strong> 边可以让图结构更加完整，也能更清晰地表达“流程到此结束”的语义。尤其是在分支、条件跳转、循环退出等场景中，显式指向 <strong><code>END</code></strong> 通常更利于阅读和维护。</p>
<p>和 <strong><code>END</code></strong> 不同，<strong><code>START</code> 通常不能省略</strong>。因为 <strong><code>START</code></strong> 不只是一个语义上的起点标记，它还用于告诉 LangGraph：图运行时应当从哪些节点开始执行。也就是说，下面这条边是启动图执行的关键：</p>
<pre><code class="language-python">builder.add_edge(START, &quot;node_a&quot;)
</code></pre>
<p>如果没有从 <strong><code>START</code></strong> 出发的边，或者没有通过其他方式指定入口节点，LangGraph 就无法确定图的初始执行节点。</p>
<p>因此，在实际开发中，建议保留指向 <strong><code>END</code></strong> 的边。虽然在某些简单流程中省略这些边也能正常运行，但显式添加它们，可以让图结构更加完整、语义更加清晰，便于后续维护。</p>
<h2 id="4-2-分支结构">4.2. 分支结构</h2>
<h3 id="4-2-1-静态分支-static-branch">4.2.1. 静态分支（Static Branch）</h3>
<ul>
<li><strong>定义</strong>：节点的下游候选节点 <strong>在图编译阶段就完全确定</strong>，只是运行时根据条件选择哪条边执行。</li>
<li>特点：
<ul>
<li>下游节点集合固定，数量、目标在编译时确定</li>
<li>运行时可选择<strong>一个或多个</strong>下游目标</li>
<li>可以用来做条件分支，但不生成新的节点</li>
</ul>
</li>
</ul>
<p>⚡ 核心判断：</p>
<blockquote>
<p><strong>编译期知道下游集合 → 静态分支</strong></p>
</blockquote>
<hr />
<h4 id="4-2-1-1-并行节点">4.2.1.1. 并行节点</h4>
<p>并行节点是最简单的静态分支形式。</p>
<p>当多个节点都从同一个上游节点触发时，它们会在同一个<strong>超步</strong>被激活。典型写法如下：</p>
<pre><code class="language-python">builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(START, &quot;node_b&quot;)
</code></pre>
<p><strong>示例：两个节点并行执行</strong></p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import TypedDict
from langchain_deepseek import ChatDeepSeek
from langchain.messages import HumanMessage

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    poem: str
    joke: str

def node_a(state: OverAllState) -&gt; OverAllState:
    poem = model.invoke(
        [
            HumanMessage(f&quot;写一首关于 {state['topic']} 的七言绝句&quot;)
        ]
    ).content
    return {
        &quot;poem&quot;: poem
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    joke = model.invoke(
        [
            HumanMessage(f&quot;写一个关于 {state['topic']} 的笑话&quot;)
        ]
    ).content
    return {
        &quot;joke&quot;: joke
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(START, &quot;node_b&quot;)

builder.add_edge(&quot;node_a&quot;, END)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
res = graph.invoke({&quot;topic&quot;: &quot;猫咪&quot;})
print(res)

from IPython.display import display

display(graph)
</code></pre>
<p>上述案例中，<strong><code>node_a</code></strong> 和 <strong><code>node_b</code></strong> 都由 <strong><code>START</code></strong> 触发。图运行时，二者会在<strong>同一个超步中</strong>被调度，它们各自读取当前状态并独立执行。</p>
<p>需要注意的是：</p>
<ul>
<li>这里的“并行”主要指<strong>调度语义上的并行</strong>；两个节点之间没有先后依赖；</li>
<li>它们的输出会在当前<strong>超步</strong>执行完成后统一合并到状态中；</li>
<li>如果两个节点写入同一个状态字段，则该字段通常需要配置合适的 <strong><code>Reducer</code></strong>，否则可能出现状态更新冲突，抛出 <strong><code>InvalidUpdateError</code></strong> 异常</li>
</ul>
<p><strong>输出如下</strong></p>
<pre><code class="language-json">{
    &quot;topic&quot;: &quot;猫咪&quot;,
    &quot;poem&quot;: &quot;《猫咪》\n夜巡檐角步轻悄，昼卧花阴晒暖毛。\n偶逐流莺穿柳过，忽惊蝴蝶上眉梢。\n\n赏析：这首作品以猫咪日常为切入点，通过“夜巡檐角”、“昼卧花阴”展现其昼伏夜出的习性，“逐流莺”、“惊蝴蝶”则捕捉其灵动瞬间。全诗未著“猫”字，却以“轻悄步”、“晒暖毛”等细节勾勒出猫咪的形神特征，尾句“上眉梢”更将好奇神态融入蝴蝶意象，物我交融，余韵悠长。&quot;,
    &quot;joke&quot;: &quot;有一只猫走进了一家披萨店，店员问：“您好，请问您要什么？”\n猫淡定地说：“喵～（我要一个12寸的海鲜披萨，加双倍芝士。）”\n店员愣了愣，说：“猫先生，您确定？”\n猫点点头，店员只好照做，然后猫掏出钱包付了钱，趴在小桌子上优雅地吃了起来。\n\n这时，旁边桌的客人看呆了，忍不住问店员：“它怎么能像人一样点餐、付钱、吃披萨？”\n店员耸耸肩，小声说：“我也不知道，但自从它上次用我的电脑打了一局《猫里奥》通关后，我就没敢拒绝它的任何要求。”&quot;
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260528141239527.png" alt="image-20260528141239527" /></p>
<h4 id="4-2-1-2-条件分支">4.2.1.2. 条件分支</h4>
<p><strong><code>StateGraph</code></strong> 提供了 <strong><code>add_conditional_edges</code></strong> 方法，用于从某个上游节点出发，根据运行时状态选择下游节点。</p>
<p>方法签名如下</p>
<pre><code class="language-python">def add_conditional_edges(
    self,
    source: str,
    path: Callable[..., Hashable | Sequence[Hashable]]
    | Callable[..., Awaitable[Hashable | Sequence[Hashable]]]
    | Runnable[Any, Hashable | Sequence[Hashable]],
    path_map: dict[Hashable, str] | list[str] | None = None,
) -&gt; Self:
</code></pre>
<p>不考虑 <strong><code>self</code></strong>，核心参数有三个：</p>
<ul>
<li><strong><code>source</code></strong>：条件分支的起始节点；</li>
<li><strong><code>path</code></strong>：路由规则，是一个可执行对象，通常是函数</li>
<li><strong><code>path_map</code></strong>：路由规则的返回值到真实节点名之间的映射关系。</li>
</ul>
<p>其中，<strong><code>path</code></strong> 的返回值表示跳转的目标节点，可以是：</p>
<ul>
<li>字符串或特殊对象 <strong><code>END</code></strong> 表示的单个目标；</li>
<li>字符串或特殊对象 <strong><code>END</code></strong> 表示的多个目标组成的序列；</li>
</ul>
<p><strong><code>path_map</code></strong></p>
<ul>
<li>
<p>可以省略，即取默认值 <strong><code>None</code></strong>，此时 <strong><code>path</code></strong> 返回值中出现的字符串必须是合法的节点名称。</p>
</li>
<li>
<p>可以是字典，维护 <strong><code>path</code></strong> 返回值和真实节点的映射。</p>
</li>
<li>
<p>也可以是列表，如下</p>
<pre><code class="language-python">path_map=[&quot;node_a&quot;, &quot;node_b&quot;, &quot;node_c&quot;]
</code></pre>
<p>相当于</p>
<pre><code class="language-python">path_map={
    &quot;node_a&quot;: &quot;node_a&quot;,
    &quot;node_b&quot;: &quot;node_b&quot;,
    &quot;node_c&quot;: &quot;node_c&quot;,
}
</code></pre>
<p>此时也要求 <strong><code>path</code></strong> 返回值中出现的字符串必须是合法的节点名称。</p>
</li>
</ul>
<h5 id="4-2-1-2-1-不使用-path_map">4.2.1.2.1. 不使用 <code>path_map</code></h5>
<p>如果不传 <strong><code>path_map</code></strong>，那么路由函数的返回值通常应当直接是图中的节点名称。如下</p>
<pre><code class="language-python">def router(state: OverAllState) -&gt; Literal[&quot;node_a&quot;, &quot;node_b&quot;]:
    if &quot;诗&quot; in state[&quot;content_type&quot;]:
        return &quot;node_a&quot;
    return &quot;node_b&quot;
</code></pre>
<p>此时，<strong><code>router</code></strong> 返回的 <strong><code>&quot;node_a&quot;</code></strong> 和 <strong><code>&quot;node_b&quot;</code></strong> 必须能够直接对应图中已经注册的节点名。</p>
<p><strong>完整案例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    content_type: str
    poem: str
    joke: str

def node_a(state: OverAllState) -&gt; OverAllState:
    poem = model.invoke([HumanMessage(f&quot;写一首关于 {state['topic']} 的七言绝句&quot;)]).content

    return {
        &quot;poem&quot;: poem
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    joke = model.invoke([HumanMessage(f&quot;写一个关于 {state['topic']} 的笑话&quot;)]).content

    return {
        &quot;joke&quot;: joke
    }

def router(state: OverAllState) -&gt; Literal[&quot;node_a&quot;, &quot;node_b&quot;]:
    if &quot;诗&quot; in state[&quot;content_type&quot;]:
        return &quot;node_a&quot;
    return &quot;node_b&quot;

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_conditional_edges(START, router)
builder.add_edge(&quot;node_a&quot;, END)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
poem_res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;, &quot;content_type&quot;: &quot;诗&quot;})
joke_res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;, &quot;content_type&quot;: &quot;笑话&quot;})

print('=' * 30, '-&gt; poem_res &lt;-', '=' * 30)
print(poem_res)
print('=' * 30, '-&gt; joke_res &lt;-', '=' * 30)
print(joke_res)

from IPython.display import display

display(graph)
</code></pre>
<p><strong>输出如下</strong></p>
<pre><code class="language-json">============================== -&gt; poem_res &lt;- ==============================

{
    &quot;topic&quot;: &quot;布偶狗&quot;,
    &quot;content_type&quot;: &quot;诗&quot;,
    &quot;poem&quot;: &quot;《咏布偶狗》\n绒身静卧似憨眠，无骨何妨守户前。\n不吠晨昏随主步，垂髫怜抱共跚连。\n\n注：我的诗中“无骨”暗喻布偶狗没有生命骨架，却依然守护着家门；“跚连”形容孩童与布偶狗相偕学步的蹒跚之态。通过“绒身”“垂髫”等意象，既保留了布偶狗的柔软特质，又赋予其超越玩偶的温情守护之意，在静态与动态的转换间实现了拟人化的诗意升华。&quot;
}

============================== -&gt; joke_res &lt;- ==============================

{
    &quot;topic&quot;: &quot;布偶狗&quot;,
    &quot;content_type&quot;: &quot;笑话&quot;,
    &quot;joke&quot;: &quot;有一天，一只布偶狗在街上闲逛，遇到了一只真狗。\n\n真狗好奇地问：“你是什么品种？怎么看起来毛这么长、这么滑，眼神还这么呆？”\n\n布偶狗骄傲地挺起胸膛：“我可是最顶级的仿真布偶狗！纯手工缝制，水晶眼睛，进口棉花填充，咬不坏、摔不烂，还不用溜。”\n\n真狗愣了一下，问：“那你平时都干嘛？”\n\n布偶狗叹了口气：“看家呗……主人说我最大的优点就是——丢不了，谁捡了都想还回来，因为太假了，连肉都不香。”&quot;
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260528141804888.png" alt="image-20260528141804888" /></p>
<p>观察图结构可以发现，**<code>__start__</code><strong>指向</strong><code>node_a</code><strong>和</strong><code>node_b</code>**的线是虚线，这表示节点的跳转是有条件的，不是固定的。</p>
<h5 id="4-2-1-2-2-使用-path_map">4.2.1.2.2. 使用 <code>path_map</code></h5>
<p>如果不希望路由函数直接返回节点名，而是返回业务语义更强的标识，可以使用 <strong><code>path_map</code></strong> 进行映射。如下：</p>
<pre><code class="language-python">def router(state: OverAllState) -&gt; Literal[&quot;a&quot;, &quot;b&quot;]:
    if &quot;诗&quot; in state[&quot;content_type&quot;]:
        return &quot;a&quot;
    return &quot;b&quot;

builder.add_conditional_edges(
    START,
    router,
    path_map={
        &quot;a&quot;: &quot;node_a&quot;,
        &quot;b&quot;: &quot;node_b&quot;,
    }
)
</code></pre>
<p>此时返回的 <strong><code>&quot;a&quot;</code></strong> 和 <strong><code>&quot;b&quot;</code></strong> 可以不是图中已注册的节点名称，但要通过 <strong><code>path_map</code></strong> 映射到正确的节点</p>
<p>这种写法的好处是：</p>
<ul>
<li>路由函数可以返回业务含义更清晰的标签；</li>
<li>图节点名称可以保持工程化命名；</li>
<li>渲染图结构时，边上可以显示路由标签，使图更容易理解。</li>
</ul>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    content_type: str
    poem: str
    ci_poem: str
    joke: str

def node_a(state: OverAllState) -&gt; OverAllState:
    poem = model.invoke([HumanMessage(f&quot;写一首关于 {state['topic']} 的七言绝句&quot;)]).content

    return {
        &quot;poem&quot;: poem
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    joke = model.invoke([HumanMessage(f&quot;写一个关于 {state['topic']} 的笑话&quot;)]).content

    return {
        &quot;joke&quot;: joke
    }

def router(state: OverAllState) -&gt; Literal[&quot;a&quot;, &quot;b&quot;]:
    if &quot;诗&quot; in state[&quot;content_type&quot;]:
        return &quot;a&quot;
    return &quot;b&quot;

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_conditional_edges(
    START,
    router,
    path_map={
        &quot;a&quot;: &quot;node_a&quot;,
        &quot;b&quot;: &quot;node_b&quot;,
    }
)
builder.add_edge(&quot;node_a&quot;, END)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
poem_res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;, &quot;content_type&quot;: &quot;诗&quot;})
joke_res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;, &quot;content_type&quot;: &quot;笑话&quot;})

print('=' * 30, '-&gt; poem_res &lt;-', '=' * 30)
print(poem_res)
print('=' * 30, '-&gt; joke_res &lt;-', '=' * 30)
print(joke_res)

from IPython.display import display

display(graph)
</code></pre>
<p><strong>输出如下</strong></p>
<pre><code class="language-json">============================== -&gt; poem_res &lt;- ==============================

{
    &quot;topic&quot;: &quot;布偶狗&quot;,
    &quot;content_type&quot;: &quot;诗&quot;,
    &quot;poem&quot;: &quot;《布偶狗》\n布偶妆成似犬形，绒毛细软眼如星。\n不吠不跳乖巧坐，疑是仙童降画屏。\n\n注：我的仿写创作思路是以玩具布偶狗为意象，通过“妆成似犬形”点明其拟态特征。后三句运用比喻（眼如星）、拟人（乖巧坐）、想象（仙童降画屏）手法，赋予静态玩偶动态神韵。末句以“仙童”暗喻其灵动气质，呼应首句的“布偶”属性，使全诗在虚实之间形成童趣与仙气的张力。&quot;
}

============================== -&gt; joke_res &lt;- ==============================

{
    &quot;topic&quot;: &quot;布偶狗&quot;,
    &quot;content_type&quot;: &quot;笑话&quot;,
    &quot;joke&quot;: &quot;好的，这里有一个关于布偶狗的笑话：\n\n有一只布偶狗，它从来不叫，也从来不跑。它的主人很担心，就带它去看兽医。\n\n兽医检查了半天，推了推眼镜说：“你这狗啊，没别的毛病，就是太乖了，乖到连狗的身份都忘了。你知道它为什么这么安静吗？”\n\n主人摇摇头。\n\n兽医叹了口气，说：“因为它是个‘布偶’，没有嘴，也没有腿。它唯一的技能，就是等你给它配个‘程序’，然后坐在那里，假装自己是一只不会乱咬东西的电子狗。说真的，这笑话比它还冷。”&quot;
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260528141959832.png" alt="image-20260528141959832" /></p>
<p>观察图结构可以发现，<strong><code>__start__</code><strong>指向</strong><code>node_a</code><strong>和</strong><code>node_b</code><strong>的虚线上出现了文本</strong><code>a</code><strong>和</strong><code>b</code></strong>，它们是路由函数返回的名称，通过**<code>path_map</code>**映射到具体的下游节点。</p>
<h5 id="4-2-1-2-3-同时路由至多个节点">4.2.1.2.3. 同时路由至多个节点</h5>
<p><strong><code>add_conditional_edges</code></strong> 也支持一次路由到多个下游节点。</p>
<h6 id="1-不用-path_map">1. 不用<code>path_map</code></h6>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from collections.abc import Sequence
from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    content_type: str
    poem: str
    ci_poem: str
    joke: str

def node_a(state: OverAllState) -&gt; OverAllState:
    poem = model.invoke([HumanMessage(f&quot;写一首关于 {state['topic']} 的七言绝句&quot;)]).content

    return {
        &quot;poem&quot;: poem
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    joke = model.invoke([HumanMessage(f&quot;写一个关于 {state['topic']} 的笑话&quot;)]).content

    return {
        &quot;joke&quot;: joke
    }

def node_c(state: OverAllState) -&gt; OverAllState:
    ci_poem = model.invoke([HumanMessage(f&quot;写一首关于 {state['topic']} 的词&quot;)]).content

    return {
        &quot;ci_poem&quot;: ci_poem
    }

def router(state: OverAllState) -&gt; Sequence[Literal[&quot;node_a&quot;, &quot;node_b&quot;, &quot;node_c&quot;]]:
    if &quot;诗&quot; in state[&quot;content_type&quot;]:
        return [&quot;node_a&quot;, &quot;node_c&quot;]
    return [&quot;node_b&quot;, &quot;node_c&quot;]

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)
builder.add_conditional_edges(
    START,
    router
)
builder.add_edge(&quot;node_a&quot;, END)
builder.add_edge(&quot;node_b&quot;, END)
builder.add_edge(&quot;node_c&quot;, END)

graph = builder.compile()
poem_res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;, &quot;content_type&quot;: &quot;诗&quot;})
joke_res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;, &quot;content_type&quot;: &quot;笑话&quot;})

print('=' * 30, '-&gt; poem_res &lt;-', '=' * 30)
print(poem_res)
print('=' * 30, '-&gt; joke_res &lt;-', '=' * 30)
print(joke_res)

from IPython.display import display

display(graph)
</code></pre>
<p>在这个例子中：</p>
<ul>
<li>当 <strong><code>content_type</code></strong> 包含 <strong><code>&quot;诗&quot;</code></strong> 时，同时触发 <strong><code>node_a</code></strong> 和 <strong><code>node_c</code></strong>；</li>
<li>否则同时触发 <strong><code>node_b</code></strong> 和 <strong><code>node_c</code></strong>。</li>
</ul>
<p><strong>输出如下</strong></p>
<pre><code class="language-json">============================== -&gt; poem_res &lt;- ==============================

{
    &quot;topic&quot;: &quot;布偶狗&quot;,
    &quot;content_type&quot;: &quot;诗&quot;,
    &quot;poem&quot;: &quot;《咏布偶狗》\n铁骨棉裘本异俦，垂头摇尾学温柔。\n非关皮相失真性，恐向人间惹旧愁。\n\n赏析：这首作品以布偶狗为题，通过“铁骨棉裘”的悖论意象，巧妙揭示材质与形态的错位。后两句笔锋一转，从拟态之“学温柔”深入至本真之“失真性”，结句“恐向人间惹旧愁”更以物喻人，赋予玩偶以灵性，暗含对世情虚伪的讽喻，余韵悠长。&quot;,
    &quot;ci_poem&quot;: &quot;《捣练子·布偶狗》\n针线巧，布绒柔。静卧妆台伴月幽。未解铃铛摇夜语，却随清梦上云舟。\n\n注：我的仿作将布偶狗拟人化，通过“针线巧，布绒柔”展现其手工质感，“伴月幽”营造静谧氛围。末句“随清梦上云舟”暗合现代人渴望逃离现实的隐喻，既保留古典词牌的意境美，又赋予布偶狗超越物象的情感寄托。&quot;
}

============================== -&gt; joke_res &lt;- ==============================

{
    &quot;topic&quot;: &quot;布偶狗&quot;,
    &quot;content_type&quot;: &quot;笑话&quot;,
    &quot;ci_poem&quot;: &quot;《鹧鸪天·布偶狗》\n绒目含星尾作虹，垂蹄静卧笑春风。\n无人自戏绒球转，得主频摇铃铎浓。\n\n偎暖日，扑帘栊。娇憨总在爪痕中。\n痴心不解浮生事，一抱虚温万事空。\n\n注：本词以布偶狗为意象，通过“绒目含星”、“垂蹄静卧”等细节摹写其柔顺之态。下阕“偎暖日”、“扑帘栊”拟人化笔法，寄寓物我相忘之趣。结句“一抱虚温”暗喻繁华终归寂灭之理，将玩偶之趣升华为对生命本真的哲思。&quot;,
    &quot;joke&quot;: &quot;小明带他的布偶狗去宠物医院，医生检查后说：“你这狗没心跳没呼吸，已经死了。”\n小明急了：“不可能！它刚刚还对我摇尾巴！”\n医生叹了口气：“那是你兜里的钥匙串，走起路来叮当响，它尾巴上的铃铛在共振。”\n小明沉默片刻，掏出手机拍了张照发朋友圈：“我的狗死了，但它的尾巴还在听我走路。”\n评论区炸了：“建议主人也去查查脑子，可能也共振坏了。”&quot;
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260528142244747.png" alt="image-20260528142244747" /></p>
<p>观察图结构可以发现，<strong><code>node_a</code></strong>、**<code>node_b</code><strong>和</strong><code>node_c</code>**独立于图结构之外。</p>
<p>和上一节案例相比，**<code>router</code>**函数返回的是序列而非单个节点，渲染器无法推断节点间的映射关系。</p>
<h6 id="2-添加映射">2. 添加映射</h6>
<p>通过 <strong><code>path_map</code></strong> 显示声明映射关系，明确下游节点集合，在提升代码可读性的同时，也有助于渲染器正确展示条件边。</p>
<p>当前场景下 <strong><code>path</code></strong> 返回值中的字符串就是合法的节点名称，<strong><code>path_map</code></strong> 可以是字典</p>
<pre><code class="language-python">path_map={
    &quot;node_a&quot;: &quot;node_a&quot;,
    &quot;node_b&quot;: &quot;node_b&quot;,
    &quot;node_c&quot;: &quot;node_c&quot;,
}
</code></pre>
<p>也可以是列表</p>
<pre><code class="language-python">path_map=[&quot;node_a&quot;, &quot;node_b&quot;, &quot;node_c&quot;]
</code></pre>
<p><strong>完整案例如下</strong></p>
<pre><code class="language-python">from collections.abc import Sequence
from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    content_type: str
    poem: str
    ci_poem: str
    joke: str

def node_a(state: OverAllState) -&gt; OverAllState:
    poem = model.invoke([HumanMessage(f&quot;写一首关于 {state['topic']} 的七言绝句&quot;)]).content

    return {
        &quot;poem&quot;: poem
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    joke = model.invoke([HumanMessage(f&quot;写一个关于 {state['topic']} 的笑话&quot;)]).content

    return {
        &quot;joke&quot;: joke
    }

def node_c(state: OverAllState) -&gt; OverAllState:
    ci_poem = model.invoke([HumanMessage(f&quot;写一首关于 {state['topic']} 的词&quot;)]).content

    return {
        &quot;ci_poem&quot;: ci_poem
    }

def router(state: OverAllState) -&gt; Sequence[Literal[&quot;node_a&quot;, &quot;node_b&quot;, &quot;node_c&quot;]]:
    if &quot;诗&quot; in state[&quot;content_type&quot;]:
        return [&quot;node_a&quot;, &quot;node_c&quot;]
    return [&quot;node_b&quot;, &quot;node_c&quot;]

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)
builder.add_conditional_edges(
    START,
    router,
    path_map={
        &quot;node_a&quot;: &quot;node_a&quot;,
        &quot;node_b&quot;: &quot;node_b&quot;,
        &quot;node_c&quot;: &quot;node_c&quot;,
    }
    # path_map=[&quot;node_a&quot;, &quot;node_b&quot;, &quot;node_c&quot;]
)
builder.add_edge(&quot;node_a&quot;, END)
builder.add_edge(&quot;node_b&quot;, END)
builder.add_edge(&quot;node_c&quot;, END)

graph = builder.compile()
poem_res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;, &quot;content_type&quot;: &quot;诗&quot;})
joke_res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;, &quot;content_type&quot;: &quot;笑话&quot;})

print('=' * 30, '-&gt; poem_res &lt;-', '=' * 30)
print(poem_res)
print('=' * 30, '-&gt; joke_res &lt;-', '=' * 30)
print(joke_res)

from IPython.display import display

display(graph)
</code></pre>
<p><strong>输出如下</strong></p>
<pre><code class="language-json">============================== -&gt; poem_res &lt;- ==============================

{
    &quot;topic&quot;: &quot;布偶狗&quot;,
    &quot;content_type&quot;: &quot;诗&quot;,
    &quot;poem&quot;: &quot;《布偶狗》\n布偶憨然卧锦茵，牵绳随主步香尘。\n偶因客至微昂首，原是无声座上宾。\n\n注：我的创作思路是抓住布偶狗静中有动的特质。首句以“憨然卧锦茵”勾勒其静态萌态，次句“牵绳随主步香尘”暗喻宠物与人的陪伴关系。转句“偶因客至微昂首”捕捉其反应，既保留布偶的拟真特性，又暗示机械性动作。结句“原是无声座上宾”点明其非生物属性，与开篇“布偶”形成闭环。全诗通过动静、真假的微妙转化，赋予静物以灵性。&quot;,
    &quot;ci_poem&quot;: &quot;《醉花阴·布偶狗》\n绒耳垂垂星作眸，憨卧小窗幽。棉絮裹憨态，线描眉眼，缝就三分柔。\n旧尘不染春衫袖，伴我度春秋。虽无温舌语，月斜人静，也解倚床头。\n\n注：我以拟人笔法赋予布偶狗灵性，通过“绒耳垂垂”“棉絮裹憨态”等细节勾勒其柔软形态，又于“月斜人静”处暗藏陪伴深情。下阕“旧尘不染”暗喻其纯净守护，末句“解倚床头”更将静物写活，道出玩偶与主人之间无声的慰藉。&quot;
}

============================== -&gt; joke_res &lt;- ==============================

{
    &quot;topic&quot;: &quot;布偶狗&quot;,
    &quot;content_type&quot;: &quot;笑话&quot;,
    &quot;ci_poem&quot;: &quot;《南歌子·布偶狗》\n绒耳垂春水，圆睛印月痕。\n棉絮缝成憨态真。\n不向朱门摇尾，卧蓬门。\n\n旧线牵云袖，新绒补雪身。\n线头散作泪斑纹。\n但把残年缝进，掌中温。\n\n注：我的布偶狗，是外婆用旧棉裤的棉花和二姐穿小的灯芯绒外套一针一线缝成的。二十年过去，棉絮从它破了洞的耳朵里露出，像吐着舌头。我模仿陆游“我与狸奴不出门”的意境，通过“棉絮缝春水”喻指时光在针脚里流逝，而“线头泪斑”则是故意缝补的裂痕——我们总要在破碎的事物里，重新认出圆满的形状。&quot;,
    &quot;joke&quot;: &quot;好的，这是一个关于布偶狗的笑话：\n\n有一只布偶狗，它特别特别懒，懒到什么程度呢？它的主人叫它去捡飞盘，它都懒得动。主人决定训练它，就扔出一个飞盘，大声喊：“布偶，快去捡！”\n\n布偶狗懒洋洋地抬头看了一眼飞盘落在远处，然后又趴下了。主人很生气，又扔了一个，它还是不理。\n\n主人无奈，只好采取了终极手段——假扮成一只会动的布偶狗，自己冲过去把飞盘叼了回来。\n\n布偶狗一看，浑身毛发都惊得炸了起来，它对旁边的一只小花狗低声说：“天哪，你看，那玩意儿居然能自己动！它还是个永动机！吓死狗了，好赖哦！”&quot;
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260528143421030.png" alt="image-20260528143421030" /></p>
<p>如图所示，图结构被正确渲染。</p>
<h4 id="4-2-1-3-defer-node-execution">4.2.1.3. defer node execution</h4>
<p>某些情况下，我们希望在所有常规任务节点执行完毕后，再进行日志、审计等收尾工作</p>
<p>此时可以在添加节点时设置**<code>defer=True</code>**，如下：</p>
<pre><code class="language-python">builder.add_node(&quot;audit_node&quot;, audit_node, defer=True)
</code></pre>
<p><strong><code>defer=True</code></strong> 的含义是：</p>
<p>当前节点不会在其被触发后立即执行，而是被延迟到常规图运行流程结束后，再在<strong>额外的超步中</strong>触发执行。</p>
<p>这类节点适合用于：</p>
<ul>
<li>日志记录；</li>
<li>审计检查；</li>
<li>结果汇总；</li>
<li>收尾清理；</li>
<li>统一校验前面节点是否已完成。</li>
</ul>
<h5 id="4-2-1-3-1-底层实现机制">4.2.1.3.1. 底层实现机制</h5>
<p><img src="/images/langgraph-study-notes/Defer%E5%BB%B6%E8%BF%9F%E8%A7%A6%E5%8F%91%E5%BA%95%E5%B1%82%E6%9C%BA%E5%88%B6.png" alt="Defer延迟触发底层机制" /></p>
<h6 id="1-编译阶段-使用特殊-channel">1. 编译阶段：使用特殊 Channel</h6>
<ol>
<li>
<p><strong><code>LangGraph</code></strong> 在编译状态图时，会为边创建对应的 <strong><code>Channel</code></strong>。</p>
</li>
<li>
<p>此时会根据节点的 <strong><code>defer</code></strong> 属性创建不同类型的 <strong><code>Channel</code></strong>，如下。</p>
<pre><code class="language-python">self.channels[branch_channel] = (
    LastValueAfterFinish(Any)
    if node.defer
    else EphemeralValue(Any, guard=False)
)
</code></pre>
<p><strong><code>defer</code></strong> 默认值为 <strong><code>False</code></strong></p>
<p>对于普通节点，边对应的通道类型是 <strong><code>EphemeralValue</code></strong>，可以理解为普通临时通道；而对于 <strong><code>defer=True</code></strong> 的节点，边对应的通道类型是特殊的 <strong><code>LastValueAfterFinish</code></strong>。</p>
</li>
</ol>
<h6 id="2-常规运行阶段-写入但不触发">2. 常规运行阶段-写入但不触发</h6>
<ol>
<li>在图运行过程中，每个节点执行完成后，会向其下游边对应的 <strong><code>Channel</code></strong> 写入数据。</li>
<li>常规的 <strong><code>Channel</code></strong> 在运行开始后处于可用状态，被写入后记录在 <strong><code>updated_channels</code></strong> 列表中，从而在下一个超步中触发下游节点的执行。</li>
<li>但是，<strong><code>LastValueAfterFinish</code></strong> 类型的通道起初是不可用的，首次被写入时不会添加到 <strong><code>updated_channels</code></strong> 列表中，下游节点自然不会被触发。</li>
</ol>
<h6 id="3-常规流程结束后-调用-finish-唤醒延迟节点">3. 常规流程结束后：调用<code>finish()</code>唤醒延迟节点</h6>
<ol>
<li><strong><code>LangGraph</code></strong> 底层用 <strong><code>trigger_to_nodes</code></strong> 维护了 <strong>边的 <code>Channel</code> -&gt; 节点</strong> 的映射，是一个字典。</li>
<li>在每个超步结束后，运行时会根据 <strong><code>updated_channels</code></strong> 判断是否还有新的节点需要被触发。</li>
<li>如果 <strong><code>updated_channels</code></strong> 和 <strong><code>trigger_to_nodes</code></strong> 的 key 没有交集，说明当前没有新的普通节点需要继续执行，常规运行流程已结束。</li>
<li>此时，<strong><code>LangGraph</code></strong> 运行时会调用所有 <strong><code>Channel</code></strong> 的 <strong><code>finish()</code></strong> 方法。</li>
<li>对于普通 <strong><code>Channel</code></strong> ，<strong><code>finish()</code></strong> 通常不会产生新的触发效果；但对于 <strong><code>LastValueAfterFinish</code></strong> 类型通道，首次调用 <strong><code>finish()</code></strong> 时，会将内部的 <strong><code>finished</code></strong> 标记设置为 <strong><code>True</code></strong>，并返回 <strong><code>True</code></strong>。</li>
<li>一旦 <strong><code>finished=True</code></strong>，该通道的 <strong><code>is_available()</code></strong> 就会变为 <strong><code>True</code></strong>。</li>
<li>于是，原本被延迟的通道会被加入 <strong><code>updated_channels</code></strong>，从而在额外的超步中触发对应的 <strong><code>defer</code></strong> 节点。</li>
</ol>
<h6 id="4-总结">4. 总结</h6>
<p>因此，<strong><code>defer=True</code></strong> 的运行机制可以概括为：</p>
<blockquote>
<p>触发边的 <strong><code>Channel</code></strong> 为特殊类型，首次写入不触发；常规流程结束后，特殊通道 <strong><code>finish()</code></strong>；通道变为可用；从而触发延迟节点，后者在额外超步中执行。</p>
</blockquote>
<h5 id="4-2-1-3-2-案例">4.2.1.3.2. 案例</h5>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek
from loguru import logger

from dotenv import load_dotenv

load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    poem: str
    joke: str

def node_a(state: OverAllState) -&gt; OverAllState:
    poem = model.invoke([HumanMessage(f&quot;写一首关于 {state['topic']} 的七言绝句&quot;)]).content

    return {
        &quot;poem&quot;: poem
    }

def node_b(state: OverAllState) -&gt; OverAllState:
    joke = model.invoke([HumanMessage(f&quot;写一个关于 {state['topic']} 的笑话&quot;)]).content

    return {
        &quot;joke&quot;: joke
    }

def audit_node(state: OverAllState) -&gt; OverAllState:
    logger.info(f&quot;任务节点全部执行完毕，诗 {'已生成' if state['poem'] else '未生成'}，笑话 {'已生成' if state['joke'] else '未生成'}&quot;)

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;audit_node&quot;, audit_node, defer=True)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(START, &quot;node_b&quot;)
builder.add_edge(START, &quot;audit_node&quot;)
builder.add_edge(&quot;node_a&quot;, END)
builder.add_edge(&quot;node_b&quot;, END)
builder.add_edge(&quot;audit_node&quot;, END)

graph = builder.compile()
res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;})
print(res)

from IPython.display import display

display(graph)
</code></pre>
<p>在这个图中：</p>
<ul>
<li><strong><code>node_a</code></strong> 和 <strong><code>node_b</code></strong> 是普通节点，在常规流程中被触发；</li>
<li><strong><code>audit_node</code></strong> 虽然也由 <strong><code>START</code></strong> 触发，但由于设置了 <strong><code>defer=True</code></strong>，不会立即执行；</li>
<li><strong><code>node_a</code></strong> 和 <strong><code>node_b</code></strong> 都执行完成后，常规运行流程结束，<strong><code>audit_node</code></strong> 才会在额外的超步中执行；</li>
<li>因此，<strong><code>audit_node</code></strong> 中可以读取到 <strong><code>node_a</code></strong> 和 <strong><code>node_b</code></strong> 已经写入并提交后的状态。</li>
</ul>
<p><strong>输出如下</strong></p>
<pre><code class="language-json">2026-05-28 17:07:09.618 | INFO | __main__:audit_node:40 - 任务节点全部执行完毕，诗 已生成，笑话 已生成

{
    'topic': '布偶狗',
    'poem': '《咏布偶狗》\n'
            '绸身棉腑自憨娇，静卧无眠守寂寥。\n'
            '不吠不嗔街巷事，偎人一晌解无聊。\n'
            '\n'
            '注：我的创作思路是通过拟人化手法，赋予布偶狗以温柔静默的品格。'
            '首句以“绸身棉腑”点明材质，次句“守寂寥”暗合宠物陪伴的特质。'
            '后两句通过“不吠不嗔”与“解无聊”的对比，凸显布偶狗无声胜有声的治愈力量，'
            '在机械玩物中注入人文温度。',
    'joke': '这是一个关于布偶狗的笑话：\n'
            '\n'
            '有一只布偶狗，它什么都好，毛茸茸的，蓝眼睛，性格温顺，'
            '唯一的缺点就是——它是个布偶，不会动。\n'
            '\n'
            '它的主人每天都把它放在沙发上，假装它在看电视、在思考狗生。\n'
            '\n'
            '有一天，主人带了一只真的活狗回家，想让布偶狗有个伴。'
            '活狗非常活泼，对着布偶狗又叫又摇尾巴，但布偶狗一动不动。\n'
            '\n'
            '活狗很奇怪，绕着布偶狗转了三圈，然后凑上去闻了闻。\n'
            '\n'
            '最后，活狗叹了口气，对主人说：\n'
            '\n'
            '“主人，这只狗……是不是在用‘静默模式’跟我交流？'
            '它的蓝牙是不是断了？”'
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260528164128093.png" alt="image-20260528164128093" /></p>
<h5 id="4-2-1-3-3-小结">4.2.1.3.3. 小结</h5>
<p><strong><code>defer=True</code></strong> 适合“最后执行”的收尾节点。</p>
<p>它的本质是通过特殊的 <strong><code>Channel</code></strong> 控制触发时机：</p>
<ol>
<li>编译阶段为延迟节点创建 <strong><code>LastValueAfterFinish</code></strong> 类型通道；</li>
<li>常规运行阶段该通道可以被写入，但不可用，不会触发下游节点；</li>
<li>常规流程结束后调用通道的 <strong><code>finish()</code></strong> 方法；</li>
<li><strong><code>finish()</code></strong> 首次生效后将通道标记为可用；</li>
<li>延迟节点在额外超步中被触发执行。</li>
</ol>
<blockquote>
<p>因此， <strong><code>defer=True</code></strong> 将节点的触发信号延迟释放，使该节点在常规流程结束后执行。</p>
</blockquote>
<h3 id="4-2-2-动态分支-dynamic-branch">4.2.2. 动态分支（Dynamic Branch）</h3>
<ul>
<li>
<p><strong>定义</strong>：节点的后续执行路径在 <strong>运行时</strong> 才确定，可以根据当前状态、输入数据或中间结果，动态决定要触发哪些下游任务或跳转到哪个下游节点。</p>
</li>
<li>
<p>特点：</p>
<ul>
<li>下游执行目标可以在运行时选择；</li>
<li>下游任务数量可以在运行时决定；</li>
<li>可以为同一个下游节点动态创建多个执行任务；</li>
<li>适合实现 Map-Reduce 式动态扇出、多任务并行处理、运行时条件跳转等场景。</li>
</ul>
<p>需要注意的是，所谓“动态”通常不是指运行时临时创建新的节点定义。节点本身一般仍需要在图编译前注册。动态性主要体现在：<strong>运行时决定触发哪些节点，以及为这些节点创建多少个执行任务</strong>。</p>
</li>
<li>
<p>典型用法：</p>
<ul>
<li><strong><code>Send</code></strong>（动态扇出任务/数据）</li>
<li><strong><code>Command(goto=...)</code></strong>（运行时跳转到下游节点）</li>
</ul>
</li>
</ul>
<p>⚡ 核心判断：</p>
<blockquote>
<p><strong>运行时决定后续执行目标或任务数量 → 动态分支</strong></p>
</blockquote>
<h4 id="4-2-2-1-并行节点">4.2.2.1. 并行节点</h4>
<p><strong><code>Send</code></strong> 结合 <strong><code>add_conditional_edges()</code></strong> 使用，可以用于动态扇出任务</p>
<p>所谓<strong>扇出（Fan-out）</strong>，是指一个上游节点像扇子一样，向外分发出多个下游任务。例如，根据一个主题，同时生成诗、词、笑话三个任务；又如，根据一个列表，为列表中的每个元素动态启动一个处理任务。</p>
<p>具体来说，路由函数可以返回一个 <strong><code>Send</code></strong> 实例序列。每个 <strong><code>Send</code></strong> 实例都描述了一次独立的任务分发：</p>
<ul>
<li>分发到哪个下游节点；</li>
<li>给这个下游节点传入什么私有状态。</li>
</ul>
<p>运行时，<strong><code>LangGraph</code></strong> 会根据返回的每个 <strong><code>Send</code></strong> 实例创建对应的任务。这些任务通常会在同一个超步中并行执行。</p>
<p><strong><code>Send</code></strong> 类构造器源码如下</p>
<pre><code class="language-python">def __init__(self, /, node: str, arg: Any) -&gt; None:
    &quot;&quot;&quot;
    Initialize a new instance of the `Send` class.

    Args:
        node: The name of the target node to send the message to.
        arg: The state or message to send to the target node.
    &quot;&quot;&quot;
    self.node = node
    self.arg = arg
</code></pre>
<p>该构造器接收两个参数，并记录为实例属性：</p>
<ul>
<li>node：待启动的下游节点名称，必须是已在状态图中注册的合法名称</li>
<li>arg：传递给下游节点的信息，仅对**<code>node</code>**指向的节点可见，通常应是私有状态。每个 <strong><code>Send</code></strong> 实例接收到的 <strong><code>arg</code></strong> 是独立的，互不相干。</li>
</ul>
<p><strong>实例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Literal
from collections.abc import Sequence
from langgraph.graph import StateGraph, START, END
from langgraph.types import Send
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
load_dotenv(override=True)

CONTENT_TYPES = [&quot;poem&quot;, &quot;ci_poem&quot;, &quot;joke&quot;]

model = ChatDeepSeek(
    model = &quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    poem: str
    ci_poem: str
    joke: str

class WorkerState(TypedDict):
    &quot;&quot;&quot;
    私有状态，只对 Worker 节点可用
    &quot;&quot;&quot;
    content_type: Literal[&quot;poem&quot;, &quot;ci_poem&quot;, &quot;joke&quot;]
    prompt: str

class InputState(TypedDict):
    topic: str

class OutputState(TypedDict):
    poem: str
    ci_poem: str
    joke: str

def worker_node(state: WorkerState) -&gt; OutputState:
    content_type = state[&quot;content_type&quot;]
    prompt = state[&quot;prompt&quot;]

    content = model.invoke([HumanMessage(prompt)]).content
    return {
        content_type: content
    }

def router(state: InputState) -&gt; Sequence[Send]:
    prompt = &quot;请生成关于 {} 的 {}&quot;
    english2chinese = {
        &quot;poem&quot;: &quot;一首诗&quot;,
        &quot;ci_poem&quot;: &quot;一首词&quot;,
        &quot;joke&quot;: &quot;一个笑话&quot;
    }

    topic = state[&quot;topic&quot;]

    return [Send(
            &quot;worker_node&quot;,
            {
                &quot;content_type&quot;: content_type,
                &quot;prompt&quot;: prompt.format(topic, english2chinese[content_type]),
            }
        ) for content_type in CONTENT_TYPES
    ]

builder = StateGraph(state_schema=OverAllState, input_schema=InputState, output_schema=OutputState)
builder.add_node(&quot;worker_node&quot;, worker_node)
builder.add_conditional_edges(
    START,
    router,
    path_map=[&quot;worker_node&quot;]
)
builder.add_edge(&quot;worker_node&quot;, END)

graph = builder.compile()
res = graph.invoke({&quot;topic&quot;: &quot;布偶狗&quot;})
print(res)

from IPython.display import display
display(graph)
</code></pre>
<p>在这个案例中，<strong><code>router()</code></strong> 并没有直接返回某一个固定的下游节点名称，而是返回了多个 <strong><code>Send</code></strong> 实例：</p>
<pre><code class="language-python">[
    Send(&quot;worker_node&quot;, {&quot;content_type&quot;: &quot;poem&quot;, ...}),
    Send(&quot;worker_node&quot;, {&quot;content_type&quot;: &quot;ci_poem&quot;, ...}),
    Send(&quot;worker_node&quot;, {&quot;content_type&quot;: &quot;joke&quot;, ...}),
]
</code></pre>
<p>因此，运行时会动态创建三个指向 <strong><code>worker_node</code></strong> 的任务。三个任务执行的是同一个节点函数，但它们接收到的私有状态不同，所以可以分别生成<strong>诗、词和笑话</strong>。</p>
<p><strong>输出如下</strong></p>
<pre><code class="language-json">{
  &quot;poem&quot;: &quot;## 《布偶狗》\n\n它曾蜷在藤椅里数谷粒，\n一只耳朵垂向磨损的地板。\n现在檐角的水滴是它的心跳，\n牵动穿珠眼珠转动。\n\n穿碎花裙的小女孩，\n用膝盖丈量过它的静默。\n她松开橡皮筋的午后，\n线缝里渗出稚嫩的气流。\n\n有人将米尺抖成琴弦，\n折叠的假寐里，\n它代替远方的稻草人，\n在积雨的布纹中扎根。\n\n田野在练习告别，\n而它守着发芽的梯田。\n当月光填满每道接缝，\n皮毛间涌出成群的往事。&quot;,
  &quot;ci_poem&quot;: &quot;《临江仙·布偶狗》\n绒作皮毛棉作骨，琉璃眸底春凝。\n也无吠影也无惊。倚床垂耳听，抱主索糖迎。\n\n不向残羹争冷炙，旧衾犹带余馨。\n学人解语却无声。偶沾星与月，懒问雨和晴。\n\n注：我以布偶狗为题，通过“绒作皮毛棉作骨”等句，以拟人手法赋予玩偶灵性。下阕“不向残羹争冷炙”暗喻玩偶超然物外的品性，末句“偶沾星与月”更添空灵意境，将玩偶与自然意象相融，表达出静默相伴的永恒温馨。&quot;,
  &quot;joke&quot;: &quot;# 布偶狗的笑话\n\n有一天，一只布偶狗走进一家宠物店，对店主说：\n\n“老板，我想买一只真正的狗朋友。”\n\n店主看了看它，好奇地问：“你不是狗吗？”\n\n布偶狗叹了口气，说：“唉，我是布偶做的，只能陪主人睡觉和卖萌，但我不会汪汪叫，也不会摇尾巴，更不会追球。每次主人想带我出去散步，我只能在包里安静地坐着，感觉自己特别没用。”\n\n店主笑了笑，说：“那你想要什么样的狗朋友？”\n\n布偶狗眼睛一亮：“我想要一只真正的狗，可以带着我去跑、去叫、去玩！我要学习怎么当一只好狗！”\n\n店主想了想，指着一只活泼的小金毛说：“那这只怎么样？它精力旺盛，可以教你很多。”\n\n布偶狗激动地冲过去，结果因为自己是布做的，腿太软，直接摔了个跟头，四脚朝天躺在地上。\n\n小金毛跑过来，闻了闻它，然后叼起它的棉花尾巴，一路拖到了狗窝里。\n\n布偶狗挣扎着喊：“等等！我还没学会怎么站起来！”\n\n小金毛歪了歪头，汪汪了两声，像是说：“没事，我先教你躺平。”\n\n从此以后，布偶狗成了小金毛最好的“垫子”——每天被压在身下睡觉，还觉得特别温暖。\n\n布偶狗心想：“原来，当一只合格的布偶狗朋友，最重要的技能是——当沙发。” 🛋️🐾&quot;
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260529111549501.png" alt="image-20260529111549501" /></p>
<p>此处图结构能成功渲染的关键，是在 <strong><code>path_map</code></strong> 中显式声明了可能被路由到的下游节点：</p>
<pre><code class="language-python">path_map=[&quot;worker_node&quot;]
</code></pre>
<p>因为 <strong><code>Send</code></strong> 的目标和数量可以运行时动态决定，图渲染器无法仅靠运行时返回值提前知道图结构。通过 <strong><code>path_map</code></strong> 声明候选下游节点后，渲染图才能正确展示 <strong><code>START</code></strong> 到 <strong><code>worker_node</code></strong> 的条件边关系。</p>
<p>补充说明：如果多个并行任务写入同一个状态字段，通常需要为该字段定义 reducer，用于合并多个任务的输出。本例中三个任务分别写入 <strong><code>poem</code></strong>、<strong><code>ci_poem</code></strong>、<strong><code>joke</code></strong> 三个不同字段，因此不会发生同一字段的并发合并问题。</p>
<hr />
<h4 id="4-2-2-2-条件分支">4.2.2.2. 条件分支</h4>
<h5 id="4-2-2-2-1-command介绍">4.2.2.2.1. Command介绍</h5>
<p><strong><code>Command</code></strong> 是 <strong><code>LangGraph</code></strong> 中用于控制图执行的多功能原语，它的构造器可以接受四个参数，并记录在同名类属性中：</p>
<ul>
<li><strong><code>update</code></strong>：更新图状态，效果等同于节点直接返回状态更新字典；</li>
<li><strong><code>goto</code></strong>：指定节点执行完成后的跳转目标，可用于运行时条件分支。当需要同时更新状态并控制跳转时，比单独使用条件边更合适；</li>
<li><strong><code>graph</code></strong>：存在子图时，用于指定跳转发生在哪一层图中，例如从子图跳转到父图；</li>
<li><strong><code>resume</code></strong>：用于恢复被中断的图执行，常见于 <strong><code>human-in-the-loop</code></strong> 场景。</li>
</ul>
<h5 id="4-2-2-2-2-用-command-实现条件分支">4.2.2.2.2. 用<code>Command</code>实现条件分支</h5>
<p>可以通过 <strong><code>Command(goto=...)</code></strong> 在节点内部实现条件分支。</p>
<p>与 <strong><code>add_conditional_edges()</code></strong> 相比，<strong><code>Command</code></strong> 更适合“状态更新”和“控制流跳转”需要放在同一个节点返回值中的场景。</p>
<p>例如，一个节点既要更新状态，又要根据当前状态决定下一步跳转目标，就可以返回：</p>
<pre><code class="language-python">return Command(
    update={&quot;foo&quot;: &quot;bar&quot;},
    goto=&quot;next_node&quot;
)
</code></pre>
<p><strong>示例如下：</strong></p>
<pre><code class="language-python">from typing import TypedDict, Literal
from langgraph.types import Command
from langgraph.graph import StateGraph, START, END
from langchain.messages import HumanMessage
from langchain_deepseek import ChatDeepSeek

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(TypedDict):
    topic: str
    content_type: Literal[&quot;poem&quot;, &quot;joke&quot;]
    poem: str
    joke: str

def router(state: OverAllState) -&gt; Command[Literal[&quot;poem_node&quot;, &quot;joke_node&quot;, END]]:
    content_type = state[&quot;content_type&quot;]

    if content_type == &quot;poem&quot;:
        return Command (
            goto=&quot;poem_node&quot;
        )
    elif content_type == &quot;joke&quot;:
        return Command (
            goto=&quot;joke_node&quot;
        )
    return Command (
        goto=END
    )

def poem_node(state: OverAllState) -&gt; OverAllState:
    topic = state[&quot;topic&quot;]
    prompt = f&quot;请生成一首关于 {topic} 的七言绝句&quot;
    poem = model.invoke([HumanMessage(prompt)]).content

    return {
        &quot;poem&quot;: poem
    }

def joke_node(state: OverAllState) -&gt; OverAllState:
    topic = state[&quot;topic&quot;]
    prompt = f&quot;请生成一个关于 {topic} 的冷笑话&quot;
    joke = model.invoke([HumanMessage(prompt)]).content

    return {
        &quot;joke&quot;: joke
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;router&quot;, router)
builder.add_node(&quot;poem_node&quot;, poem_node)
builder.add_node(&quot;joke_node&quot;, joke_node)
builder.add_edge(START, &quot;router&quot;)
builder.add_edge(&quot;poem_node&quot;, END)
builder.add_edge(&quot;joke_node&quot;, END)

graph = builder.compile()
poem_res = graph.invoke({&quot;topic&quot;: &quot;布偶猫&quot;, &quot;content_type&quot;: &quot;poem&quot;})
joke_res = graph.invoke({&quot;topic&quot;: &quot;布偶猫&quot;, &quot;content_type&quot;: &quot;joke&quot;})

# 这里故意传入一个不符合 Literal[&quot;poem&quot;, &quot;joke&quot;] 的值，
# 用来观察运行时兜底分支。
# 注意：Literal 主要服务于静态类型检查，
# 默认不会在 Python 运行时阻止非法值传入。
illegal_res = graph.invoke({
    &quot;topic&quot;: &quot;布偶猫&quot;,
    &quot;content_type&quot;: &quot;illegal&quot;
})

print('=' * 30, '-&gt; poem &lt;-', '=' * 30)
print(poem_res)
print('=' * 30, '-&gt; joke &lt;-', '=' * 30)
print(joke_res)
print('=' * 30, '-&gt; illegal &lt;-', '=' * 30)
print(illegal_res)

from IPython.display import display
display(graph)
</code></pre>
<p>上述代码中，<strong><code>router()</code></strong> 节点根据 <strong><code>content_type</code></strong> 的值决定下一步跳转目标：</p>
<ul>
<li>当 <strong><code>content_type == &quot;poem&quot;</code></strong> 时，跳转到 <strong><code>poem_node</code></strong>；</li>
<li>当 <strong><code>content_type == &quot;joke&quot;</code></strong> 时，跳转到 <strong><code>joke_node</code></strong>；</li>
<li>当传入其他非法值时，跳转到 <strong><code>END</code></strong>，图直接结束。</li>
</ul>
<p><strong>结果如下</strong></p>
<pre><code class="language-python">============================== -&gt; poem &lt;- ==============================
{
    'topic': '布偶猫',
    'content_type': 'poem',
    'poem': '《布偶猫》\n雪砌绒身碧眼秋，一帘幽梦卧云舟。\n忽听银铃摇碎月，轻衔星子入西楼。\n\n（注：此诗通过“雪砌绒身”、“碧眼秋”等意象勾勒布偶猫雪白柔顺的毛发与澄澈眼眸；“卧云舟”、“衔星子”等超现实笔法，赋予其精灵般的气质；末句“入西楼”暗合其优雅步态与神秘秉性，整体形成月光浸染的梦幻意境。）'
}

============================== -&gt; joke &lt;- ==============================
{
    'topic': '布偶猫',
    'content_type': 'joke',
    'joke': '给你分享一个关于布偶猫的冷笑话：\n\n---\n\n有一天，一只布偶猫跑去宠物店里应聘。  \n店员问：“你有什么特长呢？”  \n布偶猫懒洋洋地回答：“我特别软。”  \n店员说：“还有呢？”  \n猫翻个身，露出肚皮：“你看，我连摸都不需要自己动——你只要一伸手，我自己就‘布偶’过去了。”\n\n---\n\n希望能让你会心一笑（或者冷到打个哆嗦）😺❄️'
}

============================== -&gt; illegal &lt;- ==============================
{
    'topic': '布偶猫',
    'content_type': 'illegal'
}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260529164837663.png" alt="image-20260529164837663" /></p>
<p>这里使用：</p>
<pre><code class="language-python">Command[Literal[&quot;poem_node&quot;, &quot;joke_node&quot;, END]]
</code></pre>
<p>作为 <strong><code>router()</code></strong> 的返回值类型注解。</p>
<p>注解不能限制 <strong><code>goto</code></strong> 在运行时只能写这些值，而是为了让 LangGraph 和类型检查工具知道：这个节点可能跳转到哪些目标。对于图结构渲染来说，这个注解非常重要。如果不声明，渲染出来的图可能无法正确表达 <strong><code>router</code></strong> 节点的潜在跳转关系。</p>
<p>需要注意的是，如果某个节点使用 <strong><code>Command(goto=...)</code></strong> 控制后续跳转，一般不要再给这个节点额外添加普通下游边。否则，普通边和 <strong><code>Command</code></strong> 指定的跳转都可能生效，导致多个下游节点被同时触发。</p>
<p>因此，本例中只添加：</p>
<pre><code class="language-python">builder.add_edge(START, &quot;router&quot;)
</code></pre>
<p>而不添加类似下面这样的边：</p>
<pre><code class="language-python">builder.add_edge(&quot;router&quot;, &quot;poem_node&quot;)
builder.add_edge(&quot;router&quot;, &quot;joke_node&quot;)
</code></pre>
<p>因为 <strong><code>router</code></strong> 的后续跳转已经由 <strong><code>Command(goto=...)</code></strong> 决定。</p>
<hr />
<h4 id="4-2-2-3-小结">4.2.2.3. 小结</h4>
<p>动态分支有两类典型应用场景：</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>典型 API</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>动态扇出</td>
<td><strong><code>Send</code></strong> + <strong><code>add_conditional_edges()</code></strong></td>
<td>运行时创建多个任务，常用于 <strong><code>Map-Reduce</code></strong></td>
</tr>
<tr>
<td>动态跳转</td>
<td><strong><code>Command(goto=...)</code></strong></td>
<td>节点内部根据状态决定跳转目标</td>
</tr>
</tbody>
</table>
<p>二者的区别如下：</p>
<ul>
<li><strong><code>Send</code></strong> 更强调“一个节点动态分发多个任务实例”；</li>
<li><strong><code>Command(goto=...)</code></strong> 更强调“当前节点执行完后动态跳转到哪个节点”。</li>
</ul>
<blockquote>
<p><strong><code>Send</code> 解决的是：运行时要启动多少个任务实例。</strong>
<strong><code>Command(goto=...)</code> 解决的是：当前节点执行完后要去哪里。</strong></p>
</blockquote>
<p>不过，无论是 <strong><code>Send</code></strong> 还是 <strong><code>Command(goto=...)</code></strong>，目标节点通常都需要提前注册到图中。所谓“动态”，主要是运行时动态决定任务数量、任务输入或跳转路径，而不是运行时临时创建新的节点。</p>
<h2 id="4-3-多分支汇聚-fan-in">4.3. 多分支汇聚：Fan-in</h2>
<p>上文提到了<strong>扇出（Fan-out）</strong>，它是指像打开扇子一样，由一个上游节点分发出多个下游分支。</p>
<p>与之相对，**扇入（Fan-in）**是指像合拢扇子一样，多个上游分支汇聚到同一个下游节点。</p>
<p><strong>Fan-out</strong> 对应的是“分支结构”，<strong>Fan-in</strong> 对应的是“多分支汇聚结构”。</p>
<h3 id="4-3-1-静态扇入">4.3.1. 静态扇入</h3>
<p>在计算机中，**“与”<strong>表示两个或多个条件同时满足，而</strong>“或”**表示两个或多个条件任意一个满足。</p>
<p>当多个上游分支同时汇入同一个下游节点时，根据触发条件的不同，可以分成两种情况：</p>
<ul>
<li>**“与”**触发：上游所有分支全部到达才可触发下游节点</li>
<li>**“或”**触发：任意一个分支到达，都可以触发下游节点</li>
</ul>
<p>需要注意，这里的 <strong>“与 / 或”</strong> 只是帮助理解的类比，并不是 <strong><code>LangGraph</code></strong> 的官方术语。</p>
<h4 id="4-3-1-1-与-触发-等待所有上游分支到达">4.3.1.1 **“与”**触发：等待所有上游分支到达</h4>
<p>这是多分支汇聚中<strong>最常见</strong>的情况。</p>
<p><strong><code>LangGraph</code></strong> 的计算图是在一系列超步中完成的，如果能看到每个节点实例运行时的超步编号，则节点执行顺序一目了然。</p>
<p><strong><code>LangGraph</code></strong> 的节点可以通过名为 <strong><code>config</code></strong> 的有名参数获取运行时配置</p>
<p><strong><code>config</code></strong> 的元数据中记录了当前节点所在的 <strong><code>SuperStep</code></strong> 序号，可以通过下面的方式获取：</p>
<pre><code class="language-python">config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
</code></pre>
<p><strong>详见下文。</strong></p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langchain_core.runnables import RunnableConfig

from loguru import logger

class EmptyState(TypedDict):
    pass

def node_a(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_a 被触发&quot;, cur_step)
    return {}

def node_b(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_b 被触发&quot;, cur_step)
    return {}

def node_c(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_c 被触发&quot;, cur_step)
    return {}

def node_d(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_d 被触发&quot;, cur_step)
    return {}

def node_e(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_e 被触发&quot;, cur_step)
    return {}

builder = StateGraph(state_schema=EmptyState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)
builder.add_node(&quot;node_d&quot;, node_d)
builder.add_node(&quot;node_e&quot;, node_e)

builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_c&quot;)
builder.add_edge(&quot;node_b&quot;, &quot;node_d&quot;)
builder.add_edge([&quot;node_c&quot;, &quot;node_d&quot;], &quot;node_e&quot;)

graph = builder.compile()
graph.invoke({})

from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">2026-06-05 14:55:01.705 | INFO     | __main__:node_a:12 - cur_step: 1, node_a 被触发
2026-06-05 14:55:01.708 | INFO     | __main__:node_b:17 - cur_step: 2, node_b 被触发
2026-06-05 14:55:01.710 | INFO     | __main__:node_c:22 - cur_step: 2, node_c 被触发
2026-06-05 14:55:01.713 | INFO     | __main__:node_d:27 - cur_step: 3, node_d 被触发
2026-06-05 14:55:01.714 | INFO     | __main__:node_e:32 - cur_step: 4, node_e 被触发
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260605145540986.png" alt="image-20260605145540986" /></p>
<p>由运行结果可知：</p>
<ul>
<li><strong><code>node_a</code></strong> 在第 1 个超步执行；</li>
<li><strong><code>node_b</code></strong> 和 <strong><code>node_c</code></strong> 在第 2 个超步并行执行；</li>
<li><strong><code>node_d</code></strong> 在第 3 个超步执行；</li>
<li><strong><code>node_e</code></strong> 在第 4 个超步执行。</li>
</ul>
<p>虽然 <strong><code>node_c</code></strong> 在第 2 个超步已经执行完成，但是 <strong><code>node_e</code></strong> 并不会立刻触发。
因为这里使用的是：</p>
<pre><code class="language-python">builder.add_edge([&quot;node_c&quot;, &quot;node_d&quot;], &quot;node_e&quot;)
</code></pre>
<p>这表示 <strong><code>node_e</code></strong> 需要等待 <strong><code>node_c</code></strong> 和 <strong><code>node_d</code></strong> 两个上游节点全部完成后，才会被触发一次。</p>
<p>因此，这种写法对应的是 <strong>“与”触发</strong>：等待所有上游分支到达方可触发。</p>
<h4 id="4-3-1-2-或-触发-任意上游分支到达即可触发">4.3.1.2 **“或”**触发：任意上游分支到达即可触发</h4>
<p>另一种情况是：多个上游分支分别连接到同一个下游节点，但它们之间没有显式的同步等待关系。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langchain_core.runnables import RunnableConfig

from loguru import logger

class EmptyState(TypedDict):
    pass

def node_a(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_a 被触发&quot;, cur_step)
    return {}

def node_b(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_b 被触发&quot;, cur_step)
    return {}

def node_c(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_c 被触发&quot;, cur_step)
    return {}

def node_d(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_d 被触发&quot;, cur_step)
    return {}

def node_e(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;cur_step: {}, node_e 被触发&quot;, cur_step)
    return {}

builder = StateGraph(state_schema=EmptyState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)
builder.add_node(&quot;node_d&quot;, node_d)
builder.add_node(&quot;node_e&quot;, node_e)

builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_c&quot;)
builder.add_edge(&quot;node_b&quot;, &quot;node_d&quot;)

# 独立触发：node_c 完成后可以触发 node_e
builder.add_edge(&quot;node_c&quot;, &quot;node_e&quot;)
# 独立触发：node_d 完成后可以触发 node_e
builder.add_edge(&quot;node_d&quot;, &quot;node_e&quot;)

graph = builder.compile()
graph.invoke({})

from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">2026-06-05 15:09:11.715 | INFO     | __main__:node_a:12 - cur_step: 1, node_a 被触发
2026-06-05 15:09:11.720 | INFO     | __main__:node_b:17 - cur_step: 2, node_b 被触发
2026-06-05 15:09:11.723 | INFO     | __main__:node_c:22 - cur_step: 2, node_c 被触发
2026-06-05 15:09:11.729 | INFO     | __main__:node_d:27 - cur_step: 3, node_d 被触发
2026-06-05 15:09:11.729 | INFO     | __main__:node_e:32 - cur_step: 3, node_e 被触发
2026-06-05 15:09:11.732 | INFO     | __main__:node_e:32 - cur_step: 4, node_e 被触发
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260605151209760.png" alt="image-20260605151209760" /></p>
<p>从运行结果可以看出，<strong><code>node_e</code></strong> 被触发了两次：</p>
<ol>
<li><strong><code>node_c</code></strong> 在第 2 个超步执行完成后，触发 <strong><code>node_e</code></strong> 在第 3 个超步执行；</li>
<li><strong><code>node_d</code></strong> 在第 3 个超步执行完成后，再次触发 <strong><code>node_e</code></strong> 在第 4 个超步执行。</li>
</ol>
<p>原因是这里使用了两条独立的边：</p>
<pre><code class="language-python">builder.add_edge(&quot;node_c&quot;, &quot;node_e&quot;)
builder.add_edge(&quot;node_d&quot;, &quot;node_e&quot;)
</code></pre>
<p>这表示 <strong><code>node_c</code></strong> 和 <strong><code>node_d</code></strong> 都可以分别触发 <strong><code>node_e</code></strong>，它们之间没有同步等待关系。</p>
<p>因此，这种写法对应的是 <strong>独立触发</strong>，也可以类比为 <strong>“或”触发</strong>。</p>
<p>需要特别注意：</p>
<pre><code class="language-python">builder.add_edge([&quot;node_c&quot;, &quot;node_d&quot;], &quot;node_e&quot;)
</code></pre>
<p>和</p>
<pre><code class="language-python">builder.add_edge(&quot;node_c&quot;, &quot;node_e&quot;)
builder.add_edge(&quot;node_d&quot;, &quot;node_e&quot;)
</code></pre>
<p>不是等价写法。</p>
<p>前者表示 <strong>等待多个上游全部完成后才触发一次</strong>；
后者表示 <strong>多个上游分支分别独立触发下游节点</strong>。</p>
<h3 id="4-3-2-动态扇入-mapreduce结构">4.3.2. 动态扇入-MapReduce结构</h3>
<p>在上文我们通过 <strong><code>Send</code></strong> 动态生成了多个下游任务。</p>
<p>这种模式本质上是一种<strong>动态扇出</strong>：根据运行时数据，将一个任务拆分为多个子任务，并分别发送给下游节点处理。</p>
<p>如果这些子任务分别产生中间结果，并在后续通过 <strong><code>Reducer</code></strong> 进行合并，也就是重新 <strong>扇入</strong> 到一个下游节点中，最终得到统一的结果，那么就构成了典型的 <strong><code>MapReduce</code></strong> 结构。</p>
<p><strong><code>MapReduce</code></strong> 是大数据计算中的经典模型，通常包含两个核心阶段：</p>
<ul>
<li>
<p><strong><code>Map</code>：映射阶段</strong>
将输入数据映射为中间结果。</p>
<p>在 <strong><code>LangGraph</code></strong> 中，可以理解为通过 <strong><code>Send</code></strong> 将子任务分发给多个 <strong><code>mapper</code></strong> 节点实例，每个节点实例独立完成局部计算：将部分输入数据映射为中间结果。</p>
</li>
<li>
<p><strong><code>Reduce</code>：归约阶段</strong>
将多个子任务产生的中间结果进行汇总、合并或聚合，得到最终结果。</p>
<p>在 <strong><code>LangGraph</code></strong> 中，通常由一个特定的 <strong><code>reducer</code></strong> 节点完成归约：它接收上游 <strong><code>mapper</code></strong> 节点实例产生的中间结果，处理后得到计算图的最终输出。</p>
</li>
</ul>
<p>本节实现一个经典的词频统计任务</p>
<pre><code class="language-python">输入数据
  |
  | hello world
  | hello Atguigu
  | hello LLM
  v

Map：映射为中间键值对
  |
  | (hello, 1), (world, 1)
  | (hello, 1), (Atguigu, 1)
  | (hello, 1), (LLM, 1)
  v

Shuffle / Group：按 Key 分组
  |
  | hello   -&gt; [1, 1, 1]
  | world   -&gt; [1]
  | Atguigu -&gt; [1]
  | LLM     -&gt; [1]
  v

Reduce：归约 / 聚合
  |
  | hello   -&gt; 3
  | world   -&gt; 1
  | Atguigu -&gt; 1
  | LLM     -&gt; 1
  v

最终结果
</code></pre>
<p>需要注意，本节示例中没有单独实现一个 <strong><code>Shuffle</code></strong> 节点，而是把 <strong><code>Shuffle / Group</code></strong> 的逻辑也放在了 <strong><code>reducer_node</code></strong> 中完成。</p>
<p>在下面的示例中，我们用 <strong><code>Send</code></strong> 动态派发任务，创建了多个 <strong><code>mapper_node</code></strong> 实例。</p>
<p>每个 <strong><code>mapper_node</code></strong> 实例负责处理一条输入数据，并输出中间产物：<strong>键值对</strong>。</p>
<p>随后，下游的 <strong><code>reducer_node</code></strong> 收集所有中间结果，完成 <strong><code>Shuffle / Group</code></strong> 和最终的 <strong><code>Reduce</code></strong> 聚合。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Annotated
from langgraph.graph import StateGraph, START, END
from langgraph.types import Send
from _collections_abc import Sequence
from operator import add

from loguru import logger

class OverAllState(TypedDict):
    input_values: list[str]
    entries: Annotated[list[tuple[str, int]], add]
    word_counts: dict[str, int]

def router_map(state: OverAllState) -&gt; Sequence[Send]:
    input_values = state[&quot;input_values&quot;]

    tasks = []
    for input_value in input_values:
        tasks.append(
            Send(
                &quot;mapper_node&quot;,
                {&quot;input_value&quot;: input_value}
            )
        )

    return tasks

class MaperInputState(TypedDict):
    input_value: str

def mapper_node(state: MaperInputState) -&gt; OverAllState:
    input_value = state[&quot;input_value&quot;]
    words = input_value.split(&quot; &quot;)
    entries = []

    for word in words:
        entries.append((word, 1))

    return {
        &quot;entries&quot;: entries
    }

def reducer_node(state: OverAllState) -&gt; OverAllState:
    entries = state[&quot;entries&quot;]
    logger.info(&quot;reducer entries: {}&quot;, entries)

    shuffle_dict = {}

    for k, v in entries:
        if k not in shuffle_dict:
            shuffle_dict[k] = [v]
        else:
            shuffle_dict[k].append(v)

    logger.info(&quot;reducer shuffle entries: {}&quot;, shuffle_dict)

    reduce_dict = {}

    for k, v in shuffle_dict.items():
        reduce_dict[k] = sum(v)

    return {
        &quot;word_counts&quot;: reduce_dict
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;mapper_node&quot;, mapper_node)
builder.add_node(&quot;reducer_node&quot;, reducer_node)
builder.add_conditional_edges(START, router_map, path_map=[&quot;mapper_node&quot;])
builder.add_edge(&quot;mapper_node&quot;, &quot;reducer_node&quot;)
builder.add_edge(&quot;reducer_node&quot;, END)

graph = builder.compile()
word_counts = graph.invoke(
    {&quot;input_values&quot;: [
        &quot;hello world&quot;,
        &quot;hello Atguigu&quot;,
        &quot;hello LLM&quot;
    ]}
)

print(word_counts)

from IPython.display import display
display(graph)
</code></pre>
<p>运行结果如下</p>
<p><img src="/images/langgraph-study-notes/image-20260605140650633.png" alt="image-20260605140650633" /></p>
<pre><code class="language-json">2026-06-05 13:52:01.917 | INFO     | __main__:reducer_node:45 - reducer entries: [('hello', 1), ('world', 1), ('hello', 1), ('Atguigu', 1), ('hello', 1), ('LLM', 1)]
2026-06-05 13:52:01.918 | INFO     | __main__:reducer_node:55 - reducer shuffle entries: {'hello': [1, 1, 1], 'world': [1], 'Atguigu': [1], 'LLM': [1]}
{
    &quot;input_values&quot;: [
        &quot;hello world&quot;,
        &quot;hello Atguigu&quot;,
        &quot;hello LLM&quot;,
    ],
    &quot;entries&quot;: [
        (&quot;hello&quot;, 1),
        (&quot;world&quot;, 1),
        (&quot;hello&quot;, 1),
        (&quot;Atguigu&quot;, 1),
        (&quot;hello&quot;, 1),
        (&quot;LLM&quot;, 1),
    ],
    &quot;word_counts&quot;: {
        &quot;hello&quot;: 3,
        &quot;world&quot;: 1,
        &quot;Atguigu&quot;: 1,
        &quot;LLM&quot;: 1,
    },
}
</code></pre>
<p>从结果可以看出，三个输入字符串分别被映射为了中间键值对：</p>
<pre><code class="language-python">(&quot;hello&quot;, 1), (&quot;world&quot;, 1)
(&quot;hello&quot;, 1), (&quot;Atguigu&quot;, 1)
(&quot;hello&quot;, 1), (&quot;LLM&quot;, 1)
</code></pre>
<p>由于 <strong><code>entries</code></strong> 字段使用了 <strong><code>Reducer</code></strong>（<strong><code>LangGraph</code></strong> 状态的 <strong><code>Reducer</code></strong>，而不是当前示例中的 <strong><code>reducer_node</code></strong>）：</p>
<pre><code class="language-python">entries: Annotated[list[tuple[str, int]], add]
</code></pre>
<p>所以多个 <strong><code>mapper_node</code></strong> 任务实例返回的 <strong><code>entries</code></strong> 会被合并到同一个状态字段中。</p>
<p>随后，<strong><code>reducer_node</code></strong> 读取合并后的 <strong><code>entries</code></strong>，先按单词进行分组：</p>
<pre><code class="language-python">{
    &quot;hello&quot;: [1, 1, 1],
    &quot;world&quot;: [1],
    &quot;Atguigu&quot;: [1],
    &quot;LLM&quot;: [1],
}
</code></pre>
<p>再对每个分组中的数值进行求和，得到最终词频统计结果：</p>
<pre><code class="language-python">{
    &quot;hello&quot;: 3,
    &quot;world&quot;: 1,
    &quot;Atguigu&quot;: 1,
    &quot;LLM&quot;: 1,
}
</code></pre>
<p>如果按照 <strong>任务实例</strong> 而不是 <strong>节点定义</strong> 来绘制运行图，那么这个示例实际的任务级运行图如下所示：</p>
<pre><code class="language-mermaid">flowchart TD
    START([__start__])

    START --&gt; M1[&quot;mapper_node # task 1&quot;]
    START --&gt; M2[&quot;mapper_node # task 2&quot;]
    START --&gt; M3[&quot;mapper_node # task 3&quot;]

    M1 --&gt; R[&quot;reducer_node task&quot;]
    M2 --&gt; R
    M3 --&gt; R

    R --&gt; END([__end__])
</code></pre>
<p>需要注意，<strong><code>display(graph)</code></strong> 展示的是节点级计算图，而不是运行时的任务实例图。
因此在可视化图中只会看到一个 <strong><code>mapper_node</code></strong> 节点；但在实际运行时，<strong><code>Send</code></strong> 会根据输入数据动态创建多个 <strong><code>mapper_node</code></strong> 任务实例。</p>
<h2 id="4-4-循环结构">4.4. 循环结构</h2>
<h3 id="4-4-1-循环结构的实现">4.4.1. 循环结构的实现</h3>
<p>本节通过两种方式实现经典的 <strong><code>ReAct</code></strong> 循环结构。<strong><code>LangChain Agent</code></strong> 底层运行图架构正是 <strong><code>ReAct</code></strong> 。</p>
<p><strong><code>ReAct</code></strong> 是 <strong><code>Reason + Action</code></strong> 的缩写，即“推理 + 行动”架构。其核心思想是：</p>
<ol>
<li><strong>Reason</strong>：大模型根据当前消息状态进行推理，判断是否需要调用工具；</li>
<li><strong>Action</strong>：如果需要调用工具，则生成工具调用请求；</li>
<li><strong>Observation</strong>：工具执行后，将执行结果以 <strong><code>ToolMessage</code></strong> 的形式返回给大模型；</li>
<li><strong>Loop</strong>：大模型基于新的观察结果继续推理，决定是否继续调用工具；</li>
<li><strong>Final Answer</strong>：当大模型不再发起工具调用时，生成最终回答，流程结束。</li>
</ol>
<p>因此，<strong><code>ReAct</code></strong> 本质上是一个典型的 <strong>“LLM → Tool → LLM → Tool → ... → LLM”</strong> 循环结构。</p>
<p>本节分别使用两种方式实现该循环：</p>
<ul>
<li><strong>静态实现</strong>：通过 <strong><code>add_conditional_edges()</code></strong> 在图结构中显式定义条件路由；</li>
<li><strong>动态实现</strong>：通过 <strong><code>Command(goto=...)</code></strong> 在节点返回值中触发运行时跳转。</li>
</ul>
<hr />
<h4 id="4-4-1-1-静态实现">4.4.1.1. 静态实现</h4>
<p>所谓“静态实现”，并不是指执行路径完全固定，而是指：</p>
<blockquote>
<p><strong>节点的下游候选集合在图编译阶段已经确定，运行时只是从这些候选节点中选择下一步。</strong></p>
</blockquote>
<p>在本例中，<strong><code>llm_node</code></strong> 的下游候选节点是固定的：</p>
<ul>
<li>如果大模型返回了 <strong><code>tool_calls</code></strong>，则进入 <strong><code>tool_node</code></strong>；</li>
<li>如果大模型没有返回 <strong><code>tool_calls</code></strong>，则进入 <strong><code>output_node</code></strong>。</li>
</ul>
<p>也就是说，图结构在编译阶段已经知道 <strong><code>llm_node</code></strong> 可能流向 <strong><code>tool_node</code></strong> 或 <strong><code>output_node</code></strong>，运行时只负责判断具体走哪一条路径。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import MessagesState
from langchain.messages import HumanMessage, ToolMessage, SystemMessage
from langchain.tools import tool
from langchain_deepseek import ChatDeepSeek

from random import randint
from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

@tool(parse_docstring=True)
def get_weather(city: str=&quot;北京&quot;):
    &quot;&quot;&quot;
    查询指定城市当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    return f&quot;{city} 今天天气晴朗，东南风三级，气温 25~30 ℃&quot;

@tool(parse_docstring=True)
def get_news(domain: Literal[&quot;AI&quot;, &quot;食品安全&quot;]):
    &quot;&quot;&quot;
    查询特定领域的当日热点

    Args:
        domain: 特定领域
    &quot;&quot;&quot;
    if domain == &quot;AI&quot;:
        return &quot;Anthropic 发布了 Claude Opus-4.8，但通过 API 用中文向它发送“你是谁？”时，大多数情况下返回的却是“Qwen”或“Deepseek”。&quot;
    else:
        return &quot;双汇发展子公司猪肉产品被抽检出抗生素超标37.5倍&quot;

tools = [get_weather, get_news]

model_with_tools = model.bind_tools(tools=tools)

class OverAllState(MessagesState):
    user_input: str
    final_answer: str

def input_node(state: OverAllState) -&gt;OverAllState:
    return {
        &quot;messages&quot;: [HumanMessage(state[&quot;user_input&quot;])],
    }

def llm_node(state: OverAllState) -&gt; OverAllState:
    messages = state[&quot;messages&quot;]
    ai_msg = model_with_tools.invoke(messages)
    return {
        &quot;messages&quot;: [ai_msg],
    }

def tool_node(state: OverAllState) -&gt; OverAllState:
    messages = state[&quot;messages&quot;]
    ai_msg = messages[-1]
    tool_calls = ai_msg.tool_calls

    fail_prob = 6 # 失败概率，6表示 60% 概率失败，7 -&gt; 70%，8 -&gt; 80%，依次类推

    for tool_call in tool_calls:
        if tool_call[&quot;name&quot;] == &quot;get_weather&quot;:
            # 生成 [0, 9] 范围内的随机数
            if randint(0, 9) &lt; fail_prob: # 60% 概率因 “网络波动” 而调用失败
                messages.append(
                    ToolMessage(
                        content=&quot;网络波动，调用失败，请重试&quot;,
                        tool_call_id=tool_call[&quot;id&quot;]
                    )
                )
            else:
                messages.append(get_weather.invoke(tool_call))
        elif tool_call[&quot;name&quot;] == &quot;get_news&quot;:
            # 生成 [0, 9] 范围内的随机数
            if randint(0, 9) &lt; fail_prob: # 60% 概率因 “网络波动” 而调用失败
                messages.append(
                    ToolMessage(
                        content=&quot;网络波动，调用失败，请重试&quot;,
                        tool_call_id=tool_call[&quot;id&quot;]
                    )
                )
            else:
                messages.append(get_news.invoke(tool_call))
        else:
            messages.append(
                ToolMessage(
                    content=&quot;工具名称错误，调用失败，请重试&quot;,
                    tool_call_id=tool_call[&quot;id&quot;]
                )
            )

    return {
        &quot;messages&quot;: messages
    }

def output_node(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;final_answer&quot;: state[&quot;messages&quot;][-1].content
    }

def router(state: OverAllState) -&gt; Literal[&quot;tool_node&quot;, &quot;output_node&quot;]:
    messages = state[&quot;messages&quot;]
    lst_msg = messages[-1]

    if lst_msg.tool_calls:
        return &quot;tool_node&quot;
    return &quot;output_node&quot;

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;input_node&quot;, input_node)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, tool_node)
builder.add_node(&quot;output_node&quot;, output_node)

builder.add_edge(START, &quot;input_node&quot;)
builder.add_edge(&quot;input_node&quot;, &quot;llm_node&quot;)
builder.add_conditional_edges(
    &quot;llm_node&quot;,
    router
)
builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)
builder.add_edge(&quot;output_node&quot;, END)

graph = builder.compile()
ai_res = graph.invoke({
    &quot;user_input&quot;: &quot;帮我查询杭州当日天气和AI热点&quot;,
    &quot;messages&quot;: [SystemMessage(&quot;如果工具调用失败，必须重新调用直至成功&quot;)]
})
food_safety_res = graph.invoke({
    &quot;user_input&quot;: &quot;帮我查询当日天气和食品安全相关的热点&quot;,
    &quot;messages&quot;: [SystemMessage(&quot;如果工具调用失败，必须重新调用直至成功&quot;)]
})

print('=' * 30, '-&gt; ai_res &lt;-', '=' * 30)
print(&quot;user_input: &quot;, ai_res[&quot;user_input&quot;])
print(&quot;final_answer: &quot;, ai_res[&quot;final_answer&quot;])
for msg in ai_res[&quot;messages&quot;]:
    msg.pretty_print()

print('=' * 30, '-&gt; food_safety_res &lt;-', '=' * 30)
print(&quot;user_input: &quot;, food_safety_res[&quot;user_input&quot;])
print(&quot;final_answer: &quot;, food_safety_res[&quot;final_answer&quot;])
for msg in food_safety_res[&quot;messages&quot;]:
    msg.pretty_print()

from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code class="language-json">============================== -&gt; ai_res &lt;- ==============================
user_input:  帮我查询杭州当日天气和AI热点
final_answer:  查询成功！以下是结果汇总：

---

### 🌤️ 杭州当日天气
- **天气状况**：晴朗
- **气温范围**：25°C ~ 30°C
- **风力**：东南风三级

### 🤖 AI热点
**Anthropic 发布了 Claude Opus-4.8**，但通过 API 用中文向它发送&quot;你是谁？&quot;时，大多数情况下返回的却是&quot;Qwen&quot;或&quot;Deepseek&quot;。（这条AI新闻可能反映了当前大模型领域的一些有趣现象或争议）

---

杭州今天天气不错，晴朗舒适，适合外出活动！而AI领域似乎又有了新的模型发布动态。您还想了解其他信息吗？
================================[1m System Message [0m================================

如果工具调用失败，必须重新调用直至成功
================================[1m Human Message [0m=================================

帮我查询杭州当日天气和AI热点
==================================[1m Ai Message [0m==================================

好的，我来同时查询杭州的天气和AI热点。
Tool Calls:
  get_weather (call_00_DPkgkfOYSBOuic3ipHj11702)
 Call ID: call_00_DPkgkfOYSBOuic3ipHj11702
  Args:
    city: 杭州
  get_news (call_01_clcon3yt50HhIKGKBaxl4926)
 Call ID: call_01_clcon3yt50HhIKGKBaxl4926
  Args:
    domain: AI
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
==================================[1m Ai Message [0m==================================

两次调用都因网络波动失败了，让我重新尝试查询。
Tool Calls:
  get_weather (call_00_ruPVWhBySBDG9B9AR7vN4587)
 Call ID: call_00_ruPVWhBySBDG9B9AR7vN4587
  Args:
    city: 杭州
  get_news (call_01_ENBOMtrHXaJn7u1o7PPS6090)
 Call ID: call_01_ENBOMtrHXaJn7u1o7PPS6090
  Args:
    domain: AI
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
=================================[1m Tool Message [0m=================================
Name: get_news

Anthropic 发布了 Claude Opus-4.8，但通过 API 用中文向它发送“你是谁？”时，大多数情况下返回的却是“Qwen”或“Deepseek”。
==================================[1m Ai Message [0m==================================

天气查询仍然失败，我再次尝试查询天气。
Tool Calls:
  get_weather (call_00_BSSyk3lOTQxZEzXLmbJr7873)
 Call ID: call_00_BSSyk3lOTQxZEzXLmbJr7873
  Args:
    city: 杭州
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
==================================[1m Ai Message [0m==================================
Tool Calls:
  get_weather (call_00_WQZzpmwpfIQfGjkO98752270)
 Call ID: call_00_WQZzpmwpfIQfGjkO98752270
  Args:
    city: 杭州
=================================[1m Tool Message [0m=================================
Name: get_weather

杭州 今天天气晴朗，东南风三级，气温 25~30 ℃
==================================[1m Ai Message [0m==================================

查询成功！以下是结果汇总：

---

### 🌤️ 杭州当日天气
- **天气状况**：晴朗
- **气温范围**：25°C ~ 30°C
- **风力**：东南风三级

### 🤖 AI热点
**Anthropic 发布了 Claude Opus-4.8**，但通过 API 用中文向它发送&quot;你是谁？&quot;时，大多数情况下返回的却是&quot;Qwen&quot;或&quot;Deepseek&quot;。（这条AI新闻可能反映了当前大模型领域的一些有趣现象或争议）

---

杭州今天天气不错，晴朗舒适，适合外出活动！而AI领域似乎又有了新的模型发布动态。您还想了解其他信息吗？
============================== -&gt; food_safety_res &lt;- ==============================
user_input:  帮我查询当日天气和食品安全相关的热点
final_answer:  查询成功！以下是您需要的信息：

---

### 🌤️ 当日天气（北京）
- **天气状况：** 晴朗
- **气温：** 25~30℃
- **风力：** 东南风三级

### 🔍 食品安全热点
**双汇发展子公司猪肉产品被抽检出抗生素超标37.5倍**
&gt; 这是一则关于知名食品企业双汇旗下子公司猪肉产品被检出抗生素残留严重超标的新闻，超标幅度高达37.5倍，属于较为严重的食品安全问题。

---

如果您想查询其他城市的天气，也可以告诉我哦！
================================[1m System Message [0m================================

如果工具调用失败，必须重新调用直至成功
================================[1m Human Message [0m=================================

帮我查询当日天气和食品安全相关的热点
==================================[1m Ai Message [0m==================================

好的，我来同时查询当日天气和食品安全相关的热点。
Tool Calls:
  get_weather (call_00_NkvnFHT4M4h9wwENZmyu9753)
 Call ID: call_00_NkvnFHT4M4h9wwENZmyu9753
  Args:
    city: 北京
  get_news (call_01_rbFBctitbMLZQivR1TYd2949)
 Call ID: call_01_rbFBctitbMLZQivR1TYd2949
  Args:
    domain: 食品安全
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
=================================[1m Tool Message [0m=================================
Name: get_news

双汇发展子公司猪肉产品被抽检出抗生素超标37.5倍
==================================[1m Ai Message [0m==================================

天气查询因为网络波动失败了，我重新查询一下天气信息。
Tool Calls:
  get_weather (call_00_fWmSdUOseojF7PADvgOA7804)
 Call ID: call_00_fWmSdUOseojF7PADvgOA7804
  Args:
    city: 北京
=================================[1m Tool Message [0m=================================
Name: get_weather

北京 今天天气晴朗，东南风三级，气温 25~30 ℃
==================================[1m Ai Message [0m==================================

查询成功！以下是您需要的信息：

---

### 🌤️ 当日天气（北京）
- **天气状况：** 晴朗
- **气温：** 25~30℃
- **风力：** 东南风三级

### 🔍 食品安全热点
**双汇发展子公司猪肉产品被抽检出抗生素超标37.5倍**
&gt; 这是一则关于知名食品企业双汇旗下子公司猪肉产品被检出抗生素残留严重超标的新闻，超标幅度高达37.5倍，属于较为严重的食品安全问题。

---

如果您想查询其他城市的天气，也可以告诉我哦！
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260529172457629.png" alt="image-20260529172457629" /></p>
<p>本例运行图中的循环结构如下：</p>
<pre><code class="language-text">START
  ↓
input_node
  ↓
llm_node
  ├── 有 tool_calls → tool_node → llm_node
  └── 无 tool_calls → output_node → END
</code></pre>
<p>其中，循环发生在：</p>
<pre><code class="language-text">llm_node → tool_node → llm_node
</code></pre>
<p>只要大模型持续返回工具调用，流程就会不断回到 <strong><code>llm_node</code></strong>，形成 <strong><code>ReAct</code></strong> 循环。</p>
<p>需要注意的是，本例中“工具调用失败后继续重试”主要依赖这条系统提示词：</p>
<pre><code class="language-python">SystemMessage(&quot;如果工具调用失败，必须重新调用直至成功&quot;)
</code></pre>
<p>也就是说，是否继续重试，在当前实现中主要由大模型决定，而不是由程序逻辑强制保证。</p>
<p>因此，如果模型在多次工具调用失败后选择停止重试并生成最终回答，运行图不会阻止它。</p>
<hr />
<h4 id="4-4-1-2-动态实现">4.4.1.2. 动态实现</h4>
<p>除了静态路由，我们还可以利用 <strong><code>Command</code></strong> 在运行时动态指定下一个要执行的节点。</p>
<p>在本节的实现中，<strong><code>llm_node</code></strong> 借助 <strong><code>Command</code></strong> 在返回值中同时完成两件事：</p>
<ul>
<li>更新状态；</li>
<li>控制下一步的跳转。</li>
</ul>
<p>这里的 <strong>动态</strong> 是指：</p>
<blockquote>
<p><strong>节点在运行时，根据自身的执行结果，直接返回“状态更新 + 下一跳控制指令”。</strong></p>
</blockquote>
<p>在本例中，<strong><code>llm_node</code></strong> 调用大模型后，会根据返回结果决定后续流程：</p>
<ul>
<li>如果 <strong><code>ai_msg.tool_calls</code></strong> 不为空，则 <strong><code>goto=&quot;tool_node&quot;</code></strong>；</li>
<li>如果 <strong><code>ai_msg.tool_calls</code></strong> 为空，则 <strong><code>goto=&quot;output_node&quot;</code></strong>。</li>
</ul>
<p>这样一来，路由逻辑就被内聚在 <strong><code>llm_node</code></strong> 节点，不再需要额外定义 <strong><code>router()</code></strong> 函数，也不再需要调用 <strong><code>add_conditional_edges()</code></strong> 从 <strong><code>llm_node</code></strong> 添加条件边。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import Literal
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import MessagesState
from langgraph.types import Command
from langchain.messages import HumanMessage, ToolMessage, SystemMessage
from langchain.tools import tool
from langchain_deepseek import ChatDeepSeek

from random import randint
from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model=&quot;deepseek-v4-flash&quot;,
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

@tool(parse_docstring=True)
def get_weather(city: str=&quot;北京&quot;):
    &quot;&quot;&quot;
    查询指定城市当日天气

    Args:
        city: 城市名称
    &quot;&quot;&quot;
    return f&quot;{city} 今天天气晴朗，东南风三级，气温 25~30 ℃&quot;

@tool(parse_docstring=True)
def get_news(domain: Literal[&quot;AI&quot;, &quot;食品安全&quot;]):
    &quot;&quot;&quot;
    查询特定领域的当日热点

    Args:
        domain: 特定领域
    &quot;&quot;&quot;
    if domain == &quot;AI&quot;:
        return &quot;Anthropic 发布了 Claude Opus-4.8，但通过 API 用中文向它发送“你是谁？”时，大多数情况下返回的却是“Qwen”或“Deepseek”。&quot;
    else:
        return &quot;双汇发展子公司猪肉产品被抽检出抗生素超标37.5倍&quot;

tools = [get_weather, get_news]

model_with_tools = model.bind_tools(tools=tools)

class OverAllState(MessagesState):
    user_input: str
    final_answer: str

def input_node(state: OverAllState) -&gt;OverAllState:
    return {
        &quot;messages&quot;: [HumanMessage(state[&quot;user_input&quot;])],
    }

def llm_node(state: OverAllState) -&gt; Command[Literal[&quot;tool_node&quot;, &quot;output_node&quot;]]:
    messages = state[&quot;messages&quot;]
    ai_msg = model_with_tools.invoke(messages)
    if ai_msg.tool_calls:
        goto = &quot;tool_node&quot;
    else:
        goto = &quot;output_node&quot;

    return Command(
        goto=goto,
        update={
            &quot;messages&quot;: [ai_msg],
        }
    )

def tool_node(state: OverAllState) -&gt; OverAllState:
    messages = state[&quot;messages&quot;]
    ai_msg = messages[-1]
    tool_calls = ai_msg.tool_calls

    fail_prob = 6 # 失败概率，6表示 60% 概率失败，7 -&gt; 70%，8 -&gt; 80%，依次类推

    for tool_call in tool_calls:
        if tool_call[&quot;name&quot;] == &quot;get_weather&quot;:
            # 生成 [0, 9] 范围内的随机数
            if randint(0, 9) &lt; fail_prob: # 60% 概率因 “网络波动” 而调用失败
                messages.append(
                    ToolMessage(
                        content=&quot;网络波动，调用失败，请重试&quot;,
                        tool_call_id=tool_call[&quot;id&quot;]
                    )
                )
            else:
                messages.append(get_weather.invoke(tool_call))
        elif tool_call[&quot;name&quot;] == &quot;get_news&quot;:
            # 生成 [0, 9] 范围内的随机数
            if randint(0, 9) &lt; fail_prob: # 60% 概率因 “网络波动” 而调用失败
                messages.append(
                    ToolMessage(
                        content=&quot;网络波动，调用失败，请重试&quot;,
                        tool_call_id=tool_call[&quot;id&quot;]
                    )
                )
            else:
                messages.append(get_news.invoke(tool_call))
        else:
            messages.append(
                ToolMessage(
                    content=&quot;工具名称错误，调用失败，请重试&quot;,
                    tool_call_id=tool_call[&quot;id&quot;]
                )
            )

    return {
        &quot;messages&quot;: messages
    }

def output_node(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;final_answer&quot;: state[&quot;messages&quot;][-1].content
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;input_node&quot;, input_node)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_node(&quot;tool_node&quot;, tool_node)
builder.add_node(&quot;output_node&quot;, output_node)

builder.add_edge(START, &quot;input_node&quot;)
builder.add_edge(&quot;input_node&quot;, &quot;llm_node&quot;)
builder.add_edge(&quot;tool_node&quot;, &quot;llm_node&quot;)
builder.add_edge(&quot;output_node&quot;, END)

graph = builder.compile()
ai_res = graph.invoke({
    &quot;user_input&quot;: &quot;帮我查询杭州当日天气和AI热点&quot;,
    &quot;messages&quot;: [SystemMessage(&quot;如果工具调用失败，必须重新调用直至成功&quot;)]
})
food_safety_res = graph.invoke({
    &quot;user_input&quot;: &quot;帮我查询当日天气和食品安全相关的热点&quot;,
    &quot;messages&quot;: [SystemMessage(&quot;如果工具调用失败，必须重新调用直至成功&quot;)]
})

print('=' * 30, '-&gt; ai_res &lt;-', '=' * 30)
print(&quot;user_input: &quot;, ai_res[&quot;user_input&quot;])
print(&quot;final_answer: &quot;, ai_res[&quot;final_answer&quot;])
for msg in ai_res[&quot;messages&quot;]:
    msg.pretty_print()

print('=' * 30, '-&gt; food_safety_res &lt;-', '=' * 30)
print(&quot;user_input: &quot;, food_safety_res[&quot;user_input&quot;])
print(&quot;final_answer: &quot;, food_safety_res[&quot;final_answer&quot;])
for msg in food_safety_res[&quot;messages&quot;]:
    msg.pretty_print()

from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下所示</strong></p>
<pre><code class="language-json">============================== -&gt; ai_res &lt;- ==============================
user_input:  帮我查询杭州当日天气和AI热点
final_answer:  以下是查询结果：

---

### 🌤️ 杭州当日天气
- **天气状况**：晴朗
- **气温**：25~30 ℃
- **风力**：东南风三级

### 🤖 AI 热点
- **Anthropic 发布了 Claude Opus-4.8**，但有趣的是，通过 API 用中文向它发送&quot;你是谁？&quot;时，大多数情况下返回的却是&quot;Qwen&quot;或&quot;Deepseek&quot;。

---

看起来杭州今天天气不错，适合外出活动！而AI领域这则关于Claude的有趣现象也引发了不少讨论。还有其他想了解的吗？
================================[1m System Message [0m================================

如果工具调用失败，必须重新调用直至成功
================================[1m Human Message [0m=================================

帮我查询杭州当日天气和AI热点
==================================[1m Ai Message [0m==================================

好的，我来同时查询杭州的天气和AI热点。
Tool Calls:
  get_weather (call_00_QAXNeCIuy4MEe91ZiBQp0274)
 Call ID: call_00_QAXNeCIuy4MEe91ZiBQp0274
  Args:
    city: 杭州
  get_news (call_01_5hXZth3YLTZFZZKE48i62455)
 Call ID: call_01_5hXZth3YLTZFZZKE48i62455
  Args:
    domain: AI
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
=================================[1m Tool Message [0m=================================
Name: get_news

Anthropic 发布了 Claude Opus-4.8，但通过 API 用中文向它发送“你是谁？”时，大多数情况下返回的却是“Qwen”或“Deepseek”。
==================================[1m Ai Message [0m==================================

天气查询遇到了网络波动，我重新尝试一下。
Tool Calls:
  get_weather (call_00_ooZcTsB9ntQRU3tXDrVj7186)
 Call ID: call_00_ooZcTsB9ntQRU3tXDrVj7186
  Args:
    city: 杭州
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
==================================[1m Ai Message [0m==================================

再次重试：
Tool Calls:
  get_weather (call_00_EwVrJmFUZaYZfvkOxveK7822)
 Call ID: call_00_EwVrJmFUZaYZfvkOxveK7822
  Args:
    city: 杭州
=================================[1m Tool Message [0m=================================
Name: get_weather

杭州 今天天气晴朗，东南风三级，气温 25~30 ℃
==================================[1m Ai Message [0m==================================

以下是查询结果：

---

### 🌤️ 杭州当日天气
- **天气状况**：晴朗
- **气温**：25~30 ℃
- **风力**：东南风三级

### 🤖 AI 热点
- **Anthropic 发布了 Claude Opus-4.8**，但有趣的是，通过 API 用中文向它发送&quot;你是谁？&quot;时，大多数情况下返回的却是&quot;Qwen&quot;或&quot;Deepseek&quot;。

---

看起来杭州今天天气不错，适合外出活动！而AI领域这则关于Claude的有趣现象也引发了不少讨论。还有其他想了解的吗？
============================== -&gt; food_safety_res &lt;- ==============================
user_input:  帮我查询当日天气和食品安全相关的热点
final_answer:  非常抱歉，天气查询接口持续出现网络波动，暂时无法获取到天气信息。以下是目前已获取到的信息：

---

### 📰 食品安全热点
**双汇发展子公司猪肉产品被抽检出抗生素超标37.5倍**
&gt; 这是今日食品安全领域的一条重要新闻，涉及知名肉制品企业双汇发展，其子公司检测出猪肉产品中抗生素残留超标情况较为严重。

---

### 🌤️ 天气查询
天气接口当前多次重试均因网络波动失败，暂时无法获取当日天气信息，建议稍后再尝试查询。
================================[1m System Message [0m================================

如果工具调用失败，必须重新调用直至成功
================================[1m Human Message [0m=================================

帮我查询当日天气和食品安全相关的热点
==================================[1m Ai Message [0m==================================

我来为你查询当日天气和食品安全相关的热点。
Tool Calls:
  get_weather (call_00_JYYAWJk569VD0TZ4LCyr0840)
 Call ID: call_00_JYYAWJk569VD0TZ4LCyr0840
  Args:
    city: 北京
  get_news (call_01_YkzloPKV0DVwL1r5nBgi8164)
 Call ID: call_01_YkzloPKV0DVwL1r5nBgi8164
  Args:
    domain: 食品安全
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
=================================[1m Tool Message [0m=================================
Name: get_news

双汇发展子公司猪肉产品被抽检出抗生素超标37.5倍
==================================[1m Ai Message [0m==================================

天气查询遇到网络波动，我来重试一下。
Tool Calls:
  get_weather (call_00_MjNdBAQz2gW6EzZavsZ91123)
 Call ID: call_00_MjNdBAQz2gW6EzZavsZ91123
  Args:
    city: 北京
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
==================================[1m Ai Message [0m==================================
Tool Calls:
  get_weather (call_00_pkcSelcAJiFDNpU7II0d9212)
 Call ID: call_00_pkcSelcAJiFDNpU7II0d9212
  Args:
    city: 北京
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
==================================[1m Ai Message [0m==================================
Tool Calls:
  get_weather (call_00_QKgt5xYTos0H1c6CIS9a6172)
 Call ID: call_00_QKgt5xYTos0H1c6CIS9a6172
  Args:
    city: 北京
=================================[1m Tool Message [0m=================================

网络波动，调用失败，请重试
==================================[1m Ai Message [0m==================================

非常抱歉，天气查询接口持续出现网络波动，暂时无法获取到天气信息。以下是目前已获取到的信息：

---

### 📰 食品安全热点
**双汇发展子公司猪肉产品被抽检出抗生素超标37.5倍**
&gt; 这是今日食品安全领域的一条重要新闻，涉及知名肉制品企业双汇发展，其子公司检测出猪肉产品中抗生素残留超标情况较为严重。

---

### 🌤️ 天气查询
天气接口当前多次重试均因网络波动失败，暂时无法获取当日天气信息，建议稍后再尝试查询。
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260529172516935.png" alt="image-20260529172516935" /></p>
<p>本例和<strong>静态实现</strong>的核心区别体现在 <strong><code>llm_node</code></strong> 中：</p>
<pre><code class="language-python">def llm_node(state: OverAllState) -&gt; Command[Literal[&quot;tool_node&quot;, &quot;output_node&quot;]]:
    ai_msg = model_with_tools.invoke(state[&quot;messages&quot;])

    if ai_msg.tool_calls:
        goto = &quot;tool_node&quot;
    else:
        goto = &quot;output_node&quot;

    return Command(
        update={
            &quot;messages&quot;: [ai_msg],
        },
        goto=goto,
    )
</code></pre>
<p>其返回值类型：</p>
<pre><code class="language-python">Command[Literal[&quot;tool_node&quot;, &quot;output_node&quot;]]
</code></pre>
<p>表明该节点返回的是一个 <strong><code>Command</code></strong> 对象，且 <strong><code>goto</code></strong> 字段的值只能是：</p>
<ul>
<li><strong><code>&quot;tool_node&quot;</code></strong></li>
<li>或 <strong><code>&quot;output_node&quot;</code></strong></li>
</ul>
<p>这是静态约束，不会参与运行时校验。主要作用是提升代码可读性、通过静态类型检查，同时帮助 <strong><code>LangGraph</code></strong> 更准确地推断图结构。</p>
<hr />
<h4 id="4-4-1-3-小结">4.4.1.3. 小结</h4>
<table>
<thead>
<tr>
<th></th>
<th>静态实现</th>
<th>动态实现</th>
</tr>
</thead>
<tbody>
<tr>
<td>路由位置</td>
<td>路由逻辑在独立的 <strong><code>router()</code></strong> 函数中</td>
<td>路由逻辑写在节点返回值中</td>
</tr>
<tr>
<td>图结构表达</td>
<td>显式声明条件边，更直观</td>
<td>控制逻辑更内聚，代码更紧凑</td>
</tr>
<tr>
<td>适合场景</td>
<td>路由规则独立、希望图结构更清晰</td>
<td>节点执行结果直接决定下一跳</td>
</tr>
</tbody>
</table>
<p>两种方式都能实现 <strong><code>ReAct</code></strong> 循环，本质区别不在于是否能循环，而在于：</p>
<blockquote>
<p><strong>路由逻辑写在哪里。</strong></p>
</blockquote>
<ul>
<li>基于 <strong><code>add_conditional_edges()</code></strong> 的静态实现：把控制逻辑放在图结构定义阶段；</li>
<li>基于 <strong><code>Command(goto=...)</code></strong> 的动态实现：把控制逻辑放在节点返回值中。</li>
</ul>
<hr />
<h3 id="4-4-2-引入递归限制">4.4.2. 引入递归限制</h3>
<p>在存在循环结构的图中，如果没有合理的停止条件，运行图可能会一直循环执行。为了避免无限循环，<strong><code>LangGraph</code></strong> 提供了<strong>递归限制</strong>机制，用于限制单次图运行过程中允许执行的最大 <strong><code>SuperStep</code></strong> 数量。</p>
<p>当运行图在达到停止条件之前耗尽允许的最大步数时，<strong><code>LangGraph</code></strong> 会抛出 <strong><code>GraphRecursionError</code></strong>。开发者既可以在图内部提前检测剩余步数并优雅退出，也可以在图外部捕获异常并集中处理。</p>
<h4 id="4-4-2-1-步骤计数器">4.4.2.1. 步骤计数器</h4>
<h5 id="4-4-2-1-1-运行时配置对象-config">4.4.2.1.1. 运行时配置对象<code>config</code></h5>
<p><strong><code>LangGraph</code></strong> 运行节点时，会向节点函数注入运行时配置对象 <strong><code>config</code></strong>。该对象通常使用 <strong><code>RunnableConfig</code></strong> 类型标注，用于记录本次运行的配置信息和部分运行时元数据。</p>
<p>节点函数的第一个参数必须是运行时状态。如果需要访问运行时配置，可以在状态参数之后声明额外参数 <strong><code>config</code></strong>。此时，<strong><code>LangGraph</code></strong> 会在调用节点函数之前，将运行时配置对象以关键字参数的方式传入节点函数。</p>
<p><strong><code>config</code></strong> 的元数据中记录了当前节点所在的 <strong><code>SuperStep</code></strong> 序号，可以通过下面的方式获取：</p>
<pre><code class="language-python">config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
</code></pre>
<p>需要注意的是，这里的步骤编号对应图运行过程中的 <strong><code>SuperStep</code></strong>，从 <code>1</code> 开始计数。对于顺序执行的节点，不同节点通常位于不同的 <strong><code>SuperStep</code></strong>；对于并行执行的节点，多个节点可能处于同一个  <strong><code>SuperStep</code></strong>，因此它们读取到的 <strong><code>langgraph_step</code></strong> 可能相同。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langchain_core.runnables import RunnableConfig

from loguru import logger

class EmptyState(TypedDict):
    pass

def node_a(state: EmptyState, config: RunnableConfig) -&gt; EmptyState | None:
    current_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(current_step)

def node_b(state: EmptyState, config: RunnableConfig) -&gt; EmptyState | None:
    current_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(current_step)

def node_c(state: EmptyState, config: RunnableConfig) -&gt; EmptyState | None:
    current_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(current_step)

builder = StateGraph(state_schema=EmptyState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, &quot;node_c&quot;)
builder.add_edge(&quot;node_c&quot;, END)

graph = builder.compile()
graph.invoke({})

from IPython.display import display
display(graph)
</code></pre>
<p>运行结果如下</p>
<pre><code>2026-05-29 19:40:07.569 | INFO     | __main__:node_a:12 - 1
2026-05-29 19:40:07.571 | INFO     | __main__:node_b:16 - 2
2026-05-29 19:40:07.573 | INFO     | __main__:node_c:20 - 3
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260529195252649.png" alt="image-20260529195252649" /></p>
<p>可以看到，<strong><code>node_a</code></strong>、<strong><code>node_b</code></strong>、<strong><code>node_c</code></strong> 是顺序执行的三个节点，因此分别处于第 <strong><code>1</code></strong>、<strong><code>2</code></strong>、<strong><code>3</code></strong> 个 <strong><code>SuperStep</code></strong>。</p>
<h4 id="4-4-2-2-配置-递归限制">4.4.2.2. 配置<strong>递归限制</strong></h4>
<p><strong><code>recursion_limit</code></strong> 表示单次图运行过程中允许执行的最大 <strong><code>SuperStep</code></strong> 数量。可以在调用运行图时通过 <strong><code>config</code></strong> 显式配置：</p>
<pre><code class="language-python">graph.invoke(input, config={&quot;recursion_limit&quot;: 10})
</code></pre>
<p>需要注意的是，<strong><code>recursion_limit</code> 的默认值可能随版本变化、运行环境切换而变化</strong>。</p>
<h5 id="4-4-2-2-1-版本的变化">4.4.2.2.1. 版本的变化</h5>
<p>官方文档提到：从 <strong><code>LangGraph 1.0.6</code></strong> 开始，<strong><code>recursion_limit</code></strong> 默认为 <strong><code>1000</code></strong>，本项目运行在 <strong><code>LangGraph 1.1.2</code></strong> 版本，实测发现 <strong><code>recursion_limit</code></strong> 默认为 <strong><code>10000</code></strong>。</p>
<h5 id="4-4-2-2-2-环境的变化">4.4.2.2.2. 环境的变化</h5>
<p>阅读源码可知，<strong><code>LangGraph</code></strong> 运行时可能从多个来源读取默认值</p>
<p>如下：</p>
<table>
<thead>
<tr>
<th style="text-align:left">来源</th>
<th style="text-align:left">默认值</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>langchain_core.runnables.config</code></strong></td>
<td style="text-align:left"><strong><code>25</code></strong></td>
<td style="text-align:left"><strong><code>RunnableConfig</code></strong> 中定义的默认值，<strong><code>LangChain Agent</code></strong> 运行时生效</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>langgraph._internal._config</code></strong></td>
<td style="text-align:left"><strong><code>10000</code></strong></td>
<td style="text-align:left"><strong><code>LangGraph</code></strong> 本地图运行时内部配置中的默认值，自定义 <strong><code>LangGraph</code></strong> 图结构时生效</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>langgraph_api.utils.config</code></strong></td>
<td style="text-align:left"><strong><code>10011</code></strong></td>
<td style="text-align:left">**<code>LangGraph API / 服务侧</code>**相关配置中的默认值</td>
</tr>
</tbody>
</table>
<p>对于自定义图结构的 <strong><code>LangGraph</code></strong> 运行时，其默认值取自 <strong><code>langgraph._internal._config</code></strong>，所以调试看到的默认值为 <strong><code>10000</code></strong>。</p>
<h5 id="4-4-2-2-3-最佳实践">4.4.2.2.3. 最佳实践</h5>
<p>在实际开发中，建议显式传入 <strong><code>recursion_limit</code></strong>，避免不同版本、不同运行入口或不同部署方式下默认值不同导致实际运行结果和预期不符。</p>
<h4 id="4-4-2-3-优雅退出-主动方法-proactive-approach">4.4.2.3. 优雅退出：主动方法（Proactive Approach）</h4>
<p><strong><code>RemainingSteps</code></strong> 是 <strong><code>LangGraph</code></strong> 提供的特殊托管值，表示<strong>剩余可用步数</strong>，由运行时维护。</p>
<p><strong><code>LangGraph</code></strong> 运行时会根据当前步数和 <strong><code>recursion_limit</code></strong> 计算剩余步数，并填充到 <strong><code>RemainingSteps</code></strong> 类型的状态字段中。开发者可以在状态中声明一个 <strong><code>RemainingSteps</code></strong> 类型的字段，来获取<strong>剩余可用步数</strong>。</p>
<p>借助 <strong><code>RemainingSteps</code></strong>，开发者可以在图内部提前判断剩余步数是否充足，并根据剩余步数动态路由。例如，当剩余步数较少时，不再继续循环，而是路由到 <strong><code>END</code></strong> 或兜底节点，从而让运行图正常结束。</p>
<p>这种方式是在达到递归限制之前主动处理，因此称为 <strong>主动方法（Proactive Approach）</strong>。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict, Literal
from langgraph.graph import StateGraph, START, END
from langgraph.managed import RemainingSteps
from langchain_core.runnables.config import RunnableConfig

from loguru import logger

class OverAllState(TypedDict):
    remaining_steps: RemainingSteps

def loop_node(state: OverAllState, config: RunnableConfig) -&gt; OverAllState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    remaining_steps = state[&quot;remaining_steps&quot;]
    logger.info(&quot;loop_node, cur_step: {}, remaining_step: {}&quot;, cur_step, remaining_steps)

def router(state: OverAllState) -&gt; Literal[&quot;loop_node&quot;, END]:
    remaining_steps = state[&quot;remaining_steps&quot;]
    if remaining_steps &lt; 3:
        logger.info(&quot;当前可用超步：{}，已不足2步，终止运行图&quot;, remaining_steps)
        return END
    return &quot;loop_node&quot;

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;loop_node&quot;, loop_node)
builder.add_edge(START, &quot;loop_node&quot;)
builder.add_conditional_edges(&quot;loop_node&quot;, router)

graph = builder.compile()
graph.invoke({}, config={&quot;recursion_limit&quot;: 10})

from IPython.display import display
display(graph)
</code></pre>
<p><strong>输出如下</strong></p>
<pre><code>2026-06-01 10:23:40.412 | INFO | __main__:loop_node:14 - loop_node, cur_step: 1, remaining_step: 9
2026-06-01 10:23:40.413 | INFO | __main__:loop_node:14 - loop_node, cur_step: 2, remaining_step: 8
2026-06-01 10:23:40.414 | INFO | __main__:loop_node:14 - loop_node, cur_step: 3, remaining_step: 7
2026-06-01 10:23:40.414 | INFO | __main__:loop_node:14 - loop_node, cur_step: 4, remaining_step: 6
2026-06-01 10:23:40.415 | INFO | __main__:loop_node:14 - loop_node, cur_step: 5, remaining_step: 5
2026-06-01 10:23:40.416 | INFO | __main__:loop_node:14 - loop_node, cur_step: 6, remaining_step: 4
2026-06-01 10:23:40.417 | INFO | __main__:loop_node:14 - loop_node, cur_step: 7, remaining_step: 3
2026-06-01 10:23:40.417 | INFO | __main__:loop_node:14 - loop_node, cur_step: 8, remaining_step: 2
2026-06-01 10:23:40.418 | INFO | __main__:router:19    - 当前可用超步：2，已不足2步，终止运行图
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260601102425369.png" alt="image-20260601102425369" /></p>
<p>本例中，<strong><code>recursion_limit</code></strong> 被显式设置为 <strong><code>10</code></strong>。</p>
<p>第 <strong><code>1</code></strong> 个 <strong><code>SuperStep</code></strong> 执行时，剩余步数为 <strong><code>9</code></strong>；第 <strong><code>2</code></strong> 个 <strong><code>SuperStep</code></strong> 执行时，剩余步数为 <strong><code>8</code></strong>；依次类推。。。当前<strong>超步序号</strong>和<strong>剩余步数之和</strong>等于 <strong><code>recursion_limit</code></strong>。</p>
<p>第 <strong><code>8</code></strong> 个 SuperStep 执行时，剩余步数为 <strong><code>2</code></strong>。此时路由函数判断剩余步数较少，不再继续循环，而是路由到 <strong><code>END</code></strong>，运行图正常结束。</p>
<p>这种方式在运行图内部主动处理，不会抛出异常，因此是<strong>优雅退出</strong>的<strong>主动方法</strong>。</p>
<h4 id="4-4-2-4-异常中断-被动方法-reactive-approach">4.4.2.4. 异常中断：被动方法（Reactive Approach）</h4>
<p>如果图中存在循环结构，并且运行图在达到停止条件之前耗尽了允许的最大步数，<strong><code>LangGraph</code></strong> 会抛出 <strong><code>GraphRecursionError</code></strong>。</p>
<p>开发者可以在图外部捕获该异常，处理递归限制超限的情况。由于这种方式是在异常已经发生之后再处理，因此称为 <strong>被动方法（<code>Reactive Approach</code>）</strong>。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.errors import GraphRecursionError
from langchain_core.runnables import RunnableConfig

from loguru import logger

class EmptyState(TypedDict):
    pass

def loop_node(state: EmptyState, config: RunnableConfig) -&gt; EmptyState:
    cur_step = config[&quot;metadata&quot;][&quot;langgraph_step&quot;]
    logger.info(&quot;loop_node, cur_step: {}&quot;, cur_step)

builder = StateGraph(state_schema=EmptyState)
builder.add_node(&quot;loop_node&quot;, loop_node)
builder.add_edge(START, &quot;loop_node&quot;)
builder.add_edge(&quot;loop_node&quot;, &quot;loop_node&quot;)

graph = builder.compile()
try:
    graph.invoke({}, config={&quot;recursion_limit&quot;: 10})
except GraphRecursionError as e:
    logger.info(&quot;超步数量达到最大限制，抛出异常: {}&quot;, e)

from IPython.display import display
display(graph)
</code></pre>
<p><strong>运行结果如下</strong></p>
<pre><code>2026-06-01 10:13:06.116 | INFO | __main__:loop_node:13 - loop_node, cur_step: 1
2026-06-01 10:13:06.117 | INFO | __main__:loop_node:13 - loop_node, cur_step: 2
2026-06-01 10:13:06.118 | INFO | __main__:loop_node:13 - loop_node, cur_step: 3
2026-06-01 10:13:06.119 | INFO | __main__:loop_node:13 - loop_node, cur_step: 4
2026-06-01 10:13:06.119 | INFO | __main__:loop_node:13 - loop_node, cur_step: 5
2026-06-01 10:13:06.120 | INFO | __main__:loop_node:13 - loop_node, cur_step: 6
2026-06-01 10:13:06.120 | INFO | __main__:loop_node:13 - loop_node, cur_step: 7
2026-06-01 10:13:06.121 | INFO | __main__:loop_node:13 - loop_node, cur_step: 8
2026-06-01 10:13:06.121 | INFO | __main__:loop_node:13 - loop_node, cur_step: 9
2026-06-01 10:13:06.122 | INFO | __main__:loop_node:13 - loop_node, cur_step: 10
2026-06-01 10:13:06.122 | INFO | __main__:&lt;module&gt;:24  - 超步数量达到最大限制，抛出异常: Recursion limit of 10 reached without hitting a stop condition. You can increase the limit by setting the `recursion_limit` config key.
                                                    For troubleshooting, visit: https://docs.langchain.com/oss/python/langgraph/errors/GRAPH_RECURSION_LIMIT
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260601104129478.png" alt="image-20260601104129478" /></p>
<p>本例中，<strong><code>loop_node</code></strong> 的下游仍然是 <strong><code>loop_node</code></strong> 本身，因此运行图会不断循环。由于没有任何停止条件，当执行步数达到 <strong><code>recursion_limit=10</code></strong> 后，LangGraph 抛出 <strong><code>GraphRecursionError</code></strong>。</p>
<p>这种方式虽然可以避免异常导致整个进程崩溃，但图本身已经被异常中断，不能像主动方法那样正常结束。</p>
<h4 id="4-4-2-5-主动方法和被动方法的对比">4.4.2.5. 主动方法和被动方法的对比</h4>
<p>主动方法和被动方法的最大区别在于：</p>
<blockquote>
<p><strong>主动方法是在图内部提前处理递归限制；被动方法是在图外部捕获<code>递归限制异常</code>。</strong></p>
</blockquote>
<table>
<thead>
<tr>
<th style="text-align:left">方法</th>
<th style="text-align:left">超限检测</th>
<th style="text-align:left">处理方法</th>
<th style="text-align:left">控制流</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">主动(借助 <strong><code>RemainingSteps</code></strong>)</td>
<td style="text-align:left">达到限制之前</td>
<td style="text-align:left">在图结构内部通过条件路由处理</td>
<td style="text-align:left">图继续运行，正常结束</td>
</tr>
<tr>
<td style="text-align:left">被动(捕获 <strong><code>GraphRecursionError</code></strong>)</td>
<td style="text-align:left">超出限制之后</td>
<td style="text-align:left">图结构外部，通过 <strong><code>try/except</code></strong> 处理</td>
<td style="text-align:left">图运行被异常中断</td>
</tr>
</tbody>
</table>
<p><strong>主动方法的优势：</strong></p>
<ul>
<li>在图内部实现优雅降级</li>
<li>可以将中间状态保存到检查点中</li>
<li>通过返回部分结果改善用户体验</li>
<li>图可以正常完成，不会抛出异常</li>
</ul>
<p><strong>被动方法的优势：</strong></p>
<ul>
<li>实现更简单</li>
<li>不需要修改图的内部逻辑</li>
<li>可以集中处理错误</li>
</ul>
<p>实际开发中，如果循环结构本身是业务逻辑的一部分，例如 <strong>ReAct 循环、多轮反思、自我修正、工具调用重试</strong> 等，更推荐使用主动方法，在图内部提前处理递归限制。</p>
<p>如果只是为了兜底防止无限循环，也可以保留被动方法，在最外层捕获 <strong><code>GraphRecursionError</code></strong>，作为最后一道防线。</p>
<h2 id="4-5-edges总结-langgraph中的边">4.5. Edges总结：LangGraph中的边</h2>
<p>上文我们已经学习了 <strong><code>LangGraph</code></strong> 中各种边的用法。本节对 <strong><code>LangGraph</code> 中的边</strong> 做一个系统性总结。</p>
<h3 id="4-5-1-langgraph-官方对边的定义和分类">4.5.1. <code>LangGraph</code>官方对边的定义和分类</h3>
<p><strong><code>Edges(边)</code></strong> 定义了节点之间的依赖关系，决定图如何根据当前状态决定下一步，以及何时停止运行。</p>
<p>边主要有以下几种类型：</p>
<h4 id="4-5-1-1-普通边-normal-edges">4.5.1.1. 普通边(Normal Edges)</h4>
<p><strong>普通边</strong> 表示从当前节点直接流转到下一个节点。</p>
<p><strong>例如</strong></p>
<pre><code class="language-python">builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
</code></pre>
<p>其含义是：<strong><code>node_a</code></strong> 执行完成后，下一步执行 <strong><code>node_b</code></strong>。</p>
<h4 id="4-5-1-2-条件边-conditional-edges">4.5.1.2. 条件边(Conditional Edges)</h4>
<p><strong>条件边</strong> 表示当前节点执行完成后，调用一个 <strong>路由函数</strong> 来决定下一步进入<strong>哪个或哪些</strong>节点。</p>
<p><strong>例如</strong></p>
<pre><code class="language-python">builder.add_conditional_edges(&quot;node_a&quot;, router)
</code></pre>
<p>其中，<strong><code>router</code></strong> 会根据当前图状态返回后续目标节点。</p>
<p>如果路由函数返回一个节点名，则下一步进入该节点；如果返回多个节点名，则这些目标节点会在下一个超步中并行执行。</p>
<h4 id="4-5-1-3-入口点-entry-point">4.5.1.3. 入口点(Entry Point)</h4>
<p><strong>入口点</strong> 表示图开始运行时，首先执行哪个节点。</p>
<p>本质上，入口点就是一条 <strong>以 <code>START</code> 为起点的普通边</strong>。</p>
<p><strong>例如</strong></p>
<pre><code class="language-python">builder.add_edge(START, &quot;node_a&quot;)
</code></pre>
<p>其含义是：当用户输入进入图时，首先执行 <strong><code>node_a</code></strong>。</p>
<p><strong><code>LangGraph</code></strong> 也提供了专门的 API 来声明入口点：</p>
<pre><code class="language-python">builder.set_entry_point(&quot;node_a&quot;)
</code></pre>
<p><strong>二者是等价的。</strong></p>
<h4 id="4-5-1-4-条件入口点-conditional-entry-point">4.5.1.4. 条件入口点(Conditional Entry Point)</h4>
<p><strong>条件入口点</strong> 表示图开始运行时，并不固定进入某一个节点，而是先调用一个 <strong>路由函数</strong>，再根据路由结果决定首先执行哪个节点，或者哪些节点。</p>
<p>本质上，条件入口点就是一条 <strong>以 <code>START</code> 为起点的条件边</strong>。</p>
<p><strong>例如</strong></p>
<pre><code class="language-python">builder.add_conditional_edges(START, router)
</code></pre>
<p>其含义是：图开始运行时，先执行 <strong><code>router</code></strong>，再根据 <strong><code>router</code></strong> 的返回值决定首个执行节点。</p>
<h4 id="4-5-1-5-注意">4.5.1.5. 注意</h4>
<ol>
<li>一个节点可以有多个出边。此时，这些出边对应的目标节点会在下一个超步中<strong>并行</strong>执行。</li>
<li>除了上述方式，条件边还可以通过在节点中返回 <strong><code>Command(goto=...)</code></strong> 来实现，虽然方式不同，但边的性质和作用完全一致。</li>
<li>在实际开发中，建议同一个节点尽量只选择一种路由机制：
<ul>
<li>要么使用普通边，固定流转；</li>
<li>要么使用条件边或 <strong><code>Command(goto=...)</code></strong>，动态流转。</li>
</ul>
</li>
</ol>
<p>不要在同一个节点上同时混用普通边和动态路由，否则多个路径可能同时生效，最终导致运行图逻辑混乱，与预期不符。</p>
<h3 id="4-5-2-指向end的终止边">4.5.2. 指向END的终止边</h3>
<p>除了官方文档中列出的几种边，还有一类非常重要的边：<strong>指向 <code>END</code> 的边</strong>。</p>
<p>严格来说，<strong><code>END</code></strong> 不是普通业务节点，而是 <strong>LangGraph 中的特殊终止标记</strong>。当流程流转到 <strong><code>END</code></strong> 时，表示该路径不再继续执行后续节点。</p>
<p>为了便于理解，可以将指向 <strong><code>END</code></strong> 的边分为两种情况。</p>
<h4 id="4-5-2-1-终止点-finish-point">4.5.2.1. 终止点（Finish Point）</h4>
<p><strong>终止点</strong> 表示某个节点执行完成后，图流程结束。</p>
<p>本质上，它是一条 <strong>以 <code>END</code> 为目标点的普通边</strong>。</p>
<p><strong>例如</strong></p>
<pre><code class="language-python">builder.add_edge(&quot;node_a&quot;, END)
</code></pre>
<p>其含义是：当 <strong><code>node_a</code></strong> 执行完成后，当前路径终止，不再继续执行其他节点。</p>
<p><strong><code>LangGraph</code></strong> 也提供了专门的 API 来声明结束点：</p>
<pre><code class="language-python">builder.set_finish_point(&quot;node_a&quot;)
</code></pre>
<p>二者是等价的。</p>
<h4 id="4-5-2-2-条件终止点-conditional-finish-point">4.5.2.2. 条件终止点（Conditional Finish Point）</h4>
<p><strong>条件终止点</strong> 表示图在运行过程中，通过 <strong>路由函数</strong> 判断是继续执行后续节点，还是终止当前路径。</p>
<p>本质上，它是一条 <strong>路由结果中包含 <code>END</code> 的条件边</strong>。</p>
<p><strong>例如</strong></p>
<pre><code class="language-python">def router(state: EmptyState) -&gt; Literal[&quot;node_b&quot;, END]:
    if state[&quot;is_finished&quot;]:
        return END
    return &quot;node_b&quot;

builder.add_conditional_edges(&quot;node_a&quot;, router)
</code></pre>
<p>其含义是：</p>
<ul>
<li>如果 <strong><code>state[&quot;is_finished&quot;]</code></strong> 为真，则返回 <strong><code>END</code></strong>，流程终止；</li>
<li>否则返回 <strong><code>&quot;node_b&quot;</code></strong>，继续执行 <strong><code>node_b</code></strong>。</li>
</ul>
<h3 id="4-5-3-end和终止边的说明">4.5.3. END和终止边的说明</h3>
<ol>
<li>在编译后的运行图中，<strong>终止边</strong> 和 <strong><code>END</code></strong> 并没有具体的 <strong><code>Channel</code></strong> 和 <strong>节点</strong> 与之对应，它们只是<strong>终止标记</strong>，表示运行图的结束。</li>
<li>终止边的声明可以省略，<strong><code>LangGraph</code></strong> 运行时会在没有活跃节点时自然终止。但是，为了程序的可读性和图结构可视化渲染器的正常工作，最好显式声明指向 <strong><code>END</code></strong> 的边。</li>
</ol>
<h1 id="5-节点执行与容错机制">5. 节点执行与容错机制</h1>
<p>节点执行失败时，如果未做任何处理，可能导致整个运行图执行失败。为了提升运行图的稳定性，处理临时故障、超时、异常恢复以及重复计算等问题，<strong><code>LangGraph</code></strong> 提供了一系列节点执行与容错机制：重试、超时设置、异常处理和节点缓存。对应关系如下。</p>
<table>
<thead>
<tr>
<th>问题/场景</th>
<th>对应机制</th>
<th>机制类型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>临时故障</strong></td>
<td><strong>重试 Retry</strong></td>
<td><strong>节点容错机制</strong></td>
<td>网络抖动、接口偶发失败、模型服务短暂不可用，可以通过重试机制再次执行节点。</td>
</tr>
<tr>
<td><strong>超时</strong></td>
<td><strong>超时设置 Timeout</strong></td>
<td><strong>节点容错机制</strong></td>
<td>某个节点执行时间过长，可以通过超时限制避免图运行被长时间阻塞。</td>
</tr>
<tr>
<td><strong>异常恢复</strong></td>
<td><strong>异常处理 Error Handling</strong></td>
<td><strong>节点容错机制</strong></td>
<td>节点抛出异常后，通过异常捕获、兜底逻辑、降级返回、路由到错误处理节点等方式恢复流程。</td>
</tr>
<tr>
<td><strong>重复计算</strong></td>
<td><strong>缓存 Cache</strong></td>
<td><strong>节点执行优化机制</strong></td>
<td>相同输入反复触发耗时节点，可以通过节点缓存避免重复执行，提高性能。</td>
</tr>
</tbody>
</table>
<h2 id="5-1-langgraph的节点容错机制">5.1. LangGraph的节点容错机制</h2>
<p><strong><code>LangGraph</code></strong> 提供了三种异常处理机制</p>
<ul>
<li><strong><code>retries</code></strong>：重试机制。节点执行失败时，根据重试策略自动重新执行。</li>
<li><strong><code>Timeouts</code></strong>：超时控制。限制单次节点执行的最大等待时间。</li>
<li><strong><code>Error Handling</code></strong>：错误处理。重试次数耗尽后，执行特定的异常处理逻辑。</li>
</ul>
<p>三者的执行顺序如下：</p>
<ol>
<li>节点开始执行。</li>
<li>如果节点执行超时或抛出异常，本次节点尝试失败。</li>
<li>重试策略 <strong><code>RetryPolicy</code></strong> 判断该异常是否需要重试。</li>
<li>如果满足重试条件，并且尚未达到最大尝试次数，则重新执行节点。</li>
<li>如果重试次数耗尽，节点仍然失败，则进入 <strong><code>error_handler</code></strong> 错误处理逻辑。</li>
<li>如果没有配置错误处理逻辑，则异常继续向外抛出，可能导致整个图运行失败。</li>
</ol>
<p>需要注意：</p>
<ul>
<li><strong>重试机制</strong> 是当前版本中最常用、最基础的节点容错能力。</li>
<li><strong>超时控制</strong> 和 <strong>错误处理</strong> 是较新版本引入的能力，要求 <strong><code>langgraph&gt;=1.2</code></strong>。</li>
<li>本课程当前环境为 <strong><code>langgraph==1.1.2</code></strong>，因此重点讲解重试机制和节点缓存，超时控制与错误处理仅做概念说明。</li>
</ul>
<h2 id="5-2-重试机制">5.2. 重试机制</h2>
<h3 id="5-2-1-基本用法">5.2.1. 基本用法</h3>
<p>重试机制用于处理临时性异常，例如网络抖动、远程服务短暂不可用、API 偶发失败等。</p>
<p>在添加节点时，可以通过 <strong><code>retry_policy=</code></strong> 配置节点的重试策略。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END
from langgraph.types import RetryPolicy

from requests.exceptions import HTTPError
from loguru import logger

class EmptyState(TypedDict):
    pass

def node_a(state: EmptyState) -&gt; EmptyState:
    logger.info(f&quot;node a 运行&quot;)
    raise HTTPError(&quot;网络连接超时...&quot;)

builder = StateGraph(state_schema=EmptyState)
builder.add_node(
    &quot;node_a&quot;,
    node_a,
    retry_policy=RetryPolicy(
        max_attempts=3,
        jitter=False
    )
)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, END)

graph = builder.compile()

try:
    graph.invoke({})
except HTTPError as e:
    logger.info(&quot;重试次数耗尽: {}&quot;, e)

from IPython.display import display
display(graph)
</code></pre>
<p><strong>输出如下</strong></p>
<pre><code>2026-06-01 16:44:56.684 | INFO     | __main__:node_a:12 - node a 运行
2026-06-01 16:44:57.185 | INFO     | __main__:node_a:12 - node a 运行
2026-06-01 16:44:58.186 | INFO     | __main__:node_a:12 - node a 运行
2026-06-01 16:44:58.187 | INFO     | __main__:&lt;module&gt;:32 - 重试次数耗尽: 网络连接超时...
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260601164518909.png" alt="image-20260601164518909" /></p>
<p>本例中：</p>
<ul>
<li><strong><code>max_attempts=3</code></strong> 表示最多尝试 3 次。</li>
<li>这 3 次包括首次执行，而不是首次失败后的 3 次重试。</li>
<li>因此，节点总共会被调用 3 次。</li>
<li>3 次全部失败后，异常继续向外抛出。</li>
<li>外层的 <strong><code>try...except</code></strong> 捕获该异常，并打印日志。</li>
</ul>
<h3 id="5-2-2-重试策略配置详解">5.2.2. 重试策略配置详解</h3>
<p>超时策略通过 <strong><code>RetryPolicy</code></strong> 配置，其主要参数如下</p>
<table>
<thead>
<tr>
<th style="text-align:left">参数</th>
<th style="text-align:left">类型</th>
<th style="text-align:left">默认值</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>max_attempts</code></strong></td>
<td style="text-align:left"><strong><code>int</code></strong></td>
<td style="text-align:left"><strong><code>3</code></strong></td>
<td style="text-align:left">最大尝试次数（包括首次执行）</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>initial_interval</code></strong></td>
<td style="text-align:left"><strong><code>float</code></strong></td>
<td style="text-align:left"><strong><code>0.5</code></strong></td>
<td style="text-align:left">第一次重试前的等待时间，单位：秒</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>backoff_factor</code></strong></td>
<td style="text-align:left"><strong><code>float</code></strong></td>
<td style="text-align:left"><strong><code>2.0</code></strong></td>
<td style="text-align:left">每次重试后等待时间的放大倍数</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>max_interval</code></strong></td>
<td style="text-align:left"><strong><code>float</code></strong></td>
<td style="text-align:left"><strong><code>128.0</code></strong></td>
<td style="text-align:left">相邻两次重试之间的最大等待时间，单位：秒</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>jitter</code></strong></td>
<td style="text-align:left"><strong><code>bool</code></strong></td>
<td style="text-align:left"><strong><code>True</code></strong></td>
<td style="text-align:left">是否为重试间隔添加随机抖动</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>retry_on</code></strong></td>
<td style="text-align:left">**`type[Exception]</td>
<td style="text-align:left">Sequence[type[Exception]]</td>
<td style="text-align:left">Callable[[Exception], bool]`**</td>
</tr>
</tbody>
</table>
<h4 id="5-2-2-1-max_attempts">5.2.2.1. <code>max_attempts</code></h4>
<p><strong><code>max_attempts</code></strong> 表示最大尝试次数，包括首次执行。</p>
<p>例如：</p>
<pre><code class="language-python">RetryPolicy(max_attempts=3)
</code></pre>
<p>表示：</p>
<pre><code class="language-text">第 1 次：首次执行
第 2 次：第 1 次重试
第 3 次：第 2 次重试
</code></pre>
<p>如果第 3 次仍然失败，则不再继续重试。</p>
<h4 id="5-2-2-2-initial_interval">5.2.2.2. <code>initial_interval</code></h4>
<p><strong><code>initial_interval</code></strong> 表示第一次重试前的等待时间，单位为秒。</p>
<p>例如：</p>
<pre><code class="language-python">RetryPolicy(initial_interval=0.5)
</code></pre>
<p>表示首次失败后，等待 <strong><code>0.5s</code></strong> 再进行第一次重试。</p>
<h4 id="5-2-2-3-backoff_factor">5.2.2.3. <code>backoff_factor</code></h4>
<p><strong><code>backoff_factor</code></strong> 表示重试间隔的增长倍数。</p>
<p>默认情况下，<strong><code>RetryPolicy</code></strong> 使用指数退避策略：</p>
<pre><code class="language-text">第一次失败后：等待 initial_interval
第二次失败后：等待 initial_interval * backoff_factor
第三次失败后：继续乘以 backoff_factor
</code></pre>
<p>例如：</p>
<pre><code class="language-python">RetryPolicy(
    initial_interval=0.5,
    backoff_factor=2.0
)
</code></pre>
<p>等待时间大致为：</p>
<pre><code class="language-text">0.5s -&gt; 1.0s -&gt; 2.0s -&gt; 4.0s ...
</code></pre>
<h4 id="5-2-2-4-max_interval">5.2.2.4. <code>max_interval</code></h4>
<p><strong><code>max_interval</code></strong> 表示相邻两次重试之间的最大等待时间。</p>
<p>即使指数退避计算出的等待时间继续增长，也不会超过 <strong><code>max_interval</code></strong>。</p>
<p>例如：</p>
<pre><code class="language-python">RetryPolicy(
    initial_interval=1,
    backoff_factor=2,
    max_interval=5
)
</code></pre>
<p>等待时间大致为：</p>
<pre><code class="language-text">1s -&gt; 2s -&gt; 4s -&gt; 5s -&gt; 5s ...
</code></pre>
<h4 id="5-2-2-5-jitter">5.2.2.5. <code>jitter</code></h4>
<p><strong><code>jitter</code></strong> 表示是否为重试间隔添加随机抖动（添加随机数，可正可负）。</p>
<p>在并发场景中，如果大量任务同时失败，并且按照固定间隔同时重试，可能导致服务在某个时间点被大量请求打爆。</p>
<p>加入 <strong><code>jitter</code></strong> 后，不同任务的重试时间会被打散，从而降低瞬时流量峰值。</p>
<p>上述案例中，为了让日志时间更稳定，观察指数退避策略，所以设置了：</p>
<pre><code class="language-python">RetryPolicy(jitter=False)
</code></pre>
<p>如果是生产环境，通常建议保留默认值：</p>
<pre><code class="language-python">RetryPolicy(jitter=True)
</code></pre>
<h4 id="5-2-2-6-retry_on">5.2.2.6. <code>retry_on</code></h4>
<p><strong><code>retry_on</code></strong> 用于设置哪些异常需要触发重试。</p>
<p>它支持三种写法：</p>
<h5 id="写法一-指定单个异常类型">写法一：指定单个异常类型</h5>
<pre><code class="language-python">RetryPolicy(retry_on=HTTPError)
</code></pre>
<p>表示只有抛出 <strong><code>HTTPError</code></strong> 时才重试。</p>
<h5 id="写法二-指定多个异常类型">写法二：指定多个异常类型</h5>
<pre><code class="language-python">RetryPolicy(
    retry_on=(HTTPError, ConnectionError)
)
</code></pre>
<p>表示抛出 <strong><code>HTTPError</code></strong> 或 <strong><code>ConnectionError</code></strong> 时触发重试。</p>
<h5 id="写法三-自定义判断函数">写法三：自定义判断函数</h5>
<pre><code class="language-python">def should_retry(exc: Exception) -&gt; bool:
    return isinstance(exc, HTTPError)

RetryPolicy(retry_on=should_retry)
</code></pre>
<p>这种写法最灵活。</p>
<p><strong><code>should_retry</code></strong> 接收一个异常实例，返回一个布尔值：</p>
<ul>
<li>返回 <strong><code>True</code></strong>：表示该异常需要重试。</li>
<li>返回 <strong><code>False</code></strong>：表示该异常不需要重试。</li>
</ul>
<h3 id="5-2-3-默认重试条件">5.2.3. 默认重试条件</h3>
<p><strong><code>retry_on</code></strong> 的默认值为 <strong><code>default_retry_on</code></strong>。</p>
<p><strong><code>default_retry_on</code></strong> 是一个可调用对象，它接收一个 <strong><code>Exception</code></strong> 实例，并返回一个 <strong><code>bool</code></strong> 值：</p>
<pre><code class="language-text">True  -&gt; 需要重试
False -&gt; 不需要重试
</code></pre>
<p>默认情况下，<strong><code>default_retry_on</code></strong> 会对大多数异常进行重试，但以下异常及其子类通常不会触发重试：</p>
<ul>
<li><strong><code>ValueError</code></strong></li>
<li><strong><code>TypeError</code></strong></li>
<li><strong><code>ArithmeticError</code></strong></li>
<li><strong><code>ImportError</code></strong></li>
<li><strong><code>LookupError</code></strong></li>
<li><strong><code>NameError</code></strong></li>
<li><strong><code>SyntaxError</code></strong></li>
<li><strong><code>RuntimeError</code></strong></li>
<li><strong><code>ReferenceError</code></strong></li>
<li><strong><code>StopIteration</code></strong></li>
<li><strong><code>StopAsyncIteration</code></strong></li>
<li><strong><code>OSError</code></strong></li>
</ul>
<p>这些异常通常更像是代码逻辑错误、类型错误、语法错误或运行时错误，单纯重试通常没有意义。</p>
<p>对于主流 <strong><code>HTTP</code></strong> 库中的异常，例如 <strong><code>requests</code></strong> 和 <strong><code>httpx</code></strong>，默认策略通常只对 <strong><code>5xx</code></strong> 状态码进行重试。</p>
<p>原因是：</p>
<ul>
<li><strong><code>4xx</code></strong> 通常表示客户端请求错误，例如参数错误、鉴权失败、资源不存在等。</li>
<li><strong><code>5xx</code></strong> 通常表示服务端异常，例如服务暂时不可用、网关错误、内部服务错误等。</li>
</ul>
<p>因此，<strong><code>5xx</code></strong> 更适合通过重试进行恢复。</p>
<p><strong><code>default_retry_on</code></strong> 的核心逻辑如下：</p>
<pre><code class="language-python">def default_retry_on(exc: Exception) -&gt; bool:
    import httpx
    import requests

    if isinstance(exc, ConnectionError):
        return True
    if isinstance(exc, httpx.HTTPStatusError):
        return 500 &lt;= exc.response.status_code &lt; 600
    if isinstance(exc, requests.HTTPError):
        return 500 &lt;= exc.response.status_code &lt; 600 if exc.response else True
    if isinstance(
        exc,
        (
            ValueError,
            TypeError,
            ArithmeticError,
            ImportError,
            LookupError,
            NameError,
            SyntaxError,
            RuntimeError,
            ReferenceError,
            StopIteration,
            StopAsyncIteration,
            OSError,
        ),
    ):
        return False
    return True
</code></pre>
<h2 id="5-3-超时控制">5.3. 超时控制</h2>
<h3 id="5-3-1-使用要求">5.3.1. 使用要求</h3>
<p>超时控制用于限制单次节点执行的最大耗时，避免某个节点因为外部服务无响应、网络阻塞或异步任务卡住而长时间不返回。</p>
<p>需要注意：</p>
<ol>
<li>
<p><strong>版本要求</strong></p>
<p>节点级超时控制要求：</p>
<pre><code class="language-text">langgraph&gt;=1.2
</code></pre>
</li>
<li>
<p><strong>节点类型要求</strong></p>
<p>节点级超时控制仅适用于异步节点，即使用 <strong><code>async def</code></strong> 定义的节点。</p>
<p>同步节点一旦开始执行，通常会阻塞当前线程。<strong><code>Python</code></strong> 进程内缺少一种通用、安全的机制，可以从外部强行终止正在运行的同步函数；如果强行中断，可能导致资源未释放、锁未释放或副作用处于不一致状态。因此，<strong><code>LangGraph </code></strong> 的节点级超时更适合用于异步节点。对于同步节点，应优先在节点内部使用具体库提供的超时参数，例如 <strong><code>HTTP </code></strong> 客户端、数据库驱动或 <strong><code>SDK</code></strong> 自带的 <strong><code>timeout</code></strong> 配置。</p>
</li>
</ol>
<p>本课程当前环境为：</p>
<pre><code class="language-text">langgraph==1.1.2
</code></pre>
<p>因此不支持该特性，本节只做概念说明。</p>
<h3 id="5-3-2-核心思想">5.3.2. 核心思想</h3>
<p>超时控制的核心思想是：</p>
<blockquote>
<p>为单次节点设置超时时间，运行时长超过超时时间，节点运行失败，并抛出超时异常。</p>
</blockquote>
<p>在 <strong><code>add_node</code></strong> 中通过 <strong><code>timeout=</code></strong> 配置超时策略。</p>
<p>例如：</p>
<pre><code class="language-python">from langgraph.types import TimeoutPolicy

builder.add_node(
    &quot;call_model&quot;,
    call_model,
    timeout=TimeoutPolicy(run_timeout=60)
)
</code></pre>
<p>这里的含义是：</p>
<ul>
<li><strong><code>call_model</code></strong> 节点单次执行最多运行 60 秒。</li>
<li>如果超过 60 秒仍未完成，则触发超时。</li>
<li>超时后会抛出 <strong><code>NodeTimeoutError</code></strong>。</li>
<li>该异常会交给 <strong><code>RetryPolicy</code></strong> 判断是否需要重试。</li>
<li>如果重试次数耗尽，才会进入错误处理逻辑。</li>
</ul>
<h3 id="5-3-3-timeoutpolicy-简要说明">5.3.3. TimeoutPolicy 简要说明</h3>
<p><strong><code>TimeoutPolicy</code></strong> 常见参数包括：</p>
<table>
<thead>
<tr>
<th style="text-align:left">参数</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>run_timeout</code></strong></td>
<td style="text-align:left">超时时间，即单次节点运行的最长时间</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>idle_timeout</code></strong></td>
<td style="text-align:left">节点没有可观察进展（如写状态、流式输出等）时的最长空闲时间</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>refresh_on</code></strong></td>
<td style="text-align:left">空闲时间的刷新方式，可以是手动刷新或自动刷新。默认为自动刷新，此时写状态、流式输出等都会刷新</td>
</tr>
</tbody>
</table>
<p>本课程环境暂不支持，不展开实操。</p>
<p>感兴趣的同学参考官方文档</p>
<blockquote>
<p>https://docs.langchain.com/oss/python/langgraph/fault-tolerance#timeouts</p>
</blockquote>
<h2 id="5-4-错误处理">5.4. 错误处理</h2>
<h3 id="5-4-1-使用要求">5.4.1. 使用要求</h3>
<p>错误处理机制要求：</p>
<pre><code class="language-text">langgraph&gt;=1.2
</code></pre>
<p>本课程当前环境为：</p>
<pre><code class="language-text">langgraph==1.1.2
</code></pre>
<p>因此不支持该特性，本节只做概念说明。</p>
<h3 id="5-4-2-核心思想">5.4.2. 核心思想</h3>
<p>错误处理机制用于在节点最终失败后，执行兜底逻辑。</p>
<p>节点执行失败时，整体顺序如下：</p>
<pre><code class="language-text">节点执行失败
    ↓
是否满足 retry_policy
    ↓
满足则继续重试，直到重试次数耗尽，否则直接进行下一步
    ↓
进入 error_handler
    ↓
执行兜底恢复逻辑
</code></pre>
<p>在 <strong><code>add_node</code></strong> 中通过 <strong><code>error_handler=</code></strong> 设置错误处理逻辑。</p>
<p>例如：</p>
<pre><code class="language-python">builder.add_node(
    &quot;call_api&quot;,
    call_api,
    retry_policy=RetryPolicy(max_attempts=3),
    error_handler=handle_api_error
)
</code></pre>
<p>其含义是：</p>
<ol>
<li><strong><code>call_api</code></strong> 执行失败。</li>
<li><strong><code>RetryPolicy</code></strong> 判断是否重试。</li>
<li>最多尝试 3 次。</li>
<li>3 次仍然失败后，调用 <strong><code>handle_api_error</code></strong>。</li>
<li><strong><code>handle_api_error</code></strong> 可以返回状态更新，也可以通过 <strong><code>Command</code></strong> 路由到其他节点。</li>
</ol>
<p>错误处理适合以下场景：</p>
<ul>
<li>API 调用失败后返回兜底结果。</li>
<li>远程服务失败后切换到备用服务。</li>
<li>多步骤业务流程中执行补偿逻辑。</li>
<li>不希望整个图因为单个节点失败而直接终止。</li>
</ul>
<p>当前环境不支持该特性，不展开实操</p>
<p>参考官方文档</p>
<blockquote>
<p>https://docs.langchain.com/oss/python/langgraph/fault-tolerance#error-handling</p>
</blockquote>
<h2 id="5-5-节点缓存">5.5. 节点缓存</h2>
<h3 id="5-5-1-什么是节点缓存">5.5.1. 什么是节点缓存？</h3>
<p>节点缓存是指：</p>
<blockquote>
<p>将节点的历史运行结果保存下来，后续当节点收到相同输入时，不再重复执行节点函数，而是直接返回之前缓存的结果。</p>
</blockquote>
<p>节点缓存主要用于减少重复计算，降低延迟和成本。</p>
<p>例如，某个节点内部需要：</p>
<ul>
<li>调用大模型</li>
<li>调用外部 API</li>
<li>查询远程数据库</li>
<li>执行复杂计算</li>
<li>进行耗时的数据处理</li>
</ul>
<p>如果相同输入会多次出现，那么可以考虑启用节点缓存。</p>
<h3 id="5-5-2-适用场景">5.5.2. 适用场景</h3>
<p>节点缓存通常适用于同时满足以下条件的场景：</p>
<ol>
<li>
<p><strong>节点函数具有确定性</strong></p>
<p>相同输入应当产生相同输出。</p>
<p>例如：</p>
<pre><code class="language-text">输入 A -&gt; 输出 B
再次输入 A -&gt; 仍然输出 B
</code></pre>
</li>
<li>
<p><strong>节点执行成本较高</strong></p>
<p>例如大模型调用、API 请求、复杂计算、耗时查询等。</p>
</li>
<li>
<p><strong>相同输入会重复出现</strong></p>
<p>例如多次调试图、重复执行相同流程、多个分支复用同一中间结果等。</p>
</li>
<li>
<p><strong>输入能够被缓存键函数稳定处理</strong></p>
<p>缓存是以 <strong>键值对</strong> 形式存储的，每个缓存都有唯一的 <strong><code>Key</code></strong>，<strong>缓存键函数</strong> 用于生成唯一键。</p>
<p>默认情况下，<strong><code>CachePolicy</code></strong> 使用 <strong><code>default_cache_key</code></strong> 作为缓存键函数。后者基于节点输入调用 <strong><code>pickle</code></strong> 库的序列化功能生成<strong>缓存唯一键</strong>。</p>
<p>使用默认缓存键函数时，建议输入尽量使用简单、稳定、可序列化的数据结构，例如：</p>
<ul>
<li><strong><code>str</code></strong></li>
<li><strong><code>int</code></strong></li>
<li><strong><code>float</code></strong></li>
<li><strong><code>bool</code></strong></li>
<li><strong><code>dict</code></strong></li>
<li><strong><code>list</code></strong></li>
<li><strong><code>tuple</code></strong></li>
</ul>
<p>如果节点输入中包含复杂对象、文件句柄、数据库连接、模型实例、动态对象等，建议自定义 <strong><code>key_func</code></strong>，避免缓存 <strong><code>Key</code></strong> 不稳定或无法生成。</p>
</li>
</ol>
<h3 id="5-5-3-基本用法">5.5.3. 基本用法</h3>
<p>节点缓存需要两步配置。</p>
<h4 id="第一步-为节点配置-cache_policy">第一步：为节点配置 <code>cache_policy</code></h4>
<p>在 <strong><code>add_node</code></strong> 调用时，通过 <strong><code>cache_policy=</code></strong> 设置节点缓存策略。</p>
<pre><code class="language-python">builder.add_node(
    &quot;node_a&quot;,
    node_a,
    cache_policy=CachePolicy(ttl=10)
)
</code></pre>
<h4 id="第二步-编译图时启用缓存后端">第二步：编译图时启用缓存后端</h4>
<p>仅仅为节点配置 <strong><code>cache_policy</code></strong> 还不够，还需要在编译图时指定缓存后端。</p>
<p>例如：</p>
<pre><code class="language-python">graph = builder.compile(cache=InMemoryCache())
</code></pre>
<p>其中：</p>
<ul>
<li><strong><code>CachePolicy</code></strong>：声明节点是否启用缓存，以及缓存策略是什么。</li>
<li><strong><code>InMemoryCache</code></strong>：声明缓存数据实际保存在哪里。</li>
</ul>
<p>如果只配置 <strong><code>cache_policy</code></strong>，但编译图时没有传入 <strong><code>cache=</code></strong>，缓存不会真正生效。</p>
<p><strong>示例如下</strong></p>
<pre><code class="language-python">import time
from typing import TypedDict, Annotated
from langgraph.graph import StateGraph, START, END
from langgraph.cache.memory import InMemoryCache
from langgraph.types import CachePolicy

from operator import add
from loguru import logger

class EmptyState(TypedDict):
    user: str
    invoke_counts: Annotated[int, add]

def node_a(state: EmptyState) -&gt; EmptyState:
    logger.info(&quot;node_a 被调用, user: {}&quot;, state[&quot;user&quot;])
    time.sleep(3) # 模拟耗时操作
    logger.info(&quot;node_a 耗时操作执行完毕&quot;)

    return {
        &quot;invoke_counts&quot;: 1
    }

builder = StateGraph(state_schema=EmptyState)
builder.add_node(
    &quot;node_a&quot;,
    node_a,
    cache_policy=CachePolicy(ttl=10)
)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, END)

graph = builder.compile(cache=InMemoryCache())
logger.info(&quot;首次调用图&quot;)
logger.info(&quot;运行结果: {}\n\n&quot;, graph.invoke({&quot;user&quot;: &quot;小明&quot;, &quot;invoke_counts&quot;: 0}))
logger.info(&quot;相同输入再次调用图&quot;)
logger.info(&quot;运行结果: {}\n\n&quot;, graph.invoke({&quot;user&quot;: &quot;小明&quot;, &quot;invoke_counts&quot;: 0}))
logger.info(&quot;不同输入再次调用图&quot;)
logger.info(&quot;运行结果: {}\n\n&quot;, graph.invoke({&quot;user&quot;: &quot;小花&quot;, &quot;invoke_counts&quot;: 0}))
logger.info(&quot;不同输入再次调用图&quot;)
logger.info(&quot;运行结果: {}\n\n&quot;, graph.invoke({&quot;user&quot;: &quot;小花&quot;, &quot;invoke_counts&quot;: 2}))
time.sleep(10) # 确保第一次调用缓存失效
logger.info(&quot;10秒后相同输入再次调用图，此时缓存已失效&quot;)
logger.info(&quot;运行结果: {}&quot;, graph.invoke({&quot;user&quot;: &quot;小明&quot;, &quot;invoke_counts&quot;: 0}))

from IPython.display import display
display(graph)
</code></pre>
<p>运行日志如下</p>
<pre><code>2026-06-01 18:46:22.959 | INFO     | __main__:&lt;module&gt;:33 - 首次调用图
2026-06-01 18:46:22.960 | INFO     | __main__:node_a:15 - node_a 被调用, user: 小明
2026-06-01 18:46:25.962 | INFO     | __main__:node_a:17 - node_a 耗时操作执行完毕
2026-06-01 18:46:25.963 | INFO     | __main__:&lt;module&gt;:34 - 运行结果: {'user': '小明', 'invoke_counts': 1}


2026-06-01 18:46:25.964 | INFO     | __main__:&lt;module&gt;:35 - 相同输入再次调用图
2026-06-01 18:46:25.965 | INFO     | __main__:&lt;module&gt;:36 - 运行结果: {'user': '小明', 'invoke_counts': 1}


2026-06-01 18:46:25.966 | INFO     | __main__:&lt;module&gt;:37 - 不同输入再次调用图
2026-06-01 18:46:25.967 | INFO     | __main__:node_a:15 - node_a 被调用, user: 小花
2026-06-01 18:46:28.968 | INFO     | __main__:node_a:17 - node_a 耗时操作执行完毕
2026-06-01 18:46:28.970 | INFO     | __main__:&lt;module&gt;:38 - 运行结果: {'user': '小花', 'invoke_counts': 1}


2026-06-01 18:46:28.970 | INFO     | __main__:&lt;module&gt;:39 - 不同输入再次调用图
2026-06-01 18:46:28.971 | INFO     | __main__:node_a:15 - node_a 被调用, user: 小花
2026-06-01 18:46:31.973 | INFO     | __main__:node_a:17 - node_a 耗时操作执行完毕
2026-06-01 18:46:31.974 | INFO     | __main__:&lt;module&gt;:40 - 运行结果: {'user': '小花', 'invoke_counts': 3}


2026-06-01 18:46:36.976 | INFO     | __main__:&lt;module&gt;:42 - 11秒后相同输入再次调用图，此时缓存已失效
2026-06-01 18:46:36.977 | INFO     | __main__:node_a:15 - node_a 被调用, user: 小明
2026-06-01 18:46:39.979 | INFO     | __main__:node_a:17 - node_a 耗时操作执行完毕
2026-06-01 18:46:39.981 | INFO     | __main__:&lt;module&gt;:43 - 运行结果: {'user': '小明', 'invoke_counts': 1}
</code></pre>
<p><img src="/images/langgraph-study-notes/image-20260601184654991.png" alt="image-20260601184654991" /></p>
<p>在该示例中，缓存的 <strong><code>ttl</code></strong> 被设置为 <strong><code>10s</code></strong>。</p>
<p>缓存失效前：</p>
<ul>
<li>相同输入会命中缓存。</li>
<li>命中缓存时，节点函数不会再次执行。</li>
<li>因此不会打印 <strong><code>node_a 被调用</code></strong>。</li>
<li>输入不同则不会命中缓存，节点函数仍然会执行。</li>
</ul>
<p>缓存失效后：</p>
<ul>
<li>即使输入相同，也会再次执行节点函数。</li>
<li>执行完成后，会重新写入缓存。</li>
</ul>
<h3 id="5-5-4-缓存策略配置详解">5.5.4. 缓存策略配置详解</h3>
<p>缓存策略通过 <strong><code>CachePolicy</code></strong> 配置，只有两个配置项</p>
<table>
<thead>
<tr>
<th style="text-align:left">参数</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong><code>key_func</code></strong></td>
<td style="text-align:left">根据节点输入生成缓存 <strong><code>Key</code></strong> 的函数</td>
</tr>
<tr>
<td style="text-align:left"><strong><code>ttl</code></strong></td>
<td style="text-align:left">缓存键值对的存活时间，单位为秒</td>
</tr>
</tbody>
</table>
<h4 id="5-5-4-1-key_func">5.5.4.1. <code>key_func</code></h4>
<p><strong><code>key_func</code></strong> 用于根据节点输入生成唯一的缓存 Key。</p>
<p>节点缓存本质上是一个 <strong><code>K-V</code></strong> 缓存：</p>
<pre><code class="language-text">缓存 Key -&gt; 节点运行结果
</code></pre>
<p>当节点再次执行时，LangGraph 会先根据当前输入计算缓存 Key：</p>
<ul>
<li>如果缓存 Key 已存在，并且没有过期，则直接返回缓存结果。</li>
<li>如果缓存 Key 不存在，或者已经过期，则正常执行节点函数，并将结果写入缓存。</li>
</ul>
<p>默认缓存键函数是 <strong><code>default_cache_key</code></strong>。</p>
<p>其核心实现如下：</p>
<pre><code class="language-python">def default_cache_key(*args: Any, **kwargs: Any) -&gt; str | bytes:
    &quot;&quot;&quot;Default cache key function that uses the arguments and keyword arguments to generate a hashable key.&quot;&quot;&quot;
    import pickle

    # protocol 5 strikes a good balance between speed and size
    return pickle.dumps((_freeze(args), _freeze(kwargs)), protocol=5, fix_imports=False)
</code></pre>
<p>它会把节点输入参数经过处理后，使用 <strong><code>pickle</code></strong> 序列化为字节序列，并将后者作为缓存 <strong><code>Key</code></strong>。</p>
<h4 id="5-5-4-2-ttl">5.5.4.2. <code>ttl</code></h4>
<p><strong><code>ttl</code></strong> 表示缓存的存活时间，单位为秒。</p>
<p>例如：</p>
<pre><code class="language-python">CachePolicy(ttl=10)
</code></pre>
<p>表示缓存结果最多保存 10 秒。</p>
<p>如果设置为：</p>
<pre><code class="language-python">CachePolicy(ttl=None)
</code></pre>
<p>表示缓存不会因为时间原因自动过期。</p>
<p>不过在实际项目中，不建议随意设置永久缓存。</p>
<p>如果节点依赖外部状态，例如：</p>
<ul>
<li>当前时间</li>
<li>远程 API 数据</li>
<li>数据库查询结果</li>
<li>用户权限</li>
<li>环境变量</li>
<li>模型版本</li>
<li>提示词版本</li>
</ul>
<p>那么应该根据业务场景设置合适的 <strong><code>ttl</code></strong>，或者把这些变量纳入缓存 Key 的计算逻辑。</p>
<h2 id="5-6-全图默认配置">5.6. 全图默认配置</h2>
<h3 id="5-6-1-使用要求">5.6.1. 使用要求</h3>
<p>默认配置功能要求：</p>
<pre><code class="language-text">langgraph&gt;=1.2
</code></pre>
<p>本课程当前环境为：</p>
<pre><code class="language-text">langgraph==1.1.2
</code></pre>
<p>因此不支持该特性，本节只做概念说明。</p>
<h3 id="5-6-2-核心思想">5.6.2. 核心思想</h3>
<p>当多个节点都需要配置相同的执行策略时，如果每个节点都重复配置，繁琐且耗时。</p>
<p>例如：</p>
<pre><code class="language-python">builder.add_node(
    &quot;node_a&quot;,
    node_a,
    retry_policy=RetryPolicy(max_attempts=3)
)

builder.add_node(
    &quot;node_b&quot;,
    node_b,
    retry_policy=RetryPolicy(max_attempts=3)
)

builder.add_node(
    &quot;node_c&quot;,
    node_c,
    retry_policy=RetryPolicy(max_attempts=3)
)
</code></pre>
<p>在较新版本中，可以通过 <strong><code>set_node_defaults</code></strong> 为所有节点设置默认配置，即<strong>全图默认配置 <code>Graph defaults</code></strong>。</p>
<p>默认配置可以包括：</p>
<ul>
<li><strong><code>retry_policy</code></strong></li>
<li><strong><code>timeout</code></strong></li>
<li><strong><code>error_handler</code></strong></li>
<li><strong><code>cache_policy</code></strong></li>
</ul>
<p>这样可以避免在每个 <strong><code>add_node</code></strong> 中重复相同配置。</p>
<p>本课程环境暂不支持，不展开实操。</p>
<p>参考官方文档</p>
<blockquote>
<p>https://docs.langchain.com/oss/python/langgraph/fault-tolerance#graph-defaults</p>
</blockquote>
<h2 id="5-7-小结">5.7. 小结</h2>
<p>本章介绍了 <strong><code>LangGraph</code></strong> 中节点执行与容错相关的能力。</p>
<p>总结如下：</p>
<table>
<thead>
<tr>
<th style="text-align:left">机制</th>
<th>机制类型</th>
<th style="text-align:left">作用</th>
<th style="text-align:left">当前课程是否重点讲解</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><strong>重试机制 <code>Retries</code></strong></td>
<td>节点容错机制</td>
<td style="text-align:left">节点失败后自动重试</td>
<td style="text-align:left">是</td>
</tr>
<tr>
<td style="text-align:left"><strong>超时设置 <code>Timeouts</code></strong></td>
<td>节点容错机制</td>
<td style="text-align:left">限制单次节点执行时间</td>
<td style="text-align:left">否，要求 <strong><code>langgraph&gt;=1.2</code></strong></td>
</tr>
<tr>
<td style="text-align:left"><strong>错误处理 <code>Error Handling</code></strong></td>
<td>节点容错机制</td>
<td style="text-align:left">重试耗尽后执行兜底逻辑</td>
<td style="text-align:left">否，要求 <strong><code>langgraph&gt;=1.2</code></strong></td>
</tr>
<tr>
<td style="text-align:left"><strong>节点缓存 <code>Node Cache</code></strong></td>
<td>节点执行优化机制</td>
<td style="text-align:left">缓存节点历史运行结果</td>
<td style="text-align:left">是</td>
</tr>
<tr>
<td style="text-align:left"><strong>全图默认配置 <code>Graph defaults</code></strong></td>
<td>默认策略配置</td>
<td style="text-align:left">为所有节点设置默认执行策略</td>
<td style="text-align:left">否，要求 <strong><code>langgraph&gt;=1.2</code></strong></td>
</tr>
</tbody>
</table>
<p>实际使用时，可以按照以下原则选择：</p>
<ol>
<li><strong>临时性失败</strong>：优先使用 <strong><code>RetryPolicy</code></strong>。</li>
<li><strong>外部服务可能卡住</strong>：使用 <strong><code>timeout</code></strong> 控制单次节点执行时间。</li>
<li><strong>失败后需要兜底恢复</strong>：使用 <strong><code>error_handler</code></strong>。</li>
<li><strong>相同输入重复执行且成本较高</strong>：使用 <strong><code>CachePolicy</code></strong>。</li>
<li><strong>多个节点使用相同策略</strong>：使用 <strong><code>set_node_defaults</code></strong> 统一配置。</li>
</ol>
<p>其中，<strong>重试机制</strong> 和 <strong>节点缓存</strong> 是本课程的重点。</p>
]]></content>
  </entry>
  <entry>
    <title>01-LangGraph基础</title>
    <url>/2025/05/13/langgraph-overview/</url>
    <content><![CDATA[<h1 id="1-langgraph总览">1. LangGraph总览</h1>
<p>LangGraph 运行时底层基于自研的 Pregel 运行时，其核心思想借鉴了 Google Pregel 计算模型，用于组织和执行复杂的图计算流程。</p>
<h2 id="1-0-langgraph-和-langchain-的版本迭代及定位">1.0. <code>LangGraph</code>和<code>LangChain</code>的版本迭代及定位</h2>
<p><strong>LangChain 1.x</strong> 的定位发生了明显变化：旧式 Chain、Retriever、Memory 等组件被迁移至 langchain-classic；LCEL 保留为 langchain-core 的底层组合机制，但不再是主要开发入口。</p>
<p><strong>LangChain 1.x</strong> 现在聚焦于 Agent 开发，核心入口是 create_agent，围绕 Agent 提供模型、工具、消息、结构化输出、Middleware` 和记忆管理等高层抽象。</p>
<p><strong>LangGraph</strong> 是更底层的编排框架与 Agent Runtime，负责复杂工作流和有状态 Agent 的执行，提供持久化、流式输出、Durable Execution、Human-in-the-loop 等运行时能力。create_agent 底层基于 LangGraph` 实现。</p>
<p>二者的定位对比如下：</p>
<table>
<thead>
<tr>
<th>对比维度</th>
<th>LangChain</th>
<th>LangGraph</th>
</tr>
</thead>
<tbody>
<tr>
<td>定位</td>
<td>Agent 高层开发框架</td>
<td>底层编排框架 &amp; Agent Runtime</td>
</tr>
<tr>
<td>核心入口</td>
<td><code>create_agent</code></td>
<td><code>StateGraph</code> / <code>@entrypoint</code></td>
</tr>
<tr>
<td>适用场景</td>
<td>结构直接的 Agent 应用</td>
<td>复杂工作流、持久化状态、长时间运行、人工介入</td>
</tr>
<tr>
<td>流程控制</td>
<td>Agent 循环自动管理</td>
<td>精细控制节点、边、条件分支</td>
</tr>
<tr>
<td>学习成本</td>
<td>较低</td>
<td>较高</td>
</tr>
</tbody>
</table>
<blockquote>
<p><code>LangChain</code> 提供易于使用的 Agent 高层抽象，<code>LangGraph</code> 提供可靠、可持久化且可精细控制的底层执行能力。</p>
</blockquote>
<p>对于大多数 Agent 项目，从 LangChain 的 create_agent 开始即可；需要复杂工作流编排、确定性步骤与 Agent 步骤混合、长时间运行或底层状态控制时，再引入 LangGraph。</p>
<p>![ChatGPT Image 2026年6月25日 18_25_23](images/ChatGPT Image 2026年6月25日 18_25_23.png)</p>
<h2 id="1-1-构成图的基本要素">1.1. 构成图的基本要素</h2>
<p>LangGraph 运行时主要由三个基本要素构成：State（状态）、Node（节点） 和 Edge（边）</p>
<p>State（状态）：LangGraph 运行过程中的共享数据结构，用于表示应用在某一时刻的状态快照。它承载了图运行所需的上下文信息、中间结果和后续节点需要读取的数据，是节点之间传递信息的核心载体。它与我们在学习 LangChain Agent 时使用的 State 是同一概念。</p>
<p>Node（节点）：LangGraph 中的具体执行单元，通常实现为一个函数。节点会读取当前 State，执行相应的业务逻辑，并返回对 State 的局部更新。节点本身并不直接修改全局状态，状态的合并与提交由运行时统一完成。</p>
<p>Edges（边）：用于定义节点之间的流转关系，决定一个节点执行完成后下一步应该进入哪个节点。Edge 可以是固定流转，也可以根据当前 State 进行条件判断，从而实现分支、循环等复杂控制流程。</p>
<p>下图展示了一个非常简单的运行图，只有两个计算节点，其拓扑结构为</p>
<pre><code>START --&gt; node_1 --&gt; node_2 --&gt; END
</code></pre>
<p>如下图所示</p>
<p><img src="/images/langgraph-study-notes/image-20260521155704139.png" alt="image-20260521155704139" /></p>
<h2 id="1-2-图运行过程">1.2. 图运行过程</h2>
<p>LangGraph 的图运行过程基于 Superstep（超步） 来组织和推进。Superstep 可以理解为图运行过程中的一次”单步循环”。一次图运行过程从开始到结束，就是由一系列连续的 Superstep 串联而成。</p>
<p>下文以 <strong>node_1执行完毕后的Superstep</strong> 为例说明。</p>
<p>每个 Superstep 通常可以分为三个阶段：</p>
<ol>
<li>计划/路由阶段（Plan / Routing）：根据当前的 State（状态） 和 Edge（边） 的逻辑，确定本轮超步中应该被执行的节点。</li>
</ol>
<p><img src="/images/langgraph-study-notes/image-20260521160436752.png" alt="image-20260521160436752" /></p>
<ol start="2">
<li>执行阶段（Execution）：运行本轮被选中的节点。如果本轮有多个节点同时被触发，它们会并行执行。每个节点都会基于本轮开始时的状态快照进行计算，并输出各自对状态的局部更新。在本阶段中，一个节点产生的更新不会立即被其他节点读取到。</li>
</ol>
<p><img src="/images/langgraph-study-notes/image-20260521160523119.png" alt="image-20260521160523119" /></p>
<ol start="3">
<li>状态更新/提交阶段（Update / Commit）：当本轮所有节点都执行完成后，LangGraph 会将它们的输出统一合并到 State 中，生成新的状态快照。这个新状态会作为下一轮 Superstep 的输入。</li>
</ol>
<p><img src="/images/langgraph-study-notes/image-20260521160659113.png" alt="image-20260521160659113" /></p>
<p>完整的运行流程查看 <a href="langgraph-runtime-viz%5Cindex.html">index.html</a> ，双击在浏览器打开即可。</p>
<p>其中和Checkpoint相关的部分，学习相关内容后再看。</p>
<h2 id="1-3-graph-api-vs-functional-api">1.3. Graph API vs Functional API</h2>
<p>LangGraph 提供了两种不同的 API 来构建运行图：<code>Graph API</code>（图式 API） 和 <code>Functional API</code>（函数式 API）。这两种 API 共享相同的底层运行时，可以在同一应用程序中协同使用，但它们针对不同的使用场景和开发偏好而设计。</p>
<hr />
<h3 id="1-3-1-graph-api">1.3.1. Graph API</h3>
<p>Graph API 采用声明式方式构建工作流。开发者需要显式定义 State、Node 和 Edge，将业务流程组织成一个可视化的图结构。</p>
<p>当流程中存在较复杂的分支、多个节点之间共享状态、并行执行、结果汇聚，或者需要通过图结构帮助调试和团队协作时，更适合使用 Graph API。官方文档也明确建议，在需要复杂流程可视化、显式状态管理、多条件分支、并行路径以及团队协作时，优先选择 Graph API。</p>
<p>总之：</p>
<blockquote>
<p>Graph API 更适合构建结构清晰、节点关系复杂、需要长期维护的工作流。</p>
</blockquote>
<p>典型场景包括：</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>多节点复杂流程</td>
<td>流程中存在多个处理节点，需要清晰表达节点之间的关系</td>
</tr>
<tr>
<td>条件分支较多</td>
<td>根据 State 中的不同字段决定后续执行路径</td>
</tr>
<tr>
<td>并行执行与结果汇聚</td>
<td>多个节点并行运行，之后汇总结果</td>
</tr>
<tr>
<td>多组件共享状态</td>
<td>多个节点都需要读写同一个全局 State</td>
</tr>
<tr>
<td>需要图结构展示</td>
<td>便于调试、讲解、文档化和团队协作</td>
</tr>
</tbody>
</table>
<hr />
<h3 id="1-3-2-functional-api">1.3.2. Functional API</h3>
<p>Functional API 采用命令式方式构建工作流，更接近普通 Python 函数调用。开发者可以使用 <code>@entrypoint</code> 定义工作流入口，使用 <code>@task</code> 定义可被检查点记录的任务，然后在函数内部使用普通的 <code>if/else</code>、循环和函数调用来组织流程。官方文档指出，当已有过程式代码需要最小改造、流程主要是线性的、分支逻辑较简单、希望快速原型验证时，更适合使用 Functional API。</p>
<p>可以这样理解：</p>
<blockquote>
<p>Functional API 更适合在普通 Python 函数流程中，以较低成本接入 LangGraph 的持久化、中断恢复和任务记录能力。</p>
</blockquote>
<p>典型场景包括：</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>现有代码改造</td>
<td>原本已有函数式或过程式代码，不希望重构成完整图结构</td>
</tr>
<tr>
<td>线性流程</td>
<td>主要是 A → B → C 的顺序执行</td>
</tr>
<tr>
<td>简单分支</td>
<td>只有少量 <code>if/else</code> 判断</td>
</tr>
<tr>
<td>快速原型验证</td>
<td>希望减少样板代码，快速验证业务逻辑</td>
</tr>
<tr>
<td>局部任务持久化</td>
<td>希望某些函数作为独立 task 被检查点记录</td>
</tr>
</tbody>
</table>
<hr />
<h3 id="1-3-3-二者的核心区别">1.3.3. 二者的核心区别</h3>
<table>
<thead>
<tr>
<th>对比项</th>
<th>Graph API</th>
<th>Functional API</th>
</tr>
</thead>
<tbody>
<tr>
<td>编程风格</td>
<td>声明式图结构</td>
<td>命令式函数流程</td>
</tr>
<tr>
<td>核心抽象</td>
<td>State、Node、Edge</td>
<td>entrypoint、task</td>
</tr>
<tr>
<td>状态管理</td>
<td>显式定义全局 State</td>
<td>更多依赖函数参数和返回值</td>
</tr>
<tr>
<td>流程表达</td>
<td>通过节点和边表达</td>
<td>通过普通 Python 控制流表达</td>
</tr>
<tr>
<td>可视化能力</td>
<td>强，天然适合画图和调试</td>
<td>弱，更像普通代码流程</td>
</tr>
<tr>
<td>适合场景</td>
<td>复杂工作流、多分支、多节点协作</td>
<td>简单流程、快速原型、已有代码改造</td>
</tr>
<tr>
<td>学习成本</td>
<td>相对更高</td>
<td>相对更低</td>
</tr>
</tbody>
</table>
<hr />
<h3 id="1-3-4-选型建议">1.3.4. 选型建议</h3>
<p>从零构建或流程结构复杂，用 <code>Graph API</code>；现有代码改造、快速原型验证或流程逻辑简单，用 <code>Functional API</code>。</p>
<p>学习 <code>LangGraph</code> 建议优先掌握 <code>Graph API</code>。因为后者更能体现 <code>LangGraph</code> 的核心思想：通过 <code>State、Node、Edge</code> 显式描述一个可执行的计算图。</p>
<p>本文介绍 <code>Graph API</code>，对 <code>Functional API</code> 感兴趣的同学自行查阅</p>
<blockquote>
<p>https://docs.langchain.com/oss/python/langgraph/functional-api</p>
</blockquote>
<h1 id="2-图的基础构建与运行">2. 图的基础构建与运行</h1>
<p>本节将从一个基础案例入手，掌握图的基本创建、调用和可视化方法。</p>
<p>图的构建与运行分为三个阶段：定义状态图、编译状态图、调用状态图</p>
<h2 id="2-1-定义状态图">2.1. 定义状态图</h2>
<p>状态图的定义可以分为以下几个步骤：</p>
<p><strong>1. 定义全局状态</strong>  定义整个运行图共享的全局状态（实际上，除了全局状态，LangGraph还支持其它类型的状态，下文详述）</p>
<p><strong>2. 创建状态图</strong>     创建 StateGraph实例，将状态和状态图绑定</p>
<p><strong>3. 定义图结构</strong>     定义节点和边并添加到状态图中</p>
<h3 id="2-1-1-定义全局状态">2.1.1. 定义全局状态</h3>
<p>全局状态是 LangGraph 运行图每个节点都可以访问的公共对象。</p>
<pre><code class="language-python">class OverAllState(TypedDict):
    logs: Annotated[list[str], add]
    cur_id: str
</code></pre>
<h3 id="2-1-2-创建状态图">2.1.2. 创建状态图</h3>
<pre><code class="language-python">builder = StateGraph(state_schema=OverAllState)
</code></pre>
<h3 id="2-1-3-定义图结构">2.1.3. 定义图结构</h3>
<h4 id="2-1-3-1-定义图节点">2.1.3.1. 定义图节点</h4>
<pre><code class="language-python">def node_1(state: OverAllState) -&gt; OverAllState:
    pre_id = state[&quot;cur_id&quot;]
    return {
        &quot;logs&quot;: [&quot;node_1 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_1&quot;
    }

def node_2(state: OverAllState) -&gt; OverAllState:
    pre_id = state[&quot;cur_id&quot;]
    return {
        &quot;logs&quot;: [&quot;node_2 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_2&quot;
    }
</code></pre>
<h4 id="2-1-3-2-添加节点">2.1.3.2. 添加节点</h4>
<pre><code class="language-python">builder.add_node(&quot;node_1&quot;, node_1)
builder.add_node(&quot;node_2&quot;, node_2)
</code></pre>
<h4 id="2-1-3-3-添加边">2.1.3.3. 添加边</h4>
<pre><code class="language-python">builder.add_edge(START, &quot;node_1&quot;)
builder.add_edge(&quot;node_1&quot;, &quot;node_2&quot;)
builder.add_edge(&quot;node_2&quot;, END)
</code></pre>
<h2 id="2-2-编译状态图">2.2. 编译状态图</h2>
<p>将上一步得到的状态图编译为可执行的运行图</p>
<pre><code class="language-python">graph = builder.compile()
</code></pre>
<h2 id="2-3-调用状态图">2.3. 调用状态图</h2>
<p>可以通过invoke调用计算图</p>
<pre><code class="language-python">print(graph.invoke({&quot;cur_id&quot;: &quot;start&quot;}))
</code></pre>
<h2 id="2-4-完整代码">2.4. 完整代码</h2>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import TypedDict, Annotated
from operator import add

class OverAllState(TypedDict):
    logs: Annotated[list[str], add]
    cur_id: str

def node_1(state: OverAllState) -&gt; OverAllState:
    pre_id = state[&quot;cur_id&quot;]
    return {
        &quot;logs&quot;: [&quot;node_1 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_1&quot;
    }

def node_2(state: OverAllState) -&gt; OverAllState:
    pre_id = state[&quot;cur_id&quot;]
    return {
        &quot;logs&quot;: [&quot;node_2 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_2&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_1&quot;, node_1)
builder.add_node(&quot;node_2&quot;, node_2)
builder.add_edge(START, &quot;node_1&quot;)
builder.add_edge(&quot;node_1&quot;, &quot;node_2&quot;)
builder.add_edge(&quot;node_2&quot;, END)

graph = builder.compile()

print(graph.invoke({&quot;cur_id&quot;: &quot;start&quot;}))
</code></pre>
<p>运行结果如下</p>
<pre><code>{'logs': ['node_1 运行完毕', 'node_2 运行完毕'], 'cur_id': 'start, node_1, node_2'}
</code></pre>
<h2 id="2-5-图结构可视化">2.5. 图结构可视化</h2>
<h3 id="2-5-1-绘制mermaid并获取源码">2.5.1. 绘制mermaid并获取源码</h3>
<p>mermaid是一种生成图表和流程图的文本标记语言，它可以把简单的文本描述直接渲染成可视化图形。</p>
<p>通过以下代码可以用mermaid语法表达图结构，并打印mermaid源码</p>
<pre><code class="language-python">raw_mermaid = graph.get_graph().draw_mermaid()
print(raw_mermaid)
</code></pre>
<p>输出如下</p>
<pre><code>---
config:
  flowchart:
    curve: linear
---
graph TD;
    __start__([&lt;p&gt;__start__&lt;/p&gt;]):::first
    node_1(node_1)
    node_2(node_2)
    __end__([&lt;p&gt;__end__&lt;/p&gt;]):::last
    __start__ --&gt; node_1;
    node_1 --&gt; node_2;
    node_2 --&gt; __end__;
    classDef default fill:#f2f0ff,line-height:1.2
    classDef first fill-opacity:0
    classDef last fill:#bfb6fcmermaidmermain
</code></pre>
<p>输出文本从<code>graph TD;</code>开始的部分为mermaid源码。</p>
<pre><code class="language-mermaid">graph TD;
    __start__([&lt;p&gt;__start__&lt;/p&gt;]):::first
    node_1(node_1)
    node_2(node_2)
    __end__([&lt;p&gt;__end__&lt;/p&gt;]):::last
    __start__ --&gt; node_1;
    node_1 --&gt; node_2;
    node_2 --&gt; __end__;
    classDef default fill:#f2f0ff,line-height:1.2
    classDef first fill-opacity:0
    classDef last fill:#bfb6fc
</code></pre>
<h3 id="2-5-2-绘制mermaid并转换为png">2.5.2. 绘制mermaid并转换为png</h3>
<p>Langgraph也支持将mermaid转换为图片字节流</p>
<pre><code class="language-python">png_bytes = graph.get_graph().draw_mermaid_png()
</code></pre>
<p><code>draw_mermaid_png()</code>底层会先调用<code>draw_mermaid()</code>得到mermaid语法的图表代码，然后调用在线服务渲染为mermaid图片。</p>
<p>默认的mermaid在线渲染服务链接为：https://mermaid.ink，由于网络问题可能存在渲染失败的情况，通常重试即可成功。</p>
<p>可以通过<code>draw_mermaid_png()</code>的<code>base_url</code>参数替换可用的mermaid在线渲染服务，</p>
<h4 id="2-5-2-1-jupyter环境下直接展示图片">2.5.2.1. jupyter环境下直接展示图片</h4>
<p>在jupyter环境下，可以通过IPython提供的功能将图片字节流直接展示为图片。</p>
<pre><code class="language-python">from IPython.display import display, Image

png_bytes = graph.get_graph().draw_mermaid_png()
png = Image(png_bytes)
display(png)
</code></pre>
<p>运行结果如下所示</p>
<p><img src="/images/langgraph-study-notes/image-20260522103648868.png" alt="image-20260522103648868" /></p>
<h4 id="2-5-2-2-保存为图片文件">2.5.2.2. 保存为图片文件</h4>
<p>也可以将图片字节流转换为PNG文件落盘</p>
<pre><code class="language-python">png_bytes = graph.get_graph().draw_mermaid_png()
png_filename = 'first_demo_graph.png'
with open(png_filename, &quot;wb&quot;) as f:
    f.write(png_bytes)
</code></pre>
<p>运行结束后，代码文件同级目录下将会出现生成好的图片文件，如下图所示。</p>
<p><img src="/images/langgraph-study-notes/image-20260522103907281.png" alt="image-20260522103907281" /></p>
<p>如果看不到文件，可以刷新上级目录</p>
<p><img src="/images/langgraph-study-notes/image-20260522104032434.png" alt="image-20260522104032434" /></p>
<p>图片内容如下</p>
<p><img src="/images/langgraph-study-notes/first_demo_graph.png" alt="first_demo_graph" /></p>
<h4 id="2-5-2-3-快捷用法">2.5.2.3. 快捷用法</h4>
<p>在 <code>Jupyter</code> 环境下，可以直接使用 <code>display(graph)</code> 快速展示编译后的图结构：</p>
<pre><code class="language-python">from IPython.display import display

display(graph)
</code></pre>
<p>效果如下</p>
<p><img src="/images/langgraph-study-notes/image-20260527174858380.png" alt="image-20260527174858380" /></p>
<h1 id="3-图的状态-state-管理">3. 图的状态（State）管理</h1>
<h2 id="3-1-状态定义">3.1. 状态定义</h2>
<p>状态的定义实际上是在声明状态的Schema，后者是状态字段的完整描述。</p>
<p>官方推荐了三种定义Schema的方式：TypedDict、dataclass、Pydantic</p>
<h3 id="3-1-1-typeddict">3.1.1. TypedDict</h3>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import TypedDict, Annotated
from operator import add

class OverAllState(TypedDict):
    logs: Annotated[list[str], add]
    cur_id: str

def node_1(state: OverAllState) -&gt; OverAllState:
    pre_id = state[&quot;cur_id&quot;]
    return {
        &quot;logs&quot;: [&quot;node_1 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_1&quot;
    }

def node_2(state: OverAllState) -&gt; OverAllState:
    pre_id = state[&quot;cur_id&quot;]
    return {
        &quot;logs&quot;: [&quot;node_2 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_2&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_1&quot;, node_1)
builder.add_node(&quot;node_2&quot;, node_2)
builder.add_edge(START, &quot;node_1&quot;)
builder.add_edge(&quot;node_1&quot;, &quot;node_2&quot;)
builder.add_edge(&quot;node_2&quot;, END)

graph = builder.compile()

print(graph.invoke({&quot;cur_id&quot;: &quot;start&quot;}))
</code></pre>
<p>输出</p>
<pre><code class="language-json">{'logs': ['node_1 运行完毕', 'node_2 运行完毕'], 'cur_id': 'start, node_1, node_2'}
</code></pre>
<h3 id="3-1-2-dataclass">3.1.2. dataclass</h3>
<p>属性调用方式由<code>['字段名']</code>变为<code>.字段名</code>。</p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import Annotated
from dataclasses import dataclass
from operator import add

@dataclass
class OverAllState:
    logs: Annotated[list[str], add]
    cur_id: str

def node_1(state: OverAllState) -&gt; OverAllState:
    pre_id = state.cur_id
    return {
        &quot;logs&quot;: [&quot;node_1 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_1&quot;
    }

def node_2(state: OverAllState) -&gt; OverAllState:
    pre_id = state.cur_id
    return {
        &quot;logs&quot;: [&quot;node_2 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_2&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_1&quot;, node_1)
builder.add_node(&quot;node_2&quot;, node_2)
builder.add_edge(START, &quot;node_1&quot;)
builder.add_edge(&quot;node_1&quot;, &quot;node_2&quot;)
builder.add_edge(&quot;node_2&quot;, END)

graph = builder.compile()

print(graph.invoke({&quot;cur_id&quot;: &quot;start&quot;}))
</code></pre>
<p>输出</p>
<pre><code class="language-json">{'logs': ['node_1 运行完毕', 'node_2 运行完毕'], 'cur_id': 'start, node_1, node_2'}
</code></pre>
<h3 id="3-1-3-pydantic">3.1.3. Pydantic</h3>
<p>Pydantic模型的字段访问方式和dataclass相同。</p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import Annotated
from pydantic import BaseModel
from operator import add

class OverAllState(BaseModel):
    logs: Annotated[list[str], add]
    cur_id: str

def node_1(state: OverAllState) -&gt; OverAllState:
    pre_id = state.cur_id
    return {
        &quot;logs&quot;: [&quot;node_1 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_1&quot;
    }

def node_2(state: OverAllState) -&gt; OverAllState:
    pre_id = state.cur_id
    return {
        &quot;logs&quot;: [&quot;node_2 运行完毕&quot;],
        &quot;cur_id&quot;: pre_id + &quot;, node_2&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_1&quot;, node_1)
builder.add_node(&quot;node_2&quot;, node_2)
builder.add_edge(START, &quot;node_1&quot;)
builder.add_edge(&quot;node_1&quot;, &quot;node_2&quot;)
builder.add_edge(&quot;node_2&quot;, END)

graph = builder.compile()

print(graph.invoke({&quot;cur_id&quot;: &quot;start&quot;}))
</code></pre>
<p>输出</p>
<pre><code class="language-json">{'logs': ['node_1 运行完毕', 'node_2 运行完毕'], 'cur_id': 'start, node_1, node_2'}
</code></pre>
<h3 id="3-1-4-校验行为">3.1.4. 校验行为</h3>
<p>学习LangChain的结构化输出时我们提到：Pydantic对格式要求最严格，如果模型返回的内容不符合结构化Schema的要求，则抛出<code>ValidationError</code>。而其余方式都不会对模型的返回结果进行校验，即便模型返回的内容不符合结构化要求，也会原样返回给用户。</p>
<p>而作为LangGraph计算图的状态时，这三种方式都要求字段名称完全一致。只是处理方式不同。具体规则如下</p>
<h4 id="3-1-4-1-输入字段不匹配">3.1.4.1. 输入字段不匹配</h4>
<h5 id="1-typeddict">1. TypedDict</h5>
<p>TypedDict将输入字段视为字典的<code>Key</code>，不匹配时抛出<code>KeyError</code>异常。</p>
<h5 id="2-dataclass">2. dataclass</h5>
<p>dataclass将输入字段视为类的<code>属性</code>，不匹配时抛出<code>TypeError（类型错误）</code>异常。</p>
<h5 id="3-pydantic">3. Pydantic</h5>
<p>Pydantic对输入字段进行校验，不匹配时抛出<code>ValidationError</code>异常。</p>
<h4 id="3-1-4-2-节点返回字段不匹配">3.1.4.2. 节点返回字段不匹配</h4>
<p>图节点返回的是对于状态的更新，如果返回字段和状态字段不匹配，上述三种Schema定义方式的行为是统一的：状态更新会被忽略。</p>
<h3 id="3-1-5-推荐用法">3.1.5. 推荐用法</h3>
<p>在实际使用中，推荐优先使用 <code>TypedDict</code> 定义 LangGraph 状态图的 State Schema。</p>
<p>大多数官方案例也采用 <code>TypedDict</code> 方式定义状态 <code>Schema</code>。这种方式写法简洁、结构清晰，能够直接描述状态中包含哪些字段，以及每个字段对应的数据类型，非常适合用于定义图运行过程中的共享状态。</p>
<p>相比普通 <code>dict</code>，<code>TypedDict</code> 可以提供更明确的字段约束和类型提示；相比 <code>dataclass</code>，<code>TypedDict</code> 更贴近 LangGraph 中状态的更新方式，因为节点通常返回的是表示“部分状态更新”的字典，而不是完整对象；相比 <code>Pydantic BaseModel</code>，它又更加轻量，不会引入额外的数据校验开销。因此，在没有复杂校验需求的情况下，<code>TypedDict</code> 是定义 LangGraph State Schema 的首选方式。</p>
<h2 id="3-2-state-reducer">3.2. State Reducer</h2>
<h4 id="3-2-1-什么是state-reducer">3.2.1. 什么是State Reducer</h4>
<p>State Reducer 是 LangGraph 中用于合并状态更新的核心机制。在 LangGraph 的 <code>StateGraph</code> 中，每个节点可以读取和写入共享状态，而 Reducer 定义了如何将多个节点对同一状态键的更新合并</p>
<p>Reducer 的核心特征：</p>
<ul>
<li>函数签名：<code>(Value, Value) -&gt; Value</code>，接收当前值和更新值，返回合并后的新值</li>
<li>注解定义：通过 <code>Annotated[Type, reducer_function]</code> 为状态键指定 Reducer</li>
<li>默认行为：未指定 Reducer 的状态键使用覆盖策略（Last-Write-Wins）</li>
</ul>
<h4 id="3-2-2-如何定义reducer">3.2.2. 如何定义Reducer</h4>
<h5 id="3-2-2-1-定义reducer函数">3.2.2.1. 定义Reducer函数</h5>
<p>Reducer 本质上是一个二元合并函数，用于定义当同一个字段产生多个更新值时，LangGraph 应该如何将这些值合并为一个最终结果。</p>
<p>函数签名：<code>(Value, Value) -&gt; Value</code></p>
<p>示例代码如下：</p>
<pre><code class="language-python">def my_reducer(left: list[str], right: list[str]) -&gt; list[str]:
    return left + right

left = ['a', 'b']
right = ['c']
print(my_reducer(left, right))
</code></pre>
<p>其中，<code>my_reducer</code> 用于处理 <code>list[str]</code> 类型的数据。它接收两个列表参数：</p>
<ul>
<li><code>left</code>：当前已累计的状态值；</li>
<li><code>right</code>：本次待合并的新值。</li>
</ul>
<p>函数内部通过 <code>left + right</code> 将两个列表合并，并返回合并后的结果。</p>
<p>因此，该 Reducer 的作用是：当某个状态字段存在多次列表更新时，将这些列表内容追加合并，而不是直接覆盖原值。</p>
<p>运行结果如下</p>
<pre><code>['a', 'b', 'c']
</code></pre>
<h5 id="3-2-2-2-将reducer和状态字段关联">3.2.2.2. 将Reducer和状态字段关联</h5>
<p>在 LangGraph 中，Reducer 通常通过 Python 的 <code>typing.Annotated</code> 与状态字段进行关联。</p>
<p><code>Annotated[]</code> 是 Python 提供的一种类型注解扩展机制，用于在原始类型之外附加额外的元数据信息。需要注意的是，<code>Annotated[]</code> 本身并不规定这些元数据的具体含义，它只负责在类型注解中保留这些信息。</p>
<p>严格来说，<code>Annotated</code> 的第一个参数是被注解的原始类型，后续参数是附加的元数据。至于这些元数据表示什么、如何解析，则由使用它的框架或工具自行决定。</p>
<p>在 LangGraph 中，框架利用这一机制，将状态字段的类型和 Reducer 规则同时声明在字段定义中。其基本形式如下：</p>
<pre><code class="language-python">Annotated[Type, reducer_function]
</code></pre>
<p>其中：</p>
<ul>
<li><code>Type</code>：表示状态字段的数据类型；</li>
<li><code>reducer_function</code>：表示该字段对应的 Reducer 函数。</li>
</ul>
<p>示例代码如下：</p>
<pre><code>from typing import TypedDict, Annotated

class OverAllState(TypedDict):
    logs: Annotated[list[str], my_reducer]
    cur_id: str
</code></pre>
<p>在上述代码中：</p>
<ul>
<li><code>logs</code> 字段的类型是 <code>list[str]</code>；</li>
<li><code>my_reducer</code> 是与 <code>logs</code> 字段关联的 Reducer 函数；</li>
<li>当多个节点同时更新 <code>logs</code> 字段时，LangGraph 会使用 <code>my_reducer</code> 将多个列表合并；</li>
<li><code>cur_id</code> 字段没有指定 Reducer，因此采用默认更新规则。</li>
</ul>
<h5 id="3-2-2-3-常用内置reducer函数">3.2.2.3. 常用内置Reducer函数</h5>
<h6 id="1-operator-add">1. operator.add</h6>
<p><code>operator.add</code> 是 Python 内置的加法操作函数，底层由 C 实现</p>
<p>它接收两个参数，等价于 <code>a（第一个参数）+b（第二个参数）</code></p>
<p>代码如下</p>
<pre><code class="language-python">from operator import add

print(f&quot;{add(1,2) = }&quot;)
print(f&quot;{add([1,2], [3,4]) = }&quot;)
print(f&quot;{add(['a','b'], ['c']) = }&quot;)
</code></pre>
<p>输出如下</p>
<pre><code>add(1,2) = 3
add([1,2], [3,4]) = [1, 2, 3, 4]
add(['a','b'], ['c']) = ['a', 'b', 'c']
</code></pre>
<h6 id="2-langgraph-graph-message-add_messages">2. langgraph.graph.message.add_messages</h6>
<p><code>add_messages</code> 是 LangGraph 中专用于合并消息列表的 Reducer 函数，常用于维护对话历史类的状态字段。其函数签名如下：</p>
<pre><code class="language-python">def add_messages(
    left: Messages,
    right: Messages,
    *,
    format: Literal[&quot;langchain-openai&quot;] | None = None,
) -&gt; Messages:
    ...
    return merged
</code></pre>
<p>参数说明：</p>
<ul>
<li><code>left</code>：状态中已有的消息列表；</li>
<li><code>right</code>：当前节点返回的消息更新值；</li>
<li><code>format</code>：可选参数，用于指定返回消息的格式，通常无需手动设置。</li>
</ul>
<p><code>left</code> 与 <code>right</code> 的类型均为 <code>Messages</code>。<code>Messages</code> 可以理解为 LangChain 消息对象的列表，其中每个元素都是 <code>BaseMessage</code> 或其子类的实例，常见子类包括：</p>
<ul>
<li><code>HumanMessage</code>：用户的输入消息；</li>
<li><code>AIMessage</code>：AI 的回复消息；</li>
<li><code>SystemMessage</code>：系统提示消息；</li>
<li><code>ToolMessage</code>：工具调用的结果消息。</li>
</ul>
<p><code>add_messages</code> 处理的是对话消息序列，而非普通的字符串列表。</p>
<p><code>BaseMessage</code> 包含一个可选的 <code>id</code> 属性，用于唯一标识一条消息。<code>add_messages</code> 在合并 <code>left</code> 与 <code>right</code> 时，不是简单地执行列表拼接，而是依据消息的 <code>id</code> 进行合并：</p>
<ul>
<li>若 <code>right</code> 中的某条消息的 <code>id</code> 在 <code>left</code> 中不存在，则将该消息追加到结果列表末尾；</li>
<li>若 <code>right</code> 中的某条消息的 <code>id</code> 与 <code>left</code> 中已有消息的 <code>id</code> 相同，则使用 <code>right</code> 中的新消息替换 <code>left</code> 中的旧消息。</li>
</ul>
<p>因此，<code>add_messages</code> 的作用可以概括为：在保留历史消息的基础上追加新消息，并允许通过相同的消息 <code>id</code> 覆盖已有消息。</p>
<p>需要特别说明，<code>add_messages</code> 并非简单地对 <code>left</code> 与 <code>right</code> 求“并集”。更准确地说，它是一个基于消息 <code>id</code> 的消息列表合并函数：既支持追加新消息，也支持更新已有消息。</p>
<p>可以理解为：</p>
<pre><code class="language-python">merged = left + right
</code></pre>
<p>但若 <code>right</code> 中存在与 <code>left</code> 相同 <code>id</code> 的消息，则最终结果中不会出现重复消息，而是用 <code>right</code> 中的消息覆盖 <code>left</code> 中对应的旧消息。</p>
<p>示例代码如下</p>
<pre><code class="language-python">from langgraph.graph.message import add_messages
from langchain.messages import HumanMessage, AIMessage, SystemMessage

left = [
    SystemMessage(content=&quot;你是个善解人意的助手&quot;, id='1'),
    HumanMessage(content=&quot;你好&quot;, id='2'),
    AIMessage(content=&quot;你好~&quot;, id='3'),
]

right = [
    HumanMessage(content=&quot;我是老王，你是小王&quot;, id='2'),
    AIMessage(content=&quot;好的，我记住啦&quot;, id='3'),
    HumanMessage(content=&quot;你是谁？&quot;, id='4'),
    AIMessage(content=&quot;我是小王&quot;, id='5'),
]

merged = add_messages(left, right)

for msg in merged:
    print(msg)
</code></pre>
<p>输出如下</p>
<pre><code>content='你是个善解人意的助手' additional_kwargs={} response_metadata={} id='1'
content='我是老王，你是小王' additional_kwargs={} response_metadata={} id='2'
content='好的，我记住啦' additional_kwargs={} response_metadata={} id='3' tool_calls=[] invalid_tool_calls=[]
content='你是谁？' additional_kwargs={} response_metadata={} id='4'
content='我是小王' additional_kwargs={} response_metadata={} id='5' tool_calls=[] invalid_tool_calls=[]
</code></pre>
<h3 id="3-2-3-默认行为">3.2.3. 默认行为</h3>
<p>如果某个 State 字段没有显式定义 <code>Reducer</code>，LangGraph 会使用默认的状态更新行为：后一次更新值会覆盖该字段原有的状态值。</p>
<p>换句话说，当节点返回的更新结果中包含某个字段时，如果该字段没有配置 Reducer，LangGraph 不会对新旧值进行追加、合并或累加，而是直接使用本次返回的新值替换原来的旧值。</p>
<p>示例代码如下：</p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import TypedDict

class OverAllState(TypedDict):
    logs: list[str]
    id: str

def node_a(state: OverAllState):
    return {
        &quot;logs&quot;: [&quot;node_a&quot;],
        &quot;id&quot;: &quot;node_a&quot;
    }

def node_b(state: OverAllState):
    return {
        &quot;logs&quot;: [&quot;node_b&quot;],
        &quot;id&quot;: &quot;node_b&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, END)

graph = builder.compile()
result = graph.invoke({&quot;logs&quot;: [&quot;START&quot;], &quot;id&quot;: &quot;start&quot;})
print('=' * 30, '-&gt; result &lt;-', '=' * 30)
print(result)
</code></pre>
<p>输出如下</p>
<pre><code>============================== -&gt; result &lt;- ==============================
{'logs': ['node_b'], 'id': 'node_b'}
</code></pre>
<p>可以看到，<code>logs</code>字段和<code>id</code>字段都没有定义<code>Reducer</code>，因此，节点返回的新值会覆盖初始状态中的旧值，图运行结果中的状态值和最后一次更新保持一致。</p>
<h2 id="3-3-节点中访问state">3.3. 节点中访问State</h2>
<h3 id="3-3-1-图节点中读取state">3.3.1. 图节点中读取State</h3>
<p>在 LangGraph 中，节点本质上是一个可调用对象，通常定义为普通 Python 函数。节点函数被执行时，LangGraph 会自动将当前图运行到该节点时的 <code>State</code> 传入节点函数。</p>
<p>节点函数的第一个参数通常是当前运行图的状态对象，也就是 <code>State</code>。</p>
<pre><code>def node(state: StateSchema):
    ...
</code></pre>
<p>其中，<code>state</code> 表示当前节点执行时可以访问到的全局状态快照。节点可以通过读取 <code>state</code> 中的字段获取上游节点写入的数据，并基于这些数据完成当前节点的业务逻辑。</p>
<p>示例代码如下：</p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import TypedDict, Annotated
from operator import add

class OverAllState(TypedDict):
    logs: Annotated[list[str], add]
    id: str

def node_a(state: OverAllState):
    for k, v in state.items():
        print(f&quot;k: {k}, v: {v}&quot;)

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, END)

graph = builder.compile()
result = graph.invoke({&quot;logs&quot;: [&quot;START&quot;], &quot;id&quot;: &quot;start&quot;})
</code></pre>
<p>输出如下</p>
<pre><code>k: logs, v: ['START']
k: id, v: start
</code></pre>
<h3 id="3-3-2-图节点更新state">3.3.2. 图节点更新State</h3>
<p>在 LangGraph 中，节点函数通常不需要返回更新后的完整状态，只需要返回本节点对状态的局部更新。</p>
<p>也就是说，节点的返回值可以只包含需要修改的状态字段。</p>
<ul>
<li>
<p>对于节点没有返回的字段，LangGraph 会保留其原有状态值；</p>
</li>
<li>
<p>对于节点返回的字段，LangGraph 会根据该字段是否配置了 <code>Reducer</code> 来决定如何合并更新值。</p>
<ul>
<li>如果字段配置了 <code>Reducer</code>，则使用对应的 <code>Reducer</code> 函数将旧值和新值合并；</li>
<li>如果字段没有配置 <code>Reducer</code>，则按照默认规则使用节点返回的新值覆盖原值。</li>
</ul>
</li>
</ul>
<p>LangGraph运行时会按照状态字段的<code>Reducer</code>函数将其与当前的最新状态合并。</p>
<p>示例代码如下：</p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from typing import TypedDict, Annotated
from operator import add

class OverAllState(TypedDict):
    logs: Annotated[list[str], add]
    id: str

def node_a(state: OverAllState):
    for k, v in state.items():
        print(f&quot;k: {k}, v: {v}&quot;)
    return {
        &quot;logs&quot;: [&quot;node_a 更新状态&quot;]
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, END)

graph = builder.compile()
result = graph.invoke({&quot;logs&quot;: [&quot;START&quot;], &quot;id&quot;: &quot;start&quot;})
print('=' * 30, '-&gt; result &lt;-', '=' * 30)
print(result)
</code></pre>
<p>输出如下</p>
<pre><code>k: logs, v: ['START']
k: id, v: start
============================== -&gt; result &lt;- ==============================
{'logs': ['START', 'node_a 更新状态'], 'id': 'start'}
</code></pre>
<p>在上述示例中：</p>
<ul>
<li><code>logs</code> 字段通过 <code>Annotated[list[str], add]</code> 绑定了 Reducer 函数 <code>operator.add</code>，因此 LangGraph 会将原有的 <code>logs</code> 值和 <code>node_a</code> 返回的新 <code>logs</code> 值进行列表拼接：</li>
</ul>
<pre><code>[&quot;START&quot;] + [&quot;node_a 更新状态&quot;]
</code></pre>
<p>最终得到：</p>
<pre><code>[&quot;START&quot;, &quot;node_a 更新状态&quot;]
</code></pre>
<ul>
<li><code>id</code> 字段没有出现在 <code>node_a</code> 的返回值中，因此该字段不会被更新。图运行结束后，输出状态中的 <code>id</code> 仍然保持输入时的值：</li>
</ul>
<pre><code>&quot;id&quot;: &quot;start&quot;
</code></pre>
<p>因此，LangGraph 节点更新 State 的核心规则可以概括为：</p>
<blockquote>
<p>节点只返回需要更新的字段；未返回的字段保持不变；返回的字段根据是否配置 Reducer 决定是合并还是覆盖。</p>
</blockquote>
<h3 id="3-3-3-overwrite绕过reducer">3.3.3. Overwrite绕过Reducer</h3>
<p>在前面的示例中，如果某个状态字段定义了 <code>Reducer</code>，那么节点返回该字段的更新值时，LangGraph 默认会通过对应的 <code>Reducer</code> 将新值与已有状态值进行合并。</p>
<p>在某些场景下，我们可能并不希望继续执行 <code>Reducer</code> 的聚合逻辑，而是希望本次更新直接覆盖旧值。这时可以使用 <code>Overwrite</code>。</p>
<p><code>Overwrite</code> 的作用是：告诉 LangGraph 本次状态更新不走该字段原本定义的 <code>Reducer</code>，而是直接用新值覆盖状态中的旧值。</p>
<p>需要注意的是，<code>Overwrite</code> 只影响当前这一次更新，并不会修改状态字段本身的 <code>Reducer</code> 定义。后续节点如果继续正常返回该字段的更新值，仍然会按照原来的 <code>Reducer</code> 逻辑进行合并。</p>
<p>示例代码如下：</p>
<pre><code class="language-python">from langgraph.graph import StateGraph, START, END
from langgraph.types import Overwrite
from typing import TypedDict, Annotated
from operator import add

class OverAllState(TypedDict):
    logs: Annotated[list[str], add]
    id: str

def node_a(state: OverAllState):
    return {
        &quot;logs&quot;: [&quot;node_a&quot;],
        &quot;id&quot;: &quot;node_a&quot;
    }

def node_b(state: OverAllState):
    return {
        &quot;logs&quot;: Overwrite([&quot;node_b&quot;]),
        &quot;id&quot;: &quot;node_b&quot;
    }

def node_c(state: OverAllState):
    return {
        &quot;logs&quot;: [&quot;node_c&quot;],
        &quot;id&quot;: &quot;node_c&quot;
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;node_b&quot;, node_b)
builder.add_node(&quot;node_c&quot;, node_c)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;node_b&quot;)
builder.add_edge(&quot;node_b&quot;, &quot;node_c&quot;)
builder.add_edge(&quot;node_c&quot;, END)

graph = builder.compile()
result = graph.invoke({&quot;logs&quot;: [&quot;START&quot;], &quot;id&quot;: &quot;start&quot;})
print('=' * 30, '-&gt; result &lt;-', '=' * 30)
print(result)
</code></pre>
<p>输出如下</p>
<pre><code>============================== -&gt; result &lt;- ==============================
{'logs': ['node_b', 'node_c'], 'id': 'node_c'}
</code></pre>
<p>上述代码中：</p>
<ul>
<li><code>logs</code> 字段绑定了 <code>operator.add()</code> 函数
<ul>
<li>如果没有 <code>Overwrite</code>，则最终输出的 <code>logs</code> 的值应为 <code>['START', 'node_a', 'node_b', 'node_c']</code></li>
<li><code>node_b</code> 返回更新时，用 <code>Overwrite</code> 包裹了 <code>logs</code> 字段的值，那么当前状态的 <code>logs</code> 会被 <code>[&quot;node_b&quot;]</code> 覆盖，因此最终输出的 <code>logs</code> 字段值变成了 <code>['node_b', 'node_c']</code></li>
</ul>
</li>
<li><code>id</code> 字段按照默认行为，保留最后一次更新的值。</li>
</ul>
<h2 id="3-4-multi-schema用法">3.4. Multi Schema用法</h2>
<h3 id="3-4-1-状态类型">3.4.1. 状态类型</h3>
<p><code>LangGraph</code> 支持在一个图中使用多个状态 Schema，用于区分图的外部输入、外部输出、内部共享状态以及节点间的临时状态。</p>
<p>常见状态类型可以分为以下几类：</p>
<ul>
<li>全局状态 / 内部状态：图内部主要使用的状态，创建 <code>StateGraph</code> 时传递给 <code>state_schema</code> 参数。它通常包含图运行过程中需要读写的大部分字段。</li>
<li>输入状态：图对外接收输入时使用的状态，创建 <code>StateGraph</code> 时传递给 <code>input_schema</code> 参数。它用于约束调用图时允许传入哪些字段。</li>
<li>输出状态：图最终对外返回结果时使用的状态，创建 <code>StateGraph</code> 时传递给 <code>output_schema</code> 参数。它用于约束图运行结束后只返回哪些字段。</li>
<li>私有状态：图内部节点之间传递的临时状态，通常不作为图的输入，也不作为图的最终输出。它可以通过节点函数的入参类型注解声明，并在节点返回值中写入。</li>
</ul>
<p>需要注意，输入状态和输出状态主要面向图的边界，即“图如何接收外部输入”和“图如何返回外部结果”；而全局状态和私有状态主要面向图内部节点之间的数据传递。</p>
<h3 id="3-4-2-状态之间的关系">3.4.2. 状态之间的关系</h3>
<h4 id="3-4-2-1-设计规范">3.4.2.1. 设计规范</h4>
<p>本节主要说明 <code>LangGraph</code> 状态设计中的规范。以下规则属于工程上的最佳实践，违反这些规范未必一定导致程序报错，但容易降低代码的可读性和可维护性。</p>
<ol>
<li>
<p>输入状态和输出状态通常应是全局状态的子集
输入状态描述图对外需要接收的数据，输出状态描述图最终需要返回的数据。通常情况下，它们都应该是全局状态的一部分。
如：</p>
<pre><code class="language-python">class InputState(TypedDict):
    username: str

class OutputState(TypedDict):
    graph_output: str

class OverAllState(TypedDict):
    username: str
    nickname: str
    graph_output: str
</code></pre>
<p>其中，<code>InputState</code> 和 <code>OutputState</code> 的所有字段均存在于 <code>OverAllState</code> 中。</p>
</li>
<li>
<p>私有状态和全局状态应尽量避免字段重名
私有状态的定位是图内部某些节点之间传递的临时字段。如果私有状态字段和全局状态字段重名，虽然某些情况下程序仍然可以运行，但容易让人误以为该字段是全局共享字段，从而造成理解混乱。
因此，推荐让私有状态字段和全局状态字段保持清晰边界。</p>
</li>
<li>
<p>节点函数应明确声明入参状态类型和返回状态类型
节点函数的第一个参数通常是当前节点可读取的状态。通过类型注解声明该参数，可以明确表达该节点需要读取哪些字段。
同时，给节点函数声明返回状态类型，也可以帮助阅读者理解该节点会更新哪些字段。
例如：</p>
<pre><code class="language-python">def node_1(state: InputState) -&gt; OverAllState:
    return {
        &quot;nickname&quot;: &quot;Dear &quot; + state[&quot;username&quot;]
    }
</code></pre>
</li>
<li>
<p>节点函数中不应该访问入参状态类型中不存在的字段
节点实际接收到的状态会按照其入参类型进行裁剪。因此，如果节点入参声明为 <code>InputState</code>，就不应该在节点内部访问 <code>InputState</code> 中不存在的字段。
例如：</p>
<pre><code class="language-python">def node_1(state: InputState) -&gt; OverAllState:
    return {
        &quot;nickname&quot;: state[&quot;username&quot;]
    }
</code></pre>
<p>如果在该函数中访问：</p>
<pre><code>state[&quot;nickname&quot;]
</code></pre>
<p>而 <code>nickname</code> 不属于 <code>InputState</code>，运行时就可能抛出 <code>KeyError</code>。</p>
</li>
<li>
<p>节点函数返回的字典应尽量和返回类型注解保持一致
从 Python 类型注解的角度看，函数返回类型只是静态提示，运行时不会自动强制校验。
从 LangGraph 的运行机制看，节点返回的是对状态的部分更新，不是完整状态。只要返回字段已经被图记录为可用状态字段，LangGraph 就可以将其作为状态更新处理。
不过，从工程规范上讲，节点返回字典中的字段最好和函数返回类型注解保持一致，这样更利于阅读、调试和维护。</p>
</li>
</ol>
<h4 id="3-4-2-2-源码层面的约束">3.4.2.2. 源码层面的约束</h4>
<p>本节从底层机制角度说明 <code>LangGraph</code> 如何记录、裁剪和更新状态。</p>
<h5 id="3-4-2-2-1-状态的记录">3.4.2.2.1. 状态的记录</h5>
<p><code>LangGraph</code> 的状态并不是简单保存在一个普通字典中，而是会被拆分成多个可读写的状态字段。每个状态字段在底层通常对应一个 <code>Channel</code>。</p>
<p>这些状态字段会在不同阶段被记录到状态图中。</p>
<ol>
<li>
<p><code>StateGraph</code> 记录状态字段的核心方法是 <code>_add_schema()</code>
<code>_add_schema()</code> 会解析传入的状态 Schema，并将其中声明的字段记录到图中，使这些字段成为图运行时可以读写的状态字段。</p>
</li>
<li>
<p>创建 <code>StateGraph</code> 时，会记录 <code>state_schema</code>、<code>input_schema</code> 和 <code>output_schema</code> 中的字段
当创建状态图时：</p>
<pre><code>builder = StateGraph(
    OverAllState,
    input_schema=InputState,
    output_schema=OutputState
)
</code></pre>
<p>LangGraph 会解析这些 Schema，并将其中涉及的字段加入图的状态管理体系。</p>
</li>
<li>
<p>调用 <code>add_node()</code> 添加节点时，也可能记录节点入参声明的状态 Schema
当添加节点时，<code>LangGraph</code> 会根据节点函数第一个参数的类型注解推断该节点的输入状态类型。
如果这个输入状态类型之前没有被图记录过，LangGraph 也会通过 <code>_add_schema()</code> 将其加入图中。
这也是私有状态能够生效的原因。
例如：</p>
<pre><code class="language-python">class PrivateState(TypedDict):
    greeting: str

def node_3(state: PrivateState) -&gt; OutputState:
    return {
        &quot;graph_output&quot;: state[&quot;greeting&quot;]
    }
</code></pre>
<p>当 <code>node_3</code> 被添加到图中时，<code>PrivateState</code> 中的 <code>greeting</code> 字段会被记录到图中，从而成为图内部可以传递的状态字段。</p>
</li>
<li>
<p>总结</p>
<ul>
<li>全局状态、输入状态、输出状态通常在创建 <code>StateGraph</code> 时被记录。</li>
<li>私有状态通常在调用 <code>add_node()</code> 添加节点时，根据节点入参类型注解被记录。</li>
<li>被记录后的状态字段，底层会成为图运行时可以读写的状态字段。</li>
</ul>
</li>
</ol>
<h5 id="3-4-2-2-2-状态的访问">3.4.2.2.2. 状态的访问</h5>
<ol>
<li>
<p>调用图时，输入会按照 <code>input_schema</code> 进行约束
当调用图时：</p>
<pre><code class="language-python">graph.invoke({&quot;username&quot;: &quot;小黄&quot;})
</code></pre>
<p>如果创建图时声明了 <code>input_schema</code>，那么外部输入会按照 <code>input_schema</code> 进行约束。
如果没有声明 <code>input_schema</code>，则通常按照 <code>state_schema</code> 作为图的输入 Schema。
因此，<code>input_schema</code> 的作用不是“只让第一个节点可见”，而是约束图的外部输入结构。
此处的约束是指：按照 <code>schema</code> 裁剪输入，只保留 <code>schema</code> 中出现的状态字段</p>
</li>
<li>
<p>节点接收到的状态会按照节点入参类型进行裁剪
每个节点能读取哪些字段，主要取决于该节点第一个参数的类型注解。
例如：</p>
<pre><code class="language-python">def node_1(state: InputState) -&gt; OverAllState:
    ...
</code></pre>
<p>此时，<code>node_1</code> 接收到的 <code>state</code> 会按照 <code>InputState</code> 进行裁剪。即使图的全局状态中还有其他字段，<code>node_1</code> 也不应该访问不属于 <code>InputState</code> 的字段。
如果访问了入参状态中不存在的字段，例如：</p>
<pre><code class="language-python">state[&quot;nickname&quot;]
</code></pre>
<p>就可能抛出：</p>
<pre><code class="language-python">KeyError
</code></pre>
</li>
<li>
<p>节点返回的是状态更新，而不是完整状态
节点函数不需要返回完整状态，只需要返回本节点想要更新的字段。
例如：</p>
<pre><code class="language-python">def node_1(state: InputState) -&gt; OverAllState:
    return {
        &quot;nickname&quot;: &quot;Dear &quot; + state[&quot;username&quot;]
    }
</code></pre>
<p>这里虽然返回类型注解是 <code>OverAllState</code>，但函数实际只返回了 <code>nickname</code> 一个字段。这是允许的，因为 LangGraph 会把节点返回值视为对状态的部分更新。</p>
</li>
<li>
<p>节点返回值的应用主要由字段名称和图中已记录的状态字段决定
节点返回的字典会根据字段名称写入对应状态字段，并按照该字段的 <code>Reducer</code> 规则进行合并。
需要注意的是，函数返回类型注解主要用于表达代码意图，不是严格的运行时写入边界。
也就是说，如果某个字段已经被图记录为可用状态字段，那么节点即使没有在返回类型注解中声明该字段，也可能仍然可以返回并更新它。
不过，为了代码清晰，仍然推荐让节点的返回值和返回类型注解保持一致。</p>
</li>
<li>
<p>最终输出会按照 <code>output_schema</code> 进行裁剪
图运行完成后，最终返回给外部调用方的结果会按照 <code>output_schema</code> 进行裁剪。
因此，<code>output_schema</code> 的作用不是“只让最后一个节点可见”，而是约束图最终对外暴露哪些字段。
例如，图内部状态中可能同时存在：</p>
<pre><code>username
nickname
greeting
graph_output
</code></pre>
<p>但如果 <code>output_schema</code> 只包含：</p>
<pre><code>graph_output
</code></pre>
<p>那么最终 <code>graph.invoke()</code> 的返回结果就只会包含 <code>graph_output</code>。</p>
</li>
</ol>
<h3 id="3-4-3-案例">3.4.3. 案例</h3>
<p>下面通过一个简单案例说明四类状态的定义和使用。</p>
<pre><code class="language-python">from typing import TypedDict
from langgraph.graph import StateGraph, START, END

class InputState(TypedDict):
    username: str

class OutputState(TypedDict):
    graph_output: str

class OverAllState(TypedDict):
    nickname: str
    username: str
    graph_output: str

class PrivateState(TypedDict):
    greeting: str

def node_1(state: InputState) -&gt; OverAllState:
    # 向全局状态写入数据
    return {
        &quot;nickname&quot;: &quot;Dear &quot; + state[&quot;username&quot;]
    }

def node_2(state: OverAllState) -&gt; PrivateState:
    # 从全局状态读取数据，写入私有状态
    return {
        &quot;greeting&quot;: state[&quot;nickname&quot;] + &quot;, 早上好~&quot;
    }

def node_3(state: PrivateState) -&gt; OutputState:
    # 从私有状态读取数据，写入输出状态
    return {
        &quot;graph_output&quot;: state[&quot;greeting&quot;] + &quot; 很高兴认识你！&quot;
    }

builder = StateGraph(OverAllState,input_schema=InputState,output_schema=OutputState)
builder.add_node(&quot;node_1&quot;, node_1)
builder.add_node(&quot;node_2&quot;, node_2)
builder.add_node(&quot;node_3&quot;, node_3)
builder.add_edge(START, &quot;node_1&quot;)
builder.add_edge(&quot;node_1&quot;, &quot;node_2&quot;)
builder.add_edge(&quot;node_2&quot;, &quot;node_3&quot;)
builder.add_edge(&quot;node_3&quot;, END)

graph = builder.compile()
print(graph.invoke({&quot;username&quot;:&quot;小黄&quot;}))
</code></pre>
<p>输出如下</p>
<pre><code class="language-json">{'graph_output': 'Dear 小黄, 早上好~ 很高兴认识你！'}
</code></pre>
<h3 id="3-4-4-案例执行过程分析">3.4.4. 案例执行过程分析</h3>
<p>上述案例中，各类状态的作用如下。</p>
<h4 id="1-输入状态">1. 输入状态</h4>
<pre><code class="language-python">class InputState(TypedDict):
    username: str
</code></pre>
<p><code>InputState</code> 用于约束图的外部输入。</p>
<p>因此调用图时，只需要传入：</p>
<pre><code>{&quot;username&quot;: &quot;小黄&quot;}
</code></pre>
<h4 id="2-全局状态">2. 全局状态</h4>
<pre><code class="language-python">class OverAllState(TypedDict):
    username: str
    nickname: str
    graph_output: str
</code></pre>
<p><code>OverAllState</code> 是图内部主要使用的状态 Schema。</p>
<p>其中：</p>
<ul>
<li><code>username</code> 来自图的输入；</li>
<li><code>nickname</code> 由 <code>node_1</code> 写入；</li>
<li><code>graph_output</code> 由 <code>node_3</code> 写入，并最终作为图的输出返回。</li>
</ul>
<h4 id="3-私有状态">3. 私有状态</h4>
<pre><code class="language-python">class PrivateState(TypedDict):
    greeting: str
</code></pre>
<p><code>PrivateState</code> 用于节点之间传递临时数据。</p>
<p>在本例中：</p>
<pre><code class="language-python">def node_2(state: OverAllState) -&gt; PrivateState:
    return {
        &quot;greeting&quot;: state[&quot;nickname&quot;] + &quot;, 早上好~&quot;
    }
</code></pre>
<p><code>node_2</code> 写入了 <code>greeting</code> 字段。</p>
<p>随后：</p>
<pre><code class="language-python">def node_3(state: PrivateState) -&gt; OutputState:
    return {
        &quot;graph_output&quot;: state[&quot;greeting&quot;] + &quot; 很高兴认识你！&quot;
    }
</code></pre>
<p><code>node_3</code> 通过 <code>PrivateState</code> 读取 <code>greeting</code> 字段，并生成最终输出。</p>
<h4 id="4-输出状态">4. 输出状态</h4>
<pre><code class="language-python">class OutputState(TypedDict):
    graph_output: str
</code></pre>
<p><code>OutputState</code> 用于约束图最终返回给外部调用方的数据。</p>
<p>虽然图内部运行过程中还存在 <code>username</code>、<code>nickname</code>、<code>greeting</code> 等字段，但最终结果只返回：</p>
<pre><code class="language-json">{'graph_output': 'Dear 小黄, 早上好~ 很高兴认识你！'}
</code></pre>
<p>这是因为图创建时声明了：</p>
<pre><code class="language-python">output_schema=OutputState
</code></pre>
<p>所以最终输出会按照 <code>OutputState</code> 进行裁剪。</p>
<h2 id="3-5-预定义状态">3.5. 预定义状态</h2>
<h3 id="3-5-1-messagesstate">3.5.1. MessagesState</h3>
<p><code>LangGraph</code> 构建的计算图通常会和 <code>LLM</code> 结合使用，而 <code>LLM</code> 在运行过程中通常需要维护一组消息列表。为了提升开发效率，<code>LangGraph</code> 官方提供了一个预定义状态类型：<code>langgraph.graph.message.MessagesState</code>。</p>
<p>开发者可以直接继承该状态类型，并在其基础上扩展自定义状态字段。</p>
<p>源码如下：</p>
<pre><code class="language-python">class MessagesState(TypedDict):
    messages: Annotated[list[AnyMessage], add_messages]
</code></pre>
<p>由此可知，<code>MessagesState</code> 只有一个字段：<code>messages</code>。</p>
<p>该字段的类型是列表，元素类型为 <code>AnyMessage</code>；同时，它通过 <code>Annotated</code> 绑定了内置 <code>Reducer</code> 函数 <code>add_messages</code>。</p>
<p><code>add_messages</code> 的完全限定名（英文全称 <code>fully qualified name</code>）是：</p>
<p><code>langgraph.graph.message.add_messages</code>，正是上文 3.2.2.3.2 节介绍的内置 <code>Reducer</code> 函数。</p>
<p>示例如下：</p>
<pre><code class="language-python">from langchain_deepseek import ChatDeepSeek
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import MessagesState
from langchain.messages import HumanMessage

from dotenv import load_dotenv
load_dotenv(override=True)

model = ChatDeepSeek(
    model='deepseek-v4-flash',
    extra_body={
        &quot;thinking&quot;: {
            &quot;type&quot;: &quot;disabled&quot;
        }
    }
)

class OverAllState(MessagesState):
    username: str
    output: str

def node_a(state: OverAllState) -&gt; OverAllState:
    return {
        &quot;messages&quot;: [HumanMessage(&quot;你好，我是 &quot; + state[&quot;username&quot;])]
    }

def llm_node(state: OverAllState) -&gt; OverAllState:
    res = model.invoke(state[&quot;messages&quot;])

    return {
        &quot;messages&quot;: [res],
        &quot;output&quot;: res.content
    }

builder = StateGraph(state_schema=OverAllState)
builder.add_node(&quot;node_a&quot;, node_a)
builder.add_node(&quot;llm_node&quot;, llm_node)
builder.add_edge(START, &quot;node_a&quot;)
builder.add_edge(&quot;node_a&quot;, &quot;llm_node&quot;)
builder.add_edge(&quot;llm_node&quot;, END)

graph = builder.compile()
response = graph.invoke({&quot;username&quot;: &quot;小黄&quot;})
print(response)
</code></pre>
<p>输出如下</p>
<pre><code class="language-json">{
    &quot;messages&quot;: [
        HumanMessage(
            content=&quot;你好，我是 小黄&quot;,
            additional_kwargs={},
            response_metadata={},
            id=&quot;155e1ef4-5bbc-4250-b978-62a7a5918cef&quot;,
        ),
        AIMessage(
            content=&quot;你好呀，小黄！😊 我是DeepSeek，很高兴认识你！有什么我可以帮你的吗？无论是聊天、解答问题、帮你写作、编程，还是其他任何需要，尽管告诉我吧！你名字里的“黄”是哪个黄呀？😄&quot;,
            additional_kwargs={
                &quot;refusal&quot;: &quot;None&quot;,
            },
            response_metadata={
                &quot;token_usage&quot;: {
                    &quot;completion_tokens&quot;: 57,
                    &quot;prompt_tokens&quot;: 10,
                    &quot;total_tokens&quot;: 67,
                    &quot;completion_tokens_details&quot;: &quot;None&quot;,
                    &quot;prompt_tokens_details&quot;: {
                        &quot;audio_tokens&quot;: &quot;None&quot;,
                        &quot;cached_tokens&quot;: 0,
                    },
                    &quot;prompt_cache_hit_tokens&quot;: 0,
                    &quot;prompt_cache_miss_tokens&quot;: 10,
                },
                &quot;model_provider&quot;: &quot;deepseek&quot;,
                &quot;model_name&quot;: &quot;deepseek-v4-flash&quot;,
                &quot;system_fingerprint&quot;: &quot;fp_8b330d02d0_prod0820_fp8_kvcache_20260402&quot;,
                &quot;id&quot;: &quot;17d57633-2ffb-4af3-be87-b3d58f9acd2b&quot;,
                &quot;finish_reason&quot;: &quot;stop&quot;,
                &quot;logprobs&quot;: &quot;None&quot;,
            },
            id=&quot;lc_run--019e6890-4544-7212-b4f2-aa20fc079911-0&quot;,
            tool_calls=[],
            invalid_tool_calls=[],
            usage_metadata={
                &quot;input_tokens&quot;: 10,
                &quot;output_tokens&quot;: 57,
                &quot;total_tokens&quot;: 67,
                &quot;input_token_details&quot;: {
                    &quot;cache_read&quot;: 0,
                },
                &quot;output_token_details&quot;: {},
            },
        ),
    ],
    &quot;username&quot;: &quot;小黄&quot;,
    &quot;output&quot;: &quot;你好呀，小黄！😊 我是DeepSeek，很高兴认识你！有什么我可以帮你的吗？无论是聊天、解答问题、帮你写作、编程，还是其他任何需要，尽管告诉我吧！你名字里的“黄”是哪个黄呀？😄&quot;,
}
</code></pre>
<p>上述案例中，<code>OverAllState</code> 继承了 <code>MessagesState</code>，因此可用的状态字段为</p>
<pre><code>messages
username
output
</code></pre>
<p>只关注 <code>messages</code> 状态，执行流程如下：</p>
<ol>
<li><code>node_a</code> 返回一条 <code>HumanMessage</code>；</li>
<li><code>LangGraph</code> 使用 <code>add_messages</code> 将该消息合并到 <code>messages</code> 状态字段中；</li>
<li><code>llm_node</code> 从 <code>state[&quot;messages&quot;]</code> 中读取完整消息列表，并调用模型；</li>
<li><code>llm_node</code> 将模型生成的 <code>AIMessage</code> 作为状态更新返回；</li>
<li><code>LangGraph</code> 再次通过 <code>add_messages</code> 将 <code>AIMessage</code> 合并到 <code>messages</code> 中；</li>
<li>最终状态中包含完整消息列表。</li>
</ol>
<p><code>MessagesState</code> 帮开发者预先定义好了 <code>messages</code> 字段及其合并规则。在构建聊天机器人、Agent、工具调用流程、多轮对话流程时，它可以提升开发效率。</p>
<h3 id="3-5-2-agentstate">3.5.2. AgentState</h3>
<p><code>AgentState</code> 是 <code>LangChain Agent</code> 内部使用的状态类型。由于 <code>LangChain Agent</code> 底层也是基于 <code>LangGraph</code> 运行图构建的，所以从技术上讲，开发者也可以将 <code>AgentState</code> 或其子类作为自定义 <code>LangGraph</code> 的状态类型。</p>
<p><code>AgentState</code> 的全类名，也可以称为类的完全限定名（英文全称： <code>fully qualified class name</code>）是：<code>langchain.agents.middleware.types.AgentState</code></p>
<p>源码如下</p>
<pre><code class="language-python">class AgentState(TypedDict, Generic[ResponseT]):
    &quot;&quot;&quot;State schema for the agent.&quot;&quot;&quot;

    messages: Required[Annotated[list[AnyMessage], add_messages]]
    jump_to: NotRequired[Annotated[JumpTo | None, EphemeralValue, PrivateStateAttr]]
    structured_response: NotRequired[Annotated[ResponseT, OmitFromInput]]
</code></pre>
<p>该状态中主要包含三个字段。</p>
<ol>
<li>messages</li>
</ol>
<pre><code>messages: Required[Annotated[list[AnyMessage], add_messages]]
</code></pre>
<p><code>messages</code> 用于存储 Agent 运行过程中的消息列表。</p>
<p>该字段和 <code>MessagesState</code> 中的 <code>messages</code> 字段类似，也使用 <code>add_messages</code> 作为 <code>Reducer</code>。</p>
<ol start="2">
<li>jump_to</li>
</ol>
<pre><code>jump_to: NotRequired[Annotated[JumpTo | None, EphemeralValue, PrivateStateAttr]]
</code></pre>
<p><code>jump_to</code> 是 <code>LangChain Agent</code> 内部使用的控制字段，主要服务于 Agent 中间件体系。</p>
<p>它通常用于表示运行流程的跳转意图，例如某些中间件希望影响 Agent 后续应该进入哪个节点。</p>
<p>需要注意的是，<code>jump_to</code> 并不是普通 LangGraph 状态图中的通用跳转机制。</p>
<p>在自定义 <code>StateGraph</code> 中，即使状态中定义了 <code>jump_to</code> 字段，<code>LangGraph</code> 也不会因为该字段的值自动跳转到某个节点。普通 <code>LangGraph</code> 运行图如果需要控制后续流向，通常应使用：</p>
<pre><code class="language-python">Command(goto=&quot;node_name&quot;)
</code></pre>
<p>见下文。</p>
<ol start="3">
<li>structured_response</li>
</ol>
<pre><code class="language-python">structured_response: NotRequired[Annotated[ResponseT, OmitFromInput]]
</code></pre>
<p><code>structured_response</code> 用于存储 Agent 最终生成的结构化输出。</p>
<p>当使用 <code>LangChain Agent</code> 的结构化输出能力时，例如指定 <code>response_format</code>，Agent 最终生成的结构化结果通常会被写入该字段。</p>
<p>其中，<code>OmitFromInput</code> 表示该字段不应作为外部输入字段暴露给调用方，而是由 Agent 运行过程中内部生成。</p>
<p>总体来看，<code>AgentState</code> 是专门为 LangChain Agent 运行时设计的状态类型。</p>
<p>因此，在普通自定义 <code>LangGraph</code> 项目中，一般不建议直接基于 <code>AgentState</code> 扩展图状态。</p>
]]></content>
  </entry>
</search>
